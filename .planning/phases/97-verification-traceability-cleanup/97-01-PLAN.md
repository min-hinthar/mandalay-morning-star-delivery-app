---
phase: 97-verification-traceability-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/89-critical-bug-fixes/89-VERIFICATION.md
  - .planning/phases/90-menu-photo-pipeline/90-VERIFICATION.md
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06
  - BUG-07
  - MENU-01
  - MENU-02
  - MENU-03
  - MENU-04
  - MENU-05
  - MENU-06
  - MENU-07
  - ADMIN-02

must_haves:
  truths:
    - "89-VERIFICATION.md exists with per-requirement evidence for all 7 BUG requirements"
    - "90-VERIFICATION.md exists with per-requirement evidence for all 8 MENU/ADMIN requirements"
    - "Each requirement has a VERIFIED/FAILED status with file:line evidence from current source code"
    - "Both files follow the 91-VERIFICATION.md template format exactly"
  artifacts:
    - path: ".planning/phases/89-critical-bug-fixes/89-VERIFICATION.md"
      provides: "Phase 89 verification evidence for BUG-01..07"
      contains: "score:"
    - path: ".planning/phases/90-menu-photo-pipeline/90-VERIFICATION.md"
      provides: "Phase 90 verification evidence for MENU-01..07, ADMIN-02"
      contains: "score:"
  key_links: []
---

<objective>
Create formal VERIFICATION.md files for Phase 89 (Critical Bug Fixes) and Phase 90 (Menu & Photo Pipeline) by verifying each requirement against the actual codebase and documenting per-requirement evidence.

Purpose: Close the 15 "partial" requirements from the v2.0 audit that lack formal verification documentation. Phase 89 and 90 were completed before the verification workflow was established.
Output: Two VERIFICATION.md files following the 91-VERIFICATION.md template pattern.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-verification-traceability-cleanup/97-RESEARCH.md
@.planning/phases/91-checkout-payment-hardening/91-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 89 VERIFICATION.md with per-requirement evidence</name>
  <files>.planning/phases/89-critical-bug-fixes/89-VERIFICATION.md</files>
  <action>
Create `.planning/phases/89-critical-bug-fixes/89-VERIFICATION.md` following the exact format of `91-VERIFICATION.md`.

**CRITICAL: Verify line numbers against CURRENT source code** -- do NOT copy line numbers from SUMMARY files or RESEARCH.md, as subsequent phases may have shifted line numbers. Read each source file and confirm evidence.

**Frontmatter:**
```yaml
---
phase: 89-critical-bug-fixes
verified: {current ISO-8601 timestamp}
status: passed (or failed if any requirement not met)
score: N/7 must-haves verified
re_verification: false
---
```

**Phase Goal:** "All known payment, checkout, and cart bugs are eliminated before building new features on top"

**Observable Truths table (7 rows):** Map each success criterion from ROADMAP.md lines 89-95 to file:line evidence:
1. BUG-01: Payment retries reuse deterministic idempotency key from order ID -- check `src/app/api/orders/[id]/retry-payment/route.ts` for `retry_${order.id}`
2. BUG-02: Checkout rejects modifier constraint violations -- check `src/lib/utils/order.ts` for ModifierGroupWithItems and validation logic, `src/app/api/checkout/session/route.ts` for fetch+validate
3. BUG-03: Failed checkout cleanup rolls back independently -- check `src/app/api/checkout/session/helpers.ts` for cleanupOrder with multiple try/catch
4. BUG-04: RPC checkout result handles null without type assertion crash -- check `src/app/api/checkout/session/route.ts` for typeof/Array.isArray guards
5. BUG-05: Refund rejects amount exceeding total_cents -- check `src/app/api/admin/orders/[id]/refund/route.ts` for ceiling validation
6. BUG-06: Concurrent cart addItem cannot bypass debounce -- check `src/lib/stores/cart-store.ts` for debounce inside set()
7. BUG-07: Orders within 10s of cutoff rejected -- check `src/lib/utils/delivery-dates.ts` for CUTOFF_SAFETY_BUFFER_MS

**Required Artifacts table:** List the 10 key files from Phase 89 (see RESEARCH.md artifact inventory), verify each still exists with expected content.

**Key Link Verification table:** Verify critical wiring:
- retry-payment/route.ts -> Stripe API (idempotency key passed)
- checkout/route.ts -> order.ts (modifier validation called)
- checkout/route.ts -> helpers.ts (cleanupOrder imported)
- refund/route.ts -> order total (ceiling comparison)
- cart-store.ts -> Zustand set() (debounce tracking inside set)
- delivery-dates.ts -> isPastCutoff (buffer subtracted)

**Requirements Coverage table:** 7 rows (BUG-01..07), each with source plans, description, SATISFIED/NOT_SATISFIED status, evidence summary.

**Anti-Patterns Found table:** Check for files over 400 lines, stub code, placeholder comments in Phase 89 artifacts.

**Human Verification Required section:** Note any items that need manual testing (e.g., payment retry flow needs real Stripe test environment).

**Gaps Summary:** Final assessment. If all 7 verified, state "No gaps found."

Per user decision: verify-only -- do NOT fix any code. If a requirement is not fully met, mark as FAILED with explanation.

Per user decision: MENU-03 says "WebP/AVIF" but only WebP is implemented -- mark VERIFIED with a note per research recommendation.

Per user decision: MENU-06 (inactive filtering) pre-existed Phase 90 -- mark VERIFIED with evidence of pre-existing code.

Per user decision: MENU-07 (seed script) -- mark VERIFIED for code existence, note in human verification that production seeding is a deployment step.
  </action>
  <verify>
    <automated>test -f ".planning/phases/89-critical-bug-fixes/89-VERIFICATION.md" && grep -c "VERIFIED\|SATISFIED" ".planning/phases/89-critical-bug-fixes/89-VERIFICATION.md"</automated>
  </verify>
  <done>89-VERIFICATION.md exists with YAML frontmatter, all 7 observable truths have VERIFIED/FAILED status with file:line evidence, required artifacts listed, key links verified, requirements coverage complete, anti-patterns checked, gaps summary present</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 90 VERIFICATION.md with per-requirement evidence</name>
  <files>.planning/phases/90-menu-photo-pipeline/90-VERIFICATION.md</files>
  <action>
Create `.planning/phases/90-menu-photo-pipeline/90-VERIFICATION.md` following the exact format of `91-VERIFICATION.md`.

**CRITICAL: Verify line numbers against CURRENT source code** -- do NOT copy line numbers from SUMMARY files or RESEARCH.md. Read each source file and confirm evidence.

**Frontmatter:**
```yaml
---
phase: 90-menu-photo-pipeline
verified: {current ISO-8601 timestamp}
status: passed (or failed if any requirement not met)
score: N/N must-haves verified
re_verification: false
---
```

**Phase Goal:** "Admin can manage all menu item photos from the dashboard, photos are production-quality, and all items have at least a fallback photo"

**Observable Truths table (6 rows from ROADMAP success criteria + implicit requirements):**
1. Admin can upload a photo for any menu item and see it reflected on customer menu -- check PhotoUploadZone.tsx, storage.ts uploadMenuPhotoViaServer
2. Admin can drag-drop multiple photos auto-matched by slug -- check BulkUploadMatcher.tsx, admin/photos/page.tsx
3. Uploaded photos auto-converted to WebP at 4:3 crop with size/dimension validation -- check photos/process/route.ts for sharp processing (note: WebP only, not AVIF -- mark with note)
4. Admin can mark items inactive so they disappear from customer menu -- check menu/route.ts and menu/search/route.ts for is_active filter (pre-existing code, verified not created)
5. Single authoritative allergen source (no tag/allergen overlap) -- check MenuItemFormFields.tsx ALLERGEN_OPTIONS, seed-menu.ts validateAllergens
6. All 53 items have at least a fallback photo seeded -- check scripts/seed-photos.ts, package.json seed:photos script
7. (Implicit) Menu items track photo freshness via image_updated_at -- check migration 033_photo_pipeline.sql or 001_schema.sql

**Required Artifacts table:** List the 14 key files from Phase 90 (see RESEARCH.md artifact inventory), verify each still exists.

**Key Link Verification table:**
- PhotoUploadZone.tsx -> storage.ts uploadMenuPhotoViaServer (upload flow)
- photos/process/route.ts -> sharp (WebP processing pipeline)
- BulkUploadMatcher.tsx -> admin/photos/page.tsx (integration)
- seed-photos.ts -> Supabase Storage (slug-based matching)
- MenuItemFormFields.tsx -> ALLERGEN_OPTIONS (single source)
- menu/route.ts -> is_active filter (customer-facing filtering)

**Requirements Coverage table:** 8 rows (MENU-01..07, ADMIN-02).

**Anti-Patterns Found table:** Check Phase 90 artifacts for over-limit files, stubs, etc.

**Human Verification Required section:**
- MENU-01/ADMIN-02: Photo upload visual flow needs manual test
- MENU-02: Bulk upload drag-drop slug matching needs manual test
- MENU-07: seed-photos.ts exists but production execution is a deployment step

**Gaps Summary:** Final assessment.

Per user decision: verify-only -- do NOT fix any code.
  </action>
  <verify>
    <automated>test -f ".planning/phases/90-menu-photo-pipeline/90-VERIFICATION.md" && grep -c "VERIFIED\|SATISFIED" ".planning/phases/90-menu-photo-pipeline/90-VERIFICATION.md"</automated>
  </verify>
  <done>90-VERIFICATION.md exists with YAML frontmatter, all observable truths have VERIFIED/FAILED status with file:line evidence, all 8 requirements (MENU-01..07, ADMIN-02) covered, required artifacts listed, key links verified, anti-patterns checked, human verification items noted, gaps summary present</done>
</task>

</tasks>

<verification>
- Both VERIFICATION.md files exist in their respective phase directories
- Both have valid YAML frontmatter with phase, verified, status, score fields
- Both follow the 91-VERIFICATION.md template (Observable Truths, Required Artifacts, Key Links, Requirements Coverage, Anti-Patterns, Human Verification, Gaps Summary)
- All 15 requirements appear across the two files (7 in Phase 89, 8 in Phase 90)
- Evidence references current file:line locations (not stale SUMMARY references)
</verification>

<success_criteria>
- 89-VERIFICATION.md covers BUG-01..07 with per-requirement evidence
- 90-VERIFICATION.md covers MENU-01..07 and ADMIN-02 with per-requirement evidence
- Both files match the 91-VERIFICATION.md format exactly
- Line number references verified against current source code
</success_criteria>

<output>
After completion, create `.planning/phases/97-verification-traceability-cleanup/97-01-SUMMARY.md`
</output>
