---
phase: 61-admin-pages
plan: 04
type: execute
wave: 2
depends_on: ["61-02"]
files_modified:
  - src/app/(admin)/admin/profile/page.tsx
  - src/app/(admin)/admin/profile/loading.tsx
  - src/components/ui/admin/profile/index.tsx
  - src/components/ui/admin/profile/AdminProfileClient.tsx
  - src/components/ui/admin/profile/ProfileInfoCard.tsx
  - src/components/ui/admin/profile/ActivityStatsCard.tsx
  - src/components/ui/admin/profile/NotificationPrefsCard.tsx
  - src/components/ui/admin/profile/ThemeCard.tsx
  - src/components/ui/admin/profile/types.ts
autonomous: true

must_haves:
  truths:
    - "Admin profile page renders at /admin/profile with editable name and phone"
    - "Profile shows role, auth provider, and member-since date"
    - "Activity stats show orders processed count and last login time"
    - "Notification preference toggles can be saved"
    - "Theme toggle switches between dark/light/system"
    - "Sign out button logs admin out and redirects"
    - "Save button shows loading state and success toast"
  artifacts:
    - path: "src/app/(admin)/admin/profile/page.tsx"
      provides: "Admin profile page route"
      contains: "AdminProfileClient"
    - path: "src/components/ui/admin/profile/AdminProfileClient.tsx"
      provides: "Main profile client component"
      contains: "Save Changes"
    - path: "src/components/ui/admin/profile/NotificationPrefsCard.tsx"
      provides: "Email notification toggles"
      contains: "notification"
  key_links:
    - from: "src/components/ui/admin/profile/AdminProfileClient.tsx"
      to: "/api/admin/profile"
      via: "fetch on mount + PATCH on save"
      pattern: "fetch.*admin/profile"
    - from: "src/components/ui/admin/profile/NotificationPrefsCard.tsx"
      to: "/api/admin/profile/notifications"
      via: "GET on mount + PUT on save"
      pattern: "fetch.*notifications"
---

<objective>
Build the admin profile page at `/admin/profile` with profile info, activity stats, notification preferences, theme toggle, and sign out.

Purpose: Admins need a self-service profile page to view their role/permissions, update contact info, set notification preferences, and manage their account.
Output: Admin profile page with 4 card sections + save/sign-out actions
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-admin-pages/61-RESEARCH.md
@.planning/phases/61-admin-pages/61-02-SUMMARY.md
@src/components/ui/admin/settings/ToggleSwitch.tsx
@src/components/ui/admin/settings/SaveButton.tsx
@src/components/ui/admin/settings/FloatingUnsavedBar.tsx
@src/components/ui/account/SettingsTab/ThemeSelector.tsx
@src/lib/supabase/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin profile page route and main client component</name>
  <files>
    src/app/(admin)/admin/profile/page.tsx
    src/app/(admin)/admin/profile/loading.tsx
    src/components/ui/admin/profile/index.tsx
    src/components/ui/admin/profile/AdminProfileClient.tsx
    src/components/ui/admin/profile/ProfileInfoCard.tsx
    src/components/ui/admin/profile/types.ts
  </files>
  <action>
**1. Route files:**

`page.tsx`: `'use client'` directive, import and render `<AdminProfileClient />`.

`loading.tsx`: Render `<RouteLoading message="Loading profile..." />`.

**2. Types** (`types.ts`):
```typescript
export interface AdminProfile {
  id: string;
  email: string;
  fullName: string | null;
  phone: string | null;
  role: string;
  authProvider: string;
  memberSince: string;
  createdAt: string;
}

export interface AdminStats {
  lastLoginAt: string | null;
  ordersProcessed: number;
}

export interface NotificationPrefs {
  orderConfirmation: boolean;
  orderCancellation: boolean;
  orderDelivered: boolean;
  newOrderAlert: boolean;
}
```

**3. AdminProfileClient** (`AdminProfileClient.tsx`):

Main client component:
- Fetch profile from `GET /api/admin/profile` on mount
- Manage form state: `fullName`, `phone` (editable)
- Track dirty state: compare current form values vs fetched values
- Card-based layout matching order detail design language (use CollapsibleCard from OrderDetailPage if importable, or create a simple card wrapper)
- Breadcrumb: "Dashboard" > "Profile"
- Render: ProfileInfoCard, ActivityStatsCard, NotificationPrefsCard, ThemeCard
- Explicit "Save Changes" button:
  - Disabled when not dirty or while saving
  - Loading spinner during save
  - Calls `PATCH /api/admin/profile` with `{ fullName, phone }`
  - On success: show toast `{ variant: "success", description: "Profile updated" }`, reset dirty state
  - On error: show toast `{ variant: "destructive", description: "Failed to update profile" }`
- Use `FloatingUnsavedBar` from `@/components/ui/admin/settings/FloatingUnsavedBar` for dirty form state indicator if it works outside settings page context. Otherwise, inline the save button.
- Sign out button at bottom:
  - Use `signOut` server action from `@/lib/supabase/actions`
  - `<form action={signOut}>` with submit button
  - Red outline style with LogOut icon

**4. ProfileInfoCard** (`ProfileInfoCard.tsx`):

Card showing profile info:
- Full name (editable input field)
- Email (read-only, displayed as text)
- Phone (editable input field)
- Role badge (read-only): "Admin" or "Super Admin" etc. using Badge component
- Permissions list (read-only): based on role, show what admin can do (e.g., "Manage orders", "Manage menu", "Manage drivers"). Hardcode a permissions map by role.
- Auth provider: "Signed in with Google" or "Signed in with Email" with provider icon
- Member since: format date with date-fns `format(parseISO(memberSince), "MMMM yyyy")` → "January 2026"

Props: profile data + onChange handlers for editable fields. The parent (AdminProfileClient) manages the form state.
  </action>
  <verify>
- `pnpm typecheck` passes
- Profile page renders at /admin/profile
- Profile info displays with editable name/phone
  </verify>
  <done>
- Admin profile page renders at /admin/profile
- Profile shows role, auth provider, member since
- Name and phone are editable
- Save button updates profile and shows success toast
- Sign out button works
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ActivityStatsCard, NotificationPrefsCard, and ThemeCard</name>
  <files>
    src/components/ui/admin/profile/ActivityStatsCard.tsx
    src/components/ui/admin/profile/NotificationPrefsCard.tsx
    src/components/ui/admin/profile/ThemeCard.tsx
  </files>
  <action>
**1. ActivityStatsCard** (`ActivityStatsCard.tsx`):

Card showing admin activity metrics:
- Fetch from `GET /api/admin/profile/stats` on mount (independent fetch from profile)
- Display:
  - Last login time: formatted with date-fns `formatDistanceToNow(parseISO(...), { addSuffix: true })` → "2 hours ago"
  - Orders processed count: large number display, clickable — wraps in `<Link href="/admin/orders">` per locked decision (links to order list, which could be filtered by admin in future)
- Loading state: skeleton while fetching
- Error state: "Unable to load stats" with retry button

**2. NotificationPrefsCard** (`NotificationPrefsCard.tsx`):

Card with email notification toggles:
- Fetch from `GET /api/admin/profile/notifications` on mount
- Toggle switches for each notification type using existing `ToggleSwitch` component from `@/components/ui/admin/settings/ToggleSwitch`
- Notification types per locked decisions (email-only for now):
  - Order confirmed notifications
  - Order cancellation notifications
  - Order delivered notifications
  - New order alert notifications
- Each toggle updates local state immediately
- Explicit "Save Preferences" button (or integrate into parent's save flow)
- On save: `PUT /api/admin/profile/notifications` with preferences object
- Success toast on save
- This card manages its own save state (separate from profile save) because notification prefs go to a different API endpoint

**3. ThemeCard** (`ThemeCard.tsx`):

Card wrapping the existing ThemeSelector:
- Import `ThemeSelector` from `@/components/ui/account/SettingsTab/ThemeSelector` (or wherever it's defined — search codebase if path differs)
- Wrap in a simple card with title "Appearance"
- ThemeSelector already handles dark/light/system toggle with `useTheme()` from next-themes
- No save button needed — theme changes apply immediately (next-themes persists to localStorage)
- `'use client'` directive required

**4. Update barrel** (`index.tsx`):
Re-export `{ AdminProfileClient }` from the barrel.
  </action>
  <verify>
- `pnpm typecheck` passes
- All card components compile
- ThemeSelector import path resolves
- ToggleSwitch import path resolves
  </verify>
  <done>
- Activity stats card shows last login and orders processed count
- Orders processed count links to /admin/orders
- Notification toggles render and save preferences
- Theme toggle switches between dark/light/system
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Admin profile page renders all 4 card sections
- Save updates profile, notification prefs save independently
- Theme toggle works immediately
- Sign out redirects to login
</verification>

<success_criteria>
- Admin profile page at /admin/profile with all required sections
- Profile editing with save button and success feedback
- Notification preferences toggle and save
- Theme toggle functional
- Sign out works
</success_criteria>

<output>
After completion, create `.planning/phases/61-admin-pages/61-04-SUMMARY.md`
</output>
