---
phase: 57-admin-driver-polish
plan: 04
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/app/(admin)/admin/routes/page.tsx
  - src/components/ui/admin/routes/RouteListTable/RouteListTable.tsx
  - src/components/ui/admin/routes/RouteListTable/RouteMobileCard.tsx
  - src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx
  - src/components/ui/admin/routes/RouteListTable/RoutesPageSkeleton.tsx
  - src/components/ui/admin/routes/RouteDetailDrawer.tsx
autonomous: true

must_haves:
  truths:
    - "Routes page uses AdminPageHeader with animated count badge and teal accent"
    - "Routes table uses card rows with progress indicators, staggered entry, hover micro-interactions"
    - "Routes loading state uses shimmer Skeleton with crossfade (no animate-pulse)"
    - "Routes empty state shows truck/map food-themed emoji with 'Create Route' CTA"
    - "Clicking a route row opens a slide-in drawer with route summary"
    - "Route cards show delivery progress (stops completed/total) with visual indicator"
  artifacts:
    - path: "src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx"
      provides: "Card-based route row with progress indicator"
    - path: "src/components/ui/admin/routes/RouteListTable/RoutesPageSkeleton.tsx"
      provides: "Shimmer skeleton matching routes page layout"
    - path: "src/components/ui/admin/routes/RouteDetailDrawer.tsx"
      provides: "Slide-in drawer for route quick view"
  key_links:
    - from: "src/app/(admin)/admin/routes/page.tsx"
      to: "src/components/ui/admin/AdminPageHeader.tsx"
      via: "AdminPageHeader import"
      pattern: "AdminPageHeader"
    - from: "src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx"
      to: "src/components/ui/admin/CardRow.tsx"
      via: "CardRow base component"
      pattern: "CardRow"
---

<objective>
Transform admin routes page from traditional table to premium card-based layout with progress indicators, shimmer skeleton, empty state, and slide-in drawer. Covers POLH-01, POLH-02, POLH-03, POLH-04, POLH-09.

Purpose: Routes page completes the admin table trio (orders, drivers, routes) with consistent premium card treatment.
Output: Card-based routes table with drawer, skeleton, empty state, progress indicators
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/app/(admin)/admin/routes/page.tsx
@src/components/ui/admin/routes/RouteListTable/RouteListTable.tsx
@src/components/ui/admin/routes/RouteListTable/RouteMobileCard.tsx
@src/components/ui/admin/routes/RouteListTable/types.tsx
@src/components/ui/admin/routes/RouteStatsBar.tsx
@src/components/ui/admin/CardRow.tsx
@src/components/ui/admin/StatusBadge.tsx
@src/components/ui/admin/AdminPageHeader.tsx
@src/components/ui/admin/SkeletonCrossfade.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RouteCardRow + RoutesPageSkeleton + RouteDetailDrawer</name>
  <files>
    src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx
    src/components/ui/admin/routes/RouteListTable/RoutesPageSkeleton.tsx
    src/components/ui/admin/routes/RouteDetailDrawer.tsx
  </files>
  <action>
**RouteCardRow:**
- Create `RouteCardRow.tsx` with `'use client'`
- Props: match existing route type from RouteListTable types.tsx + `selected?: boolean`, `onClick?: () => void`
- Uses `CardRow` base component
- Layout (desktop): flex row with: route date, route name/label, driver name (with small avatar), stops progress (e.g. "5/8 delivered" with a thin progress bar beneath -- `div` with `bg-accent-teal` width percentage), status badge (StatusBadge), estimated vs actual duration, always-visible action buttons ("View", "Optimize")
- Progress bar: thin (h-1) rounded-full bar with `bg-surface-tertiary` track and `bg-accent-teal` fill, animated width via m.div with spring transition
- Layout (mobile): stacked card with route date + driver name + progress + status
- Status tint: active/in_progress routes get `bg-blue-50/50`, completed `bg-green-50/50`, planned `bg-gray-50/50`
- Date navigation: sticky date section headers per CONTEXT ("Today", "Yesterday", date string)

**RoutesPageSkeleton:**
- Match layout: 4 stat card skeletons (grid-cols-4), date section header skeleton, 5 card row skeletons (h=80)
- Uses Skeleton shimmer variant

**RouteDetailDrawer:**
- Same slide-in pattern as OrderDetailDrawer
- Content: route header (date, name, status), driver assignment (avatar + name), progress summary (X/Y stops), mini stop list (stop name + status icon for each), estimated vs actual time, action buttons (Optimize, Edit, Delete)
- "View Full Details" link button navigating to `/admin/routes/[id]`
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - All 3 new files exist
  </verify>
  <done>RouteCardRow renders premium card with progress bar. RoutesPageSkeleton uses shimmer. RouteDetailDrawer slides in with route summary.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routes page with card rows, skeleton crossfade, empty state, drawer</name>
  <files>
    src/app/(admin)/admin/routes/page.tsx
    src/components/ui/admin/routes/RouteListTable/RouteListTable.tsx
    src/components/ui/admin/routes/RouteListTable/RouteMobileCard.tsx
  </files>
  <action>
**Routes page.tsx:**
- Replace loading block (animate-pulse at lines 175-190) with `SkeletonCrossfade` + `RoutesPageSkeleton`
- Replace page title with `AdminPageHeader`: `title="Routes"`, `count={routes.length}`, actions slot for Create Route button + date navigation + filter controls
- Breadcrumbs: `[{ label: 'Dashboard', href: '/admin' }, { label: 'Routes' }]`
- Error state: `InlineErrorCard` with onRetry
- Migrate colors: replace `saffron`/`curry`/`primary` accent with `accent-teal`
- RouteStatsBar: ensure it uses AnimatedValue for stat numbers

**RouteListTable.tsx:**
- Replace Table/ExpandableTableRow with card row layout:
  - Container: `m.div variants={cardContainer}` for 40ms stagger
  - Each row: `RouteCardRow` component
  - Sticky column header (hidden mobile)
- Sort animation: `layout` prop on rows
- Selected row: track `selectedRouteId`, pass `selected` prop
- Row click: open RouteDetailDrawer
- Empty state: `EmptyState variant="admin-routes"` for truly empty, `EmptyState variant="admin-routes-filtered"` for filtered
- Date section headers: group routes by date with sticky headers
- "Load more" pagination: initial 20 routes, Load More button

**RouteMobileCard.tsx:**
- Update to use CardRow base for consistent mobile card behavior
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint && pnpm lint:css` passes
    - Grep routes/page.tsx for `animate-pulse` returns 0
    - Grep RouteListTable.tsx for `EmptyState` confirms usage
    - Grep routes/page.tsx for `AdminPageHeader` confirms usage
    - Grep routes/page.tsx for `saffron\|curry` returns 0 (colors migrated)
  </verify>
  <done>Routes page uses shimmer skeleton crossfade, AdminPageHeader with teal count badge, card-based rows with progress indicators/stagger/hover, drawer detail, empty states, date grouping. No animate-pulse. Teal accent throughout.</done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- Routes page renders card rows with progress bars
- Loading shows shimmer skeleton crossfade
- Empty state shows truck/map emoji with Create Route CTA
- Clicking row opens slide-in drawer
- No animate-pulse in routes page
- Teal accent color (no saffron/curry)
</verification>

<success_criteria>
Admin routes page has premium card-based layout matching CONTEXT.md: card rows with progress, hover, status tint, stagger, drawer detail, skeleton crossfade, empty states, date grouping. POLH-09 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-04-SUMMARY.md`
</output>
