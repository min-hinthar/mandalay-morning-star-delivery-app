---
phase: 57-admin-driver-polish
plan: 07
type: execute
wave: 3
depends_on: ["57-01"]
files_modified:
  - src/app/(driver)/driver/route/page.tsx
  - src/app/(driver)/driver/route/[stopId]/page.tsx
  - src/components/ui/driver/ActiveRouteView.tsx
  - src/components/ui/driver/StopDetailView.tsx
  - src/components/ui/driver/StopCard.tsx
  - src/components/ui/driver/StopList.tsx
autonomous: true

must_haves:
  truths:
    - "Driver route page loading state uses shimmer skeleton with crossfade (no animate-pulse)"
    - "Driver stop detail page loading state uses shimmer skeleton with crossfade (no animate-pulse)"
    - "Driver stop detail has premium animations: status transition, timeline step sequence"
    - "Stop cards have status-based left border coloring and micro-interactions"
    - "Driver route page shows progress indicator for stops completed"
  artifacts:
    - path: "src/components/ui/driver/StopDetailView.tsx"
      provides: "Premium animated stop detail with status transitions and timeline"
    - path: "src/components/ui/driver/ActiveRouteView.tsx"
      provides: "Active route view with progress and animated stop cards"
  key_links:
    - from: "src/app/(driver)/driver/route/page.tsx"
      to: "src/components/ui/driver/ActiveRouteView.tsx"
      via: "ActiveRouteView component"
      pattern: "ActiveRouteView"
    - from: "src/app/(driver)/driver/route/[stopId]/page.tsx"
      to: "src/components/ui/driver/StopDetailView.tsx"
      via: "StopDetailView component"
      pattern: "StopDetailView"
---

<objective>
Polish driver route and stop detail pages with premium animations: shimmer skeletons, status transitions, timeline step sequences, progress indicators. Covers POLH-06.

Purpose: Driver stop detail is where drivers spend most of their time. Premium animations for status transitions, map marker pulse, and timeline sequence make the operational experience feel as polished as the customer app.
Output: Premium driver route and stop detail pages
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/app/(driver)/driver/route/page.tsx
@src/app/(driver)/driver/route/[stopId]/page.tsx
@src/components/ui/driver/ActiveRouteView.tsx
@src/components/ui/driver/StopDetailView.tsx
@src/components/ui/driver/StopCard.tsx
@src/components/ui/driver/StopList.tsx
@src/components/ui/driver/DeliveryActions.tsx
@src/components/ui/admin/SkeletonCrossfade.tsx
@src/components/ui/skeleton/base.tsx
@src/lib/motion-tokens/stagger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver route page skeleton + ActiveRouteView progress + StopCard polish</name>
  <files>
    src/app/(driver)/driver/route/page.tsx
    src/components/ui/driver/ActiveRouteView.tsx
    src/components/ui/driver/StopCard.tsx
    src/components/ui/driver/StopList.tsx
  </files>
  <action>
**Route page.tsx:**
- Replace animate-pulse loading block (lines 152-180 per research) with SkeletonCrossfade + ActiveRouteSkeleton
- ActiveRouteSkeleton (inline): progress bar skeleton (Skeleton w=full h=6 radius-full), route info skeleton (Skeleton w=full h=60 radius-xl), 4 stop card skeletons (Skeleton w=full h=72 radius-xl)
- Error state: InlineErrorCard with onRetry
- Apply teal accent color

**ActiveRouteView.tsx:**
- Add route progress indicator at top:
  - Thin progress bar (h-2 rounded-full) showing stops completed vs total
  - `m.div` fill bar with `bg-accent-teal` animated width via spring transition
  - Text label: "3 of 8 stops completed" below bar
  - Use AnimatedValue for the completed count
- Add staggered entry animation for the stops list section
- Replace any `primary` (red) accent with `accent-teal`

**StopCard.tsx:**
- Add status-based left border: 4px left border with status color (green=delivered, blue=in transit, amber=pending, red=failed, gray=skipped)
- Add `m.div` with `whileHover={{ scale: 1.01 }}` and `whileTap={{ scale: 0.98 }}` for card interaction
- Entry animation via stagger: `m.div variants={staggerItem}` with `initial="hidden" animate="visible"`
- Status badge: use StatusBadge component or inline badge with pulse for active statuses
- Card styling: rounded-xl bg-surface-primary shadow-sm p-4 border border-border
- Show ETA or delivery time in a prominent position

**StopList.tsx:**
- Wrap in `m.div variants={cardContainer}` for staggered entry (40ms per card)
- Empty state: if no stops, show EmptyState variant="driver-route"
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Grep route/page.tsx for `animate-pulse` returns 0
    - Grep ActiveRouteView.tsx for `accent-teal` confirms teal accent
    - Grep StopCard.tsx for `whileHover` confirms hover interaction
  </verify>
  <done>Driver route page uses shimmer skeleton crossfade. ActiveRouteView has progress indicator. StopCards have status-colored left borders, hover interactions, and staggered entry. Teal accent throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Driver stop detail premium animations</name>
  <files>
    src/app/(driver)/driver/route/[stopId]/page.tsx
    src/components/ui/driver/StopDetailView.tsx
  </files>
  <action>
**Stop detail page.tsx:**
- Replace animate-pulse loading block (lines 148-182 per research) with SkeletonCrossfade + StopDetailSkeleton
- StopDetailSkeleton (inline): header skeleton (Skeleton w=full h=80 radius-xl), map skeleton (Skeleton w=full h=200 radius-xl), info sections (3x Skeleton w=full h=60 radius-xl), action buttons skeleton (Skeleton w=full h=48 radius-xl)
- Error state: InlineErrorCard with onRetry

**StopDetailView.tsx:**
- Status transition animation: when stop status changes (e.g., pending -> in_transit -> arrived -> delivered), animate the status section:
  - Status badge scales up briefly (1.2x) then settles to 1x with spring
  - Previous status fades out, new status fades in using AnimatePresence mode="wait"
  - Use key={stop.status} on the status section wrapper to trigger re-animation
- Map marker pulse: if the page shows a map, add a pulsing ring animation around the stop marker position
  - Implemented as a CSS animation: expanding circle from marker center that fades out (`ring-pulse` keyframe: scale 1->2, opacity 1->0, over 1.5s, repeat 3x)
  - Only pulses for active stops (pending, in_transit, arrived)
- Timeline step sequence: show a mini-timeline at top of detail page showing the stop's journey through statuses
  - Horizontal row of status dots (small circles connected by lines)
  - Completed steps: filled with green, check icon
  - Current step: larger, pulsing (accent-teal)
  - Future steps: gray, unfilled
  - Step transition animation: when status advances, new dot fills in with scale animation (0->1), connecting line extends with width animation
- Premium section animations:
  - Customer info section: m.div with initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} with delay 0.1s
  - Address section: same with delay 0.2s
  - Notes section: same with delay 0.3s
  - Actions section: same with delay 0.4s (cascading reveal)
- Replace accent colors with teal
- Ensure all animations respect useAnimationPreference (check isFullMotion before applying)
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint && pnpm lint:css` passes
    - Grep [stopId]/page.tsx for `animate-pulse` returns 0
    - Grep StopDetailView.tsx for `AnimatePresence` confirms status transition animation
    - Grep StopDetailView.tsx for `ring-pulse\|pulsing\|pulse` confirms marker pulse
  </verify>
  <done>Driver stop detail has premium animations: status transitions with scale/fade, map marker pulse for active stops, horizontal timeline step sequence, cascading section reveals. All animations respect reduced motion preference. Shimmer skeleton loading. No animate-pulse.</done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- Driver route page shows progress bar and shimmer skeleton
- Stop cards have status-colored left borders and hover interactions
- Stop detail shows status transition animation when status changes
- Stop detail has horizontal timeline step sequence
- Map marker pulses for active stops
- No animate-pulse on any driver route/stop page
- Teal accent throughout
- Animations respect prefers-reduced-motion
</verification>

<success_criteria>
Driver route and stop detail pages match dashboard quality with premium animations. POLH-06 (driver stop detail premium animations) complete. All driver pages use shimmer skeleton, status transitions animate, timeline step sequence shows delivery progress.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-07-SUMMARY.md`
</output>
