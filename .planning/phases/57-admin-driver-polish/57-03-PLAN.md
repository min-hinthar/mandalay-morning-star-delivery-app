---
phase: 57-admin-driver-polish
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/app/(admin)/admin/drivers/page.tsx
  - src/components/ui/admin/drivers/DriverListTable/DriverListTable.tsx
  - src/components/ui/admin/drivers/DriverListTable/DriverMobileCard.tsx
  - src/components/ui/admin/drivers/DriverListTable/DriverCardRow.tsx
  - src/components/ui/admin/drivers/DriverListTable/DriversPageSkeleton.tsx
  - src/components/ui/admin/drivers/DriverDetailDrawer.tsx
autonomous: true

must_haves:
  truths:
    - "Drivers page uses AdminPageHeader with animated count badge and teal accent"
    - "Drivers table uses card rows with avatars, status indicators, staggered entry, hover micro-interactions"
    - "Drivers loading state uses shimmer Skeleton with crossfade (no animate-pulse)"
    - "Drivers empty state shows food-themed emoji illustration with 'Add Driver' CTA"
    - "Clicking a driver row opens a slide-in drawer overlay with driver summary"
    - "Active driver status badges pulse; inactive badges are static"
  artifacts:
    - path: "src/components/ui/admin/drivers/DriverListTable/DriverCardRow.tsx"
      provides: "Card-based driver row with avatar, status, stats"
    - path: "src/components/ui/admin/drivers/DriverListTable/DriversPageSkeleton.tsx"
      provides: "Shimmer skeleton matching drivers page layout"
    - path: "src/components/ui/admin/drivers/DriverDetailDrawer.tsx"
      provides: "Slide-in drawer for driver quick view"
  key_links:
    - from: "src/app/(admin)/admin/drivers/page.tsx"
      to: "src/components/ui/admin/AdminPageHeader.tsx"
      via: "AdminPageHeader import"
      pattern: "AdminPageHeader"
    - from: "src/components/ui/admin/drivers/DriverListTable/DriverCardRow.tsx"
      to: "src/components/ui/admin/CardRow.tsx"
      via: "CardRow base component"
      pattern: "CardRow"
---

<objective>
Transform admin drivers page from traditional table to premium card-based layout with avatars, status indicators, shimmer skeleton, empty state, and slide-in drawer. Covers POLH-01, POLH-02, POLH-03, POLH-04, POLH-07.

Purpose: Drivers page gets the same premium treatment as orders -- card rows with hover, avatars, animated status, drawer detail.
Output: Card-based drivers table with drawer, skeleton, empty state
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/app/(admin)/admin/drivers/page.tsx
@src/components/ui/admin/drivers/DriverListTable/DriverListTable.tsx
@src/components/ui/admin/drivers/DriverListTable/DriverMobileCard.tsx
@src/components/ui/admin/drivers/DriverListTable/types.tsx
@src/components/ui/admin/drivers/DriverStatsCards.tsx
@src/components/ui/admin/CardRow.tsx
@src/components/ui/admin/StatusBadge.tsx
@src/components/ui/admin/AdminPageHeader.tsx
@src/components/ui/admin/SkeletonCrossfade.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DriverCardRow + DriversPageSkeleton + DriverDetailDrawer</name>
  <files>
    src/components/ui/admin/drivers/DriverListTable/DriverCardRow.tsx
    src/components/ui/admin/drivers/DriverListTable/DriversPageSkeleton.tsx
    src/components/ui/admin/drivers/DriverDetailDrawer.tsx
  </files>
  <action>
**DriverCardRow:**
- Create `DriverCardRow.tsx` with `'use client'`
- Props: match existing driver type from DriverListTable types.tsx + `selected?: boolean`, `onClick?: () => void`
- Uses `CardRow` base component
- Layout (desktop): flex row with: avatar circle (40px, initials if no image, bg-accent-teal/10 text-accent-teal), driver name + phone, status badge (StatusBadge), total deliveries count, avg rating stars, vehicle info, always-visible action buttons ("View", "Edit")
- Layout (mobile): stacked card with avatar + name + status badge on first line, stats on second line
- Status tint: active drivers get `bg-green-50/50`, inactive `bg-gray-50/50`

**DriversPageSkeleton:**

- Match layout: 4 stat card skeletons across top (Skeleton w=full h=24 radius-xl in 4-column grid), 5 card row skeletons below (Skeleton w=full h=80 radius-xl each)
- Uses Skeleton shimmer variant

**DriverDetailDrawer:**

- Same pattern as OrderDetailDrawer: slide-in from right, dark backdrop, overlay.drawer variant
- Content: driver avatar (large, centered), name, phone, email, status badge, vehicle info, recent stats (deliveries, avg time, rating), quick actions (Edit Profile, Archive, View Detail)
- "View Full Profile" link button at bottom navigating to `/admin/drivers/[id]`
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint` passes - All 3 new files exist
  </verify>
  <done>DriverCardRow renders premium card with avatar and status. DriversPageSkeleton uses shimmer. DriverDetailDrawer slides in with driver summary.</done>
  </task>

<task type="auto">
  <name>Task 2: Wire drivers page with card rows, skeleton crossfade, empty state, drawer</name>
  <files>
    src/app/(admin)/admin/drivers/page.tsx
    src/components/ui/admin/drivers/DriverListTable/DriverListTable.tsx
    src/components/ui/admin/drivers/DriverListTable/DriverMobileCard.tsx
  </files>
  <action>
**Drivers page.tsx:**
- Replace loading block (animate-pulse at lines 174-189) with `SkeletonCrossfade` + `DriversPageSkeleton`
- Replace page title with `AdminPageHeader`: `title="Drivers"`, `count={drivers.length}`, actions slot for Add Driver button + search/filter controls
- Add breadcrumbs: `[{ label: 'Dashboard', href: '/admin' }, { label: 'Drivers' }]`
- Error state: `InlineErrorCard` with onRetry
- Migrate colors: replace any `brand-red`/`primary` accent with `accent-teal` for non-destructive elements
- DriverStatsCards section: ensure it uses AnimatedValue for counting numbers

**DriverListTable.tsx:**

- Replace `<Table>` / ExpandableTableRow with card row layout:
  - Container: `m.div variants={cardContainer}` for 40ms stagger
  - Each row: `DriverCardRow` component
  - Sticky column header row above cards (hidden on mobile)
- Sort animation: `layout` prop on each row for animated reorder
- Selected row: track `selectedDriverId`, pass `selected` prop
- Row click: open DriverDetailDrawer
- Empty state: `EmptyState variant="admin-drivers"` for truly empty, `EmptyState variant="admin-drivers-filtered"` for filtered empty

**DriverMobileCard.tsx:**

- Update to use CardRow base for consistent hover/selection behavior
- Ensure tap-to-expand works for mobile (replaces desktop drawer with inline expansion on mobile)
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint && pnpm lint:css` passes - Grep drivers/page.tsx for `animate-pulse` returns 0 - Grep DriverListTable.tsx for `EmptyState` confirms usage - Grep drivers/page.tsx for `AdminPageHeader` confirms usage
  </verify>
  <done>Drivers page uses shimmer skeleton crossfade, AdminPageHeader with teal count badge, card-based rows with avatars/status/stagger/hover, drawer detail, empty states. No animate-pulse. Teal accent throughout.</done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- Drivers page renders card rows with avatars
- Loading shows shimmer skeleton crossfade
- Empty state shows food emoji with Add Driver CTA
- Clicking row opens slide-in drawer
- Status badges pulse for active drivers
- No animate-pulse in drivers page
- Teal accent color
</verification>

<success_criteria>
Admin drivers page has premium card-based layout matching CONTEXT.md: card rows with avatars, hover, status tint, stagger, animated badges, drawer detail, skeleton crossfade, empty states. POLH-07 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-03-SUMMARY.md`
</output>
