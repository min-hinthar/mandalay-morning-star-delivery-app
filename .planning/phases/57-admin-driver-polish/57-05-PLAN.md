---
phase: 57-admin-driver-polish
plan: 05
type: execute
wave: 3
depends_on: ["57-01"]
files_modified:
  - src/components/ui/admin/AdminDashboard/AdminDashboard.tsx
  - src/components/ui/admin/AdminDashboard/KPICard.tsx
  - src/components/ui/admin/AdminDashboard/QuickStat.tsx
  - src/components/ui/admin/drivers/DriverDetailClient/DriverDetailClient.tsx
  - src/components/ui/admin/drivers/DriverDetailClient/PerformanceSection.tsx
  - src/components/ui/admin/drivers/DriverStatsCards.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteTimeline.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
  - src/components/ui/admin/routes/RouteStopCard.tsx
autonomous: true

must_haves:
  truths:
    - "Admin dashboard stat cards use teal gradients and animated counters that link to detail pages"
    - "Admin driver detail page has performance dashboard section with 4 animated stat cards"
    - "Route detail page shows vertical timeline with connected dots, status icons, and time between stops"
    - "Route detail shows estimated vs actual time comparison with green/red visual diff"
    - "Exception display shows alert card at page top for unresolved exceptions"
    - "Driver detail and route detail loading states use shimmer skeleton (no animate-pulse)"
  artifacts:
    - path: "src/components/ui/admin/drivers/DriverDetailClient/PerformanceSection.tsx"
      provides: "4-stat performance dashboard for driver detail page"
    - path: "src/components/ui/admin/routes/RouteDetailClient/RouteTimeline.tsx"
      provides: "Vertical timeline with connected dots and time indicators"
  key_links:
    - from: "src/components/ui/admin/AdminDashboard/KPICard.tsx"
      to: "src/components/ui/admin/AdminDashboard/AnimatedValue.tsx"
      via: "AnimatedValue for counting animation"
      pattern: "AnimatedValue"
    - from: "src/components/ui/admin/drivers/DriverDetailClient/PerformanceSection.tsx"
      to: "src/components/ui/admin/AdminDashboard/AnimatedValue.tsx"
      via: "AnimatedValue for stat counters"
      pattern: "AnimatedValue"
---

<objective>
Polish admin dashboard stat cards, driver detail page (performance section), and route detail page (timeline, time comparison, exception alerts). Covers POLH-08, POLH-10, POLH-11, and admin dashboard stats from CONTEXT.

Purpose: Detail pages and dashboard are the premium showcase. Stat cards with teal gradients, driver performance dashboard, and route timeline deliver the highest visual impact.
Output: Premium admin dashboard stats, driver detail performance section, route detail timeline
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/components/ui/admin/AdminDashboard/AdminDashboard.tsx
@src/components/ui/admin/AdminDashboard/KPICard.tsx
@src/components/ui/admin/AdminDashboard/QuickStat.tsx
@src/components/ui/admin/AdminDashboard/AnimatedValue.tsx
@src/components/ui/admin/drivers/DriverDetailClient/DriverDetailClient.tsx
@src/components/ui/admin/drivers/DriverDetailClient/types.tsx
@src/components/ui/admin/drivers/DriverStatsCards.tsx
@src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
@src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
@src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx
@src/components/ui/admin/routes/RouteStopCard.tsx
@src/components/ui/admin/routes/StopsList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin dashboard stats polish + driver detail performance section</name>
  <files>
    src/components/ui/admin/AdminDashboard/AdminDashboard.tsx
    src/components/ui/admin/AdminDashboard/KPICard.tsx
    src/components/ui/admin/AdminDashboard/QuickStat.tsx
    src/components/ui/admin/drivers/DriverDetailClient/DriverDetailClient.tsx
    src/components/ui/admin/drivers/DriverDetailClient/PerformanceSection.tsx
    src/components/ui/admin/drivers/DriverStatsCards.tsx
  </files>
  <action>
**AdminDashboard KPICard polish:**
- Add subtle teal gradient background to stat cards: `bg-gradient-to-br from-accent-teal/5 to-accent-teal/10` (or similar subtle gradient)
- Ensure each KPICard already uses AnimatedValue for counting animation (if not, add it)
- Make each stat card a clickable Link navigating to its relevant page: orders count -> /admin/orders, revenue -> /admin/orders, active drivers -> /admin/drivers, on-time rate -> /admin/routes, pending -> /admin/orders?status=pending, exceptions -> /admin/routes
- Add `whileHover={{ scale: 1.02, y: -2 }}` on the card wrapper for hover lift
- Replace any `primary`/`brand-red` accent colors with `accent-teal`

**QuickStat polish:**
- If QuickStat uses plain numbers, wrap in AnimatedValue
- Add subtle teal accent on the icon or border

**AdminDashboard.tsx:**
- Ensure the dashboard metrics include: orders, revenue, active drivers, on-time rate, pending, exceptions (per CONTEXT all key metrics)
- If any metrics are missing, add them using existing data sources
- Replace any non-teal accent colors

**DriverDetailClient.tsx:**
- Replace animate-pulse loading block (lines 175-198 per research) with SkeletonCrossfade + driver detail skeleton (Skeleton components matching layout: profile header, stats grid, recent routes, recent ratings)
- Add breadcrumbs via AdminPageHeader at top: `[{ label: 'Dashboard', href: '/admin' }, { label: 'Drivers', href: '/admin/drivers' }, { label: driverName }]`
- Replace accent colors with teal

**PerformanceSection (new):**
- Create `PerformanceSection.tsx` with `'use client'`
- Props: `driver` object (typed from DriverDetailClient types)
- Renders 4 stat cards in a 2x2 or 4-column grid:
  1. Total Deliveries: AnimatedValue format="number", icon=Package
  2. Avg Delivery Time: AnimatedValue format="duration", icon=Clock
  3. On-Time Rate: AnimatedValue format="percentage", icon=CheckCircle, with circular progress ring (SVG circle with stroke-dasharray animated via m.circle)
  4. Exceptions: AnimatedValue format="number", icon=AlertTriangle
- Each card: rounded-xl bg-gradient-to-br from-accent-teal/5 to-accent-teal/10, p-4, icon in accent-teal circle
- On-time ring: SVG 48px circle, stroke=accent-teal, stroke-dasharray computed from percentage, animated from 0 to final value

**DriverStatsCards.tsx:**
- Ensure stats use AnimatedValue counting animation
- Apply teal gradient styling consistent with KPICard
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Grep KPICard.tsx for `accent-teal` confirms teal usage
    - Grep DriverDetailClient.tsx for `animate-pulse` returns 0
    - PerformanceSection.tsx exists and exports PerformanceSection
    - Grep PerformanceSection.tsx for `AnimatedValue` confirms counting
  </verify>
  <done>Admin dashboard stats have teal gradients, counting animations, and link to detail pages. Driver detail has performance section with 4 animated stat cards including on-time circular progress ring. No animate-pulse in driver detail.</done>
</task>

<task type="auto">
  <name>Task 2: Route detail timeline + time comparison + exception display</name>
  <files>
    src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
    src/components/ui/admin/routes/RouteDetailClient/RouteTimeline.tsx
    src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
    src/components/ui/admin/routes/RouteStopCard.tsx
  </files>
  <action>
**RouteTimeline (new):**
- Create `RouteTimeline.tsx` with `'use client'`
- Props: `stops: RouteStop[]` (typed from RouteDetailClient types)
- Renders vertical timeline with:
  - Left column: connected vertical line (2px wide, bg-border) with dots at each stop position
  - Dots: 12px circles, filled with status color (green=delivered, blue=in transit, gray=pending, red=failed, amber=skipped)
  - Right column: stop card at each position showing stop name, address, status, delivery time
  - Connecting line between dots: show time between stops (e.g., "12 min") as small label on the connecting line
- Each stop entry is a `m.div` with stagger entry animation (40ms)
- Status icons: CheckCircle (delivered), Truck (in transit), Clock (pending), XCircle (failed), SkipForward (skipped)
- Timeline step sequence animation: dots fill in sequence with 100ms delay each using staggered `m.div` initial={{ scale: 0 }} animate={{ scale: 1 }}

**Estimated vs actual time comparison:**
- In RouteHeader or a new section within RouteDetailClient:
  - Show two side-by-side bars: "Estimated" and "Actual" with the time values
  - Green fill for actual bar if on time (actual <= estimated), red fill if late (actual > estimated)
  - Delta indicator: "+15 min late" in red or "-5 min early" in green, displayed as a badge
  - Animate bar widths with m.div spring transition

**Exception display:**
- In RouteDetailClient at the top of the page (below header, above timeline):
  - If route has unresolved exceptions, show an alert card: rounded-xl bg-status-error/5 border border-status-error/20 p-4 with AlertTriangle icon, count of unresolved exceptions, "Mark Resolved" quick action button
  - Inline badges on affected stop cards in the timeline: small red dot or "Exception" badge next to status
  - Exception actions: "Mark Resolved" button calls existing API (if available) or is a placeholder onClick

**RouteDetailClient.tsx:**
- Replace loading state with SkeletonCrossfade + route detail skeleton
- Integrate RouteTimeline component replacing existing StopsList (or alongside it)
- Add breadcrumbs: `[{ label: 'Dashboard', href: '/admin' }, { label: 'Routes', href: '/admin/routes' }, { label: routeName }]`
- Replace accent colors with teal
- Polish existing LazyRouteMap: add rounded-xl shadow-lg wrapper, loading Skeleton overlay

**RouteStopCard.tsx:**
- Add status-based left border color (4px left border matching status color)
- Add exception indicator if stop has unresolved exception
- Improve card styling: rounded-xl bg-surface-primary shadow-sm, better spacing
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint && pnpm lint:css` passes
    - RouteTimeline.tsx exists with vertical timeline
    - Grep RouteDetailClient.tsx for `animate-pulse` returns 0
    - Grep RouteDetailClient.tsx for `RouteTimeline` confirms integration
  </verify>
  <done>Route detail has vertical timeline with connected dots, status icons, time between stops. Estimated vs actual comparison shows green/red visual diff. Exception alert card at top with Mark Resolved action. No animate-pulse.</done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- Admin dashboard stat cards are clickable and have teal gradients
- Driver detail has 4 animated stat cards with circular on-time ring
- Route detail shows vertical timeline with connected dots
- Route detail shows estimated vs actual time comparison
- Exception alert card appears when unresolved exceptions exist
- No animate-pulse in any detail pages
- Teal accent throughout admin
</verification>

<success_criteria>
Admin dashboard stats, driver detail performance section, and route detail timeline all have premium animations and teal styling. POLH-08 (driver detail polish), POLH-10 (route detail polish), POLH-11 (driver assignment feedback) complete.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-05-SUMMARY.md`
</output>
