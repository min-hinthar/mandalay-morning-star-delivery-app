---
phase: 57-admin-driver-polish
plan: 08
type: execute
wave: 4
depends_on: ["57-02", "57-03", "57-04", "57-05", "57-06", "57-07"]
files_modified:
  - src/components/ui/admin/AdminNav.tsx
  - src/components/ui/driver/DriverNav.tsx
  - src/components/ui/Toast.tsx
  - src/lib/hooks/useToastV8.ts
  - src/components/ui/admin/settings/OperationsSettingsForm.tsx
  - src/components/ui/admin/settings/DeliverySettingsForm.tsx
  - src/components/ui/admin/settings/NotificationSettingsForm.tsx
  - src/components/ui/admin/drivers/DriverDetailClient/EditProfileModal.tsx
  - src/components/ui/admin/drivers/AddDriverModal.tsx
autonomous: true

must_haves:
  truths:
    - "Admin sidebar has animated active indicator that slides between nav items"
    - "Admin sidebar nav icons have hover animation (wobble or scale)"
    - "Driver bottom tab bar has animated active indicator with badge counts"
    - "Toast notifications are floating cards with stacking, swipe dismiss, and auto-dismiss"
    - "Critical toasts (new orders, exceptions) play a subtle chime sound"
    - "Admin form inputs use floating labels matching auth experience"
    - "Admin forms have FloatingUnsavedBar for unsaved changes"
    - "Form validation shows shake + inline error animation"
  artifacts:
    - path: "src/components/ui/admin/AdminNav.tsx"
      provides: "Admin sidebar with animated active indicator and icon hover"
    - path: "src/components/ui/driver/DriverNav.tsx"
      provides: "Driver bottom tab bar with animated active indicator and badges"
    - path: "src/components/ui/Toast.tsx"
      provides: "Premium toast system with stacking, swipe dismiss, sounds"
  key_links:
    - from: "src/components/ui/admin/AdminNav.tsx"
      to: "framer-motion layoutId"
      via: "layoutId for sliding active indicator"
      pattern: "layoutId"
    - from: "src/components/ui/Toast.tsx"
      to: "src/lib/hooks/useToastV8.ts"
      via: "Toast state management hook"
      pattern: "useToast"
---

<objective>
Polish navigation (AdminNav animated indicator, DriverNav badges), toast system (floating cards, stacking, sounds), and admin forms (floating labels, shake validation, FloatingUnsavedBar). Covers navigation, notification, and form decisions from CONTEXT.md.

Purpose: Navigation, toasts, and forms are the cross-cutting polish layer. Applied last to avoid rework from earlier plans. This completes the premium feel across all admin/driver interactions.
Output: Animated navigation, premium toasts, polished forms
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/components/ui/admin/AdminNav.tsx
@src/components/ui/driver/DriverNav.tsx
@src/components/ui/Toast.tsx
@src/lib/hooks/useToastV8.ts
@src/components/ui/FloatingLabelInput.tsx
@src/components/ui/admin/settings/SaveButton.tsx
@src/components/ui/admin/settings/FloatingUnsavedBar.tsx
@src/components/ui/admin/settings/OperationsSettingsForm.tsx
@src/components/ui/admin/settings/DeliverySettingsForm.tsx
@src/components/ui/admin/settings/NotificationSettingsForm.tsx
@src/components/ui/admin/drivers/DriverDetailClient/EditProfileModal.tsx
@src/components/ui/admin/drivers/AddDriverModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminNav animated indicator + DriverNav badges + Toast upgrade</name>
  <files>
    src/components/ui/admin/AdminNav.tsx
    src/components/ui/driver/DriverNav.tsx
    src/components/ui/Toast.tsx
    src/lib/hooks/useToastV8.ts
  </files>
  <action>
**AdminNav.tsx:**
- Add animated active indicator: `m.div layoutId="admin-nav-indicator"` behind the active nav item
  - Background pill that slides between items: `bg-accent-teal/10 rounded-lg` positioned absolutely behind the active link
  - Uses Framer Motion `layoutId` for automatic slide animation between positions
  - `transition={{ type: "spring", stiffness: 300, damping: 30 }}` for smooth spring
- Icon hover animations: wrap each nav icon in `m.div` with `whileHover={{ scale: 1.15, rotate: [-3, 3, 0] }}` for a subtle wobble-scale effect
  - Use `transition={{ duration: 0.3 }}` for the hover
- Replace `primary` (red) active color with `accent-teal`: active nav text/icon should be `text-accent-teal`, not `text-primary`
- Collapsed state: indicator still slides, icons still hover-animate

**DriverNav.tsx:**
- Add animated active indicator: `m.div layoutId="driver-nav-indicator"` as a pill/underline below active tab
  - Small rounded pill (h-1 w-6) centered below icon, `bg-accent-teal`
  - Slides between tabs via layoutId
- Add badge counts: accept optional `badges?: Record<string, number>` prop
  - If badge count > 0 for a tab, show small red circle badge with count (min-w-5 h-5 rounded-full bg-status-error text-white text-xs)
  - Badge entrance: m.div with `initial={{ scale: 0 }}` `animate={{ scale: 1 }}` spring
- Replace `text-primary` (red) active state with `text-accent-teal`

**Toast.tsx upgrade:**
- Redesign toast as floating card: `rounded-xl shadow-lg bg-surface-primary border border-border p-4` (card style, not flat bar)
- Add icon per type: CheckCircle (success, green), XCircle (error, red), AlertTriangle (warning, amber), Info (info, blue)
- Position: change from bottom-right to top-right per CONTEXT (fixed top-4 right-4)
- Toast stacking: first toast visible, additional toasts collapse into "+N more" badge
  - Track visible toast index: only first toast fully rendered
  - Additional toasts: small collapsed card showing "+{count} more" with click to expand
  - Expand: clicking "+N more" shows all toasts stacked with 8px gap
- Swipe to dismiss: add `drag="x"` on toast m.div, `onDragEnd` checks if drag distance > 100px → dismiss
  - `dragConstraints={{ left: 0 }}` to only allow right swipe
- Auto-dismiss: existing timeout behavior + fade out animation on timeout

**useToastV8.ts:**
- Add `sound?: boolean` field to Toast type (default false)
- Add toast type `'order' | 'exception'` to trigger sound
- Sound implementation: use AudioContext pattern from existing `use-card-sound.ts` (if it exists)
  - Create short sine wave "chime" (440Hz, 150ms duration, exponential decay)
  - Only play if: (1) user has interacted with page (AudioContext resume), (2) toast type is 'order' or 'exception'
  - iOS safety: AudioContext only created after first user gesture, use try/catch around play
- Add `expanded` state for stacking collapse/expand
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Grep AdminNav.tsx for `layoutId` confirms animated indicator
    - Grep DriverNav.tsx for `layoutId` confirms animated indicator
    - Grep Toast.tsx for `drag="x"` confirms swipe dismiss
    - Grep useToastV8.ts for `AudioContext` confirms sound support
  </verify>
  <done>AdminNav has sliding active indicator and icon hover animations. DriverNav has sliding active indicator and badge counts. Toasts are floating cards with stacking, swipe dismiss, and chime sounds for critical notifications.</done>
</task>

<task type="auto">
  <name>Task 2: Admin forms floating labels + shake validation + FloatingUnsavedBar</name>
  <files>
    src/components/ui/admin/settings/OperationsSettingsForm.tsx
    src/components/ui/admin/settings/DeliverySettingsForm.tsx
    src/components/ui/admin/settings/NotificationSettingsForm.tsx
    src/components/ui/admin/drivers/DriverDetailClient/EditProfileModal.tsx
    src/components/ui/admin/drivers/AddDriverModal.tsx
  </files>
  <action>
**Form floating labels:**
- In each admin form, replace standard `<Input>` components with `<FloatingLabelInput>` (created in plan 01)
- Map existing label text to the `label` prop
- Map existing error display to the `error` prop
- Preserve all existing form validation logic (react-hook-form/zod)
- For inputs with icons, pass the `icon` prop (e.g., Mail icon for email, Phone for phone, User for name)
- For textarea fields: keep as-is (FloatingLabelInput is for single-line inputs; textareas use standard styling with floating label CSS applied inline)

**Shake validation:**
- FloatingLabelInput already has shake animation (from plan 01) via `animate-shake` CSS class applied when `error` prop is truthy
- Ensure form submission triggers shake: on form.handleSubmit failure, errors are set which triggers shake on invalid fields
- Add `aria-invalid="true"` when error is present for accessibility

**FloatingUnsavedBar integration:**
- Check each form: if it doesn't already use FloatingUnsavedBar, add it
- Import FloatingUnsavedBar from `@/components/ui/admin/settings/FloatingUnsavedBar`
- Track dirty state via react-hook-form's `formState.isDirty`
- FloatingUnsavedBar props: `show={isDirty}`, `onSave={handleSubmit}`, `onDiscard={reset}`
- SaveButton: ensure all forms use SaveButton for submit (checkmark morph animation)

**EditProfileModal.tsx:**
- Replace Input fields with FloatingLabelInput: name (User icon), phone (Phone icon), email (Mail icon)
- Add FloatingUnsavedBar if form tracks changes
- SaveButton for submit

**AddDriverModal.tsx:**
- Replace Input fields with FloatingLabelInput: name (User icon), email (Mail icon), phone (Phone icon), vehicle (Truck icon)
- SaveButton for submit

**Settings forms:**
- OperationsSettingsForm: replace relevant Input fields with FloatingLabelInput
- DeliverySettingsForm: replace relevant Input fields with FloatingLabelInput
- NotificationSettingsForm: mostly toggles, but any text inputs get FloatingLabelInput
- Verify each settings form has FloatingUnsavedBar (most should from Phase 50, but verify)
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint && pnpm lint:css` passes
    - Grep OperationsSettingsForm.tsx for `FloatingLabelInput` confirms usage
    - Grep EditProfileModal.tsx for `FloatingLabelInput` confirms usage
    - Grep AddDriverModal.tsx for `FloatingLabelInput` confirms usage
  </verify>
  <done>All admin forms use floating label inputs with shake validation. FloatingUnsavedBar present on all admin forms. SaveButton used for all submit actions. Form validation shows shake + inline error animation.</done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck && pnpm build` all pass
- Admin sidebar active indicator slides between items
- Admin sidebar icons wobble/scale on hover
- Driver bottom bar has sliding active indicator
- Toasts appear as floating cards from top-right
- Toast stacking collapses to "+N more"
- Swipe right dismisses toast
- Admin forms have floating labels
- Invalid fields shake on submission
- FloatingUnsavedBar appears on unsaved changes
- Critical toasts play chime sound (manual verification)
</verification>

<success_criteria>
Navigation, toasts, and forms are polished across admin and driver apps. This is the final plan -- after completion, all 12 POLH requirements are addressed and the admin/driver experience matches customer-facing quality.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-08-SUMMARY.md`
</output>
