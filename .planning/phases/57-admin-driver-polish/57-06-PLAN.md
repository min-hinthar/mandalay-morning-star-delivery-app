---
phase: 57-admin-driver-polish
plan: 06
type: execute
wave: 3
depends_on: ["57-01"]
files_modified:
  - src/app/(driver)/driver/page.tsx
  - src/app/(driver)/driver/history/page.tsx
  - src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
  - src/components/ui/driver/DriverDashboard/StatCard.tsx
  - src/components/ui/driver/DriverDashboard/RouteCard.tsx
  - src/components/ui/driver/DriverDashboard/HistorySkeleton.tsx
  - src/components/ui/driver/DriverDashboard/HistorySummaryCard.tsx
autonomous: true

must_haves:
  truths:
    - "Driver history page shows real on-time percentage computed from route_stops data (not hardcoded 98%)"
    - "Driver dashboard loading state uses shimmer skeleton with crossfade (no animate-pulse)"
    - "Driver history loading state uses shimmer skeleton with crossfade (no animate-pulse)"
    - "Driver stat cards use animated counters (AnimatedValue)"
    - "Driver history shows summary cards (date, stop count, on-time %, total time) collapsed by default, expandable"
    - "Driver empty states use food-themed emoji illustrations"
  artifacts:
    - path: "src/components/ui/driver/DriverDashboard/HistorySummaryCard.tsx"
      provides: "Collapsible history summary card with on-time percentage"
    - path: "src/components/ui/driver/DriverDashboard/HistorySkeleton.tsx"
      provides: "Shimmer skeleton for driver history page"
  key_links:
    - from: "src/app/(driver)/driver/history/page.tsx"
      to: "route_stops data"
      via: "On-time percentage computation from delivered_at vs delivery_window_end"
      pattern: "onTimePercentage"
    - from: "src/components/ui/driver/DriverDashboard/StatCard.tsx"
      to: "src/components/ui/admin/AdminDashboard/AnimatedValue.tsx"
      via: "AnimatedValue for counting animation"
      pattern: "AnimatedValue"
---

<objective>
Polish driver dashboard and history pages: real on-time percentage computation, shimmer skeletons, animated stat cards, collapsible history summary cards. Covers POLH-05, POLH-06 (partially).

Purpose: Driver history showing "98%" for all drivers is a data integrity issue. Computing real on-time percentage from route_stops data is the highest-priority driver fix. Dashboard and history loading states get shimmer treatment.
Output: Real on-time %, shimmer skeletons, animated stats, history summary cards
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-RESEARCH.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/app/(driver)/driver/page.tsx
@src/app/(driver)/driver/history/page.tsx
@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/StatCard.tsx
@src/components/ui/driver/DriverDashboard/RouteCard.tsx
@src/components/ui/driver/DriverDashboard/StreakDisplay.tsx
@src/components/ui/admin/AdminDashboard/AnimatedValue.tsx
@src/components/ui/admin/SkeletonCrossfade.tsx
@src/components/ui/skeleton/base.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver dashboard shimmer skeleton + animated stat cards</name>
  <files>
    src/app/(driver)/driver/page.tsx
    src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
    src/components/ui/driver/DriverDashboard/StatCard.tsx
    src/components/ui/driver/DriverDashboard/RouteCard.tsx
  </files>
  <action>
**Driver page.tsx:**
- Replace animate-pulse loading block (lines 133-165 per research) with SkeletonCrossfade + DriverDashboardSkeleton
- DriverDashboardSkeleton (inline or separate component): match layout with Skeleton shimmer -- greeting area (Skeleton w=200 h=28), active route card (Skeleton w=full h=120 radius-xl), 3 stat cards (Skeleton w=full h=80 radius-xl in grid)
- Error state: InlineErrorCard with onRetry if data fetch fails
- Apply teal accent color to stat card highlights (per CONTEXT: "Driver app also uses teal accent")

**StatCard.tsx:**

- Import AnimatedValue from `@/components/ui/admin/AdminDashboard/AnimatedValue`
- Wrap numeric values in AnimatedValue for counting animation on load
- Add subtle teal gradient background: `bg-gradient-to-br from-accent-teal/5 to-accent-teal/10`
- Add `m.div` entry animation: `initial={{ opacity: 0, y: 10 }}` `animate={{ opacity: 1, y: 0 }}`
- Replace any `primary` (red) accent with `accent-teal` for non-critical elements

**RouteCard.tsx:**

- Add `m.div` with `whileHover={{ scale: 1.01 }}` and `whileTap={{ scale: 0.98 }}` for card interaction
- Entry animation with stagger if multiple cards
- Replace accent colors with teal

**DriverDashboard.tsx:**

- Verify overall layout works with new animated components
- Add staggered entry to the stat cards section: wrap in m.div with staggerContainer
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint` passes - Grep driver/page.tsx for `animate-pulse` returns 0 - Grep StatCard.tsx for `AnimatedValue` confirms counting animation
  </verify>
  <done>Driver dashboard uses shimmer skeleton crossfade, animated stat counters with teal gradients, card hover interactions. No animate-pulse.</done>
  </task>

<task type="auto">
  <name>Task 2: Driver history -- real on-time percentage + summary cards + skeleton</name>
  <files>
    src/app/(driver)/driver/history/page.tsx
    src/components/ui/driver/DriverDashboard/HistorySkeleton.tsx
    src/components/ui/driver/DriverDashboard/HistorySummaryCard.tsx
  </files>
  <action>
**Real on-time percentage (POLH-05 -- CRITICAL):**
- In `history/page.tsx`, find the hardcoded `const onTimePercentage = 98` (line 57 per research) and REPLACE with real computation
- Computation logic:
  1. From the route_stops data already fetched, filter stops with status "delivered"
  2. For each delivered stop, check if it was delivered on time. Two approaches depending on available data:
     a. If `delivered_at` exists on route_stops: compare `delivered_at <= order.delivery_window_end`
     b. If no `delivered_at`: use `updated_at` as proxy for delivery time
     c. If no delivery window data: compute from route's scheduled time + stop position estimate
  3. `onTimePercentage = totalDelivered > 0 ? Math.round((onTimeStops / totalDelivered) * 100) : 0`
- Read the actual database types to determine available timestamp columns before implementing
- If timestamps are insufficient for precise computation, use available data and add a comment noting the limitation
- Display: AnimatedValue with format="percentage" for the on-time stat

**HistorySummaryCard (new):**

- Create `HistorySummaryCard.tsx` with `'use client'`
- Props: `route: RouteData` (date, stops, onTimePercentage, totalDuration)
- Renders a collapsible card: collapsed shows date + stop count + on-time % + total time in a single row
- Expand/collapse: use m.div with AnimatePresence for the detail section
- Expanded detail: list of individual stops with status, address, delivery time
- Collapse/expand triggered by card click with chevron rotation animation
- On-time percentage shown with color coding: green >= 90%, amber >= 75%, red < 75%
- Card styling: rounded-xl bg-surface-primary border border-border p-4, m.div with whileHover scale 1.01

**HistorySkeleton (new):**

- Create `HistorySkeleton.tsx`
- Match history page layout: 3 stat skeletons across top, 5 HistorySummaryCard-shaped skeletons below
- Shimmer variant

**History page.tsx:**

- Replace animate-pulse loading block (lines 69-98 per research) with SkeletonCrossfade + HistorySkeleton
- Replace route list with HistorySummaryCard components
- Group history by date if not already
- Apply EmptyState variant="driver-history" when no routes exist
- Apply teal accent color
- Use AnimatedValue for all stat numbers (total routes, total deliveries, on-time percentage)
- Staggered card entry animation (40ms per card)
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint && pnpm lint:css` passes - Grep history/page.tsx for `= 98` returns 0 (hardcoded value removed) - Grep history/page.tsx for `onTimePercentage` confirms computed value - Grep history/page.tsx for `animate-pulse` returns 0 - HistorySummaryCard.tsx exists - HistorySkeleton.tsx exists
  </verify>
  <done>Driver history shows real on-time percentage computed from route_stops data. History displays collapsible summary cards with date/stops/on-time/duration. Shimmer skeleton crossfade for loading. EmptyState for empty history. No animate-pulse. No hardcoded 98%.</done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- Driver dashboard shows shimmer skeleton during load
- Driver history shows real on-time percentage (not 98% for everyone)
- History displays collapsible summary cards
- All stat numbers animate (count up from 0)
- Empty history shows food-themed emoji empty state
- No animate-pulse on any driver page
- Teal accent color throughout driver app
</verification>

<success_criteria>
Driver dashboard and history have premium shimmer loading, animated stat cards, and real on-time computation. POLH-05 (real on-time percentage) is the key deliverable. History summary cards are collapsible with date/stops/on-time/duration display.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-06-SUMMARY.md`
</output>
