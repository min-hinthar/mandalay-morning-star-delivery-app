---
phase: 62-production-operations
plan: 04
type: execute
wave: 2
depends_on: ["62-01", "62-02", "62-03"]
files_modified: []
autonomous: false
user_setup:
  - service: Google OAuth (GCP Console)
    why: "Production redirect URI for Google sign-in"
    dashboard_config:
      - task: "Add Supabase callback as authorized redirect URI"
        location: "GCP Console > APIs & Services > Credentials > OAuth 2.0 Client ID"
        detail: "Add https://ukuzkhuppqwtrdkjqrkv.supabase.co/auth/v1/callback"
      - task: "Set consent screen name and support email"
        location: "GCP Console > APIs & Services > OAuth consent screen"
        detail: "App name: 'Mandalay Morning Star Delivery (Los Angeles)', Support email: admin@mandalaymorningstar.com"
      - task: "Publish consent screen to production"
        location: "GCP Console > APIs & Services > OAuth consent screen > Publishing status"
        detail: "Click 'Publish App' (will show unverified warning until Phase 63 adds privacy policy)"
  - service: Supabase Auth
    why: "Production redirect URL allowlist"
    dashboard_config:
      - task: "Add production redirect URL"
        location: "Supabase Dashboard > Authentication > URL Configuration > Redirect URLs"
        detail: "Add https://mandalaymorningstar.com/** (keep existing localhost entry)"
      - task: "Verify Google provider is enabled with GCP client ID/secret"
        location: "Supabase Dashboard > Authentication > Providers > Google"
        detail: "Ensure Client ID and Client Secret from GCP are configured"
  - service: Vercel
    why: "Production environment variables"
    env_vars:
      - name: NEXT_PUBLIC_APP_URL
        source: "Set to https://mandalaymorningstar.com (no trailing slash)"
      - name: EMAIL_FROM
        source: "Set to admin@mandalaymorningstar.com"
      - name: GOOGLE_SITE_VERIFICATION
        source: "Google Search Console > Add property > HTML tag > copy verification code"
  - service: Resend
    why: "Domain verification for email deliverability"
    dashboard_config:
      - task: "Verify mandalaymorningstar.com domain is verified"
        location: "Resend Dashboard > Domains"
        detail: "Confirm root domain shows 'Verified' status (not subdomain)"
  - service: Stripe
    why: "Webhook endpoint reachability"
    dashboard_config:
      - task: "Send test webhook event"
        location: "Stripe Dashboard > Developers > Webhooks > Select endpoint > Send test webhook"
        detail: "Send checkout.session.completed test event, verify 200 response"
  - service: Google Search Console
    why: "Domain ownership verification for search indexing"
    dashboard_config:
      - task: "Add property and get verification code"
        location: "Google Search Console > Add property > URL prefix > https://mandalaymorningstar.com"
        detail: "Choose HTML tag method, copy the content= value for GOOGLE_SITE_VERIFICATION env var"

must_haves:
  truths:
    - "User can sign in with Google on production domain"
    - "Transactional emails arrive in inbox (not spam) with correct sender name"
    - "Stripe webhook test event returns 200 from production endpoint"
    - "Production domain is verified in Google Search Console"
    - "/api/health shows all 5 services healthy"
  artifacts: []
  key_links: []
---

<objective>
Configure external services for production and verify end-to-end functionality.

Purpose: All production services (OAuth, email, webhooks, SEO) are configured and validated. This is the integration checkpoint.
Output: Production-ready external service configuration, verified end-to-end
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-production-operations/62-01-SUMMARY.md
@.planning/phases/62-production-operations/62-02-SUMMARY.md
@.planning/phases/62-production-operations/62-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure Google OAuth for production</name>
  <action>
Configure Google OAuth to work on the production domain. Three dashboards need updates:

**GCP Console (console.cloud.google.com):**
1. Go to APIs & Services > Credentials > your OAuth 2.0 Client ID
2. Under "Authorized redirect URIs", add: `https://ukuzkhuppqwtrdkjqrkv.supabase.co/auth/v1/callback`
   - Note: This is the SUPABASE callback URL, not your app's /auth/callback
   - Keep any existing localhost/dev entries
3. Go to APIs & Services > OAuth consent screen
4. Set app name: "Mandalay Morning Star Delivery (Los Angeles)"
5. Set support email: admin@mandalaymorningstar.com
6. Scopes: email, openid, profile (basic scopes only)
7. Click "Publish App" to move from Testing to Production
   - Users will see "unverified app" warning until Phase 63 adds privacy policy

**Supabase Dashboard (supabase.com/dashboard):**
1. Go to Authentication > URL Configuration > Redirect URLs
2. Add: `https://mandalaymorningstar.com/**`
3. Verify `http://localhost:3000/**` is still present for local dev
4. Go to Authentication > Providers > Google
5. Confirm Client ID and Client Secret from GCP are configured and enabled
  </action>
  <verify>
After configuration, test Google sign-in:
1. Visit https://mandalaymorningstar.com/login (or wait for next deploy)
2. Click "Continue with Google"
3. Complete Google consent flow
4. Verify redirect back to the app with active session
  </verify>
  <done>Google OAuth sign-in works on production domain</done>
  <resume-signal>Type "oauth configured" when GCP + Supabase settings are saved, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure Vercel env vars and verify services</name>
  <action>
**Vercel Dashboard (vercel.com):**
1. Go to project Settings > Environment Variables
2. Verify/update these production env vars:
   - `NEXT_PUBLIC_APP_URL` = `https://mandalaymorningstar.com` (no trailing slash)
   - `EMAIL_FROM` = `admin@mandalaymorningstar.com` (verify matches code constant)
3. After obtaining Search Console verification code (Task 3), add:
   - `GOOGLE_SITE_VERIFICATION` = (verification code from Search Console)

**Google Search Console (search.google.com/search-console):**
1. Click "Add property"
2. Choose "URL prefix" and enter: `https://mandalaymorningstar.com`
3. Select "HTML tag" verification method
4. Copy the `content="..."` value (this is the verification code)
5. Add this value as `GOOGLE_SITE_VERIFICATION` in Vercel env vars
6. Trigger a redeploy in Vercel for the env var to take effect
7. Return to Search Console and click "Verify"

**Resend Dashboard (resend.com/domains):**
1. Verify `mandalaymorningstar.com` shows as "Verified" status
2. If it shows a subdomain instead, verify the root domain is configured

**Stripe Dashboard (dashboard.stripe.com):**
1. Go to Developers > Webhooks
2. Select the production webhook endpoint
3. Verify it points to `https://mandalaymorningstar.com/api/webhooks/stripe`
4. Click "Send test webhook" > select `checkout.session.completed`
5. Verify the test returns a 200 response
6. Check that subscribed events include: checkout.session.completed, checkout.session.expired, payment_intent.payment_failed, charge.refunded

**Email Deliverability Test:**
After Vercel redeploy with updated env vars:
1. Use the admin email compose feature (or `/api/emails/test` if available) to send a test email to admin@mandalaymorningstar.com
2. Check the email arrives in inbox (not spam)
3. View email headers and verify:
   - SPF: pass
   - DKIM: pass
   - DMARC: pass
   - Sender shows "Mandalay Morning Star Burmese Kitchen (Los Angeles)"
  </action>
  <verify>
1. Visit https://mandalaymorningstar.com/api/health -- all services should show "healthy"
2. Visit https://mandalaymorningstar.com/api/health?deep=true -- all services connected
3. Visit https://mandalaymorningstar.com/sitemap.xml -- valid XML with public routes
4. Visit https://mandalaymorningstar.com/robots.txt -- correct disallow rules
5. Google Search Console shows domain as verified
  </verify>
  <done>
- All Vercel env vars are set for production
- Google Search Console domain verified
- Resend domain verified
- Stripe webhook responds to test events
- Email deliverability passes SPF/DKIM/DMARC
- /api/health reports all services healthy
  </done>
  <resume-signal>Type "services verified" when all checks pass, or list which services need attention</resume-signal>
</task>

</tasks>

<verification>
- Google OAuth sign-in works on https://mandalaymorningstar.com
- Test email arrives with correct sender name, passes SPF/DKIM/DMARC
- Stripe webhook test returns 200
- Google Search Console shows verified
- /api/health and /api/health?deep=true report all services healthy
- /sitemap.xml and /robots.txt serve correctly
</verification>

<success_criteria>
- All 5 phase success criteria met:
  1. Google sign-in works on production (consent screen published, redirect configured)
  2. (Apple deferred -- N/A)
  3. Emails arrive in inbox from verified Resend domain with SPF/DKIM/DMARC
  4. Stripe webhook fires with correct signing secret (test event 200)
  5. Production domain verified in Google Search Console
</success_criteria>

<output>
After completion, create `.planning/phases/62-production-operations/62-04-SUMMARY.md`
</output>
