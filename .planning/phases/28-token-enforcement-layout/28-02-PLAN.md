---
phase: 28-token-enforcement-layout
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/components/ui/badge.tsx
  - src/components/ui/NavDots.tsx
  - src/components/ui/cart/CartButton.tsx
  - src/components/ui/cart/CartBar.tsx
  - src/components/ui/checkout/TimeSlotPicker.tsx
  - src/components/ui/checkout/CheckoutStepperV8.tsx
  - src/components/ui/layout/CheckoutLayout.tsx
  - src/components/ui/layout/MobileDrawer/DrawerFooter.tsx
  - src/components/ui/layout/AppHeader/CartIndicator.tsx
  - src/components/ui/menu/UnifiedMenuItemCard/DietaryBadges.tsx
autonomous: true

must_haves:
  truths:
    - "Zero text-[10px] violations in codebase"
    - "Zero text-[11px] violations in codebase"
    - "All small text uses text-2xs (10px) or text-xs (12px) appropriately"
    - "Build passes with zero type errors"
  artifacts:
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component with text-2xs"
      contains: "text-2xs"
    - path: "src/components/ui/cart/CartButton.tsx"
      provides: "CartButton with semantic font size"
      contains: "text-xs"
  key_links: []
---

<objective>
Migrate all typography violations (text-[10px], text-[11px]) to semantic tokens.

Purpose: Standardize small font sizes using the new text-2xs token
Output: Zero arbitrary font size violations in component files
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-token-enforcement-layout/28-CONTEXT.md
@.planning/phases/28-token-enforcement-layout/28-RESEARCH.md
@.planning/phases/28-token-enforcement-layout/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate badge and navigation typography</name>
  <files>
    src/components/ui/badge.tsx
    src/components/ui/NavDots.tsx
    src/components/ui/menu/UnifiedMenuItemCard/DietaryBadges.tsx
  </files>
  <action>
Replace text-[10px] with text-2xs in badge/navigation components:

1. **badge.tsx** (line ~89):
   - `text-[10px]` → `text-2xs`
   - Context: Small badge variant

2. **NavDots.tsx** (line ~82):
   - `text-[10px]` → `text-2xs`
   - Context: Nav dot labels

3. **DietaryBadges.tsx** (line ~96):
   - `text-[10px]` → `text-2xs`
   - Context: Dietary indicator badges

Note: text-2xs is 10px, same as the current arbitrary value. No visual change expected.
  </action>
  <verify>Run `grep -E "text-\[1[01]px\]" src/components/ui/badge.tsx src/components/ui/NavDots.tsx src/components/ui/menu/UnifiedMenuItemCard/DietaryBadges.tsx` returns no matches</verify>
  <done>Badge, NavDots, and DietaryBadges use text-2xs instead of text-[10px]</done>
</task>

<task type="auto">
  <name>Task 2: Migrate cart component typography</name>
  <files>
    src/components/ui/cart/CartButton.tsx
    src/components/ui/cart/CartBar.tsx
    src/components/ui/layout/AppHeader/CartIndicator.tsx
  </files>
  <action>
Replace arbitrary font sizes in cart-related components:

1. **CartButton.tsx** (line ~142):
   - `text-[11px]` → `text-xs`
   - Context: Cart badge count (11px close enough to 12px)

2. **CartBar.tsx** (line ~294):
   - `text-[10px]` → `text-2xs`
   - Context: Cart item count badge

3. **CartIndicator.tsx** (line ~149):
   - `text-[11px]` → `text-xs`
   - Context: Cart count indicator (matches CartButton pattern)

Note: 11px → text-xs (12px) is acceptable per research - close enough for badges.
  </action>
  <verify>Run `grep -E "text-\[1[01]px\]" src/components/ui/cart/CartButton.tsx src/components/ui/cart/CartBar.tsx src/components/ui/layout/AppHeader/CartIndicator.tsx` returns no matches</verify>
  <done>CartButton, CartBar, CartIndicator use text-xs or text-2xs instead of arbitrary values</done>
</task>

<task type="auto">
  <name>Task 3: Migrate checkout and layout typography</name>
  <files>
    src/components/ui/checkout/TimeSlotPicker.tsx
    src/components/ui/checkout/CheckoutStepperV8.tsx
    src/components/ui/layout/CheckoutLayout.tsx
    src/components/ui/layout/MobileDrawer/DrawerFooter.tsx
  </files>
  <action>
Replace text-[10px] in checkout and layout components:

1. **TimeSlotPicker.tsx** (line ~121):
   - `text-[10px]` → `text-2xs`
   - Context: Time slot "Popular" badge

2. **CheckoutStepperV8.tsx** (line ~197):
   - `text-[10px]` → `text-2xs`
   - Context: Step labels (uppercase tracking-wider)

3. **CheckoutLayout.tsx** (line ~185):
   - `text-[10px]` → `text-2xs`
   - Context: Step indicator labels

4. **DrawerFooter.tsx** (line ~59):
   - `text-[10px]` → `text-2xs`
   - Context: Footer disclaimer text

Note: All use 10px for small supplementary text - text-2xs is the exact equivalent.
  </action>
  <verify>Run `grep -rE "text-\[1[01]px\]" src/components/ui/checkout/ src/components/ui/layout/` returns no matches</verify>
  <done>TimeSlotPicker, CheckoutStepperV8, CheckoutLayout, DrawerFooter use text-2xs instead of text-[10px]</done>
</task>

</tasks>

<verification>
1. Run `pnpm typecheck` - no type errors
2. Run `grep -rE "text-\[1[01]px\]" src/` - returns no matches (zero violations)
3. Run `pnpm lint` - no new ESLint errors related to typography
4. Run `pnpm build` - builds successfully
</verification>

<success_criteria>
- Zero text-[10px] occurrences in codebase
- Zero text-[11px] occurrences in codebase
- All 10 files migrated to text-2xs or text-xs as appropriate
- Build passes with zero errors
- ESLint passes for typography rules
</success_criteria>

<output>
After completion, create `.planning/phases/28-token-enforcement-layout/28-02-SUMMARY.md`
</output>
