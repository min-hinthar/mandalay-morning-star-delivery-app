---
phase: 74-guided-walkthrough-driver-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx
  - src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
  - src/components/ui/driver/DriverDashboard/types.ts
  - src/components/ui/driver/DriverDashboard/index.tsx
  - src/components/ui/driver/StopCard.tsx
  - src/components/ui/driver/DriverHeader.tsx
  - src/components/ui/driver/StopDetail.tsx
  - src/app/(driver)/driver/history/DriverHistoryContent.tsx
  - src/app/(driver)/driver/earnings/EarningsPageClient.tsx
autonomous: true
requirements:
  - DPROF-04
  - DUI-02

must_haves:
  truths:
    - "New driver with 0 deliveries sees onboarding walkthrough card on dashboard"
    - "Walkthrough shows 3 milestones: profile complete, route viewed, first delivery"
    - "Walkthrough card auto-hides after all 3 milestones complete with 3-second celebration"
    - "Walkthrough card does not appear for drivers with deliveriesCount > 0"
    - "All interactive driver elements meet 44px minimum touch target"
    - "Primary driver CTAs meet 56px touch target"
  artifacts:
    - path: "src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx"
      provides: "Walkthrough card with 3 milestones and celebration animation"
      min_lines: 80
  key_links:
    - from: "src/components/ui/driver/DriverDashboard/DriverDashboard.tsx"
      to: "OnboardingWalkthroughCard"
      via: "component import, renders between ProfileCompletenessCard and EarningsSummaryCard"
      pattern: "OnboardingWalkthroughCard"
---

<objective>
Create the onboarding walkthrough card for new drivers and fix touch targets across 7 driver components.

Purpose: New drivers need guided onboarding milestones, and all driver interactive elements must meet WCAG 2.1 touch target minimums.
Output: OnboardingWalkthroughCard component, touch target fixes on StopCard, DriverHeader, StopDetail, period toggles, and MonthGroup.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/driver/DriverDashboard/ProfileCompletenessCard.tsx
@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/types.ts
@src/components/ui/driver/DriverDashboard/index.tsx
@src/components/ui/driver/StopCard.tsx
@src/components/ui/driver/DriverHeader.tsx
@src/components/ui/driver/StopDetail.tsx
@src/app/(driver)/driver/history/DriverHistoryContent.tsx
@src/app/(driver)/driver/earnings/EarningsPageClient.tsx
@src/lib/motion-tokens/stagger.ts
@src/lib/motion-tokens/core.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnboardingWalkthroughCard component</name>
  <files>
    src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx
    src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
    src/components/ui/driver/DriverDashboard/types.ts
    src/components/ui/driver/DriverDashboard/index.tsx
  </files>
  <action>
Create `OnboardingWalkthroughCard.tsx` as a SEPARATE component from `ProfileCompletenessCard`. Do NOT extend or modify ProfileCompletenessCard.

**Component structure (mirror ProfileCompletenessCard patterns):**
- Props: `driver` (same shape as ProfileCompletenessCard) + `deliveriesCount: number`
- Only render when `deliveriesCount === 0` AND not dismissed via localStorage key `"walkthrough-dismissed"`
- 3 milestones:
  1. "Complete your profile" — check all 5 fields from ProfileCompletenessCard's `getCompletionState()` logic (fullName, phone, vehicleType, licensePlate, profileImageUrl)
  2. "View today's route" — check localStorage key `"walkthrough-route-viewed"` (set by route page)
  3. "Complete your first delivery" — check `deliveriesCount > 0` (this milestone triggers card auto-hide by definition)

**Visual styling:**
- Driver glass shell: `rounded-2xl border-2 shadow-card bg-surface-primary/80 sm:backdrop-blur-sm p-4`
- Progress bar: `h-1.5` bar with `X/3` count, same as ProfileCompletenessCard pattern
- Border color: `border-accent-teal` (not color-coded like profile card)
- Use `staggerContainer` + `staggerItem` for milestone list animation
- Each milestone item: `m.div` with check/circle icon, label text
- Tappable incomplete milestones: milestone 1 navigates to `/driver/profile`, milestone 2 navigates to `/driver/route`

**Celebration + auto-hide (same pattern as ProfileCompletenessCard):**
- When all 3 complete: trigger `showCelebration` state
- 3-second celebration animation with `spring.ultraBouncy`, particle burst (copy particle effect from ProfileCompletenessCard)
- After 3 seconds: set localStorage `"walkthrough-dismissed"` = `"true"`, fade out
- Use `AnimatePresence mode="wait"` for checklist → celebration transition

**localStorage reads in `useEffect` only** (SSR safety).

**Animation preference guards:**
- Destructure `{ shouldAnimate, getSpring }` from `useAnimationPreference()`
- Conditionally apply all `initial/animate` props

**In DriverDashboard.tsx:**
- Import `OnboardingWalkthroughCard`
- Place AFTER `<ProfileCompletenessCard>` and BEFORE `<EarningsSummaryCard>`:
  ```tsx
  <ProfileCompletenessCard driver={driver} />
  <OnboardingWalkthroughCard driver={driver} deliveriesCount={driver.deliveriesCount} />
  <EarningsSummaryCard ... />
  ```

**In types.ts:** No changes needed — `deliveriesCount` already in `DriverDashboardProps.driver`.

**In index.tsx:** Add `OnboardingWalkthroughCard` to barrel exports.
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Component renders only when deliveriesCount === 0
- Component uses established glass shell styling
- localStorage reads are in useEffect
- Animation preference guards present on all motion elements
  </verify>
  <done>
- OnboardingWalkthroughCard renders below ProfileCompletenessCard for new drivers (deliveriesCount === 0)
- Shows 3 milestones with progress bar
- Celebration triggers when all milestones complete
- Auto-hides via localStorage after celebration
- Does not appear for drivers with deliveriesCount > 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix touch targets across 7 driver components</name>
  <files>
    src/components/ui/driver/StopCard.tsx
    src/components/ui/driver/DriverHeader.tsx
    src/components/ui/driver/StopDetail.tsx
    src/app/(driver)/driver/history/DriverHistoryContent.tsx
    src/app/(driver)/driver/earnings/EarningsPageClient.tsx
  </files>
  <action>
Apply the specific touch target fixes from the CONTEXT.md audit table. Use existing CSS custom properties `--touch-target-min: 44px` and `--touch-target-driver: 56px` where appropriate.

**StopCard.tsx:**
- Add `min-h-[72px]` to the outer `m.button` element (was no min-height)
- Change stop number badge from `h-6 w-6` to `h-8 w-8` (badge size increase)
- Change customer name from `font-medium` to `font-medium text-base` (readability)

**DriverHeader.tsx:**
- Add `min-h-[44px] min-w-[44px]` to the avatar button wrapper (the `button` with `type="button"` and `onClick={() => setShowMenu(...)}`)
- Add `min-h-[44px]` to both dropdown menu items (the Profile `<Link>` and Sign Out `<button>`)

**StopDetail.tsx:**
- Change copy address button from `h-10 w-10` to `min-h-[44px] min-w-[44px]` (was 40px)
- Add `min-h-[56px]` to the call customer button (`handleCall` onClick) — it's a primary action, needs 56px

**DriverHistoryContent.tsx (period toggle + MonthGroup):**
- Add `min-h-[44px]` to the period toggle buttons (currently `py-1.5` with no min-h)
- Add `min-h-[44px]` to the `MonthGroup` collapse button

**EarningsPageClient.tsx (period toggle):**
- Check for period toggle buttons and add `min-h-[44px]` if they have the same pattern as DriverHistoryContent
- Read the file first to verify the pattern exists

All fixes use Tailwind utility classes directly. Do NOT create new CSS variables — use the existing tokens via `min-h-[44px]` and `min-h-[56px]` which align with `--touch-target-min` and `--touch-target-driver`.
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Visually inspect: no layout breakage from added min-height/min-width
- All modified interactive elements: minimum 44px tap target
- Primary CTA (call button): minimum 56px tap target
  </verify>
  <done>
- StopCard has min-h-[72px], larger badge, text-base name
- DriverHeader avatar and dropdown items meet 44px minimum
- StopDetail copy button and call button meet 44px/56px minimums
- Period toggle buttons (History + Earnings) meet 44px minimum
- MonthGroup collapse button meets 44px minimum
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint` pass
- OnboardingWalkthroughCard visible for new drivers, hidden for experienced drivers
- Touch targets meet WCAG 2.1 minimum (44px interactive, 56px primary)
- No visual regressions in driver dashboard or history pages
- Animation preferences respected (reduced motion users see no animations)
</verification>

<success_criteria>
- OnboardingWalkthroughCard.tsx exists with 3 milestones and celebration
- All 7 driver components have corrected touch targets per audit table
- TypeScript compilation and linting clean
</success_criteria>

<output>
After completion, create `.planning/phases/74-guided-walkthrough-driver-ui-polish/74-01-SUMMARY.md`
</output>
