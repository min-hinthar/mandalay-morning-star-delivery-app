---
phase: 36.2-feature-finalization-polish
plan: 05
type: execute
wave: 3
depends_on: ["36.2-01"]
files_modified:
  - src/components/ui/admin/routes/OptimizationModal.tsx
  - src/app/(admin)/admin/routes/[id]/page.tsx
  - src/components/ui/admin/routes/RouteDetailClient.tsx
autonomous: true

must_haves:
  truths:
    - "Route detail page has Optimize button"
    - "Clicking Optimize shows before/after comparison modal"
    - "Modal displays time and distance savings"
    - "Admin can Apply or Cancel optimization"
    - "Manual reorder shows 'Not optimized' warning"
  artifacts:
    - path: "src/components/ui/admin/routes/OptimizationModal.tsx"
      provides: "Modal showing route optimization comparison"
      exports: ["OptimizationModal"]
  key_links:
    - from: "src/components/ui/admin/routes/OptimizationModal.tsx"
      to: "/api/admin/routes/optimize"
      via: "fetch POST for optimization"
      pattern: "fetch.*optimize"
    - from: "src/app/(admin)/admin/routes/[id]/page.tsx"
      to: "src/components/ui/admin/routes/OptimizationModal.tsx"
      via: "modal render"
      pattern: "OptimizationModal"
---

<objective>
Add route optimization comparison UI to route detail page.

Purpose: Show admins before/after comparison when optimizing routes with time/distance savings.
Output: OptimizationModal component and integration with route detail page.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-feature-finalization-polish/36.2-CONTEXT.md
@src/lib/services/route-optimization.ts
@src/app/(admin)/admin/routes/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OptimizationModal component</name>
  <files>src/components/ui/admin/routes/OptimizationModal.tsx</files>
  <action>
    Create modal component for route optimization comparison:

    **Props:**
    ```typescript
    interface OptimizationModalProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      routeId: string;
      currentStops: StopSummary[];
      onApply: (optimizedStops: StopSummary[]) => void;
    }

    interface StopSummary {
      id: string;
      stopNumber: number;
      customerName: string;
      address: string;
    }
    ```

    **Modal content:**
    - Title: "Optimize Route"
    - Loading state while fetching optimization
    - Two columns: "Current Order" | "Optimized Order"
    - Each column shows numbered list of stops
    - Highlight stops that moved (different background)
    - Bottom summary: "Saves X min / X.X miles" (green text)
    - Footer: Cancel button, Apply button (primary)

    **Behavior:**
    1. On open, call POST /api/admin/routes/optimize with routeId
    2. Display comparison while waiting for user action
    3. On Apply, call onApply with optimized order
    4. On Cancel, close modal without changes

    **Styling:**
    - Use Dialog/Modal components
    - Side-by-side layout on desktop, stacked on mobile
    - Green highlight for savings text
    - Moved stops get subtle highlight
  </action>
  <verify>Component compiles, displays before/after comparison</verify>
  <done>
    - Modal shows current vs optimized stop order
    - Displays time/distance savings
    - Apply/Cancel buttons functional
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate optimization into route detail page</name>
  <files>src/app/(admin)/admin/routes/[id]/page.tsx, src/components/ui/admin/routes/RouteDetailClient.tsx</files>
  <action>
    **Add Optimize button:**
    - Place near route header (alongside existing action buttons)
    - Icon: Sparkles or Route or similar
    - Text: "Optimize"
    - Disabled when route has < 2 stops

    **State management:**
    - Add optimizationModalOpen state
    - Track if route is manually reordered (not optimized)
    - Pass current stops to OptimizationModal

    **Apply optimization:**
    - When user clicks Apply in modal:
      1. Call PATCH /api/admin/routes/[id]/stops to reorder
      2. Update local state with new order
      3. Close modal
      4. Show success toast: "Route optimized - saved X min"

    **Manual reorder warning:**
    - If user drags stops after optimization, show warning badge
    - Badge text: "Manually reordered" or "Not optimized"
    - Small amber/yellow badge near route info
    - Clear badge when Optimize is applied again

    **Auto-optimize option:**
    - Per CONTEXT.md: auto-optimize when route created
    - Check if route optimization is enabled in settings
    - If enabled, auto-call optimize on new route creation
    - (This may already be handled in route creation - verify)
  </action>
  <verify>Optimize button appears on route detail, modal opens</verify>
  <done>
    - Optimize button opens comparison modal
    - Apply updates stop order
    - Manual reorder shows warning badge
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. Route detail page shows Optimize button
4. Modal displays before/after comparison
5. Apply updates stop order and shows toast
</verification>

<success_criteria>
- Admin can open optimization modal from route detail
- Modal shows clear before/after comparison
- Time and distance savings prominently displayed
- Apply button updates route stop order
- Manual reordering shows "Not optimized" indicator
</success_criteria>

<output>
After completion, create `.planning/phases/36.2-feature-finalization-polish/36.2-05-SUMMARY.md`
</output>
