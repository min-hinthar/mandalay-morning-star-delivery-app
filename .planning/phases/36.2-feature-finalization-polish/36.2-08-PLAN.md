---
phase: 36.2-feature-finalization-polish
plan: 08
type: execute
wave: 5
depends_on: ["36.2-01"]
files_modified:
  - src/components/ui/account/ProfileTab.tsx
  - src/components/ui/account/OrdersTab.tsx
  - src/components/ui/account/AddressesTab.tsx
  - src/components/ui/account/AccountClient.tsx
autonomous: true

must_haves:
  truths:
    - "Profile form has inline validation on submit"
    - "Orders tab shows skeleton while loading"
    - "Addresses tab shows empty state with illustration"
    - "All tabs have proper loading and error states"
  artifacts:
    - path: "src/components/ui/account/ProfileTab.tsx"
      provides: "Polished profile form with validation"
    - path: "src/components/ui/account/OrdersTab.tsx"
      provides: "Orders list with skeleton loading"
    - path: "src/components/ui/account/AddressesTab.tsx"
      provides: "Addresses with empty state illustration"
  key_links:
    - from: "src/components/ui/account/ProfileTab.tsx"
      to: "/api/account/profile"
      via: "fetch for save"
      pattern: "fetch.*profile"
---

<objective>
Polish customer account tabs with validation, loading states, and empty state illustrations.

Purpose: Consistent UX across customer account with clear feedback and visual polish.
Output: Polished ProfileTab, OrdersTab, and AddressesTab components.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-feature-finalization-polish/36.2-CONTEXT.md
@src/components/ui/account/AccountClient.tsx
@src/components/ui/account/ProfileTab.tsx
@src/components/ui/account/OrdersTab.tsx
@src/components/ui/account/AddressesTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish ProfileTab with validation and loading states</name>
  <files>src/components/ui/account/ProfileTab.tsx</files>
  <action>
    **Form validation (on submit only):**
    - Full name: required, 2-100 chars
    - Email: required, valid email format (may be read-only)
    - Phone: optional but if provided, valid format
    - Preferred delivery time: valid option

    **Loading states:**
    - Skeleton for profile data while loading
    - Button spinner while saving
    - Checkmark flash on success + toast

    **Error handling:**
    - Inline errors below invalid fields
    - Generic toast for server errors
    - Map known errors (e.g., email taken) to inline

    **Discard changes:**
    - Use DiscardChangesModal (from Plan 07)
    - Show when navigating away with unsaved changes
    - Or simpler: just use beforeunload for now

    **Pattern:**
    - Match settings form polish patterns
    - Use React Hook Form + Zod or controlled with manual validation

  </action>
  <verify>Profile form shows validation and loading states</verify>
  <done>
    - Inline validation errors on submit
    - Skeleton loading state
    - Save button spinner and success feedback
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish OrdersTab with skeleton and empty state</name>
  <files>src/components/ui/account/OrdersTab.tsx</files>
  <action>
    **Skeleton loading:**
    - Show order card skeletons while fetching
    - 3-4 skeleton cards to indicate content loading
    - Match actual order card dimensions

    **Empty state:**
    - If no orders: show illustration + message
    - Illustration: food/delivery themed SVG or emoji composition
    - Message: "No orders yet" with subtext "When you place an order, it will appear here"
    - CTA button: "Browse Menu" linking to /menu

    **Order cards:**
    - Ensure each order card has consistent loading states
    - Reorder button shows spinner while processing
    - Cancel confirmation uses modal (already implemented)

    **Error state:**
    - If fetch fails: show error message with retry button
    - "Unable to load orders. Please try again."

  </action>
  <verify>OrdersTab shows skeletons, empty state, and error state correctly</verify>
  <done>
    - Skeleton cards while loading
    - Empty state with illustration when no orders
    - Error state with retry button
  </done>
</task>

<task type="auto">
  <name>Task 3: Polish AddressesTab with validation and empty state</name>
  <files>src/components/ui/account/AddressesTab.tsx</files>
  <action>
    **Empty state:**
    - If no addresses: show illustration + message
    - Illustration: map/location themed SVG
    - Message: "No saved addresses" with subtext "Add an address for faster checkout"
    - CTA button: "Add Address" opens add form

    **Skeleton loading:**
    - Show address card skeletons while fetching
    - Match address card dimensions

    **Form validation (on submit):**
    - line_1: required
    - city: required
    - state: required (dropdown or validated text)
    - postal_code: required, valid US ZIP
    - label: optional, max 50 chars

    **Error handling:**
    - Inline errors below invalid fields
    - "Max 5 addresses" error shown in toast (from API)
    - Delete confirmation modal

    **Loading states:**
    - Add/Edit form button spinner while saving
    - Delete shows loading state
    - Optimistic updates where safe

  </action>
  <verify>AddressesTab shows empty state, skeletons, and validation</verify>
  <done>
    - Empty state with illustration when no addresses
    - Skeleton loading while fetching
    - Form validation on submit
    - Delete confirmation and loading states
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. All account tabs show appropriate loading states
4. Empty states have illustrations and helpful messages
5. Forms validate on submit with inline errors
</verification>

<success_criteria>

- Profile form has inline validation on submit
- Orders tab shows skeletons while loading, empty state when no orders
- Addresses tab shows empty state with illustration, form validation
- All tabs have consistent loading and error handling
- Save/delete actions show appropriate loading feedback
  </success_criteria>

<output>
After completion, create `.planning/phases/36.2-feature-finalization-polish/36.2-08-SUMMARY.md`
</output>
