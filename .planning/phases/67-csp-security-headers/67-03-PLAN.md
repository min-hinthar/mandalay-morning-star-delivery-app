---
phase: 67-csp-security-headers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/currency.ts
  - src/lib/utils/delivery-dates.ts
  - src/lib/utils/format.ts
  - src/lib/services/geocoding.ts
  - src/lib/stores/cart-store.ts
  - src/types/cart.ts
  - src/lib/web-vitals.tsx
  - src/lib/hooks/useABTest.ts
  - src/lib/hooks/index.ts
  - src/components/ui/admin/orders/OrderDetailExpanded/index.tsx
  - src/components/ui/homepage/SiteFooter.tsx
  - src/emails/components/BrandFooter.tsx
autonomous: false

must_haves:
  truths:
    - "All zero-consumer exports removed from utility files"
    - "useABTest.ts file deleted and its barrel re-export removed from hooks/index.ts"
    - "OrderDetailExpanded/index.tsx barrel file deleted (consumers import directly from config.ts)"
    - "SiteFooter and BrandFooter contain verified Facebook and Instagram URLs provided by user (no placeholder or generic URLs remain)"
    - "Build and all tests pass after dead code removal"
  artifacts:
    - path: "src/lib/utils/currency.ts"
      provides: "formatPrice (parsePriceToCents removed)"
    - path: "src/lib/utils/format.ts"
      provides: "Active exports only (formatPriceValue, formatDate removed)"
    - path: "src/lib/hooks/index.ts"
      provides: "Barrel without useABTest re-export"
  key_links:
    - from: "src/lib/stores/cart-store.ts"
      to: "createItemSignature internal usage"
      via: "Internal function used on lines 99, 111 -- re-export on line 289 removed, function stays"
      pattern: "createItemSignature"
---

<objective>
Remove all dead code (zero-consumer exports, dead barrel file, unused hooks file), remove unused npm dependencies, and resolve placeholder social links. CLN-01, CLN-02, CLN-03 requirements.

Purpose: Reduce maintenance surface, eliminate dead code, resolve placeholder content.
Output: Cleaner codebase with no zero-consumer exports, no dead barrel files, resolved social links.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-csp-security-headers/67-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead exports from utility and service files</name>
  <files>
    src/lib/utils/currency.ts
    src/lib/utils/delivery-dates.ts
    src/lib/utils/format.ts
    src/lib/services/geocoding.ts
    src/lib/stores/cart-store.ts
    src/types/cart.ts
    src/lib/web-vitals.tsx
  </files>
  <action>
Remove these zero-consumer exports (verified in research with grep):

1. **`src/lib/utils/currency.ts`** -- Remove `parsePriceToCents` function entirely. Keep all other exports.

2. **`src/lib/utils/delivery-dates.ts`** -- Remove `canEditOrder` function entirely. Keep all other exports.

3. **`src/lib/utils/format.ts`** -- Remove `formatPriceValue` and `formatDate` functions entirely. Note: local `formatDate` functions exist in 6+ files but NONE import from format.ts -- they are independent local implementations. Keep all other exports.

4. **`src/lib/services/geocoding.ts`** -- Remove `reverseGeocode` function entirely. Keep `geocodeAddress` and all other exports.

5. **`src/lib/stores/cart-store.ts`** -- Remove the re-export of `createItemSignature` (around line 289: `export { createItemSignature }`). The function itself STAYS (used internally on lines 99, 111). Only remove the `export` statement that makes it available externally. If the export is part of a destructured export like `export { createItemSignature, __clearDebounceState }`, only remove `createItemSignature` from that export statement.

6. **`src/types/cart.ts`** -- Remove `getDeliveryFeeMessage` function entirely.

7. **`src/lib/web-vitals.tsx`** -- Remove `WEB_VITALS_THRESHOLDS` export and `getPerformanceScore` function. `WEB_VITALS_THRESHOLDS` is used internally on line 34 -- if the `export` keyword is present, remove ONLY the `export` keyword, keep the constant itself. If `getPerformanceScore` is NOT used internally, delete the entire function.

**Before each removal:** Do a quick grep of the export name across `src/` to double-confirm zero consumers. If a consumer IS found that research missed, do NOT remove that export -- note it in the summary.

**After removal:** If any import statements at the top of these files become unused (e.g., a type that was only used by the removed function), remove those too.
</action>
<verify>
Run `pnpm typecheck` -- must pass (no broken imports).
Run `pnpm test` -- must pass (no test relies on removed exports).
Run `pnpm build` -- must succeed.
</verify>
<done>
All 10+ zero-consumer exports removed. Internal-only usages preserved (createItemSignature stays as function, WEB_VITALS_THRESHOLDS stays as const -- only export keywords removed). Build and tests pass.
</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead files and barrel</name>
  <files>
    src/lib/hooks/useABTest.ts
    src/lib/hooks/index.ts
    src/components/ui/admin/orders/OrderDetailExpanded/index.tsx
  </files>
  <action>
**Delete useABTest:**
1. Delete `src/lib/hooks/useABTest.ts` entirely (zero consumers).
2. In `src/lib/hooks/index.ts`, remove line 130 (approximately): `export { useABTest } from "./useABTest"`. Keep all other re-exports.

**Delete dead barrel file:** 3. Delete `src/components/ui/admin/orders/OrderDetailExpanded/index.tsx`. This barrel re-exports `STATUS_COLORS`, `STATUS_LABELS`, `NEXT_STATUSES`, `AUDIT_ACTION_LABELS` from `./config` but has zero consumers -- all 3 consumers import directly from `OrderDetailExpanded/config`. Verify with grep before deleting.

**Audit other barrel files:** Grep for barrel files (index.tsx that only re-export) in `src/components/`. Check if any others have zero consumers. If found, delete them too and note in summary.

**Check for unused npm dependencies:** Run `npx knip --dependencies` (knip is in devDeps) to identify unused packages in package.json. If any are found with HIGH confidence (zero imports across entire codebase), remove them from package.json and run `pnpm install`. If knip fails or output is ambiguous, skip this step.
</action>
<verify>
Run `pnpm typecheck` -- must pass.
Run `pnpm test` -- must pass.
Run `pnpm build` -- must succeed.
Verify `src/lib/hooks/useABTest.ts` does not exist.
Verify `src/components/ui/admin/orders/OrderDetailExpanded/index.tsx` does not exist.
</verify>
<done>
useABTest.ts deleted and its barrel re-export removed. OrderDetailExpanded/index.tsx barrel deleted. Build, tests, and typecheck pass.
</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Collect Facebook and Instagram URLs from user</name>
  <action>Provide the verified Facebook and Instagram page URLs for Mandalay Morning Star. These will be inserted into SiteFooter.tsx and BrandFooter.tsx.</action>
  <instructions>
Please provide the following URLs:
1. **Facebook page URL** (e.g., https://facebook.com/MandalayMorningStar)
2. **Instagram page URL** (e.g., https://instagram.com/mandalaymorningstar)

These will replace the current placeholder social links in the website footer and email footer.
</instructions>
<resume-signal>Provide both URLs to continue, e.g.: "Facebook: https://facebook.com/... Instagram: https://instagram.com/..."</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Insert verified social media URLs into footers</name>
  <files>
    src/components/ui/homepage/SiteFooter.tsx
    src/emails/components/BrandFooter.tsx
  </files>
  <action>
Using the Facebook and Instagram URLs provided by the user in Task 3:

**`src/components/ui/homepage/SiteFooter.tsx`:**

- Replace any placeholder Facebook URL with the verified Facebook URL
- Replace any placeholder Instagram URL with the verified Instagram URL
- Remove any TODO comments related to social link placeholders
- Keep the "Find Us Online" section structure intact

**`src/emails/components/BrandFooter.tsx`:**

- Replace `https://instagram.com/mandalaymorningstar` (line ~78) with the verified Instagram URL
- Replace `https://facebook.com/mandalaymorningstar` (line ~85) with the verified Facebook URL
- Remove any TODO comments related to social link verification

Do NOT change the component structure, styling, or any other content. Only URLs change.
</action>
<verify>
Run `pnpm typecheck` -- must pass.
Run `pnpm build` -- must succeed.
Grep both files for the verified URLs -- should find exact matches.
Grep both files for placeholder/generic social URLs -- should return zero results.
</verify>
<done>
SiteFooter.tsx and BrandFooter.tsx contain the user-provided verified Facebook and Instagram URLs. No placeholder or generic social URLs remain. Build passes. CLN-03 satisfied.
</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes -- no broken imports from removed exports
2. `pnpm test` passes -- no tests depended on removed code
3. `pnpm build` succeeds
4. `pnpm lint` passes (no new issues)
5. Deleted files confirmed absent: useABTest.ts, OrderDetailExpanded/index.tsx
6. Grep for removed exports returns zero results
</verification>

<success_criteria>

- All 10+ zero-consumer exports removed from utility files
- createItemSignature internal usage preserved (only export removed)
- WEB_VITALS_THRESHOLDS internal usage preserved (only export removed)
- useABTest.ts file deleted, re-export removed from hooks/index.ts
- OrderDetailExpanded/index.tsx barrel deleted
- SiteFooter.tsx and BrandFooter.tsx contain verified Facebook and Instagram URLs provided by user (no placeholders remain)
- Build, tests, typecheck, and lint all pass
  </success_criteria>

<output>
After completion, create `.planning/phases/67-csp-security-headers/67-03-SUMMARY.md`
</output>
