---
phase: 93-customer-ux-engagement-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/036_rating_dismissed_share_token.sql
  - src/app/api/orders/[id]/share-token/route.ts
  - src/app/(admin)/admin/ratings/page.tsx
  - src/app/(public)/orders/[shareToken]/share/page.tsx
autonomous: true
requirements:
  - CUX-12
  - CUX-13

must_haves:
  truths:
    - "Orders table has rating_dismissed boolean column"
    - "Orders table has share_token text column with unique index"
    - "POST /api/orders/:id/share-token generates and returns a share token lazily"
    - "Public share page at /orders/:shareToken/share renders order summary without auth"
    - "Admin ratings page lists submitted ratings with order number, customer name, stars, feedback, date"
    - "Admin ratings page supports sorting by date or stars via query param"
  artifacts:
    - path: "supabase/migrations/036_rating_dismissed_share_token.sql"
      provides: "DB columns for rating dismissal and order sharing"
      contains: "rating_dismissed"
    - path: "src/app/api/orders/[id]/share-token/route.ts"
      provides: "Lazy share token generation endpoint"
      exports: ["POST"]
    - path: "src/app/(public)/orders/[shareToken]/share/page.tsx"
      provides: "Public order share page"
      contains: "share_token"
    - path: "src/app/(admin)/admin/ratings/page.tsx"
      provides: "Admin ratings list view"
      contains: "driver_ratings"
  key_links:
    - from: "src/app/api/orders/[id]/share-token/route.ts"
      to: "orders.share_token"
      via: "Supabase update + select"
      pattern: "crypto\\.randomUUID"
    - from: "src/app/(public)/orders/[shareToken]/share/page.tsx"
      to: "orders + order_items"
      via: "Supabase query by share_token"
      pattern: "eq.*share_token"
    - from: "src/app/(admin)/admin/ratings/page.tsx"
      to: "driver_ratings + orders + profiles"
      via: "Supabase join query for ratings with order and customer data"
      pattern: "driver_ratings.*order_id.*profiles"
---

<objective>
Create the backend foundation for rating dismissal (CUX-12) and order sharing (CUX-13): DB migration adding `rating_dismissed` and `share_token` columns, a lazy share token generation API, a public share page, and an admin ratings list page.

Purpose: Engagement features in Plan 02 depend on these DB columns and API routes existing.
Output: Migration 036, share-token API route, public share page, admin ratings page.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-customer-ux-engagement-accessibility/93-CONTEXT.md
@.planning/phases/93-customer-ux-engagement-accessibility/93-RESEARCH.md

<interfaces>
<!-- Existing rating API (already complete, no changes needed): -->
From src/app/api/orders/[id]/rating/route.ts:
- GET: returns { hasRating: boolean, rating?: { stars, feedback, createdAt } }
- POST: accepts { stars: number, feedback?: string }, writes to driver_ratings table

From src/components/ui/orders/tracking/ShareButton.tsx:
```typescript
interface ShareButtonProps {
  orderId: string;
  orderStatus: string;
}
export function ShareButton({ orderId, orderStatus }: ShareButtonProps)
```

From src/types/order.ts:
```typescript
export type OrderStatus = "pending" | "confirmed" | "preparing" | "in_transit" | "out_for_delivery" | "delivered" | "completed" | "cancelled" | "failed";
```

From supabase/migrations - latest sequential: 035_checkout_hardening.sql
Also timestamp-based: 20260214_add_orders_is_priority.sql, 20260302_driver_simple_mode_default_false.sql

DB table: driver_ratings (existing) has columns: id, order_id, driver_id, customer_id, stars, feedback, created_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration + share token API route</name>
  <files>supabase/migrations/036_rating_dismissed_share_token.sql, src/app/api/orders/[id]/share-token/route.ts</files>
  <action>
1. Create migration `036_rating_dismissed_share_token.sql`:
   - ALTER TABLE orders ADD COLUMN IF NOT EXISTS rating_dismissed BOOLEAN NOT NULL DEFAULT FALSE
   - ALTER TABLE orders ADD COLUMN IF NOT EXISTS share_token TEXT UNIQUE
   - CREATE INDEX IF NOT EXISTS idx_orders_share_token ON orders(share_token) WHERE share_token IS NOT NULL
   - Add RLS note: share page reads via API route (service role), not direct client query

2. Create POST `/api/orders/[id]/share-token/route.ts`:
   - Auth: require authenticated user, verify order belongs to user via RLS
   - If order already has share_token, return it
   - Otherwise generate via `crypto.randomUUID()`, update orders row, return token
   - Response: `{ shareToken: string, shareUrl: string }` where shareUrl = `${origin}/orders/${token}/share` (per locked decision: `{origin}/orders/{shareToken}/share`)
   - Validate orderId param exists
   - Use `createClient` from `@/lib/supabase/server` (server component client)
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Migration file exists with both columns. Share token API route returns a UUID token and persists it to the orders table. shareUrl uses /orders/{token}/share path. Subsequent calls return the same token.</done>
</task>

<task type="auto">
  <name>Task 2: Public share page + admin ratings page</name>
  <files>src/app/(public)/orders/[shareToken]/share/page.tsx, src/app/(admin)/admin/ratings/page.tsx</files>
  <action>
1. Create public share page at `src/app/(public)/orders/[shareToken]/share/page.tsx`:
   - Server component (no auth required -- under (public) route group)
   - URL resolves to `/orders/{shareToken}/share` matching locked decision
   - Use Supabase service role client (createServiceClient from `@/lib/supabase/actions`) to query orders by share_token
   - Select: order_items (name_snapshot, quantity, line_total_cents, order_item_modifiers.name_snapshot), total_cents, subtotal_cents, delivery_fee_cents, delivery_window_start
   - Do NOT expose: user_id, address, payment info, special instructions, customer name
   - If no order found for token, call notFound()
   - Render: "Morning Star Delivery" branding header, order summary card with items list, quantities, modifiers, totals, delivery date
   - Minimal styling matching the app's design (use cream gradient background, Card component, formatPrice utility)
   - Add og:title meta for link previews: "Order from Morning Star Delivery"

2. Create admin ratings page at `src/app/(admin)/admin/ratings/page.tsx`:
   - Server component under admin route group (already has admin auth middleware)
   - Query driver_ratings joined with orders (order short ID) and profiles (customer name)
   - SELECT: driver_ratings.id, stars, feedback, created_at, orders.id as order_id, profiles.full_name
   - Accept `?sort=date|stars` searchParam (default: `date`)
     - `sort=date`: ORDER BY created_at DESC
     - `sort=stars`: ORDER BY stars DESC, created_at DESC
   - Render sort toggle links/buttons at top of table (active sort highlighted)
   - Render: table with columns: Order #, Customer, Stars (render as filled/empty star icons), Feedback, Date
   - Use Lucide Star icon for rating display
   - Empty state: "No ratings yet" message
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>Public share page at /orders/{shareToken}/share renders order items without auth. Admin ratings page shows all ratings with customer name and star display, sortable by date or stars via query param. Both pages typecheck and lint cleanly.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes (all new files type-safe)
- `pnpm lint` passes (no hardcoded colors, proper imports)
- `pnpm build` passes (pages render without build errors)
- Migration SQL is valid (both columns, index, no syntax errors)
</verification>

<success_criteria>
- Migration 036 adds rating_dismissed boolean and share_token text columns to orders
- POST /api/orders/:id/share-token lazily generates and returns a share token with shareUrl using /orders/{token}/share path
- /orders/:shareToken/share renders order summary publicly without auth
- /admin/ratings lists all ratings with order number, customer name, stars, feedback, date -- sortable by date or stars
</success_criteria>

<output>
After completion, create `.planning/phases/93-customer-ux-engagement-accessibility/93-01-SUMMARY.md`
</output>
