---
phase: 32-quality-assurance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stories/design-system/Colors.mdx
  - src/stories/design-system/Shadows.mdx
  - src/stories/design-system/Blur.mdx
  - src/stories/design-system/Motion.mdx
  - src/stories/design-system/Typography.mdx
  - src/stories/design-system/Spacing.mdx
  - src/stories/design-system/ZIndex.mdx
autonomous: true

must_haves:
  truths:
    - "Token documentation visible in Storybook sidebar under Design System"
    - "Each token category shows visual examples with theme toggle"
    - "Color tokens show swatches for both light and dark mode values"
    - "All 7 token categories documented: colors, shadows, blur, motion, typography, spacing, z-index"
  artifacts:
    - path: "src/stories/design-system/Colors.mdx"
      provides: "Color token documentation with ColorPalette blocks"
      min_lines: 50
    - path: "src/stories/design-system/Shadows.mdx"
      provides: "Shadow token documentation with visual examples"
      min_lines: 30
    - path: "src/stories/design-system/Blur.mdx"
      provides: "Blur token documentation with visual examples"
      min_lines: 20
    - path: "src/stories/design-system/Motion.mdx"
      provides: "Motion/animation token documentation"
      min_lines: 30
    - path: "src/stories/design-system/Typography.mdx"
      provides: "Typography scale documentation"
      min_lines: 30
    - path: "src/stories/design-system/Spacing.mdx"
      provides: "Spacing scale documentation"
      min_lines: 20
    - path: "src/stories/design-system/ZIndex.mdx"
      provides: "Z-index layer documentation"
      min_lines: 20
  key_links:
    - from: "src/stories/design-system/*.mdx"
      to: ".storybook/main.ts"
      via: "stories glob pattern includes src/**/*.mdx"
      pattern: "\\*\\*/\\*\\.mdx"
---

<objective>
Create comprehensive Storybook documentation for all design system tokens.

Purpose: Enable designers and developers to browse, understand, and correctly use tokens via interactive Storybook stories. This fulfills TOKN-18 requirement.

Output: 7 MDX documentation files in src/stories/design-system/ viewable in Storybook.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source of truth for tokens:
@src/styles/tokens.css

Storybook config (stories pattern already includes *.mdx):
@.storybook/main.ts
@.storybook/preview.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Colors.mdx with all color token categories</name>
  <files>src/stories/design-system/Colors.mdx</files>
  <action>
Create src/stories/design-system/ directory if it doesn't exist.

Create Colors.mdx documenting ALL color tokens from tokens.css:
- Primary palette (primary, primary-hover, primary-active, primary-light, secondary variants)
- Accent colors (green, orange, teal, magenta)
- Surface colors (primary, secondary, tertiary, elevated)
- Text colors (primary, secondary, muted, inverse, money)
- Border colors (default, strong, subtle)
- Status colors (success, warning, error, info)
- Overlay tokens (overlay, overlay-light, overlay-heavy)
- Hero colors (gradient-start, gradient-mid, gradient-end, etc.)

Use Storybook's ColorPalette and ColorItem blocks from '@storybook/addon-docs/blocks'.
For each token show:
- Token name (CSS variable name)
- Tailwind class (e.g., bg-primary, text-text-inverse)
- Light mode value
- Dark mode value (from tokens.css [data-theme="dark"])

Structure with clear headings for each category.
  </action>
  <verify>
Run `pnpm storybook` and navigate to Design System > Colors. Verify all token categories render with color swatches. Toggle theme to verify both light/dark values display.
  </verify>
  <done>Colors.mdx shows all color tokens with visual swatches, organized by category.</done>
</task>

<task type="auto">
  <name>Task 2: Create Shadows.mdx and Blur.mdx documentation</name>
  <files>src/stories/design-system/Shadows.mdx, src/stories/design-system/Blur.mdx</files>
  <action>
Create Shadows.mdx documenting all shadow tokens from tokens.css:
- Base shadows (shadow-xs, shadow-sm, shadow-md, shadow-lg, shadow-xl, shadow-2xl)
- Inner shadows (shadow-inner-sm, shadow-inner-md)
- Glow shadows (shadow-glow-primary, shadow-glow-success, shadow-glow-amber)
- Focus shadows (shadow-focus, shadow-focus-success, shadow-focus-error)
- Specialty shadows (shadow-nav-top, shadow-card, shadow-hint-sm, shadow-hint-md)

For each shadow show:
- Token name and Tailwind class
- Visual example (div with the shadow applied)
- Use case description

Create Blur.mdx documenting blur tokens:
- blur-none, blur-sm, blur-md, blur-lg, blur-xl, blur-2xl, blur-3xl
- Show visual examples with backdrop blur effect applied to divs over background images
  </action>
  <verify>
Run `pnpm storybook`. Navigate to Design System > Shadows and Design System > Blur. Verify shadow boxes render and blur examples show glass effect.
  </verify>
  <done>Shadows.mdx and Blur.mdx show visual examples for all shadow and blur tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Create Motion.mdx, Typography.mdx, Spacing.mdx, and ZIndex.mdx</name>
  <files>src/stories/design-system/Motion.mdx, src/stories/design-system/Typography.mdx, src/stories/design-system/Spacing.mdx, src/stories/design-system/ZIndex.mdx</files>
  <action>
Create Motion.mdx documenting animation tokens from motion-tokens.ts:
- Duration tokens (fast: 150ms, normal: 200ms, slow: 350ms, slower: 500ms, slowest: 750ms)
- Easing tokens (ease-in, ease-out, ease-in-out, spring)
- Framer Motion presets (with note about spring physics)
- Include animated examples showing different durations

Create Typography.mdx documenting the typography scale:
- Font sizes (text-2xs through text-9xl)
- Font weights (font-normal, font-medium, font-semibold, font-bold)
- Line heights (leading-tight, leading-normal, leading-relaxed)
- Show visual examples of each size

Create Spacing.mdx documenting the spacing scale:
- Standard Tailwind scale (1=4px, 2=8px, etc.)
- Custom tokens if any (--tabs-offset: 72px)
- Show visual examples with boxes at different spacings

Create ZIndex.mdx documenting z-index layers:
- Layers from z-index.ts: base(0), dropdown(10), sticky(20), fixed(30), modal-backdrop(40), modal(50), popover(60), tooltip(70), toast(80), max(100)
- Show stacking diagram
- Include usage guidance
  </action>
  <verify>
Run `pnpm storybook`. Navigate to all 4 new pages. Verify:
- Motion shows animated boxes with different durations
- Typography shows text at all sizes
- Spacing shows spacing visual guide
- ZIndex shows layer hierarchy
  </verify>
  <done>All 7 token documentation files exist and render correctly in Storybook.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run Storybook: `pnpm storybook`
2. Verify Design System folder appears in sidebar with 7 pages
3. Check theme toggle works (Storybook globalTypes decorator switches themes)
4. Verify Colors page shows accurate light/dark values from tokens.css
5. Run `pnpm build-storybook` to verify no MDX parse errors
</verification>

<success_criteria>
- 7 MDX files exist in src/stories/design-system/
- Each file renders without errors in Storybook
- Theme toggle updates color swatches appropriately
- Visual examples render for shadows, blur, motion, spacing
- TOKN-18 requirement satisfied: token documentation with visual examples
</success_criteria>

<output>
After completion, create `.planning/phases/32-quality-assurance/32-01-SUMMARY.md`
</output>
