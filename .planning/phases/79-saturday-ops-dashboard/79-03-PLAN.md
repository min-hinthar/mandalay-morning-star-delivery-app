---
phase: 79-saturday-ops-dashboard
plan: 03
type: execute
wave: 3
depends_on: [79-02]
files_modified:
  - src/components/ui/admin/ops/OpsDriverPanel.tsx
  - src/app/(admin)/admin/ops/OpsCenter.tsx
  - src/components/ui/admin/ops/index.ts
autonomous: false
requirements: [OPS-05, OPS-07]

must_haves:
  truths:
    - "Driver availability widget shows all active drivers with available ones highlighted and unavailable ones grayed with reason"
    - "Driver panel shows name, vehicle type, rating, and unavailable reason if applicable"
    - "Clicking a driver links to their detail page"
    - "Driver panel is positioned below the order list as a full-width section"
    - "Ops page handles all interactive flows end-to-end: filter, select, bulk change, toast feedback"
  artifacts:
    - path: "src/components/ui/admin/ops/OpsDriverPanel.tsx"
      provides: "Driver availability widget with readiness derivation"
      min_lines: 50
  key_links:
    - from: "src/components/ui/admin/ops/OpsDriverPanel.tsx"
      to: "/api/admin/drivers"
      via: "fetch for driver list"
      pattern: "fetch.*api/admin/drivers"
    - from: "src/components/ui/admin/ops/OpsDriverPanel.tsx"
      to: "src/components/ui/admin/ops/helpers.ts"
      via: "deriveDriverReadiness for each driver"
      pattern: "deriveDriverReadiness"
---

<objective>
Driver availability panel and end-to-end verification of the complete ops dashboard.

Purpose: Complete the ops dashboard with the driver readiness widget and verify all features work together in the browser.
Output: Fully functional `/admin/ops` page with driver panel, verified by human.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-saturday-ops-dashboard/79-01-SUMMARY.md
@.planning/phases/79-saturday-ops-dashboard/79-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 helpers -->
From src/components/ui/admin/ops/helpers.ts:
```typescript
export interface DriverReadiness {
  id: string;
  fullName: string | null;
  vehicleType: string | null;
  ratingAvg: number;
  isAvailable: boolean;
  unavailableReason: string | null;
}
export function deriveDriverReadiness(driver: DriverApiResponse, today: Date): DriverReadiness;
```

From src/app/api/admin/drivers/route.ts (GET response shape):
```typescript
// Returns array of:
{
  id: string;
  userId: string;
  email: string;
  fullName: string | null;
  phone: string | null;
  vehicleType: string | null;
  isActive: boolean;
  ratingAvg: number;
  deliveriesCount: number;
  availability: { available_days: string[], blocked_dates: string[] } | null;
}
```

From src/app/(admin)/admin/ops/OpsCenter.tsx (Plan 02 output):
```typescript
// OpsCenter renders: AdminPageHeader, OpsCountdownBar, OpsKPIGrid, OpsOrderList, OpsBulkToolbar
// Has a placeholder/comment area where OpsDriverPanel should be inserted below the order list
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create driver availability panel and wire into OpsCenter</name>
  <files>
    src/components/ui/admin/ops/OpsDriverPanel.tsx,
    src/app/(admin)/admin/ops/OpsCenter.tsx,
    src/components/ui/admin/ops/index.ts
  </files>
  <action>
    **OpsDriverPanel.tsx** (`'use client'`):
    - Receives no props (self-contained data fetching).
    - Uses `useState` + `useEffect` to fetch from `/api/admin/drivers` on mount.
    - Filters to `isActive` drivers only from API response.
    - Calls `deriveDriverReadiness(driver, new Date())` for each driver.
    - Sorts: available drivers first, then unavailable. Within each group, sort by name.
    - Renders inside a `Card` with `CardHeader` title "Driver Readiness" and a count badge showing "{available}/{total} available".
    - Each driver rendered as a compact row:
      - Name (bold if available, muted if unavailable).
      - Vehicle type icon or text (car/motorcycle/bicycle/van/truck).
      - Rating as stars or numeric (e.g., "4.8").
      - If unavailable: grayed out row with reason text in muted color.
      - If available: highlighted row with green left border or green dot indicator.
    - Each driver row is a `Link` to `/admin/drivers/{id}` (clickable per CONTEXT.md).
    - Loading state: 3 skeleton rows.
    - Empty state: "No active drivers" with link to drivers page.
    - Use `cardContainer`/`cardItem` Framer Motion variants for staggered entrance.
    - Full-width section below order list per CONTEXT.md layout decision.

    **OpsCenter.tsx update**:
    - Import `OpsDriverPanel`.
    - Render `<OpsDriverPanel />` below the order list section, above the `OpsBulkToolbar`.
    - Add a section divider or spacing between order list and driver panel.

    **index.ts update**:
    - Add `OpsDriverPanel` to barrel exports.
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
    Driver panel renders below order list showing all active drivers with availability status, vehicle type, rating. Available drivers highlighted, unavailable grayed with reason. Each driver links to detail page. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete ops dashboard end-to-end</name>
  <action>
    Human verifies the complete Saturday Ops Dashboard at /admin/ops. All automated implementation is done in prior tasks -- this is visual and functional verification only.
  </action>
  <what-built>
    Complete Saturday Ops Dashboard at /admin/ops with:
    - Sticky countdown bar (cutoff + delivery start timers)
    - Clickable KPI cards filtering order list by status
    - Red unassigned orders badge on Confirmed card
    - Order list grouped by time window with checkboxes
    - Floating bulk toolbar for forward-only status transitions
    - Confirmation dialog before bulk changes
    - Success/error toasts after bulk operations
    - Driver readiness panel showing available/unavailable drivers
    - 5-second auto-refresh preserving selections
    - Ops Center link in admin navigation
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and navigate to `/admin/ops`
    2. Verify countdown bar is sticky at top with two timers ticking every second
    3. Verify 5 KPI cards show order counts (may be 0 if no test orders)
    4. Click a KPI card -- order list should filter to that status
    5. Click same KPI card again -- filter should clear
    6. If orders exist: check the checkbox on 2+ orders with same status
    7. Verify floating bulk toolbar appears at bottom with "Move N to [next status]"
    8. Click the bulk action button -- confirmation dialog should appear
    9. Confirm the action -- toast should appear, selections should clear
    10. Scroll down to Driver Readiness panel -- verify drivers shown with availability
    11. Click a driver row -- should navigate to driver detail page
    12. Wait 5+ seconds -- verify subtle refresh indicator appears briefly
    13. Check mobile view (narrow viewport) -- cards and list should stack responsively
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build` all pass
- /admin/ops renders complete ops dashboard
- All 7 OPS requirements and RULES-09 addressed
- Human verifies interactive flows work end-to-end
</verification>

<success_criteria>
- Driver panel shows all active drivers sorted by availability, with reasons for unavailability
- Clicking a driver navigates to their detail page
- Full ops dashboard works end-to-end: view, filter, select, bulk change, toast feedback, auto-refresh
- Human has verified the page in browser
</success_criteria>

<output>
After completion, create `.planning/phases/79-saturday-ops-dashboard/79-03-SUMMARY.md`
</output>
