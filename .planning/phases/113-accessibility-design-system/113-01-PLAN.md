---
phase: 113-accessibility-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Button sm renders at 44px (h-11) on all viewports"
    - "Input sm renders at 44px (h-11) on all viewports"
    - "Button xs variant exists at 36px (h-9) for icon-only escape hatch"
    - "Input xs variant exists at 36px (h-9) for tight containers"
    - "No interactive element shrinks below 44px in flex containers"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "sm=h-11, xs=h-9 variants"
      contains: "sm: \"h-11"
    - path: "src/components/ui/input.tsx"
      provides: "sm=h-11, xs=h-9 variants"
      contains: "sm: \"h-11"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "all 68 files using size='sm'"
      via: "CVA variant propagation"
      pattern: "sm.*h-11"
    - from: "src/components/ui/input.tsx"
      to: "all ~15 files using size='sm'"
      via: "CVA variant propagation"
      pattern: "sm.*h-11"
---

<objective>
Enforce 44px minimum touch targets on Button and Input sm variants (A11Y-01).

Purpose: WCAG AAA touch target compliance — sm buttons/inputs currently render at 36px (h-9), below the 44px minimum. This affects all 68+ files using Button sm and ~15 files using Input sm across customer, admin, and driver surfaces.
Output: Updated button.tsx and input.tsx with sm=h-11 (44px) and new xs=h-9 (36px) escape hatch variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-accessibility-design-system/113-CONTEXT.md
@.planning/phases/113-accessibility-design-system/113-PRECONTEXT-RESEARCH.md

<interfaces>
<!-- Current Button CVA size variants (button.tsx line 106-120) -->
sm: "h-9 px-4 py-2 text-sm rounded-button [&_svg]:h-4 [&_svg]:w-4",
md: "h-11 px-6 py-3 text-base rounded-button [&_svg]:h-4 [&_svg]:w-4",
lg: "h-[52px] px-8 py-3.5 text-base rounded-button [&_svg]:h-5 [&_svg]:w-5",
xl: "h-[60px] px-10 py-4 text-lg rounded-button [&_svg]:h-5 [&_svg]:w-5",
icon: "h-11 w-11 rounded-button [&_svg]:h-5 [&_svg]:w-5",
"icon-sm": "h-9 w-9 rounded-button [&_svg]:h-4 [&_svg]:w-4",

<!-- Current Input CVA size variants (input.tsx line 44-49) -->
default: "h-11 px-4 py-3",
sm: "h-9 px-3 py-2 text-sm",
lg: "h-12 px-4 py-3",
xl: "h-14 px-5 py-4 text-lg",

<!-- ButtonProps interface (button.tsx line 135-146) -->
export interface ButtonProps extends ButtonHTMLProps, VariantProps<typeof buttonVariants> {
  asChild?: boolean;
  isLoading?: boolean;
  loadingText?: string;
  leftIcon?: React.ReactNode;
  rightIcon?: React.ReactNode;
}

<!-- InputProps interface (input.tsx line 73-78) -->
export interface InputProps extends InputHTMLProps, VariantProps<typeof inputVariants> {
  error?: string;
  helperText?: string;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button sm → h-11 (44px) + add xs variant at h-9 (36px)</name>
  <files>src/components/ui/button.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (current CVA variants, full file)
    - .planning/phases/113-accessibility-design-system/113-CONTEXT.md (D-01, D-03, D-05)
  </read_first>
  <action>
Edit the `buttonVariants` CVA `size` object in `src/components/ui/button.tsx`:

1. Change `sm` from `"h-9 px-4 py-2 text-sm rounded-button [&_svg]:h-4 [&_svg]:w-4"` to `"h-11 px-4 py-2.5 text-sm rounded-button [&_svg]:h-4 [&_svg]:w-4 min-h-11"` (per D-01: 44px always, per D-05: min-h-11 prevents flex collapse)

2. Add new `xs` variant BEFORE `sm`: `xs: "h-9 px-3 py-1.5 text-xs rounded-button [&_svg]:h-3.5 [&_svg]:w-3.5"` (per D-03: 36px for non-mobile icon-only buttons in tight containers)

3. Add new `"icon-xs"` variant after `"icon-sm"`: `"icon-xs": "h-9 w-9 rounded-button [&_svg]:h-3.5 [&_svg]:w-3.5"` (parallel to xs text variant)

4. Update the JSDoc comment at top of file: change `Sizes: sm (36px)` to `Sizes: xs (36px), sm (44px)` to reflect the new size scale.

5. Update `spinnerSize` logic (line 175-176): add `size === "xs"` to the sm spinner case — xs gets the small spinner too. The condition becomes: `size === "lg" || size === "xl" || size === "icon-lg" || size === "icon-xl" ? "md" : "sm"` (already covers xs implicitly since it falls to else, so no change needed here).

Do NOT modify:
- variant definitions (primary, secondary, ghost, etc.)
- motion props or animation logic
- asChild/Slot handling
- ButtonProps interface (VariantProps auto-picks up new size)
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - button.tsx `sm:` line contains `h-11` and `min-h-11`
    - button.tsx contains `xs: "h-9 px-3`
    - button.tsx contains `"icon-xs": "h-9 w-9`
    - button.tsx JSDoc contains `xs (36px), sm (44px)`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Button sm = 44px (h-11), xs = 36px (h-9) escape hatch exists, icon-xs exists, typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Input sm → h-11 (44px) + add xs variant + add focus-visible ring</name>
  <files>src/components/ui/input.tsx</files>
  <read_first>
    - src/components/ui/input.tsx (current CVA variants, full file)
    - .planning/phases/113-accessibility-design-system/113-CONTEXT.md (D-02, D-03, D-09)
    - src/components/ui/button.tsx (verify xs pattern matches after Task 1)
  </read_first>
  <action>
Edit the `inputVariants` CVA in `src/components/ui/input.tsx`:

1. Change `sm` from `"h-9 px-3 py-2 text-sm"` to `"h-11 px-3 py-2.5 text-sm min-h-11"` (per D-02: 44px always, per D-05: min-h-11 prevents flex collapse)

2. Add new `xs` variant BEFORE `sm`: `xs: "h-9 px-2.5 py-1.5 text-xs"` (per D-03: 36px escape hatch for tight containers)

3. Add `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2` to the base CVA array (per D-09: layer CSS ring ON TOP of existing Framer Motion animated glow). The base array currently has `"focus-visible:outline-none focus-visible:border-primary"` — append the ring classes to that same line so it becomes: `"focus-visible:outline-none focus-visible:border-primary focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"`

4. Update the JSDoc comment at top: change `Height: 44px default` to `Height: xs=36px, sm=44px, default=44px` to reflect the new size scale.

Do NOT modify:
- Framer Motion animate/transition logic (glow stays — ring layers on top per D-09)
- error/success variant definitions
- InputProps interface (VariantProps auto-picks up new size)
- Error message rendering or helperText rendering
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - input.tsx `sm:` line contains `h-11` and `min-h-11`
    - input.tsx contains `xs: "h-9 px-2.5`
    - input.tsx base CVA array contains `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2`
    - input.tsx JSDoc contains `xs=36px, sm=44px`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Input sm = 44px (h-11), xs = 36px (h-9), focus-visible ring layered on glow, typecheck passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies shared UI component sizing — no new API routes, authentication flows, or data storage.

| Boundary | Description |
|----------|-------------|
| None | Pure CSS/styling changes to component variants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | N/A | button.tsx, input.tsx | accept | No attack surface — CSS class changes only. No user input handling modified, no data flow changes. |
</threat_model>

<verification>
- `pnpm typecheck` passes (no new type errors from xs variant addition)
- `pnpm lint` passes (no ESLint violations from new classes)
- `pnpm build` passes (production build succeeds)
- grep confirms: `h-11` in both sm variants, `h-9` in both xs variants
</verification>

<success_criteria>
- Button sm = h-11 (44px), Input sm = h-11 (44px) — WCAG AAA touch target compliance
- xs escape hatch available at h-9 (36px) for both Button and Input
- Input has focus-visible ring layered on existing glow animation
- Zero type errors, zero lint errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/113-accessibility-design-system/113-01-SUMMARY.md`
</output>
