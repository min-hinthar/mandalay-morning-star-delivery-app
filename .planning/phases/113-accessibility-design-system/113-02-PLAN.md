---
phase: 113-accessibility-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/radio-group.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/select.tsx
  - src/components/ui/orders/tracking/MuteToggle.tsx
  - src/components/ui/checkout/AddressFormV8.tsx
  - src/components/ui/cart/ClearCartConfirmation.tsx
  - src/components/ui/cart/CartDrawerParts.tsx
  - src/components/ui/cart/CartItem/CartItem.tsx
  - src/components/ui/cart/CartItem/ValidationOverlay.tsx
  - src/components/ui/cart/CartPage/CartPageHeader.tsx
  - src/components/ui/cart/CartPage/CheckoutGate.tsx
  - src/components/ui/menu/FavoriteButton.tsx
  - src/components/ui/admin/ops/OpsKPIGrid.tsx
  - src/components/ui/layout/MobileDrawer/DrawerUserSection.tsx
  - src/components/ui/layout/AppHeader/AccountIndicator.tsx
  - src/components/ui/layout/AppHeader/CartIndicator.tsx
autonomous: true
requirements: [A11Y-03, A11Y-04]

must_haves:
  truths:
    - "All standard interactive components use focus-visible:ring-2 ring-primary ring-offset-2"
    - "Checkbox uses downsized focus-visible:ring-1 ring-primary ring-offset-1"
    - "Card interactive uses shadow-[var(--shadow-focus)] instead of ring-offset"
    - "No component uses focus: prefix for ring — all use focus-visible:"
    - "No component uses legacy ring-ring token — all use ring-primary"
    - "No component uses hardcoded ring-red-500/ring-zinc-400 — all use semantic tokens"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "shadow-focus based Card focus"
      contains: "shadow-[var(--shadow-focus)]"
    - path: "src/components/ui/checkbox.tsx"
      provides: "Downsized ring for 20px element"
      contains: "ring-1"
    - path: "src/components/ui/textarea.tsx"
      provides: "Migrated from ring-ring"
      contains: "ring-primary"
    - path: "src/components/ui/radio-group.tsx"
      provides: "Migrated from ring-ring"
      contains: "ring-primary"
    - path: "src/components/ui/dialog.tsx"
      provides: "Migrated from ring-ring + focus:"
      contains: "focus-visible:ring-primary"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "src/styles/tokens.css"
      via: "var(--shadow-focus) CSS custom property"
      pattern: "shadow-focus"
    - from: "all ring-red-500 files"
      to: "ring-status-error"
      via: "semantic token migration"
      pattern: "ring-status-error"
---

<objective>
Harmonize focus indicators across all interactive components and clean up hardcoded ring colors (A11Y-03, A11Y-04).

Purpose: Five different focus patterns currently exist across the codebase — this plan standardizes to three patterns (standard, small, animated) and migrates all hardcoded ring colors to semantic tokens for dark mode compliance.
Output: Consistent focus-visible:ring behavior across all 70+ interactive UI components, zero hardcoded ring colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-accessibility-design-system/113-CONTEXT.md
@.planning/phases/113-accessibility-design-system/113-PRECONTEXT-RESEARCH.md

<interfaces>
<!-- Focus Ring Contracts (target state from PRECONTEXT-RESEARCH.md §5) -->

Standard interactive (Button, Input, Select, Textarea, RadioGroup, Toggle, Tabs):
  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2

Small interactive (Checkbox 20px):
  focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-primary focus-visible:ring-offset-1

Animated interactive (Card with translateY lift):
  focus-visible:outline-none focus-visible:shadow-[var(--shadow-focus)]

<!-- Shadow focus tokens from tokens.css -->
:root  { --shadow-focus: 0 0 0 3px rgba(164, 16, 52, 0.15); }
.dark  { --shadow-focus: 0 0 0 3px rgba(255, 77, 109, 0.2); }

<!-- Semantic ring token mappings -->
ring-red-500    → ring-status-error (destructive actions)
ring-zinc-400   → ring-primary (neutral cancel actions)
ring-amber-500  → ring-status-warning (warning context)
ring-blue-500   → ring-primary (info context)
ring-green-500  → ring-status-success (success context)
ring-white / ring-zinc-900/950  → ring-surface-primary dark:ring-surface-primary (avatar borders)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize focus rings on core UI components</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/checkbox.tsx
    src/components/ui/textarea.tsx
    src/components/ui/radio-group.tsx
    src/components/ui/dialog.tsx
    src/components/ui/alert-dialog.tsx
    src/components/ui/select.tsx
    src/components/ui/orders/tracking/MuteToggle.tsx
    src/components/ui/checkout/AddressFormV8.tsx
  </files>
  <read_first>
    - src/components/ui/card.tsx (interactive variant focus line 35)
    - src/components/ui/checkbox.tsx (focus line 38)
    - src/components/ui/textarea.tsx (focus line 13)
    - src/components/ui/radio-group.tsx (focus line 23)
    - src/components/ui/dialog.tsx (DialogClose focus line 53)
    - src/components/ui/alert-dialog.tsx (verify current state)
    - src/components/ui/select.tsx (SelectTrigger focus line 24)
    - src/components/ui/orders/tracking/MuteToggle.tsx (focus line ~48)
    - src/components/ui/checkout/AddressFormV8.tsx (ring-ring line ~116)
    - src/styles/tokens.css (shadow-focus token values — lines 209-211, 579-581)
    - .planning/phases/113-accessibility-design-system/113-CONTEXT.md (D-06 through D-13)
  </read_first>
  <action>
**card.tsx** (per D-08, D-12):
- In `cardVariants` > `interactive` array, replace `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` with `"focus-visible:outline-none focus-visible:shadow-[var(--shadow-focus)]"` — boxShadow moves with translateY lift, ring-offset does not.
- Note: D-12 specifies disabling 3D tilt during keyboard focus via `isKeyboardFocused` state. However, Card does NOT currently have 3D tilt (only translateY hover). Skip D-12 — no tilt to disable.

**checkbox.tsx** (per D-07):
- Change `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` to `"focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-primary focus-visible:ring-offset-1"` — ring-2 + offset-2 is visually overwhelming on a 20px element.

**textarea.tsx** (per D-06, D-11):
- Replace the entire className string. Change from:
  `"flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm"` to `"flex min-h-[80px] w-full rounded-input border border-border bg-surface-primary px-3 py-2 text-sm font-body text-text-primary"`
- Replace `"placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"` with `"placeholder:text-text-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"`
- This migrates ring-ring → ring-primary, adds ring-offset-2, and updates legacy shadcn tokens (border-input → border-border, bg-background → bg-surface-primary, text-muted-foreground → text-text-muted) to project token system.

**radio-group.tsx** (per D-06, D-11):
- In RadioGroupItem, change `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"` to `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` — migrate ring-ring → ring-primary, add ring-offset-2.
- Also update `border border-input` to `border border-border` (legacy shadcn token).

**dialog.tsx** (per D-10, D-11):
- In DialogClose (line 53), replace `"focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2"` with `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` — three fixes: focus: → focus-visible:, ring-ring → ring-primary.
- Also replace `ring-offset-background` with `ring-offset-surface-primary` on the same line.
- Also replace `data-[state=open]:text-muted-foreground` with `data-[state=open]:text-text-muted` and `data-[state=open]:bg-accent` with `data-[state=open]:bg-surface-secondary`.
- In DialogContent (line 39), replace `bg-background` with `bg-surface-primary`.
- In DialogDescription, replace `text-muted-foreground` with `text-text-secondary`.

**alert-dialog.tsx** (per D-11):
- In AlertDialogContent (line 38), replace `bg-background` with `bg-surface-primary`.
- In AlertDialogDescription, replace `text-muted-foreground` with `text-text-secondary`.

**select.tsx** (per D-10):
- In SelectTrigger, change `"focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2"` to `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` — focus: → focus-visible:.

**MuteToggle.tsx** (per D-10, D-11):
- Change `"focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2"` to `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` — both focus: → focus-visible: AND ring-ring → ring-primary.

**AddressFormV8.tsx** (per D-11):
- Find `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"` and replace with `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` — ring-ring → ring-primary, add ring-offset-2.

CRITICAL — DO NOT MODIFY:
- Drawer.tsx exit animation (D-13: must stay duration: 0.15s easeIn per Phase 112 contract)
- MuteToggle aria-pressed, aria-label, h-11 w-11 sizing (Phase 112 contract)
- Any role="status" or aria-live attributes on any component
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - card.tsx interactive variant contains `shadow-[var(--shadow-focus)]` and does NOT contain `ring-offset-2` in interactive variant
    - checkbox.tsx contains `focus-visible:ring-1` and `focus-visible:ring-offset-1` (NOT ring-2/offset-2)
    - textarea.tsx contains `ring-primary` and does NOT contain `ring-ring`
    - radio-group.tsx contains `ring-primary` and `ring-offset-2` and does NOT contain `ring-ring`
    - dialog.tsx DialogClose contains `focus-visible:ring-primary` and does NOT contain `focus:ring-ring`
    - select.tsx contains `focus-visible:` and does NOT contain `focus:outline-none focus:ring`
    - MuteToggle.tsx contains `focus-visible:ring-primary` and does NOT contain `focus:ring-ring`
    - AddressFormV8.tsx contains `ring-primary` and does NOT contain `ring-ring`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All 9 core UI components use standardized focus-visible patterns, no legacy ring-ring token remains, no focus: (mouse-showing) ring remains in core components</done>
</task>

<task type="auto">
  <name>Task 2: Migrate hardcoded ring colors to semantic tokens + fix focus: → focus-visible: in secondary files</name>
  <files>
    src/components/ui/cart/ClearCartConfirmation.tsx
    src/components/ui/cart/CartDrawerParts.tsx
    src/components/ui/cart/CartItem/CartItem.tsx
    src/components/ui/cart/CartItem/ValidationOverlay.tsx
    src/components/ui/cart/CartPage/CartPageHeader.tsx
    src/components/ui/cart/CartPage/CheckoutGate.tsx
    src/components/ui/menu/FavoriteButton.tsx
    src/components/ui/admin/ops/OpsKPIGrid.tsx
    src/components/ui/layout/MobileDrawer/DrawerUserSection.tsx
    src/components/ui/layout/AppHeader/AccountIndicator.tsx
    src/components/ui/layout/AppHeader/CartIndicator.tsx
  </files>
  <read_first>
    - src/components/ui/cart/ClearCartConfirmation.tsx (lines ~150-175 for ring-zinc-400, ring-red-500)
    - src/components/ui/cart/CartDrawerParts.tsx (line ~152 for ring-red-500)
    - src/components/ui/cart/CartItem/CartItem.tsx (line ~236 for ring-red-500)
    - src/components/ui/cart/CartItem/ValidationOverlay.tsx (line ~98 for ring-red-500)
    - src/components/ui/cart/CartPage/CartPageHeader.tsx (line ~96 for ring-red-500)
    - src/components/ui/cart/CartPage/CheckoutGate.tsx (line ~72 for ring-amber-500)
    - src/components/ui/menu/FavoriteButton.tsx (line ~191 for ring-red-500)
    - src/components/ui/admin/ops/OpsKPIGrid.tsx (lines ~39,57,63 for ring-amber/blue/green-500)
    - src/components/ui/layout/MobileDrawer/DrawerUserSection.tsx (line ~72 for ring-white/ring-zinc-900)
    - src/components/ui/layout/AppHeader/AccountIndicator.tsx (line ~249 for ring-white/ring-zinc-950)
    - src/components/ui/layout/AppHeader/CartIndicator.tsx (line ~121 for ring-white/ring-zinc-950)
    - .planning/phases/113-accessibility-design-system/113-CONTEXT.md (D-15, D-16)
  </read_first>
  <action>
**Ring color migrations (per D-15, D-16):**

ClearCartConfirmation.tsx:
- Line ~152: `"focus-visible:ring-zinc-400 focus-visible:ring-offset-2"` → `"focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` (cancel button — neutral action gets ring-primary)
- Line ~172: `"focus-visible:ring-red-500 focus-visible:ring-offset-2"` → `"focus-visible:ring-2 focus-visible:ring-status-error focus-visible:ring-offset-2"` (clear/destructive button)

CartDrawerParts.tsx:
- Line ~152: `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-500 focus-visible:ring-offset-2"` → `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-status-error focus-visible:ring-offset-2"`

CartItem.tsx:
- Line ~236: `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-500 focus-visible:ring-offset-2"` → `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-status-error focus-visible:ring-offset-2"`

ValidationOverlay.tsx:
- Line ~98: `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-500 focus-visible:ring-offset-2"` → `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-status-error focus-visible:ring-offset-2"`

CartPageHeader.tsx:
- Line ~96: `"focus-visible:ring-red-500 focus-visible:ring-offset-2"` → `"focus-visible:ring-2 focus-visible:ring-status-error focus-visible:ring-offset-2"`

CheckoutGate.tsx:
- Line ~72: `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-500 focus-visible:ring-offset-2"` → `"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-status-warning focus-visible:ring-offset-2"`

FavoriteButton.tsx:
- Line ~191: `"focus-visible:ring-red-500 focus-visible:ring-offset-2"` → `"focus-visible:ring-2 focus-visible:ring-status-error focus-visible:ring-offset-2"` (heart/favorite uses error red token)

OpsKPIGrid.tsx:
- Line ~39: `activeRing: "ring-amber-500"` → `activeRing: "ring-status-warning"`
- Line ~57: `activeRing: "ring-blue-500"` → `activeRing: "ring-primary"`
- Line ~63: `activeRing: "ring-green-500"` → `activeRing: "ring-status-success"`

**Avatar border ring migrations (decorative, not focus — per D-15):**

DrawerUserSection.tsx:
- Line ~72: `"ring-2 ring-white dark:ring-zinc-900"` → `"ring-2 ring-surface-primary"` (CSS custom property works in both themes)

AccountIndicator.tsx:
- Line ~249: `"ring-2 ring-white dark:ring-zinc-950"` → `"ring-2 ring-surface-primary"`

CartIndicator.tsx:
- Line ~121: `"ring-2 ring-white dark:ring-zinc-950"` → `"ring-2 ring-surface-primary"`

CRITICAL — DO NOT MODIFY:
- Any component's functional behavior, event handlers, or state management
- Any aria-* attributes
- Any layout or sizing (only ring/focus classes change)
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && npx grep-cli "ring-(red|zinc|blue|green|amber)-[0-9]" src/components/ui/cart/ src/components/ui/menu/FavoriteButton.tsx src/components/ui/admin/ops/OpsKPIGrid.tsx src/components/ui/layout/MobileDrawer/ src/components/ui/layout/AppHeader/ 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `ring-red-500` in: ClearCartConfirmation.tsx, CartDrawerParts.tsx, CartItem.tsx, ValidationOverlay.tsx, CartPageHeader.tsx, FavoriteButton.tsx
    - Zero occurrences of `ring-zinc-400` in ClearCartConfirmation.tsx
    - Zero occurrences of `ring-amber-500` in OpsKPIGrid.tsx and CheckoutGate.tsx
    - Zero occurrences of `ring-blue-500` in OpsKPIGrid.tsx
    - Zero occurrences of `ring-green-500` in OpsKPIGrid.tsx
    - Zero occurrences of `ring-white` or `ring-zinc-900` or `ring-zinc-950` in DrawerUserSection.tsx, AccountIndicator.tsx, CartIndicator.tsx
    - All replacements use semantic tokens: `ring-status-error`, `ring-status-warning`, `ring-status-success`, `ring-primary`, `ring-surface-primary`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All hardcoded Tailwind ring colors migrated to semantic tokens, dark mode works without fallback, zero ring-red/zinc/blue/green/amber-NNN patterns remain in modified files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies focus indicator styling and color tokens — no new API routes, authentication flows, or data storage.

| Boundary | Description |
|----------|-------------|
| None | Pure CSS class changes on existing components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02 | N/A | UI components (20 files) | accept | No attack surface — CSS class string replacements only. No data flow, auth, or input handling modified. |
</threat_model>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` passes
- grep for `ring-ring` in src/components/ui/ returns zero results
- grep for `ring-red-500` in modified files returns zero results
- grep for `focus:ring` (without -visible) in modified files returns zero results (except focus:ring-primary/20 and similar sub-ring patterns in files NOT modified by this plan)
</verification>

<success_criteria>
- Three standardized focus patterns established: standard (ring-2), small (ring-1), animated (shadow-focus)
- All legacy ring-ring tokens migrated to ring-primary
- All hardcoded ring colors migrated to semantic tokens
- All focus: prefixes changed to focus-visible: in modified files
- Zero type errors, zero lint errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/113-accessibility-design-system/113-02-SUMMARY.md`
</output>
