---
phase: 49-branded-404-error-pages
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/app/not-found.tsx
  - src/app/(admin)/admin/not-found.tsx
  - src/app/(driver)/driver/not-found.tsx
  - src/components/ui/RouteError.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting any non-existent URL shows branded 404 page with mascot emoji, animated sunset gradient, floating food emojis, and navigation card grid"
    - "Root not-found.tsx renders full-screen takeover (no app shell) with customer navigation cards (Home, Menu, Orders)"
    - "Admin not-found.tsx renders full-screen takeover with admin navigation cards (Dashboard, Orders, Drivers)"
    - "Driver not-found.tsx renders full-screen takeover with driver navigation cards (Dashboard, Routes, History)"
    - "Error pages display food-themed contextual messaging (food puns, not generic text)"
    - "RouteError shows food-themed copy and emoji mascot matching error type while keeping retry/go-home functionality"
    - "404 page provides working links to home, menu, and orders pages"
  artifacts:
    - path: "src/app/not-found.tsx"
      provides: "Root branded 404 page with customer nav cards"
      min_lines: 20
    - path: "src/app/(admin)/admin/not-found.tsx"
      provides: "Admin branded 404 page with admin nav cards"
      min_lines: 15
    - path: "src/app/(driver)/driver/not-found.tsx"
      provides: "Driver branded 404 page with driver nav cards"
      min_lines: 15
    - path: "src/components/ui/RouteError.tsx"
      provides: "Food-themed error boundary with emoji mascot"
      min_lines: 60
  key_links:
    - from: "src/app/not-found.tsx"
      to: "src/components/ui/error-pages/"
      via: "imports ErrorPageShell, ErrorMascot, NavigationCardGrid"
      pattern: "error-pages"
    - from: "src/app/(admin)/admin/not-found.tsx"
      to: "src/components/ui/error-pages/"
      via: "imports ErrorPageShell, ErrorMascot, NavigationCardGrid"
      pattern: "error-pages"
    - from: "src/components/ui/RouteError.tsx"
      to: "src/components/ui/error-pages/ErrorMascot.tsx"
      via: "imports ErrorMascot for emoji display"
      pattern: "ErrorMascot"
---

<objective>
Assemble the error page components from Plan 01 into three branded not-found.tsx pages (root, admin, driver) and upgrade the RouteError component with food-themed personality.

Purpose: Users who hit dead ends see delightful branded pages with food puns, emoji mascots, and portal-specific navigation that guides them back. All 14 existing error.tsx files benefit automatically from RouteError upgrade.
Output: 3 not-found.tsx files (new/modified), 1 upgraded RouteError.tsx.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-branded-404-error-pages/49-01-SUMMARY.md
@src/app/not-found.tsx
@src/components/ui/RouteError.tsx
@src/components/ui/error-pages/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded not-found.tsx pages for all portals</name>
  <files>
    src/app/not-found.tsx
    src/app/(admin)/admin/not-found.tsx
    src/app/(driver)/driver/not-found.tsx
  </files>
  <action>
    **Root not-found.tsx** (~30 lines):
    Replace the existing 8-line generic page with a branded full-screen 404 page.
    - Server component (no `'use client'`).
    - Import `ErrorPageShell`, `ErrorMascot`, `NavigationCardGrid` from `@/components/ui/error-pages`.
    - Structure:
      ```tsx
      export default function NotFound() {
        return (
          <ErrorPageShell>
            <ErrorMascot errorType="not-found" />
            <h1> ... food pun headline ... </h1>
            <p> ... food-themed subtext ... </p>
            <NavigationCardGrid portal="customer" />
          </ErrorPageShell>
        );
      }
      ```
    - Food pun headline: something like "This dish got lost in delivery!" or "We dropped this plate somewhere..." (Claude's discretion — lean into food humor).
    - Subtext: guide the user to try the navigation cards below.
    - Headline: `text-2xl sm:text-3xl font-display font-bold text-white mb-2 text-center`.
    - Subtext: `text-base text-white/80 mb-8 text-center max-w-md`.
    - No `<Button>` component. No Framer Motion.

    **Admin not-found.tsx** (~20 lines):
    - NEW file at `src/app/(admin)/admin/not-found.tsx`.
    - Same structure as root but with `portal="admin"` on NavigationCardGrid.
    - Same food pun headline and subtext (per locked decision: "same copy across portals — only navigation links differ").
    - Server component. No `'use client'`.

    **Driver not-found.tsx** (~20 lines):
    - NEW file at `src/app/(driver)/driver/not-found.tsx`.
    - Same structure as root but with `portal="driver"` on NavigationCardGrid.
    - Same headline/subtext.
    - Server component. No `'use client'`.

    NOTE on Next.js not-found behavior: Root `not-found.tsx` catches ALL unmatched URLs globally. The admin and driver `not-found.tsx` files only activate when `notFound()` is called programmatically within those route segments. This is correct and expected — per-portal files handle programmatic 404s within their segments.

    CONSTRAINTS:
    - Zero Framer Motion imports.
    - No `<Button>` component (imports Framer Motion).
    - All three files are server components.
    - Use food puns — not generic "Page Not Found" text.
  </action>
  <verify>
    - All 3 not-found.tsx files exist
    - `grep -r "framer-motion" src/app/not-found.tsx src/app/\(admin\)/admin/not-found.tsx src/app/\(driver\)/driver/not-found.tsx` returns nothing
    - `grep -r "'use client'" src/app/not-found.tsx src/app/\(admin\)/admin/not-found.tsx src/app/\(driver\)/driver/not-found.tsx` returns nothing
    - `pnpm typecheck` passes
  </verify>
  <done>Three branded not-found.tsx pages exist with food puns, emoji mascots, sunset gradient backgrounds, floating food emojis, and portal-specific navigation cards</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade RouteError with food-themed personality</name>
  <files>src/components/ui/RouteError.tsx</files>
  <action>
    Upgrade the existing RouteError component to add food-themed personality while preserving ALL existing functionality (Sentry reporting, retry counter, go-home emphasis logic, Button component usage).

    Changes to make:

    1. **Import ErrorMascot** from `@/components/ui/error-pages` (just the ErrorMascot component — RouteError stays within app shell, no full-screen takeover).

    2. **Replace the AlertTriangle icon block** with `<ErrorMascot errorType="server-error" />`. The ErrorMascot renders the 🤯 emoji at large size with bobbing animation. Remove the `<AlertTriangle>` icon circle div (lines ~47-49 in current file).

    3. **Update heading copy** from "Oops, we hit a bump!" to a food pun like "Kitchen meltdown!" or "The chef dropped the wok!" (Claude's discretion — keep it food-themed per locked decision).

    4. **Update body copy** to be food-themed per error type. The context-based message should use food metaphors:
       - With context: something like `We burned the ${context}. Give it another shot!` or `The ${context} fell off the tray. Let's try again!`
       - Without context: "Something boiled over in the kitchen. Give it another shot!"

    5. **Keep ALL existing functionality intact:**
       - `'use client'` directive stays (RouteError uses hooks).
       - Sentry reporting stays.
       - `retryCount` useRef and `handleRetry` function stay.
       - `showHomeEmphasis` state stays.
       - Both Button components (Try Again + Go Home) stay exactly as they are.
       - dev-mode error stack display stays.
       - Image logo stays.

    6. **Do NOT change the layout structure.** RouteError renders within app shell (`min-h-[60vh]`), not full-screen. This is per locked decision: "Route error pages (error.tsx) = within app shell (header/nav remain, less jarring)."

    The net diff should be small: replace AlertTriangle icon with ErrorMascot, change 2-3 strings of copy text.

    CONSTRAINTS:
    - RouteError KEEPS `'use client'` (it uses hooks).
    - RouteError KEEPS the `<Button>` component (it's inside app shell where Framer Motion is safe).
    - Do NOT add ErrorPageShell or FloatingFoodEmojis to RouteError (those are for full-screen 404 pages only).
    - File must stay under 400 lines.
  </action>
  <verify>
    - `grep "ErrorMascot" src/components/ui/RouteError.tsx` returns a match
    - `grep "AlertTriangle" src/components/ui/RouteError.tsx` returns nothing (replaced)
    - `grep "use client" src/components/ui/RouteError.tsx` returns a match (still a client component)
    - `grep "handleRetry" src/components/ui/RouteError.tsx` returns a match (retry logic preserved)
    - `grep "Sentry" src/components/ui/RouteError.tsx` returns a match (error reporting preserved)
    - `pnpm typecheck` passes
    - `pnpm lint` passes
  </verify>
  <done>RouteError displays food-themed copy with emoji mascot while preserving all existing retry, go-home, and Sentry functionality. All 14 error.tsx files benefit automatically.</done>
</task>

</tasks>

<verification>
```bash
# Not-found pages exist
ls src/app/not-found.tsx
ls "src/app/(admin)/admin/not-found.tsx"
ls "src/app/(driver)/driver/not-found.tsx"

# RouteError upgraded
grep "ErrorMascot" src/components/ui/RouteError.tsx && echo "PASS: mascot" || echo "FAIL"
grep "AlertTriangle" src/components/ui/RouteError.tsx && echo "FAIL: old icon" || echo "PASS: icon replaced"
grep "handleRetry" src/components/ui/RouteError.tsx && echo "PASS: retry preserved" || echo "FAIL"

# No Framer Motion in not-found pages
grep -r "framer-motion" src/app/not-found.tsx "src/app/(admin)/admin/not-found.tsx" "src/app/(driver)/driver/not-found.tsx" && echo "FAIL" || echo "PASS"

# Full build verification
pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build
```
</verification>

<success_criteria>
- Root not-found.tsx shows branded 404 with food pun, mascot, gradient, floating emojis, and customer nav cards
- Admin not-found.tsx shows same branded 404 with admin nav cards
- Driver not-found.tsx shows same branded 404 with driver nav cards
- RouteError shows food-themed copy + emoji mascot while preserving retry/go-home/Sentry
- All 14 existing error.tsx files automatically benefit from RouteError upgrade
- Zero Framer Motion in not-found pages
- Full build passes (lint, typecheck, test, build)
</success_criteria>

<output>
After completion, create `.planning/phases/49-branded-404-error-pages/49-02-SUMMARY.md`
</output>
