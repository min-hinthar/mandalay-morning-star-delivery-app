---
phase: 47-final-lcp-measurement-gap-closure
plan: 06
type: execute
wave: 2
depends_on: [47-04, 47-05]
files_modified:
  - .planning/phases/47-final-lcp-measurement-gap-closure/47-VERIFICATION.md
  - .planning/phases/47-final-lcp-measurement-gap-closure/47-GOAL-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "React Compiler active in production build"
    - "LazyMotion domMax loaded at app root"
    - "All v1.5 follow-up items verified"
    - "Milestone v1.5 ready to close"
  artifacts:
    - path: ".planning/phases/47-final-lcp-measurement-gap-closure/47-VERIFICATION.md"
      provides: "Final verification status"
      contains: "v1.5 READY TO CLOSE"
    - path: ".planning/STATE.md"
      provides: "Updated milestone status"
      contains: "v1.5"
  key_links:
    - from: "next.config.ts"
      to: ".next build output"
      via: "reactCompiler: true"
      pattern: "reactCompiler.*true"
    - from: "src/app/providers.tsx"
      to: "bundle"
      via: "LazyMotion domMax"
      pattern: "LazyMotion.*domMax"
---

<objective>
Verify LazyMotion + React Compiler are active in production, update verification docs, close v1.5 milestone.

Purpose: Final follow-up verification before closing v1.5 milestone.
Output: Updated VERIFICATION.md, GOAL-VERIFICATION.md, STATE.md with v1.5 closure status.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-final-lcp-measurement-gap-closure/47-GOAL-VERIFICATION.md
@.planning/phases/47-final-lcp-measurement-gap-closure/47-VERIFICATION.md
@next.config.ts
@src/app/providers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify React Compiler in production build</name>
  <files>N/A (verification only)</files>
  <action>
Verify React Compiler is active:

1. **Config check** (already confirmed):

   ```bash
   grep -q "reactCompiler: true" next.config.ts && echo "React Compiler enabled in config"
   ```

2. **Build output check**:

   ```bash
   pnpm build 2>&1 | grep -i "compiler\|compiled"
   ```

   Look for: "Compiled successfully" or React Compiler-specific messages.

3. **Bundle analysis** (optional):
   React Compiler removes useCallback/useMemo calls. Check bundle doesn't contain excessive memo wrappers:

   ```bash
   # If build exists
   grep -r "useMemo\|useCallback" .next/static/chunks/ | wc -l
   ```

   Lower count = compiler is optimizing.

4. **Next.js telemetry** (if available):
   Build output may show "React Compiler" enabled.

Record evidence of React Compiler being active.
</action>
<verify>

```bash
grep -q "reactCompiler: true" next.config.ts
echo $?  # Should be 0
```

  </verify>
  <done>React Compiler verified active in production build</done>
</task>

<task type="auto">
  <name>Task 2: Verify LazyMotion in production bundle</name>
  <files>N/A (verification only)</files>
  <action>
Verify LazyMotion domMax is loaded at app root:

1. **Source check** (already confirmed):

   ```bash
   grep -A2 "LazyMotion" src/app/providers.tsx
   ```

   Expected: `<LazyMotion features={domMax} strict>`

2. **Bundle check**:
   After `pnpm build`, verify framer-motion features are code-split:

   ```bash
   # List framer-motion chunks
   find .next/static/chunks -name "*.js" -exec grep -l "framer-motion\|domMax" {} \;
   ```

3. **Runtime check** (optional):
   In browser devtools, check `window.__FRAMER_MOTION__` or similar.

4. **Bundle size**:
   LazyMotion with domMax should be ~15-20KB (vs ~34KB for full motion).
   ```bash
   # Rough check
   ls -la .next/static/chunks/*.js | head -20
   ```

Record evidence of LazyMotion optimization.
</action>
<verify>

```bash
grep -q "LazyMotion" src/app/providers.tsx && grep -q "domMax" src/app/providers.tsx
echo $?  # Should be 0
```

  </verify>
  <done>LazyMotion domMax verified in production bundle</done>
</task>

<task type="auto">
  <name>Task 3: Update verification documents and close milestone</name>
  <files>
    .planning/phases/47-final-lcp-measurement-gap-closure/47-VERIFICATION.md
    .planning/phases/47-final-lcp-measurement-gap-closure/47-GOAL-VERIFICATION.md
    .planning/STATE.md
  </files>
  <action>
Update all verification documents with final status:

**47-GOAL-VERIFICATION.md:**

- Update `status: gaps_found` to `status: gaps_closed`
- Update score to reflect closed gaps
- Mark E2E CI gap as closed
- Mark desktop profile gap as closed (documented)
- Mark Lighthouse persistence gap as verified

**47-VERIFICATION.md:**

- Update status to "v1.5 READY TO CLOSE"
- Add section "Gap Closure Plans (47-04, 47-05, 47-06)"
- Update follow-up checklist with completion status

**STATE.md:**

- Update "Blockers/Concerns" section — mark items complete
- Update milestone status: "v1.5 Performance & Repo Health" → "In Progress" to "Complete"
- Update "Last session" and "Next action"
- Remove or archive pending follow-up items

Example STATE.md update:

```markdown
### Blockers/Concerns

**v1.5 Follow-up Verification Complete** (2026-02-07):

- [x] Cart E2E tests integrated in CI pipeline (47-04)
- [x] Lighthouse CI workflow verified (47-04)
- [x] LazyMotion + React Compiler verified (47-06)
- [x] Documentation reflects deployed state (47-06)
```

  </action>
  <verify>
```bash
grep -q "READY TO CLOSE\|gaps_closed" .planning/phases/47-final-lcp-measurement-gap-closure/47-*.md
grep -q "\[x\]" .planning/STATE.md | head -5
```
  </verify>
  <done>All verification docs updated, v1.5 milestone ready to close</done>
</task>

</tasks>

<verification>
1. React Compiler: `grep "reactCompiler: true" next.config.ts` returns match
2. LazyMotion: `grep "LazyMotion.*domMax" src/app/providers.tsx` returns match
3. VERIFICATION.md contains "READY TO CLOSE"
4. STATE.md blockers all marked [x]
</verification>

<success_criteria>

- React Compiler verified active
- LazyMotion verified optimized
- All gap closure evidence documented
- v1.5 milestone marked ready to close
  </success_criteria>

<output>
After completion, create `.planning/phases/47-final-lcp-measurement-gap-closure/47-06-SUMMARY.md`
</output>
