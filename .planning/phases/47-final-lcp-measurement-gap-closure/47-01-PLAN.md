---
phase: 47-final-lcp-measurement-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .lighthouseci/
  - .planning/phases/47-final-lcp-measurement-gap-closure/47-01-SUMMARY.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Production build completes without Google Fonts 403 error"
    - "Lighthouse CLI measures LCP on homepage with 3 runs"
    - "Lighthouse CLI measures LCP on menu page with 3 runs"
    - "Lighthouse CLI measures LCP on cart and checkout pages"
    - "Both mobile and desktop profiles captured"
  artifacts:
    - path: ".lighthouseci/"
      provides: "Lighthouse JSON reports"
    - path: ".planning/phases/47-final-lcp-measurement-gap-closure/47-01-SUMMARY.md"
      provides: "Measurement results summary"
  key_links:
    - from: "lighthouserc.js"
      to: "pnpm lighthouse"
      via: "LHCI autorun"
      pattern: "startServerCommand.*pnpm start"
---

<objective>
Measure final LCP performance on 4 customer routes after Phases 40-44 optimizations.

Purpose: Establish baseline for v1.5 milestone completion. This is the first comprehensive measurement since Phase 40 (which showed 9.8-11.4s LCP).
Output: Lighthouse reports for homepage, menu, cart, checkout with median LCP values.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-final-lcp-measurement-gap-closure/47-CONTEXT.md
@.planning/phases/47-final-lcp-measurement-gap-closure/47-RESEARCH.md
@lighthouserc.js
@PERFORMANCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production Build Verification</name>
  <files>.next/</files>
  <action>
Run production build to verify no Google Fonts 403 or other build errors.

1. Run `pnpm build`
2. If Google Fonts 403 error occurs:
   - First attempt: Retry the build (transient network issue)
   - If persistent: Check `src/app/layout.tsx` uses `next/font/google` correctly
   - Last resort: Download fonts and convert to `next/font/local` (see RESEARCH.md)
3. Verify build completes with exit code 0
4. Start production server: `pnpm start` (in background)
5. Verify server responds at http://localhost:3000/
  </action>
  <verify>
`pnpm build` exits with code 0.
`curl http://localhost:3000/` returns 200.
  </verify>
  <done>Production build successful, server running at localhost:3000.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse Measurements on 4 Routes</name>
  <files>.lighthouseci/</files>
  <action>
Run Lighthouse CLI using existing `lighthouserc.js` configuration.

1. With production server running from Task 1, run:
   ```bash
   pnpm lighthouse
   ```
   This uses `@lhci/cli autorun` which:
   - Measures 4 URLs: /, /menu, /cart, /checkout
   - Takes 3 runs per URL (median used)
   - Uses mobile throttling (150ms RTT, 1.6Mbps, 4x CPU slowdown)
   - Outputs to `.lighthouseci/` directory

2. Extract LCP values from each route's JSON report:
   - LCP is in `audits['largest-contentful-paint'].numericValue` (milliseconds)
   - Convert to seconds for readability

3. Record median LCP for each route:
   | Route | Target | Measured |
   |-------|--------|----------|
   | Homepage / | < 4.0s | ? |
   | Menu /menu | < 4.0s | ? |
   | Cart /cart | < 3.5s | ? |
   | Checkout /checkout | < 4.5s | ? |

4. If any route fails to load (404, timeout), document the issue.

Note: Order tracking (/orders/[id]/tracking) requires auth + valid order ID. Skip for this measurement per RESEARCH.md recommendation.
  </action>
  <verify>
`.lighthouseci/` directory contains JSON reports.
At least 4 routes measured with 3 runs each.
LCP values extracted for each route.
  </verify>
  <done>Lighthouse measurements complete for homepage, menu, cart, checkout with LCP values recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Create Measurement Summary</name>
  <files>.planning/phases/47-final-lcp-measurement-gap-closure/47-01-SUMMARY.md</files>
  <action>
Create SUMMARY.md documenting measurement results.

Include:
1. Environment (local production build, Lighthouse CI version, date)
2. LCP results table with pass/fail status:
   - Pass: Within tiered thresholds (homepage/menu < 4s, cart < 3.5s, checkout < 4.5s)
   - Fail: Above threshold
3. Overall pass/fail for phase target (< 4s average)
4. Comparison to Phase 40 baseline (homepage 11.4s, menu 9.8s)
5. If LCP > 4s: Note top 3 bottlenecks from Lighthouse diagnostics for REQ-47.9

Use SUMMARY template from @./.claude/get-shit-done/templates/summary.md
  </action>
  <verify>
SUMMARY.md exists with complete LCP table.
Pass/fail status documented.
Phase 40 comparison included.
  </verify>
  <done>Measurement summary created with LCP results and pass/fail status.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm build` completes without error
- [ ] Production server responds at localhost:3000
- [ ] Lighthouse reports generated for 4 routes
- [ ] LCP values extracted and documented
- [ ] SUMMARY.md created with results
</verification>

<success_criteria>
1. Production build successful (no Google Fonts 403)
2. Lighthouse measurements complete for homepage, menu, cart, checkout
3. LCP values documented with comparison to Phase 40 baseline
4. Pass/fail determination based on < 4s target
</success_criteria>

<output>
After completion, create `.planning/phases/47-final-lcp-measurement-gap-closure/47-01-SUMMARY.md`
</output>
