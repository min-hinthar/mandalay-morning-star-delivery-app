---
phase: 78-configurable-business-rules
plan: 04
type: execute
wave: 3
depends_on:
  - 78-01
  - 78-02
files_modified:
  - src/types/delivery.ts
  - src/types/cart.ts
  - src/lib/stores/cart-store.ts
  - src/lib/hooks/useCart.ts
  - src/lib/hooks/useTimeSlot.ts
  - src/components/ui/cart/CartBar.tsx
  - src/components/ui/cart/FreeDeliveryProgress.tsx
  - src/components/ui/checkout/CheckoutSummaryV8.tsx
  - src/components/ui/checkout/TimeSlotPicker/TimeSlotPicker.tsx
  - src/components/ui/checkout/TimeSlotDisplay.tsx
  - src/components/ui/homepage/Hero/HeroContent.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/menu/page.tsx
  - src/app/(customer)/checkout/page.tsx
  - src/lib/stores/__tests__/cart-store.test.ts
autonomous: true
requirements:
  - RULES-08
  - RULES-09

must_haves:
  truths:
    - "No non-test file in src/ imports CUTOFF_DAY, CUTOFF_HOUR, DELIVERY_FEE_CENTS, FREE_DELIVERY_THRESHOLD_CENTS, or TIME_WINDOWS as constants"
    - "Cart bar and free delivery progress display the configured threshold from props, not a hardcoded constant"
    - "TimeSlotPicker receives time windows as props generated from configured start/end hours"
    - "Homepage hero displays configured delivery fee and cutoff schedule dynamically"
    - "Checkout summary displays configured free delivery threshold from props"
    - "Cart store receives fee settings from outside (not from imported constants)"
  artifacts:
    - path: "src/types/delivery.ts"
      provides: "TimeWindow, DeliveryDate, DeliverySelection types only (constants removed)"
    - path: "src/types/cart.ts"
      provides: "CartItem, CartStore types only (fee constants removed)"
    - path: "src/lib/stores/cart-store.ts"
      provides: "Cart store with injectable fee settings"
    - path: "src/lib/hooks/useCart.ts"
      provides: "Cart hook accepting threshold as parameter"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/lib/settings/business-rules.ts"
      via: "getBusinessRules() in server component, passed as props to Hero"
      pattern: "getBusinessRules"
    - from: "src/app/(public)/menu/page.tsx"
      to: "src/lib/settings/business-rules.ts"
      via: "getBusinessRules() in server component, passed as props to MenuContent"
      pattern: "getBusinessRules"
    - from: "src/app/(customer)/checkout/page.tsx"
      to: "business rules via props or server fetch"
      via: "Props from layout or inline fetch"
      pattern: "deliveryFeeCents|freeDeliveryThresholdCents"
---

<objective>
Replace all hardcoded constant imports in client components with dynamic values from server components, and remove dead constant exports from type files.

Purpose: Customer-facing pages display configured delivery fee, cutoff time, and delivery hours dynamically. No component imports business rule constants directly.
Output: All client components receive business rules via props from server components. Constants removed from type files.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-configurable-business-rules/78-RESEARCH.md
@.planning/phases/78-configurable-business-rules/78-01-SUMMARY.md
@.planning/phases/78-configurable-business-rules/78-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/lib/settings/business-rules.ts:
```typescript
export interface BusinessRules {
  cutoffDay: number;
  cutoffHour: number;
  deliveryFeeCents: number;
  freeDeliveryThresholdCents: number;
  deliveryStartHour: number;
  deliveryEndHour: number;
  deliveryRadiusMiles: number;
  maxDeliveryDurationMinutes: number;
  minimumOrderCents: number;
}
export const getBusinessRules: () => Promise<BusinessRules>;
```

From src/lib/settings/generate-time-windows.ts:
```typescript
export function generateTimeWindows(startHour: number, endHour: number): TimeWindow[];
```

<!-- Existing client components that import constants -->
From src/lib/stores/cart-store.ts:
```typescript
import { DELIVERY_FEE_CENTS, FREE_DELIVERY_THRESHOLD_CENTS } from "@/types/cart";
// Used in getEstimatedDeliveryFee(): subtotal >= FREE_DELIVERY_THRESHOLD_CENTS ? 0 : DELIVERY_FEE_CENTS
```

From src/lib/hooks/useCart.ts:
```typescript
import { FREE_DELIVERY_THRESHOLD_CENTS } from "@/types/cart";
// amountToFreeDelivery: Math.max(0, FREE_DELIVERY_THRESHOLD_CENTS - itemsSubtotal)
```

From src/lib/hooks/useTimeSlot.ts:
```typescript
import { TIME_WINDOWS } from "@/types/delivery";
// windows: TIME_WINDOWS in return value
```

From src/components/ui/cart/CartBar.tsx:
```typescript
import { FREE_DELIVERY_THRESHOLD_CENTS } from "@/types/cart";
// Progress bar calculation
```

From src/components/ui/cart/FreeDeliveryProgress.tsx:
```typescript
import { FREE_DELIVERY_THRESHOLD_CENTS } from "@/types/cart";
// Progress bar calculation
```

From src/components/ui/checkout/CheckoutSummaryV8.tsx:
```typescript
import { FREE_DELIVERY_THRESHOLD_CENTS } from "@/types/cart";
// Free delivery progress display
```

From src/components/ui/checkout/TimeSlotPicker/TimeSlotPicker.tsx:
```typescript
import { TIME_WINDOWS } from "@/types/delivery";
// Renders TIME_WINDOWS.map() for slot selection
```

From src/components/ui/checkout/TimeSlotDisplay.tsx:
```typescript
import { TIME_WINDOWS } from "@/types/delivery";
// Finds selected window label
```

Customer pages (server components that must fetch and pass rules):
- src/app/(public)/page.tsx — homepage, passes to Hero
- src/app/(public)/menu/page.tsx — menu, passes to MenuContent (which renders CartBar)
- src/app/(customer)/checkout/page.tsx — checkout (client component, needs rules from layout or API)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate cart store, hooks, and cart/checkout components from constants to props</name>
  <files>
    src/lib/stores/cart-store.ts,
    src/lib/hooks/useCart.ts,
    src/lib/hooks/useTimeSlot.ts,
    src/components/ui/cart/CartBar.tsx,
    src/components/ui/cart/FreeDeliveryProgress.tsx,
    src/components/ui/checkout/CheckoutSummaryV8.tsx,
    src/components/ui/checkout/TimeSlotPicker/TimeSlotPicker.tsx,
    src/components/ui/checkout/TimeSlotDisplay.tsx,
    src/lib/stores/__tests__/cart-store.test.ts
  </files>
  <action>
**Strategy for cart store (Zustand, client-side):** The cart store cannot call `getBusinessRules()` (server-only). Instead:
- Remove `DELIVERY_FEE_CENTS` and `FREE_DELIVERY_THRESHOLD_CENTS` imports
- Change `getEstimatedDeliveryFee()` to accept params: `getEstimatedDeliveryFee(deliveryFeeCents: number, freeDeliveryThresholdCents: number)`
- OR: Add a `setBusinessRules(fee, threshold)` action to the store that client components call on mount with server-provided values. This is cleaner — avoids passing params through every call.
- Recommended approach: Add `deliveryFeeCents: number` and `freeDeliveryThresholdCents: number` fields to the store state (NOT persisted, populated on each page load from server component props). Add `setDeliverySettings(fee: number, threshold: number)` action. `getEstimatedDeliveryFee()` uses store fields. Default to 1500/10000 until set.

**useCart.ts:**
- Remove `FREE_DELIVERY_THRESHOLD_CENTS` import
- Accept `freeDeliveryThresholdCents` as parameter: `useCart(freeDeliveryThresholdCents: number)`
- Use parameter in `amountToFreeDelivery` calculation
- OR if cart store approach is used: read from store state instead

**useTimeSlot.ts:**
- Remove `TIME_WINDOWS` import
- Accept `timeWindows: TimeWindow[]` as parameter: `useTimeSlot(timeWindows: TimeWindow[])`
- Use parameter instead of constant

**CartBar.tsx:**
- Remove `FREE_DELIVERY_THRESHOLD_CENTS` import
- Add `freeDeliveryThresholdCents: number` prop
- Use prop in progress bar calculation

**FreeDeliveryProgress.tsx:**
- Remove `FREE_DELIVERY_THRESHOLD_CENTS` import
- Add `freeDeliveryThresholdCents: number` prop
- Use prop in progress bar calculation

**CheckoutSummaryV8.tsx:**
- Remove `FREE_DELIVERY_THRESHOLD_CENTS` import
- Add `freeDeliveryThresholdCents: number` prop
- Use prop in free delivery progress display

**TimeSlotPicker.tsx:**
- Remove `TIME_WINDOWS` import
- Add `timeWindows: TimeWindow[]` prop
- Use prop in `.map()` for rendering slots and default selection

**TimeSlotDisplay.tsx:**
- Remove `TIME_WINDOWS` import
- Add `timeWindows: TimeWindow[]` prop
- Use prop to find selected window label

**cart-store.test.ts:**
- Remove constant imports, use literal values or mock store initialization
- If store approach used: call `setDeliverySettings(1500, 10000)` in test setup
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- src/lib/stores/__tests__/cart-store.test.ts --run</automated>
  </verify>
  <done>
    - cart-store.ts does NOT import DELIVERY_FEE_CENTS or FREE_DELIVERY_THRESHOLD_CENTS
    - useCart.ts does NOT import FREE_DELIVERY_THRESHOLD_CENTS
    - useTimeSlot.ts does NOT import TIME_WINDOWS
    - All cart/checkout UI components receive business rule values via props
    - Cart store test passes with new approach
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire server components to pass business rules and remove dead constants</name>
  <files>
    src/app/(public)/page.tsx,
    src/app/(public)/menu/page.tsx,
    src/app/(customer)/checkout/page.tsx,
    src/components/ui/homepage/Hero/HeroContent.tsx,
    src/types/delivery.ts,
    src/types/cart.ts
  </files>
  <action>
**Server component wiring — pass getBusinessRules() output as props:**

1. `src/app/(public)/page.tsx` (server component):
   - Import `getBusinessRules` from `@/lib/settings`
   - Call `const rules = await getBusinessRules()` at top of component
   - Pass `deliveryFeeCents={rules.deliveryFeeCents}`, `cutoffDay={rules.cutoffDay}`, `cutoffHour={rules.cutoffHour}` to `<Hero>`
   - Hero already has `HeroContentProps` — extend as needed

2. `src/components/ui/homepage/Hero/HeroContent.tsx`:
   - Add props for `deliveryFeeCents`, `cutoffDay`, `cutoffHour`
   - Display configured schedule: replace hardcoded "Every Saturday" with dynamic text using day name and hour (e.g., "Order by Friday 3:00 PM for Saturday delivery") per user decision
   - Display delivery fee if appropriate (e.g., "$15 delivery, free over $100")
   - Update `Hero.tsx` and `types.ts` as needed to pass props through

3. `src/app/(public)/menu/page.tsx` (server component):
   - Import `getBusinessRules` and `generateTimeWindows` from `@/lib/settings`
   - Fetch rules, generate time windows
   - Pass `freeDeliveryThresholdCents`, `deliveryFeeCents`, `timeWindows` to `<MenuContent>` as props
   - MenuContent passes these through to CartBar and other cart-related components

4. `src/app/(customer)/checkout/page.tsx` (client component):
   - This is a `'use client'` component and cannot call `getBusinessRules()` directly
   - Options: (a) Convert to server component wrapper + client child, or (b) Create a small server layout/wrapper that fetches rules and passes via context/props, or (c) Fetch via a public API endpoint
   - **Recommended approach (a):** Rename current page to `CheckoutClient.tsx`. Create new `page.tsx` as server component that fetches rules and renders `<CheckoutClient rules={rules} timeWindows={timeWindows} />`
   - Pass `deliveryFeeCents`, `freeDeliveryThresholdCents`, `timeWindows` through to CheckoutSummaryV8, TimeSlotPicker, etc.

5. Remove dead constants from type files:
   - `src/types/delivery.ts`: Remove `CUTOFF_DAY`, `CUTOFF_HOUR`, `TIME_WINDOWS` exports. Keep `TIMEZONE`, `TimeWindow`, `DeliveryDate`, `DeliverySelection` types.
   - `src/types/cart.ts`: Remove `DELIVERY_FEE_CENTS`, `FREE_DELIVERY_THRESHOLD_CENTS` exports. Keep `MINIMUM_ORDER_CENTS`, `MAX_ITEM_QUANTITY`, `MAX_CART_ITEMS` (these are UI limits, not business rules). Keep all interfaces.

6. Final verification grep:
   - Run `grep -r "CUTOFF_DAY\|CUTOFF_HOUR\|DELIVERY_FEE_CENTS\|FREE_DELIVERY_THRESHOLD_CENTS\|TIME_WINDOWS" src/ --include="*.ts" --include="*.tsx" | grep -v __tests__ | grep -v node_modules`
   - Must return 0 results (no non-test file imports dead constants)
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test -- --run && pnpm build</automated>
  </verify>
  <done>
    - Homepage hero displays dynamic delivery fee and cutoff schedule from DB
    - Menu page passes business rules to cart components
    - Checkout page passes business rules to summary and time slot components
    - CUTOFF_DAY, CUTOFF_HOUR, TIME_WINDOWS removed from types/delivery.ts
    - DELIVERY_FEE_CENTS, FREE_DELIVERY_THRESHOLD_CENTS removed from types/cart.ts
    - Zero non-test imports of removed constants across src/
    - Full build passes (lint, typecheck, tests, build)
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test -- --run && pnpm build` all pass
- `grep -r "CUTOFF_DAY\|CUTOFF_HOUR\|DELIVERY_FEE_CENTS\|FREE_DELIVERY_THRESHOLD_CENTS\|TIME_WINDOWS" src/ --include="*.ts" --include="*.tsx" | grep -v __tests__ | grep -v node_modules` returns 0 results
- Homepage hero shows dynamic delivery schedule text
- Cart components use props-based fee/threshold values
- TimeSlotPicker renders dynamic time windows from props
</verification>

<success_criteria>
- All customer-facing pages display configured values from the database (not hardcoded constants)
- No production source file imports the removed constants
- Server components fetch business rules and pass as props to client components
- Cart store uses injectable fee settings (not imported constants)
- Full verification suite passes: lint, CSS lint, format, typecheck, tests, build
</success_criteria>

<output>
After completion, create `.planning/phases/78-configurable-business-rules/78-04-SUMMARY.md`
</output>
