---
phase: 78-configurable-business-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/029_business_rules_settings.sql
  - src/lib/settings/index.ts
  - src/lib/settings/business-rules.ts
  - src/lib/settings/generate-time-windows.ts
  - src/lib/settings/__tests__/business-rules.test.ts
  - src/lib/settings/__tests__/generate-time-windows.test.ts
  - src/lib/validations/settings.ts
  - src/components/ui/admin/settings/settings-types.ts
  - src/components/ui/admin/settings/SettingsClient/settings-defaults.ts
  - src/app/api/admin/settings/route.ts
  - src/app/api/admin/settings/restore/route.ts
autonomous: true
requirements:
  - RULES-01
  - RULES-02
  - RULES-03
  - RULES-04
  - RULES-05
  - RULES-07
  - RULES-10

must_haves:
  truths:
    - "getBusinessRules() returns all 9 delivery settings from app_settings table"
    - "generateTimeWindows(start, end) produces 1-hour slots matching current TIME_WINDOWS format"
    - "Settings PATCH for delivery category calls revalidateTag('business-rules')"
    - "New migration adds cutoff_day, cutoff_hour, delivery_start_hour, delivery_end_hour, max_delivery_duration_minutes to app_settings"
    - "Existing DB seed values (599/5000) are updated to match app constants (1500/10000)"
  artifacts:
    - path: "src/lib/settings/business-rules.ts"
      provides: "getBusinessRules() with unstable_cache and tag-based invalidation"
      exports: ["getBusinessRules", "BusinessRules", "BUSINESS_RULES_DEFAULTS"]
    - path: "src/lib/settings/generate-time-windows.ts"
      provides: "Pure function replacing TIME_WINDOWS constant"
      exports: ["generateTimeWindows"]
    - path: "src/lib/settings/index.ts"
      provides: "Barrel re-exports"
      exports: ["getBusinessRules", "BusinessRules", "BUSINESS_RULES_DEFAULTS", "generateTimeWindows"]
    - path: "supabase/migrations/029_business_rules_settings.sql"
      provides: "DB migration for new settings keys + seed fix"
      contains: "INSERT INTO app_settings"
  key_links:
    - from: "src/lib/settings/business-rules.ts"
      to: "app_settings table"
      via: "Supabase query filtered by category=delivery"
      pattern: "from.*app_settings.*eq.*delivery"
    - from: "src/app/api/admin/settings/route.ts"
      to: "business-rules cache"
      via: "revalidateTag('business-rules') after upsert"
      pattern: "revalidateTag.*business-rules"
---

<objective>
Create the business rules foundation: server-side settings reader with cache, dynamic time window generator, DB migration for new keys, schema/type extensions, and cache invalidation wiring.

Purpose: Every subsequent plan depends on `getBusinessRules()` to replace hardcoded constants. This plan creates that function, seeds the database, and ensures cache busting works.
Output: `src/lib/settings/` module, migration 029, extended Zod/type definitions, revalidateTag wiring.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-configurable-business-rules/78-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/delivery.ts:
```typescript
export interface TimeWindow {
  start: string;
  end: string;
  label: string;
}
export const TIME_WINDOWS: TimeWindow[] = [
  { start: "11:00", end: "12:00", label: "11:00 AM - 12:00 PM" },
  // ... 8 total, from 11:00 to 19:00
];
export const CUTOFF_DAY = 5;   // Friday
export const CUTOFF_HOUR = 15;  // 3 PM
```

From src/types/cart.ts:
```typescript
export const MINIMUM_ORDER_CENTS = 2500;
export const DELIVERY_FEE_CENTS = 1500;
export const FREE_DELIVERY_THRESHOLD_CENTS = 10000;
```

From src/lib/validations/settings.ts:
```typescript
export const deliverySettingsSchema = z.object({
  delivery_radius_miles: z.number().min(1).max(100),
  minimum_order_cents: z.number().min(0),
  free_delivery_threshold_cents: z.number().min(0),
  base_delivery_fee_cents: z.number().min(0),
  delivery_cutoff_time: z.string().regex(hhmmRegex).optional(),
  delivery_time_windows: z.array(deliveryTimeWindowSchema).optional(),
  delivery_zones: z.array(deliveryZoneSchema).optional(),
});
```

From src/components/ui/admin/settings/settings-types.ts:
```typescript
export interface DeliverySettings {
  deliveryRadiusMiles: number;
  minimumOrderCents: number;
  freeDeliveryThresholdCents: number;
  baseDeliveryFeeCents: number;
  deliveryCutoffTime: string;
  deliveryTimeWindows: DeliveryTimeWindow[];
  deliveryZones: DeliveryZone[];
}
```

From src/app/api/admin/settings/route.ts:
- PATCH handler upserts to `app_settings` table by category
- Does NOT currently call revalidateTag
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings library and DB migration</name>
  <files>
    src/lib/settings/index.ts,
    src/lib/settings/business-rules.ts,
    src/lib/settings/generate-time-windows.ts,
    src/lib/settings/__tests__/business-rules.test.ts,
    src/lib/settings/__tests__/generate-time-windows.test.ts,
    supabase/migrations/029_business_rules_settings.sql
  </files>
  <action>
1. Create `src/lib/settings/business-rules.ts`:
   - Export `BusinessRules` interface with 9 fields: `cutoffDay` (number 0-6), `cutoffHour` (number 0-23), `deliveryFeeCents`, `freeDeliveryThresholdCents`, `deliveryStartHour`, `deliveryEndHour`, `deliveryRadiusMiles`, `maxDeliveryDurationMinutes`, `minimumOrderCents`
   - Export `BUSINESS_RULES_DEFAULTS` const matching current hardcoded app values: cutoffDay=5, cutoffHour=15, deliveryFeeCents=1500, freeDeliveryThresholdCents=10000, deliveryStartHour=11, deliveryEndHour=19, deliveryRadiusMiles=40, maxDeliveryDurationMinutes=60, minimumOrderCents=2500
   - Export `getBusinessRules` using `unstable_cache` from `next/cache` with key `["business-rules"]` and options `{ tags: ["business-rules"], revalidate: 300 }`
   - Inside: use `createPublicClient()` from `@/lib/supabase/server` to query `app_settings` where `category = 'delivery'`
   - Map DB keys to interface: `cutoff_day` -> `cutoffDay`, `cutoff_hour` -> `cutoffHour`, `base_delivery_fee_cents` -> `deliveryFeeCents`, `free_delivery_threshold_cents` -> `freeDeliveryThresholdCents`, `delivery_start_hour` -> `deliveryStartHour`, `delivery_end_hour` -> `deliveryEndHour`, `delivery_radius_miles` -> `deliveryRadiusMiles`, `max_delivery_duration_minutes` -> `maxDeliveryDurationMinutes`, `minimum_order_cents` -> `minimumOrderCents`
   - On error, return DEFAULTS (per user decision: "app always reads from DB" but graceful degradation on DB error is sensible)
   - Add `'use server'` directive or import `server-only` to prevent client bundle inclusion

2. Create `src/lib/settings/generate-time-windows.ts`:
   - Import `TimeWindow` from `@/types/delivery`
   - Export `generateTimeWindows(startHour: number, endHour: number): TimeWindow[]`
   - Generate 1-hour slots from startHour to endHour (exclusive)
   - Label format: "11:00 AM - 12:00 PM" matching existing TIME_WINDOWS format
   - Handle AM/PM conversion correctly (12 = "12:00 PM", 13 = "1:00 PM")

3. Create `src/lib/settings/index.ts`:
   - Re-export everything from `business-rules.ts` and `generate-time-windows.ts`

4. Create `supabase/migrations/029_business_rules_settings.sql`:
   - INSERT new keys with ON CONFLICT DO NOTHING: `cutoff_day` (5), `cutoff_hour` (15), `delivery_start_hour` (11), `delivery_end_hour` (19), `max_delivery_duration_minutes` (60) — all category 'delivery', JSONB values
   - UPDATE existing mismatched seeds: `base_delivery_fee_cents` from 599 to 1500, `free_delivery_threshold_cents` from 5000 to 10000 (only WHERE current value matches old seed)

5. Create `src/lib/settings/__tests__/business-rules.test.ts`:
   - Mock `@/lib/supabase/server` `createPublicClient` to return controlled data
   - Test: `getBusinessRules()` returns all 9 fields with correct types when DB has data
   - Test: `getBusinessRules()` returns `BUSINESS_RULES_DEFAULTS` when DB query returns error/null
   - Test: `getBusinessRules()` maps DB snake_case keys to camelCase correctly (e.g., `base_delivery_fee_cents` -> `deliveryFeeCents`)
   - Test: `getBusinessRules()` uses fallback defaults for any missing individual keys (partial data scenario)
   - Mock `unstable_cache` as a passthrough (call the inner function directly)

6. Create `src/lib/settings/__tests__/generate-time-windows.test.ts`:
   - Test: `generateTimeWindows(11, 19)` returns exactly 8 windows
   - Test: First window is `{ start: "11:00", end: "12:00", label: "11:00 AM - 12:00 PM" }`
   - Test: Last window is `{ start: "18:00", end: "19:00", label: "6:00 PM - 7:00 PM" }`
   - Test: `generateTimeWindows(9, 12)` returns 3 windows (boundary check)
   - Test: `generateTimeWindows(12, 13)` returns 1 window with correct PM formatting
   - Test: `generateTimeWindows(0, 2)` returns 2 windows with AM formatting (edge case)
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run</automated>
  </verify>
  <done>
    - `getBusinessRules()` is exported and callable from server code
    - `generateTimeWindows(11, 19)` returns 8 windows matching current TIME_WINDOWS output
    - Migration 029 exists with correct INSERT and UPDATE statements
    - Unit tests for `getBusinessRules()` pass (fallback, mapping, partial data)
    - Unit tests for `generateTimeWindows()` pass (8 slots for 11-19, boundary cases, AM/PM formatting)
    - No type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend schemas, types, defaults, and wire cache invalidation</name>
  <files>
    src/lib/validations/settings.ts,
    src/components/ui/admin/settings/settings-types.ts,
    src/components/ui/admin/settings/SettingsClient/settings-defaults.ts,
    src/app/api/admin/settings/route.ts,
    src/app/api/admin/settings/restore/route.ts
  </files>
  <action>
1. Extend `src/lib/validations/settings.ts` `deliverySettingsSchema`:
   - Add `cutoff_day`: `z.number().int().min(0).max(6)` (0=Sunday..6=Saturday)
   - Add `cutoff_hour`: `z.number().int().min(0).max(23)`
   - Add `delivery_start_hour`: `z.number().int().min(0).max(23)`
   - Add `delivery_end_hour`: `z.number().int().min(1).max(24)` (24 = midnight end)
   - Add `max_delivery_duration_minutes`: `z.number().int().min(1).max(480)`
   - Add `.refine()` to ensure `delivery_end_hour > delivery_start_hour` with message "End hour must be after start hour"
   - Remove `delivery_cutoff_time` (replaced by `cutoff_day` + `cutoff_hour`)
   - Remove `delivery_time_windows` (replaced by dynamic generation from start/end hours)

2. Extend `src/components/ui/admin/settings/settings-types.ts` `DeliverySettings` interface:
   - Add `cutoffDay: number`, `cutoffHour: number`, `deliveryStartHour: number`, `deliveryEndHour: number`, `maxDeliveryDurationMinutes: number`
   - Remove `deliveryCutoffTime: string` and `deliveryTimeWindows: DeliveryTimeWindow[]` (replaced by new fields)
   - Keep `DeliveryTimeWindow` type (still used for display purposes)

3. Update `src/components/ui/admin/settings/SettingsClient/settings-defaults.ts`:
   - Add defaults for new fields matching BUSINESS_RULES_DEFAULTS: cutoffDay=5, cutoffHour=15, deliveryStartHour=11, deliveryEndHour=19, maxDeliveryDurationMinutes=60
   - Update baseDeliveryFeeCents default to 1500 (not 599)
   - Update freeDeliveryThresholdCents default to 10000 (not 5000)
   - Remove deliveryCutoffTime and deliveryTimeWindows defaults

4. Wire cache invalidation in `src/app/api/admin/settings/route.ts` PATCH handler:
   - Import `revalidateTag` from `next/cache`
   - After successful upsert loop, add: `if (category === "delivery") { revalidateTag("business-rules"); }`
   - Place BEFORE the return statement

5. Update `src/app/api/admin/settings/restore/route.ts`:
   - Update default values to match app constants (1500/10000, not 599/5000)
   - Add new fields to restore defaults: cutoff_day=5, cutoff_hour=15, delivery_start_hour=11, delivery_end_hour=19, max_delivery_duration_minutes=60
   - Add `revalidateTag("business-rules")` after restore for delivery category
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run</automated>
  </verify>
  <done>
    - Zod schema validates cutoff_day, cutoff_hour, delivery_start_hour, delivery_end_hour, max_delivery_duration_minutes
    - DeliverySettings interface has all new fields
    - PATCH handler calls revalidateTag('business-rules') for delivery category
    - Restore defaults uses 1500/10000, includes new fields
    - No type errors across codebase
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes (no broken imports from schema/type changes)
- `pnpm test -- --run` passes
- `pnpm build` succeeds
- `grep -r "revalidateTag.*business-rules" src/` shows the PATCH and restore handlers
- `src/lib/settings/index.ts` exports `getBusinessRules`, `BusinessRules`, `BUSINESS_RULES_DEFAULTS`, `generateTimeWindows`
</verification>

<success_criteria>
- `getBusinessRules()` is callable from any server context and returns `BusinessRules` object
- `generateTimeWindows(11, 19)` returns 8 `TimeWindow[]` objects matching the current hardcoded array format
- Migration 029 seeds 5 new keys and fixes 2 existing seed values
- Admin settings PATCH for delivery category busts the business-rules cache via revalidateTag
- Zod schema and TypeScript types are extended for all new fields
- Unit tests pass for `getBusinessRules()` (fallback defaults, key mapping) and `generateTimeWindows()` (slot count, formatting)
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/78-configurable-business-rules/78-01-SUMMARY.md`
</output>
