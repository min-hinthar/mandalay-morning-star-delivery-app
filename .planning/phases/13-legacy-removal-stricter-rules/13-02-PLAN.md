---
phase: 13-legacy-removal-stricter-rules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/OrderManagement.tsx
  - src/components/admin/RouteOptimization.tsx
  - src/components/admin/analytics/Charts.tsx
  - src/components/admin/analytics/DriverLeaderboard.tsx
  - src/components/admin/analytics/PeakHoursChart.tsx
  - src/components/auth/MagicLinkSent.tsx
  - src/components/checkout/AddressInput.tsx
  - src/components/checkout/CheckoutStepperV8.tsx
  - src/components/checkout/TimeSlotPicker.tsx
  - src/components/driver/DeliverySuccess.tsx
  - src/components/driver/Leaderboard.tsx
autonomous: true

must_haves:
  truths:
    - "Admin components pass TypeScript strict checks"
    - "Auth, checkout, and driver components have no unused vars"
  artifacts:
    - path: "src/components/admin/**/*.tsx"
      provides: "Admin components without unused React imports"
    - path: "src/components/checkout/**/*.tsx"
      provides: "Checkout components without unused React imports"
  key_links:
    - from: "Component files"
      to: "TypeScript compiler"
      via: "noUnusedLocals check"
---

<objective>
Fix TypeScript unused variable violations in admin, auth, checkout, and driver components.

Purpose: Prepare component layer for TypeScript strict flags by removing unused React imports and prefixing unused variables.

Output: 11 component files with zero unused variable violations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-legacy-removal-stricter-rules/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin and analytics components</name>
  <files>
    - src/components/admin/OrderManagement.tsx
    - src/components/admin/RouteOptimization.tsx
    - src/components/admin/analytics/Charts.tsx
    - src/components/admin/analytics/DriverLeaderboard.tsx
    - src/components/admin/analytics/PeakHoursChart.tsx
  </files>
  <action>
**Pattern A: Remove unused React imports**
Modern JSX transform doesn't need explicit React import. Remove:
```typescript
import React from 'react';
```

**Pattern B: Prefix unused variables**
- `_V5_CHART_COLORS` in DriverLeaderboard.tsx - verify already prefixed
- `entry` param in PeakHoursChart.tsx line 96 - prefix with `_entry`

Check each file with `npx tsc --noUnusedLocals --noUnusedParameters --noEmit [file]` after changes.
  </action>
  <verify>
`npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "admin.*tsx" | wc -l` returns 0
  </verify>
  <done>All 5 admin/analytics component files have no unused variable violations.</done>
</task>

<task type="auto">
  <name>Task 2: Fix auth, checkout, and driver components</name>
  <files>
    - src/components/auth/MagicLinkSent.tsx
    - src/components/checkout/AddressInput.tsx
    - src/components/checkout/CheckoutStepperV8.tsx
    - src/components/checkout/TimeSlotPicker.tsx
    - src/components/driver/DeliverySuccess.tsx
    - src/components/driver/Leaderboard.tsx
  </files>
  <action>
**Pattern A: Remove unused React imports**
Remove `import React from 'react';` from files that don't use React namespace features.

**Pattern B: Prefix unused variables**
- `_Icon` in CheckoutStepperV8.tsx - verify already prefixed

Check each file for other unused variables and prefix with underscore or remove if truly dead.
  </action>
  <verify>
`npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(auth|checkout|driver).*tsx" | wc -l` returns 0
  </verify>
  <done>All 6 auth/checkout/driver component files have no unused variable violations.</done>
</task>

</tasks>

<verification>
Run after all tasks:
```bash
npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(admin|auth|checkout|driver).*tsx" | wc -l
```
Expected: 0 (no errors in these component directories)
</verification>

<success_criteria>
- [ ] No unused React imports in admin, auth, checkout, driver components
- [ ] All unused variables prefixed with underscore
- [ ] 11 files pass `npx tsc --noUnusedLocals --noUnusedParameters --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/13-legacy-removal-stricter-rules/13-02-SUMMARY.md`
</output>
