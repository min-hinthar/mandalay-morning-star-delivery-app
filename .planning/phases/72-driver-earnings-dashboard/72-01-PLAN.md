---
phase: 72-driver-earnings-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/025_driver_pay_rate.sql
  - src/lib/earnings/index.ts
  - src/lib/earnings/compute.ts
  - src/app/api/driver/earnings/route.ts
  - src/components/ui/driver/DriverNav.tsx
  - src/app/(driver)/driver/earnings/page.tsx
  - src/app/(driver)/driver/earnings/loading.tsx
  - src/app/(driver)/driver/schedule/page.tsx
autonomous: true
requirements: [DDASH-01, DDASH-02, DUI-01]

must_haves:
  truths:
    - "Driver pay rate is configurable via app_settings and defaults to 500 cents ($5/stop)"
    - "Earnings API returns per-route earnings breakdown with delivered stop counts and computed cents"
    - "Bottom nav shows 5 tabs: Home, Route, Earnings, Schedule, History"
    - "Earnings and Schedule routes resolve without 404"
  artifacts:
    - path: "supabase/migrations/025_driver_pay_rate.sql"
      provides: "driver_pay_per_stop_cents setting in app_settings"
      contains: "driver_pay_per_stop_cents"
    - path: "src/lib/earnings/compute.ts"
      provides: "Shared earnings computation logic"
      exports: ["computeRouteEarnings", "aggregateByPeriod"]
    - path: "src/app/api/driver/earnings/route.ts"
      provides: "GET endpoint for driver earnings data"
      exports: ["GET"]
    - path: "src/components/ui/driver/DriverNav.tsx"
      provides: "5-tab bottom navigation"
      contains: "Banknote"
  key_links:
    - from: "src/app/api/driver/earnings/route.ts"
      to: "src/lib/earnings/compute.ts"
      via: "import computeRouteEarnings"
      pattern: "computeRouteEarnings"
    - from: "src/app/api/driver/earnings/route.ts"
      to: "app_settings"
      via: "supabase query for driver_pay_per_stop_cents"
      pattern: "driver_pay_per_stop_cents"
    - from: "src/components/ui/driver/DriverNav.tsx"
      to: "/driver/earnings"
      via: "navItems href"
      pattern: "/driver/earnings"
---

<objective>
Foundation for driver earnings: DB migration for pay rate, shared computation library, earnings API, 5-tab bottom nav, and route placeholders.

Purpose: Establishes the data layer and navigation restructure that Plans 02 and 03 depend on.
Output: Working API, expanded navigation, placeholder pages for new routes.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-driver-earnings-dashboard/72-RESEARCH.md
@src/components/ui/driver/DriverNav.tsx
@src/app/(driver)/driver/layout.tsx
@supabase/migrations/010_app_settings.sql
@src/app/api/driver/routes/[routeId]/complete/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + earnings computation library + API endpoint</name>
  <files>
    supabase/migrations/025_driver_pay_rate.sql
    src/lib/earnings/index.ts
    src/lib/earnings/compute.ts
    src/app/api/driver/earnings/route.ts
  </files>
  <action>
    1. **Migration (025_driver_pay_rate.sql):**
       - INSERT `driver_pay_per_stop_cents` with value `500` (= $5/stop), category `operations` into `app_settings`
       - Use `ON CONFLICT (key) DO NOTHING` pattern (matches 010_app_settings.sql)

    2. **Shared computation lib (src/lib/earnings/compute.ts):**
       - Export `computeRouteEarnings(routes, rateCents)` — takes array of routes with nested route_stops, returns per-route earnings: `{ routeId, date, deliveredStops, earningsCents }`
       - Export `aggregateByPeriod(routeEarnings, period)` — groups route earnings by `daily` / `weekly` / `monthly`, returns `{ label: string, value: number }[]` for chart data. Use ISO week (Monday start) for weekly. Format labels: daily="Mon 2/5", weekly="Feb 3-9", monthly="Feb 2026"
       - Export types: `RouteEarning`, `EarningsPeriod`, `ChartDataPoint`
       - Barrel re-export from `src/lib/earnings/index.ts`

    3. **Earnings API (GET /api/driver/earnings):**
       - Use `requireDriver()` auth guard (from `@/lib/auth`)
       - Apply rate limiting via `driverActionLimiter`
       - Accept query param `period` (default: `weekly`), validate against `daily | weekly | monthly`
       - Read `driver_pay_per_stop_cents` from `app_settings` — fallback to 500 if missing
       - Compute date range: daily=14 days, weekly=12 weeks, monthly=12 months from today
       - Query `routes` with nested `route_stops` join where `driver_id` matches and `delivery_date >= startDate`
       - Use `computeRouteEarnings` and `aggregateByPeriod` from shared lib
       - Return JSON: `{ routeEarnings: RouteEarning[], chartData: ChartDataPoint[], rateCents: number, period: string, totalCents: number }`
       - Use the LA timezone pattern from `src/app/(driver)/driver/page.tsx` for date consistency
       - Import pattern: match existing driver API routes (requireDriver, checkRateLimit, logger)
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Migration SQL is valid (INSERT with ON CONFLICT)
    - API route exports GET function
    - Earnings compute functions are exported from barrel index
  </verify>
  <done>
    GET /api/driver/earnings returns JSON with routeEarnings array, chartData array, rateCents, period, and totalCents. Computation correctly multiplies delivered stops by rate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand DriverNav from 3 to 5 tabs + route placeholders</name>
  <files>
    src/components/ui/driver/DriverNav.tsx
    src/app/(driver)/driver/earnings/page.tsx
    src/app/(driver)/driver/earnings/loading.tsx
    src/app/(driver)/driver/schedule/page.tsx
  </files>
  <action>
    1. **DriverNav.tsx — expand navItems from 3 to 5:**
       - Import `Banknote` and `CalendarDays` from `lucide-react`
       - New navItems array (order matters):
         ```
         Home (/driver, exact:true, key:"home")
         Route (/driver/route, exact:false, key:"route")
         Earnings (/driver/earnings, exact:true, key:"earnings")
         Schedule (/driver/schedule, exact:true, key:"schedule")
         History (/driver/history, exact:true, key:"history")
         ```
       - Keep Home tab avatar logic exactly as-is (Phase 71 pattern — do NOT change)
       - All other tabs use standard Icon component pattern (no avatar)
       - Keep existing min-h-[56px] min-w-[64px] touch targets (already fit 5 tabs at 375px)
       - Keep existing layoutId="driver-nav-indicator" for animated pill
       - Keep existing scale-110, strokeWidth 2.5 active states
       - Keep existing badge count support on all tabs

    2. **Earnings placeholder page (src/app/(driver)/driver/earnings/page.tsx):**
       - Server component, minimal placeholder
       - Display: "Earnings" heading with Banknote icon, "Coming soon" message
       - Use same page shell pattern as other driver pages (px-4 py-6 space-y-6)
       - This will be replaced by Plan 03 with the full earnings page

    3. **Earnings loading skeleton (src/app/(driver)/driver/earnings/loading.tsx):**
       - Use `Skeleton` component from `@/components/ui/skeleton/base`
       - Show 2-column stat card skeletons + chart area skeleton + 3 list item skeletons
       - Match driver page loading pattern from `src/app/(driver)/driver/loading.tsx`

    4. **Schedule placeholder page (src/app/(driver)/driver/schedule/page.tsx):**
       - Server component, minimal placeholder
       - Display: "Schedule" heading with CalendarDays icon, "Coming in Phase 73" message
       - Phase 73 will implement the full schedule page
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes (check for semantic token compliance)
    - DriverNav renders 5 tabs with correct icons and hrefs
    - /driver/earnings and /driver/schedule resolve without 404
  </verify>
  <done>
    Bottom nav shows 5 tabs (Home, Route, Earnings, Schedule, History). Tapping Earnings and Schedule navigates to placeholder pages. Active indicator pill animates between all 5 tabs.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm build` — all pass
- Migration file is syntactically valid SQL
- GET /api/driver/earnings returns valid JSON structure
- DriverNav renders 5 tabs, active states work on all tabs
- /driver/earnings and /driver/schedule pages render
- Earnings computation: 3 delivered stops * 500 cents = 1500 cents ($15.00)
</verification>

<success_criteria>
- app_settings has `driver_pay_per_stop_cents` key with default 500
- Earnings API computes per-route and aggregated earnings from route_stops * rate
- DriverNav has 5 tabs with Banknote and CalendarDays icons
- Both new routes render without errors
- All existing driver pages/routes still work (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/72-driver-earnings-dashboard/72-01-SUMMARY.md`
</output>
