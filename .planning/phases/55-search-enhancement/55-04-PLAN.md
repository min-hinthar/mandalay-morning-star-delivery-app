---
phase: 55-search-enhancement
plan: 04
type: execute
wave: 3
depends_on: ["55-02", "55-03"]
files_modified:
  - src/components/ui/search/CommandPalette/CommandPalette.tsx
  - src/components/ui/search/CommandPalette/index.ts
autonomous: false

must_haves:
  truths:
    - "Tapping a search result opens item detail sheet overlay without closing the search palette"
    - "Closing the item detail sheet returns to search palette with query and results intact"
    - "Search state persists across item detail view and back navigation"
    - "Tapping a result navigates to item detail page (via sheet overlay, not route push)"
  artifacts:
    - path: "src/components/ui/search/CommandPalette/CommandPalette.tsx"
      provides: "Integrated ItemDetailSheet overlay within search palette"
      contains: "ItemDetailSheet"
  key_links:
    - from: "CommandPalette"
      to: "ItemDetailSheet"
      via: "selectedItem state opens sheet on top of palette"
      pattern: "ItemDetailSheet.*selectedItem"
    - from: "CommandPalette"
      to: "useCart"
      via: "onAddToCart handler for items viewed from search"
      pattern: "handleAddToCart"
---

<objective>
Integrate ItemDetailSheet into CommandPalette so tapping a result opens the item detail as an overlay without closing the palette, preserving search state. Add final polish and verify all requirements.

Purpose: Search state persistence (locked decision) and final end-to-end verification.
Output: Updated CommandPalette with integrated ItemDetailSheet, full phase verification.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-search-enhancement/55-RESEARCH.md
@.planning/phases/55-search-enhancement/55-01-SUMMARY.md
@.planning/phases/55-search-enhancement/55-02-SUMMARY.md
@.planning/phases/55-search-enhancement/55-03-SUMMARY.md
@src/components/ui/search/CommandPalette/CommandPalette.tsx
@src/components/ui/menu/ItemDetailSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ItemDetailSheet into CommandPalette for search state persistence</name>
  <files>
    src/components/ui/search/CommandPalette/CommandPalette.tsx
    src/components/ui/search/CommandPalette/index.ts
  </files>
  <action>
    1. Update `CommandPalette.tsx`:
       - Import ItemDetailSheet from `@/components/ui/menu`
       - Import useCart from cart store (or useCartStore) for `addItem` functionality
       - Add state: `const [selectedItem, setSelectedItem] = useState<EnrichedMenuItem | null>(null)`
       - Replace `handleSelectItem` with these exact changes:
         1. REMOVE: `router.push('/menu?item=slug')` (no route navigation)
         2. REMOVE: `onOpenChange(false)` (do NOT close the palette)
         3. REMOVE: `setQuery("")` (do NOT clear the query)
         4. ADD: `setSelectedItem(item)` (open item detail as overlay instead)
         5. ADD: `addSearch(item.nameEn)` (save to recent searches)
         - Net effect: palette stays open, query persists, item detail renders on top (locked decision: search state persists)
       - Add `handleCloseItemDetail`: `setSelectedItem(null)` -- returns to search results with query intact
       - Add `handleAddToCart` handler matching ItemDetailSheet's `onAddToCart` signature:
         - Use useCartStore.getState().addItem() or the appropriate cart hook
         - After adding, optionally close the item sheet (keep palette open for continued browsing)
       - Render ItemDetailSheet AFTER the Command component (sibling, not child):
         ```tsx
         <ItemDetailSheet
           item={selectedItem}
           isOpen={!!selectedItem}
           onClose={handleCloseItemDetail}
           onAddToCart={handleAddToCart}
         />
         ```
       - The ItemDetailSheet renders as a modal/drawer overlay on top of the search palette
       - When ItemDetailSheet closes, user is back in the palette with their query and results

    2. Handle edge case: if palette is closed while item detail is open, close both:
       - In `handleOpenChange(false)`: also `setSelectedItem(null)` and `setQuery("")`

    3. Handle sold-out items:
       - When a sold-out item is tapped, still open ItemDetailSheet but the sheet's own sold-out handling will prevent adding to cart (existing behavior)

    4. Update barrel `index.ts` if any new exports needed
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - `pnpm build` passes
    - Tapping a search result opens item detail sheet overlay
    - Closing item detail sheet returns to search with query intact
    - Adding item to cart from search works correctly
    - Closing palette clears all state (query, selected item, active tab)
  </verify>
  <done>
    Tapping a search result opens ItemDetailSheet overlay without closing palette. Query and results preserved while viewing item detail. Closing sheet returns to search. Adding to cart from search works. Search state persists across item detail navigation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete search enhancement with:
    - Fuse.js fuzzy matching ("mohiga" finds "Mohinga")
    - Rich result cards with 64px thumbnails, category badges, dietary tags
    - Category tabs filtering results with tab counts
    - Match text highlighting (amber/yellow background)
    - Sold-out grayed items with badge
    - Popular/Bestseller badges
    - Skeleton loading cards
    - Staggered fade-in animation
    - Enhanced recent searches with individual delete and "Clear all"
    - Clear (X) button in search input
    - "From your orders" section for past order matches
    - No-results state with popular item fallback
    - Item detail sheet overlay preserving search state
    - Category emoji fallback for missing thumbnails
  </what-built>
  <how-to-verify>
    1. Open dev server: `pnpm dev`
    2. Open the command palette (Cmd/Ctrl+K or tap search icon)
    3. Verify empty state shows recent searches (if any) and popular items

    4. **SRCH-01 - Fuzzy matching:**
       - Type "mohiga" → Mohinga should appear in results
       - Type "lahpet" → Laphet Thoke (Tea Leaf Salad) should appear
       - Type "shan" → Shan Noodles should appear

    5. **SRCH-02 - Category grouping:**
       - Type a broad query like "rice" or "curry"
       - Verify category tabs appear above results (e.g., "Soups (2)", "Curries (3)")
       - Tap a category tab → results filter to that category
       - Active tab has solid primary-color background

    6. **SRCH-03 - Thumbnails:**
       - Verify each result card shows a 64px food thumbnail
       - If an item has no image, verify emoji fallback appears

    7. **Rich card details:**
       - Verify matched text has yellow/amber highlight
       - Verify category tag badge on each card
       - Verify dietary/allergen tags show
       - Verify base price displayed (plain number, no "from $X" range)
       - Verify sold-out items are grayed with "Sold Out" badge
       - Verify 8-10 result cards visible before needing to scroll
       - Verify tab switching uses fade transition (not instant swap)

    8. **Search interactions:**
       - Type and verify results appear with stagger animation
       - Clear search with X button in input
       - Verify recent searches appear on next open (previously searched terms)
       - Delete individual recent search via X
       - "Clear all" removes all recent searches

    9. **Item detail persistence:**
       - Search for an item, tap a result
       - Verify item detail sheet opens as overlay
       - Close the item detail sheet
       - Verify search palette still shows with the same query and results

    10. **Mobile (resize browser or test on device):**
        - Verify no Safari crashes (no backdrop-blur on mobile)
        - Verify cards are readable and tappable
        - Verify tab overflow scrolls horizontally
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build`
- Manual verification of all 3 SRCH requirements
- Search state persistence verified
- Mobile and desktop verified
- No regressions in existing palette trigger (Cmd/Ctrl+K, search icon)
</verification>

<success_criteria>
- SRCH-01: "mohiga" finds "Mohinga" (fuzzy matching works)
- SRCH-02: Category tabs appear and filter results
- SRCH-03: 64px food thumbnails display in result cards
- All locked decisions implemented (rich cards, sold-out, popular badges, individual delete, clear button, category tabs, emoji fallback, skeleton, stagger animation, highlight, state persistence, order history section, no-results fallback)
- No deferred ideas implemented (no browse-by-category, no allergen filter UI)
- Full verification suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/55-search-enhancement/55-04-SUMMARY.md`
</output>
