---
phase: 55-search-enhancement
plan: 03
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - src/components/ui/search/CommandPalette/SearchEmptyState.tsx
  - src/components/ui/search/CommandPalette/SearchOrderHistory.tsx
  - src/components/ui/search/CommandPalette/SearchInput.tsx
  - src/components/ui/search/CommandPalette/CommandPalette.tsx
autonomous: true

must_haves:
  truths:
    - "Opening search with empty input shows recent searches (up to 10) and popular items"
    - "Individual recent searches can be deleted via X button"
    - "Clear all button removes all recent searches"
    - "Tapping a recent search fills input and immediately runs the search"
    - "Order history results appear in a separate 'From your orders' section below menu results"
    - "No results state shows message plus popular items to browse"
    - "Search input has a clear (X) button when text is present"
  artifacts:
    - path: "src/components/ui/search/CommandPalette/SearchEmptyState.tsx"
      provides: "Enhanced empty state with individual delete and popular items"
      contains: "SearchEmptyState"
    - path: "src/components/ui/search/CommandPalette/SearchOrderHistory.tsx"
      provides: "Order history search results section"
      contains: "SearchOrderHistory"
  key_links:
    - from: "CommandPalette"
      to: "SearchOrderHistory"
      via: "useOrderHistorySearch results passed as prop"
      pattern: "orderHistoryResults"
    - from: "SearchEmptyState"
      to: "useRecentSearches"
      via: "removeSearch callback for individual deletion"
      pattern: "onRemoveRecent"
---

<objective>
Enhance SearchEmptyState with individual recent search deletion and rich popular items display, create SearchOrderHistory section for past order matches, add clear button to SearchInput, and wire the no-results fallback.

Purpose: Complete the search interaction model -- initial state, order history integration, and empty result handling.
Output: Enhanced SearchEmptyState, new SearchOrderHistory, updated SearchInput with clear button.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-search-enhancement/55-RESEARCH.md
@.planning/phases/55-search-enhancement/55-01-SUMMARY.md
@src/components/ui/search/CommandPalette/SearchEmptyState.tsx
@src/components/ui/search/CommandPalette/SearchInput.tsx
@src/components/ui/search/CommandPalette/CommandPalette.tsx
@src/lib/hooks/useRecentSearches.ts
@src/lib/hooks/useOrderHistorySearch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance SearchEmptyState and SearchInput, create SearchOrderHistory</name>
  <files>
    src/components/ui/search/CommandPalette/SearchEmptyState.tsx
    src/components/ui/search/CommandPalette/SearchOrderHistory.tsx
    src/components/ui/search/CommandPalette/SearchInput.tsx
  </files>
  <action>
    1. Enhance `SearchEmptyState.tsx`:
       - Add `onRemoveRecent: (term: string) => void` to props
       - Recent searches section:
         - Each recent search item gets an X button on the right for individual deletion (locked decision)
         - X button calls `onRemoveRecent(term)` with `e.stopPropagation()` to prevent selecting the search
         - Keep "Clear all" button in the section header (locked decision)
         - Tapping a recent search term: call `onSelectRecent(term)` which fills input AND runs search immediately (discretion: fill + run, per research recommendation)
       - Popular items section:
         - Use enriched items if available, fall back to slug-based lookup
         - Show items with `tags.includes("popular")` from the full enriched items list, not just POPULAR_ITEM_SLUGS
         - If no items have "popular" tag, fall back to POPULAR_ITEM_SLUGS then first 4 items
         - Show 64px thumbnails using SearchResultCard layout (or simplified version matching rich card style)
         - Show "Popular" badge next to popular item names
       - Staggered fade-in animation on recent searches and popular items (existing pattern preserved)

    2. Create `SearchOrderHistory.tsx`:
       - 'use client' directive
       - Props: `{ results: OrderHistorySearchResult[]; onSelectItem: (nameSnapshot: string) => void }` where OrderHistorySearchResult includes the Fuse result shape
       - Render only when results.length > 0
       - Section header: "From your orders" with a shopping bag or history icon, styled like existing section headers (uppercase, muted, small)
       - Each result: Command.Item showing nameSnapshot, quantity, relative date ("2 weeks ago"), with stagger animation
       - Tapping an order history result: fills search input with that item name and searches menu items for it (so user can re-order)
       - Max 5 results shown (already limited in useOrderHistorySearch)
       - Show "Sign in to search your orders" hint for unauthenticated users (optional, controlled by parent)

    3. Update `SearchInput.tsx`:
       - Add `onClear?: () => void` prop
       - When value is non-empty, show an X (clear) button on the right side between the input and ESC hint (locked decision: clear button inside search input)
       - X button: calls `onClear()`, styled with text-text-muted hover:text-text-primary transition
       - Keep ESC hint visible on desktop alongside clear button
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - All files export expected components
    - SearchEmptyState renders with X delete buttons on recent searches
    - SearchInput shows clear X button when text is present
  </verify>
  <done>
    SearchEmptyState supports individual recent search deletion via X buttons and "Clear all". Popular items use tag-based detection with 64px thumbnails. SearchOrderHistory shows "From your orders" section with order item results. SearchInput has clear button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire enhanced empty state, order history, and no-results into CommandPalette</name>
  <files>
    src/components/ui/search/CommandPalette/CommandPalette.tsx
    src/components/ui/search/CommandPalette/index.ts
  </files>
  <action>
    1. Update `CommandPalette.tsx`:
       - Import SearchOrderHistory, useOrderHistorySearch, useAuth
       - Get userId: `const { user } = useAuth()` (already available in scope or import)
       - Call order history search: `const orderHistoryResults = useOrderHistorySearch(query, user?.id)`
       - Pass `removeSearch` from useRecentSearches to SearchEmptyState as `onRemoveRecent`
       - Wire SearchInput clear button: pass `onClear={() => setQuery("")}` to SearchInput
       - Recent search tap behavior: Update `handleSelectRecent` to set query AND trigger search immediately (the query state change will trigger Fuse search via the useMemo dependency, so just `setQuery(term)` is sufficient since Fuse runs synchronously)
       - Render order in Command.List when query has results:
         1. SearchCategoryTabs (from plan 02, or added here if plan 03 runs before 02 -- handle gracefully)
         2. SearchResults (menu item results)
         3. SearchOrderHistory (below menu results, only if results exist)
       - No-results state (locked decision: "No results" + popular items):
         - When query has no fuseResults AND no orderHistoryResults: show "No results for [query]" message PLUS popular items grid below (reuse popular items from SearchEmptyState or a shared component)
         - Replace the existing plain Command.Empty with this enhanced no-results state
       - Initial state (locked decision): recent searches + popular items when search opened with empty input -- already handled by SearchEmptyState

    2. Update `index.ts` barrel:
       - Add exports for SearchOrderHistory
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - `pnpm build` passes
    - Opening search with empty input: shows recent searches with individual X delete + popular items
    - Typing and getting no results: shows "No results" + popular items to browse
    - Searching items that match order history: "From your orders" section appears below menu results
    - Clear (X) button in search input clears the query
    - Deleting individual recent search removes it from the list
    - "Clear all" removes all recent searches
  </verify>
  <done>
    CommandPalette wires enhanced SearchEmptyState with individual deletion, SearchOrderHistory for past order matches below menu results, clear button in SearchInput, and enhanced no-results state with popular item fallback. Full search interaction lifecycle complete.
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm build`
- Empty search shows recent searches (with X delete) + popular items
- Individual recent search deletion works
- "Clear all" works
- Tapping recent search fills and runs search
- Clear button in input clears query
- No results shows message + popular items
- Order history section appears when logged-in user's past orders match query
- Unauthenticated users don't see order history section (graceful absence)
</verification>

<success_criteria>
- Recent searches show with individual X delete and "Clear all"
- Popular items display with thumbnails and popular badge
- "From your orders" section renders below menu results for authenticated users
- No-results fallback shows popular items to browse
- SearchInput has clear (X) button
- All interactions work on mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/55-search-enhancement/55-03-SUMMARY.md`
</output>
