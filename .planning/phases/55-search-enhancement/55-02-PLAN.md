---
phase: 55-search-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - src/components/ui/search/CommandPalette/SearchResultCard.tsx
  - src/components/ui/search/CommandPalette/SearchCategoryTabs.tsx
  - src/components/ui/search/CommandPalette/SearchSkeleton.tsx
  - src/components/ui/search/CommandPalette/HighlightedText.tsx
  - src/components/ui/search/CommandPalette/SearchResults.tsx
  - src/components/ui/search/CommandPalette/CommandPalette.tsx
  - src/components/ui/search/CommandPalette/index.ts
autonomous: true

must_haves:
  truths:
    - "Search results appear as rich cards with 64px thumbnail, name, price, category badge, and dietary tags"
    - "Matched text has accent/yellow background highlight on the matched portion"
    - "Category tabs appear at top of results with only categories containing matches"
    - "Active tab has filled/solid primary-color background"
    - "Sold-out items appear grayed out with Sold Out badge"
    - "Popular items show Popular or Bestseller badge"
    - "Skeleton loading cards appear while search computes"
    - "Results appear with staggered fade-in animation"
    - "8-10 results visible before scrolling"
  artifacts:
    - path: "src/components/ui/search/CommandPalette/SearchResultCard.tsx"
      provides: "Rich result card with thumbnail, badges, tags, highlight"
      contains: "SearchResultCard"
    - path: "src/components/ui/search/CommandPalette/SearchCategoryTabs.tsx"
      provides: "Horizontal scrolling category tab bar"
      contains: "SearchCategoryTabs"
    - path: "src/components/ui/search/CommandPalette/HighlightedText.tsx"
      provides: "Match highlight renderer using Fuse indices"
      contains: "HighlightedText"
    - path: "src/components/ui/search/CommandPalette/SearchSkeleton.tsx"
      provides: "Skeleton card loading states"
      contains: "SearchSkeleton"
  key_links:
    - from: "SearchResultCard"
      to: "HighlightedText"
      via: "renders match highlights in item name"
      pattern: "HighlightedText.*matches"
    - from: "CommandPalette"
      to: "SearchCategoryTabs"
      via: "activeTab state filters displayed results"
      pattern: "activeTab.*setActiveTab"
---

<objective>
Build the rich search result UI: SearchResultCard with thumbnails/badges/tags, match highlighting, category tabs, skeleton loading, and staggered animations.

Purpose: Visual upgrade from plain text rows to rich cards fulfilling SRCH-02 (category grouping) and SRCH-03 (thumbnails).
Output: 4 new components + rewritten SearchResults + updated CommandPalette with category tab state.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-search-enhancement/55-RESEARCH.md
@.planning/phases/55-search-enhancement/55-01-SUMMARY.md
@src/types/menu.ts
@src/components/ui/search/CommandPalette/CommandPalette.tsx
@src/components/ui/search/CommandPalette/SearchResults.tsx
@src/lib/search/search-config.ts
@src/lib/search/use-fuzzy-search.ts
@src/lib/search/category-helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HighlightedText, SearchResultCard, SearchCategoryTabs, and SearchSkeleton components</name>
  <files>
    src/components/ui/search/CommandPalette/HighlightedText.tsx
    src/components/ui/search/CommandPalette/SearchResultCard.tsx
    src/components/ui/search/CommandPalette/SearchCategoryTabs.tsx
    src/components/ui/search/CommandPalette/SearchSkeleton.tsx
  </files>
  <action>
    1. Create `HighlightedText.tsx`:
       - 'use client' directive
       - Props: `{ text: string; matches?: readonly Fuse.FuseResultMatch[]; fieldKey: string }`
       - Filter matches for the given fieldKey
       - Merge overlapping ranges from match indices
       - Render unmatched portions as plain text, matched portions wrapped in `<mark>` with `bg-amber-200 dark:bg-amber-800/50 rounded px-0.5` (accent/yellow background highlight per locked decision)
       - Return plain text if no matches for the field

    2. Create `SearchResultCard.tsx`:
       - 'use client' directive
       - Props: `{ item: EnrichedMenuItem; matches?: readonly Fuse.FuseResultMatch[]; onSelect: (item: EnrichedMenuItem) => void }`
       - Layout: horizontal flex card within Command.Item
       - Left: 64px (w-16 h-16) rounded-xl thumbnail via Next.js Image with `sizes="64px"`, object-cover
         - If no imageUrl: show category-based emoji fallback via `getCategoryEmoji(item._categorySlug)` in a centered 64px div with bg-surface-secondary (locked decision: category emoji fallback)
       - Middle (flex-1):
         - Item name using `<HighlightedText text={item.nameEn} matches={matches} fieldKey="nameEn" />`
         - Below name: category tag badge (small, muted, rounded-full pill with category name)
         - Below badge row: dietary/spice tags from item.tags (small pills, secondary style) and allergen tags from item.allergens
       - Right:
         - Base price via `formatPrice(item.basePriceCents)` (base price only, no "from $X" per locked decision)
         - If `tags.includes("popular")`: show "Popular" badge (small, accent colored) (locked decision: popularity indicator)
       - Sold-out state (locked decision):
         - If `item.isSoldOut`: entire card gets `opacity-50 pointer-events-none` styles, show "Sold Out" badge in red/muted
       - Selected state: `data-[selected=true]:bg-primary/10 dark:data-[selected=true]:bg-primary/20`
       - Use `m.div` with staggered fade-in: `initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }}` with stagger delay based on index (pass index as prop or use custom)

    3. Create `SearchCategoryTabs.tsx`:
       - 'use client' directive
       - Props: `{ tabs: { slug: string; name: string; count: number }[]; activeTab: string | null; onTabChange: (slug: string | null) => void }`
       - Horizontal scrollable container: `overflow-x-auto scrollbar-hide flex gap-1.5 px-3 py-2 border-b border-border/30`
       - "All" tab always first (activeTab === null means All is active)
       - Each tab: pill button with category name + count in parentheses, e.g., "Soups (3)" (discretion: show counts)
       - Active tab: `bg-primary text-primary-foreground` (filled/solid primary-color background per locked decision)
       - Inactive tab: `bg-surface-secondary text-text-secondary hover:bg-surface-hover`
       - Fade crossfade animation when switching tabs (locked decision): use `m.div` with `layoutId="active-tab"` for smooth indicator or simple opacity transition
       - Tab ordering: menu sort order (same as categories array order, per discretion recommendation)

    4. Create `SearchSkeleton.tsx`:
       - 'use client' directive
       - Export `SearchSkeleton` component showing 4-6 skeleton cards mimicking SearchResultCard layout
       - Each skeleton: 64px square pulse, text lines pulse (name width ~60%, price width ~20%, badge width ~30%)
       - Use existing `animate-pulse` pattern from codebase
       - Optional count prop to control number of skeleton cards
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - All 4 new files exist and export their components
  </verify>
  <done>
    HighlightedText renders match highlights from Fuse indices. SearchResultCard shows rich 64px cards with thumbnails, badges, tags, sold-out state, and popularity indicators. SearchCategoryTabs provides horizontal scrolling tab bar with counts. SearchSkeleton shows loading placeholder cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SearchResults and wire category tabs into CommandPalette</name>
  <files>
    src/components/ui/search/CommandPalette/SearchResults.tsx
    src/components/ui/search/CommandPalette/CommandPalette.tsx
    src/components/ui/search/CommandPalette/index.ts
  </files>
  <action>
    1. Rewrite `SearchResults.tsx`:
       - Update props: `{ results: FuseSearchResult[]; onSelect: (item: EnrichedMenuItem) => void }`
       - Remove old compact list layout
       - Render results using `SearchResultCard` for each FuseSearchResult
       - Each card receives `item={result.item}`, `matches={result.matches}`, `onSelect`
       - Wrap in Command.Group with no heading
       - Staggered fade-in animation (locked decision): wrap results in m.div with staggerChildren variant
       - Ensure 8-10 results visible before scrolling (locked decision): set card height so ~8-10 fit in max-h-[60vh]

    2. Update `CommandPalette.tsx` to integrate category tabs:
       - Import SearchCategoryTabs, SearchSkeleton, deriveCategoryTabs from search lib, groupResultsByCategory
       - Add `activeTab` state: `const [activeTab, setActiveTab] = useState<string | null>(null)` (null = "All")
       - Reset activeTab to null when query changes
       - Derive tabs: `const categoryTabs = useMemo(() => deriveCategoryTabs(fuseResults, categories), [fuseResults, categories])`
       - Filter results by active tab: `const displayResults = useMemo(() => activeTab ? fuseResults.filter(r => r.item._categorySlug === activeTab) : fuseResults, [fuseResults, activeTab])`
       - Render SearchCategoryTabs between SearchInput and Command.List (only when query has results)
       - Show SearchSkeleton briefly during initial render or when transitioning (use a short isSearching state if needed, or just show when query exists but results haven't computed -- given Fuse.js is instant on 78 items, this may be near-invisible but satisfies locked decision)
       - Pass displayResults to SearchResults
       - Add crossfade animation between tab switches using AnimatePresence with mode="wait" and key={activeTab}

    3. Update `index.ts` barrel:
       - Add exports for SearchResultCard, SearchCategoryTabs, SearchSkeleton, HighlightedText
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - `pnpm build` passes
    - Search results show as rich cards (64px thumbnails, badges, tags)
    - Category tabs appear above results with active state
    - Switching tabs filters results with crossfade
    - Matched text has yellow/amber highlight
    - Sold-out items show grayed with badge
    - Staggered fade-in on results appearing
  </verify>
  <done>
    SearchResults renders rich cards via SearchResultCard with match highlighting. Category tabs filter results by category. Active tab has solid primary background. Stagger and crossfade animations applied. Skeleton loading state exists. 8-10 cards visible before scrolling.
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm build`
- Search shows rich cards with 64px thumbnails (SRCH-03)
- Category tabs group results (SRCH-02)
- Matched text has amber/yellow background highlight
- "Popular" badge shows on items with popular tag
- Sold-out items grayed out with badge
- Category emoji shows when image missing
- Staggered animation on result cards
- Tab switch shows crossfade
</verification>

<success_criteria>
- Rich card layout with thumbnails, badges, tags replaces compact list
- Category tabs filter by category with count and solid active state
- Match highlighting uses Fuse indices for precise character-level highlight
- Sold-out and popularity visual states implemented
- Skeleton loading cards exist
- All animations use framer-motion m.* components (not motion.*)
</success_criteria>

<output>
After completion, create `.planning/phases/55-search-enhancement/55-02-SUMMARY.md`
</output>
