---
phase: 95-observability-performance-testing-launch-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/delivery.ts
  - src/lib/utils/delivery-dates.ts
  - src/lib/utils/__tests__/delivery-dates.test.ts
  - src/app/(driver)/driver/schedule/page.tsx
  - src/app/api/driver/me/route.ts
  - src/app/(driver)/driver/route/page.tsx
  - src/app/api/driver/earnings/route.ts
  - src/app/(driver)/driver/page.tsx
  - src/app/api/driver/routes/upcoming/route.ts
  - src/app/api/driver/routes/active/route.ts
  - src/components/ui/checkout/TimeSlotPicker/DatePill.tsx
  - src/components/ui/checkout/TimeSlotDisplay.tsx
autonomous: true
requirements:
  - OBS-07
  - OBS-05
  - OBS-02

must_haves:
  truths:
    - "TIMEZONE is read from DELIVERY_TIMEZONE env var with America/Los_Angeles fallback"
    - "All files importing TIMEZONE get the env-var-backed constant from types/delivery.ts"
    - "No file defines its own local TIMEZONE constant"
    - "First 4 menu images are priority-loaded (not lazy) above the fold"
    - "Webhook events log body hash, signature, and event metadata"
  artifacts:
    - path: "src/types/delivery.ts"
      provides: "TIMEZONE constant backed by env var"
      contains: "process.env.DELIVERY_TIMEZONE"
    - path: "src/lib/utils/__tests__/delivery-dates.test.ts"
      provides: "Test coverage for timezone env var fallback"
  key_links:
    - from: "src/types/delivery.ts"
      to: "process.env.DELIVERY_TIMEZONE"
      via: "env var read with fallback"
      pattern: "process\\.env\\.DELIVERY_TIMEZONE.*America/Los_Angeles"
    - from: "src/lib/utils/delivery-dates.ts"
      to: "src/types/delivery.ts"
      via: "import { TIMEZONE }"
      pattern: "import.*TIMEZONE.*from.*types/delivery"
---

<objective>
Consolidate timezone to env var, verify image preloading, and audit webhook logging.

Purpose: Eliminate hardcoded timezone constants scattered across driver/checkout files, confirm existing image priority and webhook logging implementations are complete.
Output: Single env-var-backed TIMEZONE, verified image preloading, verified webhook logging.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-CONTEXT.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-RESEARCH.md

@src/types/delivery.ts
@src/lib/utils/delivery-dates.ts
@src/lib/utils/__tests__/delivery-dates.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate TIMEZONE to env var (OBS-07)</name>
  <files>
    src/types/delivery.ts,
    src/lib/utils/delivery-dates.ts,
    src/lib/utils/__tests__/delivery-dates.test.ts,
    src/app/(driver)/driver/schedule/page.tsx,
    src/app/api/driver/me/route.ts,
    src/app/(driver)/driver/route/page.tsx,
    src/app/api/driver/earnings/route.ts,
    src/app/(driver)/driver/page.tsx,
    src/app/api/driver/routes/upcoming/route.ts,
    src/app/api/driver/routes/active/route.ts,
    src/components/ui/checkout/TimeSlotPicker/DatePill.tsx,
    src/components/ui/checkout/TimeSlotDisplay.tsx
  </files>
  <behavior>
    - Test: TIMEZONE defaults to "America/Los_Angeles" when DELIVERY_TIMEZONE env var is not set
    - Test: TIMEZONE reads from DELIVERY_TIMEZONE env var when set (e.g., "Asia/Yangon")
    - Test: Existing delivery-dates tests still pass with env var approach
  </behavior>
  <action>
    1. In `src/types/delivery.ts`, change line 21 from `export const TIMEZONE = "America/Los_Angeles"` to `export const TIMEZONE = process.env.DELIVERY_TIMEZONE || "America/Los_Angeles"`. This is server-side only (used in delivery-dates.ts and API routes), so module-level read is safe.

    2. Remove duplicate local `const TIMEZONE = "America/Los_Angeles"` declarations from these 6 files, replacing with `import { TIMEZONE } from "@/types/delivery"`:
       - `src/app/(driver)/driver/schedule/page.tsx` (line 10)
       - `src/app/api/driver/me/route.ts` (line 7)
       - `src/app/(driver)/driver/route/page.tsx` (line 9)
       - `src/app/api/driver/earnings/route.ts` (line 8)
       - `src/app/(driver)/driver/page.tsx` (line 8)
       - `src/app/api/driver/routes/upcoming/route.ts` (line 7)
       - `src/app/api/driver/routes/active/route.ts` (line 7)

    3. Verify client-side TIMEZONE imports (DatePill.tsx, TimeSlotDisplay.tsx) -- these are "use client" components that import from `@/types/delivery`. Since Next.js inlines the constant at build time for client components, the env var fallback value gets baked in. This is acceptable -- client formatting is display-only. Document this in a code comment.

    4. Add 2 tests to `delivery-dates.test.ts`:
       - Test that TIMEZONE defaults to "America/Los_Angeles" (vi.stubEnv to clear DELIVERY_TIMEZONE)
       - Test that TIMEZONE reads custom value when DELIVERY_TIMEZONE is set

    NOTE: The `TIMEZONE` import in test files uses `@/types/delivery` -- since vitest runs in Node, `process.env` is read at import time. Use `vi.stubEnv` and dynamic import to test env var behavior.
  </action>
  <verify>
    <automated>pnpm test src/lib/utils/__tests__/delivery-dates.test.ts</automated>
  </verify>
  <done>TIMEZONE constant reads from DELIVERY_TIMEZONE env var with "America/Los_Angeles" fallback. All 7 files with local TIMEZONE declarations now import from types/delivery. Tests confirm env var behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Verify image preloading and webhook logging (OBS-05, OBS-02)</name>
  <files>
    src/components/ui/menu/MenuGrid.tsx,
    src/app/api/webhooks/stripe/route.ts,
    src/app/api/webhooks/resend/route.ts
  </files>
  <action>
    **OBS-05 Image Preload Verification:**
    1. Confirm `MenuGrid.tsx` passes `priority={index < 4}` to menu card components (already at line 80).
    2. Check that FeaturedCarousel and any section carousel components also pass priority for their first items. If they don't pass priority to above-fold items, add `priority={index < 4}` prop.
    3. If any changes needed, add them. If already complete, document in SUMMARY.

    **OBS-02 Webhook Logging Audit:**
    1. Read `src/app/api/webhooks/stripe/route.ts` and verify it logs: event ID, event type, order ID, signature verification result.
    2. Read `src/app/api/webhooks/resend/route.ts` and verify it logs: SHA256 body hash, svix HMAC verification, source IP, event type.
    3. Check both handlers use `webhook_audit_logs` or `webhook_events` table for idempotency.
    4. If any field is missing from logging, add it. If complete, document in SUMMARY.

    Both items are verification-first: read code, confirm completeness, fix gaps if any.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>OBS-05: First 4 menu images confirmed priority-loaded. OBS-02: Both webhook handlers confirmed logging body hash, signature, event ID, type, and metadata. Any gaps documented and fixed.</done>
</task>

</tasks>

<verification>
- `pnpm test src/lib/utils/__tests__/delivery-dates.test.ts` passes with new env var tests
- `pnpm typecheck` passes (no import errors from removed local constants)
- Grep confirms no file defines local `const TIMEZONE = "America/Los_Angeles"` except types/delivery.ts
- `grep -r "const TIMEZONE" src/ --include="*.ts" --include="*.tsx"` returns only types/delivery.ts
</verification>

<success_criteria>
- TIMEZONE backed by DELIVERY_TIMEZONE env var with fallback
- Zero duplicate TIMEZONE declarations across codebase
- Image priority loading verified for above-fold items
- Webhook logging verified complete with hash + signature + metadata
</success_criteria>

<output>
After completion, create `.planning/phases/95-observability-performance-testing-launch-prep/95-01-SUMMARY.md`
</output>
