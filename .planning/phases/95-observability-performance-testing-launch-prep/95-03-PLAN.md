---
phase: 95-observability-performance-testing-launch-prep
plan: 03
type: execute
wave: 1
depends_on:
  - 95-02
files_modified:
  - src/app/api/admin/sections/route.ts
  - src/app/api/admin/sections/[id]/route.ts
  - src/app/api/admin/sections/[id]/items/route.ts
  - src/app/api/admin/sections/publish/route.ts
  - src/app/api/admin/sections/reorder/route.ts
  - src/app/api/admin/sections/most-popular/suggest/route.ts
  - src/app/api/webhooks/stripe/route.ts
  - src/app/api/webhooks/resend/route.ts
  - src/app/api/cron/delivery-reminders/route.ts
  - src/app/api/emails/test/route.ts
  - src/app/api/driver/location/route.ts
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "All admin/sections API routes return errors in {error: {code, message, details?}} format"
    - "Webhook error responses use standardized format"
    - "cron/delivery-reminders, emails/test, and driver/location routes use standardized error format"
  artifacts:
    - path: "src/app/api/admin/sections/route.ts"
      provides: "Standardized error responses for sections CRUD"
    - path: "src/app/api/cron/delivery-reminders/route.ts"
      provides: "Standardized error responses for delivery reminders cron"
    - path: "src/app/api/emails/test/route.ts"
      provides: "Standardized error responses for test email endpoint"
    - path: "src/app/api/driver/location/route.ts"
      provides: "Standardized error responses for driver location"
  key_links:
    - from: "src/app/api/admin/sections/*/route.ts"
      to: "src/lib/utils/api-error.ts"
      via: "import { apiError }"
      pattern: "import.*apiError.*from.*api-error"
    - from: "src/app/api/webhooks/*/route.ts"
      to: "src/lib/utils/api-error.ts"
      via: "import { apiError }"
      pattern: "import.*apiError.*from.*api-error"
---

<objective>
Standardize error responses across admin/sections, webhooks, and remaining non-compliant routes.

Purpose: Complete OBS-01 coverage by migrating all remaining routes with flat error formats. RESEARCH.md identified cron/delivery-reminders, emails/test, and driver/location as non-compliant -- include them here for full coverage.
Output: 11 migrated route files completing the API error standardization.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-CONTEXT.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-RESEARCH.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-02-SUMMARY.md

<interfaces>
<!-- apiError utility created in Plan 02 -->
From src/lib/utils/api-error.ts:
```typescript
export type ApiErrorCode =
  | "UNAUTHORIZED" | "FORBIDDEN" | "NOT_FOUND"
  | "VALIDATION_ERROR" | "RATE_LIMITED" | "INTERNAL_ERROR"
  | "STRIPE_ERROR" | "CONFLICT" | "BAD_REQUEST";

export function apiError(
  code: ApiErrorCode,
  message: string,
  status: number,
  details?: unknown
): NextResponse;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate admin/sections routes to apiError (6 files)</name>
  <files>
    src/app/api/admin/sections/route.ts,
    src/app/api/admin/sections/[id]/route.ts,
    src/app/api/admin/sections/[id]/items/route.ts,
    src/app/api/admin/sections/publish/route.ts,
    src/app/api/admin/sections/reorder/route.ts,
    src/app/api/admin/sections/most-popular/suggest/route.ts
  </files>
  <action>
    In each of the 6 admin/sections route files, replace all `NextResponse.json({ error: "..." }, { status: N })` with `apiError("CODE", "message", N)`:
    - Import: `import { apiError } from "@/lib/utils/api-error";`
    - Same code mapping as Plan 02 Task 2 (401->UNAUTHORIZED, 403->FORBIDDEN, 404->NOT_FOUND, 400->VALIDATION_ERROR/BAD_REQUEST, 500->INTERNAL_ERROR).
    - Preserve the original error message strings.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>All 6 admin/sections route files use apiError() with structured error format.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate webhooks + remaining non-compliant routes (5 files)</name>
  <files>
    src/app/api/webhooks/stripe/route.ts,
    src/app/api/webhooks/resend/route.ts,
    src/app/api/cron/delivery-reminders/route.ts,
    src/app/api/emails/test/route.ts,
    src/app/api/driver/location/route.ts
  </files>
  <action>
    Migrate the 5 remaining non-compliant routes to apiError():

    1. **Webhook routes (2 files):**
       - `webhooks/stripe/route.ts` -- 3 flat error responses
       - `webhooks/resend/route.ts` -- 2 flat error responses
       - Import `apiError` and replace flat errors with structured format.

    2. **Other routes with flat errors (3 files):**
       - `cron/delivery-reminders/route.ts` -- 2 flat error responses
       - `emails/test/route.ts` -- 4 flat error responses
       - `driver/location/route.ts` -- 2 flat error responses
       - These routes were identified in RESEARCH.md as non-compliant but were excluded from the original Plan 02. Include them here for full OBS-01 coverage.

    Same import and code mapping pattern as Plan 02.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>All webhook, cron, email, and driver location routes use apiError(). Zero flat error responses remain in any API route across the entire codebase.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Grep for flat error format across all API routes returns 0: `grep -rn "NextResponse.json({ error: \"" src/app/api/ --include="*.ts"` should return no matches
</verification>

<success_criteria>
- All admin/sections routes (6 files) migrated to structured error format
- Both webhook routes migrated
- 3 previously-excluded routes (cron, emails, driver/location) migrated for full OBS-01 coverage
</success_criteria>

<output>
After completion, create `.planning/phases/95-observability-performance-testing-launch-prep/95-03-SUMMARY.md`
</output>
