---
phase: 95-observability-performance-testing-launch-prep
plan: 04
type: execute
wave: 2
depends_on:
  - 95-02
  - 95-03
files_modified:
  - src/app/(admin)/admin/orders/page.tsx
  - src/app/(admin)/admin/sections/page.tsx
  - src/app/(admin)/admin/orders/[id]/EmailHistory.tsx
  - src/app/(admin)/admin/emails/page.tsx
  - src/components/ui/admin/orders/OrderDetailPage/StatusChangeDialog.tsx
  - src/components/ui/admin/orders/OrderDetailPage/OrderDetailClient.tsx
  - src/components/ui/admin/orders/OrderDetailPage/ManualEmailDialog.tsx
  - src/components/ui/admin/orders/OrderDetailPage/OrderHeaderCard.tsx
  - src/components/ui/admin/sections/DraftBanner.tsx
  - src/components/ui/admin/ops/OpsOrderList.tsx
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "Frontend components consuming admin/orders and admin/sections APIs handle the new error format without showing [object Object]"
    - "extractErrorMessage() is used in all frontend error handlers for backward compatibility"
  artifacts:
    - path: "src/app/(admin)/admin/orders/page.tsx"
      provides: "Updated error handling for admin orders page"
    - path: "src/app/(admin)/admin/sections/page.tsx"
      provides: "Updated error handling for admin sections page"
  key_links:
    - from: "src/app/(admin)/admin/orders/page.tsx"
      to: "src/lib/utils/api-error.ts"
      via: "import { extractErrorMessage }"
      pattern: "extractErrorMessage"
    - from: "src/app/(admin)/admin/sections/page.tsx"
      to: "src/lib/utils/api-error.ts"
      via: "import { extractErrorMessage }"
      pattern: "extractErrorMessage"
---

<objective>
Update frontend error consumers for the new structured error format.

Purpose: After Plans 02-03 changed API error format from `{ error: "string" }` to `{ error: { code, message } }`, frontend code doing `throw new Error(err.error || "fallback")` will show "[object Object]" instead of the message string. This plan fixes all frontend consumers.
Output: ~10 updated frontend files using extractErrorMessage() for backward-compatible error display.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-CONTEXT.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-02-SUMMARY.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-03-SUMMARY.md

<interfaces>
<!-- extractErrorMessage from Plan 02 -->
From src/lib/utils/api-error.ts:
```typescript
/** Extract human-readable message from API error response.
 *  Handles both old flat format and new structured format for backward compat. */
export function extractErrorMessage(data: unknown, fallback: string): string;
```

<!-- Current frontend error extraction (BEFORE) -->
throw new Error(err.error || "Failed to update status");
throw new Error(error.error || "Failed to save section");
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin page error handlers (4 files)</name>
  <files>
    src/app/(admin)/admin/orders/page.tsx,
    src/app/(admin)/admin/sections/page.tsx,
    src/app/(admin)/admin/orders/[id]/EmailHistory.tsx,
    src/app/(admin)/admin/emails/page.tsx
  </files>
  <action>
    CRITICAL: After changing API error format from `{ error: "string" }` to `{ error: { code, message } }`, frontend code doing `throw new Error(err.error || "fallback")` will show "[object Object]" instead of the message string.

    1. Import `extractErrorMessage` from `@/lib/utils/api-error` in each file.

    2. Replace all `err.error || "fallback"` / `error.error || "fallback"` / `data.error || "fallback"` patterns with `extractErrorMessage(err, "fallback")` or `extractErrorMessage(data, "fallback")`.

    3. Specifically update these patterns:
       - `src/app/(admin)/admin/orders/page.tsx:116` -- `err.error || "Failed to update status"` -> `extractErrorMessage(err, "Failed to update status")`
       - `src/app/(admin)/admin/sections/page.tsx:86,213,232` -- 3 occurrences
       - `src/app/(admin)/admin/orders/[id]/EmailHistory.tsx:117,143` -- 2 occurrences
       - `src/app/(admin)/admin/emails/page.tsx:92` -- 1 occurrence

    4. The `extractErrorMessage` function handles both old and new format, so it's backward-compatible if any unconverted routes still return flat strings.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>4 admin page files updated with extractErrorMessage(). No "[object Object]" in error messages for orders and sections pages.</done>
</task>

<task type="auto">
  <name>Task 2: Update admin component error handlers (6 files)</name>
  <files>
    src/components/ui/admin/orders/OrderDetailPage/StatusChangeDialog.tsx,
    src/components/ui/admin/orders/OrderDetailPage/OrderDetailClient.tsx,
    src/components/ui/admin/orders/OrderDetailPage/ManualEmailDialog.tsx,
    src/components/ui/admin/orders/OrderDetailPage/OrderHeaderCard.tsx,
    src/components/ui/admin/sections/DraftBanner.tsx,
    src/components/ui/admin/ops/OpsOrderList.tsx
  </files>
  <action>
    1. Import `extractErrorMessage` from `@/lib/utils/api-error` in each component file that handles errors from admin/orders or admin/sections APIs.

    2. Replace all `err.error || "fallback"` patterns with `extractErrorMessage(err, "fallback")`.

    3. Specifically check and update:
       - `StatusChangeDialog.tsx` -- handles order status update errors
       - `OrderDetailClient.tsx` -- handles order detail fetch errors
       - `ManualEmailDialog.tsx` -- handles email send errors
       - `OrderHeaderCard.tsx` -- handles order header action errors
       - `DraftBanner.tsx` -- handles section publish errors
       - `OpsOrderList.tsx` -- handles ops order action errors

    4. Search for any other frontend files consuming admin/orders or admin/sections APIs and update them too. Use: `grep -rn "\.error || " src/app/\(admin\) src/components/ui/admin --include="*.tsx"` to find all instances.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build</automated>
  </verify>
  <done>All frontend components consuming admin/orders and admin/sections APIs use extractErrorMessage(). Build passes without error toast regressions.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` passes
- Grep for old error access pattern in updated frontend files returns 0
</verification>

<success_criteria>
- ~10 frontend files updated with extractErrorMessage() for compatibility
- Build passes without error toast regressions
- No "[object Object]" possible in error messages from admin routes
</success_criteria>

<output>
After completion, create `.planning/phases/95-observability-performance-testing-launch-prep/95-04-SUMMARY.md`
</output>
