---
phase: 34-full-src-consolidation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/contexts/DriverContrastContext.tsx
autonomous: true

must_haves:
  truths:
    - "app/contexts/ directory exists"
    - "DriverContrastContext.tsx exists in app/contexts/"
    - "File content is identical to original"
  artifacts:
    - path: "src/app/contexts/DriverContrastContext.tsx"
      provides: "Driver high contrast mode context and provider"
      min_lines: 50
  key_links: []
---

<objective>
Create target directory and copy contexts file to app/

Purpose: Prepare app/contexts/ as destination before updating imports (per CONTEXT.md decision to co-locate contexts with app router)
Output: New directory with copied context file ready for import path migration
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-full-src-consolidation/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/contexts directory</name>
  <files>src/app/contexts/</files>
  <action>
    Create the directory structure:
    - src/app/contexts/

    Use mkdir or equivalent to create the directory.
  </action>
  <verify>ls -la src/app/contexts/ shows empty directory</verify>
  <done>Directory src/app/contexts/ exists</done>
</task>

<task type="auto">
  <name>Task 2: Copy DriverContrastContext to new location</name>
  <files>src/app/contexts/DriverContrastContext.tsx</files>
  <action>
    Copy the context file from contexts/ to app/contexts/:
    - src/contexts/DriverContrastContext.tsx → src/app/contexts/DriverContrastContext.tsx

    Do NOT delete original yet - imports still point there.
    Content must be byte-for-byte identical.
  </action>
  <verify>
    diff src/contexts/DriverContrastContext.tsx src/app/contexts/DriverContrastContext.tsx (no output = identical)
  </verify>
  <done>DriverContrastContext.tsx exists in app/contexts/ with identical content</done>
</task>

</tasks>

<verification>
- [ ] src/app/contexts/ directory exists
- [ ] DriverContrastContext.tsx copied and identical to original
- [ ] Original file in contexts/ still exists (not deleted yet)
- [ ] pnpm typecheck passes (no broken imports)
</verification>

<success_criteria>
app/contexts/ contains exact copy of DriverContrastContext.tsx, ready for import migration in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/34-full-src-consolidation/34-04-SUMMARY.md`
</output>
