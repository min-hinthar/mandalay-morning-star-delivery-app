---
phase: 34-full-src-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/design-system/tokens/z-index.ts
  - src/lib/design-system/tokens/motion.ts
autonomous: true

must_haves:
  truths:
    - "lib/design-system/tokens/ directory exists with z-index.ts and motion.ts"
    - "Both files have identical content to originals"
  artifacts:
    - path: "src/lib/design-system/tokens/z-index.ts"
      provides: "Z-index layer system"
      min_lines: 30
    - path: "src/lib/design-system/tokens/motion.ts"
      provides: "Overlay motion configurations"
      min_lines: 30
  key_links: []
---

<objective>
Create target directory structure for design-system migration

Purpose: Prepare lib/design-system/tokens/ as destination before updating imports
Output: New directory with copied token files ready for import path migration
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-full-src-consolidation/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/design-system/tokens directory structure</name>
  <files>src/lib/design-system/tokens/</files>
  <action>
    Create the directory structure:
    - src/lib/design-system/
    - src/lib/design-system/tokens/

    Use mkdir -p or equivalent to create nested directories.
  </action>
  <verify>ls -la src/lib/design-system/tokens/ shows empty directory</verify>
  <done>Directory src/lib/design-system/tokens/ exists</done>
</task>

<task type="auto">
  <name>Task 2: Copy token files to new location</name>
  <files>src/lib/design-system/tokens/z-index.ts, src/lib/design-system/tokens/motion.ts</files>
  <action>
    Copy both token files from design-system/tokens/ to lib/design-system/tokens/:
    - src/design-system/tokens/z-index.ts → src/lib/design-system/tokens/z-index.ts
    - src/design-system/tokens/motion.ts → src/lib/design-system/tokens/motion.ts

    Do NOT delete originals yet - imports still point there.
    Content must be byte-for-byte identical.
  </action>
  <verify>
    diff src/design-system/tokens/z-index.ts src/lib/design-system/tokens/z-index.ts (no output = identical)
    diff src/design-system/tokens/motion.ts src/lib/design-system/tokens/motion.ts (no output = identical)
  </verify>
  <done>Both files exist in lib/design-system/tokens/ with identical content</done>
</task>

</tasks>

<verification>
- [ ] src/lib/design-system/tokens/ directory exists
- [ ] z-index.ts copied and identical to original
- [ ] motion.ts copied and identical to original
- [ ] Original files in design-system/ still exist (not deleted yet)
- [ ] pnpm typecheck passes (no broken imports)
</verification>

<success_criteria>
lib/design-system/tokens/ contains exact copies of both token files, ready for import migration in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/34-full-src-consolidation/34-01-SUMMARY.md`
</output>
