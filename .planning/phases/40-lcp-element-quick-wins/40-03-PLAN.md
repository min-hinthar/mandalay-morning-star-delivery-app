---
phase: 40-lcp-element-quick-wins
plan: 03
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - .planning/STATE.md
  - .planning/phases/40-lcp-element-quick-wins/40-03-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "LCP measured after changes on homepage (mobile)"
    - "LCP measured after changes on menu page (mobile)"
    - "LCP improvement quantified vs baseline"
    - "STATE.md updated with Phase 40 completion"
  artifacts:
    - path: ".planning/phases/40-lcp-element-quick-wins/40-03-SUMMARY.md"
      provides: "Final LCP measurements and phase summary"
      contains: "LCP improvement"
    - path: ".planning/STATE.md"
      provides: "Updated project state"
      contains: "Phase 40"
  key_links: []
---

<objective>
Measure LCP after CardImage optimization and document Phase 40 results.

Purpose: Verify the CardImage conversion achieved the target LCP reduction (8.1s -> 4-5s). Document results for milestone tracking.
Output: Final LCP measurements, improvement quantified, STATE.md updated.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/40-lcp-element-quick-wins/40-01-SUMMARY.md
@.planning/phases/40-lcp-element-quick-wins/40-02-SUMMARY.md
@.planning/phases/40-lcp-element-quick-wins/BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure Post-Optimization LCP</name>
  <files>.planning/phases/40-lcp-element-quick-wins/RESULTS.md</files>
  <action>
Run Lighthouse audit after CardImage optimization:

1. Ensure production build for accurate measurement:

   ```bash
   pnpm build
   pnpm start
   ```

2. Run Lighthouse on homepage (mobile):

   ```bash
   npx lighthouse http://localhost:3000 --output=json --output-path=.planning/phases/40-lcp-element-quick-wins/lighthouse-homepage-mobile-after.json --only-categories=performance --form-factor=mobile --chrome-flags="--headless"
   ```

3. Run Lighthouse on menu page (mobile):

   ```bash
   npx lighthouse http://localhost:3000/menu --output=json --output-path=.planning/phases/40-lcp-element-quick-wins/lighthouse-menu-mobile-after.json --only-categories=performance --form-factor=mobile --chrome-flags="--headless"
   ```

4. Extract LCP values from JSON files:
   - `audits.largest-contentful-paint.numericValue` (in ms)
   - Convert to seconds for comparison

5. Create RESULTS.md with:
   | Page | Before | After | Improvement |
   |------|--------|-------|-------------|
   | Homepage (mobile) | X.Xs | Y.Ys | Z% |
   | Menu (mobile) | X.Xs | Y.Ys | Z% |

6. Note if target (4-5s) achieved or not.

Note: Run Lighthouse 2-3 times and average if results vary significantly.
</action>
<verify>

- lighthouse-\*-after.json files exist
- RESULTS.md contains before/after comparison
- LCP values extracted correctly
  </verify>
  <done>Post-optimization LCP: homepage Ys, menu Ys (X% improvement)</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CardImage optimization - converted raw img to Next.js Image with proper loading strategy</what-built>
  <how-to-verify>
1. Open http://localhost:3000 (ensure `pnpm start` running with production build)
2. Check homepage:
   - Featured section images load quickly (no lazy shimmer on first 4-6 images)
   - Scroll down: later images lazy load (you may see brief placeholder)
   - Hover over card: parallax effect works
3. Navigate to /menu:
   - First 4 menu item images load immediately
   - Scroll down: lazy loading for below-fold items
4. Open DevTools Network tab:
   - Filter by "Img"
   - First images should NOT have `loading="lazy"` in HTML
   - Images should be WebP/AVIF format (check Response Headers: Content-Type)
5. Break an image: Temporarily corrupt an image URL in Supabase
   - Should show emoji fallback
   - (Restore URL after testing)

Expected: Visually identical to before, but faster initial load.
</how-to-verify>
<resume-signal>Type "approved" if images work correctly, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and Create Phase Summary</name>
  <files>.planning/STATE.md, .planning/phases/40-lcp-element-quick-wins/40-03-SUMMARY.md</files>
  <action>
1. Update STATE.md:
   - Current Position: Phase 40 complete, ready for Phase 41
   - Add to Key Decisions (v1.5): "CardImage converted to Next.js Image"
   - Update Tech Debt: LCP from 8.1s to [new value]
   - Update progress bar if format supports it
   - Set next action to Phase 41

2. Create 40-03-SUMMARY.md with:
   - Phase 40 final results
   - LCP before/after table
   - What was changed (CardImage conversion)
   - Target achieved? (4-5s target)
   - Recommendation for Phase 41

3. If target NOT achieved (still > 5s):
   - Document why (maybe JS execution is bigger factor than images)
   - Note that Phase 41-44 address JS bundle size
   - Phase 40 still valuable as image optimization baseline

Use summary template with frontmatter.
</action>
<verify>

- STATE.md updated with Phase 40 completion
- 40-03-SUMMARY.md exists with results
- LCP improvement documented
  </verify>
  <done>Phase 40 complete: LCP reduced from Xs to Ys, STATE.md updated</done>
  </task>

</tasks>

<verification>
- [ ] Lighthouse run on production build (not dev)
- [ ] Before/after LCP comparison documented
- [ ] Human verified images work correctly
- [ ] STATE.md updated
- [ ] 40-03-SUMMARY.md created
- [ ] ROADMAP.md Phase 40 checkbox can be checked
</verification>

<success_criteria>

- LCP measured post-optimization
- Improvement quantified (target: 8.1s -> 4-5s)
- Visual verification passed
- STATE.md reflects Phase 40 completion
- Ready for Phase 41 (Server Component Conversions)
  </success_criteria>

<output>
After completion, create `.planning/phases/40-lcp-element-quick-wins/40-03-SUMMARY.md`
</output>
