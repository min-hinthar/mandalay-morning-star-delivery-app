---
phase: 11-v8-component-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(driver)/driver/page.tsx
autonomous: true

must_haves:
  truths:
    - "Driver dashboard page imports DriverDashboard directly (no v7-index)"
    - "Driver dashboard renders correctly with V8 import"
  artifacts:
    - path: "src/app/(driver)/driver/page.tsx"
      provides: "Driver dashboard page with V8 imports"
      contains: "from \"@/components/driver/DriverDashboard\""
  key_links:
    - from: "src/app/(driver)/driver/page.tsx"
      to: "src/components/driver/DriverDashboard.tsx"
      via: "direct import"
      pattern: "import.*DriverDashboard.*from.*driver/DriverDashboard"
---

<objective>
Migrate driver dashboard page from v7-index barrel import to direct V8 component import.

Purpose: Eliminate v7-index dependency in driver flow, enabling future barrel file removal.
Output: Driver page importing DriverDashboard directly from its source file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v8-component-migration/11-RESEARCH.md

@src/app/(driver)/driver/page.tsx
@src/components/driver/DriverDashboard.tsx
@src/components/driver/v7-index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update driver page imports to direct V8 paths</name>
  <files>src/app/(driver)/driver/page.tsx</files>
  <action>
Update the import statement at line 4:

FROM:
```typescript
import { DriverDashboard } from "@/components/driver/v7-index";
```

TO:
```typescript
import { DriverDashboard } from "@/components/driver/DriverDashboard";
```

No other changes needed - the component usage remains identical.
  </action>
  <verify>
- `grep -r "driver/v7-index" src/app/` returns empty
- `pnpm typecheck` passes
- `pnpm build` completes without errors
  </verify>
  <done>Driver page imports DriverDashboard directly, no v7-index reference remains in driver pages</done>
</task>

<task type="auto">
  <name>Task 2: Verify driver dashboard renders correctly</name>
  <files>src/app/(driver)/driver/page.tsx</files>
  <action>
Run the dev server and verify the driver dashboard loads:

1. Start dev server if not running
2. Navigate to /driver (requires auth)
3. Verify loading state displays correctly
4. Verify dashboard structure renders

Since driver page requires authentication, focus on build verification.
  </action>
  <verify>
- `pnpm build` completes (confirms SSR works with new import)
- No console errors related to DriverDashboard import
  </verify>
  <done>Driver dashboard renders correctly with direct V8 import</done>
</task>

</tasks>

<verification>
- `grep -r "driver/v7-index" src/app/` returns no matches
- `grep "DriverDashboard.*from.*driver/DriverDashboard" src/app/(driver)/driver/page.tsx` returns match
- `pnpm typecheck && pnpm build` passes
</verification>

<success_criteria>
1. Driver page imports DriverDashboard from "@/components/driver/DriverDashboard"
2. No imports from driver/v7-index in src/app/ directory
3. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-v8-component-migration/11-02-SUMMARY.md`
</output>
