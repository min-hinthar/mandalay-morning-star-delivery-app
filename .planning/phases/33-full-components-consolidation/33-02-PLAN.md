---
phase: 33-full-components-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/scroll/AnimatedSection.tsx
  - src/components/scroll/SectionNavDots.tsx
  - src/components/ui/scroll/index.ts
autonomous: true

must_haves:
  truths:
    - "All scroll components available from @/components/ui/scroll"
    - "scroll/ directory no longer exists"
    - "All consumer imports updated to ui/scroll/"
  artifacts:
    - path: "src/components/ui/scroll/AnimatedSection.tsx"
      provides: "Scroll animation component"
    - path: "src/components/ui/scroll/SectionNavDots.tsx"
      provides: "Navigation dots component"
    - path: "src/components/ui/scroll/index.ts"
      provides: "Barrel export including new components"
  key_links:
    - from: "consumers"
      to: "@/components/ui/scroll"
      via: "import"
      pattern: "from.*@/components/ui/scroll"
---

<objective>
Merge scroll/ directory into ui/scroll/.

Purpose: Consolidate duplicate scroll directories - scroll/ has AnimatedSection and SectionNavDots that don't exist in ui/scroll/
Output: Single ui/scroll/ directory with all scroll-related components
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-full-components-consolidation/33-RESEARCH.md
@src/components/ui/scroll/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move scroll/ components to ui/scroll/</name>
  <files>
    src/components/scroll/AnimatedSection.tsx
    src/components/scroll/SectionNavDots.tsx
    src/components/ui/scroll/AnimatedSection.tsx
    src/components/ui/scroll/SectionNavDots.tsx
  </files>
  <action>
    Move these 2 files from scroll/ to ui/scroll/:
    - scroll/AnimatedSection.tsx -> ui/scroll/AnimatedSection.tsx
    - scroll/SectionNavDots.tsx -> ui/scroll/SectionNavDots.tsx

    These are NEW components (don't exist in ui/scroll/), so no duplicate resolution needed.

    Check for any internal imports within the moved files and update if needed.
  </action>
  <verify>
    Files exist in ui/scroll/
    pnpm typecheck passes
  </verify>
  <done>
    Both scroll components moved to ui/scroll/
  </done>
</task>

<task type="auto">
  <name>Task 2: Update barrel export and consumer imports</name>
  <files>
    src/components/ui/scroll/index.ts
  </files>
  <action>
    1. Update ui/scroll/index.ts to export new components:
       export { AnimatedSection } from "./AnimatedSection";
       export { SectionNavDots } from "./SectionNavDots";

    2. Find and update all imports from @/components/scroll:
       grep -rn "from.*@/components/scroll" src/ --include="*.tsx" --include="*.ts"

       Change each to import from @/components/ui/scroll instead.

    3. Delete empty scroll/ directory after all imports updated.
  </action>
  <verify>
    grep -rn "@/components/scroll" src/ returns empty (no old imports)
    ls src/components/scroll/ fails (directory removed)
    pnpm build passes
  </verify>
  <done>
    All imports use ui/scroll/, old scroll/ directory deleted
  </done>
</task>

</tasks>

<verification>
- pnpm build passes
- pnpm typecheck passes
- pnpm lint passes
- No imports from @/components/scroll remain
- scroll/ directory no longer exists
</verification>

<success_criteria>
- AnimatedSection and SectionNavDots available from @/components/ui/scroll
- All consumers import from canonical location
- scroll/ directory completely removed
</success_criteria>

<output>
After completion, create `.planning/phases/33-full-components-consolidation/33-02-SUMMARY.md`
</output>
