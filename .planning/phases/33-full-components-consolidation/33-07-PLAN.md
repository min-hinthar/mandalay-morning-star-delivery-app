---
phase: 33-full-components-consolidation
plan: 07
type: execute
wave: 4
depends_on: ["33-05", "33-06"]
files_modified:
  - src/components/admin/
  - src/components/checkout/
  - src/components/driver/
  - src/components/homepage/
  - src/components/orders/
  - src/components/ui/admin/
  - src/components/ui/checkout/
  - src/components/ui/driver/
  - src/components/ui/homepage/
  - src/components/ui/orders/
autonomous: true

must_haves:
  truths:
    - "Page-specific folders moved to ui/"
    - "admin/, checkout/, driver/, homepage/, orders/ no longer at components root"
    - "All consumer imports updated"
  artifacts:
    - path: "src/components/ui/admin/"
      provides: "Admin components"
    - path: "src/components/ui/checkout/"
      provides: "Checkout components"
    - path: "src/components/ui/driver/"
      provides: "Driver components"
    - path: "src/components/ui/homepage/"
      provides: "Homepage components"
    - path: "src/components/ui/orders/"
      provides: "Orders components"
  key_links:
    - from: "consumers"
      to: "@/components/ui/{feature}"
      via: "import"
      pattern: "from.*@/components/ui/(admin|checkout|driver|homepage|orders)"
---

<objective>
Move page-specific folders (admin, checkout, driver, homepage, orders) to ui/.

Purpose: Consolidate all feature components under ui/ per CONTEXT.md structure
Output: All page-specific components under ui/ subdirectories
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-full-components-consolidation/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move admin/, checkout/, driver/ to ui/</name>
  <files>
    src/components/admin/
    src/components/checkout/
    src/components/driver/
    src/components/ui/admin/
    src/components/ui/checkout/
    src/components/ui/driver/
  </files>
  <action>
    Move entire directories:
    - src/components/admin/ -> src/components/ui/admin/
    - src/components/checkout/ -> src/components/ui/checkout/
    - src/components/driver/ -> src/components/ui/driver/

    These directories have subdirectories (admin/analytics/, admin/drivers/, admin/routes/).
    Move entire tree structure, preserving subdirectories.

    Update any internal imports within moved files to use new paths.

    Note: Unused files in admin/ and driver/ were deleted in Plan 01.
  </action>
  <verify>
    ls src/components/admin fails (moved to ui/)
    ls src/components/ui/admin succeeds
    pnpm typecheck passes
  </verify>
  <done>
    admin/, checkout/, driver/ moved to ui/
  </done>
</task>

<task type="auto">
  <name>Task 2: Move homepage/ and orders/ to ui/</name>
  <files>
    src/components/homepage/
    src/components/orders/
    src/components/ui/homepage/
    src/components/ui/orders/
  </files>
  <action>
    Move entire directories:
    - src/components/homepage/ -> src/components/ui/homepage/
    - src/components/orders/ -> src/components/ui/orders/

    Update any internal imports within moved files.
  </action>
  <verify>
    ls src/components/homepage fails (moved)
    ls src/components/orders fails (moved)
    pnpm typecheck passes
  </verify>
  <done>
    homepage/ and orders/ moved to ui/
  </done>
</task>

<task type="auto">
  <name>Task 3: Update all consumer imports</name>
  <files>
    Various consumer files
  </files>
  <action>
    Find and update all imports for moved directories:

    ```bash
    grep -rn "from.*@/components/admin" src/ --include="*.tsx" --include="*.ts"
    grep -rn "from.*@/components/checkout" src/ --include="*.tsx" --include="*.ts"
    grep -rn "from.*@/components/driver" src/ --include="*.tsx" --include="*.ts"
    grep -rn "from.*@/components/homepage" src/ --include="*.tsx" --include="*.ts"
    grep -rn "from.*@/components/orders" src/ --include="*.tsx" --include="*.ts"
    ```

    For each match:
    - @/components/admin -> @/components/ui/admin
    - @/components/checkout -> @/components/ui/checkout
    - @/components/driver -> @/components/ui/driver
    - @/components/homepage -> @/components/ui/homepage
    - @/components/orders -> @/components/ui/orders

    Create barrel exports (index.ts) for each moved directory if they don't exist.
  </action>
  <verify>
    grep "@/components/(admin|checkout|driver|homepage|orders)[^/]" returns empty
    pnpm build passes
  </verify>
  <done>
    All consumer imports point to ui/ subdirectories
  </done>
</task>

</tasks>

<verification>
- pnpm build passes
- pnpm typecheck passes
- pnpm lint passes
- No imports from old locations remain
- All directories deleted from components/ root
</verification>

<success_criteria>
- admin/, checkout/, driver/, homepage/, orders/ live under ui/
- All consumers import from @/components/ui/{feature}
- Old directories removed
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/33-full-components-consolidation/33-07-SUMMARY.md`
</output>
