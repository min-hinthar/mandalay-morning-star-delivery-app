---
phase: 53-auth-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/actions.ts
  - src/components/ui/auth/index.ts
  - src/components/ui/auth/LoginForm.tsx
  - src/components/ui/auth/SignupForm.tsx
  - src/components/ui/auth/ForgotPasswordForm.tsx
  - src/components/ui/auth/ResetPasswordForm.tsx
  - src/components/ui/auth/__tests__/login-form.test.tsx
  - src/components/ui/auth/__tests__/signup-form.test.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/auth/reset-password/page.tsx
  - src/app/(public)/terms/page.tsx
  - src/app/(public)/privacy/page.tsx
autonomous: true

must_haves:
  truths:
    - "signUp, resetPassword, updatePassword actions no longer exist"
    - "signIn action uses shouldCreateUser: true for unified login/signup"
    - "Signup, forgot-password, and reset-password pages are deleted"
    - "Placeholder /terms and /privacy pages exist and render"
    - "Obsolete auth form components and their tests are deleted"
  artifacts:
    - path: "src/lib/supabase/actions.ts"
      provides: "signInWithMagicLink (unified), signOut only"
      exports: ["signInWithMagicLink", "signOut"]
    - path: "src/app/(public)/terms/page.tsx"
      provides: "Placeholder Terms of Service page"
    - path: "src/app/(public)/privacy/page.tsx"
      provides: "Placeholder Privacy Policy page"
  key_links:
    - from: "src/lib/supabase/actions.ts"
      to: "supabase.auth.signInWithOtp"
      via: "signInWithMagicLink with shouldCreateUser: true"
      pattern: "shouldCreateUser:\\s*true"
---

<objective>
Clean up obsolete auth infrastructure and create placeholder legal pages.

Purpose: Remove password-based auth (per context decision: purely passwordless), consolidate signUp/signIn into single `signInWithMagicLink` action, delete obsolete pages/components/tests, and create placeholder Terms and Privacy pages required by the auth card legal links.

Output: Clean auth surface -- only `signInWithMagicLink` and `signOut` remain. No password fields, no signup/forgot/reset pages. Legal placeholder pages exist.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-auth-experience/53-CONTEXT.md
@.planning/phases/53-auth-experience/53-RESEARCH.md
@src/lib/supabase/actions.ts
@src/components/ui/auth/index.ts
@src/components/ui/auth/UserMenu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete auth files and update actions.ts</name>
  <files>
    src/lib/supabase/actions.ts
    src/components/ui/auth/LoginForm.tsx (DELETE)
    src/components/ui/auth/SignupForm.tsx (DELETE)
    src/components/ui/auth/ForgotPasswordForm.tsx (DELETE)
    src/components/ui/auth/ResetPasswordForm.tsx (DELETE)
    src/components/ui/auth/__tests__/login-form.test.tsx (DELETE)
    src/components/ui/auth/__tests__/signup-form.test.tsx (DELETE)
    src/app/(auth)/signup/page.tsx (DELETE)
    src/app/(auth)/forgot-password/page.tsx (DELETE)
    src/app/auth/reset-password/page.tsx (DELETE)
    src/components/ui/auth/index.ts
  </files>
  <action>
    1. **Delete obsolete files** (10 files):
       - `src/components/ui/auth/LoginForm.tsx` -- replaced by new MagicLinkForm in plan 03
       - `src/components/ui/auth/SignupForm.tsx` -- single page replaces signup
       - `src/components/ui/auth/ForgotPasswordForm.tsx` -- no passwords
       - `src/components/ui/auth/ResetPasswordForm.tsx` -- no passwords
       - `src/components/ui/auth/__tests__/login-form.test.tsx` -- old test
       - `src/components/ui/auth/__tests__/signup-form.test.tsx` -- old test
       - `src/app/(auth)/signup/page.tsx` -- single /login page
       - `src/app/(auth)/forgot-password/page.tsx` -- no passwords
       - `src/app/auth/reset-password/page.tsx` -- no passwords (this is under /auth not /(auth))

    2. **Rewrite `src/lib/supabase/actions.ts`:**
       - Keep: `getAppUrl` helper (private), `ActionResult` interface, `signOut` function
       - Remove: `signUp`, `signIn`, `resetPassword`, `updatePassword` functions
       - Add: `signInWithMagicLink` function that:
         - Accepts `formData: FormData` (email field)
         - Validates email is present
         - Uses `checkRateLimit(email, "signIn")`
         - Calls `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo: \`\${appUrl}/auth/callback\`, shouldCreateUser: true } })`
         - Returns `{ success: "Check your email for a magic link to sign in" }` on success
         - Returns `{ error: error.message }` on failure
       - Export: `signInWithMagicLink`, `signOut`, `ActionResult`
       - Do NOT export `getAppUrl` (keep private)

    3. **Update `src/components/ui/auth/index.ts`:**
       - Remove exports for LoginForm, SignupForm, ForgotPasswordForm, ResetPasswordForm
       - Keep export for UserMenu
       - Barrel will be expanded in plan 05 when new components are ready
  </action>
  <verify>
    - Run `pnpm typecheck` -- should pass (no references to deleted exports remain except pages we'll replace in later plans)
    - Verify deleted files no longer exist: `ls src/components/ui/auth/LoginForm.tsx` should fail
    - Verify `src/lib/supabase/actions.ts` exports only `signInWithMagicLink`, `signOut`, `ActionResult`
  </verify>
  <done>
    - signUp, resetPassword, updatePassword functions deleted
    - signInWithMagicLink exists with shouldCreateUser: true
    - 10 obsolete files deleted
    - Barrel exports only UserMenu
  </done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder Terms and Privacy pages</name>
  <files>
    src/app/(public)/terms/page.tsx
    src/app/(public)/privacy/page.tsx
  </files>
  <action>
    Create two simple placeholder pages in the `(public)` route group:

    1. **`src/app/(public)/terms/page.tsx`:**
       - Server component (no "use client")
       - Export default function `TermsPage`
       - Render a centered container with:
         - `<h1>` "Terms of Service"
         - `<p>` "Terms of Service for Mandalay Morning Star. This page is under construction."
         - Link back to home page using `next/link`
       - Use semantic tokens: `bg-background`, `text-text-primary`, `text-muted-foreground`
       - Add `export const metadata = { title: "Terms of Service | Mandalay Morning Star" }`

    2. **`src/app/(public)/privacy/page.tsx`:**
       - Same structure as Terms page
       - Title: "Privacy Policy"
       - Body: "Privacy Policy for Mandalay Morning Star. This page is under construction."
       - `export const metadata = { title: "Privacy Policy | Mandalay Morning Star" }`

    Both pages: max-w-2xl mx-auto px-4 py-16. Simple, clean, matches app styling.
  </action>
  <verify>
    - `pnpm build` compiles without errors
    - Both pages render at /terms and /privacy (verified by build success since they're server components in existing route group)
  </verify>
  <done>
    - /terms page exists with placeholder content
    - /privacy page exists with placeholder content
    - Both pages use semantic design tokens
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes with no type errors
- `pnpm lint` passes
- `pnpm build` succeeds
- No references to signUp, resetPassword, updatePassword in src/ (except driver onboard route's log message which is just a string)
- Deleted files confirmed absent
</verification>

<success_criteria>
- actions.ts exports only signInWithMagicLink and signOut
- 10 obsolete auth files deleted
- /terms and /privacy placeholder pages exist
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/53-auth-experience/53-01-SUMMARY.md`
</output>
