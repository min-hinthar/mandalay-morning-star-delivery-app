---
phase: 53-auth-experience
plan: 03
type: execute
wave: 2
depends_on: ["53-02"]
files_modified:
  - src/components/ui/auth/MagicLinkForm.tsx
  - src/components/ui/auth/SocialLoginButtons.tsx
  - src/components/ui/auth/OAuthLoadingOverlay.tsx
autonomous: true

must_haves:
  truths:
    - "Email input has floating label that lifts on focus with warm border glow"
    - "Invalid submit triggers animated shake + inline error text"
    - "Submit button shows gradient fill progress animation while sending magic link"
    - "Google and Apple social buttons render as icon-only rounded rectangles side by side"
    - "Clicking a social button shows full-page 'Redirecting to...' overlay"
    - "OAuth errors show toast notification"
  artifacts:
    - path: "src/components/ui/auth/MagicLinkForm.tsx"
      provides: "Email form with floating label, validation, submit progress"
      exports: ["MagicLinkForm"]
    - path: "src/components/ui/auth/SocialLoginButtons.tsx"
      provides: "Google + Apple OAuth icon buttons with hover effects"
      exports: ["SocialLoginButtons"]
    - path: "src/components/ui/auth/OAuthLoadingOverlay.tsx"
      provides: "Full-page overlay for OAuth redirect"
      exports: ["OAuthLoadingOverlay"]
  key_links:
    - from: "src/components/ui/auth/MagicLinkForm.tsx"
      to: "src/lib/supabase/actions.ts"
      via: "signInWithMagicLink server action"
      pattern: "signInWithMagicLink"
    - from: "src/components/ui/auth/SocialLoginButtons.tsx"
      to: "supabase.auth.signInWithOAuth"
      via: "Client-side Supabase call"
      pattern: "signInWithOAuth"
---

<objective>
Build the auth form components: magic link email form, social login buttons, and OAuth loading overlay.

Purpose: These are the interactive elements of the auth card's "form" state. The MagicLinkForm handles email input with premium animations. SocialLoginButtons provide Google/Apple OAuth with icon-only buttons. OAuthLoadingOverlay shows during OAuth redirect.

Output: Three client components ready to be composed inside AuthCard.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-auth-experience/53-CONTEXT.md
@.planning/phases/53-auth-experience/53-RESEARCH.md
@.planning/phases/53-auth-experience/53-02-SUMMARY.md
@src/lib/supabase/actions.ts
@src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MagicLinkForm component</name>
  <files>src/components/ui/auth/MagicLinkForm.tsx</files>
  <action>
    Create `src/components/ui/auth/MagicLinkForm.tsx` (client component):

    1. **Props:**
       ```typescript
       interface MagicLinkFormProps {
         onSuccess: (email: string) => void; // Transitions to confirmation state
       }
       ```

    2. **Form setup:**
       - Use `react-hook-form` with `@hookform/resolvers/zod`
       - Zod schema: `z.object({ email: z.string().email("Please enter a valid email address") })`
       - Single email field

    3. **Floating label input (per context decision: floating label lift + subtle warm border glow):**
       - Use CSS `peer` pattern:
         ```
         <input placeholder=" " className="peer ..." />
         <label className="peer-focus:top-2 peer-focus:text-xs peer-focus:text-primary peer-[:not(:placeholder-shown)]:top-2 ..." />
         ```
       - On focus: label lifts to top, shrinks, turns primary color
       - Border glow on focus: `focus:border-primary focus:ring-2 focus:ring-primary/20` (warm glow)
       - Input styling: `rounded-xl border border-border bg-surface-primary px-4 pt-6 pb-2 w-full transition-all duration-200`

    4. **Validation animation (per context: animated shake + inline error):**
       - On invalid submit, use `m.div` with shake animation:
         ```typescript
         animate={hasError ? { x: [0, -8, 8, -4, 4, 0] } : undefined}
         transition={{ duration: 0.4 }}
         ```
       - Show inline error text below input: `<p className="text-destructive text-sm mt-1">{error}</p>`

    5. **Submit button (per context: progress animation with gradient fill):**
       - Button text: "Send magic link"
       - While loading: button shows animated gradient fill from left to right
         - Use CSS `background-size` animation or `m.div` overlay inside button
         - Gradient: `from-primary to-primary-hover` sliding fill
       - Disable button during submission
       - Use `useTransition` or `startTransition` for server action

    6. **Social proof (per context: static placeholder):**
       - Below form, add: `<p className="text-center text-sm text-muted-foreground mt-4">Trusted by local families</p>`

    7. **Legal links (per context: Terms and Privacy Policy links at card bottom):**
       - `<p className="text-center text-xs text-muted-foreground mt-6">By continuing, you agree to our <Link href="/terms">Terms</Link> and <Link href="/privacy">Privacy Policy</Link>.</p>`
       - Links styled with `underline hover:text-text-primary transition-colors`

    8. **Submit handler:**
       - Create FormData from email value
       - Call `signInWithMagicLink(formData)` server action
       - On success: call `onSuccess(email)` to transition to confirmation state
       - On error: set form error and trigger shake animation

    9. **Animation preference:** Respect `useAnimationPreference` -- skip shake/progress animations if reduced
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Component file < 400 lines
  </verify>
  <done>
    - Email input with floating label lift + warm border glow on focus
    - Shake animation on invalid submit with inline error
    - Gradient fill progress on submit button
    - Social proof placeholder text
    - Legal links to /terms and /privacy
    - Calls signInWithMagicLink and transitions on success
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SocialLoginButtons and OAuthLoadingOverlay</name>
  <files>
    src/components/ui/auth/SocialLoginButtons.tsx
    src/components/ui/auth/OAuthLoadingOverlay.tsx
  </files>
  <action>
    **SocialLoginButtons (`src/components/ui/auth/SocialLoginButtons.tsx`):**

    1. **Layout (per context decisions):**
       - "or continue with" divider line above buttons
         - Horizontal rule with text: `<div className="relative my-6"><div className="absolute inset-0 flex items-center"><span className="w-full border-t" /></div><div className="relative flex justify-center text-xs uppercase"><span className="bg-surface-primary px-2 text-muted-foreground">or continue with</span></div></div>`
       - "Quick sign in" label above buttons: `<p className="text-xs text-muted-foreground text-center mb-3">Quick sign in</p>`
       - Two icon-only rounded rectangle buttons side by side: `flex gap-3 justify-center`

    2. **Button design (per context: icon-only rounded rectangles):**
       - Google button (left): Render Google "G" logo as inline SVG (standard Google multicolor "G")
       - Apple button (right): Render Apple logo as inline SVG
       - Both buttons: `w-14 h-12 rounded-xl border border-border flex items-center justify-center`
       - Claude's discretion on styling: Google button has white bg with border; Apple button adapts to theme (dark bg in dark mode, white bg in light mode)

    3. **Hover effects (per context: scale + shadow + border glow):**
       - Use `m.button` from framer-motion
       - `whileHover={{ scale: 1.05 }}` + `hover:shadow-lg hover:border-primary/30 transition-shadow`
       - `whileTap={{ scale: 0.95 }}`

    4. **Tooltips (per context: show provider name on hover/long-press):**
       - Use `title` attribute for native tooltip: `title="Sign in with Google"` / `title="Sign in with Apple"`
       - `aria-label` for accessibility

    5. **OAuth handler:**
       - Import `createClient` from `@/lib/supabase/client`
       - On click: set loading provider state, show OAuthLoadingOverlay, call:
         ```typescript
         const { error } = await supabase.auth.signInWithOAuth({
           provider,
           options: {
             redirectTo: `${window.location.origin}/auth/callback`,
             ...(provider === 'google' && { queryParams: { access_type: 'offline', prompt: 'consent' } }),
           },
         });
         ```
       - On error: dismiss overlay, show toast via `useToast()`

    6. **Props:** Accept `onOAuthStart?: (provider: string) => void` for overlay control

    **OAuthLoadingOverlay (`src/components/ui/auth/OAuthLoadingOverlay.tsx`):**

    1. **Design (per context: full page overlay with "Redirecting to..."):**
       - Fixed full-screen overlay: `fixed inset-0 z-50 bg-background/80 backdrop-blur-sm`
       - Center content: provider logo + "Redirecting to Google..." or "Redirecting to Apple..."
       - Use `BrandedSpinner` below the text for loading indication
       - `AnimatePresence` + `m.div` fade in/out

    2. **Props:**
       ```typescript
       interface OAuthLoadingOverlayProps {
         provider: 'google' | 'apple' | null; // null = hidden
       }
       ```
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Both component files < 400 lines
    - Google and Apple SVG icons render correctly
  </verify>
  <done>
    - "or continue with" divider separates magic link from social login
    - Google (left) and Apple (right) icon-only buttons with scale hover effect
    - Tooltips show provider name on hover
    - Clicking initiates signInWithOAuth and shows loading overlay
    - OAuth errors show toast notification
    - OAuthLoadingOverlay renders full-page "Redirecting to..." with spinner
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- All 3 components < 400 lines each
- MagicLinkForm imports signInWithMagicLink from actions.ts
- SocialLoginButtons imports createClient from supabase/client
</verification>

<success_criteria>
- Email form with floating label, shake validation, gradient submit button
- Google + Apple side-by-side icon buttons with hover effects
- OAuth loading overlay with provider-specific message
- All interactions respect animation preferences
</success_criteria>

<output>
After completion, create `.planning/phases/53-auth-experience/53-03-SUMMARY.md`
</output>
