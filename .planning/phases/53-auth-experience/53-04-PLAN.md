---
phase: 53-auth-experience
plan: 04
type: execute
wave: 2
depends_on: ["53-02"]
files_modified:
  - src/components/ui/auth/MagicLinkConfirmation.tsx
autonomous: true

must_haves:
  truths:
    - "Envelope floats in from below, then pulses/glows while waiting"
    - "User's email address is displayed in the confirmation message"
    - "Resend button available immediately with 60-second countdown timer between sends"
    - "'Use a different email' link transitions back to form state"
    - "'Check your spam folder' hint appears after 15-20 second delay"
  artifacts:
    - path: "src/components/ui/auth/MagicLinkConfirmation.tsx"
      provides: "Multi-stage envelope animation, resend, back nav, spam hint"
      exports: ["MagicLinkConfirmation"]
  key_links:
    - from: "src/components/ui/auth/MagicLinkConfirmation.tsx"
      to: "src/components/ui/auth/AuthCard.tsx"
      via: "useAuthCard context for state transitions"
      pattern: "useAuthCard"
    - from: "src/components/ui/auth/MagicLinkConfirmation.tsx"
      to: "src/lib/supabase/actions.ts"
      via: "signInWithMagicLink for resend"
      pattern: "signInWithMagicLink"
---

<objective>
Build the magic link confirmation screen with multi-stage envelope animation.

Purpose: After the user submits their email, the card transforms in-place (per context decision) to show an animated envelope confirmation. This is the "check your email" experience with resend capability, spam hint, and back navigation.

Output: MagicLinkConfirmation client component with multi-stage envelope animation and interactive controls.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-auth-experience/53-CONTEXT.md
@.planning/phases/53-auth-experience/53-RESEARCH.md
@.planning/phases/53-auth-experience/53-02-SUMMARY.md
@src/lib/supabase/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MagicLinkConfirmation with envelope animation</name>
  <files>src/components/ui/auth/MagicLinkConfirmation.tsx</files>
  <action>
    Create `src/components/ui/auth/MagicLinkConfirmation.tsx` (client component):

    1. **Props:**
       ```typescript
       interface MagicLinkConfirmationProps {
         email: string;
         onBack: () => void; // Transition back to form
       }
       ```

    2. **Multi-stage envelope animation (per context: float in -> pulse/glow -> open with sparkle):**
       - Use `useAnimate` from framer-motion for sequenced animation control
       - **Stage 1 - Float in (0.6s):** Envelope emoji (or Mail icon from lucide-react) floats up from below
         - `animate('#envelope', { y: [50, 0], opacity: [0, 1] }, { duration: 0.6, ease: 'easeOut' })`
       - **Stage 2 - Pulse/glow (continuous):** Subtle scale pulse while waiting
         - `animate('#envelope', { scale: [1, 1.05, 1] }, { duration: 2, repeat: Infinity, ease: 'easeInOut' })`
         - Add a warm glow effect: `animate('#glow', { opacity: [0.3, 0.6, 0.3] }, { duration: 2, repeat: Infinity })`
         - Glow element: absolute-positioned radial gradient behind envelope
       - Use a large envelope emoji or the `Mail` icon from lucide-react at 4rem size
       - Wrap in `ref={scope}` from `useAnimate`
       - Respect `useAnimationPreference` -- if reduced, show static envelope without animation

    3. **Content layout:**
       - Envelope animation centered at top
       - Heading: "Check your email" (text-xl font-semibold)
       - Subtext: `We sent a magic link to <strong>{email}</strong>` (text-sm text-muted-foreground)
       - Click the link in your email to sign in.

    4. **Resend button (per context: immediately available, 60-second countdown):**
       - Button: "Resend magic link" (initially enabled)
       - After click: disable button, show countdown "Resend in 60s", "Resend in 59s"...
       - Use `useEffect` + `setInterval` for countdown
       - On resend: create FormData with email, call `signInWithMagicLink(formData)`
       - Show toast on success ("Magic link resent") or error
       - After countdown reaches 0, re-enable button

    5. **Back option (per context: "Use a different email" with animated transition):**
       - Link-styled button below resend: "Use a different email"
       - On click: call `onBack()` which transitions AuthCard back to form state
       - Style: `text-sm text-primary hover:underline cursor-pointer`

    6. **Spam hint (per context: appears after 15-20 second delay):**
       - Hidden initially
       - After 18 seconds (splitting the 15-20 range), fade in:
         - "Don't see it? Check your spam folder"
         - Use `m.p` with `initial={{ opacity: 0 }} animate={{ opacity: 1 }}` triggered by timer
       - `text-xs text-muted-foreground`

    7. **Animation preference:** If reduced, show static envelope, skip pulse, still show all text content
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Component file < 400 lines
    - Resend countdown logic correct (starts at 60, counts down, re-enables at 0)
    - Spam hint delay logic correct (~18s)
  </verify>
  <done>
    - Envelope floats in, then pulses/glows continuously
    - Email address displayed in confirmation message
    - Resend button with 60-second countdown between sends
    - "Use a different email" link navigates back to form
    - Spam folder hint appears after ~18 second delay
    - Animation respects user preference
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Component < 400 lines
- MagicLinkConfirmation imports signInWithMagicLink and useToast
</verification>

<success_criteria>
- Multi-stage envelope animation renders (float -> pulse)
- Email displayed in confirmation message
- Resend with countdown works
- Back navigation transitions to form
- Spam hint appears on delay
</success_criteria>

<output>
After completion, create `.planning/phases/53-auth-experience/53-04-SUMMARY.md`
</output>
