---
phase: 53-auth-experience
plan: 06
type: execute
wave: 3
depends_on: ["53-05"]
files_modified:
  - src/components/ui/auth/LoginSuccessCeremony.tsx
  - src/components/ui/layout/AppHeader/DesktopHeader.tsx
  - src/components/ui/layout/AppHeader/MobileHeader.tsx
  - src/components/ui/auth/AuthCard.tsx
  - src/app/(auth)/login/page.tsx
  - src/components/ui/auth/index.ts
autonomous: true

must_haves:
  truths:
    - "On successful login, logo in auth card morphs to header logo position"
    - "Welcome message shows user's name if available, otherwise just 'Welcome!'"
    - "Logo morph transition is 2-3 seconds"
    - "Same ceremony plays for both OAuth and magic link flows"
    - "Header logos have layoutId='app-logo' during login transition"
  artifacts:
    - path: "src/components/ui/auth/LoginSuccessCeremony.tsx"
      provides: "Logo morph animation + welcome message"
      exports: ["LoginSuccessCeremony"]
  key_links:
    - from: "src/components/ui/auth/LoginSuccessCeremony.tsx"
      to: "src/components/ui/layout/AppHeader/DesktopHeader.tsx"
      via: "Shared layoutId='app-logo' for cross-component morph"
      pattern: "layoutId.*app-logo"
    - from: "src/components/ui/auth/LoginSuccessCeremony.tsx"
      to: "src/components/ui/layout/AppHeader/MobileHeader.tsx"
      via: "Shared layoutId='app-logo' for cross-component morph"
      pattern: "layoutId.*app-logo"
---

<objective>
Implement the login success ceremony with logo morph animation.

Purpose: The premium touch -- when a user successfully logs in (via magic link or OAuth), the logo in the auth card morphs/transitions into the app header logo position. This creates a seamless, delightful transition from auth to app.

Output: LoginSuccessCeremony component with logo morph, welcome message, and header layoutId wiring.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-auth-experience/53-CONTEXT.md
@.planning/phases/53-auth-experience/53-RESEARCH.md
@.planning/phases/53-auth-experience/53-05-SUMMARY.md
@src/components/ui/layout/AppHeader/DesktopHeader.tsx
@src/components/ui/layout/AppHeader/MobileHeader.tsx
@src/components/ui/auth/AuthCard.tsx
@src/app/(auth)/login/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginSuccessCeremony component</name>
  <files>src/components/ui/auth/LoginSuccessCeremony.tsx</files>
  <action>
    Create `src/components/ui/auth/LoginSuccessCeremony.tsx` (client component):

    1. **Props:**
       ```typescript
       interface LoginSuccessCeremonyProps {
         userName?: string | null;  // From user metadata or Google profile
         avatarUrl?: string | null; // Google avatar if available
       }
       ```

    2. **Logo morph (per context: logo-to-layout morph, 2-3 seconds):**
       - Render the logo image (`/logo.png`) inside a `m.div` with `layoutId="app-logo"`
       - Logo starts large (96x96) centered in the auth card
       - After a brief delay (500ms), trigger navigation to home page
       - The `layoutId` shared with the header logo causes Framer Motion to animate the logo from auth card position to header position
       - Transition: use a custom spring with `duration: 2.5` (within 2-3s range), gentle easing
       - `transition={{ type: "spring", stiffness: 100, damping: 25, duration: 2.5 }}`

    3. **Welcome message (per context: personalized with name + avatar):**
       - Show below logo: "Welcome, {userName}!" or just "Welcome!" if no name
       - If `avatarUrl` available: show circular avatar image next to welcome text
       - Text fades in after logo starts morphing
       - `m.div` with `initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.3 }}`

    4. **Navigation trigger:**
       - After 2.5s total (ceremony complete), use `router.replace('/')` to navigate to home
       - Use `useRouter` from `next/navigation`
       - The `layoutId` on the header logo will catch the morph destination

    5. **Reduced animation:**
       - If `useAnimationPreference` returns reduced/none: skip morph, show welcome text for 1s, then navigate immediately
       - Still show the welcome message briefly for user feedback

    6. **Integration point:** This component renders when AuthCard state === 'success'. The login page's client component sets state to 'success' after detecting a successful auth session.

  </action>
  <verify>
    - `pnpm typecheck` passes
    - Component < 400 lines
    - layoutId="app-logo" present on logo element
  </verify>
  <done>
    - Logo renders with layoutId="app-logo" for morph animation
    - Welcome message shows user name if available
    - 2.5s ceremony duration before navigation
    - Reduced animation fallback exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire layoutId into headers and integrate ceremony into login flow</name>
  <files>
    src/components/ui/layout/AppHeader/DesktopHeader.tsx
    src/components/ui/layout/AppHeader/MobileHeader.tsx
    src/components/ui/auth/AuthCard.tsx
    src/app/(auth)/login/page.tsx
    src/components/ui/auth/index.ts
  </files>
  <action>
    1. **Add `layoutId` to DesktopHeader.tsx:**
       - Find the `m.div` wrapping the logo Link (around line 98)
       - Add `layoutId="app-logo"` to this `m.div`
       - This makes it the morph destination for the login ceremony
       - The logo image size/position differences are handled automatically by Framer Motion's FLIP

    2. **Add `layoutId` to MobileHeader.tsx:**
       - Find the `m.div` wrapping the logo Link (around line 64)
       - Add `layoutId="app-logo"` to this `m.div`
       - Same purpose as desktop

    3. **Update AuthCard.tsx:**
       - The logo `m.div` in the card should also have `layoutId="app-logo"`
       - When state transitions to 'success', the AuthCard should render `<LoginSuccessCeremony />` instead of the logo + children
       - Ensure the logo's layoutId in AuthCard matches the header's layoutId exactly

    4. **Update login page.tsx client component:**
       - After auth callback success (user returns from magic link or OAuth), detect session:
         - Use `supabase.auth.onAuthStateChange` listener
         - When `SIGNED_IN` event fires, extract user metadata (name, avatar_url)
         - Set AuthCard state to 'success' with user info
       - Pass user info to LoginSuccessCeremony
       - This ensures BOTH magic link and OAuth login trigger the same ceremony

    5. **Update barrel (`src/components/ui/auth/index.ts`):**
       - Add export for `LoginSuccessCeremony`

    **Critical notes on layoutId across routes:**
    - The login page is in `(auth)` route group, header is in root layout
    - Root layout wraps both, and `<LazyMotion>` is in providers.tsx at root level
    - `layoutId` should work since both components share the same `LayoutGroup` context via the root provider
    - If layoutId fails cross-route (Open Question 1 from research), fallback: use CSS animation (scale down + translate to top-left) as a visual approximation of the morph. Document this in SUMMARY.

  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - `pnpm build` succeeds
    - grep for `layoutId.*app-logo` in DesktopHeader, MobileHeader, AuthCard/LoginSuccessCeremony
  </verify>
  <done>
    - DesktopHeader and MobileHeader logos have layoutId="app-logo"
    - AuthCard transitions to LoginSuccessCeremony on success state
    - Login page listens for auth state changes and triggers ceremony
    - Both OAuth and magic link flows trigger same ceremony
    - LoginSuccessCeremony exported from barrel
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- layoutId="app-logo" present in 3 locations (AuthCard/LoginSuccessCeremony, DesktopHeader, MobileHeader)
- LoginSuccessCeremony exported from auth barrel
</verification>

<success_criteria>

- Logo morph animation works between auth card and header (or CSS fallback if layoutId fails cross-route)
- Welcome message displays personalization when available
- Same ceremony for both OAuth and magic link
- 2-3 second transition duration
- Navigation to home after ceremony completes
  </success_criteria>

<output>
After completion, create `.planning/phases/53-auth-experience/53-06-SUMMARY.md`
</output>
