---
phase: 53-auth-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/auth/AuthBackground.tsx
  - src/components/ui/auth/AuthCard.tsx
  - src/styles/globals.css
autonomous: true

must_haves:
  truths:
    - "Auth background renders animated warm gradient shifting between amber, coral, and cream"
    - "Floating food emojis appear at medium density (10-15) with varied sizes over the gradient"
    - "Auth card slides up + fades in on mount with gradient top bar accent"
    - "Card is centered on desktop, full-width on mobile"
    - "Dark mode renders darker gradient tones"
  artifacts:
    - path: "src/components/ui/auth/AuthBackground.tsx"
      provides: "Animated gradient background + floating food emojis"
    - path: "src/components/ui/auth/AuthCard.tsx"
      provides: "Card shell with state machine (form/confirmation/success/error), slide-up animation, gradient top bar"
  key_links:
    - from: "src/components/ui/auth/AuthBackground.tsx"
      to: "src/components/ui/error-pages/FloatingFoodEmojis.tsx"
      via: "Reuses concept (adapted, not imported directly -- auth has different density/colors)"
      pattern: "FOOD_EMOJIS"
---

<objective>
Build the auth page visual shell: animated background and card container.

Purpose: Create the premium visual foundation. The AuthBackground provides the warm gradient + floating food emojis. The AuthCard provides the animated container with state machine for form/confirmation/success transitions. These are the building blocks all other auth components slot into.

Output: Two client components ready for composition. AuthCard accepts children and manages auth state transitions via AnimatePresence.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-auth-experience/53-CONTEXT.md
@.planning/phases/53-auth-experience/53-RESEARCH.md
@src/components/ui/error-pages/FloatingFoodEmojis.tsx
@src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthBackground component</name>
  <files>
    src/components/ui/auth/AuthBackground.tsx
    src/styles/globals.css
  </files>
  <action>
    1. **Create `src/components/ui/auth/AuthBackground.tsx`** (client component):
       - `'use client'` directive
       - Renders a full-screen container (`min-h-screen w-full relative overflow-hidden`)
       - **Animated gradient background:**
         - Use CSS class `auth-gradient` (defined in globals.css)
         - Light mode: `linear-gradient(135deg, amber, soft coral, cream, amber)` with `background-size: 400% 400%`
         - Dark mode: darker versions of the same tones via `.dark .auth-gradient`
         - Animation: `auth-gradient-shift` keyframes, 20s ease infinite
       - **Floating food emojis:**
         - Reuse the concept from `FloatingFoodEmojis.tsx` but adapted for auth context
         - 12 food emojis (within the 10-15 medium density range per context decision)
         - Varied sizes: range from `1.25rem` to `2.5rem` for depth effect
         - Lower opacity (0.12-0.25) to not compete with card content
         - Use the same `animate-error-drift-*` CSS animations (they're generic drift anims)
         - Render as server-renderable `<span>` elements (no JS animation needed -- CSS handles drift)
         - `aria-hidden="true"` and `pointer-events-none`
       - Accept `children` prop to render card content on top
       - Children wrapper: `relative z-10 flex items-center justify-center min-h-screen p-4`

    2. **Add CSS to `src/styles/globals.css`:**
       - Add `@keyframes auth-gradient-shift` (0% -> 33% -> 66% -> 100% background-position cycle)
       - Add `.auth-gradient` class with the warm gradient colors and animation
       - Add `.dark .auth-gradient` with darker tones
       - Colors (light): `hsl(35, 90%, 65%)` amber, `hsl(15, 80%, 70%)` soft coral, `hsl(40, 60%, 85%)` cream
       - Colors (dark): `hsl(35, 70%, 25%)`, `hsl(15, 60%, 30%)`, `hsl(40, 40%, 20%)`
       - Place these styles in the existing `@layer components` section if one exists, or add after existing keyframes

  </action>
  <verify>
    - `pnpm typecheck` passes
    - Component file < 400 lines
    - CSS keyframes added to globals.css
  </verify>
  <done>
    - AuthBackground renders animated warm gradient
    - 12 floating food emojis with varied sizes and drift animations
    - Dark mode support via .dark selector
    - Children render centered on top of background
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AuthCard container with state machine</name>
  <files>
    src/components/ui/auth/AuthCard.tsx
  </files>
  <action>
    Create `src/components/ui/auth/AuthCard.tsx` (client component):

    1. **State machine:**
       ```typescript
       type AuthState = 'form' | 'confirmation' | 'success' | 'error';
       ```
       - Export a React context `AuthCardContext` providing:
         - `state: AuthState`
         - `setState: (state: AuthState) => void`
         - `email: string`
         - `setEmail: (email: string) => void`
         - `errorMessage: string`
         - `setErrorMessage: (msg: string) => void`
       - Export `useAuthCard` hook that consumes the context

    2. **Visual design (per context decisions):**
       - Card surface: Use a subtle frosted glass effect on desktop (`backdrop-blur-xl bg-surface-primary/80 sm:bg-surface-primary/70`), solid on mobile (`bg-surface-primary`)
         - Claude's discretion: frosted glass on sm+ for readability, solid on mobile per Safari backdrop-blur concerns
       - Rounded corners: `rounded-2xl` (desktop), `rounded-t-2xl` on mobile full-width
       - Shadow: `shadow-2xl`
       - Max width: `max-w-md w-full` on desktop, `w-full` on mobile
       - Mobile: full-width card at bottom of viewport, no side margins
       - Desktop: centered card

    3. **Gradient top bar accent:**
       - Thin strip (3-4px `h-1`) at top of card
       - `bg-gradient-to-r from-primary via-brand-golden to-primary`
       - Rounded top to match card corners

    4. **Entrance animation:**
       - Use `m.div` from framer-motion
       - Slide up + fade: `initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}`
       - Use `spring.gentle` transition from motion-tokens
       - Respect `useAnimationPreference` -- skip animation if reduced/none

    5. **Internal layout:**
       - Logo at top: Full logo image (`/logo.png`) + tagline text "Mandalay Morning Star" centered
       - Logo rendered with `m.div` that will get `layoutId="app-logo"` in plan 06
       - Below logo: warm heading text ("Welcome back" for form state, adjusts per state)
       - Below heading: `<AnimatePresence mode="wait">` wrapping children keyed by state
       - Padding: `p-6 sm:p-8`

    6. **Export:** Named export `AuthCard` and `useAuthCard` hook

  </action>
  <verify>
    - `pnpm typecheck` passes
    - Component file < 400 lines
    - AuthCard renders with gradient top bar, logo, and AnimatePresence wrapper
  </verify>
  <done>
    - AuthCard renders centered card with frosted glass surface
    - Gradient top bar accent in brand colors
    - Slide-up + fade entrance animation
    - State machine context (form/confirmation/success/error) with useAuthCard hook
    - Logo + tagline at top
    - AnimatePresence wraps state-dependent children
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Both components render (verified via typecheck -- visual check in plan 05)
- AuthBackground CSS keyframes exist in globals.css
- AuthCard provides useAuthCard context hook
</verification>

<success_criteria>

- AuthBackground shows animated warm gradient with floating food emojis
- AuthCard provides state machine, entrance animation, gradient accent, logo
- Both < 400 lines
- Dark mode supported
  </success_criteria>

<output>
After completion, create `.planning/phases/53-auth-experience/53-02-SUMMARY.md`
</output>
