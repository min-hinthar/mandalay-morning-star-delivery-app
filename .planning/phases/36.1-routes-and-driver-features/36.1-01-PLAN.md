---
phase: 36.1-routes-and-driver-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/xxxxx_app_settings.sql
  - src/lib/validations/settings.ts
  - src/app/api/admin/settings/route.ts
  - src/app/api/admin/settings/restore/route.ts
autonomous: true

must_haves:
  truths:
    - "app_settings table exists in database with seed data"
    - "Admin can GET settings grouped by category"
    - "Admin can PATCH settings and see changes persist"
    - "Admin can restore defaults with single POST"
  artifacts:
    - path: "supabase/migrations/xxxxx_app_settings.sql"
      provides: "Settings table schema and seed data"
      contains: "app_settings"
    - path: "src/lib/validations/settings.ts"
      provides: "Zod schemas for settings validation"
      exports: ["updateSettingsSchema", "DeliverySettings", "OperationsSettings", "NotificationSettings"]
    - path: "src/app/api/admin/settings/route.ts"
      provides: "GET and PATCH for settings"
      exports: ["GET", "PATCH"]
    - path: "src/app/api/admin/settings/restore/route.ts"
      provides: "POST to restore defaults"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/admin/settings/route.ts"
      to: "supabase.from('app_settings')"
      via: "database query"
      pattern: "supabase.*app_settings"
---

<objective>
Create app_settings database infrastructure and APIs for the admin settings page.

Purpose: Establish the foundation for configurable delivery, operations, and notification settings that persist in the database and can be changed without redeploy.

Output: Migration file, validation schemas, and REST APIs for settings CRUD.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-routes-and-driver-features/36.1-CONTEXT.md
@.planning/phases/36.1-routes-and-driver-features/36.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app_settings migration</name>
  <files>supabase/migrations/YYYYMMDDHHMMSS_app_settings.sql</files>
  <action>
    Create migration file with:

    1. Create app_settings table:
    ```sql
    CREATE TABLE app_settings (
      id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
      key TEXT UNIQUE NOT NULL,
      value JSONB NOT NULL,
      category TEXT NOT NULL CHECK (category IN ('delivery', 'operations', 'notifications')),
      updated_at TIMESTAMPTZ DEFAULT NOW(),
      updated_by UUID REFERENCES profiles(id)
    );
    ```

    2. Add RLS policies:
    - SELECT: admin role only
    - UPDATE: admin role only
    - INSERT: admin role only (for initial seed)
    - DELETE: deny all (settings should not be deleted)

    3. Create trigger for updated_at:
    ```sql
    CREATE TRIGGER set_app_settings_updated_at
    BEFORE UPDATE ON app_settings
    FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
    ```

    4. Seed default settings:
    - delivery_radius_miles: 40
    - minimum_order_cents: 2500
    - free_delivery_threshold_cents: 5000
    - base_delivery_fee_cents: 599
    - max_stops_per_route: 15
    - auto_assign_enabled: false
    - email_notifications_enabled: true
    - sms_notifications_enabled: false
    - push_notifications_enabled: false

    Use timestamp format matching existing migrations in supabase/migrations/.
  </action>
  <verify>
    Migration file exists and SQL is valid.
    Check against existing migration patterns: `ls supabase/migrations/*.sql | head -3`
  </verify>
  <done>
    Migration file created with app_settings table, RLS, trigger, and seed data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create settings validation schemas</name>
  <files>src/lib/validations/settings.ts</files>
  <action>
    Create Zod validation schemas following existing patterns in src/lib/validations/:

    1. DeliverySettings type:
    - delivery_radius_miles: z.number().min(1).max(100)
    - minimum_order_cents: z.number().min(0)
    - free_delivery_threshold_cents: z.number().min(0)
    - base_delivery_fee_cents: z.number().min(0)
    - delivery_cutoff_time: z.string().optional() (HH:MM format)
    - delivery_time_windows: z.array() of time windows (optional)

    2. OperationsSettings type:
    - max_stops_per_route: z.number().min(1).max(50)
    - auto_assign_enabled: z.boolean()
    - route_optimization_enabled: z.boolean().optional()
    - default_vehicle_type: z.enum(['car', 'motorcycle', 'bicycle', 'van', 'truck']).optional()

    3. NotificationSettings type:
    - email_notifications_enabled: z.boolean()
    - sms_notifications_enabled: z.boolean()
    - push_notifications_enabled: z.boolean()
    - notify_on_order_placed: z.boolean().optional()
    - notify_on_order_status_change: z.boolean().optional()

    4. updateSettingsSchema for PATCH requests:
    - category: z.enum(['delivery', 'operations', 'notifications'])
    - settings: z.record(z.unknown()) with refinement based on category

    Export all types and schemas.
  </action>
  <verify>
    `npx tsc --noEmit src/lib/validations/settings.ts`
  </verify>
  <done>
    Validation schemas exported: updateSettingsSchema, DeliverySettings, OperationsSettings, NotificationSettings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create settings API endpoints</name>
  <files>
    src/app/api/admin/settings/route.ts
    src/app/api/admin/settings/restore/route.ts
  </files>
  <action>
    Create API routes following existing admin API patterns (see src/app/api/admin/drivers/[id]/route.ts):

    **GET /api/admin/settings:**
    - Auth check (admin role)
    - Fetch all settings from app_settings table
    - Group by category
    - Return: { delivery: {...}, operations: {...}, notifications: {...} }

    **PATCH /api/admin/settings:**
    - Auth check (admin role)
    - Validate body with updateSettingsSchema
    - Update matching settings (upsert pattern)
    - Set updated_by to current user
    - Return success with updated settings

    **POST /api/admin/settings/restore:**
    - Auth check (admin role)
    - Delete all settings
    - Re-insert default values (same as migration seed)
    - Return success message

    Use logger.exception for errors following existing patterns.
    Transform snake_case DB columns to camelCase for API response.
  </action>
  <verify>
    `pnpm typecheck` passes
    Files exist: `ls src/app/api/admin/settings/`
  </verify>
  <done>
    Settings API endpoints created: GET, PATCH at /api/admin/settings, POST at /api/admin/settings/restore.
  </done>
</task>

</tasks>

<verification>
1. Migration file syntax valid
2. TypeScript compilation passes
3. All exports present in validation file
4. API routes have proper auth checks
</verification>

<success_criteria>
- [ ] Migration creates app_settings table with RLS
- [ ] Default settings seeded in migration
- [ ] Validation schemas type-safe and exported
- [ ] GET /api/admin/settings returns grouped settings
- [ ] PATCH /api/admin/settings updates settings
- [ ] POST /api/admin/settings/restore resets to defaults
- [ ] `pnpm typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-routes-and-driver-features/36.1-01-SUMMARY.md`
</output>
