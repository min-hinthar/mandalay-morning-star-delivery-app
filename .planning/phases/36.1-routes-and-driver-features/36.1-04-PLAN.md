---
phase: 36.1-routes-and-driver-features
plan: 04
type: execute
wave: 2
depends_on: [36.1-02]
files_modified:
  - src/app/(admin)/admin/drivers/[id]/page.tsx
  - src/components/ui/admin/drivers/DriverDetailClient.tsx
  - src/components/ui/admin/drivers/DriverStatsCards.tsx
  - src/components/ui/admin/drivers/RecentRoutesSection.tsx
  - src/components/ui/admin/drivers/RecentRatingsSection.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can navigate to /admin/drivers/[id] and see driver profile"
    - "Admin sees driver stats (rating, deliveries, status)"
    - "Admin sees last 7 days of routes"
    - "Admin sees recent customer ratings"
    - "Admin can edit driver profile and archive driver"
  artifacts:
    - path: "src/app/(admin)/admin/drivers/[id]/page.tsx"
      provides: "Driver detail page route"
      min_lines: 30
    - path: "src/components/ui/admin/drivers/DriverDetailClient.tsx"
      provides: "Main driver detail UI"
      min_lines: 150
    - path: "src/components/ui/admin/drivers/DriverStatsCards.tsx"
      provides: "Stats cards with motion animations"
      min_lines: 50
    - path: "src/components/ui/admin/drivers/RecentRoutesSection.tsx"
      provides: "Route history table"
      min_lines: 80
    - path: "src/components/ui/admin/drivers/RecentRatingsSection.tsx"
      provides: "Ratings list with customer feedback"
      min_lines: 60
  key_links:
    - from: "src/components/ui/admin/drivers/DriverDetailClient.tsx"
      to: "/api/admin/drivers/[id]"
      via: "fetch in useEffect"
      pattern: "fetch.*api/admin/drivers"
    - from: "src/components/ui/admin/drivers/RecentRoutesSection.tsx"
      to: "/api/admin/drivers/[id]/routes"
      via: "fetch"
      pattern: "api/admin/drivers.*routes"
---

<objective>
Create the admin driver detail page with activity-focused layout.

Purpose: Enable admins to view driver profile, recent activity, ratings, and manage driver status from a dedicated detail page.

Output: Driver detail page with stats cards, route history, ratings, and action buttons.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-routes-and-driver-features/36.1-CONTEXT.md
@.planning/phases/36.1-routes-and-driver-features/36.1-RESEARCH.md
@src/app/(admin)/admin/menu/[id]/page.tsx
@src/components/ui/admin/drivers/DriverListTable.tsx
@src/components/ui/admin/analytics/MetricCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create driver detail page and client component</name>
  <files>
    src/app/(admin)/admin/drivers/[id]/page.tsx
    src/components/ui/admin/drivers/DriverDetailClient.tsx
  </files>
  <action>
    **Create src/app/(admin)/admin/drivers/[id]/page.tsx:**
    - "use client" (following menu/[id] pattern)
    - Import DriverDetailClient
    - Simple page wrapper that renders DriverDetailClient

    **Create src/components/ui/admin/drivers/DriverDetailClient.tsx:**
    Layout per CONTEXT.md: Activity-focused - recent routes and deliveries as main view, profile info secondary.

    Structure:
    ```tsx
    <div className="p-4 md:p-8 space-y-6 max-w-6xl">
      {/* Header with back button, name, status badge */}
      <motion.div> DriverHeader </motion.div>

      {/* Stats Cards Row */}
      <DriverStatsCards driver={driver} />

      {/* Two column layout on desktop */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* Main column (2/3) - Activity */}
        <div className="lg:col-span-2 space-y-6">
          <RecentRoutesSection driverId={id} />
          <RecentRatingsSection driverId={id} />
        </div>

        {/* Side column (1/3) - Profile & Actions */}
        <div className="space-y-6">
          <ProfileCard driver={driver} onEdit={...} />
          <ActionsCard driver={driver} onArchive={...} onToggleStatus={...} />
        </div>
      </div>
    </div>
    ```

    State management:
    - Fetch driver data from /api/admin/drivers/[id]
    - Loading skeleton (copy pattern from menu/[id])
    - Error handling with toast
    - Edit profile modal (simple form dialog)
    - Archive confirmation dialog with reason input

    Actions (from CONTEXT.md):
    - Edit profile (modal)
    - Toggle status (active/inactive)
    - Assign to route (link to routes page with driver filter)
    - Archive (confirmation dialog with reason)

    Use existing patterns:
    - Motion animations from menu/[id]/page.tsx
    - Button, Badge, Input from ui components
    - Toast for notifications
    - cn() for classnames
  </action>
  <verify>
    `pnpm typecheck` passes
    Page renders at /admin/drivers/[id]
  </verify>
  <done>
    Driver detail page shows driver info with activity-focused layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stats cards and sections</name>
  <files>
    src/components/ui/admin/drivers/DriverStatsCards.tsx
    src/components/ui/admin/drivers/RecentRoutesSection.tsx
    src/components/ui/admin/drivers/RecentRatingsSection.tsx
  </files>
  <action>
    **Create DriverStatsCards.tsx:**
    Following MetricCard pattern from analytics:
    - Rating (star icon, average value, /5 label)
    - Total Deliveries (truck icon, count)
    - Status (circle icon, Active/Inactive)
    - Member Since (calendar icon, formatted date)

    Use motion.div with stagger animation.
    Gradient backgrounds per existing admin aesthetic.

    **Create RecentRoutesSection.tsx:**
    - Fetch /api/admin/drivers/[id]/routes on mount
    - Display table with columns: Date, Status, Stops, Completed, Duration
    - Status badges with colors (planned=blue, in_progress=amber, completed=green)
    - Link to route detail on row click
    - "Load more" button if > 7 routes available
    - Empty state: "No routes in the last 7 days"

    **Create RecentRatingsSection.tsx:**
    - Fetch /api/admin/drivers/[id]/ratings on mount
    - Display list of ratings:
      - Star rating (1-5 filled stars)
      - Feedback text (or "No feedback provided")
      - Customer name and date
    - Average rating header with total count
    - Empty state: "No ratings yet"
  </action>
  <verify>
    `pnpm typecheck` passes
    All components exported
  </verify>
  <done>
    Stats cards show key metrics, routes section shows history, ratings section shows feedback.
  </done>
</task>

</tasks>

<verification>
1. Page accessible at /admin/drivers/[id]
2. TypeScript compilation passes
3. All components render without errors
4. Data fetches correctly from APIs
</verification>

<success_criteria>
- [ ] /admin/drivers/[id] shows driver detail page
- [ ] Stats cards display rating, deliveries, status, member since
- [ ] Recent routes section fetches and displays last 7 days
- [ ] Recent ratings section shows customer feedback
- [ ] Edit profile opens modal form
- [ ] Archive action requires reason confirmation
- [ ] Back button returns to drivers list
- [ ] `pnpm typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-routes-and-driver-features/36.1-04-SUMMARY.md`
</output>
