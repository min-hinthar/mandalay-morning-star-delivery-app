---
phase: 36.1-routes-and-driver-features
plan: 11
type: execute
wave: 4
depends_on: [36.1-04, 36.1-05, 36.1-06, 36.1-07, 36.1-09, 36.1-10]
files_modified:
  - src/components/ui/admin/AdminNav.tsx
  - src/components/layout/Header.tsx
autonomous: false

must_haves:
  truths:
    - "Admin can navigate to all admin pages via nav links"
    - "Admin sees Settings link in navigation sidebar"
    - "Customer can access account page from header dropdown"
    - "All new pages display their content correctly"
    - "Application builds and deploys successfully"
  artifacts:
    - path: "src/components/ui/admin/AdminNav.tsx"
      provides: "Updated admin nav with all links"
      contains: "/admin/settings"
    - path: "src/components/layout/Header.tsx"
      provides: "Header with account dropdown"
      contains: "/account"
  key_links:
    - from: "src/components/ui/admin/AdminNav.tsx"
      to: "all admin routes"
      via: "Link components"
      pattern: "Link.*href.*admin"
---

<objective>
Final integration, navigation verification, and build validation.

Purpose: Ensure all new pages are linked in navigation, no 404s exist, and the build succeeds.

Output: Updated navigation, verified routes, passing build.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-routes-and-driver-features/36.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin navigation</name>
  <files>src/components/ui/admin/AdminNav.tsx</files>
  <action>
    Update AdminNav to include Settings link:

    1. Find the navigation items array/list
    2. Add Settings link:
    ```tsx
    {
      href: "/admin/settings",
      label: "Settings",
      icon: <Settings className="h-5 w-5" />,
    }
    ```

    3. Verify existing links point to correct routes:
    - /admin/drivers -> exists
    - /admin/routes -> exists
    - /admin/orders -> exists
    - /admin/menu -> exists
    - /admin/analytics/* -> exists

    4. Verify that driver detail link in DriverListTable points to /admin/drivers/[id]
    5. Verify that route detail link in RouteListTable points to /admin/routes/[id]
  </action>
  <verify>
    Settings link visible in admin nav
    `pnpm typecheck` passes
  </verify>
  <done>
    Admin navigation includes Settings link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update customer header with account link</name>
  <files>src/components/layout/Header.tsx</files>
  <action>
    Per CONTEXT.md: Customer account page accessible from header dropdown.

    1. Find the user dropdown menu in Header.tsx
    2. Add Account link if not present:
    ```tsx
    <DropdownMenuItem asChild>
      <Link href="/account" className="flex items-center gap-2">
        <User className="h-4 w-4" />
        My Account
      </Link>
    </DropdownMenuItem>
    ```

    3. Ensure link appears for authenticated customers
    4. Position appropriately in dropdown (before logout)

    If dropdown already has account link, verify it points to /account.
  </action>
  <verify>
    Account link visible in header dropdown for logged-in users
    `pnpm typecheck` passes
  </verify>
  <done>
    Header dropdown includes Account link.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 36.1 features:
    - Driver detail page at /admin/drivers/[id]
    - Route detail page with map at /admin/routes/[id]
    - Settings page at /admin/settings
    - Customer account page at /account
    - Navigation links updated
  </what-built>
  <how-to-verify>
    1. Run `pnpm build` - verify it succeeds
    2. Run `pnpm dev` and test each page:

    **Admin flows:**
    - Navigate to /admin/drivers, click a driver -> verify detail page loads
    - On driver detail: verify stats, recent routes, ratings sections
    - Navigate to /admin/routes, click a route -> verify detail page loads
    - On route detail: verify map shows, stops list with customer info
    - Navigate to /admin/settings -> verify tabs work, can edit settings

    **Customer flows:**
    - Login as customer
    - Click account in header dropdown -> verify /account loads
    - Test Profile tab: edit name, save
    - Test Orders tab: view history, click Reorder (verify adds to cart)
    - Test Addresses tab: add address, edit, delete

    **Navigation:**
    - Verify no 404 errors when clicking any navigation item
    - Back buttons work on all detail pages
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all pages work, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Build succeeds: `pnpm build`
2. No TypeScript errors: `pnpm typecheck`
3. All pages accessible
4. Navigation links correct
</verification>

<success_criteria>
- [ ] Settings link in admin nav
- [ ] Account link in customer header dropdown
- [ ] /admin/drivers/[id] loads without 404
- [ ] /admin/routes/[id] loads without 404
- [ ] /admin/settings loads without 404
- [ ] /account loads without 404
- [ ] `pnpm build` succeeds
- [ ] Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-routes-and-driver-features/36.1-11-SUMMARY.md`
</output>
