---
phase: 41-server-component-conversions
plan: 07
type: execute
wave: 3
depends_on: ["41-03", "41-04", "41-05", "41-06"]
files_modified:
  - .planning/phases/41-server-component-conversions/USE_CLIENT_AUDIT.md
  - e2e/hydration-smoke.spec.ts
autonomous: true

must_haves:
  truths:
    - "All converted routes pass hydration smoke tests"
    - "Bundle size reduction is measured and documented"
    - "Audit document reflects actual changes made"
    - "No hydration errors in full app health check"
  artifacts:
    - path: ".planning/phases/41-server-component-conversions/USE_CLIENT_AUDIT.md"
      provides: "Updated audit with conversion results"
      contains: "Conversion Results"
  key_links: []
---

<objective>
Conduct final hydration health check across the entire app and update audit with conversion results.

Purpose: Verify all conversions work correctly together and document the bundle size improvements achieved.
Output: Updated audit document, verified hydration health, LCP improvement measurement
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-server-component-conversions/41-03-SUMMARY.md
@.planning/phases/41-server-component-conversions/41-04-SUMMARY.md
@.planning/phases/41-server-component-conversions/41-05-SUMMARY.md
@.planning/phases/41-server-component-conversions/41-06-SUMMARY.md
@.planning/phases/41-server-component-conversions/USE_CLIENT_AUDIT.md
@e2e/hydration-smoke.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full hydration health check</name>
  <files>e2e/hydration-smoke.spec.ts</files>
  <action>
Execute comprehensive hydration testing per CONTEXT.md requirement: "Final hydration health check pass across whole app after all conversions"

1. **Update hydration test to include all converted routes:**
   Ensure CONVERTED_ROUTES array includes all routes converted in this phase:
   ```typescript
   const CONVERTED_ROUTES = [
     { path: '/', name: 'Home Page' },
     { path: '/menu', name: 'Menu Page' },
     { path: '/admin/analytics', name: 'Analytics Page' },
   ]
   ```

2. **Run full test suite:**
   ```bash
   pnpm test:e2e e2e/hydration-smoke.spec.ts
   ```

3. **Document any failures and fixes:**
   If hydration errors found:
   - Identify the specific component/pattern causing the issue
   - Apply fix (useEffect for client-only values, two-pass rendering, etc.)
   - Re-run tests to confirm fix

4. **Run build verification:**
   ```bash
   pnpm build 2>&1 | tee build-output.log
   grep -i "hydrat\|warning\|error" build-output.log || echo "Build clean"
   ```

Per CONTEXT.md: "Invest time fixing hydration issues rather than reverting prematurely"
  </action>
  <verify>All hydration tests pass. Build completes without warnings.</verify>
  <done>Full hydration health check complete - all converted routes verified</done>
</task>

<task type="auto">
  <name>Task 2: Measure and document bundle size improvements</name>
  <files></files>
  <action>
Run bundle analysis and document improvements:

1. **Generate bundle analysis:**
   ```bash
   ANALYZE=true pnpm build
   ```
   This opens the bundle analyzer in browser.

2. **Capture key metrics:**
   - Total client JS size (before vs after)
   - Home page route bundle size
   - Menu page route bundle size
   - Shared chunks size

3. **Run Lighthouse for LCP measurement:**
   ```bash
   # Start production build
   pnpm build && pnpm start &
   sleep 10

   # Run Lighthouse (if available)
   lighthouse http://localhost:3000 --only-categories=performance --output=json --output-path=lighthouse-home.json
   lighthouse http://localhost:3000/menu --only-categories=performance --output=json --output-path=lighthouse-menu.json
   ```

4. **Document results:**
   Create a comparison table:
   | Metric | Before Phase 41 | After Phase 41 | Delta |
   |--------|-----------------|----------------|-------|
   | Home LCP | 11.4s | ? | ? |
   | Menu LCP | 9.8s | ? | ? |
   | Client JS Size | ? | ? | ? |
   | 'use client' files | 275 | ? | ? |

Target from ROADMAP.md: 4-5s to 3-3.5s LCP range
  </action>
  <verify>Bundle analysis generated. LCP measurements recorded.</verify>
  <done>Bundle size and LCP improvements measured and documented</done>
</task>

<task type="auto">
  <name>Task 3: Update audit document with conversion results</name>
  <files>.planning/phases/41-server-component-conversions/USE_CLIENT_AUDIT.md</files>
  <action>
Update the USE_CLIENT_AUDIT.md with actual results from Phase 41:

Add a new section at the top:

```markdown
## Conversion Results - Phase 41

**Completion Date:** [date]
**Original 'use client' files:** 275
**Final 'use client' files:** [new count]
**Net reduction:** [delta] files

### Bundle Size Impact

| Route | Before | After | Reduction |
|-------|--------|-------|-----------|
| Home | ? | ? | ? |
| Menu | ? | ? | ? |
| Analytics | N/A (already server) | N/A | N/A |
| Tracking | ? | ? | ? |

### LCP Impact

| Route | Before | After | Improvement |
|-------|--------|-------|-------------|
| Home | 11.4s | ? | ? |
| Menu | 9.8s | ? | ? |

### Conversions Made

| File | Status | Notes |
|------|--------|-------|
| HomePageClient.tsx | SPLIT → reduced | Extracted static sections |
| MenuContent.tsx | SPLIT → server | Converted to server component |
| ... | ... | ... |

### Conversions Skipped

| File | Reason |
|------|--------|
| Hero.tsx | Tightly coupled animations, minimal gain |
| ... | ... |

### Remaining Future Candidates

[Reference list from initial audit - candidates for Phase 42+]
```

Count new 'use client' files:
```bash
grep -rn "^\"use client\"" src --include="*.tsx" --include="*.ts" | wc -l
```
  </action>
  <verify>Audit document updated with actual results.</verify>
  <done>Audit document reflects all Phase 41 conversion results</done>
</task>

</tasks>

<verification>
```bash
# Full verification suite
pnpm typecheck
pnpm lint
pnpm test:e2e e2e/hydration-smoke.spec.ts
pnpm build

# Final 'use client' count
grep -rn "^\"use client\"" src --include="*.tsx" --include="*.ts" | wc -l
```
</verification>

<success_criteria>
- All converted routes pass hydration smoke tests
- Build completes without hydration warnings
- Bundle size reduction documented
- LCP improvement measured
- Audit document updated with final results
- 'use client' file count reduced (target: ~20-30 fewer files)
</success_criteria>

<output>
After completion, create `.planning/phases/41-server-component-conversions/41-07-SUMMARY.md`

Include:
- Full hydration test results
- Bundle size comparison table
- LCP improvement measurements
- Final 'use client' file count
- Summary of what was converted vs skipped
- Recommendations for Phase 42+
</output>
