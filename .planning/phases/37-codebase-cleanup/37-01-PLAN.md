---
phase: 37-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Badge.stories.tsx
  - src/components/ui/Button.stories.tsx
  - src/components/ui/Input.stories.tsx
  - src/components/ui/Modal.stories.tsx
  - src/components/ui/Container.stories.tsx
  - src/components/ui/Grid.stories.tsx
  - src/components/ui/Stack.stories.tsx
  - src/components/ui/menu/MenuAccordion.stories.tsx
  - src/components/ui/auth/AuthModal.tsx
  - src/components/ui/auth/MagicLinkSent.tsx
  - src/components/ui/auth/OnboardingTour.tsx
  - src/components/ui/auth/WelcomeAnimation.tsx
  - src/components/ui/auth/index.ts
  - .planning/CLEANUP_LOG.md
autonomous: true

must_haves:
  truths:
    - "Build succeeds with no imports from deleted files"
    - "No Storybook files exist in src/components/ui/"
    - "Importing deleted auth components fails"
    - "CLEANUP_LOG.md documents all deletions"
    - "src/ folder structure follows feature-based organization"
  artifacts:
    - path: ".planning/CLEANUP_LOG.md"
      provides: "Audit trail of deleted files"
      min_lines: 20
    - path: "src/components/ui/auth/index.ts"
      provides: "Updated barrel without deleted exports"
      contains: "export { LoginForm }"
  key_links:
    - from: "src/components/ui/auth/index.ts"
      to: "src/components/ui/auth/LoginForm.tsx"
      via: "barrel export"
      pattern: "export.*LoginForm"
---

<objective>
Delete production artifacts and unused auth components per REFACTOR-01, REFACTOR-02, REFACTOR-03, REFACTOR-04 requirements.

Purpose: Remove dead code that inflates bundle size and confuses developers
Output: Clean codebase with CLEANUP_LOG.md audit trail
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-codebase-cleanup/37-RESEARCH.md
@src/components/ui/auth/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Storybook files and unused auth components</name>
  <files>
    src/components/ui/Badge.stories.tsx
    src/components/ui/Button.stories.tsx
    src/components/ui/Input.stories.tsx
    src/components/ui/Modal.stories.tsx
    src/components/ui/Container.stories.tsx
    src/components/ui/Grid.stories.tsx
    src/components/ui/Stack.stories.tsx
    src/components/ui/menu/MenuAccordion.stories.tsx
    src/components/ui/auth/AuthModal.tsx
    src/components/ui/auth/MagicLinkSent.tsx
    src/components/ui/auth/OnboardingTour.tsx
    src/components/ui/auth/WelcomeAnimation.tsx
  </files>
  <action>
    Delete the following files using git rm (preserves history):

    **Storybook files (8 files) - REFACTOR-01:**
    - src/components/ui/Badge.stories.tsx
    - src/components/ui/Button.stories.tsx
    - src/components/ui/Input.stories.tsx
    - src/components/ui/Modal.stories.tsx
    - src/components/ui/Container.stories.tsx
    - src/components/ui/Grid.stories.tsx
    - src/components/ui/Stack.stories.tsx
    - src/components/ui/menu/MenuAccordion.stories.tsx

    **Deprecated auth components (4 files) - REFACTOR-03:**
    - src/components/ui/auth/AuthModal.tsx
    - src/components/ui/auth/MagicLinkSent.tsx
    - src/components/ui/auth/OnboardingTour.tsx
    - src/components/ui/auth/WelcomeAnimation.tsx

    Before deletion, verify no active imports exist using grep:
    ```bash
    grep -r "AuthModal\|MagicLinkSent\|OnboardingTour\|WelcomeAnimation" src --include="*.tsx" --include="*.ts" | grep -v "index.ts" | grep -v "\.stories\."
    ```

    If any imports found, update those files to remove the imports first.

    NOTE: REFACTOR-02 (navigation/ folder deletion) refers to the OLD `src/components/navigation/`
    directory which was deleted in a prior milestone (v1.3). The CURRENT `src/components/ui/navigation/`
    is the VALID location and must NOT be deleted. REFACTOR-02 is already complete.

    NOTE: REFACTOR-05 (src/ folder structure) is satisfied by the current feature-based organization
    (ui/, app/, lib/, etc.) which was established in prior milestones. No action needed.

  </action>
  <verify>
    ```bash
    # Verify files deleted
    ls src/components/ui/*.stories.tsx 2>/dev/null && echo "FAIL: Stories exist" || echo "PASS: No stories in ui/"
    ls src/components/ui/menu/*.stories.tsx 2>/dev/null && echo "FAIL: Menu stories exist" || echo "PASS: No menu stories"
    ls src/components/ui/auth/AuthModal.tsx 2>/dev/null && echo "FAIL: AuthModal exists" || echo "PASS: AuthModal deleted"
    ```
  </verify>
  <done>All 12 files deleted, no active imports broken</done>
</task>

<task type="auto">
  <name>Task 2: Update auth barrel exports</name>
  <files>src/components/ui/auth/index.ts</files>
  <action>
    Update src/components/ui/auth/index.ts to remove exports for deleted files - REFACTOR-04.

    **Remove these lines:**
    ```typescript
    // Auth Modal
    export { AuthModal, AuthModal as AuthModalV7 } from "./AuthModal";
    export type { AuthModalProps, AuthModalProps as AuthModalV7Props } from "./AuthModal";

    export { MagicLinkSent } from "./MagicLinkSent";
    export type { MagicLinkSentProps } from "./MagicLinkSent";

    export { WelcomeAnimation } from "./WelcomeAnimation";
    export type { WelcomeAnimationProps } from "./WelcomeAnimation";

    // Onboarding
    export { OnboardingTour, OnboardingTour as OnboardingTourV7 } from "./OnboardingTour";
    export type {
      OnboardingTourProps,
      OnboardingTourProps as OnboardingTourV7Props,
      OnboardingStep,
    } from "./OnboardingTour";
    ```

    **Keep these exports:**
    - LoginForm
    - SignupForm
    - ForgotPasswordForm
    - ResetPasswordForm
    - UserMenu

  </action>
  <verify>
    ```bash
    # Verify barrel has no deleted exports
    grep -E "AuthModal|MagicLinkSent|OnboardingTour|WelcomeAnimation" src/components/ui/auth/index.ts && echo "FAIL: Dangling exports" || echo "PASS: Barrel clean"
    ```
  </verify>
  <done>auth/index.ts exports only existing files (5 components)</done>
</task>

<task type="auto">
  <name>Task 3: Create CLEANUP_LOG.md and verify build</name>
  <files>.planning/CLEANUP_LOG.md</files>
  <action>
    Create .planning/CLEANUP_LOG.md documenting all deletions:

    ```markdown
    # Cleanup Log

    **Phase:** 37-codebase-cleanup
    **Date:** [current date]

    ## Files Deleted

    ### Storybook Files (REFACTOR-01)
    | File | Reason | Lines |
    |------|--------|-------|
    | src/components/ui/Badge.stories.tsx | Production artifact | - |
    | src/components/ui/Button.stories.tsx | Production artifact | - |
    | src/components/ui/Input.stories.tsx | Production artifact | - |
    | src/components/ui/Modal.stories.tsx | Production artifact | - |
    | src/components/ui/Container.stories.tsx | Production artifact | - |
    | src/components/ui/Grid.stories.tsx | Production artifact | - |
    | src/components/ui/Stack.stories.tsx | Production artifact | - |
    | src/components/ui/menu/MenuAccordion.stories.tsx | Production artifact | - |

    ### Deprecated Auth Components (REFACTOR-03)
    | File | Reason | Lines |
    |------|--------|-------|
    | src/components/ui/auth/AuthModal.tsx | Superseded by direct forms | 465 |
    | src/components/ui/auth/MagicLinkSent.tsx | Unused | - |
    | src/components/ui/auth/OnboardingTour.tsx | Feature removed | 461 |
    | src/components/ui/auth/WelcomeAnimation.tsx | Feature removed | 449 |

    ### Already Completed
    - REFACTOR-02: navigation/ folder (6 files) - Already deleted in prior milestone

    ## Barrel Exports Updated (REFACTOR-04)
    | File | Exports Removed |
    |------|-----------------|
    | src/components/ui/auth/index.ts | AuthModal, AuthModalV7, MagicLinkSent, WelcomeAnimation, OnboardingTour, OnboardingTourV7 + their Props types |

    ## Summary
    - Files deleted: 12
    - Lines removed: ~1,375
    - Barrel exports updated: 1
    ```

    After creating the log, run full verification:
    ```bash
    pnpm lint && pnpm typecheck && pnpm build
    ```

  </action>
  <verify>
    ```bash
    # Build must succeed
    pnpm build
    echo "Exit code: $?"
    ```
  </verify>
  <done>CLEANUP_LOG.md created, build succeeds with 0 errors</done>
</task>

</tasks>

<verification>
1. `pnpm lint` passes (no dangling imports)
2. `pnpm typecheck` passes (no missing exports)
3. `pnpm build` succeeds (no broken imports)
4. No .stories.tsx files in src/components/ui/
5. No deleted auth components exist
6. auth/index.ts exports only 5 components
7. CLEANUP_LOG.md exists with deletion record
</verification>

<success_criteria>

- Build succeeds with no imports from deleted files
- All 12 files deleted via git rm
- auth/index.ts barrel updated (5 remaining exports)
- CLEANUP_LOG.md documents all deletions
  </success_criteria>

<output>
After completion, create `.planning/phases/37-codebase-cleanup/37-01-SUMMARY.md`
</output>
