---
phase: 59-monitoring-error-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/app/layout.tsx
  - src/lib/web-vitals.tsx
autonomous: true

must_haves:
  truths:
    - "Real user Core Web Vitals (LCP, CLS, INP) are visible in Vercel Speed Insights dashboard"
    - "Page views and referrer data are visible in Vercel Web Analytics dashboard"
    - "SpeedInsights component renders in production layout alongside existing Analytics component"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with SpeedInsights and Analytics components"
      contains: "SpeedInsights"
    - path: "src/lib/web-vitals.tsx"
      provides: "Dev-only CWV console reporter (production reporting removed)"
      contains: "WebVitalsReporter"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/speed-insights"
      via: "SpeedInsights component import"
      pattern: "import.*SpeedInsights.*speed-insights"
    - from: "src/app/layout.tsx"
      to: "@vercel/analytics"
      via: "Analytics component import (already exists)"
      pattern: "import.*Analytics.*analytics"
---

<objective>
Install @vercel/speed-insights and add the SpeedInsights component to the root layout for automatic Core Web Vitals reporting. Clean up web-vitals.tsx to remove redundant production reporting (sendBeacon, window.Sentry global access) since Speed Insights + Sentry SDK handle production CWV reporting automatically.

Purpose: MNTR-04 (Speed Insights), Vercel Web Analytics (already working via <Analytics />)
Output: SpeedInsights in layout, simplified web-vitals.tsx
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/layout.tsx
@src/lib/web-vitals.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vercel/speed-insights and add to layout</name>
  <files>
    package.json
    pnpm-lock.yaml
    src/app/layout.tsx
  </files>
  <action>
1. Install Speed Insights:
   ```bash
   pnpm add @vercel/speed-insights
   ```

2. Update `src/app/layout.tsx`:
   - ADD import: `import { SpeedInsights } from "@vercel/speed-insights/next";`
   - ADD `<SpeedInsights sampleRate={50} />` in the body, right before the existing `<Analytics />` component
   - Do NOT move or change any existing components or imports

The sampleRate of 50 (50%) balances free tier quota (10,000 data points/month on Hobby plan) with statistical significance. Each page view generates multiple data points (one per CWV metric).

The `<Analytics />` component (Vercel Web Analytics) is already present and working -- no changes needed for traffic analytics.
  </action>
  <verify>
    - `pnpm list @vercel/speed-insights` shows installed version
    - `src/app/layout.tsx` contains `<SpeedInsights` component
    - `src/app/layout.tsx` still contains `<Analytics />` component
    - `pnpm typecheck` passes
  </verify>
  <done>
    @vercel/speed-insights installed. SpeedInsights component renders in root layout at 50% sample rate. Existing Analytics component unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify web-vitals.tsx for dev-only reporting</name>
  <files>
    src/lib/web-vitals.tsx
  </files>
  <action>
Simplify `src/lib/web-vitals.tsx` to remove redundant production reporting. Speed Insights handles CWV reporting to Vercel automatically. Sentry SDK (from Plan 01) handles CWV measurements via its own integration. The manual `window.Sentry` global access and `sendBeacon` to `/api/analytics/vitals` are no longer needed.

Changes to `reportMetric` function:
1. REMOVE the `window.Sentry` global access block (lines 66-87). The Sentry SDK (initialized in `instrumentation-client.ts`) automatically captures web vitals via `browserTracingIntegration`. Accessing `window.Sentry` is an anti-pattern -- it uses an untyped global instead of the SDK's proper integration.

2. REMOVE the production `sendBeacon` block (lines 90-106). The `/api/analytics/vitals` endpoint does not exist -- this was a dead code path. Speed Insights handles all production CWV reporting.

3. KEEP the dev console.log block (lines 57-63). This is useful for development debugging.

4. KEEP all exports (`WebVitalsReporter`, `useWebVitals`, `initWebVitals`, `getPerformanceScore`, `WEB_VITALS_THRESHOLDS`). The `WebVitalsReporter` component is used in `layout.tsx` and provides dev-mode console output. Removing it would break imports.

Simplified `reportMetric`:
```typescript
function reportMetric(metric: Metric) {
  const { name, value } = metric;
  const rating = getRating(name as MetricName, value);

  // Console logging in development
  if (process.env.NODE_ENV === "development") {
    console.log(
      `%c[Web Vitals] ${name}: ${formatValue(name, value)} (${rating})`,
      `color: ${rating === "good" ? "#22c55e" : rating === "needs-improvement" ? "#eab308" : "#ef4444"}`
    );
  }
  // Production CWV reporting handled by:
  // - @vercel/speed-insights (Vercel dashboard)
  // - @sentry/nextjs browserTracingIntegration (Sentry dashboard)
}
```

The `id` and `navigationType` destructuring can be removed from the function since they are no longer used. The `Metric` type import stays.
  </action>
  <verify>
    - `src/lib/web-vitals.tsx` contains NO `window.Sentry` or `"Sentry" in window`
    - `src/lib/web-vitals.tsx` contains NO `sendBeacon`
    - `src/lib/web-vitals.tsx` still exports `WebVitalsReporter`
    - `pnpm typecheck` passes
    - `pnpm lint` passes
  </verify>
  <done>
    web-vitals.tsx simplified to dev-only console reporting. Production CWV reporting delegated to Speed Insights and Sentry SDK integrations. All exports preserved for backward compatibility.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm typecheck` passes with zero errors
2. `pnpm lint` passes
3. `pnpm build` succeeds
4. `src/app/layout.tsx` has both `<SpeedInsights />` and `<Analytics />` components
5. `src/lib/web-vitals.tsx` has no `window.Sentry` or `sendBeacon` references
6. No new unused imports or dead code
</verification>

<success_criteria>
- @vercel/speed-insights installed and SpeedInsights component in root layout at 50% sample rate
- Vercel Web Analytics already working (no changes needed)
- web-vitals.tsx cleaned up: dev console output only, no redundant production reporting
- All exports preserved for backward compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/59-monitoring-error-tracking/59-02-SUMMARY.md`
</output>
