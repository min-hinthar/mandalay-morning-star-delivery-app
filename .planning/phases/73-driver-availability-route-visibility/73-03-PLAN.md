---
phase: 73-driver-availability-route-visibility
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/(driver)/driver/history/page.tsx
  - src/app/(driver)/driver/history/DriverHistoryContent.tsx
  - src/app/api/driver/routes/history/route.ts
  - src/components/ui/admin/routes/CreateRouteModal/CreateRouteModal.tsx
  - src/components/ui/admin/routes/CreateRouteModal/types.ts
  - src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx
  - src/components/ui/admin/routes/RouteDetailClient/types.ts
  - src/app/api/admin/drivers/route.ts
autonomous: true
requirements: [DDASH-08, DDASH-09, DDASH-10, DUI-04]

must_haves:
  truths:
    - "Driver can filter history by Daily (14d), Weekly (12w), or Monthly (12mo) periods"
    - "Driver can load more results with a Load More button"
    - "History page shows per-period aggregate stats (route count, stop count, avg stops/route)"
    - "History page groups routes by month with collapsible month headers"
    - "Admin sees availability badge on driver cards in CreateRouteModal"
    - "Admin sees (Unavailable) suffix in DriverInfoCard reassignment Select"
  artifacts:
    - path: "src/app/(driver)/driver/history/DriverHistoryContent.tsx"
      provides: "Enhanced history with period toggle, pagination, monthly grouping"
    - path: "src/app/api/driver/routes/history/route.ts"
      provides: "Extended history API with total count"
    - path: "src/components/ui/admin/routes/CreateRouteModal/CreateRouteModal.tsx"
      provides: "Driver cards with availability badges"
    - path: "src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx"
      provides: "Select items with unavailable suffix"
  key_links:
    - from: "src/app/(driver)/driver/history/DriverHistoryContent.tsx"
      to: "src/app/api/driver/routes/history/route.ts"
      via: "fetch with limit param for pagination"
      pattern: "fetch.*history.*limit"
    - from: "src/components/ui/admin/routes/CreateRouteModal/CreateRouteModal.tsx"
      to: "src/lib/availability.ts"
      via: "isDriverAvailable helper"
      pattern: "isDriverAvailable"
    - from: "src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx"
      to: "src/lib/availability.ts"
      via: "isDriverAvailable helper"
      pattern: "isDriverAvailable"
---

<objective>
Enhance driver history page with period filtering, pagination, and monthly grouping. Add admin-facing availability indicators in route creation and driver reassignment UIs.

Purpose: Complete the history page experience and integrate driver availability into admin workflow at decision points.
Output: Enhanced history page, extended history API, admin availability badges/suffixes.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-driver-availability-route-visibility/73-RESEARCH.md
@.planning/phases/73-driver-availability-route-visibility/73-01-SUMMARY.md
@.planning/phases/73-driver-availability-route-visibility/73-02-SUMMARY.md

@src/app/(driver)/driver/history/DriverHistoryContent.tsx
@src/app/(driver)/driver/history/page.tsx
@src/app/api/driver/routes/history/route.ts
@src/app/(driver)/driver/earnings/EarningsPageClient.tsx
@src/components/ui/admin/routes/CreateRouteModal/CreateRouteModal.tsx
@src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx
@src/lib/availability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: History page enhancements (period filter, pagination, monthly grouping)</name>
  <files>
    src/app/(driver)/driver/history/page.tsx
    src/app/(driver)/driver/history/DriverHistoryContent.tsx
    src/app/api/driver/routes/history/route.ts
  </files>
  <action>
1. **Extend history API** (`src/app/api/driver/routes/history/route.ts`):
   - Add `offset` query param alongside existing `limit` (default 0): `const offset = parseInt(url.searchParams.get("offset") || "0", 10);`
   - Add Supabase `.range(offset, offset + limit - 1)` instead of `.limit(limit)` for proper pagination
   - Add total count query: `const { count } = await supabase.from("routes").select("id", { count: "exact", head: true }).eq("driver_id", driverId).eq("status", "completed");`
   - Return `totalRoutes: count ?? 0` alongside existing `routes` and `driver` fields
   - Also add `onTimePercentage` to driver stats: compute from `routes` average of `stats_json->completion_rate` or use existing driver stats

2. **Convert history page to client-side data fetching** (`src/app/(driver)/driver/history/page.tsx`):
   - Currently the page is a server component that fetches data. Convert to a simpler server wrapper that authenticates and passes initial data, with `DriverHistoryContent` handling subsequent fetches (pagination, filtering).
   - Keep auth check server-side. Fetch initial batch (limit 20, offset 0) from Supabase directly.
   - Pass initial data to `DriverHistoryContent` along with driver stats.

3. **Enhance DriverHistoryContent** (`src/app/(driver)/driver/history/DriverHistoryContent.tsx`):
   - Convert to `'use client'` component (already is)
   - Add `useState` for:
     - `selectedPeriod: "daily" | "weekly" | "monthly"` (default "weekly")
     - `routes: HistoryRouteData[]` (initialized from props)
     - `offset: number` (starts at routes.length from initial load)
     - `hasMore: boolean` (computed from totalRoutes vs loaded count)
     - `isLoadingMore: boolean`
   - **Period toggle**: Copy EXACT pattern from `EarningsPageClient.tsx`:
     ```tsx
     <div className="flex gap-0.5 rounded-lg border border-border bg-surface-primary p-1">
       {PERIODS.map(...)}
     </div>
     ```
     Periods: `{ value: "daily", label: "Daily" }, { value: "weekly", label: "Weekly" }, { value: "monthly", label: "Monthly" }`
   - **Client-side date filtering**: Use `getPeriodStartDate()` function (same logic as EarningsPageClient):
     - daily: 14 days back
     - weekly: 12 weeks back
     - monthly: 12 months back
     Filter `routes` by `route.date >= periodStartDate`
   - **Per-period aggregate stats**: Compute with `useMemo`:
     - Route count for period
     - Total stops for period (sum of `stopCount`)
     - Average stops per route (`totalStops / routeCount`)
     Display as 3-column `StatMiniCard` grid below the period toggle (reuse existing StatMiniCard pattern from current DriverHistoryContent)
   - **Monthly grouping**: Group filtered routes by `YYYY-MM` key. Render collapsible month headers:
     - Month header: `Intl.DateTimeFormat("en-US", { month: "long", year: "numeric" })` format
     - Use `AnimatePresence` + `m.div` for expand/collapse (same pattern as `HistorySummaryCard` expansion)
     - Header shows month name + route count badge
     - Default: all months expanded
     - Click header to collapse/expand
   - **Load More button**: Render below route list when `hasMore` is true:
     - Button text: "Load More" (or "Loading..." with Loader2 spinner when loading)
     - On click: `fetch(/api/driver/routes/history?limit=20&offset=${offset})`, append returned routes to state, increment offset
     - Style: full-width button, `rounded-xl border border-border bg-surface-primary py-3 text-sm font-medium text-text-secondary hover:bg-surface-secondary`
   - Keep existing `StatMiniCard` grid at top (Total Deliveries, Rating, On Time %) -- these are lifetime stats
   - Add the new period aggregate stats as a second row below the lifetime stats, labeled "Period Stats"
   - Keep `HistorySummaryCard` for individual route cards within month groups
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - History page renders with period toggle
    - Load More button appears and loads additional routes
    - Routes group by month with collapsible headers
  </verify>
  <done>
    - Period toggle filters history by Daily/Weekly/Monthly date ranges
    - Per-period aggregate stats display route count, total stops, avg stops/route
    - Routes grouped under collapsible month headers
    - Load More button paginated 20 routes at a time
    - Lifetime stats grid preserved at top
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin availability indicators in route creation and reassignment</name>
  <files>
    src/app/api/admin/drivers/route.ts
    src/components/ui/admin/routes/CreateRouteModal/CreateRouteModal.tsx
    src/components/ui/admin/routes/CreateRouteModal/types.ts
    src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx
    src/components/ui/admin/routes/RouteDetailClient/types.ts
  </files>
  <action>
1. **Extend admin drivers API** (`src/app/api/admin/drivers/route.ts`):
   - Add `availability_json` to the SELECT query in GET handler:
     ```
     id, user_id, ..., availability_json, profiles (email, full_name, phone)
     ```
   - Add `availability` field to response transform:
     ```typescript
     availability: driver.availability_json ?? null,
     ```
   - Update the `DriverWithProfile` interface to include `availability_json: DriverAvailability | null`

2. **Update CreateRouteModal types** (`src/components/ui/admin/routes/CreateRouteModal/types.ts`):
   - Add `availability?: DriverAvailability | null;` to the `Driver` interface
   - Import `DriverAvailability` from `@/types/driver`

3. **Add availability badge to CreateRouteModal** (`src/components/ui/admin/routes/CreateRouteModal/CreateRouteModal.tsx`):
   - Import `isDriverAvailable` from `@/lib/availability`
   - Import `DriverAvailability` from `@/types/driver`
   - Update `fetchDrivers` response mapping to include `availability: d.availability ?? null`
   - In the driver grid, for each driver button, compute availability:
     ```typescript
     const available = isDriverAvailable(driver.availability, deliveryDate);
     ```
   - Add availability badge below driver name in each driver card:
     - Available: `<span className="text-2xs px-1.5 py-0.5 rounded-full bg-green/10 text-green">Available</span>`
     - Unavailable: `<span className="text-2xs px-1.5 py-0.5 rounded-full bg-amber-500/10 text-amber-600">Unavailable</span>`
   - Unavailable drivers should still be selectable (not disabled) -- admin may override availability. Just show the visual indicator.
   - Badge recalculates when `deliveryDate` state changes

4. **Update DriverInfoCard types** (`src/components/ui/admin/routes/RouteDetailClient/types.ts`):
   - Add `availability?: DriverAvailability | null;` to `DriverOption` interface
   - Import `DriverAvailability` from `@/types/driver`

5. **Add unavailable suffix to DriverInfoCard** (`src/components/ui/admin/routes/RouteDetailClient/DriverInfoCard.tsx`):
   - Import `isDriverAvailable` from `@/lib/availability`
   - Accept `deliveryDate: string` as additional prop (from parent route detail page)
   - In the `Select` dropdown, for each driver `SelectItem`, check availability:
     ```typescript
     const available = isDriverAvailable(driver.availability, deliveryDate);
     const label = driver.fullName || driver.userId.slice(0, 8);
     const displayLabel = available ? label : `${label} (Unavailable)`;
     ```
   - Render `displayLabel` as the SelectItem text content
   - Note: DriverInfoCard's parent component needs to pass `deliveryDate` prop. Check `RouteDetailClient` for where `DriverInfoCard` is rendered and pass `route.deliveryDate` as the `deliveryDate` prop. If the parent needs updating, update it too (add the file to the files list if needed).
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Admin drivers API returns availability field
    - CreateRouteModal shows Available/Unavailable badges on driver cards
    - DriverInfoCard Select shows (Unavailable) suffix
  </verify>
  <done>
    - Admin drivers API includes availability_json in response
    - CreateRouteModal driver picker shows green/amber availability badge per driver for selected delivery date
    - DriverInfoCard reassignment Select appends (Unavailable) to unavailable driver names
    - Admin can still select unavailable drivers (visual indicator only, not blocking)
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` -- no type errors
- `pnpm lint` -- all files pass
- `pnpm build` -- production build succeeds
- History page shows period toggle with functional filtering
- Load More pagination works in history
- Monthly grouping with collapsible headers works
- CreateRouteModal driver cards have availability badges
- DriverInfoCard Select items show unavailable suffix
</verification>

<success_criteria>
- History page has 3-pill period toggle matching earnings page pattern
- Load More button increments 20 routes per tap
- Per-period aggregate stats compute and display correctly
- Monthly collapsible groups render with expand/collapse animation
- Admin CreateRouteModal shows availability badge per driver
- Admin DriverInfoCard Select shows (Unavailable) suffix
- All requirement IDs (DDASH-08, DDASH-09, DDASH-10, DUI-04) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/73-driver-availability-route-visibility/73-03-SUMMARY.md`
</output>
