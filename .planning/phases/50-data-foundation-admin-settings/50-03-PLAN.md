---
phase: 50-data-foundation-admin-settings
plan: 03
type: execute
wave: 2
depends_on: ["50-01", "50-02"]
files_modified:
  - src/components/ui/admin/settings/DeliverySettingsForm.tsx
  - src/components/ui/admin/settings/OperationsSettingsForm.tsx
  - src/components/ui/admin/settings/NotificationSettingsForm.tsx
  - src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx
  - src/app/api/admin/settings/route.ts
autonomous: true

must_haves:
  truths:
    - "Admin can configure delivery time windows and delivery zones with per-zone fees"
    - "Admin can set store hours (open/close per day, toggle closed days) and capacity limits per time slot"
    - "Admin can toggle low stock alerts and daily summary email"
    - "Saving settings shows morphing save button animation"
    - "Unsaved changes show floating bottom bar with Save + Discard"
    - "Modified fields show subtle left border accent"
    - "Tab switching with unsaved changes shows warning dialog"
    - "Restore defaults shows confirmation dialog"
    - "Admin settings load from database on page open, reflecting last saved values (ADMN-03)"
  artifacts:
    - path: "src/components/ui/admin/settings/DeliverySettingsForm.tsx"
      provides: "Delivery form with time windows and zones sections"
      contains: "DeliveryZone"
    - path: "src/components/ui/admin/settings/OperationsSettingsForm.tsx"
      provides: "Operations form with store hours and capacity limits"
      contains: "storeHours"
    - path: "src/components/ui/admin/settings/NotificationSettingsForm.tsx"
      provides: "Notifications form with low stock threshold and daily summary toggle"
      contains: "lowStockThreshold"
    - path: "src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx"
      provides: "Settings orchestrator using SaveButton, FloatingUnsavedBar, and confirmation dialogs"
      contains: "SaveButton"
  key_links:
    - from: "src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx"
      to: "src/components/ui/admin/settings/SaveButton.tsx"
      via: "import and render"
      pattern: "import.*SaveButton"
    - from: "src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx"
      to: "src/components/ui/admin/settings/FloatingUnsavedBar.tsx"
      via: "import and render"
      pattern: "import.*FloatingUnsavedBar"
    - from: "src/components/ui/admin/settings/DeliverySettingsForm.tsx"
      to: "src/components/ui/admin/settings/settings-types.ts"
      via: "import expanded types"
      pattern: "DeliveryTimeWindow|DeliveryZone"
---

<objective>
Expand admin settings forms with new fields (time windows, zones, store hours, capacity, alerts, daily summary) and integrate the save UX components (SaveButton, FloatingUnsavedBar, dialogs, changed-field highlights) into SettingsClient.

Purpose: This is the core UI integration plan -- brings together the DB schema (Plan 01) and UI components (Plan 02) into the working admin settings experience.
Output: Updated form components with new fields + upgraded SettingsClient with premium save UX.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-data-foundation-admin-settings/50-01-SUMMARY.md
@.planning/phases/50-data-foundation-admin-settings/50-02-SUMMARY.md
@src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx
@src/components/ui/admin/settings/DeliverySettingsForm.tsx
@src/components/ui/admin/settings/OperationsSettingsForm.tsx
@src/components/ui/admin/settings/NotificationSettingsForm.tsx
@src/components/ui/admin/settings/settings-types.ts
@src/components/ui/admin/settings/SaveButton.tsx
@src/components/ui/admin/settings/FloatingUnsavedBar.tsx
@src/components/ui/admin/settings/ToggleSwitch.tsx
@src/components/ui/admin/settings/RestoreDefaultsDialog.tsx
@src/components/ui/admin/settings/ConfirmDialog.tsx
@src/app/api/admin/settings/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand form components with new settings fields</name>
  <files>
    src/components/ui/admin/settings/DeliverySettingsForm.tsx
    src/components/ui/admin/settings/OperationsSettingsForm.tsx
    src/components/ui/admin/settings/NotificationSettingsForm.tsx
  </files>
  <action>
**DeliverySettingsForm.tsx:**

Update props interface to accept expanded `DeliverySettings` (now includes `deliveryTimeWindows` and `deliveryZones`). Also accept `originalSettings: DeliverySettings` prop for changed-field highlighting.

Add two new collapsible sections below existing fields:

1. **Delivery Time Windows** section:
   - Display existing time windows as a list of cards showing start-end times + optional label
   - "Add Time Window" button that appends `{start: "09:00", end: "12:00", label: ""}` to the array
   - Each window has: start time input (type="time"), end time input (type="time"), label text input, remove (X) button
   - Validation: start must be before end

2. **Delivery Zones** section (inline in Delivery tab per discretion recommendation):
   - Display existing zones as a list of cards showing name, fee, description
   - "Add Zone" button that appends `{name: "", feeCents: 0, description: ""}`
   - Each zone has: name input, fee input (currency — use dollars display / cents storage like existing pattern), description textarea, remove (X) button

**Changed-field highlighting:** Per CONTEXT.md: "Subtle left border accent on modified fields."

- Accept `originalSettings` prop
- For each field, compare current value to original value
- If different, add `border-l-2 border-l-primary pl-3` class to the field wrapper div
- Use a helper: `const isChanged = (field: keyof DeliverySettings) => JSON.stringify(settings[field]) !== JSON.stringify(originalSettings[field])`

Import `ToggleSwitch` from `./ToggleSwitch` (shared component from Plan 02) if any toggles needed.

**OperationsSettingsForm.tsx:**

Update to accept expanded `OperationsSettings` (now includes `storeHours` and `maxOrdersPerSlot`). Accept `originalSettings` prop.

Replace inline `ToggleSwitch` with import from `./ToggleSwitch`.

Add two new sections:

1. **Store Hours** section:
   - 7 rows, one per day of week (Monday-Sunday)
   - Each row: day name label, "Closed" toggle, open time input (type="time"), close time input (type="time")
   - When "Closed" is toggled ON, gray out the time inputs (disabled)
   - Per CONTEXT.md: "Simple open/close per day -- NO breaks/lunch gaps"

2. **Capacity Limits** section:
   - Single number input: "Max Orders Per Time Slot" (min 1, max 100)
   - Helper text: "Maximum orders that can be scheduled in each delivery time window"
   - This ties to delivery time windows conceptually

Add changed-field highlighting using `originalSettings` comparison.

**NotificationSettingsForm.tsx:**

Update to accept expanded `NotificationSettings` (now includes `lowStockThreshold` and `dailySummaryEnabled`). Accept `originalSettings` prop.

Replace inline `ToggleSwitch` with import from `./ToggleSwitch`.

Add a new **Admin Alerts** section below existing sections:

1. **Low Stock Alerts**:
   - Toggle: "Low Stock Alerts" with description "Get notified when any item's stock falls below threshold"
   - When enabled, show number input for threshold value (min 0, max 1000)
   - Per CONTEXT.md: "Global threshold, not per-item"

2. **Daily Summary Email**:
   - Toggle: "Daily Summary Email" with description "Receive a daily email summarizing orders and activity"
   - Per CONTEXT.md: "Configurable toggle, not always-on"

Add changed-field highlighting using `originalSettings` comparison.
</action>
<verify>
Run `pnpm typecheck` — must pass (forms accept new expanded types).
Verify each form:

1. DeliverySettingsForm renders time windows and zones sections
2. OperationsSettingsForm renders store hours grid and capacity input
3. NotificationSettingsForm renders low stock threshold and daily summary toggle
4. All forms use shared ToggleSwitch import (no inline duplicates)
5. All forms accept and use originalSettings for changed-field highlighting
   </verify>
   <done>All 3 form components expanded with new fields, shared ToggleSwitch, and changed-field left-border highlighting</done>
   </task>

<task type="auto">
  <name>Task 2: Upgrade SettingsClient with save UX and confirmation dialogs</name>
  <files>
    src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx
    src/app/api/admin/settings/route.ts
  </files>
  <action>
Major refactor of `SettingsClient.tsx` to integrate all Plan 02 components and support expanded settings.

**Replace existing save button with SaveButton component:**

- Remove the inline save button JSX from the header
- Import and render `<SaveButton onClick={handleSave} hasChanges={hasChanges} disabled={restoring} />`

**Replace existing unsaved changes indicator with FloatingUnsavedBar:**

- Remove the existing `AnimatePresence` block that shows "You have unsaved changes" amber bar
- Import and render `<FloatingUnsavedBar show={hasChanges} onSave={handleSave} onDiscard={() => setShowDiscardDialog(true)} isSaving={saving} />`
- Note: Discard button opens ConfirmDialog (see below)

**Replace `window.confirm()` in handleRestoreDefaults with RestoreDefaultsDialog:**

- Remove the `if (!confirm(...))` call
- Add `showRestoreDialog` state
- Render `<RestoreDefaultsDialog open={showRestoreDialog} onConfirm={executeRestore} onCancel={() => setShowRestoreDialog(false)} isRestoring={restoring} />`

**Add tab-switch warning with ConfirmDialog:**

- Per CONTEXT.md: "Warn about unsaved changes before switching tabs"
- Track `pendingTabId` state
- On tab change: if `hasChanges`, set `pendingTabId` and show ConfirmDialog instead of switching
- ConfirmDialog: title "Unsaved Changes", description "Discard changes to current tab?", confirmLabel "Discard", cancelLabel "Cancel"
- On confirm: discard changes for current tab, switch to pending tab
- On cancel: clear pendingTabId

**Add Discard confirmation from FloatingUnsavedBar:**

- Per CONTEXT.md: "Discard unsaved changes?" dialog before reverting
- Track `showDiscardDialog` state
- When FloatingUnsavedBar Discard is clicked, show ConfirmDialog
- ConfirmDialog: title "Discard Changes", description "Discard all unsaved changes? This can't be undone.", confirmLabel "Discard", confirmVariant "destructive"
- On confirm: reset settings to originalSettings, close dialog

**Update DEFAULT_SETTINGS to include new fields:**

- Add `deliveryTimeWindows: []` and `deliveryZones: []` to delivery defaults
- Add `storeHours: {monday: {open:"09:00", close:"21:00", closed:false}, ...}` and `maxOrdersPerSlot: 20` to operations defaults
- Add `lowStockThreshold: 10` and `dailySummaryEnabled: false` to notifications defaults

**Verify API route returns new keys (ADMN-03 compliance):**

- Read `src/app/api/admin/settings/route.ts` GET handler
- Confirm it queries ALL settings from `app_settings` (not a hardcoded WHERE IN list that would miss new keys)
- If the route filters by key name, add the 5 new keys: `delivery_zones`, `store_hours`, `max_orders_per_slot`, `low_stock_threshold`, `daily_summary_enabled`
- If the route uses `camelCaseKey()` conversion, verify new snake_case keys map correctly to camelCase
- If route needs updates, modify it to include new keys

**Update fetchSettings mapping:**

- Map new camelCase fields from API response (deliveryTimeWindows, deliveryZones, storeHours, maxOrdersPerSlot, lowStockThreshold, dailySummaryEnabled)
- Use `?? DEFAULT_SETTINGS.*` fallback pattern like existing fields
- After mapping, verify that saved values load from DB (not always falling back to DEFAULT_SETTINGS)

**Pass originalSettings to each form:**

- `<DeliverySettingsForm settings={settings.delivery} originalSettings={originalSettings.delivery} onChange={handleDeliveryChange} />`
- Same for Operations and Notifications forms

**Optimistic update pattern:**
Per CONTEXT.md: "Optimistic update — UI updates immediately, rolls back on server failure."

- The existing save flow already does this (settings state is local, only synced on save)
- On save failure: keep user's changes in form + show persistent error banner at top with retry button
- Add `saveError` state. On failure, set error message. Render error banner at top of page:
  ```
  <div className="bg-red-50 border border-red-200 rounded-card-sm p-3 mb-4 flex items-center justify-between">
    <p className="text-sm text-red-700">{saveError}</p>
    <Button variant="outline" size="sm" onClick={handleSave}>Retry</Button>
  </div>
  ```
- Clear error on successful save

**Important:** Keep file under 400 lines. If it exceeds, the SettingsClient subfolder pattern already exists — extract helpers (DEFAULT_SETTINGS, fetch mapping) into a sibling file like `settings-defaults.ts`.
</action>
<verify>
Run `pnpm typecheck && pnpm lint` — must pass.
Verify:

1. SaveButton renders in place of old inline button
2. FloatingUnsavedBar replaces old amber indicator
3. RestoreDefaultsDialog replaces window.confirm()
4. Tab switching with unsaved changes shows ConfirmDialog
5. Discard from FloatingUnsavedBar shows ConfirmDialog
6. Save error shows persistent banner with retry
7. originalSettings passed to all 3 form components
8. New default settings fields present
9. File stays under 400 lines (or is properly split)
10. GET /api/admin/settings returns all 5 new keys (not filtered out)
11. Settings page loads saved values from DB, not always DEFAULT_SETTINGS
    </verify>
    <done>SettingsClient fully upgraded with SaveButton morphing animation, FloatingUnsavedBar slide-up, tab-switch/discard/restore confirmation dialogs, changed-field highlighting, and error recovery with retry</done>
    </task>

</tasks>

<verification>
1. `pnpm typecheck && pnpm lint` pass
2. Admin settings page loads with all new fields visible
3. Delivery tab shows time windows and zones management
4. Operations tab shows store hours grid and capacity input
5. Notifications tab shows low stock threshold and daily summary toggles
6. Saving triggers morphing button animation
7. Making changes shows floating bar at bottom
8. Tab switching with changes shows warning
9. Restore defaults shows confirmation dialog
10. Modified fields have left border accent
</verification>

<success_criteria>

- All 5 success criteria from roadmap are achievable after this plan:
  1. Admin can view and edit delivery, operations, and notification settings (expanded fields)
  2. Settings persist to Supabase (existing API routes handle new keys via upsert)
  3. Settings load from database (fetch mapping updated for new fields)
  4. Saving shows success animation (SaveButton morph)
  5. Customer settings table exists (Plan 01)
- No `window.confirm()` calls remain -- all replaced with proper dialogs
- Shared ToggleSwitch used everywhere (no inline duplicates)
- File size stays under 400 lines per file
- `pnpm typecheck && pnpm lint` pass
  </success_criteria>

<output>
After completion, create `.planning/phases/50-data-foundation-admin-settings/50-03-SUMMARY.md`
</output>
