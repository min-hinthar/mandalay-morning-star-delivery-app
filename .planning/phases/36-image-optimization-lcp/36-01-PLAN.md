---
phase: 36-image-optimization-lcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/lib/utils/image-optimization.ts
autonomous: true

must_haves:
  truths:
    - "Next.js image optimization uses quality 70 for menu photos"
    - "Image configuration includes qualities array for Next.js 16 compatibility"
    - "Default quality in getImageProps returns 70 for non-hero sizes"
  artifacts:
    - path: "next.config.ts"
      provides: "qualities configuration for Next.js 16"
      contains: "qualities:"
    - path: "src/lib/utils/image-optimization.ts"
      provides: "Updated quality defaults (70 instead of 85)"
      contains: "quality: options?.quality ?? 70"
  key_links:
    - from: "src/lib/utils/image-optimization.ts"
      to: "next.config.ts"
      via: "quality values must match qualities array"
      pattern: "qualities:.*70"
---

<objective>
Configure Next.js 16 image optimization settings and update quality defaults.

Purpose: Establish foundation for image optimization by adding required Next.js 16 config and reducing default quality to 70% for smaller file sizes while maintaining visual quality.
Output: Updated next.config.ts with qualities array, updated image-optimization.ts with quality=70 default.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-image-optimization-lcp/36-RESEARCH.md

@next.config.ts
@src/lib/utils/image-optimization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add qualities array to next.config.ts</name>
  <files>next.config.ts</files>
  <action>
Add `qualities: [70, 85]` to the images configuration in next.config.ts. This is REQUIRED for Next.js 16 per research findings.

Location: Inside the `images: { ... }` block, add:

```typescript
qualities: [70, 85], // Required for Next.js 16 - 70 for menu, 85 for hero
```

Place it after `formats` and before `deviceSizes`.

Why: Next.js 16 requires explicit qualities configuration. 70 is for menu card images (smaller files), 85 is for hero images (higher quality for LCP).
</action>
<verify>
Build succeeds with `pnpm build` (no config errors).
Grep config: `grep -n "qualities" next.config.ts` shows the new array.
</verify>
<done>next.config.ts has qualities: [70, 85] in images config</done>
</task>

<task type="auto">
  <name>Task 2: Update image-optimization.ts quality defaults</name>
  <files>src/lib/utils/image-optimization.ts</files>
  <action>
Update the default quality in `getImageProps` from 85 to 70:

```typescript
// Change this line:
quality: options?.quality ?? 85,
// To:
quality: options?.quality ?? 70,
```

Also add a JSDoc comment explaining the quality choice:

```typescript
/**
 * Get optimized image props for a given size preset
 *
 * @param size - Size preset key from IMAGE_SIZES
 * @param options - Optional overrides for priority and quality
 * @returns Partial ImageProps for next/image
 *
 * Note: Default quality is 70 for smaller file sizes.
 * Use quality: 85 for hero images where quality matters more.
 */
```

Why: 70% quality provides good visual quality with ~30% smaller files than 85%. Hero images can override to 85 when needed.
</action>
<verify>
TypeScript compiles: `pnpm typecheck` passes.
Verify default: `grep "quality:" src/lib/utils/image-optimization.ts` shows 70.
</verify>
<done>getImageProps defaults to quality 70, JSDoc documents the choice</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and run type checks</name>
  <files>N/A</files>
  <action>
Run full verification to ensure changes don't break the build:

1. `pnpm typecheck` - TypeScript compilation
2. `pnpm lint` - ESLint rules
3. `pnpm build` - Full production build

All must pass with no errors.
</action>
<verify>
All three commands exit with code 0.
No TypeScript errors, no ESLint errors, build completes.
</verify>
<done>Build and type checks pass with updated configuration</done>
</task>

</tasks>

<verification>
- [ ] next.config.ts has `qualities: [70, 85]` in images config
- [ ] image-optimization.ts getImageProps defaults to quality 70
- [ ] pnpm typecheck passes
- [ ] pnpm lint passes
- [ ] pnpm build passes
</verification>

<success_criteria>
Configuration changes complete:

1. Next.js 16 qualities array configured (70, 85)
2. Default image quality reduced to 70 for smaller file sizes
3. All builds and type checks pass
4. IMAGE-05 requirement addressed (non-hero images use quality=70)
   </success_criteria>

<output>
After completion, create `.planning/phases/36-image-optimization-lcp/36-01-SUMMARY.md`
</output>
