---
phase: 54-email-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/020_email_system.sql
  - src/lib/email/index.ts
  - src/lib/email/client.ts
  - src/lib/email/send.ts
  - src/lib/email/types.ts
  - src/lib/email/constants.ts
  - src/types/database.ts
  - package.json
  - next.config.ts
autonomous: true
user_setup:
  - service: resend
    why: "Email sending provider"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify domain mail.mandalaymorningstar.com"
        location: "Resend Dashboard -> Domains -> Add Domain"

must_haves:
  truths:
    - "webhook_events table exists with UNIQUE constraint on event_id"
    - "notification_type enum includes cancellation, refund, delivery_reminder"
    - "notification_status enum includes opened, clicked"
    - "sendEmail() checks admin kill switch, user preference, renders, sends with retry, and logs"
    - "Resend SDK client is initialized as a singleton"
  artifacts:
    - path: "supabase/migrations/020_email_system.sql"
      provides: "webhook_events table + enum expansions"
      contains: "webhook_events"
    - path: "src/lib/email/send.ts"
      provides: "sendEmail function with retry, preference check, logging"
      exports: ["sendEmail"]
    - path: "src/lib/email/client.ts"
      provides: "Resend client singleton"
      exports: ["getResendClient"]
    - path: "src/lib/email/types.ts"
      provides: "EmailType, SendEmailOptions, NotificationPrefs mapping"
      exports: ["EmailType", "SendEmailOptions"]
    - path: "src/lib/email/constants.ts"
      provides: "Brand constants for email"
      exports: ["EMAIL_FROM", "APP_URL"]
  key_links:
    - from: "src/lib/email/send.ts"
      to: "src/lib/email/client.ts"
      via: "getResendClient() import"
      pattern: "getResendClient"
    - from: "src/lib/email/send.ts"
      to: "notification_logs table"
      via: "supabase insert"
      pattern: "notification_logs"
    - from: "src/lib/email/send.ts"
      to: "customer_settings table"
      via: "notification_prefs check"
      pattern: "customer_settings"
---

<objective>
Install email packages, create DB migration for webhook idempotency table + enum expansions, build the email service layer with sendEmail() function.

Purpose: Foundation for all email sending — every subsequent plan depends on this service layer.
Output: Working email infrastructure (DB migration + service layer + Resend client)
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-RESEARCH.md
@src/lib/supabase/server.ts
@src/lib/utils/logger.ts
@src/components/ui/account/SettingsTab/settings-types.ts
@supabase/migrations/000_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages + DB migration</name>
  <files>
    package.json
    supabase/migrations/020_email_system.sql
    next.config.ts
    src/types/database.ts
  </files>
  <action>
    1. Install packages:
       ```bash
       pnpm add resend @react-email/components @react-email/render
       pnpm add -D react-email
       ```

    2. Create migration `supabase/migrations/020_email_system.sql`:
       - CREATE TABLE webhook_events: id UUID PK, event_id TEXT NOT NULL UNIQUE, event_type TEXT NOT NULL, processed_at TIMESTAMPTZ DEFAULT NOW(), created_at TIMESTAMPTZ DEFAULT NOW()
       - CREATE INDEX idx_webhook_events_event_id ON webhook_events(event_id)
       - ALTER TYPE notification_type ADD VALUE IF NOT EXISTS 'cancellation'
       - ALTER TYPE notification_type ADD VALUE IF NOT EXISTS 'refund'
       - ALTER TYPE notification_type ADD VALUE IF NOT EXISTS 'delivery_reminder'
       - ALTER TYPE notification_status ADD VALUE IF NOT EXISTS 'opened'
       - ALTER TYPE notification_status ADD VALUE IF NOT EXISTS 'clicked'
       - Enable RLS on webhook_events with service-role-only policies (no public access)
       - INSERT INTO app_settings (key, value, category, label) the email_sending_enabled setting defaulting to true, category 'email', only if not exists

    3. Add `serverExternalPackages: ['@react-email/render']` to `next.config.ts` experimental section (prevents Turbopack bundling issues with prettier dependency).

    4. Update `src/types/database.ts` to add 'cancellation' | 'refund' | 'delivery_reminder' to the NotificationType union, 'opened' | 'clicked' to NotificationStatus, and add WebhookEvents table type with id, event_id, event_type, processed_at, created_at.
  </action>
  <verify>
    - `pnpm ls resend @react-email/components @react-email/render` shows all three installed
    - Migration file exists at correct path with webhook_events CREATE TABLE and ALTER TYPE statements
    - `pnpm build` succeeds (no serverExternalPackages errors)
  </verify>
  <done>
    Packages installed, migration ready, next.config.ts updated, database types extended
  </done>
</task>

<task type="auto">
  <name>Task 2: Email service layer</name>
  <files>
    src/lib/email/client.ts
    src/lib/email/types.ts
    src/lib/email/constants.ts
    src/lib/email/send.ts
    src/lib/email/index.ts
  </files>
  <action>
    1. Create `src/lib/email/types.ts`:
       - Export `EmailType` = 'order_confirmation' | 'cancellation' | 'refund' | 'delivery_reminder' | 'out_for_delivery' | 'arriving_soon' | 'delivered'
       - Export `SendEmailOptions` interface: { to: string, subject: string, react: React.ReactElement, type: EmailType, orderId: string, userId: string, idempotencyKey?: string, mandatory?: boolean }
       - Export `MANDATORY_EMAIL_TYPES` = ['order_confirmation', 'refund'] as const
       - Export `mapTypeToPrefKey()` function mapping EmailType to NotificationPrefs keys: order_confirmation -> 'order_updates', cancellation -> 'order_updates', refund -> 'order_updates', delivery_reminder -> 'reminders', others -> 'order_updates'

    2. Create `src/lib/email/constants.ts`:
       - EMAIL_FROM = 'Mandalay Morning Star <orders@mail.mandalaymorningstar.com>'
       - EMAIL_REPLY_TO = 'support@mandalaymorningstar.com'
       - APP_URL = process.env.NEXT_PUBLIC_APP_URL || 'https://mandalaymorningstar.com'
       - BRAND_COLORS = { primary: '#A41034', secondary: '#EBCD00', accent: '#3D8B22', warmBg: '#FFF9E6', darkBrown: '#8B4513', gold: '#D4A017' }
       - BUSINESS_ADDRESS = '750 Terrado Plaza, Suite 33, Covina, CA 91723'
       - MAX_RETRY_ATTEMPTS = 3
       - RETRY_BASE_DELAY_MS = 10_000

    3. Create `src/lib/email/client.ts`:
       - Singleton pattern: let instance: Resend | null = null
       - Export `getResendClient()`: checks RESEND_API_KEY env var, creates Resend instance, caches, returns
       - Throw clear error if RESEND_API_KEY missing

    4. Create `src/lib/email/send.ts`:
       - Import createServiceClient, logger, getResendClient, render from @react-email/render, types, constants
       - Export async function sendEmail(options: SendEmailOptions): Promise<{ success: boolean; resendId?: string; error?: string }>
       - Step 1: Check admin kill switch via app_settings.email_sending_enabled (use service client)
       - Step 2: Check user notification preference (skip for mandatory emails per MANDATORY_EMAIL_TYPES). Query customer_settings.notification_prefs, use mapTypeToPrefKey(). If customer_settings record doesn't exist for the user, treat all preferences as true (opted-in by default — new customers get all emails). If pref is false, return early with { success: true } (respected preference)
       - Step 3: Render React element to HTML via `await render(options.react)` and plain text via `await render(options.react, { plainText: true })`
       - Step 4: Send via Resend with retry loop (MAX_RETRY_ATTEMPTS). Use getResendClient().emails.send() with: from (EMAIL_FROM), to, subject, html, text, tags [{name:'type', value:options.type}, {name:'order_id', value:options.orderId}], headers (List-Unsubscribe pointing to APP_URL/account?tab=settings, List-Unsubscribe-Post). Pass idempotencyKey if provided.
       - Step 5: On success, insert into notification_logs: order_id, user_id, notification_type (options.type), channel='email', recipient, subject, resend_id (data.id), status='sent', sent_at
       - Step 6: On failure after all retries, insert notification_logs with status='failed' and error_message. Log via logger.error.
       - Retry backoff: attempt * RETRY_BASE_DELAY_MS (10s, 20s, 30s)

    5. Create `src/lib/email/index.ts`:
       - Barrel re-export everything from client.ts, send.ts, types.ts, constants.ts
  </action>
  <verify>
    - `pnpm typecheck` passes
    - All files exist under src/lib/email/
    - sendEmail function is exported from src/lib/email/index.ts
  </verify>
  <done>
    Email service layer complete: singleton client, sendEmail with preference check + retry + logging, types, constants, barrel exports
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` succeeds
- All 6 src/lib/email/*.ts files exist
- Migration 020 exists with webhook_events + enum expansions
- resend, @react-email/components, @react-email/render in dependencies
</verification>

<success_criteria>
Email infrastructure is complete: packages installed, DB migration ready, sendEmail() function handles kill switch, preference check, rendering, sending with retry, and logging.
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-01-SUMMARY.md`
</output>
