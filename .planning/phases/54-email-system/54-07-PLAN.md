---
phase: 54-email-system
plan: 07
type: execute
wave: 4
depends_on: ["54-05", "54-06"]
files_modified:
  - src/app/api/admin/emails/route.ts
  - src/app/api/admin/emails/[id]/route.ts
  - src/app/api/admin/emails/[id]/resend/route.ts
  - src/app/api/admin/emails/send/route.ts
  - src/app/api/emails/test/route.ts
autonomous: true

must_haves:
  truths:
    - "Admin can view paginated email log with search by order ID, filter by type/status, date range"
    - "Admin can view single email detail including delivery status timeline"
    - "Admin can resend a failed email"
    - "Admin can manually trigger any email type for any order"
    - "Admin can send test emails per type from settings"
  artifacts:
    - path: "src/app/api/admin/emails/route.ts"
      provides: "GET email log list with filtering"
      exports: ["GET"]
    - path: "src/app/api/admin/emails/[id]/route.ts"
      provides: "GET single email detail"
      exports: ["GET"]
    - path: "src/app/api/admin/emails/[id]/resend/route.ts"
      provides: "POST resend failed email"
      exports: ["POST"]
    - path: "src/app/api/admin/emails/send/route.ts"
      provides: "POST manual email trigger"
      exports: ["POST"]
    - path: "src/app/api/emails/test/route.ts"
      provides: "POST send test email"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/admin/emails/route.ts"
      to: "notification_logs table"
      via: "SELECT with filters"
      pattern: "notification_logs"
    - from: "src/app/api/admin/emails/[id]/resend/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendEmail() re-trigger"
      pattern: "sendEmail"
    - from: "src/app/api/admin/emails/send/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendEmail() manual trigger"
      pattern: "sendEmail"
---

<objective>
Build admin email management API routes: email log, detail view, resend, manual trigger, and test email.

Purpose: Admin can monitor email delivery, resend failed emails, manually trigger emails, and send test emails.
Output: 5 admin API routes for email management
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-01-SUMMARY.md
@.planning/phases/54-email-system/54-05-SUMMARY.md
@src/lib/auth/admin.ts
@src/lib/email/send.ts
@src/lib/email/types.ts
@src/emails/OrderConfirmation.tsx
@src/emails/OrderCancellation.tsx
@src/emails/RefundNotification.tsx
@src/emails/DeliveryReminder.tsx
@src/emails/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Email log list + detail API routes</name>
  <files>
    src/app/api/admin/emails/route.ts
    src/app/api/admin/emails/[id]/route.ts
  </files>
  <action>
    **1. Create `src/app/api/admin/emails/route.ts` — GET email log list:**
    - Auth: requireAdmin()
    - Query params:
      - `orderId` (string) — filter by order_id
      - `type` (string) — filter by notification_type
      - `status` (string) — filter by status
      - `from` (ISO string) — filter created_at >= from
      - `to` (ISO string) — filter created_at <= to
      - `page` (number, default 1) — pagination
      - `limit` (number, default 20, max 100) — page size
      - `sort` (string, default 'created_at') — sort column
      - `order` (string, default 'desc') — sort direction
    - Build Supabase query: select('id, order_id, user_id, notification_type, channel, recipient, subject, resend_id, status, error_message, sent_at, created_at')
    - Apply filters conditionally
    - Apply pagination: range((page-1)*limit, page*limit-1)
    - Also get total count for pagination metadata
    - Return: { data: [...], pagination: { page, limit, total, totalPages } }

    **2. Create `src/app/api/admin/emails/[id]/route.ts` — GET single email detail:**
    - Auth: requireAdmin()
    - Query notification_logs by id with full fields including metadata
    - Return: { data: { ...email, statusTimeline } }
    - statusTimeline: extract from metadata.resend_events array if present, format as: [{ status, timestamp }]
    - Include: can the email be resent? (status === 'failed')
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Both files exist
    - List endpoint supports all filter params
    - Detail endpoint returns statusTimeline
  </verify>
  <done>
    Admin email log API with pagination, filtering (order ID, type, status, date range), and detail view with delivery status timeline
  </done>
</task>

<task type="auto">
  <name>Task 2: Resend, manual trigger, and test email API routes</name>
  <files>
    src/app/api/admin/emails/[id]/resend/route.ts
    src/app/api/admin/emails/send/route.ts
    src/app/api/emails/test/route.ts
  </files>
  <action>
    **1. Create `src/app/api/admin/emails/[id]/resend/route.ts` — POST resend failed email:**
    - Auth: requireAdmin()
    - Fetch the notification_log entry by id
    - Validate status is 'failed' (only resend failed emails)
    - Fetch the original order data needed to reconstruct the email template
    - Determine email type from notification_type, create appropriate React element
    - Call sendEmail() with new idempotencyKey: `resend-${logId}-${Date.now()}`
    - Return { success: true, newLogId: ... }

    **2. Create `src/app/api/admin/emails/send/route.ts` — POST manual email trigger:**
    - Auth: requireAdmin()
    - Body: { orderId: string, emailType: EmailType }
    - Validate orderId exists, fetch full order data with customer info
    - Based on emailType, create appropriate React element with order data:
      - 'order_confirmation' -> OrderConfirmation
      - 'cancellation' -> OrderCancellation
      - 'refund' -> RefundNotification
      - 'delivery_reminder' -> DeliveryReminder
    - Call sendEmail() with idempotencyKey: `manual-${emailType}-${orderId}-${Date.now()}`
    - Return { success: true }

    **3. Create `src/app/api/emails/test/route.ts` — POST send test email:**
    - Auth: requireAdmin()
    - Body: { emailType: EmailType, recipientEmail: string }
    - Uses fixtures data (import from src/emails/fixtures.ts) to create email with sample data
    - Based on emailType, create React element with fixture data
    - Send via Resend directly (bypass sendEmail preference checks — test emails are admin-initiated)
    - Use getResendClient().emails.send() with fixture-based HTML
    - Idempotency key: `test-${emailType}-${Date.now()}`
    - Return { success: true, emailId: ... }

    **Helper function (shared in this file or in src/lib/email/):**
    - `buildEmailElement(type: EmailType, orderData: OrderData): React.ReactElement` — factory function that maps email type to the correct template component with proper props. Reused by resend and manual trigger routes.
    - Place in `src/lib/email/build.ts` and export from barrel.
  </action>
  <verify>
    - `pnpm typecheck` passes
    - All three files exist
    - Resend route validates status is 'failed' before resending
    - Manual trigger accepts any valid EmailType
    - Test email uses fixture data, not real order data
  </verify>
  <done>
    Admin can resend failed emails, manually trigger any email for any order, and send test emails with fixture data
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` succeeds
- All 5 API route files exist
- All routes use requireAdmin() for auth
- Email log supports pagination and filtering
</verification>

<success_criteria>
Complete admin email management API: searchable/filterable email log, detail view with delivery timeline, resend failed emails, manual trigger for any order, and test email sending.
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-07-SUMMARY.md`
</output>
