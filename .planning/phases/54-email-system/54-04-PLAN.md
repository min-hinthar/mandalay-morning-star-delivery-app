---
phase: 54-email-system
plan: 04
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - src/emails/RefundNotification.tsx
  - src/emails/DeliveryReminder.tsx
autonomous: true

must_haves:
  truths:
    - "RefundNotification shows detailed breakdown: original amount, refund amount, method, timeline, items refunded"
    - "RefundNotification handles partial refunds with distinct treatment from full refunds"
    - "DeliveryReminder shows food excitement, delivery details, static map link, and modify/cancel CTAs"
  artifacts:
    - path: "src/emails/RefundNotification.tsx"
      provides: "MAIL-03 refund email template"
      exports: ["RefundNotification"]
    - path: "src/emails/DeliveryReminder.tsx"
      provides: "MAIL-04 delivery reminder email template"
      exports: ["DeliveryReminder"]
  key_links:
    - from: "src/emails/RefundNotification.tsx"
      to: "src/emails/components/EmailLayout.tsx"
      via: "EmailLayout wrapper"
      pattern: "EmailLayout"
    - from: "src/emails/DeliveryReminder.tsx"
      to: "src/emails/components/EmailLayout.tsx"
      via: "EmailLayout wrapper"
      pattern: "EmailLayout"
    - from: "src/emails/DeliveryReminder.tsx"
      to: "src/emails/components/DeliveryBlock.tsx"
      via: "DeliveryBlock for address/window"
      pattern: "DeliveryBlock"
---

<objective>
Build Refund Notification (MAIL-03) and Delivery Reminder (MAIL-04) email templates.

Purpose: Completes the set of 4 transactional email templates. Refund handles both full and partial scenarios. Delivery reminder drives day-of engagement.
Output: Two fully branded email template components
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-RESEARCH.md
@.planning/phases/54-email-system/54-CONTEXT.md
@.planning/phases/54-email-system/54-01-SUMMARY.md
@.planning/phases/54-email-system/54-02-SUMMARY.md
@src/emails/components/EmailLayout.tsx
@src/emails/components/DeliveryBlock.tsx
@src/emails/components/OrderItemsTable.tsx
@src/emails/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refund Notification email template (MAIL-03)</name>
  <files>src/emails/RefundNotification.tsx</files>
  <action>
    Create `src/emails/RefundNotification.tsx` as a React Email component:

    **Props interface (RefundNotificationProps):**
    - customerName: string
    - orderId: string
    - isPartialRefund: boolean
    - refundedItems: Array<{ name, quantity, refundAmountCents }>
    - originalTotalCents: number
    - refundAmountCents: number
    - refundMethod: string (e.g., "Visa ending 4242")
    - refundTimeline: string (e.g., "3-5 business days")
    - shippingRefundCents?: number
    - processedAt: string (ISO)

    **Template structure (per locked decisions):**
    1. EmailLayout wrapper with emailType='refund', previewText: "Your refund of ${refundAmountCents/100} has been processed"
    2. Greeting: "Dear {customerName},"
    3. Warm/apologetic intro:
       - Full refund: "We've processed a full refund for your order #{orderId short}."
       - Partial refund: "We've processed a partial refund for your order #{orderId short}." with distinct visual treatment
    4. Refund breakdown table (warm-bg section):
       - Original order total
       - Hr separator
       - Each refunded item: name, quantity refunded, refund amount
       - Shipping refund if applicable
       - Hr separator
       - Total refund amount (bold, brand-green)
    5. Refund details box (light green bg):
       - Method: "{refundMethod}"
       - Timeline: "Funds will appear within {refundTimeline}"
       - Date processed: formatted processedAt
    6. Partial refund notice (if isPartialRefund): amber callout — "This is a partial refund. Your remaining order items are unaffected."
    7. Primary CTA: "View Order Details" button linking to APP_URL/orders/{orderId}
    8. Secondary CTA: "Browse Menu" link to APP_URL/menu
    9. "Need help?" support section

    **Key details:**
    - Distinct visual treatment for partial vs full: partial gets amber accent, full gets green accent
    - All prices from cents via $X.XX formatting
    - Refund amount highlighted in brand-green
    - Empathetic but reassuring tone (not apologetic like cancellation)

    Export as named + default export.
  </action>
  <verify>
    - `pnpm typecheck` passes
    - File exports RefundNotification component
    - Component handles both isPartialRefund=true and false
  </verify>
  <done>
    Refund notification template renders detailed breakdown with full/partial distinction, refund timeline, and branded CTAs
  </done>
</task>

<task type="auto">
  <name>Task 2: Delivery Reminder email template (MAIL-04)</name>
  <files>src/emails/DeliveryReminder.tsx</files>
  <action>
    Create `src/emails/DeliveryReminder.tsx` as a React Email component:

    **Props interface (DeliveryReminderProps):**
    - customerName: string
    - orderId: string
    - itemCount: number
    - itemNames: string[] (first 3 item names for preview)
    - deliveryWindowStart: string (ISO)
    - deliveryWindowEnd: string (ISO)
    - address: { line1, line2?, city, state, postalCode }
    - specialInstructions?: string
    - driverName?: string

    **Template structure (per locked decisions — food excitement at top + logistics below):**
    1. EmailLayout wrapper with emailType='reminder', previewText with food emoji: "Your {itemNames[0]} & more are coming today!"
    2. Excitement section (warm-bg, large text):
       - "Your Burmese feast is arriving today!" in heading
       - Food emoji decorations
       - Item preview: "{itemNames[0]}, {itemNames[1]}" + "and {remaining} more" if > 2 items
    3. DeliveryBlock component with address, window, instructions, driver
    4. Static map section:
       - Google Static Maps URL: `https://maps.googleapis.com/maps/api/staticmap?center=${encodeURIComponent(fullAddress)}&zoom=15&size=580x200&markers=color:red|${encodeURIComponent(fullAddress)}&key=${GOOGLE_MAPS_API_KEY}`
       - Img component with static map, alt text "Delivery location map"
       - If no API key available, skip the map image (graceful degradation)
       - "View on Map" link below opening Google Maps directions URL
    5. Action links section (two buttons side by side using Row/Column):
       - "Track Your Order" primary CTA -> APP_URL/orders/{orderId}
       - "Modify Order" secondary link -> APP_URL/orders/{orderId} (same page, customer can contact from there)
    6. "Need help?" support section

    **Key details:**
    - Most food-forward/excited of all templates
    - Morning delivery reminder tone (cheerful, anticipatory)
    - Static map uses Google Maps API key from NEXT_PUBLIC_GOOGLE_MAPS_API_KEY env var, graceful fallback if not set
    - Food emoji in subject line per locked decision
    - DeliveryBlock reused from shared components

    Export as named + default export.
  </action>
  <verify>
    - `pnpm typecheck` passes
    - File exports DeliveryReminder component
    - Component renders without Google Maps API key (graceful degradation)
  </verify>
  <done>
    Delivery reminder template renders food excitement, delivery details, map image (with fallback), and action CTAs
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Both template files exist in src/emails/
- RefundNotification handles partial and full refund scenarios
- DeliveryReminder degrades gracefully without Google Maps API key
</verification>

<success_criteria>
Both remaining email templates are complete, composing from shared components, following locked branding decisions, and ready for integration with sendEmail().
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-04-SUMMARY.md`
</output>
