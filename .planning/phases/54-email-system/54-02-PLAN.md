---
phase: 54-email-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emails/components/EmailLayout.tsx
  - src/emails/components/BrandHeader.tsx
  - src/emails/components/BrandFooter.tsx
  - src/emails/components/OrderStatusTracker.tsx
  - src/emails/components/OrderItemsTable.tsx
  - src/emails/components/DeliveryBlock.tsx
  - src/emails/fixtures.ts
autonomous: true

must_haves:
  truths:
    - "Email templates render with branded header, body content, and footer when previewed in React Email"
    - "Each email type shows a distinct header mood (celebratory, concerned, etc.) matching the email context"
    - "Order confirmation email displays items grouped by category with modifiers and correct prices"
    - "Delivery emails display full structured address, delivery window, and special instructions"
    - "Footer shows business address, support email, and a working unsubscribe link to account settings"
    - "Order status progression (Confirmed -> Preparing -> Out for Delivery -> Delivered) is visually distinguishable"
    - "Email preview uses realistic Burmese menu item names (Mohinga, Shan Noodles, etc.)"
  artifacts:
    - path: "src/emails/components/EmailLayout.tsx"
      provides: "Base email layout with Tailwind config, dark mode support"
      exports: ["EmailLayout"]
    - path: "src/emails/components/BrandHeader.tsx"
      provides: "Branded gradient header with logo"
      exports: ["BrandHeader"]
    - path: "src/emails/components/BrandFooter.tsx"
      provides: "Full footer with address, social, unsubscribe"
      exports: ["BrandFooter"]
    - path: "src/emails/components/OrderStatusTracker.tsx"
      provides: "Visual order status progression"
      exports: ["OrderStatusTracker"]
    - path: "src/emails/components/OrderItemsTable.tsx"
      provides: "Categorized items table with modifiers"
      exports: ["OrderItemsTable"]
    - path: "src/emails/components/DeliveryBlock.tsx"
      provides: "Delivery address + window display"
      exports: ["DeliveryBlock"]
    - path: "src/emails/fixtures.ts"
      provides: "Sample data for email previews"
      exports: ["SAMPLE_ORDER_CONFIRMATION", "SAMPLE_CANCELLATION", "SAMPLE_REFUND", "SAMPLE_DELIVERY_REMINDER"]
  key_links:
    - from: "src/emails/components/EmailLayout.tsx"
      to: "@react-email/components"
      via: "Html, Head, Body, Container imports"
      pattern: "@react-email/components"
    - from: "src/emails/components/BrandHeader.tsx"
      to: "src/emails/components/EmailLayout.tsx"
      via: "used inside EmailLayout"
      pattern: "BrandHeader"
---

<objective>
Build shared email sub-components and sample data fixtures for React Email templates.

Purpose: All 4 email templates (plans 03-06) compose from these shared components. Building them first enables parallel template creation.
Output: 6 reusable email components + fixtures file
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-RESEARCH.md
@.planning/phases/54-email-system/54-CONTEXT.md
@src/styles/tokens.css
@supabase/functions/send-order-confirmation/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmailLayout, BrandHeader, BrandFooter</name>
  <files>
    src/emails/components/EmailLayout.tsx
    src/emails/components/BrandHeader.tsx
    src/emails/components/BrandFooter.tsx
  </files>
  <action>
    1. Create `src/emails/components/EmailLayout.tsx`:
       - Props: { children, previewText, emailType? }
       - Renders: Html > Head (color-scheme meta, supported-color-schemes meta) > Preview (previewText) > Tailwind (with isolated config for brand colors: brand-red=#A41034, brand-gold=#EBCD00, brand-green=#3D8B22, warm-bg=#FFF9E6, dark-brown=#8B4513, gold=#D4A017) > Body (bg-white, font-sans stack: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif) > Container (max-w-[600px] mx-auto)
       - Import from @react-email/components: Html, Head, Body, Container, Preview
       - Import Tailwind from @react-email/tailwind (included in @react-email/components)
       - "View in browser" link at very top of body (subtle, text-xs, text-gray-400)
       - Children rendered inside Container between BrandHeader and BrandFooter
       - Pass emailType to BrandHeader for illustration variation

    2. Create `src/emails/components/BrandHeader.tsx`:
       - Props: { emailType: 'confirmation' | 'cancellation' | 'refund' | 'reminder' }
       - Renders gradient banner Section: linear-gradient from #D4A017 to #8B4513 (match existing Edge Function)
       - H1: "Mandalay Morning Star" in white, font-family Georgia, serif (web-safe Playfair Display equivalent)
       - Tagline: "Authentic Burmese Cuisine" in rgba(255,255,255,0.9)
       - Burmese greeting: "Mingalabar!" at top in gold text
       - Unique emoji/icon per type: confirmation=celebration, cancellation=concerned, refund=handshake, reminder=bell
       - Subtle star icon (Morning Star brand mark)
       - Mobile-responsive: single column, padded

    3. Create `src/emails/components/BrandFooter.tsx`:
       - Props: { unsubscribeUrl: string }
       - Renders footer Section with:
         - Business address: 750 Terrado Plaza, Suite 33, Covina, CA 91723
         - Support email link: support@mandalaymorningstar.com
         - Social media placeholder links (Instagram, Facebook)
         - "Morning Star Delivery" brand mark with star unicode
         - Unsubscribe link pointing to unsubscribeUrl (app settings page)
         - Subtle GDPR/privacy notice: "You're receiving this because you placed an order"
         - All text in gray-400/gray-500, font-size 12px
         - Hr separator at top
  </action>
  <verify>
    - `pnpm typecheck` passes
    - All three files exist
    - EmailLayout imports and uses BrandHeader, BrandFooter
  </verify>
  <done>
    Base email layout with branded header (warm gradient + Mingalabar greeting + type-specific mood) and full footer (address, social, unsubscribe, brand mark) complete
  </done>
</task>

<task type="auto">
  <name>Task 2: OrderStatusTracker, OrderItemsTable, DeliveryBlock, fixtures</name>
  <files>
    src/emails/components/OrderStatusTracker.tsx
    src/emails/components/OrderItemsTable.tsx
    src/emails/components/DeliveryBlock.tsx
    src/emails/fixtures.ts
  </files>
  <action>
    1. Create `src/emails/components/OrderStatusTracker.tsx`:
       - Props: { currentStep: 'confirmed' | 'preparing' | 'out_for_delivery' | 'delivered' }
       - Renders horizontal step tracker: Confirmed -> Preparing -> Out for Delivery -> Delivered
       - Uses Row/Column from @react-email/components for table-based horizontal layout
       - Active step gets brand-green bg circle + white text, completed steps get green checkmark, future steps get gray circle
       - Connecting lines between steps (solid for completed, dashed for future)
       - All inline styles for email client compat (Tailwind + inline fallbacks)

    2. Create `src/emails/components/OrderItemsTable.tsx`:
       - Props: { items: Array<{ name: string, quantity: number, lineTotalCents: number, category?: string, modifiers?: Array<{name: string, priceDelta?: number}> }> }
       - Groups items by category if category provided, otherwise flat list
       - Each category gets a subtle heading row
       - Each item row: quantity badge, name, modifiers (smaller text, gray), line total right-aligned
       - Uses table elements for email compat (not CSS grid/flex)
       - Price formatting: $X.XX via cents/100
       - Modifier names shown in parentheses after item name

    3. Create `src/emails/components/DeliveryBlock.tsx`:
       - Props: { address: { line1, line2?, city, state, postalCode }, windowStart?: string, windowEnd?: string, instructions?: string, driverName?: string }
       - Renders warm-bg (#FFF9E6) Section with green left border
       - Calendar emoji + formatted delivery date and time window
       - Pin emoji + formatted address
       - Driver name if available
       - Special instructions callout in amber bg if provided
       - Date/time formatting: "Saturday, February 15, 2026" + "10:00 AM - 12:00 PM"

    4. Create `src/emails/fixtures.ts`:
       - Export SAMPLE_ORDER_CONFIRMATION: realistic order with 4 Burmese items (Mohinga, Shan Noodles, Tea Leaf Salad, Samosa), modifiers (extra fish cake, spicy level), address, delivery window, totals (subtotal, tax, delivery fee, total), customer name "Aung Myo"
       - Export SAMPLE_CANCELLATION: same order structure with reason "Changed my mind", refundIssued: true
       - Export SAMPLE_REFUND: partial refund scenario (2 of 4 items), original amount, refund amount, method "Visa ending 4242", timeline "3-5 business days"
       - Export SAMPLE_DELIVERY_REMINDER: order with tomorrow's delivery date, address, item count
       - All prices in cents for consistency
       - Use actual Burmese dish names from the menu
  </action>
  <verify>
    - `pnpm typecheck` passes
    - All four files exist
    - Fixtures export 4 sample data objects
  </verify>
  <done>
    OrderStatusTracker (4-step visual progress), OrderItemsTable (grouped by category with modifiers), DeliveryBlock (address + window), and realistic fixtures complete
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- All 7 files in src/emails/ exist
- Components use @react-email/components imports
- Fixtures use realistic Burmese menu item names
</verification>

<success_criteria>
All shared email sub-components render correctly with React Email's component library. Fixtures provide realistic preview data. Components are ready for template composition in plans 03-06.
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-02-SUMMARY.md`
</output>
