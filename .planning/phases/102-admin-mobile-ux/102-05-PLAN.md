---
phase: 102-admin-mobile-ux
plan: 05
type: execute
wave: 3
depends_on:
  - 102-02
  - 102-03
  - 102-04
files_modified:
  - src/components/ui/admin/ops/OpsOrderRow.tsx
  - src/app/(admin)/admin/sections/page.tsx
  - src/app/(admin)/admin/photos/PhotosPage/index.tsx
  - src/app/(admin)/admin/menu/page.tsx
  - src/app/(admin)/admin/categories/page.tsx
  - src/app/(admin)/admin/routes/page.tsx
  - src/app/(admin)/admin/emails/page.tsx
  - src/app/(admin)/admin/analytics/drivers/DriverAnalyticsDashboard.tsx
  - src/app/(admin)/admin/analytics/delivery/DeliveryMetricsDashboard.tsx
  - src/app/(admin)/admin/photos/PhotosPage/PhotoGrid.tsx
  - src/app/(admin)/admin/photos/PhotosFilters.tsx
  - src/app/(admin)/admin/photos/PhotosStatsCards.tsx
  - src/app/(admin)/admin/sections/SectionsToolbar.tsx
  - src/app/(admin)/admin/sections/SectionsList.tsx
  - src/components/ui/admin/drivers/DriversStatsCards.tsx
  - src/components/ui/admin/routes/RoutesStatsCards.tsx
  - src/app/(admin)/admin/menu/MenuFilterBar.tsx
  - src/app/(admin)/admin/menu/MenuItemsTable.tsx
  - src/app/(admin)/admin/menu/[id]/page.tsx
  - src/app/(admin)/admin/menu/[id]/MenuItemFormFields.tsx
  - src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx
autonomous: false
requirements:
  - MOBL-03
user_setup: []

must_haves:
  truths:
    - "Every interactive element on admin pages meets 44px minimum touch target on mobile"
    - "Desktop button sizes and information density are unchanged"
    - "Reduced motion preference is respected on all admin page animations"
    - "Sections page nested overflow-auto is fixed"
    - "Admin app is usable on mobile with no pinching, no horizontal scroll, no missed taps"
  artifacts:
    - path: "src/components/ui/admin/ops/OpsOrderRow.tsx"
      provides: "OpsOrderRow with 44px checkbox on mobile"
      contains: "min-h-\\[44px\\]"
    - path: "src/app/(admin)/admin/sections/page.tsx"
      provides: "Sections page without nested overflow-auto"
  key_links:
    - from: "all admin pages"
      to: "touch target compliance"
      via: "responsive h-11 md:h-9 and min-h-[44px] classes"
      pattern: "h-11 md:h-9|min-h-\\[44px\\]"
---

<objective>
Touch target sweep across all admin pages (44px minimum on mobile), reduced-motion sweep across 17 files, fix sections page nested scroll violation, and final human verification of the complete mobile experience.

Purpose: MOBL-03 requires all admin interactive elements to meet 44px on mobile. The reduced-motion sweep is a WCAG 2.1 accessibility requirement. The sections page scroll fix prevents a desktop wheel-scroll bug. Final checkpoint verifies the complete phase 102 deliverable.
Output: Touch-target-compliant admin pages, reduced-motion-aware animations, scroll fix, human verification.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-admin-mobile-ux/102-CONTEXT.md
@.planning/phases/102-admin-mobile-ux/102-UI-SPEC.md
@.planning/phases/102-admin-mobile-ux/102-01-SUMMARY.md
@.planning/phases/102-admin-mobile-ux/102-02-SUMMARY.md
@.planning/phases/102-admin-mobile-ux/102-03-SUMMARY.md
@.planning/phases/102-admin-mobile-ux/102-04-SUMMARY.md

<interfaces>
<!-- Touch target patterns from UI-SPEC -->

Button size="sm" (36px) override:
```typescript
<Button size="sm" className="h-11 md:h-9" />
```

Icon-only elements (h-6 w-6 = 24px) override:
```typescript
<button className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0">
  <ChevronUp className="h-4 w-4" />
</button>
```

Checkbox transparent padding wrapper:
```typescript
<label className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0">
  <Checkbox />
</label>
```

Reduced motion pattern (from OpsKPIGrid.tsx):
```typescript
import { useAnimationPreference } from "@/lib/hooks";
const { shouldAnimate } = useAnimationPreference();
// Guard: initial={shouldAnimate ? { opacity: 0, y: 10 } : undefined}
```

Priority touch target fixes (from UI-SPEC):
1. OpsOrderRow.tsx L57: Checkbox h-5 w-5 (20px) -> 44px wrapper
2. SectionCard: Eye/chevron/more buttons ~p-2 (32px) -> 44px
3. Routes page: Date nav h-8 w-8 (32px) -> h-11 w-11 md:h-8 md:w-8
4. Emails page: Filter inputs h-10 (40px) -> h-11
5. Emails page: "View Order" links text-xs -> min-h-[44px]

Sections page scroll fix:
- L311: Remove `overflow-auto` from left panel div
- Let admin <main> be sole scroll container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Touch target sweep + reduced-motion sweep + scroll fix</name>
  <files>
    src/components/ui/admin/ops/OpsOrderRow.tsx,
    src/app/(admin)/admin/sections/page.tsx,
    src/app/(admin)/admin/emails/page.tsx,
    src/app/(admin)/admin/routes/page.tsx,
    src/app/(admin)/admin/analytics/drivers/DriverAnalyticsDashboard.tsx,
    src/app/(admin)/admin/analytics/delivery/DeliveryMetricsDashboard.tsx,
    src/app/(admin)/admin/photos/PhotosPage/index.tsx,
    src/app/(admin)/admin/photos/PhotosFilters.tsx,
    src/app/(admin)/admin/photos/PhotosStatsCards.tsx,
    src/app/(admin)/admin/sections/SectionsToolbar.tsx,
    src/app/(admin)/admin/sections/SectionsList.tsx,
    src/components/ui/admin/drivers/DriversStatsCards.tsx,
    src/components/ui/admin/routes/RoutesStatsCards.tsx,
    src/app/(admin)/admin/menu/page.tsx,
    src/app/(admin)/admin/menu/MenuFilterBar.tsx,
    src/app/(admin)/admin/menu/MenuItemsTable.tsx,
    src/app/(admin)/admin/menu/[id]/page.tsx,
    src/app/(admin)/admin/menu/[id]/MenuItemFormFields.tsx,
    src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx,
    src/app/(admin)/admin/categories/page.tsx
  </files>
  <read_first>
    src/components/ui/admin/ops/OpsOrderRow.tsx,
    src/app/(admin)/admin/sections/page.tsx,
    src/app/(admin)/admin/emails/page.tsx,
    src/app/(admin)/admin/routes/page.tsx,
    src/components/ui/admin/ops/OpsKPIGrid.tsx,
    src/app/(admin)/admin/analytics/drivers/DriverAnalyticsDashboard.tsx,
    src/app/(admin)/admin/analytics/delivery/DeliveryMetricsDashboard.tsx,
    .claude/learnings/mobile-ux.md,
    .claude/learnings/performance.md
  </read_first>
  <action>
**Part A: Priority touch target fixes (worst offenders)**

1. **OpsOrderRow.tsx** (105 lines): Find the `Checkbox` element (~L57) with `h-5 w-5` (20px). Wrap it in a transparent padding container:
   ```tsx
   <div className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0">
     <Checkbox ... />
   </div>
   ```

2. **Sections page** (`sections/page.tsx`, 363 lines):
   - Find the SectionCard eye/chevron/more icon buttons. These use `~p-2` (32px). Add `min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0` to each button wrapper.
   - **FIX nested scroll**: Find the left panel div (~L311) that has `overflow-auto` class. Remove `overflow-auto` from it. The admin `<main>` element is the sole scroll container.

3. **Routes page** (already modified in Plan 02): Verify date navigation prev/next buttons have `h-11 w-11 md:h-8 md:w-8`. Verify "Today" button has `h-11 md:h-9`. If not done in Plan 02 Task 2, add now.

4. **Emails page** (already modified in Plan 03): Verify filter inputs have `h-11` on mobile. Verify "View Order" links have `min-h-[44px] inline-flex items-center`. If not done in Plan 03, add now.

**Part B: Systematic touch target sweep (remaining pages)**

For each admin page, scan for interactive elements below 44px and apply the mobile-only override pattern. The approach per element type:

- **Button `size="sm"`**: Add `className="h-11 md:h-9"` override (NOT changing the size prop -- adding responsive class)
- **Icon-only buttons (h-6 w-6 or similar)**: Add `min-h-[44px] min-w-[44px] md:min-h-0 md:min-w-0` wrapper or responsive class
- **Checkboxes**: Transparent padding wrapper to 44px
- **Badge filter chips**: Add `min-h-[44px]` + `px-3` for adequate tap area
- **Dropdown triggers**: Ensure trigger button/area meets 44px
- **Close/dismiss X buttons**: Transparent padding wrapper

Pages to sweep (read each file, identify interactive elements below 44px, fix):
- `admin/menu/page.tsx` -- toggle switches, dropdown triggers
- `admin/categories/page.tsx` -- (sort buttons already fixed in Plan 02)
- `admin/photos/PhotosPage/` -- filter buttons, action buttons
- `admin/sections/page.tsx` -- eye, chevron, more buttons (SectionCard)
- `admin/analytics/` -- filter pills, date range selectors
- `admin/drivers/` page -- any action buttons
- `admin/settings/` page -- toggle switches
- `admin/orders/` page -- (OrdersTable already responsive)
- `admin/feedback/page.tsx` -- expand/collapse buttons
- `admin/ratings/page.tsx` -- sort buttons if any

**CRITICAL**: Only modify INTERACTIVE elements (buttons, links, inputs, toggles with onClick/onChange handlers). Do NOT resize display-only icons or status indicators.

**Part C: Reduced-motion sweep (17 files, 31 instances)**

For each file listed in the UI-SPEC reduced-motion sweep table:

1. Add import (if not present): `import { useAnimationPreference } from "@/lib/hooks";`
2. Add hook call at top of component: `const { shouldAnimate } = useAnimationPreference();`
3. Guard each `initial={{ opacity: 0, ... }}` prop: `initial={shouldAnimate ? { opacity: 0, y: 10 } : undefined}`
4. If the file uses `m.div` with `animate` but no `initial`, leave it alone (animate-only doesn't need gating).

Files and instances:
- `analytics/drivers/DriverAnalyticsDashboard.tsx` -- 2 instances
- `analytics/delivery/DeliveryMetricsDashboard.tsx` -- 2 instances
- `photos/PhotosPage/index.tsx` (or extracted component) -- 3 instances
- `photos/PhotosFilters.tsx` -- 2 instances
- `photos/PhotosStatsCards.tsx` -- 1 instance
- `sections/SectionsToolbar.tsx` -- 2 instances
- `sections/SectionsList.tsx` -- 4 instances
- `drivers/DriversStatsCards.tsx` -- 1 instance
- `routes/RoutesStatsCards.tsx` -- 1 instance
- `routes/page.tsx` -- 3 instances
- `menu/page.tsx` -- 2 instances
- `menu/MenuFilterBar.tsx` -- 1 instance
- `menu/MenuItemsTable.tsx` -- 1 instance
- `menu/[id]/page.tsx` -- 1 instance
- `menu/[id]/MenuItemFormFields.tsx` -- 1 instance
- `menu/[id]/MenuItemPhotoSection.tsx` -- 1 instance
- `categories/page.tsx` -- 3 instances

**For each file**: Read the file, locate `initial={{` patterns, add `shouldAnimate` guard. The import and hook call are ONE addition per file. Each `initial` modification is a single-line change.

**CRITICAL**: If a file is NOT a `'use client'` component, it cannot use hooks. Check before adding. All 17 files listed should already be client components (they use Framer Motion `m.div`), but verify.

**Verify** all files stay under 400 lines after additions (each file gets ~2-3 lines for import + hook, plus ~1 line per instance for the guard change).
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `OpsOrderRow.tsx` contains `min-h-[44px]` (checkbox touch target wrapper)
    - `sections/page.tsx` does NOT contain `overflow-auto` on the left panel div (scroll fix)
    - All 17 files in reduced-motion sweep contain `useAnimationPreference` import
    - All 17 files contain `shouldAnimate` in their component body
    - `DriverAnalyticsDashboard.tsx` contains `shouldAnimate ?` (guard pattern)
    - `DeliveryMetricsDashboard.tsx` contains `shouldAnimate ?` (guard pattern)
    - No file exceeds 400 lines
    - `pnpm lint` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>All admin interactive elements meet 44px minimum on mobile. Desktop sizes unchanged. 31 animation instances across 17 files respect reduced-motion preference. Sections page nested scroll fixed. All files under 400 lines. Full verification suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final mobile experience verification</name>
  <files>none</files>
  <action>
Human verifies the complete admin mobile UX overhaul across all 22 admin pages. This is a checkpoint -- all automated work is complete in Task 1 and prior plans. The human tests on a 375px viewport (or real mobile device) to confirm the mobile experience meets the phase goal: "Solo operator can run all Saturday kitchen operations from a phone without pinching, scrolling sideways, or missing touch targets."

What was built across all 5 plans:
- Navigation: Left drawer via hamburger in fixed mobile header (Plan 01)
- Tables: All 6 data tables show card layouts on mobile (Plans 02-03)
- Widget: Route progress widget on ops dashboard with 5s polling (Plan 04)
- Touch targets: All interactive elements 44px+ on mobile (Plan 05 Task 1)
- Padding: Responsive padding on all admin pages (Plan 03)
- Accessibility: Reduced-motion preference respected on 31 animation instances (Plan 05 Task 1)
- Scroll: Sections page nested scroll violation fixed (Plan 05 Task 1)

Verification steps:
1. Open browser DevTools, set viewport to 375x812 (iPhone)
2. Navigate to /admin -- verify mobile header with hamburger and "Dashboard" title
3. Tap hamburger -- verify left drawer opens with all 12 nav items + Exit Admin
4. Tap "Orders" in drawer -- verify drawer closes and Orders page loads
5. On Orders page -- verify card layout (no table headers, no horizontal scroll)
6. Navigate to /admin/routes -- verify route cards with status badges
7. Navigate to /admin/menu -- verify menu item cards with photo thumbnails
8. Navigate to /admin/categories -- verify category cards with 44px sort buttons
9. Navigate to /admin/emails -- verify email cards (not table rows)
10. Navigate to /admin/feedback -- verify feedback cards with star ratings
11. Navigate to /admin/ratings -- verify rating cards
12. Navigate to /admin/ops -- verify route progress widget between KPI grid and order list
13. Try tapping all buttons -- verify no mis-taps (44px targets)
14. Resize viewport to 768px -- verify desktop layout snaps back (sidebar, tables)
15. Check /admin/sections -- scroll content area with mousewheel (no nested scroll block)
  </action>
  <verify>Human responds with "approved" or lists issues to fix</verify>
  <done>Human confirms admin mobile UX is complete and usable for Saturday ops on phone</done>
</task>

</tasks>

<verification>
pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build

Structural verification:
- All barrel exports intact (src/components/ui/index.ts, ops/index.ts)
- No missing imports across all modified files
- No circular dependencies
- All files under 400 lines

Wiring audit:
- AdminMobileHeader imported in layout.tsx
- RouteProgressWidget imported in OpsCenter.tsx
- useRouteProgressPolling exported from ops/index.ts
- AdminNav variant prop used in AdminMobileHeader
</verification>

<success_criteria>
- Human verifies complete mobile admin experience on 375px viewport
- All interactive elements pass 44px touch target minimum
- Desktop layouts unchanged at md: breakpoint and above
- Reduced-motion preference respected on all animations
- Sections page scroll works correctly
- Full verification suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/102-admin-mobile-ux/102-05-SUMMARY.md`
</output>
