---
phase: 26-component-consolidation
plan: 06
type: execute
wave: 2
depends_on: ["26-02", "26-03"]
files_modified:
  - src/components/ui/navigation/AppShell.tsx
  - src/components/ui/navigation/BottomNav.tsx
  - src/components/ui/navigation/Header.tsx
  - src/components/ui/navigation/MobileMenu.tsx
  - src/components/ui/navigation/PageContainer.tsx
  - src/components/ui/navigation/index.ts
  - src/components/ui/scroll/ParallaxLayer.tsx
  - src/components/ui/scroll/RevealOnScroll.tsx
  - src/components/ui/scroll/ScrollChoreographer.tsx
  - src/components/ui/scroll/index.ts
  - src/components/ui/transitions/PageTransition.tsx
  - src/components/ui/transitions/index.ts
autonomous: true

must_haves:
  truths:
    - "Navigation components moved from ui-v8/navigation to ui/navigation"
    - "Scroll components moved from ui-v8/scroll to ui/scroll"
    - "PageTransition moved and renamed (removed V8 suffix)"
    - "All components use ui/ imports for overlays"
  artifacts:
    - path: "src/components/ui/navigation/MobileMenu.tsx"
      provides: "Mobile menu using Drawer"
    - path: "src/components/ui/navigation/index.ts"
      provides: "Navigation barrel export"
    - path: "src/components/ui/scroll/index.ts"
      provides: "Scroll barrel export"
    - path: "src/components/ui/transitions/PageTransition.tsx"
      provides: "Page transition (renamed from PageTransitionV8)"
  key_links:
    - from: "src/components/ui/navigation/MobileMenu.tsx"
      to: "src/components/ui/Drawer.tsx"
      via: "import"
      pattern: "import.*Drawer.*from"
---

<objective>
Migrate navigation, scroll, and transition components from ui-v8/ to ui/.

Purpose: Complete subdirectory migrations for navigation, scroll, and transitions.
Output: Clean components in ui/navigation/, ui/scroll/, and ui/transitions/.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-component-consolidation/26-CONTEXT.md
@.planning/phases/26-component-consolidation/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate navigation components</name>
  <files>src/components/ui/navigation/</files>
  <action>
1. Create navigation directory:
   ```bash
   mkdir -p src/components/ui/navigation
   ```

2. Move all navigation components:
   ```bash
   git mv src/components/ui-v8/navigation/AppShell.tsx src/components/ui/navigation/
   git mv src/components/ui-v8/navigation/BottomNav.tsx src/components/ui/navigation/
   git mv src/components/ui-v8/navigation/Header.tsx src/components/ui/navigation/
   git mv src/components/ui-v8/navigation/MobileMenu.tsx src/components/ui/navigation/
   git mv src/components/ui-v8/navigation/PageContainer.tsx src/components/ui/navigation/
   git mv src/components/ui-v8/navigation/index.ts src/components/ui/navigation/ 2>/dev/null || echo "No index.ts"
   ```

3. Update MobileMenu.tsx imports:
   - FROM: `from "@/components/ui-v8/Drawer"`
   - TO: `from "@/components/ui"`

4. Create or update barrel export:
   ```typescript
   // src/components/ui/navigation/index.ts
   export { AppShell } from "./AppShell";
   export { BottomNav } from "./BottomNav";
   export { Header } from "./Header";
   export { MobileMenu } from "./MobileMenu";
   export { PageContainer } from "./PageContainer";
   ```
  </action>
  <verify>
- All 5 navigation components in ui/navigation/
- No ui-v8 imports in navigation files
  </verify>
  <done>Navigation components migrated with clean imports</done>
</task>

<task type="auto">
  <name>Task 2: Migrate scroll components</name>
  <files>src/components/ui/scroll/</files>
  <action>
1. Create scroll directory:
   ```bash
   mkdir -p src/components/ui/scroll
   ```

2. Move all scroll components:
   ```bash
   git mv src/components/ui-v8/scroll/ParallaxLayer.tsx src/components/ui/scroll/
   git mv src/components/ui-v8/scroll/RevealOnScroll.tsx src/components/ui/scroll/
   git mv src/components/ui-v8/scroll/ScrollChoreographer.tsx src/components/ui/scroll/
   git mv src/components/ui-v8/scroll/index.ts src/components/ui/scroll/ 2>/dev/null || echo "No index.ts"
   ```

3. Update any ui-v8 imports to ui/ paths.

4. Create or update barrel export:
   ```typescript
   // src/components/ui/scroll/index.ts
   export { ParallaxLayer } from "./ParallaxLayer";
   export { RevealOnScroll } from "./RevealOnScroll";
   export { ScrollChoreographer } from "./ScrollChoreographer";
   ```
  </action>
  <verify>
- All 3 scroll components in ui/scroll/
- No ui-v8 imports in scroll files
  </verify>
  <done>Scroll components migrated with clean imports</done>
</task>

<task type="auto">
  <name>Task 3: Migrate transitions and rename PageTransitionV8</name>
  <files>src/components/ui/transitions/</files>
  <action>
1. Create transitions directory:
   ```bash
   mkdir -p src/components/ui/transitions
   ```

2. Move and rename PageTransitionV8:
   ```bash
   git mv src/components/ui-v8/transitions/PageTransitionV8.tsx src/components/ui/transitions/PageTransition.tsx
   ```

3. Update PageTransition.tsx:
   - Rename export from `PageTransitionV8` to `PageTransition`
   - Update any ui-v8 imports to ui/ paths

4. Create barrel export:
   ```typescript
   // src/components/ui/transitions/index.ts
   export { PageTransition } from "./PageTransition";
   export type { PageTransitionProps } from "./PageTransition";
   ```
  </action>
  <verify>
- PageTransition.tsx exists in ui/transitions/
- Export name is `PageTransition` (not PageTransitionV8)
- No ui-v8 imports
  </verify>
  <done>PageTransition migrated and renamed, barrel export created</done>
</task>

</tasks>

<verification>
```bash
# Check directory structure
ls src/components/ui/navigation/
ls src/components/ui/scroll/
ls src/components/ui/transitions/

# Check no V8 suffix in transitions
grep -r "PageTransitionV8" src/components/ui/transitions/ && echo "ERROR: V8 suffix found" || echo "OK: no V8 suffix"

# Check no ui-v8 imports
grep -r "ui-v8" src/components/ui/navigation/ src/components/ui/scroll/ src/components/ui/transitions/ && echo "ERROR: ui-v8 found" || echo "OK: no ui-v8"

# TypeScript check
pnpm typecheck
```
</verification>

<success_criteria>
- 5 navigation components in ui/navigation/
- 3 scroll components in ui/scroll/
- PageTransition in ui/transitions/ (renamed from PageTransitionV8)
- All barrel exports created
- No ui-v8 imports
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-component-consolidation/26-06-SUMMARY.md`
</output>
