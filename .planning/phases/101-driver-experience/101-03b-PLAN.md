---
phase: 101-driver-experience
plan: 03b
type: execute
wave: 3
depends_on: [101-02, 101-03]
files_modified:
  - src/components/ui/admin/StatusBadge.tsx
  - src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
  - src/app/(admin)/admin/routes/page.tsx
  - src/app/(admin)/admin/routes/RoutesStatsCards.tsx
autonomous: true
requirements: [DRV-01, DRV-02]

must_haves:
  truths:
    - "StatusBadge renders correct colors and icons for assigned and accepted"
    - "Admin ops dashboard filter dropdown includes Assigned and Accepted options"
    - "Admin route detail shows assigned/accepted in status config and actions"
    - "Declined route annotation visible on admin ops dashboard"
    - "pnpm build succeeds with no Record<RouteStatus,...> mismatches"
  artifacts:
    - path: "src/components/ui/admin/StatusBadge.tsx"
      provides: "Status badge with 5 states"
      contains: "assigned"
    - path: "src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx"
      provides: "Route header with 5 status configs"
      contains: "assigned"
    - path: "src/app/(admin)/admin/routes/page.tsx"
      provides: "Status filters with assigned/accepted"
      contains: "assigned"
    - path: "src/app/(admin)/admin/routes/RoutesStatsCards.tsx"
      provides: "Stats cards with assigned/accepted counts"
      contains: "assigned"
  key_links:
    - from: "src/components/ui/admin/StatusBadge.tsx"
      to: "RouteStatus type"
      via: "STATUS_COLORS, STATUS_ICONS, ACTIVE_STATUSES maps cover all 5 values"
      pattern: "assigned.*accepted"
    - from: "src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx"
      to: "RouteStatus type"
      via: "STATUS_CONFIG Record covers all 5 values"
      pattern: "assigned.*accepted"
---

<objective>
Admin UI components updated for 5-status route lifecycle: StatusBadge, RouteHeader, RouteCardRow, ops dashboard filters and stats, merge filter, declined route annotations.

Purpose: Without these updates, admin UI shows missing/broken status badges, incomplete filters, and Record<RouteStatus,...> type errors at build time.
Output: 6 modified admin UI files with complete 5-status handling.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-driver-experience/101-CONTEXT.md
@.planning/phases/101-driver-experience/101-RESEARCH.md
@.planning/phases/101-driver-experience/101-01-SUMMARY.md
@.planning/phases/101-driver-experience/101-03-SUMMARY.md

<interfaces>
<!-- Admin UI status maps from RESEARCH.md -->

Category C - Admin UI status maps:
- StatusBadge.tsx:36-50 STATUS_COLORS -- add assigned, accepted entries
- StatusBadge.tsx:53-61 ACTIVE_STATUSES -- add assigned, accepted
- StatusBadge.tsx:69-86 STATUS_ICONS -- add assigned, accepted
- RouteCardRow.tsx:27-31 STATUS_TINT -- add assigned, accepted
- RouteHeader.tsx:29-49 STATUS_CONFIG -- add assigned, accepted entries
- RouteHeader.tsx:121-126 Status SelectContent -- add assigned/accepted options
- RouteHeader.tsx:128-148 Conditional actions -- show for assigned/accepted
- RouteDetailClient.tsx:86 merge filter -- include assigned/accepted
- admin/routes/page.tsx:27-32 STATUS_FILTERS -- add assigned/accepted options
- admin/routes/page.tsx:160-167 Stats -- add assigned/accepted counts
- RoutesStatsCards.tsx -- add assigned/accepted stat display
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin UI status updates -- StatusBadge, RouteHeader, RouteCardRow, ops dashboard, merge filter</name>
  <files>
    src/components/ui/admin/StatusBadge.tsx
    src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx
    src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
    src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
    src/app/(admin)/admin/routes/page.tsx
    src/app/(admin)/admin/routes/RoutesStatsCards.tsx
  </files>
  <read_first>
    src/components/ui/admin/StatusBadge.tsx
    src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx
    src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
    src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
    src/app/(admin)/admin/routes/page.tsx
    src/app/(admin)/admin/routes/RoutesStatsCards.tsx
    .planning/phases/101-driver-experience/101-UI-SPEC.md
  </read_first>
  <action>
**A. src/components/ui/admin/StatusBadge.tsx**

1. Add to STATUS_COLORS map (~line 36-50):
```typescript
assigned: "bg-blue-100 text-blue-800",
accepted: "bg-green-100 text-green-800",
in_progress: "bg-amber-100 text-amber-800",
```
Note: `in_progress` may already have a mapping or fall to default gray. Add explicit entry per UI-SPEC.

2. Add to ACTIVE_STATUSES set (~line 53-61):
```typescript
"assigned", "accepted"
```
These statuses get the pulse animation.

3. Add to STATUS_ICONS map (~line 69-86):
```typescript
assigned: UserCheck,   // import from lucide-react
accepted: CheckCircle, // import from lucide-react
```
For `in_progress`: if not already mapped, add `Truck` (reuse existing out_for_delivery icon mapping).

**B. src/components/ui/admin/routes/RouteListTable/RouteCardRow.tsx**

Add to STATUS_TINT map (~line 27-31):
```typescript
assigned: "border-l-blue-400",
accepted: "border-l-green-400",
```

**C. src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx**

1. Add to STATUS_CONFIG (~line 29-49). This is typed as `Record<RouteStatus, ...>` so TypeScript requires it:
```typescript
assigned: {
  label: "Assigned",
  color: "bg-blue-100 text-blue-800",
  icon: UserCheck,
},
accepted: {
  label: "Accepted",
  color: "bg-green-100 text-green-800",
  icon: CheckCircle,
},
```

2. Add to Status SelectContent dropdown (~line 121-126):
```typescript
<SelectItem value="assigned">Assigned</SelectItem>
<SelectItem value="accepted">Accepted</SelectItem>
```

3. Expand conditional actions area (~line 128-148) to also show "Add Stops", "Optimize" buttons for assigned and accepted routes (same as planned).

**D. src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx**

Find merge candidate filter (~line 86):
Change: `.filter((r) => r.id !== routeId && r.status === "planned")`
To: `.filter((r) => r.id !== routeId && ["planned", "assigned", "accepted"].includes(r.status))`

**E. src/app/(admin)/admin/routes/page.tsx**

1. Add to STATUS_FILTERS (~line 27-32):
```typescript
{ value: "assigned", label: "Assigned" },
{ value: "accepted", label: "Accepted" },
```

2. Add declined route annotation logic: when rendering route cards, if a route has `status === "planned"` and `declined_by` is populated, show `text-xs text-status-error` annotation: "Declined by [Driver Name]". This requires the routes query to include `declined_by` in the select and join to get driver name.

Add to the routes query select: `declined_by, declined_driver:drivers!routes_declined_by_fkey(profiles(full_name))`

3. Stats section (~line 160-167): Add assigned count. The "Unassigned" counter should include declined routes (they ARE unassigned after decline).

**F. src/app/(admin)/admin/routes/RoutesStatsCards.tsx**

Add stat card for "Assigned" count (routes with status "assigned"). Add "Accepted" count (routes with status "accepted").
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StatusBadge.tsx contains `assigned: "bg-blue-100 text-blue-800"` in STATUS_COLORS
    - StatusBadge.tsx contains `accepted: "bg-green-100 text-green-800"` in STATUS_COLORS
    - StatusBadge.tsx ACTIVE_STATUSES includes "assigned" and "accepted"
    - StatusBadge.tsx STATUS_ICONS includes UserCheck for assigned and CheckCircle for accepted
    - RouteCardRow.tsx STATUS_TINT contains `assigned:` and `accepted:` entries
    - RouteHeader.tsx STATUS_CONFIG contains `assigned:` and `accepted:` entries (required by Record<RouteStatus,...>)
    - RouteHeader.tsx SelectContent contains `value="assigned"` and `value="accepted"`
    - RouteDetailClient.tsx merge filter includes `"assigned"` and `"accepted"`
    - admin/routes/page.tsx STATUS_FILTERS contains `{ value: "assigned"` and `{ value: "accepted"`
    - `pnpm typecheck` passes with no errors from Record<RouteStatus,...> mismatches
  </acceptance_criteria>
  <done>All admin UI components handle the 5-status route lifecycle. StatusBadge shows correct colors/icons for all states. RouteHeader compiles with complete STATUS_CONFIG. Ops dashboard filters and stats include new statuses. Merge modal shows assigned/accepted routes as candidates.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds (no missing STATUS_CONFIG entries for Record<RouteStatus,...>)
- StatusBadge, RouteCardRow, RouteHeader all have entries for 5 statuses
- Ops dashboard filters include Assigned and Accepted
</verification>

<success_criteria>
- StatusBadge, RouteCardRow, RouteHeader all have entries for 5 statuses
- Merge filter includes assigned/accepted as candidates
- Ops dashboard filters include Assigned and Accepted
- Stats cards show assigned/accepted counts
- Declined route annotation on ops dashboard
- No TypeScript errors from Record<RouteStatus,...> exhaustiveness checks
</success_criteria>

<output>
After completion, create `.planning/phases/101-driver-experience/101-03b-SUMMARY.md`
</output>
