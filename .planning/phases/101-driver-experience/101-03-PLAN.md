---
phase: 101-driver-experience
plan: 03
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/app/(driver)/driver/page.tsx
  - src/app/(driver)/driver/route/page.tsx
  - src/app/(driver)/driver/schedule/page.tsx
  - src/app/api/driver/routes/active/route.ts
  - src/app/api/driver/routes/upcoming/route.ts
  - src/app/api/driver/me/route.ts
  - src/app/api/admin/drivers/[id]/route.ts
  - src/app/api/admin/drivers/[id]/archive/route.ts
  - src/app/api/driver/routes/[routeId]/start/route.ts
  - src/app/api/admin/routes/[id]/route.ts
  - src/app/api/admin/routes/optimize/route.ts
  - src/app/api/admin/routes/[id]/stops/reassign/route.ts
  - src/app/api/admin/routes/[id]/stops/[stopId]/route.ts
autonomous: true
requirements: [DRV-01, DRV-02]

must_haves:
  truths:
    - "All 9 status filter queries include assigned and accepted"
    - "All 6 strict planned guards accept new statuses where appropriate"
    - "Admin saving driver assignment auto-transitions route to assigned status"
    - "pnpm typecheck passes after all guard and filter changes"
  artifacts:
    - path: "src/app/(driver)/driver/page.tsx"
      provides: "Driver dashboard with 4-status filter"
      contains: "assigned"
    - path: "src/app/api/driver/routes/[routeId]/start/route.ts"
      provides: "Start route accepting planned and accepted"
      contains: "accepted"
    - path: "src/app/api/admin/routes/[id]/route.ts"
      provides: "Admin PATCH auto-transition + DELETE guard expansion"
      contains: "assigned"
  key_links:
    - from: "src/app/(driver)/driver/page.tsx"
      to: "routes query"
      via: "status filter includes assigned and accepted"
      pattern: "assigned.*accepted.*planned.*in_progress"
    - from: "src/app/api/driver/routes/[routeId]/start/route.ts"
      to: "routes update"
      via: "accepts both planned and accepted status"
      pattern: "accepted.*planned"
    - from: "src/app/api/admin/routes/[id]/route.ts"
      to: "routes table"
      via: "PATCH auto-transitions to assigned when driver_id set"
      pattern: "status.*assigned"
---

<objective>
Status filter audit (9 queries, 6 guards) and admin PATCH auto-transition for the new assigned/accepted route lifecycle.

Purpose: Without this audit, drivers with assigned routes see "No Route Today" and admin actions fail on assigned/accepted routes. The admin PATCH auto-transition is the entry point that creates assigned routes.
Output: 13 modified files with correct status handling across all driver and admin API routes.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-driver-experience/101-CONTEXT.md
@.planning/phases/101-driver-experience/101-RESEARCH.md
@.planning/phases/101-driver-experience/101-01-SUMMARY.md

<interfaces>
<!-- Every file:line that needs modification, from RESEARCH.md Complete Status Query Audit -->

Category A - Status filter queries (.in("status", [...])):
1. src/app/(driver)/driver/page.tsx:125 -- ["planned", "in_progress"]
2. src/app/(driver)/driver/page.tsx:156 -- ["planned", "in_progress"]
3. src/app/(driver)/driver/route/page.tsx:77 -- ["planned", "in_progress"]
4. src/app/(driver)/driver/schedule/page.tsx:65 -- ["planned", "in_progress"]
5. src/app/api/driver/routes/active/route.ts:168 -- ["planned", "in_progress"]
6. src/app/api/driver/routes/upcoming/route.ts:58 -- ["planned", "in_progress"]
7. src/app/api/driver/me/route.ts:117 -- ["planned", "in_progress"]
8. src/app/api/admin/drivers/[id]/route.ts:299 -- ["planned", "in_progress"]
9. src/app/api/admin/drivers/[id]/archive/route.ts:93 -- ["planned", "in_progress"]

Category B - Strict "planned" guards:
1. src/app/api/driver/routes/[routeId]/start/route.ts:57 -- !== "planned" -> also allow "accepted"
2. src/app/api/admin/routes/[id]/route.ts:417 (DELETE) -- !== "planned" -> also allow "assigned"
3. src/app/api/admin/routes/optimize/route.ts:70 -- !== "planned" -> also allow "assigned", "accepted"
4. src/app/api/admin/routes/[id]/stops/reassign/route.ts:84 -- !== "planned" -> also allow "assigned", "accepted"
5. src/app/api/admin/routes/[id]/stops/reassign/route.ts:102 -- !== "planned" -> also allow "assigned", "accepted"
6. src/app/api/admin/routes/[id]/stops/[stopId]/route.ts:222 -- !== "planned" -> also allow "assigned", "accepted"

Category C - Admin PATCH auto-transition:
- src/app/api/admin/routes/[id]/route.ts PATCH handler: when driverId is set, auto-transition to assigned
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status filter audit -- 9 queries, 6 guard expansions, and admin PATCH auto-transition</name>
  <files>
    src/app/(driver)/driver/page.tsx
    src/app/(driver)/driver/route/page.tsx
    src/app/(driver)/driver/schedule/page.tsx
    src/app/api/driver/routes/active/route.ts
    src/app/api/driver/routes/upcoming/route.ts
    src/app/api/driver/me/route.ts
    src/app/api/admin/drivers/[id]/route.ts
    src/app/api/admin/drivers/[id]/archive/route.ts
    src/app/api/driver/routes/[routeId]/start/route.ts
    src/app/api/admin/routes/[id]/route.ts
    src/app/api/admin/routes/optimize/route.ts
    src/app/api/admin/routes/[id]/stops/reassign/route.ts
    src/app/api/admin/routes/[id]/stops/[stopId]/route.ts
  </files>
  <read_first>
    src/app/(driver)/driver/page.tsx
    src/app/(driver)/driver/route/page.tsx
    src/app/(driver)/driver/schedule/page.tsx
    src/app/api/driver/routes/active/route.ts
    src/app/api/driver/routes/upcoming/route.ts
    src/app/api/driver/me/route.ts
    src/app/api/admin/drivers/[id]/route.ts
    src/app/api/admin/drivers/[id]/archive/route.ts
    src/app/api/driver/routes/[routeId]/start/route.ts
    src/app/api/admin/routes/[id]/route.ts
    src/app/api/admin/routes/optimize/route.ts
    src/app/api/admin/routes/[id]/stops/reassign/route.ts
    src/app/api/admin/routes/[id]/stops/[stopId]/route.ts
  </read_first>
  <action>
**Category A: Expand all 9 `.in("status", [...])` filters.**

For each file, find `.in("status", ["planned", "in_progress"])` and change to `.in("status", ["assigned", "accepted", "planned", "in_progress"])`.

Exact locations (verify line numbers by reading files first -- research line numbers are approximate):

1. `src/app/(driver)/driver/page.tsx` ~line 125: today's route query
2. `src/app/(driver)/driver/page.tsx` ~line 156: next upcoming route query
3. `src/app/(driver)/driver/route/page.tsx` ~line 77: active route query
4. `src/app/(driver)/driver/schedule/page.tsx` ~line 65: upcoming routes 14-day query
5. `src/app/api/driver/routes/active/route.ts` ~line 168: active route API
6. `src/app/api/driver/routes/upcoming/route.ts` ~line 58: upcoming routes API
7. `src/app/api/driver/me/route.ts` ~line 117: driver profile active route count
8. `src/app/api/admin/drivers/[id]/route.ts` ~line 299: admin driver detail active routes
9. `src/app/api/admin/drivers/[id]/archive/route.ts` ~line 93: archive guard active route check

**Category B: Expand 6 strict `!== "planned"` guards.**

1. `src/app/api/driver/routes/[routeId]/start/route.ts` ~line 57:
   Change: `if (route.status !== "planned")`
   To: `if (route.status !== "planned" && route.status !== "accepted")`
   Reason: Driver can start route from accepted state.

2. `src/app/api/admin/routes/[id]/route.ts` ~line 417 (DELETE handler):
   Change: `if (route.status !== "planned")`
   To: `if (route.status !== "planned" && route.status !== "assigned")`
   Reason: Admin can delete assigned routes. Do NOT allow deleting accepted routes (driver has committed).

3. `src/app/api/admin/routes/optimize/route.ts` ~line 70:
   Change: `if (route.status !== "planned")`
   To: `if (!["planned", "assigned", "accepted"].includes(route.status))`
   Reason: Optimize available on pre-start routes.

4. `src/app/api/admin/routes/[id]/stops/reassign/route.ts` ~line 84 (source route):
   Change: `if (sourceRoute.status !== "planned")`
   To: `if (!["planned", "assigned", "accepted"].includes(sourceRoute.status))`

5. `src/app/api/admin/routes/[id]/stops/reassign/route.ts` ~line 102 (target route):
   Change: `if (targetRoute.status !== "planned")`
   To: `if (!["planned", "assigned", "accepted"].includes(targetRoute.status))`

6. `src/app/api/admin/routes/[id]/stops/[stopId]/route.ts` ~line 222:
   Change: `if (route.status !== "planned")`
   To: `if (!["planned", "assigned", "accepted"].includes(route.status))`
   Reason: Add/remove stops on pre-start routes.

**Category C: Admin PATCH auto-transition in src/app/api/admin/routes/[id]/route.ts**

Find the section where `driverId` is processed (around lines 329-331). Add auto-transition:
```typescript
if (driverId !== undefined) {
  routeUpdate.driver_id = driverId;
  if (driverId !== null) {
    routeUpdate.status = "assigned";
    routeUpdate.accepted_at = null; // Clear if reassigning
  } else {
    // Unassigning driver -- revert to planned
    routeUpdate.status = "planned";
  }
}
```
  </action>
  <verify>
    <automated>grep -rn '"planned", "in_progress"' src/app/\(driver\) src/app/api/driver src/app/api/admin/drivers 2>/dev/null | grep -v node_modules | grep -v ".next" | wc -l | grep -q "^0$" && echo "PASS: No old 2-status filters remain"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `.in("status", ["planned", "in_progress"])` remain in driver pages or driver/admin API routes
    - src/app/(driver)/driver/page.tsx contains `["assigned", "accepted", "planned", "in_progress"]` in both route queries
    - src/app/api/driver/routes/[routeId]/start/route.ts contains `!== "accepted"` allowing accepted routes to start
    - src/app/api/admin/routes/[id]/route.ts DELETE handler allows `"assigned"` status
    - src/app/api/admin/routes/[id]/route.ts PATCH handler contains `routeUpdate.status = "assigned"` when driver assigned
    - src/app/api/admin/routes/[id]/route.ts PATCH handler contains `routeUpdate.status = "planned"` when driver unassigned
    - src/app/api/admin/routes/optimize/route.ts allows `"assigned"` and `"accepted"`
    - src/app/api/admin/routes/[id]/stops/reassign/route.ts allows `"assigned"` and `"accepted"` on both source and target
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>All 9 status filter queries expanded to include assigned and accepted. All 6 guard checks expanded to allow new pre-start statuses where appropriate. Admin PATCH auto-transitions to assigned when driver_id is set. No old 2-status filters remain in driver or admin routes.</done>
</task>

</tasks>

<verification>
- `grep -rn '"planned", "in_progress"' src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".next"` returns 0 matches in driver/admin routes (some may remain in unrelated contexts like order status)
- `pnpm typecheck` passes
- `pnpm lint` passes
- Admin PATCH contains auto-transition logic for assigned/planned
</verification>

<success_criteria>
- 9 status filter queries expanded to 4-status array
- 6 guard checks expanded for new pre-start statuses
- Admin PATCH auto-transitions to assigned when driver set, planned when driver unset
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/101-driver-experience/101-03-SUMMARY.md`
</output>
