---
phase: 101-driver-experience
plan: 05
type: execute
wave: 5
depends_on: [101-04]
files_modified: []
autonomous: false
requirements: [DRV-02]

must_haves:
  truths:
    - "Every driver page loads real data with no empty/stub content"
    - "Both simple and advanced mode views work on every page"
    - "New assigned/accepted statuses render correctly on all driver pages"
    - "Test-delivery page still works (quick check)"
    - "No console errors on any driver page"
  artifacts: []
  key_links: []
---

<objective>
Manual page audit: load every driver page with new route statuses, verify buttons work, API calls succeed, empty states render, no console errors. Fix bugs inline.

Purpose: DRV-02 requires all driver pages load real data with no empty/stub content. This audit verifies the integrated system works end-to-end after Plans 01-04.
Output: Verified driver experience with any inline fixes committed.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-driver-experience/101-CONTEXT.md
@.planning/phases/101-driver-experience/101-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite and fix any issues</name>
  <files></files>
  <read_first>
    .planning/phases/101-driver-experience/101-CONTEXT.md
    .planning/phases/101-driver-experience/101-04-SUMMARY.md
  </read_first>
  <action>
Run the complete verification suite:
```bash
pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build
```

Fix any failures inline. Common issues to watch for:
1. ESLint max-lines warnings on modified files -- extract components if over 400 lines
2. Prettier formatting on new files -- run `pnpm format:check` and fix
3. TypeScript errors from incomplete status handling in edge cases
4. Test failures from changed Zod schema (existing route validation tests may need updated assertions)
5. Build failures from SSR issues with framer-motion (use dynamic import with ssr: false if needed)

After all fixes, re-run the full suite to confirm green.
  </action>
  <verify>
    <automated>pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0
    - `pnpm lint:css` exits 0
    - `pnpm format:check` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Full verification suite passes with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Driver page audit -- verify all pages with real data</name>
  <files></files>
  <action>
Human verification checkpoint. Present the verification checklist to the user and wait for approval.

Start dev server if not running: `pnpm dev`

Verification checklist covers all driver pages (dashboard, route, schedule, earnings, history, test-delivery) plus admin routes page for status badge/filter verification.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Human verification required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 20 verification steps pass
    - No console errors on any driver page
    - Accept/decline flow works end-to-end
    - Stop reorder works in advanced mode
  </acceptance_criteria>
  <done>Human approved all driver pages load real data with correct status handling.</done>
  <what-built>
Complete driver experience with accept/decline flow, stop reorder, and status filter updates across all driver and admin pages.
  </what-built>
  <how-to-verify>
Start dev server: `pnpm dev`

**Driver Dashboard (http://localhost:3000/driver)**
1. With an `assigned` route: verify AcceptDeclineCard appears with route preview (stop count, area)
2. Tap "Accept Route" -- verify toast "Route accepted!", card transitions to normal route view
3. With an `accepted` route: verify "Start Route" button appears
4. With no route: verify "No Route Today" empty state
5. Check both simple mode and advanced mode views

**Driver Route Page (http://localhost:3000/driver/route)**
6. With `assigned` route: verify AcceptDeclineBar at bottom, stop list scrollable above
7. Tap Decline -- verify DeclineConfirmDialog with reason textarea
8. With `accepted` or `in_progress` route: verify drag handles (desktop) and move buttons (mobile)
9. Drag a pending stop to reorder -- verify order saves (no error toast)

**Driver Schedule (http://localhost:3000/driver/schedule)**
10. Verify upcoming routes include assigned/accepted status routes
11. No empty/stub content

**Driver Earnings (http://localhost:3000/driver/earnings)**
12. Verify earnings data loads
13. No empty/stub content

**Driver History (http://localhost:3000/driver/history)**
14. Verify completed route history loads
15. No empty/stub content

**Test Delivery (http://localhost:3000/driver/test-delivery)**
16. Quick check -- page loads without error

**Admin Routes (http://localhost:3000/admin/routes)**
17. Verify status filter dropdown includes "Assigned" and "Accepted"
18. Verify StatusBadge colors: assigned=blue, accepted=green, in_progress=amber
19. Assign a driver to a route -- verify auto-transition to "Assigned" status

**Console Check**
20. Open browser console on each page -- no errors (warnings OK)
  </how-to-verify>
  <resume-signal>Type "approved" if all pages work correctly, or describe issues found for inline fix.</resume-signal>
</task>

</tasks>

<verification>
- Full verification suite green (lint, css, format, typecheck, test, build)
- Human verified all driver pages render real data
- Accept/decline flow tested manually
- Stop reorder tested manually
- Admin status updates verified visually
</verification>

<success_criteria>
- Every driver page loads real data with no empty/stub content
- Accept/decline flow works end-to-end (dashboard + route page)
- Stop reorder works in advanced mode
- Admin status badges and filters show new states
- Full verification suite passes
- Human approval received
</success_criteria>

<output>
After completion, create `.planning/phases/101-driver-experience/101-05-SUMMARY.md`
</output>
