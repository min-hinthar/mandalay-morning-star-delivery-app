---
phase: 64-service-worker-hardening
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - src/components/ui/offline/OfflineIndicator.tsx
  - src/components/ui/offline/OfflinePage.tsx
  - src/components/ui/offline/index.ts
autonomous: true

must_haves:
  truths:
    - "Offline indicator shows 'You're offline -- showing cached content' when serving cached pages"
    - "When no cached version exists, user sees branded offline page with links to cached pages"
    - "Reconnection shows 'Back online!' toast with manual refresh option"
    - "Non-queueable actions (Place Order) are disabled with tooltip when offline"
    - "Offline banner (top, warning) never shows simultaneously with update banner (bottom, info)"
  artifacts:
    - path: "src/components/ui/offline/OfflineIndicator.tsx"
      provides: "Enhanced offline indicator with 'showing cached content' messaging"
      exports: ["OfflineIndicator"]
    - path: "src/components/ui/offline/OfflinePage.tsx"
      provides: "Branded offline page component used by /offline route"
      exports: ["OfflinePage"]
    - path: "src/components/ui/offline/index.ts"
      provides: "Barrel export including OfflinePage"
      exports: ["OfflinePage"]
  key_links:
    - from: "src/components/ui/offline/OfflineIndicator.tsx"
      to: "navigator.onLine"
      via: "Online/offline event listeners"
      pattern: "addEventListener.*online"
    - from: "src/app/offline/page.tsx"
      to: "src/components/ui/offline/OfflinePage.tsx"
      via: "Import and render"
      pattern: "OfflinePage"
---

<objective>
Enhance the offline indicator to show "showing cached content" messaging, create a branded offline page component, and implement banner priority logic so offline and update banners never overlap.

Purpose: Users need clear feedback about their connectivity state and graceful degradation when offline, with visual consistency across the offline experience.
Output: Enhanced OfflineIndicator, OfflinePage component, updated barrel exports.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/offline/OfflineIndicator.tsx
@src/components/ui/offline/index.ts
@.planning/phases/64-service-worker-hardening/64-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance OfflineIndicator with cached content messaging and banner priority</name>
  <files>src/components/ui/offline/OfflineIndicator.tsx</files>
  <action>
  Enhance `OfflineIndicator.tsx` with the following changes:

  **"Showing cached content" messaging (locked decision):**
  - When offline: change text from "You're offline" to "You're offline -- showing cached content"
  - Style: persistent banner at top, warning style (red/warning per locked decision)
  - Keep existing amber/orange background color (#E87D1E) for the offline state
  - Icon: keep WifiOff icon

  **Reconnection behavior (locked decision):**
  - When reconnecting: show "Back online!" text (already exists as "Back online")
  - Add a "Refresh" link/button next to "Back online!" text for manual refresh
  - Per locked decision: user chooses when to refresh (manual), don't auto-refresh
  - Keep the 3-second auto-dismiss for the "Back online" banner, but the refresh button stays visible during that time

  **Banner priority logic (locked decision):**
  - Offline banner wins over update banner
  - Dispatch a custom event when offline state changes:
    ```ts
    window.dispatchEvent(new CustomEvent("offline-state-change", {
      detail: { isOffline: !navigator.onLine }
    }));
    ```
  - The useUpdateBanner hook (Plan 02) will listen for this and suppress the update banner when offline
  - This is a coordination mechanism -- OfflineIndicator emits, useUpdateBanner listens

  **Disable non-queueable actions (locked decision):**
  - Export a simple utility or use a global CSS class approach:
    ```ts
    // When offline, add data attribute to document root
    document.documentElement.dataset.offline = "true";
    // When online, remove it
    delete document.documentElement.dataset.offline;
    ```
  - Consumers (checkout button) can use `[data-offline="true"] .checkout-action` CSS or check `navigator.onLine`
  - This is a progressive enhancement signal, not a blocking mechanism

  **Keep existing functionality:**
  - Safe-area-inset-top padding
  - Banner height CSS variable for header offset
  - Hydration-safe mounting pattern
  </action>
  <verify>
  `pnpm typecheck` passes. `pnpm lint` passes. OfflineIndicator still renders correctly with new messaging.
  </verify>
  <done>
  OfflineIndicator shows "You're offline -- showing cached content" when offline. Reconnection banner has "Refresh" button. Custom event dispatched for banner priority coordination. Document data attribute set for offline action disabling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OfflinePage component and update offline route page</name>
  <files>src/components/ui/offline/OfflinePage.tsx, src/app/offline/page.tsx, src/components/ui/offline/index.ts</files>
  <action>
  **Create `src/components/ui/offline/OfflinePage.tsx`:**
  A branded offline page component for when no cached version of a page exists.

  Design:
  - Centered layout with brand logo (use `/logo.png` from public assets)
  - Heading: "You're offline"
  - Subtext: "It looks like you've lost your internet connection. Here are some pages you might have cached:"
  - Links section with cached page links:
    - Home (/)
    - Menu (/menu)
    - Cart (/cart)
  - Each link as a styled card/button
  - "Try Again" button that calls `window.location.reload()`
  - Use Tailwind classes for styling (compatible with static generation)
  - `'use client'` directive (uses window.location.reload)
  - Brand colors: use CSS variables from the design system (--color-primary, etc.)
  - Keep it simple and light -- this page must work offline, so no heavy imports

  **Update `src/app/offline/page.tsx` (created in Plan 01):**
  - Import and render `OfflinePage` component
  - Keep `export const dynamic = "force-static"` and metadata

  **Update `src/components/ui/offline/index.ts`:**
  - Add `export { OfflinePage } from "./OfflinePage";` to barrel
  </action>
  <verify>
  `pnpm typecheck` passes. `pnpm build` produces static offline page. `pnpm lint` passes. OfflinePage is exported from barrel.
  </verify>
  <done>
  OfflinePage component renders branded offline experience with logo, cached page links, and Try Again button. Exported from barrel. Used by /offline route page.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. `pnpm build` succeeds with static offline page
4. OfflineIndicator shows enhanced messaging
5. OfflinePage rendered at /offline route
6. Barrel exports include OfflinePage
</verification>

<success_criteria>
- Offline banner says "showing cached content" (not just "You're offline")
- Reconnection has manual refresh option
- Branded offline page with cached page links
- Banner priority: offline always wins over update
- Document data attribute signals offline state to action buttons
</success_criteria>

<output>
After completion, create `.planning/phases/64-service-worker-hardening/64-03-SUMMARY.md`
</output>
