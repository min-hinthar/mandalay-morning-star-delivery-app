---
phase: 90-menu-photo-pipeline
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/(admin)/admin/photos/page.tsx
  - src/components/ui/admin/photos/PhotoUploadZone.tsx
  - src/components/ui/admin/photos/PhotoGrid.tsx
  - src/components/ui/admin/photos/BulkUploadMatcher.tsx
  - src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx
  - src/app/api/admin/photos/route.ts
autonomous: true
requirements: [MENU-01, MENU-02, ADMIN-02]
must_haves:
  truths:
    - "Admin can upload a photo for any menu item and see it reflected on the customer menu"
    - "Admin can drag-drop multiple photos and see them auto-matched to items by slug"
    - "Unmatched photos during bulk upload are uploaded as unassigned"
    - "Admin photo management grid shows all photos with assign/delete/replace actions"
  artifacts:
    - path: "src/components/ui/admin/photos/BulkUploadMatcher.tsx"
      provides: "Bulk upload preview with slug matching and confirmation step"
    - path: "src/components/ui/admin/photos/PhotoUploadZone.tsx"
      provides: "Upload zone using server-side processing"
    - path: "src/app/(admin)/admin/photos/page.tsx"
      provides: "Enhanced admin photo management page"
  key_links:
    - from: "src/components/ui/admin/photos/PhotoUploadZone.tsx"
      to: "/api/admin/photos/process"
      via: "uploadMenuPhotoViaServer"
      pattern: "uploadMenuPhotoViaServer"
    - from: "src/components/ui/admin/photos/BulkUploadMatcher.tsx"
      to: "menu_items.slug"
      via: "filename-to-slug matching"
    - from: "src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx"
      to: "/api/admin/menu/[id]"
      via: "image_url update on save"
---

<objective>
Wire admin photo upload (single + bulk) through server-side processing pipeline and enhance photo management grid.

Purpose: Admin can upload individual photos per item and drag-drop multiple photos with auto-matching by slug. Photo management grid provides full CRUD.
Output: Enhanced PhotoUploadZone using server processing, new BulkUploadMatcher component, improved admin photos page.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-menu-photo-pipeline/90-01-SUMMARY.md
@src/app/(admin)/admin/photos/page.tsx
@src/components/ui/admin/photos/PhotoUploadZone.tsx
@src/components/ui/admin/photos/PhotoGrid.tsx
@src/components/ui/admin/photos/PhotoMetadata.tsx
@src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx
@src/lib/supabase/storage.ts
@src/app/api/admin/photos/route.ts

<interfaces>
<!-- After Plan 01 executes, these will exist -->

From src/lib/supabase/storage.ts (enhanced):
```typescript
export async function uploadMenuPhotoViaServer(file: File, menuItemId?: string): Promise<UploadResult>
// POSTs to /api/admin/photos/process, returns { path, publicUrl, width, height, size }
```

From src/app/api/admin/photos/process/route.ts:
```typescript
// POST: accepts FormData { file, menuItemId? }
// Returns: { path, publicUrl, width: 800, height: 600, size, format: 'webp' }
// Validates: min 800x600, max 2MB post-processing, outputs WebP 4:3
```

From src/components/ui/admin/photos/PhotoGrid.tsx:
```typescript
export interface PhotoItem {
  id: string;
  path: string;
  publicUrl: string;
  menuItemId: string | null;
  menuItemName: string | null;
  createdAt: string;
  size: number;
}
```

From src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx:
```typescript
interface MenuItemPhotoSectionProps {
  imageUrl: string | null;
  nameEn: string;
  itemId: string;
  updatedAt: string;
  initialDriveUrl: string;
  initialDrivePreview: string | null;
  onImageChange: (url: string | null) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PhotoUploadZone to use server-side processing</name>
  <files>src/components/ui/admin/photos/PhotoUploadZone.tsx, src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx</files>
  <action>
1. Update `src/components/ui/admin/photos/PhotoUploadZone.tsx`:
   - Replace the current client-side upload flow (which calls `uploadMenuPhoto`) with `uploadMenuPhotoViaServer` from `@/lib/supabase/storage`
   - The component already handles drag-drop and file selection — keep that UX intact
   - Add validation feedback: if server returns 400 (dimension too small, size too large), show the error message in a toast
   - Add a small "WebP" badge or note in the upload zone text: "Photos are auto-optimized to WebP at 4:3"
   - Keep `onUploadComplete` callback signature the same (returns `UploadResult[]`)

2. Update `src/app/(admin)/admin/menu/[id]/MenuItemPhotoSection.tsx`:
   - The component already uses `PhotoUploadZone` with `menuItemId` prop — no changes needed IF PhotoUploadZone's callback signature stays the same
   - After upload success, the `handlePhotoUploadComplete` already calls `onImageChange(results[0].publicUrl)` which triggers parent save — verify this flow still works
   - Remove the Google Drive URL section (cleanup — Supabase Storage is now the single source). Replace with a note: "Photos are auto-processed to WebP (4:3, 800x600)" using a small info text below the upload zone
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>PhotoUploadZone uses server-side processing. MenuItemPhotoSection streamlined (no Drive URL). Upload errors surface with clear messages.</done>
</task>

<task type="auto">
  <name>Task 2: Create BulkUploadMatcher and enhance admin photos page</name>
  <files>src/components/ui/admin/photos/BulkUploadMatcher.tsx, src/app/(admin)/admin/photos/page.tsx, src/app/api/admin/photos/route.ts</files>
  <action>
1. Create `src/components/ui/admin/photos/BulkUploadMatcher.tsx`:
   - Props: `files: File[], menuItems: Array<{ id: string, slug: string, name: string, imageUrl: string | null }>, onComplete: () => void, onCancel: () => void`
   - **Step 1 — Match:** For each dropped file:
     a. Extract slug from filename: strip extension, replace spaces with hyphens, lowercase (e.g., `Beef Curry.jpg` → `beef-curry`)
     b. Find matching menu item by slug (exact match)
     c. Classify: matched (with item name) or unmatched
   - **Step 2 — Preview:** Show a two-column list:
     - Left: file thumbnail (use URL.createObjectURL) + filename
     - Right: matched item name (green badge) or "Unmatched — will upload as unassigned" (amber badge)
     - If item already has a photo: show "(will replace)" warning
     - Show summary: "X matched, Y unmatched, Z will replace existing"
   - **Step 3 — Confirm:** "Upload All" button triggers sequential upload via `uploadMenuPhotoViaServer`:
     - For matched files: pass `menuItemId`
     - For unmatched files: no `menuItemId` (goes to unassigned)
     - Show progress: "{N}/{total} uploaded"
     - After each successful matched upload, update the menu item's `image_url` by calling `PATCH /api/admin/menu/{id}` with `{ image_url: publicUrl }`
     - On complete: call `onComplete()` to refresh the parent grid
   - Use Framer Motion for list animations (m.div with staggered entry, consistent with admin page patterns)
   - "Cancel" button calls `onCancel()`

2. Update `src/app/(admin)/admin/photos/page.tsx`:
   - Add "Bulk Upload" mode toggle or detect when multiple files are dropped
   - When multiple files dropped (> 1), show `BulkUploadMatcher` overlay/modal instead of individual upload
   - Fetch menu items list for matching: add `menuItems` to state (already partially exists — the `menuItems` state is fetched for assignment)
   - After bulk upload completes, refresh the photo grid (existing `fetchPhotos` callback)

3. Verify `src/app/api/admin/photos/route.ts`:
   - Ensure the GET endpoint returns photos with menuItemId and menuItemName for the grid
   - If the response doesn't include these fields, add a join to menu_items to resolve item names
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>BulkUploadMatcher component renders preview with slug matching. Admin photos page supports multi-file bulk upload. API returns enriched photo data for grid display.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` passes (no SSR issues with new components)
- PhotoUploadZone calls server-side processing endpoint
- BulkUploadMatcher matches files to items by slug
- Admin photos page handles both single and bulk upload flows
</verification>

<success_criteria>
- Single photo upload on menu item detail page uses WebP server processing
- Bulk drag-drop on photos page matches files to items by slug with preview step
- Unmatched files upload as unassigned
- Photo management grid shows all photos with assignment status
- Admin can upload, replace, and manage photos from a single dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/90-menu-photo-pipeline/90-04-SUMMARY.md`
</output>
