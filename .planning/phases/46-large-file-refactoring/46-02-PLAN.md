---
phase: 46-large-file-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/admin/drivers/DriverDetailClient.tsx
  - src/components/ui/admin/routes/RouteDetailClient.tsx
  - src/components/ui/admin/AdminDashboard.tsx
  - src/components/ui/admin/drivers/DriverListTable.tsx
  - src/components/ui/admin/routes/RouteListTable.tsx
  - src/components/ui/admin/routes/CreateRouteModal.tsx
  - src/components/ui/admin/settings/SettingsClient.tsx
  - src/components/ui/coverage/CoverageRouteMap.tsx
  - src/components/ui/account/OrdersTab.tsx
autonomous: true

must_haves:
  truths:
    - "All 9 admin/account components render and function identically after splitting"
    - "Import paths from consumers resolve without changes (subfolder index pattern)"
    - "No TypeScript errors introduced by file splits"
  artifacts:
    - path: "src/components/ui/admin/drivers/DriverDetailClient/index.tsx"
      provides: "Barrel re-export for DriverDetailClient"
    - path: "src/components/ui/admin/routes/RouteDetailClient/index.tsx"
      provides: "Barrel re-export for RouteDetailClient"
    - path: "src/components/ui/admin/AdminDashboard/index.tsx"
      provides: "Barrel re-export for AdminDashboard"
    - path: "src/components/ui/admin/drivers/DriverListTable/index.tsx"
      provides: "Barrel re-export for DriverListTable"
    - path: "src/components/ui/admin/routes/RouteListTable/index.tsx"
      provides: "Barrel re-export for RouteListTable"
    - path: "src/components/ui/admin/routes/CreateRouteModal/index.tsx"
      provides: "Barrel re-export for CreateRouteModal"
    - path: "src/components/ui/admin/settings/SettingsClient/index.tsx"
      provides: "Barrel re-export for SettingsClient"
    - path: "src/components/ui/coverage/CoverageRouteMap/index.tsx"
      provides: "Barrel re-export for CoverageRouteMap"
    - path: "src/components/ui/account/OrdersTab/index.tsx"
      provides: "Barrel re-export for OrdersTab"
  key_links:
    - from: "Each index.tsx barrel"
      to: "Split sub-files"
      via: "export * or named re-exports"
      pattern: "export .* from '\\./'"
---

<objective>
Split 9 admin and account components (1-4 importers each) into subfolder pattern with barrel index files.

Purpose: Refactor moderately-coupled admin components. These have slightly more importers than leaf components but still follow the same mechanical splitting pattern.
Output: 9 component subfolders, each with index.tsx + extracted sub-components/hooks. All files under 400 lines.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-large-file-refactoring/46-RESEARCH.md

Reference implementation:
@src/components/ui/menu/UnifiedMenuItemCard/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split 5 admin detail/dashboard components into subfolders</name>
  <files>
    src/components/ui/admin/drivers/DriverDetailClient.tsx (597 lines -> subfolder)
    src/components/ui/admin/routes/RouteDetailClient.tsx (576 lines -> subfolder)
    src/components/ui/admin/AdminDashboard.tsx (541 lines -> subfolder)
    src/components/ui/admin/drivers/DriverListTable.tsx (523 lines -> subfolder)
    src/components/ui/admin/routes/RouteListTable.tsx (501 lines -> subfolder)
  </files>
  <action>
    For each file, follow the subfolder pattern:

    1. Read the file, identify sub-components, hooks, helpers, types, config objects
    2. Create subfolder named after the component
    3. Extract sub-components (PascalCase), hooks (camelCase), helpers (camelCase)
    4. Create `index.tsx` as barrel re-exporting everything originally exported
    5. Delete original `.tsx` file
    6. CRITICAL: `'use client'` on every extracted file that uses hooks/events/browser APIs

    Specific guidance:

    **DriverDetailClient (597 lines):** Already imports some sub-components. Extract remaining inline sections (info panel, stats, activity log) into separate files.

    **RouteDetailClient (576 lines):** Single monolith with 1 export. Extract logical JSX sections (route info header, stop list, map section) into sub-components.

    **AdminDashboard (541 lines):** Has 5 card sub-components (StatCard variants, RevenueCard, etc.). Extract each card to its own file.

    **DriverListTable (523 lines):** Extract table row component, filter controls, any helper formatting functions.

    **RouteListTable (501 lines):** Extract table row component, filter/sort controls, status badge rendering.

    Run `pnpm typecheck` after all 5 splits.
  </action>
  <verify>
    `pnpm typecheck` passes with zero errors.
    Each original file is replaced by a subfolder with index.tsx.
    All split files are under 400 lines.
  </verify>
  <done>5 admin detail/dashboard components split. No TypeScript errors. All files under 400 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Split 4 remaining admin/account components into subfolders</name>
  <files>
    src/components/ui/admin/routes/CreateRouteModal.tsx (486 lines -> subfolder)
    src/components/ui/admin/settings/SettingsClient.tsx (476 lines -> subfolder)
    src/components/ui/coverage/CoverageRouteMap.tsx (550 lines -> subfolder)
    src/components/ui/account/OrdersTab.tsx (568 lines -> subfolder)
  </files>
  <action>
    Same subfolder pattern. For each file:

    1. Read, identify extractable units
    2. Create subfolder, extract into separate files
    3. Create `index.tsx` barrel re-exporting everything
    4. Delete original file
    5. Add `'use client'` to every extracted file using hooks/events/browser APIs

    Specific guidance:

    **CreateRouteModal (486 lines):** Extract form sections, stop list component, validation helpers.

    **SettingsClient (476 lines):** Has 2 sub-components. Extract settings sections/tabs into separate files.

    **CoverageRouteMap (550 lines):** Extract map controls, legend, route overlay components.

    **OrdersTab (568 lines):** Extract order list component, order card, filter/sort controls.

    Run `pnpm typecheck && pnpm lint` after all 4 splits.
  </action>
  <verify>
    `pnpm typecheck && pnpm lint` passes (no new errors).
    Each original file is replaced by a subfolder with index.tsx.
    All split files are under 400 lines.
  </verify>
  <done>All 9 admin/account components split. Zero TypeScript errors. Zero new lint errors. All files under 400 lines.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` — zero errors
- `pnpm lint` — no new errors
- `pnpm build` — builds successfully
- All 9 original files replaced by subfolder/index.tsx pattern
- No file in any subfolder exceeds 400 lines
</verification>

<success_criteria>
- 9 admin/account component files converted to subfolder pattern
- All consumers resolve imports without changes
- pnpm typecheck && pnpm build pass
- Every split file under 400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/46-large-file-refactoring/46-02-SUMMARY.md`
</output>
