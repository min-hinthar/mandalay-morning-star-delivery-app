---
phase: 46-large-file-refactoring
plan: 07
type: execute
wave: 4
depends_on: ["46-03", "46-04", "46-05", "46-06"]
files_modified:
  - eslint.config.mjs
  - .claude/CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "ESLint max-lines rule covers all src/**/*.{ts,tsx} files"
    - "Type definition files (src/types/**) are exempt from max-lines"
    - "Test files (*.test.ts, *.test.tsx) are exempt from max-lines"
    - "Storybook stories (src/stories/**) are exempt from max-lines"
    - "max-lines is warning-only (does not block builds)"
    - "CLAUDE.md documents the subfolder splitting patterns used in this phase"
    - "Zero files in src/ exceed 400 lines (excluding exempted categories)"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Expanded max-lines rule with proper exemptions"
      contains: "max-lines"
    - path: ".claude/CLAUDE.md"
      provides: "File organization patterns documentation"
      contains: "subfolder"
  key_links:
    - from: "eslint.config.mjs max-lines rule"
      to: "All src/**/*.{ts,tsx} files"
      via: "ESLint flat config files array"
      pattern: "src/\\*\\*/\\*\\.ts"
---

<objective>
Expand ESLint max-lines rule to cover all source files (with exemptions) and document file organization patterns in CLAUDE.md.

Purpose: Prevent future regression by enforcing the 400-line limit across all source files. Document the splitting patterns so future development follows consistent conventions.
Output: Updated eslint.config.mjs with expanded max-lines scope. Updated CLAUDE.md with file organization patterns.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-large-file-refactoring/46-RESEARCH.md

@eslint.config.mjs
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand ESLint max-lines rule + verify zero violations</name>
  <files>
    eslint.config.mjs
  </files>
  <action>
    **Step 1: Update ESLint config**

    In `eslint.config.mjs`, find the existing max-lines config block (lines ~142-155):
    ```javascript
    {
      // File size enforcement - REFACTOR-06 (warning only)
      files: ["src/components/**/*.tsx"],
      ...
    }
    ```

    Replace it with expanded scope:
    ```javascript
    {
      // File size enforcement - Phase 46 (warning only, expanded to all source files)
      files: [
        "src/**/*.ts",
        "src/**/*.tsx",
      ],
      ignores: [
        "src/types/**",           // Type definition files (database.ts, driver.ts)
        "src/**/*.test.ts",       // Test files
        "src/**/*.test.tsx",      // Test files
        "src/stories/**",         // Storybook stories
      ],
      rules: {
        "max-lines": [
          "warn",
          {
            max: 400,
            skipBlankLines: true,
            skipComments: true,
          }
        ]
      }
    },
    ```

    **Step 2: Run lint and verify**

    Run `pnpm lint 2>&1 | grep "max-lines"` to check for any remaining violations.

    If any source files still exceed 400 lines (that weren't split in Plans 01-06), investigate:
    - If it's a file that was intentionally left as-is (Hero.tsx, UnifiedMenuItemCard.tsx, route-optimization.ts), that's expected — warnings are acceptable
    - If it's a file that should have been split but was missed, note it in the SUMMARY

    **Step 3: Verify the rule is warning-only**
    Confirm the rule uses `"warn"` not `"error"`. Build must not fail due to file size.

    Run `pnpm build` to confirm no build breakage.
  </action>
  <verify>
    `pnpm lint` runs successfully (max-lines warnings may appear for leave-as-is files).
    `pnpm build` passes.
    `grep -A 10 "max-lines" eslint.config.mjs` shows expanded scope with exemptions.
  </verify>
  <done>ESLint max-lines rule expanded to all src/**/*.{ts,tsx} with type/test/story exemptions. Warning-only severity. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Document file organization patterns in CLAUDE.md</name>
  <files>
    .claude/CLAUDE.md
  </files>
  <action>
    Add a "File Organization" section to `.claude/CLAUDE.md` documenting the splitting patterns established in Phase 46. Place it after the existing "Paths" section.

    Add this content (adapt to match existing CLAUDE.md style/formatting):

    ```markdown
    ## File Organization

    Files must stay under 400 lines (ESLint `max-lines` warning). When splitting:

    | File Type | Pattern | Entry File |
    |-----------|---------|------------|
    | UI Component | Subfolder with barrel | `ComponentName/index.tsx` |
    | Lib/Utility | Subfolder with barrel | `lib-file/index.ts` |
    | Admin Page | Co-located siblings | `page.tsx` + `SiblingComponent.tsx` |
    | API Route | Co-located siblings | `route.ts` + `types.ts` + `schemas.ts` |

    **Component subfolder:**
    ```
    ComponentName/
      index.tsx          # Barrel re-exports
      SubComponent.tsx   # PascalCase
      useHook.ts         # camelCase
      helpers.ts         # camelCase
    ```

    **Lib subfolder:**
    ```
    lib-file/
      index.ts           # Barrel re-exports everything
      concern-a.ts       # By domain
      concern-b.ts
    ```

    - Every extracted file using hooks/events needs `'use client'`
    - Barrel `index.tsx` must re-export ALL original exports
    - Import paths don't change (subfolder index resolves automatically)
    - Exempt from 400-line rule: `src/types/**`, test files, Storybook stories
    ```

    Keep the section concise. Match the terse style of existing CLAUDE.md sections.
  </action>
  <verify>
    `.claude/CLAUDE.md` contains "File Organization" section.
    Section documents all 4 splitting patterns (component, lib, admin page, API route).
    Existing CLAUDE.md content is preserved.
  </verify>
  <done>CLAUDE.md updated with file organization patterns. All 4 splitting conventions documented.</done>
</task>

</tasks>

<verification>
- `pnpm lint` — runs successfully with expanded max-lines scope
- `pnpm build` — passes
- `pnpm typecheck` — passes
- ESLint config covers src/**/*.{ts,tsx} with correct exemptions
- CLAUDE.md documents file organization patterns
- Full verification suite: `pnpm lint && pnpm typecheck && pnpm build`
</verification>

<success_criteria>
- ESLint max-lines expanded from components-only to all source files
- Type files, test files, and stories exempt
- Warning severity (not error) — builds never blocked
- CLAUDE.md documents subfolder pattern, sibling pattern, barrel convention
- Full build pipeline passes
</success_criteria>

<output>
After completion, create `.planning/phases/46-large-file-refactoring/46-07-SUMMARY.md`
</output>
