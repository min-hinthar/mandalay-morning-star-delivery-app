---
phase: 46-large-file-refactoring
plan: 03
type: execute
wave: 2
depends_on: ["46-01", "46-02"]
files_modified:
  - src/components/ui/FormValidation.tsx
  - src/components/ui/Modal.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/admin/ExpandableTableRow.tsx
  - src/components/ui/checkout/AddressInput.tsx
  - src/components/ui/checkout/TimeSlotPicker.tsx
  - src/components/ui/orders/tracking/DeliveryMap.tsx
  - src/components/ui/orders/tracking/StatusTimeline.tsx
  - src/components/ui/homepage/Hero.tsx
  - src/components/ui/menu/UnifiedMenuItemCard/UnifiedMenuItemCard.tsx
autonomous: true

must_haves:
  truths:
    - "All shared UI components render and function identically after splitting"
    - "FormValidation's 20 exports all resolve correctly via barrel"
    - "Modal + ConfirmModal + useModal all importable from same path"
    - "All 8 skeleton variants importable from @/components/ui/skeleton"
    - "Hero.tsx evaluated for splitting feasibility; split or documented as leave-as-is"
    - "UnifiedMenuItemCard.tsx evaluated; split further or documented as irreducible"
  artifacts:
    - path: "src/components/ui/FormValidation/index.tsx"
      provides: "Barrel re-export for all 20 FormValidation exports"
    - path: "src/components/ui/Modal/index.tsx"
      provides: "Barrel re-export for Modal, ConfirmModal, useModal"
    - path: "src/components/ui/skeleton/index.tsx"
      provides: "Barrel re-export for 8 skeleton variants"
    - path: "src/components/ui/admin/ExpandableTableRow/index.tsx"
      provides: "Barrel re-export for ExpandableTableRow + hook"
    - path: "src/components/ui/checkout/AddressInput/index.tsx"
      provides: "Barrel re-export for AddressInput + sub-components"
    - path: "src/components/ui/checkout/TimeSlotPicker/index.tsx"
      provides: "Barrel re-export for TimeSlotPicker"
    - path: "src/components/ui/orders/tracking/DeliveryMap/index.tsx"
      provides: "Barrel re-export for DeliveryMap"
    - path: "src/components/ui/orders/tracking/StatusTimeline/index.tsx"
      provides: "Barrel re-export for StatusTimeline"
  key_links:
    - from: "FormValidation/index.tsx"
      to: "Split sub-files (types, hooks, components, rules)"
      via: "Named re-exports covering all 20 original exports"
      pattern: "export .* from '\\./'"
    - from: "Modal/index.tsx"
      to: "Modal.tsx, ConfirmModal.tsx, useModal.ts"
      via: "Named re-exports"
      pattern: "export .* from '\\./'"
    - from: "skeleton/index.tsx"
      to: "Individual skeleton variant files"
      via: "Named re-exports"
      pattern: "export .* from '\\./'"
---

<objective>
Split 8 shared UI components (1-8 importers each) into subfolder pattern. Evaluate Hero.tsx and UnifiedMenuItemCard.tsx for splitting feasibility.

Purpose: These are the most widely imported UI components. Higher importer counts mean more risk if barrel re-exports are incomplete. This plan runs after Wave 1 leaf/admin splits to validate the pattern works before applying to higher-risk targets.
Output: 8 component subfolders with barrel indexes, plus evaluation decisions for Hero and UnifiedMenuItemCard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-large-file-refactoring/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split FormValidation, Modal, and skeleton (3 highest-export components)</name>
  <files>
    src/components/ui/FormValidation.tsx (1031 lines, 20 exports -> subfolder)
    src/components/ui/Modal.tsx (746 lines, 10 exports -> subfolder)
    src/components/ui/skeleton.tsx (468 lines, 11 exports -> subfolder)
  </files>
  <action>
    These are the most complex splits due to high export counts. Extra care with barrel completeness.

    **FormValidation (1031 lines, 20 exports, 6 components, 3 hooks):**
    Split into `FormValidation/` with:
    - `types.ts` — ValidationRule, ValidationState, FieldValidation and other type exports
    - `validationRules.ts` — validationRules, combineRules
    - `useFieldValidation.ts` — useFieldValidation hook (`'use client'`)
    - `FormValidationProvider.tsx` — FormValidationProvider, useFormValidation, useFormValidationOptional (`'use client'`)
    - `ValidatedInputs.tsx` — ValidatedInput, ValidatedTextarea, ValidatedForm (`'use client'`)
    - `ValidationMessage.tsx` — ValidationMessage, InlineError (`'use client'`)
    - `index.tsx` — barrel re-exporting ALL 20 original exports

    After creating `FormValidation/index.tsx`, verify: count exports in the barrel vs original file. Must match exactly.

    **Modal (746 lines, 10 exports, 5 components, 2 hooks):**
    Split into `Modal/` with:
    - `types.ts` — ModalProps, ConfirmModalProps and other type exports
    - `useModal.ts` — useModal hook (`'use client'`)
    - `ModalOverlay.tsx` — overlay/backdrop sub-component (`'use client'`)
    - `ConfirmModal.tsx` — ConfirmModal component (`'use client'`)
    - `Modal.tsx` — main Modal component (`'use client'`)
    - `index.tsx` — barrel re-exporting all 10 original exports

    **skeleton (468 lines, 11 exports, 8 skeleton variants):**
    Split into `skeleton/` with:
    - `base.tsx` — base Skeleton component + shared types
    - `menu-skeletons.tsx` — MenuItemSkeleton, MenuGridSkeleton
    - `dashboard-skeletons.tsx` — DashboardSkeleton, StatCardSkeleton
    - `table-skeletons.tsx` — TableSkeleton, TableRowSkeleton
    - `page-skeletons.tsx` — remaining skeleton variants
    - `index.tsx` — barrel re-exporting all 11 original exports

    Note: skeleton components likely don't need `'use client'` (they're pure JSX). Check each file — only add directive if hooks/events are used.

    Run `pnpm typecheck` after all 3 splits.

  </action>
  <verify>
    `pnpm typecheck` passes with zero errors.
    `grep -c "export" src/components/ui/FormValidation/index.tsx` shows all 20 exports covered.
    `grep -c "export" src/components/ui/Modal/index.tsx` shows all 10 exports covered.
    All split files under 400 lines.
  </verify>
  <done>FormValidation (20 exports), Modal (10 exports), and skeleton (11 exports) split with complete barrel re-exports. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Split remaining shared components + evaluate Hero and UnifiedMenuItemCard</name>
  <files>
    src/components/ui/admin/ExpandableTableRow.tsx (533 lines -> subfolder)
    src/components/ui/checkout/AddressInput.tsx (546 lines -> subfolder)
    src/components/ui/checkout/TimeSlotPicker.tsx (472 lines -> subfolder)
    src/components/ui/orders/tracking/DeliveryMap.tsx (476 lines -> subfolder)
    src/components/ui/orders/tracking/StatusTimeline.tsx (407 lines -> subfolder)
    src/components/ui/homepage/Hero.tsx (518 lines -> evaluate)
    src/components/ui/menu/UnifiedMenuItemCard/UnifiedMenuItemCard.tsx (540 lines -> evaluate)
  </files>
  <action>
    **Split these 5 files (same subfolder pattern):**

    **ExpandableTableRow (533 lines):** Has hook + 4 sub-components. Extract useExpandableRow hook, sub-components.

    **AddressInput (546 lines):** Has 6 exports, 4 sub-components. Extract suggestion list, address preview, validation.

    **TimeSlotPicker (472 lines):** Has 3 sub-components. Extract time slot grid, day selector, availability indicator.

    **DeliveryMap (476 lines):** Has 3 sub-components. Extract map markers, route polyline, driver position components.

    **StatusTimeline (407 lines):** Has 3 components. Extract timeline step, timeline connector components.

    **Evaluate Hero.tsx (518 lines):**
    Read the file. Determine if the 5 sub-components can cleanly separate from the animation coupling (GSAP parallax, floating emojis). Decision criteria:
    - If sub-components DON'T share state/refs with parent via closures -> SPLIT
    - If sub-components are tightly coupled via shared refs, animation contexts, or GSAP timelines -> LEAVE AS-IS and document reason in SUMMARY

    **Evaluate UnifiedMenuItemCard.tsx (540 lines):**
    Read the file. It's already in a subfolder with 7 extracted files. Determine if the remaining 540 lines can be further split:
    - If there are identifiable sub-sections (e.g., tilt logic, glassmorphism, interaction handlers) -> SPLIT further
    - If the component is a single render function with tightly interwoven logic -> LEAVE AS-IS and document as irreducible

    Run `pnpm typecheck && pnpm lint` after all work.

  </action>
  <verify>
    `pnpm typecheck && pnpm lint` passes (no new errors).
    5 confirmed splits: ExpandableTableRow, AddressInput, TimeSlotPicker, DeliveryMap, StatusTimeline.
    Hero.tsx and UnifiedMenuItemCard.tsx: either split or documented with rationale.
    All split files under 400 lines.
  </verify>
  <done>5 shared components split. Hero and UnifiedMenuItemCard evaluated and resolved (split or documented). Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` — zero errors
- `pnpm lint` — no new errors
- `pnpm build` — builds successfully
- 8-10 files split into subfolder/index.tsx pattern (depending on Hero/UnifiedMenuItemCard evaluation)
- Barrel re-exports are complete (export counts match originals)
- No file exceeds 400 lines
</verification>

<success_criteria>

- FormValidation (20 exports), Modal (10 exports), skeleton (11 exports) all have complete barrels
- 5 additional shared components split
- Hero.tsx and UnifiedMenuItemCard.tsx resolved with documented rationale
- pnpm typecheck && pnpm build pass
- Every split file under 400 lines
  </success_criteria>

<output>
After completion, create `.planning/phases/46-large-file-refactoring/46-03-SUMMARY.md`
</output>
