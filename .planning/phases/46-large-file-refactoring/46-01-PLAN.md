---
phase: 46-large-file-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/admin/orders/OrderDetailExpanded.tsx
  - src/components/ui/homepage/HowItWorksSection.tsx
  - src/components/ui/account/AddressesTab.tsx
  - src/components/ui/brand/BrandMascot.tsx
  - src/components/ui/driver/DriverDashboard.tsx
  - src/components/ui/admin/drivers/PendingInvitesTab.tsx
  - src/components/ui/checkout/PaymentSuccess.tsx
  - src/components/ui/account/ProfileTab.tsx
  - src/components/ui/MorphingMenu.tsx
  - src/components/ui/cart/CartItem.tsx
autonomous: true

must_haves:
  truths:
    - "All 10 leaf components still render and function identically after splitting"
    - "Import paths from consumers resolve without changes (subfolder index pattern)"
    - "No TypeScript errors introduced by file splits"
  artifacts:
    - path: "src/components/ui/admin/orders/OrderDetailExpanded/index.tsx"
      provides: "Barrel re-export for OrderDetailExpanded"
    - path: "src/components/ui/homepage/HowItWorksSection/index.tsx"
      provides: "Barrel re-export for HowItWorksSection"
    - path: "src/components/ui/account/AddressesTab/index.tsx"
      provides: "Barrel re-export for AddressesTab"
    - path: "src/components/ui/brand/BrandMascot/index.tsx"
      provides: "Barrel re-export for BrandMascot"
    - path: "src/components/ui/driver/DriverDashboard/index.tsx"
      provides: "Barrel re-export for DriverDashboard"
    - path: "src/components/ui/admin/drivers/PendingInvitesTab/index.tsx"
      provides: "Barrel re-export for PendingInvitesTab"
    - path: "src/components/ui/checkout/PaymentSuccess/index.tsx"
      provides: "Barrel re-export for PaymentSuccess"
    - path: "src/components/ui/account/ProfileTab/index.tsx"
      provides: "Barrel re-export for ProfileTab"
    - path: "src/components/ui/MorphingMenu/index.tsx"
      provides: "Barrel re-export for MorphingMenu"
    - path: "src/components/ui/cart/CartItem/index.tsx"
      provides: "Barrel re-export for CartItem"
  key_links:
    - from: "Each index.tsx barrel"
      to: "Split sub-files"
      via: "export * or named re-exports"
      pattern: "export .* from '\\./'"
---

<objective>
Split 10 leaf UI components (0-2 importers each) into subfolder pattern with barrel index files.

Purpose: Reduce file sizes below 400-line threshold for the lowest-risk components first. These files have minimal importers so breakage risk is near zero.
Output: 10 component subfolders, each with index.tsx + extracted sub-components/hooks/helpers. All files under 400 lines.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-large-file-refactoring/46-RESEARCH.md

Reference implementation:
@src/components/ui/menu/UnifiedMenuItemCard/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split 5 largest leaf components into subfolders</name>
  <files>
    src/components/ui/admin/orders/OrderDetailExpanded.tsx (984 lines -> subfolder)
    src/components/ui/homepage/HowItWorksSection.tsx (876 lines -> subfolder)
    src/components/ui/account/AddressesTab.tsx (802 lines -> subfolder)
    src/components/ui/brand/BrandMascot.tsx (635 lines -> subfolder)
    src/components/ui/driver/DriverDashboard.tsx (585 lines -> subfolder)
  </files>
  <action>
    For each file, follow this exact pattern:

    1. Read the file and identify: sub-components, hooks, helper functions, types, config objects
    2. Create a subfolder named after the component (e.g., `OrderDetailExpanded/`)
    3. Extract sub-components into PascalCase files, hooks into camelCase files, helpers/config into camelCase files
    4. Create `index.tsx` as thin orchestrator that imports from split files and re-exports everything the original file exported
    5. Delete the original `.tsx` file
    6. CRITICAL: Every extracted file that uses React hooks, event handlers, or browser APIs MUST have its own `'use client'` directive at the top

    Specific splitting guidance per file:

    **OrderDetailExpanded (984 lines):** Has config maps and a monolith component. Extract config/constant maps to `config.ts`, any sub-sections of JSX into separate components. Main component becomes orchestrator in `index.tsx`.

    **HowItWorksSection (876 lines):** Has 5 internal sub-components per research. Extract each sub-component to its own file. Main section component becomes thin orchestrator.

    **AddressesTab (802 lines):** Has form + card skeleton. Extract address form component, address card component, skeleton component. Main tab orchestrates.

    **BrandMascot (635 lines):** Has Eyes, Mouth, Accessories sub-components. Extract each to own file.

    **DriverDashboard (585 lines):** Has StatCard, StreakDisplay, RouteCard, BadgesDisplay sub-components. Extract each to own file.

    Run `pnpm typecheck` after completing all 5 splits to verify no breakage.

  </action>
  <verify>
    `pnpm typecheck` passes with zero errors.
    Each original file is replaced by a subfolder with index.tsx.
    All split files are under 400 lines.
  </verify>
  <done>5 largest leaf components split into subfolders. No TypeScript errors. All files under 400 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Split 5 smaller leaf components into subfolders</name>
  <files>
    src/components/ui/admin/drivers/PendingInvitesTab.tsx (524 lines -> subfolder)
    src/components/ui/checkout/PaymentSuccess.tsx (511 lines -> subfolder)
    src/components/ui/account/ProfileTab.tsx (431 lines -> subfolder)
    src/components/ui/MorphingMenu.tsx (425 lines -> subfolder)
    src/components/ui/cart/CartItem.tsx (401 lines -> subfolder)
  </files>
  <action>
    Same subfolder pattern as Task 1. For each file:

    1. Read, identify extractable units (sub-components, hooks, helpers, types, constants)
    2. Create subfolder, extract into separate files
    3. Create `index.tsx` barrel that re-exports everything originally exported
    4. Delete original file
    5. Add `'use client'` to every extracted file that uses hooks/events/browser APIs

    Specific splitting guidance:

    **PendingInvitesTab (524 lines):** Extract invite list/card rendering, form or action components.

    **PaymentSuccess (511 lines):** Has 4 sub-components per research. Extract each.

    **ProfileTab (431 lines):** Extract profile form, avatar section, any sub-components.

    **MorphingMenu (425 lines):** Extract menu items rendering, animation helpers.

    **CartItem (401 lines):** Barely over threshold. Extract swipe gesture handler or sub-component. If only 2 logical pieces, splitting into 2 files is fine.

    Run `pnpm typecheck && pnpm lint` after completing all 5 splits.

  </action>
  <verify>
    `pnpm typecheck && pnpm lint` passes (lint warnings for other files are OK; no new errors).
    Each original file is replaced by a subfolder with index.tsx.
    All split files are under 400 lines.
  </verify>
  <done>All 10 leaf components split into subfolders. Zero TypeScript errors. Zero new lint errors. All files under 400 lines.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` — zero errors
- `pnpm lint` — no new errors (existing warnings OK)
- `pnpm build` — builds successfully
- All 10 original files replaced by subfolder/index.tsx pattern
- No file in any subfolder exceeds 400 lines
</verification>

<success_criteria>

- 10 leaf component files converted to subfolder pattern
- All consumers resolve imports without changes
- pnpm typecheck && pnpm build pass
- Every split file under 400 lines
  </success_criteria>

<output>
After completion, create `.planning/phases/46-large-file-refactoring/46-01-SUMMARY.md`
</output>
