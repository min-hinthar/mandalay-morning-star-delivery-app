---
phase: 46-large-file-refactoring
plan: 05
type: execute
wave: 2
depends_on: ["46-01", "46-02"]
files_modified:
  - src/app/api/admin/sections/[id]/route.ts
  - src/app/api/admin/routes/[id]/stops/route.ts
  - src/app/api/admin/routes/[id]/route.ts
  - src/app/api/tracking/[orderId]/route.ts
autonomous: true

must_haves:
  truths:
    - "All 4 API routes respond identically to requests after splitting"
    - "HTTP method exports (GET, POST, PATCH, DELETE) remain in route.ts"
    - "Types and schemas extracted to co-located files"
    - "No runtime behavior change in any API endpoint"
  artifacts:
    - path: "src/app/api/admin/sections/[id]/types.ts"
      provides: "Extracted interfaces for sections API"
    - path: "src/app/api/admin/sections/[id]/schemas.ts"
      provides: "Extracted Zod schemas for sections API"
    - path: "src/app/api/tracking/[orderId]/types.ts"
      provides: "Extracted 10 inline interfaces for tracking API"
  key_links:
    - from: "Each route.ts"
      to: "Co-located types.ts and schemas.ts"
      via: "Named imports"
      pattern: "import .* from '\\./types'"
---

<objective>
Extract inline types, Zod schemas, and helper functions from 4 API route files into co-located sibling files.

Purpose: API routes have inline interfaces and validation schemas that inflate line counts. Extracting these to `types.ts` and `schemas.ts` sibling files keeps route.ts focused on HTTP handler logic.
Output: 4 slimmer route.ts files + co-located types.ts/schemas.ts files. All under 400 lines.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-large-file-refactoring/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract types and schemas from all 4 API routes</name>
  <files>
    src/app/api/admin/sections/[id]/route.ts (484 lines, 7 interfaces, 4 HTTP methods)
    src/app/api/admin/routes/[id]/stops/route.ts (444 lines, 3 HTTP methods)
    src/app/api/admin/routes/[id]/route.ts (426 lines, 3 HTTP methods)
    src/app/api/tracking/[orderId]/route.ts (418 lines, 10 inline interfaces)
  </files>
  <action>
    For each API route file:

    1. Read the file, identify: TypeScript interfaces/types, Zod schemas, helper functions, HTTP handlers
    2. Extract interfaces/types to `types.ts` in same directory
    3. Extract Zod schemas to `schemas.ts` in same directory (if present)
    4. Extract helper functions to `helpers.ts` in same directory (if substantial)
    5. Keep HTTP method exports (GET, POST, PATCH, DELETE) in `route.ts`
    6. Import extracted types/schemas/helpers back into `route.ts`

    CRITICAL: Next.js requires HTTP method functions exported from `route.ts`. Do NOT move these to other files.

    Specific guidance:

    **admin/sections/[id]/route.ts (484 lines, 7 interfaces):**
    - Extract all 7 interfaces to `types.ts`
    - Extract Zod validation schemas to `schemas.ts`
    - route.ts keeps GET, PATCH, DELETE, POST handlers

    **admin/routes/[id]/stops/route.ts (444 lines):**
    - Extract types to `types.ts` if interfaces present
    - Extract schemas to `schemas.ts` if Zod schemas present
    - route.ts keeps POST, PATCH, DELETE handlers

    **admin/routes/[id]/route.ts (426 lines):**
    - Extract types to `types.ts`
    - Extract schemas to `schemas.ts` if present
    - route.ts keeps GET, PATCH, DELETE handlers

    **tracking/[orderId]/route.ts (418 lines, 10 interfaces):**
    - Extract all 10 inline interfaces to `types.ts`
    - route.ts keeps GET handler

    For any route that is still over 400 lines after type/schema extraction, extract shared helper functions (database query builders, response formatters) to `helpers.ts`.

    Run `pnpm typecheck && pnpm lint` after all 4 routes.

  </action>
  <verify>
    `pnpm typecheck && pnpm lint` passes (no new errors).
    All 4 route.ts files under 400 lines.
    All extracted types.ts/schemas.ts files exist and are under 400 lines.
    HTTP method exports still in route.ts (verify with `grep "export.*function\|export.*async" route.ts`).
  </verify>
  <done>All 4 API routes under 400 lines. Types and schemas extracted to co-located files. HTTP handlers preserved in route.ts. Zero TypeScript errors.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` — zero errors
- `pnpm lint` — no new errors
- `pnpm build` — builds successfully
- All 4 route.ts files under 400 lines
- HTTP method exports verified in each route.ts
- API endpoints respond correctly (typecheck confirms type safety)
</verification>

<success_criteria>

- 4 API route files under 400 lines
- Types/interfaces extracted to co-located types.ts files
- Zod schemas extracted to co-located schemas.ts files (where applicable)
- HTTP method exports preserved in route.ts
- pnpm typecheck && pnpm build pass
  </success_criteria>

<output>
After completion, create `.planning/phases/46-large-file-refactoring/46-05-SUMMARY.md`
</output>
