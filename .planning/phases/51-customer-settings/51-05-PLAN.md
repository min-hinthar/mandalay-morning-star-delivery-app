---
phase: 51-customer-settings
plan: 05
type: execute
wave: 4
depends_on: ["51-03", "51-04"]
files_modified:
  - src/components/ui/checkout/DietarySummaryCard.tsx
  - src/components/ui/checkout/PaymentStepV8.tsx
  - src/components/ui/homepage/SettingsNudgeBanner.tsx
  - src/components/ui/account/SettingsTab/index.tsx
autonomous: true

must_haves:
  truths:
    - "Checkout review step shows dietary summary card with selected restrictions"
    - "Dietary summary card is informational-only with Edit in Settings link"
    - "SettingsNudgeBanner See all settings link navigates to /account?tab=settings"
    - "All settings page features pass lint, typecheck, and build"
  artifacts:
    - path: "src/components/ui/checkout/DietarySummaryCard.tsx"
      provides: "Informational dietary restrictions card for checkout review"
      min_lines: 30
    - path: "src/components/ui/checkout/PaymentStepV8.tsx"
      provides: "Updated checkout step with dietary summary card"
    - path: "src/components/ui/homepage/SettingsNudgeBanner.tsx"
      provides: "Updated nudge banner with deep-link to Settings tab"
  key_links:
    - from: "src/components/ui/checkout/DietarySummaryCard.tsx"
      to: "/api/account/settings"
      via: "fetch dietary restrictions"
      pattern: "api/account/settings"
    - from: "src/components/ui/checkout/PaymentStepV8.tsx"
      to: "src/components/ui/checkout/DietarySummaryCard.tsx"
      via: "import DietarySummaryCard"
      pattern: "DietarySummaryCard"
    - from: "src/components/ui/homepage/SettingsNudgeBanner.tsx"
      to: "/account?tab=settings"
      via: "href prop on Link"
      pattern: "tab=settings"
---

<objective>
Add the checkout dietary summary card and update the SettingsNudgeBanner deep-link, then run full verification.

Purpose: These are the two integration points that connect settings to the rest of the app: (1) checkout shows dietary restrictions as a review card, and (2) the home page nudge banner links directly to the settings tab. This plan also serves as the final verification pass for the entire phase.

Output: Checkout review step includes dietary summary. Nudge banner deep-links to settings. Full build/lint/typecheck passes.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-customer-settings/51-03-SUMMARY.md
@.planning/phases/51-customer-settings/51-04-SUMMARY.md
@src/components/ui/checkout/PaymentStepV8.tsx
@src/components/ui/homepage/SettingsNudgeBanner.tsx
@src/components/ui/account/SettingsTab/settings-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DietarySummaryCard and integrate into checkout</name>
  <files>
    src/components/ui/checkout/DietarySummaryCard.tsx
    src/components/ui/checkout/PaymentStepV8.tsx
  </files>
  <action>
    **DietarySummaryCard.tsx** (`'use client'`):

    1. Self-contained component that fetches dietary restrictions on mount
    2. `useEffect`: call `fetch("/api/account/settings")` → extract `dietaryRestrictions` array from response
    3. If no restrictions or empty array: render nothing (return `null`) — don't clutter checkout with empty card
    4. If restrictions exist: render an informational card:
       - Icon: Leaf or UtensilsCrossed from lucide-react
       - Header: "Dietary Preferences"
       - Subtitle: "These will be included in your order notes"
       - Render restrictions as small pills/badges (similar style to settings chips but smaller and non-interactive)
       - Import `DIETARY_EMOJIS` from settings-types for emoji display
       - Bottom: `<Link href="/account?tab=settings&section=preferences">Edit in Settings</Link>` — small text link
    5. Loading state: brief skeleton or nothing (don't block checkout flow)
    6. Error handling: silently fail — dietary display is non-critical
    7. Card style: `rounded-lg bg-amber-50/50 border border-amber-200/50 p-4` — subtle, doesn't compete with payment focus
    8. Wrap in stagger animation variants to match PaymentStepV8 pattern

    **PaymentStepV8.tsx** — Insert DietarySummaryCard:

    1. Import `DietarySummaryCard` from `./DietarySummaryCard`
    2. Add `<DietarySummaryCard />` as a new `<m.div>` with stagger variant, placed between the Order Summary Card and the Notes Input section
    3. Wrap in stagger item: `<m.div variants={shouldAnimate ? staggerItem : undefined}><DietarySummaryCard /></m.div>`
    4. No other changes to PaymentStepV8 — the card self-manages its data and visibility
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - DietarySummaryCard renders restrictions with emoji pills when they exist
    - DietarySummaryCard returns null when no restrictions
    - PaymentStepV8 includes the card in the review flow
    - "Edit in Settings" link navigates to `/account?tab=settings&section=preferences`
  </verify>
  <done>
    Checkout review step shows dietary summary card with emoji pills for selected restrictions. Card is informational-only with "Edit in Settings" link. Hidden when no restrictions set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SettingsNudgeBanner deep-link and run full verification</name>
  <files>
    src/components/ui/homepage/SettingsNudgeBanner.tsx
    src/components/ui/account/SettingsTab/index.tsx
  </files>
  <action>
    **SettingsNudgeBanner.tsx:**

    1. Find the "See all settings" `<Link>` component (currently `href="/account"`)
    2. Update href to `"/account?tab=settings"` — deep-links to Settings tab
    3. That's it — minimal change. The nudge banner already saves directly via Supabase client, which is fine. Settings page fetches fresh on mount.

    **index.tsx** — Ensure barrel exports are complete:
    1. Verify all exports: `SettingsTab`, `useCustomerSettings`, and any sub-components that need external access
    2. Add any missing exports from Plans 03/04

    **Full verification pass:**
    1. Run `pnpm lint` — fix any lint issues across all Plan 01-05 files
    2. Run `pnpm lint:css` — fix any CSS issues
    3. Run `pnpm typecheck` — fix any type errors
    4. Run `pnpm test` — ensure no regressions
    5. Run `pnpm build` — ensure production build succeeds

    Fix any issues found during verification. Common fixes:
    - Missing `'use client'` directives
    - Unused imports (ESLint)
    - Missing type annotations
    - Import path issues
  </action>
  <verify>
    - `pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build` all pass
    - SettingsNudgeBanner links to `/account?tab=settings`
    - All Phase 51 files have correct exports
  </verify>
  <done>
    SettingsNudgeBanner deep-links to Settings tab. All Phase 51 code passes lint, typecheck, tests, and build. Phase is ready for verification.
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build` all pass
- Checkout shows dietary summary card when restrictions exist
- Nudge banner "See all settings" links to /account?tab=settings
- No regressions in existing functionality
</verification>

<success_criteria>
- Checkout review step includes dietary summary card (informational, with "Edit in Settings" link)
- Card hidden when no dietary restrictions set
- SettingsNudgeBanner deep-links to Settings tab via query param
- All 5 plans produce passing lint, typecheck, test, and build
- Phase 51 requirements SETT-01 through SETT-06 (minus deferred SETT-04) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/51-customer-settings/51-05-SUMMARY.md`
</output>
