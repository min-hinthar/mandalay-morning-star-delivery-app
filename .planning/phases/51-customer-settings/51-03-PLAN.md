---
phase: 51-customer-settings
plan: 03
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - src/components/ui/account/SettingsTab/PreferencesSection.tsx
  - src/components/ui/account/SettingsTab/DietaryChipPicker.tsx
  - src/components/ui/account/SettingsTab/CustomAllergyInput.tsx
  - src/components/ui/account/SettingsTab/NotificationsSection.tsx
  - src/components/ui/account/SettingsTab/NotificationCard.tsx
  - src/components/ui/account/SettingsTab/SettingsTab.tsx
autonomous: true

must_haves:
  truths:
    - "Customer can toggle dietary restriction chips with emoji + label + checkmark"
    - "Chip selection shows fill color change + checkmark with pop animation"
    - "Customer can add custom allergies via +Add custom chip and text input"
    - "Customer can enter default delivery instructions in a text field"
    - "3 notification cards display with distinct icons, title, description, and toggle"
    - "Tapping a notification card expands it to show sub-categories as bulleted list"
    - "Toggling a notification off shows warning text below the card"
    - "ToggleSwitch click does not propagate to card expand/collapse"
  artifacts:
    - path: "src/components/ui/account/SettingsTab/PreferencesSection.tsx"
      provides: "Dietary restrictions + delivery instructions section"
      min_lines: 40
    - path: "src/components/ui/account/SettingsTab/DietaryChipPicker.tsx"
      provides: "Chip-based dietary selector with emoji, pop animation, checkmark"
      min_lines: 50
    - path: "src/components/ui/account/SettingsTab/CustomAllergyInput.tsx"
      provides: "Add custom allergy chip input"
      min_lines: 30
    - path: "src/components/ui/account/SettingsTab/NotificationsSection.tsx"
      provides: "3 expandable notification cards container"
      min_lines: 30
    - path: "src/components/ui/account/SettingsTab/NotificationCard.tsx"
      provides: "Single expandable notification card with toggle"
      min_lines: 50
  key_links:
    - from: "src/components/ui/account/SettingsTab/DietaryChipPicker.tsx"
      to: "settings-types.ts"
      via: "import DIETARY_OPTIONS, DIETARY_EMOJIS"
      pattern: "import.*DIETARY"
    - from: "src/components/ui/account/SettingsTab/NotificationCard.tsx"
      to: "ToggleSwitch"
      via: "import from Phase 50"
      pattern: "import.*ToggleSwitch"
    - from: "src/components/ui/account/SettingsTab/SettingsTab.tsx"
      to: "PreferencesSection, NotificationsSection"
      via: "renders sections in sub-tabs"
      pattern: "PreferencesSection|NotificationsSection"
---

<objective>
Build the Preferences and Notifications sections of the customer settings page.

Purpose: These are the two DB-synced settings sections (dietary restrictions, delivery instructions, notification toggles). They use the useCustomerSettings hook from Plan 02 and produce the most visible customer-facing personalization features.

Output: Working Preferences sub-tab (dietary chip picker + custom allergies + delivery instructions) and Notifications sub-tab (3 expandable cards with toggles).
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-customer-settings/51-02-SUMMARY.md
@src/components/ui/account/SettingsTab/settings-types.ts
@src/components/ui/account/SettingsTab/SettingsTab.tsx
@src/components/ui/account/SettingsTab/useCustomerSettings.ts
@src/components/ui/admin/settings/ToggleSwitch.tsx
@src/components/ui/homepage/SettingsNudgeBanner.tsx
@src/lib/motion-tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DietaryChipPicker, CustomAllergyInput, and PreferencesSection</name>
  <files>
    src/components/ui/account/SettingsTab/DietaryChipPicker.tsx
    src/components/ui/account/SettingsTab/CustomAllergyInput.tsx
    src/components/ui/account/SettingsTab/PreferencesSection.tsx
  </files>
  <action>
    All three files need `'use client'` directive.

    **DietaryChipPicker.tsx:**

    Props: `{ selected: string[]; onChange: (items: string[]) => void; }`

    1. Import `m` from framer-motion, `Check` from lucide-react, `DIETARY_OPTIONS` and `DIETARY_EMOJIS` from `./settings-types`, `useAnimationPreference` from hooks
    2. Render `DIETARY_OPTIONS` as `m.button` chips in a flex-wrap container
    3. Each chip shows: emoji + label text + checkmark (when selected)
    4. **Selection feedback (LOCKED):** Fill color change + checkmark icon on selected chips
       - Unselected: `bg-surface-primary text-text-secondary border-border hover:border-primary`
       - Selected: `bg-amber-600 text-text-inverse border-amber-600`
    5. **Selection animation (LOCKED):** Subtle pop using framer-motion `animate` prop:
       ```typescript
       animate={{
         scale: isSelected ? [1, 1.15, 1] : 1,
       }}
       transition={{ type: "spring", stiffness: 500, damping: 25 }}
       ```
       Only animate when `shouldAnimate` is true
    6. Chip styling: `px-3 py-1.5 rounded-pill text-sm font-medium` — match SettingsNudgeBanner pill style but with emoji prefix
    7. Toggle logic: if selected, remove from array; if not, add to array. Call `onChange` with new array.
    8. Mobile: full-width sections with larger touch targets (`min-h-[44px]` per chip on mobile via responsive class)

    **CustomAllergyInput.tsx:**

    Props: `{ customItems: string[]; onChange: (items: string[]) => void; }`

    1. State: `inputValue` string, `isAdding` boolean (whether input is visible)
    2. Render a "+Add custom" chip/button that toggles `isAdding`
    3. When adding: show inline text input with Enter/comma to create chip
       - On Enter or comma: trim input, validate (non-empty, max 50 chars, not duplicate, max 5 custom items)
       - Create chip and clear input
       - Show validation feedback for max items
    4. Render custom items as removable chips (with X button) below the input
    5. Style custom chips differently from predefined: use `border-dashed` or a subtle visual distinction

    **PreferencesSection.tsx:**

    Props: `{ settings: CustomerSettings; updateField: (key, value) => void; }`

    1. Layout: vertical stack with section headers
    2. **Dietary Restrictions section:**
       - Section header: "Dietary Restrictions" with subtitle "Select any that apply to you"
       - Render `<DietaryChipPicker>` with `selected={settings.dietaryRestrictions}` and `onChange` calling `updateField("dietaryRestrictions", newItems)`
       - Render `<CustomAllergyInput>` with `customItems={settings.customAllergies}` and `onChange` calling `updateField("customAllergies", newItems)`
    3. **Delivery Instructions section:**
       - Section header: "Default Delivery Instructions" with subtitle "Added to every order automatically"
       - Textarea with `value={settings.deliveryInstructions}`, `onChange` calling `updateField`, `maxLength={500}`, placeholder "e.g. Leave at door, Ring doorbell twice"
       - Character counter: `{length}/500`
    4. Sections separated by subtle divider or spacing
    5. Mobile-optimized: full-width, adequate spacing, larger text inputs

  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `pnpm lint` passes
    - DietaryChipPicker renders 6 predefined chips with emojis
    - Chips toggle with fill + checkmark + pop animation
    - CustomAllergyInput creates/removes custom chips
    - PreferencesSection renders both dietary and delivery sections
  </verify>
  <done>
    Preferences section shows dietary chip picker with 6 emoji options + custom allergy input + delivery instructions textarea. Chips animate with pop on toggle. Custom allergies limited to 5 items, 50 chars each.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NotificationCard and NotificationsSection</name>
  <files>
    src/components/ui/account/SettingsTab/NotificationCard.tsx
    src/components/ui/account/SettingsTab/NotificationsSection.tsx
    src/components/ui/account/SettingsTab/SettingsTab.tsx
  </files>
  <action>
    Both new files need `'use client'` directive.

    **NotificationCard.tsx:**

    Props:
    ```typescript
    interface NotificationCardProps {
      title: string;
      description: string;
      icon: React.ReactNode;
      subCategories: readonly string[];
      warningText: string;
      enabled: boolean;
      onToggle: (enabled: boolean) => void;
    }
    ```

    1. State: `expanded` boolean (default false)
    2. Card container: rounded-lg border bg-surface-secondary, clickable area for expand/collapse
    3. **Top row:** flex between (icon + title + description) and ToggleSwitch
       - Import `ToggleSwitch` from `@/components/ui/admin/settings/ToggleSwitch`
       - **CRITICAL:** Add `e.stopPropagation()` on the ToggleSwitch wrapper div to prevent card expand/collapse when toggling. Wrap ToggleSwitch in a `<div onClick={e => e.stopPropagation()}>` container.
    4. **Expandable detail (LOCKED):** AnimatePresence + `m.div` for expand/collapse
       - `initial={{ height: 0, opacity: 0 }}`
       - `animate={{ height: "auto", opacity: 1 }}`
       - `exit={{ height: 0, opacity: 0 }}`
       - Overflow hidden on container
       - Content: bulleted list (`<ul className="list-disc pl-5">`) of `subCategories`
    5. **Disable warning (LOCKED):** When `!enabled`, show warning text below the card content
       - Style: `text-sm text-status-warning mt-2` — subtle but visible
       - Use AnimatePresence for smooth show/hide
    6. Chevron indicator (ChevronDown icon) that rotates when expanded
    7. Mobile: full-width, adequate padding, large tap target for expand

    **NotificationsSection.tsx:**

    Props: `{ notificationPrefs: NotificationPrefs; onUpdate: (key: keyof NotificationPrefs, value: boolean) => void; }`

    1. Import `NOTIFICATION_GROUPS` from `./settings-types`
    2. Import icons: `Package`, `Megaphone`, `Bell` from lucide-react
    3. Map `iconName` to actual icon component
    4. Render section header: "Email Notifications" with subtitle "Choose which emails you'd like to receive"
    5. Render 3 `<NotificationCard>` components in a vertical stack, one per `NOTIFICATION_GROUPS` entry
    6. Each card's `enabled` reads from `notificationPrefs[group.key]`, `onToggle` calls `onUpdate(group.key, newValue)`

    **SettingsTab.tsx** — Update to wire real sections:
    1. Replace Preferences placeholder with `<PreferencesSection settings={settings} updateField={updateField} />`
    2. Replace Notifications placeholder with `<NotificationsSection notificationPrefs={settings.notificationPrefs} onUpdate={(key, val) => updateField("notificationPrefs", { ...settings.notificationPrefs, [key]: val })} />`
    3. Add imports for PreferencesSection and NotificationsSection
    4. Handle loading state: show skeleton/loading indicator while `isLoading` is true
    5. Handle null settings gracefully (don't render sections until settings loaded)

  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `pnpm lint` passes
    - 3 notification cards render with correct icons and toggles
    - Cards expand/collapse on click
    - Toggle click does not trigger expand/collapse (stopPropagation)
    - Warning text shows when a notification is toggled off
    - SettingsTab renders real sections for Preferences and Notifications
  </verify>
  <done>
    3 notification cards render with Package/Megaphone/Bell icons, ToggleSwitch, expandable sub-category lists, and disable warning text. Click propagation handled. SettingsTab wires real Preferences and Notifications sections.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- Preferences sub-tab: 6 dietary chips with emoji, custom allergy input, delivery instructions textarea
- Notifications sub-tab: 3 expandable cards with toggles and warnings
- FloatingUnsavedBar appears when any setting changes
</verification>

<success_criteria>

- Dietary chip picker shows 6 options with emoji + label, pop animation on toggle, checkmark feedback
- Custom allergy input creates removable chips (max 5, max 50 chars)
- Delivery instructions textarea with 500 char limit
- 3 notification cards with expandable sub-category lists
- ToggleSwitch reused from Phase 50, click propagation prevented
- Warning text shows when notification toggled off
  </success_criteria>

<output>
After completion, create `.planning/phases/51-customer-settings/51-03-SUMMARY.md`
</output>
