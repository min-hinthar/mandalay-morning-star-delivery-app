---
phase: 84-production-hardening
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/app/api/checkout/session/route.ts
  - src/lib/validations/checkout.ts
autonomous: true
requirements: [HARD-06]

must_haves:
  truths:
    - "Checkout validates modifier price_delta_cents against DB values before creating order"
    - "Stale modifier prices return 409 with priceDrifts array including modifier details"
    - "Bulk order ID arrays in API requests are validated to max 100 items"
  artifacts: []
  key_links:
    - "Checkout route validates modifier prices using same pattern as existing base price drift detection"
---

<objective>
Add modifier price delta validation at checkout and validate bulk request sizes.

Purpose: Prevent stale modifier prices from creating incorrect orders and protect bulk endpoints from oversized payloads.
Output: Enhanced checkout validation for modifier price drift + request body size validation on bulk endpoints.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/api/checkout/session/route.ts
@src/lib/validations/checkout.ts
@src/app/api/admin/ops/orders/route.ts

<interfaces>
<!-- Checkout validation -->
From src/app/api/checkout/session/route.ts:
  - Already validates base price drift (BUG-08): compares cartItem.basePriceCents !== dbItem.base_price_cents
  - Already validates modifier price drift: compares mod.priceDeltaCents !== dbMod.price_delta_cents
  - Returns 409 with { priceDrifts: [...] } on any drift

From src/lib/validations/checkout.ts:
  - createCheckoutSessionSchema: Zod schema validating checkout request body
  - Items array with menuItemId, quantity, modifiers[{ optionId, priceDeltaCents }]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify modifier price validation and add bulk request size limits</name>
  <files>src/app/api/checkout/session/route.ts, src/lib/validations/checkout.ts</files>
  <action>
**Part A: Verify modifier price delta validation (HARD-06)**

Read the current checkout session route carefully. The BUG-08 fix already validates modifier price drift:
```typescript
for (const mod of cartItem.modifiers) {
  const dbMod = modifierOptions.get(mod.optionId);
  if (dbMod && mod.priceDeltaCents !== dbMod.price_delta_cents) {
    priceDrifts.push({ ... field: "modifier_price" ... });
  }
}
```

This already satisfies HARD-06. However, verify that the validation is complete:

1. Check that `priceDeltaCents` is included in the Zod checkout schema (`src/lib/validations/checkout.ts`). If missing, add it as `z.number().int()` in the modifier object within the items array.

2. Ensure the 409 response includes the modifier name in the drift object. Currently it includes `modifierName: dbMod.name` -- verify this is present.

3. If `priceDeltaCents` field is NOT in the Zod schema, add it:
```typescript
// In the items array modifier schema:
modifiers: z.array(z.object({
  optionId: z.string().uuid(),
  priceDeltaCents: z.number().int(),
}))
```

**Part B: Add bulk request validation**

The success criteria requires: "Bulk status endpoint is rate-limited (max 100 orders per call)."

Read `src/app/api/admin/ops/orders/route.ts` -- this endpoint fetches orders with `.limit(200)`. This is the "bulk" read endpoint.

Add request size validation to the ops orders endpoint to enforce the 100-order limit per the success criteria. Change `.limit(200)` to `.limit(100)`.

Also check if there are any POST/PATCH endpoints that accept arrays of order IDs and add Zod validation with `.max(100)` on those arrays. Specifically check:
- Any bulk status update endpoint
- The route creation endpoint (`src/app/api/admin/routes/route.ts` POST) -- the `orderIds` array should have `.max(100)` in its Zod schema

In `src/lib/validations/route.ts` (or wherever `createRouteSchema` is defined), add `.max(100)` to the `orderIds` array:
```typescript
orderIds: z.array(z.string().uuid()).min(1).max(100),
```

**Part C: Tighten checkout items array limit**

In `src/lib/validations/checkout.ts`, ensure the items array has a reasonable max:
```typescript
items: z.array(itemSchema).min(1).max(50), // Max 50 distinct items per order
```
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>Modifier price delta is validated in checkout Zod schema and at order creation time. Ops orders endpoint limited to 100 orders. Route creation orderIds limited to 100. Checkout items limited to 50.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Checkout schema validates `priceDeltaCents` as integer
- Ops orders endpoint uses `.limit(100)` not `.limit(200)`
- Route creation schema has `.max(100)` on orderIds array
</verification>

<success_criteria>
- Modifier price delta is validated at checkout against DB values
- Bulk status endpoint limited to max 100 orders per call
- Route creation limited to max 100 orders per route
- Stale modifier prices return 409 with priceDrifts array
</success_criteria>

<output>
After completion, create `.planning/phases/84-production-hardening/84-04-SUMMARY.md`
</output>
