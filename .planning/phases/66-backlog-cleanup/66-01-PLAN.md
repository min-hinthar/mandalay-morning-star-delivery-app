---
phase: 66-backlog-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/cart.ts
  - src/lib/stores/cart-store.ts
  - src/components/ui/menu/ItemDetailSheet.tsx
  - src/components/ui/cart/CartPage/CartPageContent.tsx
  - src/components/ui/cart/CartItem/CartItem.tsx
autonomous: true

must_haves:
  truths:
    - "Tapping pencil icon on a cart item with modifiers opens ItemDetailSheet pre-populated with that item's modifiers, quantity, and notes"
    - "CTA button says 'Update Cart' (not 'Add to Cart') when editing an existing cart item"
    - "'Cart updated' toast appears after saving changes"
    - "Closing the edit sheet without saving shows discard confirmation when changes were made"
    - "Items without modifier options do not show the pencil edit icon"
    - "Editing a sold-out item opens sheet in unavailable state with remove-only option"
  artifacts:
    - path: "src/types/cart.ts"
      provides: "updateItem method signature on CartStore interface"
      contains: "updateItem"
    - path: "src/lib/stores/cart-store.ts"
      provides: "updateItem implementation that maps over items array"
      contains: "updateItem"
    - path: "src/components/ui/menu/ItemDetailSheet.tsx"
      provides: "Edit mode with editingCartItem prop, pre-populated state, Update Cart CTA"
      contains: "editingCartItem"
    - path: "src/components/ui/cart/CartPage/CartPageContent.tsx"
      provides: "handleEditItem wired to ItemDetailSheet with edit mode"
      contains: "ItemDetailSheet"
  key_links:
    - from: "src/components/ui/cart/CartPage/CartPageContent.tsx"
      to: "src/components/ui/menu/ItemDetailSheet.tsx"
      via: "editingCartItem prop passed to ItemDetailSheet"
      pattern: "editingCartItem.*CartItem"
    - from: "src/components/ui/menu/ItemDetailSheet.tsx"
      to: "src/lib/stores/cart-store.ts"
      via: "onUpdateCart callback calling updateItem"
      pattern: "updateItem"
---

<objective>
Wire cart modifier editing through ItemDetailSheet with pre-populated state, "Update Cart" CTA, dirty-state discard confirmation, and sold-out handling.

Purpose: BKLG-02 -- CartPage modifier button opens ItemDetailSheet for editing modifiers on an existing cart item
Output: Working cart edit flow where pencil icon opens pre-populated ItemDetailSheet
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/cart.ts
@src/lib/stores/cart-store.ts
@src/components/ui/menu/ItemDetailSheet.tsx
@src/components/ui/cart/CartPage/CartPageContent.tsx
@src/components/ui/cart/CartItem/CartItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateItem to cart store and wire ItemDetailSheet edit mode</name>
  <files>
    src/types/cart.ts
    src/lib/stores/cart-store.ts
    src/components/ui/menu/ItemDetailSheet.tsx
  </files>
  <action>
**1. Add `updateItem` to CartStore interface (`src/types/cart.ts`):**
- Add method: `updateItem: (cartItemId: string, updates: { modifiers: SelectedModifier[]; quantity: number; notes: string; basePriceCents: number }) => void;`
- Import `SelectedModifier` from same file (already defined there)

**2. Implement `updateItem` in cart store (`src/lib/stores/cart-store.ts`):**
```typescript
updateItem: (cartItemId, updates) => {
  set((state) => ({
    items: state.items.map((item) =>
      item.cartItemId === cartItemId
        ? {
            ...item,
            modifiers: updates.modifiers,
            quantity: Math.min(Math.max(1, updates.quantity), MAX_ITEM_QUANTITY),
            notes: updates.notes.trim(),
            basePriceCents: updates.basePriceCents,
          }
        : item
    ),
  }));
},
```

**3. Add edit mode to ItemDetailSheet (`src/components/ui/menu/ItemDetailSheet.tsx`):**
- Extend `ItemDetailSheetProps` with:
  - `editingCartItem?: CartItem` (import CartItem from `@/types/cart`)
  - `onUpdateCart?: (cartItemId: string, modifiers: SelectedModifier[], quantity: number, notes: string, basePriceCents: number) => void`
- Add `isDirty` state tracking: compare current selections against initial editingCartItem values
- Modify the `useEffect` reset (line 107-112):
  - When `editingCartItem` is provided AND `isOpen`: pre-populate `selectedModifiers`, `quantity`, `notes` from it
  - When no `editingCartItem`: keep current reset behavior (empty state)
- Add discard confirmation: if `isDirty` is true when user tries to close (onClose), show `AlertDialog` asking "Discard changes?" with "Discard" and "Keep Editing" buttons. Import AlertDialog from Radix (`@radix-ui/react-alert-dialog`) -- check if already wrapped in project, otherwise use inline.
- Modify the AddToCartButton section:
  - When `editingCartItem` is set: change button text from "Add to Cart - $X.XX" to "Update Cart - $X.XX"
  - When `editingCartItem` is set AND `item.isSoldOut`: show "Item Unavailable - Remove from Cart" button instead, calling `onClose` (parent handles removal UI)
  - On click: call `onUpdateCart` with cartItemId, modifiers, quantity, notes, and recalculated basePriceCents from `priceCalc`
  - Show toast: `toast({ title: "Cart updated" })` after successful update -- import toast from existing toast hook
- Zero-quantity behavior: minimum quantity is 1 (QuantitySelector already enforces min=1). Removing item entirely is done from cart page controls, not edit sheet.
- Live price update: already works -- `priceCalc` recalculates on every modifier/quantity change.
  </action>
  <verify>
- `pnpm typecheck` passes (new props and store method type-safe)
- `pnpm lint` passes
- `pnpm test` passes (existing cart tests unbroken)
  </verify>
  <done>
- CartStore interface has `updateItem` method
- cart-store.ts implements `updateItem` that preserves cart item position
- ItemDetailSheet accepts `editingCartItem` and `onUpdateCart` props
- Edit mode pre-populates modifiers, quantity, notes from cart item
- CTA says "Update Cart" in edit mode
- Dirty-state discard confirmation on close
- Sold-out items show unavailable state in edit mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CartPageContent to ItemDetailSheet with edit flow</name>
  <files>
    src/components/ui/cart/CartPage/CartPageContent.tsx
    src/components/ui/cart/CartItem/CartItem.tsx
  </files>
  <action>
**1. Wire edit flow in CartPageContent (`CartPageContent.tsx`):**
- Replace the `setEditingItem` state with proper state management:
  - `const [editingItem, setEditingItem] = useState<CartItem | null>(null)`
  - `const [editMenuItem, setEditMenuItem] = useState<MenuItem | null>(null)` -- the matching MenuItem from menuData
- Update `handleEditItem`:
  - Look up the `MenuItem` from `menuData.data.categories` by matching `item.menuItemId`
  - If found: set both `editingItem` and `editMenuItem`
  - If not found (item removed from menu): show toast "This item is no longer on the menu"
- Add `handleUpdateCart` callback:
  - Call `useCartStore.getState().updateItem(cartItemId, { modifiers, quantity, notes, basePriceCents })`
  - Show toast: "Cart updated"
  - Clear `editingItem` and `editMenuItem` state
- Add `handleCloseEdit` callback:
  - Clear `editingItem` and `editMenuItem`
- Render `ItemDetailSheet` in the component JSX:
  ```tsx
  <ItemDetailSheet
    item={editMenuItem}
    isOpen={editingItem !== null}
    onClose={handleCloseEdit}
    editingCartItem={editingItem ?? undefined}
    onUpdateCart={handleUpdateCart}
  />
  ```
- Gate on `hydrated === true` before allowing edit (already handled -- the edit UI only renders when hydrated)

**2. Verify CartItem edit icon visibility (`CartItem.tsx`):**
- Check that the `onEdit` prop triggers correctly -- CartItem.tsx line 169-173 shows an edit button with `onEdit` prop
- Ensure the pencil icon only appears for items with modifier options:
  - Pass a `hasModifiers` prop or check `item.modifiers.length > 0` is not the right check (modifiers are selected modifiers, not available ones)
  - The correct check: look up MenuItem from menuData, check if `menuItem.modifierGroups.length > 0`
  - In CartItemGroup (which renders CartItem), pass a `showEditButton` prop based on whether the MenuItem has modifierGroups
  - If the MenuItem lookup fails (item removed from menu), don't show edit icon

**Important:** Each cart entry is independent. Editing one doesn't affect duplicates.
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- `pnpm test` passes
  </verify>
  <done>
- CartPageContent.tsx TODO stub replaced with working edit flow
- Pencil icon tap opens ItemDetailSheet pre-populated with cart item data
- Only items with available modifier options show edit icon
- "Cart updated" toast on save
- Cart item updates in place without position change
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- Manual check: pencil icon visibility on items with/without modifiers
- Manual check: edit sheet pre-populates correctly
- Manual check: "Update Cart" CTA text and behavior
- Manual check: discard confirmation on dirty close
</verification>

<success_criteria>
- CartPage pencil icon opens ItemDetailSheet with pre-populated modifier/quantity/notes state
- CTA says "Update Cart" in edit mode, "Add to Cart" in add mode
- Cart item updates in place without creating duplicate
- "Cart updated" toast on successful edit
- Discard confirmation dialog when closing with unsaved changes
- Sold-out items open in unavailable state (remove only)
- Items without modifier groups do not show edit icon
</success_criteria>

<output>
After completion, create `.planning/phases/66-backlog-cleanup/66-01-SUMMARY.md`
</output>
