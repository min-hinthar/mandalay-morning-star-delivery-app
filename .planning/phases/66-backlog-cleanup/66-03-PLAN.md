---
phase: 66-backlog-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["66-02"]
files_modified:
  - src/components/ui/orders/tracking/TrackingPageClient.tsx
  - src/components/ui/orders/tracking/DeliveryMap/DeliveryMap.tsx
  - src/components/ui/orders/tracking/DeliveryMap/CustomMarkers.tsx
  - src/components/ui/orders/tracking/DeliveryMap/RoutePolyline.tsx
  - src/components/ui/orders/tracking/DeliveryMap/constants.ts
  - src/components/ui/orders/tracking/StatusStepper.tsx
  - src/components/ui/orders/tracking/DeliveryMap/index.tsx
autonomous: true

must_haves:
  truths:
    - "Map shows custom branded pin icons for restaurant, vehicle, and destination (not default Google markers)"
    - "Route line drawn between driver and destination with completed portion in different color"
    - "Driver marker smooth-slides from old to new position (not jump)"
    - "Map auto-fits zoom to show both driver and destination pins"
    - "Re-center button restores auto-fit view after user pans/zooms"
    - "Split view layout: map top 50%, status/info bottom 50%"
    - "Horizontal status stepper shows Confirmed -> Preparing -> Out for Delivery -> Delivered with active step highlighted"
    - "Stepper dots fill with animation and active step pulses"
    - "Pre-delivery (preparing) state shows restaurant pin on map"
    - "Stale location (>2 min no update) shows faded driver pin with 'Last updated X min ago'"
  artifacts:
    - path: "src/components/ui/orders/tracking/DeliveryMap/CustomMarkers.tsx"
      provides: "Branded marker components for restaurant, vehicle, destination"
      contains: "RestaurantMarker"
    - path: "src/components/ui/orders/tracking/DeliveryMap/RoutePolyline.tsx"
      provides: "Route line with completed/remaining color split"
      contains: "RoutePolyline"
    - path: "src/components/ui/orders/tracking/StatusStepper.tsx"
      provides: "Horizontal stepper with Confirmed->Preparing->Out for Delivery->Delivered"
      contains: "StatusStepper"
  key_links:
    - from: "src/components/ui/orders/tracking/TrackingPageClient.tsx"
      to: "src/components/ui/orders/tracking/StatusStepper.tsx"
      via: "currentStatus prop"
      pattern: "StatusStepper.*currentStatus"
    - from: "src/components/ui/orders/tracking/DeliveryMap/DeliveryMap.tsx"
      to: "src/components/ui/orders/tracking/DeliveryMap/CustomMarkers.tsx"
      via: "AdvancedMarkerElement content elements"
      pattern: "createMarkerContent"
---

<objective>
Build the enhanced delivery map with custom markers, route line, smooth animation, re-center, and split-view layout. Add horizontal status stepper component.

Purpose: BKLG-03 UI -- the tracking page map and status display feel like a premium delivery experience
Output: Enhanced DeliveryMap with branded markers + route polyline, StatusStepper component, split-view layout
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-backlog-cleanup/66-02-SUMMARY.md
@src/components/ui/orders/tracking/DeliveryMap/DeliveryMap.tsx
@src/components/ui/orders/tracking/DeliveryMap/constants.ts
@src/components/ui/orders/tracking/TrackingPageClient.tsx
@src/components/ui/orders/tracking/StatusTimeline/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhanced DeliveryMap with custom markers, route line, smooth animation</name>
  <files>
    src/components/ui/orders/tracking/DeliveryMap/DeliveryMap.tsx
    src/components/ui/orders/tracking/DeliveryMap/CustomMarkers.tsx
    src/components/ui/orders/tracking/DeliveryMap/RoutePolyline.tsx
    src/components/ui/orders/tracking/DeliveryMap/constants.ts
    src/components/ui/orders/tracking/DeliveryMap/index.tsx
  </files>
  <action>
**1. Create CustomMarkers.tsx:**
- `createRestaurantMarkerContent()`: Returns DOM element with restaurant icon -- use a custom SVG with the Morning Star branding colors (saffron/jade). A stylized building/restaurant icon inside a teardrop pin shape. Size ~40x48px.
- `createVehicleMarkerContent(heading: number | null, isStale: boolean)`: Returns DOM element with vehicle icon (car/motorcycle SVG) rotated to heading direction. When `isStale === true`, apply 50% opacity CSS. Size ~36x36px.
- `createDestinationMarkerContent()`: Returns DOM element with destination pin -- jade green teardrop pin with a home icon. Size ~40x48px.
- All markers use inline styles (AdvancedMarkerElement content is raw DOM, not React).
- Use brand colors: saffron (#D4A017) for vehicle, jade (#2E8B57) for destination, warm red (#C75050) for restaurant.

**2. Create RoutePolyline.tsx:**
- `RouteProgressLine` component that renders TWO `Polyline` components from `@react-google-maps/api`:
  - Completed portion (driver's start to current position): jade green, solid, strokeWeight 5
  - Remaining portion (driver current position to destination): charcoal-300, dashed, strokeWeight 4
- Accept props: `driverPosition: LatLng | null`, `destinationPosition: LatLng`, `routePolyline?: string | null`
- If no encoded `routePolyline`, draw a simple straight line between points (adequate for the current data model)
- When driver position is null (pre-delivery), show only a single line from restaurant to destination

**3. Enhance DeliveryMap.tsx:**
- Replace the current inline DOM creation (lines 101-132) with `CustomMarkers` helper functions
- Add **smooth driver marker animation**: On position change, use `requestAnimationFrame` to interpolate lat/lng from old position to new over 1 second (16 frames). Store previous position in ref. The AdvancedMarkerElement's `position` property can be updated directly.
  - Use `useSafeTimeout` from `@/lib/hooks/useSafeEffects` for any timers
- Add **re-center button**: When user has panned/zoomed manually (detect via `dragend` or `zoom_changed` event), show a "Re-center" floating button (bottom-right). On click, fitBounds back to show both markers.
  - Track `userHasPanned` state; reset on re-center click
- Add `restaurantLocation?: LatLng | null` prop for pre-delivery state:
  - When `orderStatus` is not `out_for_delivery`, show restaurant marker instead of driver marker
  - Map centers on restaurant + destination
- Add **stale location detection**: Accept `lastLocationUpdate: Date | null` prop. If `Date.now() - lastLocationUpdate > 2 * 60 * 1000` (2 minutes), set `isStale = true` on vehicle marker (fades it). Show small "Last updated X min ago" badge on the map.
- Add **auto-fit zoom** on initial load and when driver position changes significantly (>0.001 lat/lng delta)
- Add `RouteProgressLine` between driver and destination
- Update `DeliveryMapProps` to include new props: `restaurantLocation`, `orderStatus`, `lastLocationUpdate`
- Map loading state: grey rectangle placeholder (already exists, improve with skeleton pulse)
- Map error state: error banner with "Retry" button that calls `window.location.reload()` (simple) or re-triggers loader
- Update barrel `index.tsx` to export new sub-components

**4. Update constants.ts:**
- Add marker size constants
- Add animation duration constant: `MARKER_ANIMATION_DURATION_MS = 1000`
- Add stale threshold: `STALE_LOCATION_THRESHOLD_MS = 2 * 60 * 1000`
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
  </verify>
  <done>
- Custom branded markers for restaurant, vehicle, destination
- Route line with completed/remaining color differentiation
- Smooth driver marker animation (1s interpolation)
- Re-center button after user pan/zoom
- Stale location detection with faded marker + timestamp
- Pre-delivery state shows restaurant pin
- Map loading skeleton and error retry
  </done>
</task>

<task type="auto">
  <name>Task 2: StatusStepper component and split-view layout in TrackingPageClient</name>
  <files>
    src/components/ui/orders/tracking/StatusStepper.tsx
    src/components/ui/orders/tracking/TrackingPageClient.tsx
    src/components/ui/orders/tracking/index.ts
  </files>
  <action>
**1. Create StatusStepper.tsx:**
- Horizontal stepper showing: Confirmed -> Preparing -> Out for Delivery -> Delivered
- Skip "pending" status from display (it's the initial state before confirmation)
- Props: `currentStatus: OrderStatus`, `cancelledAt?: string | null`
- Each step: icon + label below, connected by horizontal line
- Step states: completed (filled jade circle + check icon), current (primary color + pulse animation via framer-motion `animate={{ scale: [1, 1.15, 1] }}`), future (charcoal-200 circle)
- Connecting lines: completed = jade, future = charcoal-200
- Use `m.div` for the pulse animation on current step
- aria: `role="progressbar"` with `aria-valuenow`, `aria-valuemin`, `aria-valuemax`
- aria-live region: `<div aria-live="polite" className="sr-only">` that announces current status text for screen readers
- Status icons: use lucide-react icons -- `Check` for completed, `ChefHat` or `Clock` for preparing, `Truck` for out_for_delivery, `Package` for delivered
- If cancelled: show all steps as greyed out with a red "Cancelled" badge overlay
- `'use client'` directive (uses framer-motion)

**2. Refactor TrackingPageClient.tsx to split-view layout:**
- Replace current single-column layout with split view:
  - Map section: top 50vh on mobile, left 50% on desktop/landscape
  - Info section: bottom 50vh on mobile (scrollable), right 50% on desktop
  - Use CSS: `h-[50vh]` for map container, `h-[50vh] overflow-y-auto` for info
  - Tablet/landscape: `@media (min-width: 1024px) { grid-cols-2 }` -- map left, info right, both full height
- Add StatusStepper at top of info section (replaces current StatusTimeline position -- keep StatusTimeline below as vertical detail)
- Pass new props to DeliveryMap:
  - `restaurantLocation={initialData.restaurantLocation}`
  - `orderStatus={orderStatus}`
  - `lastLocationUpdate={subscription.lastUpdate}`
- Show map in ALL states (not just `showLiveTracking`):
  - Pre-delivery: map shows restaurant + destination markers
  - Out for delivery: map shows driver + destination + route line
  - Delivered: map shows final positions (static)
- Add browser tab title updates: `useEffect` that sets `document.title` based on `orderStatus`:
  - `"Confirmed | Morning Star"`, `"Preparing... | Morning Star"`, `"Out for Delivery | Morning Star"`, `"Delivered! | Morning Star"`
  - Restore original title on unmount
- Update barrel `index.ts` to export `StatusStepper`
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- `pnpm test` passes
  </verify>
  <done>
- StatusStepper renders horizontal step indicators with animation
- Split-view layout: map top 50% / info bottom 50% (responsive)
- Map visible in all order states (pre-delivery, en route, delivered)
- Browser tab title updates with live status
- aria-live announcements on status changes
- Landscape/tablet responsive layout
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- Visual: custom markers render (not default Google pins)
- Visual: route line drawn between driver and destination
- Visual: split-view layout with map top / info bottom
- Visual: status stepper shows correct active step
</verification>

<success_criteria>
- Custom branded markers for restaurant, vehicle, destination on map
- Route line with completed/remaining color split
- Driver marker animates smoothly between positions
- Re-center button appears after user pan/zoom
- Stale location (>2 min) shows faded driver pin with timestamp
- Split-view: map top 50%, info bottom 50%
- Status stepper horizontal with animated dots
- Browser tab title reflects current order status
- Map visible in pre-delivery state (restaurant pin)
</success_criteria>

<output>
After completion, create `.planning/phases/66-backlog-cleanup/66-03-SUMMARY.md`
</output>
