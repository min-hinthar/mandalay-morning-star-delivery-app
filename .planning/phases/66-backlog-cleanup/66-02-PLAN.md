---
phase: 66-backlog-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/tracking.ts
  - src/types/database.ts
  - src/app/api/tracking/[orderId]/route.ts
  - src/app/api/tracking/[orderId]/types.ts
  - src/lib/hooks/useTrackingSubscription.ts
  - src/components/ui/orders/tracking/TrackingPageClient.tsx
  - src/app/(customer)/orders/[id]/tracking/page.tsx
autonomous: true

must_haves:
  truths:
    - "Tracking API returns routeId in the TrackingData response"
    - "TrackingPageClient passes routeId to useTrackingSubscription, enabling location channel subscription"
    - "driver_ratings table has TypeScript types in database.ts"
    - "Tracking API returns restaurantLocation for pre-delivery map state"
    - "Tracking API relaxes ownership check when shared=true query param is present (any authenticated user can view)"
  artifacts:
    - path: "src/types/tracking.ts"
      provides: "routeId field on TrackingData, restaurantLocation on TrackingOrderInfo"
      contains: "routeId"
    - path: "src/types/database.ts"
      provides: "DriverRatingsRow and DriverRatingsInsert types"
      contains: "driver_ratings"
    - path: "src/app/api/tracking/[orderId]/route.ts"
      provides: "routeId extraction from routeStopData, restaurantLocation, shared access"
      contains: "routeStopData?.routes?.id"
    - path: "src/components/ui/orders/tracking/TrackingPageClient.tsx"
      provides: "routeId passed to useTrackingSubscription"
      contains: "routeId: initialData.routeId"
  key_links:
    - from: "src/app/api/tracking/[orderId]/route.ts"
      to: "src/types/tracking.ts"
      via: "routeId field populated from routeStopData.routes.id"
      pattern: "routeId.*routes.*id"
    - from: "src/components/ui/orders/tracking/TrackingPageClient.tsx"
      to: "src/lib/hooks/useTrackingSubscription.ts"
      via: "routeId prop enabling location subscription channel"
      pattern: "routeId.*initialData"
---

<objective>
Fix the route_id extraction bug and extend tracking foundation: add routeId and restaurantLocation to API response, driver_ratings types, and shared tracking link support.

Purpose: BKLG-03 foundation -- fix the known route_id bug and extend types/API for the full tracking enhancement
Output: Working location subscription via routeId, extended TrackingData types, driver_ratings DB types
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/tracking.ts
@src/types/database.ts
@src/app/api/tracking/[orderId]/route.ts
@src/app/api/tracking/[orderId]/types.ts
@src/lib/hooks/useTrackingSubscription.ts
@src/components/ui/orders/tracking/TrackingPageClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tracking types and database types</name>
  <files>
    src/types/tracking.ts
    src/types/database.ts
  </files>
  <action>
**1. Add routeId and restaurantLocation to TrackingData (`src/types/tracking.ts`):**
- Add `routeId: string | null` to `TrackingData` interface
- Add `restaurantLocation: LatLng | null` to `TrackingData` interface (for pre-delivery map state showing restaurant pin)
- Add `cancelledAt: string | null` and `cancellationReason: string | null` to `TrackingOrderInfo` (for cancellation overlay)
- Add `rating: number | null` to `TrackingData` (for post-delivery read-only view showing existing rating)
- Add `deliveryNotes: string | null` to `TrackingOrderInfo` -- alias for `specialInstructions` but more semantic

**2. Add driver_ratings types to database.ts (`src/types/database.ts`):**

- Check existing database.ts structure for the pattern used
- Add `DriverRatingsRow` type:
  ```typescript
  export interface DriverRatingsRow {
    id: string;
    order_id: string;
    driver_id: string;
    user_id: string;
    rating: number; // 1-5
    created_at: string;
  }
  ```
- Add `DriverRatingsInsert` type (Omit id and created_at):
  ```typescript
  export type DriverRatingsInsert = Omit<DriverRatingsRow, "id" | "created_at">;
  ```
- Add to `Database` type's `Tables` if it follows that pattern, or as standalone types if database.ts uses flat exports
  </action>
  <verify>
- `pnpm typecheck` passes
  </verify>
  <done>
- TrackingData has `routeId`, `restaurantLocation`, `rating` fields
- TrackingOrderInfo has cancellation fields and deliveryNotes
- database.ts has DriverRatingsRow and DriverRatingsInsert types
  </done>
  </task>

<task type="auto">
  <name>Task 2: Fix route_id extraction in API and wire to TrackingPageClient</name>
  <files>
    src/app/api/tracking/[orderId]/route.ts
    src/app/api/tracking/[orderId]/types.ts
    src/components/ui/orders/tracking/TrackingPageClient.tsx
    src/app/(customer)/orders/[id]/tracking/page.tsx
  </files>
  <action>
**1. Fix API route (`src/app/api/tracking/[orderId]/route.ts`):**
- Extract `routeId` at the point where `routeStopData` is available (after the route_stops query, before constructing `trackingData`):
  ```typescript
  // After: const routeStopData = ... (existing route_stops query)
  const routeId = routeStopData?.routes?.id ?? null;
  ```
  Then include `routeId` in the `trackingData` response object:
  ```typescript
  const trackingData: TrackingData = {
    // ...existing fields
    routeId,
    // ...
  };
  ```
- Add `restaurantLocation` to response: query restaurant location from a config table or hardcode the restaurant's known coordinates (check if there's a restaurant_settings or similar table; if not, use a constant from the project -- the restaurant is "Mandalay Morning Star" in Los Angeles)
- Add shared access support:
  - Parse `?shared=true` from request URL: `const url = new URL(request.url); const isShared = url.searchParams.get('shared') === 'true';`
  - When `isShared` is true, skip the `order.user_id !== user.id` ownership check (still require authentication)
- Add cancellation fields: the orders table likely has `cancelled_at` and `cancellation_reason` -- add them to the select query and include in response. If columns don't exist, set to null.
- Add rating lookup: query `driver_ratings` table for this order_id and include in response as `rating: driverRating?.rating ?? null`
- Update `trackingData` construction to include `routeId`, `restaurantLocation`, `rating`

**2. Wire routeId in TrackingPageClient (`TrackingPageClient.tsx`):**

- Change line 54 from `routeId: undefined` to `routeId: initialData.routeId ?? undefined`
- Remove the TODO comment about route_id extraction
- This enables the location subscription channel in `useTrackingSubscription` (lines 243-266 in hook)

**3. Verify tracking page.tsx passes initialData correctly:**

- Read `src/app/(customer)/orders/[id]/tracking/page.tsx` to confirm it fetches from the API and passes `initialData` to `TrackingPageClient`
- No changes needed if it already passes the full TrackingData object
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm test` passes (existing tracking API tests)
- `pnpm build` succeeds
  </verify>
  <done>
- Tracking API returns `routeId` from `routeStopData.routes.id`
- TrackingPageClient passes `routeId` to subscription hook (location channel now works)
- Shared tracking links work (any authenticated user with `?shared=true`)
- Rating and restaurantLocation included in API response
- The route_id extraction bug (CONCERNS.md known bug) is fixed
  </done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- Verify route_id is no longer hardcoded as `undefined` in TrackingPageClient
- Verify API response includes routeId field
</verification>

<success_criteria>

- route_id extraction bug is fixed -- location subscription works when route is assigned
- TrackingData type includes routeId, restaurantLocation, rating
- driver_ratings TypeScript types exist in database.ts
- Shared tracking links accessible by any authenticated user
- All existing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/66-backlog-cleanup/66-02-SUMMARY.md`
</output>
