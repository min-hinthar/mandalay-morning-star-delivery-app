---
phase: 44-animation-optimization-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/lib/gsap/index.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "React Compiler auto-memoizes all client components without manual useMemo/useCallback"
    - "Build completes successfully with React Compiler enabled"
    - "All tests pass with React Compiler active"
    - "No dead GSAP plugins (SplitText, Flip, Observer) are loaded or registered"
    - "All GSAP scroll animations still function (ScrollTrigger + useGSAP intact)"
  artifacts:
    - path: "next.config.ts"
      provides: "React Compiler configuration"
      contains: "reactCompiler: true"
    - path: "src/lib/gsap/index.ts"
      provides: "Cleaned GSAP registration with only used plugins"
      contains: "gsap.registerPlugin(useGSAP, ScrollTrigger)"
    - path: "package.json"
      provides: "babel-plugin-react-compiler devDependency"
      contains: "babel-plugin-react-compiler"
  key_links:
    - from: "next.config.ts"
      to: "babel-plugin-react-compiler"
      via: "reactCompiler: true requires the Babel plugin installed"
      pattern: "reactCompiler.*true"
    - from: "src/lib/gsap/index.ts"
      to: "src/components/ui/scroll/*.tsx"
      via: "ScrollTrigger export used by ParallaxLayer, RevealOnScroll, ScrollChoreographer"
      pattern: "export.*ScrollTrigger"
---

<objective>
Enable React Compiler globally and remove dead GSAP plugins to reduce bundle size and improve runtime performance.

Purpose: React Compiler auto-memoizes 282 client components, eliminating unnecessary re-renders. Removing 3 unused GSAP plugins (SplitText, Flip, Observer) eliminates dead code from the animation bundle.
Output: next.config.ts with reactCompiler enabled, cleaned gsap/index.ts, babel-plugin-react-compiler installed
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-animation-optimization-monitoring/44-CONTEXT.md
@.planning/phases/44-animation-optimization-monitoring/44-RESEARCH.md
@next.config.ts
@src/lib/gsap/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable React Compiler globally</name>
  <files>next.config.ts, package.json</files>
  <action>
    1. Install babel-plugin-react-compiler as devDependency:
       `pnpm add -D babel-plugin-react-compiler`

    2. Add `reactCompiler: true` to next.config.ts as a TOP-LEVEL property of nextConfig (NOT under `experimental`). Place it after `reactStrictMode: true`:
       ```typescript
       const nextConfig: NextConfig = {
         reactStrictMode: true,
         reactCompiler: true,
         // ... rest unchanged
       }
       ```

    3. Run `pnpm build` to verify the compiler works with all 282 client components.

    4. Run `pnpm test` to verify all tests pass.

    5. If any component breaks (animations freeze, motion values don't update, GSAP timelines fail):
       - Add `"use no memo"` directive as the first statement inside the failing component function
       - Re-run build + test
       - Document which components needed opt-out

    Do NOT:
    - Put reactCompiler under `experimental` (it's a stable top-level option in Next.js 16)
    - Pre-emptively add "use no memo" to animation files (only add if they actually break)
    - Remove any existing config options

  </action>
  <verify>
    `pnpm build` succeeds with zero errors.
    `pnpm test` passes.
    Grep confirms: `grep "reactCompiler" next.config.ts` shows `reactCompiler: true`
  </verify>
  <done>React Compiler is active globally. Build and all tests pass. Any components needing "use no memo" are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead GSAP plugins</name>
  <files>src/lib/gsap/index.ts</files>
  <action>
    1. Edit `src/lib/gsap/index.ts`:
       - Remove import lines for SplitText, Flip, Observer:
         ```
         DELETE: import { SplitText } from "gsap/SplitText";
         DELETE: import { Flip } from "gsap/Flip";
         DELETE: import { Observer } from "gsap/Observer";
         ```
       - Update registerPlugin call to only register used plugins:
         ```typescript
         gsap.registerPlugin(useGSAP, ScrollTrigger);
         ```
       - Update the re-export line to only export used items:
         ```typescript
         export { gsap, useGSAP, ScrollTrigger };
         ```

    2. Verify no other files import SplitText, Flip, or Observer from `@/lib/gsap`:
       `grep -rn "SplitText\|Flip\|Observer" src/ --include="*.ts" --include="*.tsx" | grep -v "gsap/index.ts" | grep -v "node_modules" | grep -v "IntersectionObserver\|ResizeObserver\|MutationObserver"`
       Expected: zero results (research confirmed these are unused).

    3. If any files DO import these plugins, update them to remove the unused imports.

    4. Run `pnpm build && pnpm test` to confirm GSAP scroll animations still work (ParallaxLayer, RevealOnScroll, ScrollChoreographer, FlyToCart all use only gsap core + ScrollTrigger + useGSAP).

    Do NOT:
    - Remove ScrollTrigger or useGSAP (actively used by 4 consumer files)
    - Change gsap.config() or gsap.defaults() settings
    - Modify conflict-detector.ts or presets.ts
    - Add dynamic imports for GSAP (scroll-triggered content is already deferred)

  </action>
  <verify>
    `grep -c "SplitText\|Flip\|Observer" src/lib/gsap/index.ts` returns 0.
    `pnpm build` succeeds.
    `pnpm test` passes.
  </verify>
  <done>GSAP index.ts registers only useGSAP + ScrollTrigger. No dead plugin code is loaded. All 4 GSAP consumer files still function.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with React Compiler enabled and cleaned GSAP
- `pnpm test` all tests pass
- `grep "reactCompiler" next.config.ts` shows the config is active
- `grep -c "SplitText" src/lib/gsap/index.ts` returns 0
- No "use no memo" directives needed (ideal) or documented if needed
</verification>

<success_criteria>

- React Compiler globally active via `reactCompiler: true` in next.config.ts
- babel-plugin-react-compiler installed as devDependency
- SplitText, Flip, Observer removed from GSAP registration and exports
- Build passes, all tests pass, no animation regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/44-animation-optimization-monitoring/44-01-SUMMARY.md`
</output>
