---
phase: 52-cart-validation
plan: 03
type: execute
wave: 2
depends_on: ["52-01", "52-02"]
files_modified:
  - src/components/ui/cart/CartPage/CartPageContent.tsx
  - src/components/ui/cart/CartPage/CartPageHeader.tsx
  - src/components/ui/cart/CartPage/CartItemGroup.tsx
  - src/components/ui/cart/CartPage/CartPageSummary.tsx
  - src/components/ui/cart/CartPage/CheckoutGate.tsx
  - src/components/ui/cart/CartPage/index.tsx
  - src/app/(customer)/cart/page.tsx
autonomous: true

must_haves:
  truths:
    - "Cart page shows two-column layout on desktop (items left, summary right) and single column on mobile"
    - "Cart items are grouped by category under category headers"
    - "Cart page title shows item count: 'Your Cart (5 items)'"
    - "Checkout button is disabled when stale/sold-out items exist or minimum order not met"
    - "Warning banner above checkout button smooth-scrolls to attention section when tapped"
    - "Cart page shows skeleton cards during validation"
    - "Empty cart shows CartEmptyState with Browse Menu CTA"
  artifacts:
    - path: "src/components/ui/cart/CartPage/CartPageContent.tsx"
      provides: "Main two-column layout orchestrating all cart page sections"
      exports: ["CartPageContent"]
    - path: "src/components/ui/cart/CartPage/CartPageHeader.tsx"
      provides: "Cart page header with item count and Continue Shopping link"
      exports: ["CartPageHeader"]
    - path: "src/components/ui/cart/CartPage/CartItemGroup.tsx"
      provides: "Category-grouped cart items with category headers"
      exports: ["CartItemGroup"]
    - path: "src/components/ui/cart/CartPage/CartPageSummary.tsx"
      provides: "Order summary with subtotal, delivery fee, tax, total + minimum order shortfall"
      exports: ["CartPageSummary"]
    - path: "src/components/ui/cart/CartPage/CheckoutGate.tsx"
      provides: "Checkout button with warning banner and disabled state logic"
      exports: ["CheckoutGate"]
    - path: "src/app/(customer)/cart/page.tsx"
      provides: "Full cart page replacing stub"
      exports: ["default"]
  key_links:
    - from: "src/components/ui/cart/CartPage/CartPageContent.tsx"
      to: "src/lib/hooks/useCartValidation.ts"
      via: "useCartValidation() for validation state"
      pattern: "useCartValidation"
    - from: "src/components/ui/cart/CartPage/CartPageContent.tsx"
      to: "src/lib/hooks/useCart.ts"
      via: "useCart() for cart items and actions"
      pattern: "useCart"
    - from: "src/components/ui/cart/CartPage/CheckoutGate.tsx"
      to: "src/types/cart.ts"
      via: "MINIMUM_ORDER_CENTS for checkout blocking"
      pattern: "MINIMUM_ORDER_CENTS"
---

<objective>
Build the full cart page at /cart with two-column layout, category-grouped items, order summary, checkout gate, and attention section for stale items.

Purpose: Replace the stub cart page with a fully functional page that integrates validation results, provides category-grouped item display, and gates checkout on cart health.
Output: Complete cart page with all layout components wired to validation hook.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cart-validation/52-RESEARCH.md
@.planning/phases/52-cart-validation/52-01-SUMMARY.md
@.planning/phases/52-cart-validation/52-02-SUMMARY.md

@src/app/(customer)/cart/page.tsx
@src/components/ui/cart/CartDrawer.tsx
@src/components/ui/cart/CartSummary.tsx
@src/components/ui/cart/CartEmptyState.tsx
@src/components/ui/cart/CartItem/CartItem.tsx
@src/lib/hooks/useCartValidation.ts
@src/lib/hooks/useCart.ts
@src/types/cart.ts
@src/types/menu.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CartPageHeader, CartItemGroup, CartPageSummary, and CheckoutGate</name>
  <files>src/components/ui/cart/CartPage/CartPageHeader.tsx, src/components/ui/cart/CartPage/CartItemGroup.tsx, src/components/ui/cart/CartPage/CartPageSummary.tsx, src/components/ui/cart/CartPage/CheckoutGate.tsx</files>
  <action>
    Use `/frontend-design` skill for thematic, polished, premium, playful UI.

    **CartPageHeader.tsx:**
    - `'use client'` directive
    - Props: `itemCount: number`
    - Title: "Your Cart ({itemCount} items)" using font-display, large heading
    - "Continue Shopping" back link at top-left using `Link` from next/link pointing to `/menu`
    - Back arrow icon (`ArrowLeft` from lucide) before text
    - Stagger entrance animation on title + link

    **CartItemGroup.tsx:**
    - `'use client'` directive
    - Props:
      ```typescript
      interface CartItemGroupProps {
        categoryName: string;
        items: CartItem[];
        validations: Map<string, CartItemValidation>;
        suggestions: Map<string, MenuItem[]>;
        onRemoveItem: (cartItemId: string) => void;
        onReplaceItem: (cartItemId: string, suggestion: MenuItem, originalQuantity: number) => void;
        onEditItem: (item: CartItem) => void;
        onDismissPriceChange: (cartItemId: string, newPriceCents: number) => void;
      }
      ```
    - Category header: category name in uppercase, small text, with subtle divider line
    - Maps over items, rendering each `CartItem` component
    - For items with `sold-out` or `unavailable` validation: overlay `ValidationOverlay` on the CartItem
    - For items with `price-changed` validation: render `PriceChangeBadge` below the CartItem
    - For sold-out/unavailable items with suggestions: render `SuggestionRow` below the item
    - Item removal animation: `AnimatePresence` with `exit={{ opacity: 0, x: -200 }}` (slide out left per decision)
    - Items wrapped in `m.li` inside `AnimatePresence mode="sync"` (not popLayout per existing learnings)
    - Pass `onEdit` prop to CartItem for tap-to-edit behavior

    **CartPageSummary.tsx:**
    - `'use client'` directive
    - Props: `subtotalCents: number, deliveryFeeCents: number, minimumShortfallCents: number, className?: string`
    - Extends existing CartSummary pattern but adds:
      - Estimated tax row (estimate at 8.5% of subtotal, labeled "Est. Tax")
      - Minimum order shortfall row: if `minimumShortfallCents > 0`, show "$X.XX below minimum" in red text within the summary section (per decision: shown in order summary, not separate banner)
    - Full itemized breakdown: Subtotal, Delivery Fee, Est. Tax, divider, Total
    - Use `PriceTicker` for animated price values (reuse from existing CartSummary)
    - Total line uses count-up animation when prices change (per decision: `PriceTicker` already handles this)
    - Sticky on desktop: `lg:sticky lg:top-24` (per discretion recommendation)
    - Glass card styling matching existing design system

    **CheckoutGate.tsx:**
    - `'use client'` directive
    - Props:
      ```typescript
      interface CheckoutGateProps {
        hasBlockingIssues: boolean;
        staleCount: number;
        minimumShortfallCents: number;
        onScrollToAttention: () => void;
        onCheckout: () => void;
        className?: string;
      }
      ```
    - Warning banner above checkout button (per decision: not inside button text)
    - Banner is tappable -- calls `onScrollToAttention` for smooth-scroll (per decision)
    - Combined message when multiple blockers: "{N} items need attention {dot separator} ${X} below minimum" (per decision)
    - Banner style: amber/red gradient background, white text, `AlertTriangle` icon
    - Checkout button:
      - Disabled when `hasBlockingIssues || minimumShortfallCents > 0`
      - Disabled state: gray background, muted text, no hover effect
      - Active state: primary green, shadow-elevated
      - Pulse transition from disabled gray to active green when all issues cleared (per decision)
      - Use `m.button` with `animate` prop: when transitioning from disabled to enabled, play a scale 1->1.05->1 spring animation + background color transition
    - Below checkout button: show "+ Add more items" link pointing to `/menu` (per decision)

  </action>
  <verify>Run `pnpm typecheck` and `pnpm lint` -- no errors. CheckoutGate correctly disables button when blocking issues or minimum not met.</verify>
  <done>CartPageHeader shows title with item count + back link. CartItemGroup renders category-grouped items with validation overlays. CartPageSummary shows full itemized breakdown with minimum shortfall. CheckoutGate shows warning banner + disabled/enabled checkout button with pulse transition.</done>
</task>

<task type="auto">
  <name>Task 2: Create CartPageContent and wire up cart page.tsx</name>
  <files>src/components/ui/cart/CartPage/CartPageContent.tsx, src/components/ui/cart/CartPage/index.tsx, src/app/(customer)/cart/page.tsx</files>
  <action>
    Use `/frontend-design` skill for thematic, polished, premium, playful UI.

    **CartPageContent.tsx:**
    - `'use client'` directive
    - Main orchestration component for the cart page
    - Uses `useCartHydrated()` -- if not hydrated, show skeleton cards (match cart item count from Zustand, or 3 fixed skeletons if items unknown)
    - Uses `useCartValidation()` -- drives which items get overlays
    - Uses `useCart()` -- gets items, actions, subtotal, delivery fee
    - Uses `useMenu()` -- for finding full MenuItem data when editing
    - Uses `useRef` for `attentionRef` -- passed to AttentionSection, used by CheckoutGate scroll-to
    - `scrollToAttention` callback: `attentionRef.current?.scrollIntoView({ behavior: 'smooth', block: 'start' })`

    **Layout:**
    - Two-column on desktop: `lg:grid lg:grid-cols-[1fr_380px] lg:gap-8`
    - Single column on mobile: stacks naturally
    - Left column: CartPageHeader, AttentionSection (if problem items), then CartItemGroups by category, then "+ Add more items" button
    - Right column: CartPageSummary (sticky) + CheckoutGate

    **Category grouping logic:**
    - Get menu categories from `useMenu()` data
    - Group valid cart items by category:
      1. For each cart item, find its category from menu data (by menuItemId lookup)
      2. Use cartItem.categoryId as fallback for items not found in current menu
      3. Items with no category go into "Other" group
    - Render `CartItemGroup` for each category group with its items
    - Problem items (sold-out/unavailable) go into the `AttentionSection` at top, NOT in category groups

    **Item replacement handler:**
    - `handleReplaceItem(cartItemId, suggestion, originalQuantity)`:
      1. Get the old item's quantity
      2. Call `removeItem(cartItemId)` to remove the stale item
      3. Call `addItem({ ...suggestion data mapped to CartItem shape, quantity: originalQuantity, categoryId: suggestion's category })` to add replacement
      4. Quantity carries over per decision

    **Price dismissal handler:**
    - `handleDismissPriceChange(cartItemId, newPriceCents)`:
      1. Call `updateItemPrice(cartItemId, newPriceCents)` from cart store
      2. This updates persisted price so badge won't reappear

    **Edit item handler (bottom sheet):**
    - Tapping a cart item opens `ItemDetailSheet` (existing component) or similar bottom sheet for editing
    - If editing is complex to wire, add a TODO comment and implement the tap handler to open a `Drawer` with item details
    - For this plan: implement a basic edit modal using existing `Drawer` component that shows item name, quantity stepper, and save button. Full modifier editing is a stretch goal.

    **Empty state:**
    - If cart is empty (after hydration), render `CartEmptyState` component (existing, reuse)

    **Skeleton during validation:**
    - When `validation.status === 'validating'`, show items with a subtle pulsing border overlay (per research: show items immediately, don't replace with skeletons to avoid content flash)
    - Simple approach: render items normally but add a `animate-pulse` ring around each card that fades out when validation completes

    **CartPage/index.tsx:**
    - Update barrel to export all CartPage components: CartPageContent, CartPageHeader, CartItemGroup, CartPageSummary, CheckoutGate, AttentionSection, SuggestionRow

    **page.tsx:**
    - Replace the stub content with `CartPageContent`
    - Keep the `CartNavigationGuard` integration (already exists)
    - Wrap main content in a max-width container: `max-w-6xl mx-auto px-4 py-6 sm:px-6 lg:px-8`
    - Page metadata: export `metadata = { title: "Your Cart" }`
    - Wait -- page.tsx is `'use client'` for navigation guard. Keep as client component. Move metadata to a `generateMetadata` or just keep it simple without static metadata (client components can't export metadata).

  </action>
  <verify>
    Run `pnpm typecheck` and `pnpm lint` -- no errors.
    Run `pnpm build` -- builds without error.
    Navigate to /cart in browser:
    - Empty cart shows CartEmptyState
    - Cart with items shows two-column layout on desktop
    - Items grouped by category
    - Order summary shows on right side (desktop) or below items (mobile)
  </verify>
  <done>Cart page is fully implemented with two-column layout, category-grouped items, validation overlays, attention section, order summary with minimum order enforcement, and checkout gate. Replaces the stub page.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. `pnpm build` succeeds
4. Cart page at /cart shows full UI (not a stub)
5. Two-column layout on desktop, single column on mobile
6. Items grouped by category with headers
7. Title shows "Your Cart (N items)"
8. Checkout button disabled when blocking issues exist
9. Warning banner scrolls to attention section when tapped
10. Empty cart shows CartEmptyState
11. Price-changed items show PriceChangeBadge
12. Sold-out/unavailable items show in AttentionSection with suggestions
</verification>

<success_criteria>

- Cart page fully replaces stub with all locked decision UI requirements
- Category grouping works correctly using menu data
- Checkout gate enforces minimum order + blocking issues
- Validation results drive UI overlays and attention section
- Item replacement carries over original quantity
- Price dismissal updates persisted cart price
  </success_criteria>

<output>
After completion, create `.planning/phases/52-cart-validation/52-03-SUMMARY.md`
</output>
