---
phase: 52-cart-validation
plan: 04
type: execute
wave: 2
depends_on: ["52-01", "52-02"]
files_modified:
  - src/components/ui/cart/CartDrawer.tsx
  - src/components/ui/cart/CartItem/CartItem.tsx
  - src/components/ui/cart/CartItem/index.tsx
  - src/components/ui/cart/index.ts
autonomous: true

must_haves:
  truths:
    - "Cart drawer runs validation on mount and shows full stale UX (gray overlay, badges, remove buttons)"
    - "Cart drawer items show validation overlays matching cart page treatment"
    - "Cart drawer shows subtle loading indicator during validation (not full skeleton replacement)"
    - "CartItem component accepts validation props for overlay rendering"
  artifacts:
    - path: "src/components/ui/cart/CartDrawer.tsx"
      provides: "Cart drawer with validation integration on mount"
      contains: "useCartValidation"
    - path: "src/components/ui/cart/CartItem/CartItem.tsx"
      provides: "CartItem with optional validation overlay and price badge props"
      contains: "validationStatus"
    - path: "src/components/ui/cart/index.ts"
      provides: "Updated barrel with CartPage component exports"
      contains: "CartPageContent"
  key_links:
    - from: "src/components/ui/cart/CartDrawer.tsx"
      to: "src/lib/hooks/useCartValidation.ts"
      via: "useCartValidation() on drawer mount"
      pattern: "useCartValidation"
    - from: "src/components/ui/cart/CartItem/CartItem.tsx"
      to: "src/components/ui/cart/CartItem/ValidationOverlay.tsx"
      via: "Conditional overlay rendering based on validationStatus prop"
      pattern: "ValidationOverlay"
---

<objective>
Integrate cart validation into the existing CartDrawer and CartItem components so the drawer shows full stale item UX on mount.

Purpose: Cart drawer is the primary cart interaction point. Users must see validation status here too, not just on the full cart page.
Output: CartDrawer runs validation on open, CartItem renders overlays based on validation props.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cart-validation/52-RESEARCH.md
@.planning/phases/52-cart-validation/52-01-SUMMARY.md
@.planning/phases/52-cart-validation/52-02-SUMMARY.md

@src/components/ui/cart/CartDrawer.tsx
@src/components/ui/cart/CartItem/CartItem.tsx
@src/components/ui/cart/CartItem/index.tsx
@src/components/ui/cart/index.ts
@src/lib/hooks/useCartValidation.ts
@src/types/cart.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CartItem with validation props and overlay rendering</name>
  <files>src/components/ui/cart/CartItem/CartItem.tsx, src/components/ui/cart/CartItem/index.tsx</files>
  <action>
    **CartItem.tsx modifications:**
    1. Add optional validation props to `CartItemProps`:
    ```typescript
    export interface CartItemProps {
      item: CartItemType;
      onEdit?: (item: CartItemType) => void;
      compact?: boolean;
      className?: string;
      // Validation props (optional -- only provided when validation is active)
      validationStatus?: CartItemValidationStatus;
      priceDirection?: 'up' | 'down';
      newPriceCents?: number;
      onDismissPriceChange?: () => void;
      onRemoveStale?: () => void;
    }
    ```
    2. Import `ValidationOverlay` and `PriceChangeBadge` from sibling files
    3. Import `CartItemValidationStatus` from `@/types/cart`
    4. Inside the component render:
       - If `validationStatus === 'sold-out' || validationStatus === 'unavailable'`: render `<ValidationOverlay status={validationStatus} onRemove={onRemoveStale!} />` as an absolute overlay inside the card container
       - If `validationStatus === 'price-changed'`: render `<PriceChangeBadge direction={priceDirection!} onDismiss={onDismissPriceChange!} />` below the price section
       - If no validationStatus or `validationStatus === 'valid'`: render normally (existing behavior)
    5. When sold-out/unavailable: disable drag-to-delete (already visually indicated), disable quantity stepper, gray out the content
    6. Ensure the `relative` positioning on the outer container supports absolute overlay positioning

    **CartItem/index.tsx:**
    - Update barrel to also export `ValidationOverlay` and `PriceChangeBadge` (for direct import if needed)
  </action>
  <verify>Run `pnpm typecheck` and `pnpm lint` -- no errors. CartItem renders normally without validation props (backward compatible). CartItem shows overlay when validationStatus is 'sold-out'.</verify>
  <done>CartItem accepts optional validation props and conditionally renders ValidationOverlay or PriceChangeBadge. Backward compatible -- existing usage without validation props unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate validation into CartDrawer and update barrel exports</name>
  <files>src/components/ui/cart/CartDrawer.tsx, src/components/ui/cart/index.ts</files>
  <action>
    **CartDrawer.tsx modifications:**
    1. Import `useCartValidation` from `@/lib/hooks/useCartValidation`
    2. Import `useCartStore` for `updateItemPrice` action
    3. In `CartContent` component (the shared content between mobile/desktop):
       - Call `useCartValidation()` to get validation results
       - Pass validation data down to `CartItemsList`
    4. In `CartItemsList` component:
       - Accept validation props: `validations`, `suggestions`, cart actions
       - When rendering each `CartItem`, look up its validation status from the validations map
       - Pass `validationStatus`, `priceDirection`, `newPriceCents`, `onDismissPriceChange`, `onRemoveStale` props to each `CartItem`
       - For sold-out/unavailable items with suggestions: render a compact `SuggestionRow` below the CartItem (import from CartPage)
    5. During validation (status === 'validating'):
       - Show items immediately (not replaced with skeletons per research recommendation for drawer)
       - Add a subtle top-bar loading indicator: a thin animated line at the top of the items list (e.g., `animate-pulse` bar in primary color, 2px height)
       - Remove the loading indicator when validation completes
    6. Add `handleDismissPriceChange` callback: calls `updateItemPrice(cartItemId, newPriceCents)`
    7. Add `handleReplaceItem` callback: removes old item, adds new from suggestion with original quantity carried over
    8. In `CartFooter`: pass `hasBlockingIssues` from validation to disable checkout button when stale items exist
       - When disabled: change "Proceed to Checkout" to show muted style, add a small warning text below it

    **index.ts (cart barrel):**
    - Add exports for CartPage components:
    ```typescript
    export { CartPageContent } from "./CartPage";
    export { AttentionSection } from "./CartPage";
    export { SuggestionRow } from "./CartPage";
    export { CartPageHeader } from "./CartPage";
    export { CartItemGroup } from "./CartPage";
    export { CartPageSummary } from "./CartPage";
    export { CheckoutGate } from "./CartPage";
    ```
    - Add type exports for validation overlay components:
    ```typescript
    export { ValidationOverlay } from "./CartItem/ValidationOverlay";
    export { PriceChangeBadge } from "./CartItem/PriceChangeBadge";
    ```
  </action>
  <verify>
    Run `pnpm typecheck` and `pnpm lint` -- no errors.
    Run `pnpm build` -- builds without error.
    Test: Open cart drawer with items -> see subtle loading indicator -> validation completes -> sold-out items show overlay.
    Test: Cart drawer with all valid items -> no overlays visible, checkout button active.
  </verify>
  <done>Cart drawer runs validation on mount, shows overlays on stale items, disables checkout when blocking issues exist. All CartPage and validation overlay components exported from cart barrel.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. `pnpm build` succeeds
4. CartItem backward compatible (no validation props = normal render)
5. Cart drawer shows validation overlays on stale items
6. Cart drawer shows subtle loading during validation (not skeleton replacement)
7. Checkout button in drawer disabled when sold-out/unavailable items present
8. Price change badges appear and are dismissable in drawer
9. Suggestion rows appear below stale items in drawer
10. Cart barrel exports all new components
</verification>

<success_criteria>
- Cart drawer has full validation UX matching cart page treatment
- CartItem is backward compatible with optional validation props
- Checkout gate in drawer enforces blocking issues
- No regressions to existing cart drawer behavior
- All new components accessible via barrel imports
</success_criteria>

<output>
After completion, create `.planning/phases/52-cart-validation/52-04-SUMMARY.md`
</output>
