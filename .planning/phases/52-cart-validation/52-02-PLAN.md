---
phase: 52-cart-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/cart/CartItem/ValidationOverlay.tsx
  - src/components/ui/cart/CartItem/PriceChangeBadge.tsx
  - src/components/ui/cart/CartPage/SuggestionRow.tsx
  - src/components/ui/cart/CartPage/AttentionSection.tsx
  - src/components/ui/cart/CartPage/index.tsx
autonomous: true

must_haves:
  truths:
    - "Sold-out items display gray overlay with amber 'Sold Out' badge"
    - "Unavailable items display gray overlay with red 'Unavailable' badge"
    - "Price-changed items show dismissable amber (up) or green (down) 'Price updated' badge"
    - "Suggestions show 3 replacement items with thumbnail, name, and price"
    - "Attention section groups problem items with count header"
  artifacts:
    - path: "src/components/ui/cart/CartItem/ValidationOverlay.tsx"
      provides: "Gray overlay + status badge for sold-out and unavailable items"
      exports: ["ValidationOverlay"]
    - path: "src/components/ui/cart/CartItem/PriceChangeBadge.tsx"
      provides: "Dismissable price updated badge (amber for increase, green for decrease)"
      exports: ["PriceChangeBadge"]
    - path: "src/components/ui/cart/CartPage/SuggestionRow.tsx"
      provides: "Inline suggestion row with 3 replacement items"
      exports: ["SuggestionRow"]
    - path: "src/components/ui/cart/CartPage/AttentionSection.tsx"
      provides: "Items needing attention section with animated collapse"
      exports: ["AttentionSection"]
  key_links:
    - from: "src/components/ui/cart/CartItem/ValidationOverlay.tsx"
      to: "src/types/cart.ts"
      via: "CartItemValidationStatus type for badge variant"
      pattern: "CartItemValidationStatus"
    - from: "src/components/ui/cart/CartPage/SuggestionRow.tsx"
      to: "src/types/menu.ts"
      via: "MenuItem type for suggestion items"
      pattern: "MenuItem"
---

<objective>
Create the validation overlay UI components: gray overlays with badges for stale items, dismissable price change badges, inline suggestion rows, and the attention section container.

Purpose: Visual building blocks that communicate cart validation state to users. These are composed into the cart page and cart drawer in later plans.
Output: 4 reusable components for cart validation UX + CartPage barrel file.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cart-validation/52-RESEARCH.md

@src/components/ui/cart/CartItem/CartItem.tsx
@src/components/ui/cart/CartItem/index.tsx
@src/types/cart.ts
@src/types/menu.ts
@src/lib/motion-tokens/core.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ValidationOverlay and PriceChangeBadge components</name>
  <files>src/components/ui/cart/CartItem/ValidationOverlay.tsx, src/components/ui/cart/CartItem/PriceChangeBadge.tsx</files>
  <action>
    Use `/frontend-design` skill for thematic, polished, premium, playful UI.

    **ValidationOverlay.tsx:**
    - `'use client'` directive
    - Props: `status: 'sold-out' | 'unavailable'`, `onRemove: () => void`
    - Renders an absolute-positioned overlay on the cart item card
    - Gray overlay: `bg-black/40 dark:bg-black/60` with `backdrop-blur-[1px]` -- subtle but clear
    - Badge positioned top-right of the overlay
    - Sold-out badge: amber background (`bg-amber-500`), white text, "Sold Out" text, `AlertTriangle` icon from lucide
    - Unavailable badge: red background (`bg-red-500`), white text, "Unavailable" text, `XCircle` icon from lucide
    - Remove button at bottom of overlay: red outline button with `Trash2` icon, "Remove" text
    - Entrance animation: `m.div` with opacity 0->1, slight scale 0.95->1 (finite, not infinite)
    - Badge uses `m.div` with spring entrance (scale 0->1 with rubbery spring)
    - Keep overlay simple -- CSS opacity + absolute positioning per research anti-pattern guidance
    - No infinite animations (per existing learnings for mobile safety)

    **PriceChangeBadge.tsx:**
    - `'use client'` directive
    - Props: `direction: 'up' | 'down'`, `onDismiss: () => void`
    - Small inline badge (not an overlay -- price items stay in place per decision)
    - Direction `up`: amber background (`bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-300`)
    - Direction `down`: green background (`bg-green-100 text-green-700 dark:bg-green-900/40 dark:text-green-300`)
    - Text: "Price updated" (no difference amount per decision)
    - `TrendingUp` or `TrendingDown` icon from lucide based on direction
    - Dismissable: small `X` button at right edge. Tap triggers `onDismiss`
    - Entrance animation: slide in from right with spring
    - Exit animation via AnimatePresence: slide out to right, opacity fade
    - Cursor pointer on the badge itself (whole badge is tappable to dismiss per decision)
  </action>
  <verify>Run `pnpm typecheck` and `pnpm lint` -- no errors. Components render correct badge colors for each status.</verify>
  <done>ValidationOverlay shows gray overlay with amber (sold-out) or red (unavailable) badge + remove button. PriceChangeBadge shows dismissable amber (up) or green (down) "Price updated" inline badge.</done>
</task>

<task type="auto">
  <name>Task 2: Create SuggestionRow and AttentionSection components</name>
  <files>src/components/ui/cart/CartPage/SuggestionRow.tsx, src/components/ui/cart/CartPage/AttentionSection.tsx, src/components/ui/cart/CartPage/index.tsx</files>
  <action>
    Use `/frontend-design` skill for thematic, polished, premium, playful UI.

    **SuggestionRow.tsx:**
    - `'use client'` directive
    - Props: `suggestions: MenuItem[]`, `onReplace: (suggestion: MenuItem) => void`, `className?: string`
    - Horizontal scrollable row of up to 3 suggestion cards
    - Each suggestion card: small thumbnail (48x48 rounded), item name (truncated), price formatted with `formatPrice`
    - Card style: glass effect (`glass-menu-card`), rounded-xl, subtle border, hover lift
    - Tapping a card calls `onReplace(suggestion)` -- auto-replaces instantly per decision (no confirmation)
    - Label above row: "Try instead:" in small text-muted
    - Each card has `m.div` with `whileHover={{ scale: 1.03 }}` and `whileTap={{ scale: 0.97 }}`
    - Use `shouldAnimate` guard from `useAnimationPreference`
    - Import `formatPrice` from `@/lib/utils/format`

    **AttentionSection.tsx:**
    - `'use client'` directive
    - Props:
      ```typescript
      interface AttentionSectionProps {
        items: CartItem[];
        validations: Map<string, CartItemValidation>;
        suggestions: Map<string, MenuItem[]>;
        onRemoveItem: (cartItemId: string) => void;
        onReplaceItem: (cartItemId: string, suggestion: MenuItem, originalQuantity: number) => void;
        className?: string;
      }
      ```
    - Renders a section with header: "{N} items need attention" (direct/urgent tone per decision)
    - Uses `AlertTriangle` icon in header, amber color scheme
    - Section wrapped in `AnimatePresence` -- animates collapse when last issue resolved (per decision)
    - `m.div` with `exit={{ opacity: 0, height: 0, marginBottom: 0 }}` for smooth collapse
    - Inside: renders each problem cart item using existing `CartItem` component with `ValidationOverlay` layered on top
    - Below each item with suggestions: renders `SuggestionRow`
    - `ref` prop (forwarded) for scroll-to-attention feature
    - Background: subtle red-tinted glass card (`bg-red-50/50 dark:bg-red-950/20`) with dashed border
    - Always expanded (per discretion recommendation from research)

    **CartPage/index.tsx:**
    - Create barrel file exporting: `AttentionSection`, `SuggestionRow`
    - This file will grow as Plan 03 adds more components
  </action>
  <verify>Run `pnpm typecheck` and `pnpm lint` -- no errors. AttentionSection renders with correct header count. SuggestionRow displays up to 3 items with thumbnails.</verify>
  <done>SuggestionRow shows 3 replacement cards with thumbnails. AttentionSection groups problem items with urgent header and animates collapse when empty. CartPage barrel exports both.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. ValidationOverlay uses amber for sold-out, red for unavailable (per locked decision)
4. PriceChangeBadge uses amber for price-up, green for price-down (per locked decision)
5. PriceChangeBadge is dismissable via tap (per locked decision)
6. SuggestionRow shows up to 3 items with thumbnail + name + price
7. AttentionSection header uses direct tone: "{N} items need attention"
8. No infinite animations in any component
</verification>

<success_criteria>
- All 4 components render with correct visual treatment matching locked decisions
- Components use design system tokens (no hardcoded colors, spacing)
- Animations are finite (no infinite loops per mobile safety learnings)
- Components accept proper props for composition in cart page/drawer
</success_criteria>

<output>
After completion, create `.planning/phases/52-cart-validation/52-02-SUMMARY.md`
</output>
