---
phase: 75-fix-security-navigation-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [SEC-02, DPROF-05]

must_haves:
  truths:
    - "New drivers can tap 'Complete your first delivery' milestone in walkthrough card and navigate to /driver/test-delivery"
    - "CSP header is Content-Security-Policy (enforcing), not Report-Only"
    - "REQUIREMENTS.md shows SEC-02 and DPROF-05 as complete"
  artifacts:
    - path: "src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx"
      provides: "Walkthrough milestone 3 href wired to /driver/test-delivery"
      contains: 'href: "/driver/test-delivery"'
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement tracking with SEC-02 and DPROF-05 marked complete"
      contains: "[x] **SEC-02**"
  key_links:
    - from: "src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx"
      to: "src/app/(driver)/driver/test-delivery/page.tsx"
      via: "router.push from handleMilestoneClick when milestone.href is non-null"
      pattern: 'href: "/driver/test-delivery"'
---

<objective>
Wire the test-delivery navigation in OnboardingWalkthroughCard and update requirement tracking for both SEC-02 and DPROF-05.

Purpose: Close the two remaining gaps from v1.8 milestone audit — walkthrough milestone 3 is unreachable (href: null), and SEC-02/DPROF-05 are not marked complete in REQUIREMENTS.md.
Output: Walkthrough milestone 3 navigates to /driver/test-delivery; REQUIREMENTS.md reflects 37/37 complete for Phase 75 scope.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/75-fix-security-navigation-wiring/75-RESEARCH.md

<interfaces>
<!-- From OnboardingWalkthroughCard.tsx — the milestone array and click handler -->

```typescript
interface Milestone {
  key: string;
  label: string;
  href: string | null;
}

const milestones: Milestone[] = [
  { key: "profile", label: "Complete your profile", href: "/driver/profile" },
  { key: "route", label: "View today's route", href: "/driver/route" },
  { key: "delivery", label: "Complete your first delivery", href: null }, // <-- FIX THIS
];

const handleMilestoneClick = (milestone: Milestone, isComplete: boolean) => {
  if (isComplete || !milestone.href) return;
  router.push(milestone.href);
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire test-delivery href and update requirement tracking</name>
  <files>
    src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **OnboardingWalkthroughCard.tsx (line 42):**
    Change milestone 3 href from `null` to `"/driver/test-delivery"`:
    ```
    // BEFORE
    { key: "delivery", label: "Complete your first delivery", href: null },
    // AFTER
    { key: "delivery", label: "Complete your first delivery", href: "/driver/test-delivery" },
    ```
    Do NOT change the milestone label — it should remain "Complete your first delivery" (see Research pitfall 1). The test-delivery page is a stepping stone; the milestone auto-completes when deliveriesCount > 0 from a real delivery.

    **REQUIREMENTS.md:**
    1. Mark SEC-02 complete: Change `- [ ] **SEC-02**:` to `- [x] **SEC-02**:`
       - Reason: CSP is already enforcing (commit c5f9d2d). `unsafe-eval` is intentionally unconditional per Google Maps official CSP requirements (commit 06eff7ce). The gap was a documentation contradiction, not a code issue.
    2. Mark DPROF-05 complete: Change `- [ ] **DPROF-05**:` to `- [x] **DPROF-05**:`
       - Reason: Test delivery page exists at /driver/test-delivery (commit 5a519c7d). With href now wired, the page is reachable from the walkthrough.
    3. Update the Traceability table: Change SEC-02 and DPROF-05 status from `Pending` to `Complete`
    4. Update coverage line: Change `34/37 complete (92%)` to `36/37 complete (97%)` (DDASH-07 remains for Phase 76)
  </action>
  <verify>
    <automated>cd C:/Users/minkk/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
    - OnboardingWalkthroughCard milestone 3 has href "/driver/test-delivery" (not null)
    - REQUIREMENTS.md shows SEC-02 and DPROF-05 as [x] complete
    - Traceability table shows both as Complete
    - Coverage reads 36/37 (97%)
    - Build passes with no type or lint errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` — no type errors (href type is `string | null`, accepts string)
2. `pnpm lint` — no lint errors
3. `pnpm build` — production build succeeds
4. Grep check: `grep -n 'test-delivery' src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx` returns the wired href
5. Grep check: `grep -c '\[x\] \*\*SEC-02\*\*' .planning/REQUIREMENTS.md` returns 1
6. Grep check: `grep -c '\[x\] \*\*DPROF-05\*\*' .planning/REQUIREMENTS.md` returns 1
</verification>

<success_criteria>
- Walkthrough milestone 3 navigates to /driver/test-delivery when tapped by a new driver
- REQUIREMENTS.md accurately reflects SEC-02 and DPROF-05 as complete with 36/37 coverage
- All verification commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/75-fix-security-navigation-wiring/75-01-SUMMARY.md`
</output>
