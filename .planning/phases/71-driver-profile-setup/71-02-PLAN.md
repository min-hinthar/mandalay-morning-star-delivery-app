---
phase: 71-driver-profile-setup
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - src/components/ui/driver/DriverDashboard/ProfileCompletenessCard.tsx
  - src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
  - src/components/ui/driver/DriverDashboard/types.ts
  - src/components/ui/driver/DriverDashboard/index.tsx
  - src/components/ui/driver/DriverNav.tsx
  - src/components/ui/driver/DriverHeader.tsx
  - src/components/ui/driver/index.ts
  - src/app/(driver)/driver/page.tsx
  - src/app/(driver)/driver/layout.tsx
  - src/app/api/tracking/[orderId]/route.ts
autonomous: true
requirements:
  - DPROF-03

must_haves:
  truths:
    - "Dashboard shows profile completeness checklist when profile is incomplete"
    - "Checklist items link to profile page with field highlighting"
    - "Completeness card hides permanently when all 5 items complete (with celebration animation)"
    - "Driver avatar appears in header and bottom nav"
    - "Dashboard greeting says 'Hello, [Name]!' (not time-of-day based)"
    - "Customer tracking view shows driver photo, name, phone, vehicle type, license plate"
  artifacts:
    - path: "src/components/ui/driver/DriverDashboard/ProfileCompletenessCard.tsx"
      provides: "Animated checklist with deep links and celebration"
      min_lines: 80
    - path: "src/components/ui/driver/DriverDashboard/DriverDashboard.tsx"
      provides: "Updated dashboard with completeness card and static greeting"
    - path: "src/components/ui/driver/DriverNav.tsx"
      provides: "Bottom nav with profile avatar"
    - path: "src/components/ui/driver/DriverHeader.tsx"
      provides: "Header with avatar and dropdown menu"
  key_links:
    - from: "src/components/ui/driver/DriverDashboard/ProfileCompletenessCard.tsx"
      to: "/driver/profile?highlight=fieldName"
      via: "router.push with query param"
      pattern: "router.push.*driver/profile.*highlight"
    - from: "src/app/(driver)/driver/page.tsx"
      to: "src/components/ui/driver/DriverDashboard/DriverDashboard.tsx"
      via: "props including licensePlate and profileImageUrl"
      pattern: "licensePlate|profileImageUrl"
---

<objective>
Add profile completeness indicator to driver dashboard, integrate avatar display into header/nav, update dashboard greeting to static "Hello, [Name]!", and expose driver identity fields in the customer tracking view.

Purpose: Completes profile setup experience with progress tracking and cross-view avatar integration (DPROF-03).
Output: ProfileCompletenessCard component, updated DriverDashboard/DriverNav/DriverHeader, tracking API updates.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-driver-profile-setup/71-RESEARCH.md
@.planning/phases/71-driver-profile-setup/71-01-SUMMARY.md

@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/types.ts
@src/components/ui/driver/DriverDashboard/index.tsx
@src/components/ui/driver/DriverNav.tsx
@src/components/ui/driver/DriverHeader.tsx
@src/components/ui/driver/InitialsAvatar.tsx
@src/app/(driver)/driver/page.tsx
@src/app/(driver)/driver/layout.tsx
@src/app/api/tracking/[orderId]/route.ts
@src/types/driver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile completeness card with celebration animation</name>
  <files>
    src/components/ui/driver/DriverDashboard/ProfileCompletenessCard.tsx
    src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
    src/components/ui/driver/DriverDashboard/types.ts
    src/components/ui/driver/DriverDashboard/index.tsx
    src/app/(driver)/driver/page.tsx
  </files>
  <action>
    **1. Update `src/components/ui/driver/DriverDashboard/types.ts`:**
    - Add `licensePlate: string | null` to the `driver` object in `DriverDashboardProps`
    - Add `createdAt?: string` to the `driver` object (for future use)
    - Keep the existing `getGreeting()` function — will be replaced in DriverDashboard component usage

    **2. Create `src/components/ui/driver/DriverDashboard/ProfileCompletenessCard.tsx`:**
    - 'use client' component
    - Props: `driver: { fullName, phone, vehicleType, licensePlate, profileImageUrl }` (subset of dashboard driver props)
    - **Completeness calculation:**
      ```typescript
      const items = [
        { key: 'name', label: 'Add your name', complete: !!driver.fullName, field: 'fullName' },
        { key: 'phone', label: 'Add phone number', complete: !!driver.phone, field: 'phone' },
        { key: 'vehicleType', label: 'Set vehicle type', complete: !!driver.vehicleType, field: 'vehicleType' },
        { key: 'licensePlate', label: 'Add license plate', complete: !!driver.licensePlate, field: 'licensePlate' },
        { key: 'photo', label: 'Upload a photo', complete: !!driver.profileImageUrl, field: 'photo' },
      ];
      const completedCount = items.filter(i => i.complete).length;
      const isComplete = completedCount === 5;
      ```
    - **Hidden state:** Use localStorage key `profile-complete-dismissed-{driverId}` (or just `profile-complete-dismissed`). If `isComplete` AND localStorage flag set → don't render. If `isComplete` AND flag NOT set → show celebration then set flag.
    - **Celebration animation:** When all 5 complete for the first time:
      - Animated checkmark bounce (Framer Motion scale spring from 0 to 1.2 to 1)
      - Confetti-style particles (use Framer Motion AnimatePresence with multiple small colored circles that fly outward and fade)
      - Show for 3 seconds, then `AnimatePresence` exit animation (scale down, fade out)
      - After exit: set localStorage flag
    - **Checklist rendering (when not complete):**
      - Card title: "Complete your profile" with `{completedCount}/5` counter
      - Each item: clickable row with:
        - Green checkmark icon (Check from lucide) + strikethrough text for completed items
        - Empty circle icon + normal text for incomplete items
        - On click: `router.push('/driver/profile?highlight=' + item.field)`
      - Card border color: interpolate from `border-amber-400` (0/5) through `border-yellow-400` (2-3/5) to `border-green-500` (4-5/5) based on completedCount
      - Glassmorphism card styling: `bg-surface-primary/80 backdrop-blur-sm border rounded-2xl shadow-card`
      - Framer Motion stagger animation on checklist items
    - **Progress bar:** Thin horizontal bar below title showing completedCount/5 fill
    - Export from component

    **3. Update `src/components/ui/driver/DriverDashboard/DriverDashboard.tsx`:**
    - Import `ProfileCompletenessCard`
    - Add completeness card as FIRST item after greeting section (before streak, before stats)
    - Change greeting from `{greeting}, {firstName}!` to `Hello, {firstName}!` (per CONTEXT: static, not time-of-day aware). Remove the `getGreeting()` call.
    - Pass driver data subset to ProfileCompletenessCard

    **4. Update `src/components/ui/driver/DriverDashboard/index.tsx`:**
    - Add re-export for ProfileCompletenessCard

    **5. Update `src/app/(driver)/driver/page.tsx`:**
    - Add `license_plate` to the driver SELECT query
    - Add `licensePlate: driver.license_plate` to the data object passed to DriverDashboard
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm build` succeeds
    - ProfileCompletenessCard renders on dashboard when profile is incomplete
    - Clicking a checklist item navigates to /driver/profile?highlight=fieldName
    - `pnpm lint` passes
  </verify>
  <done>
    - Dashboard shows "Complete your profile" card at top when profile has missing fields
    - Checklist shows 5 items with green checkmarks for completed and circles for incomplete
    - Clicking incomplete item navigates to profile page with field highlight param
    - Card border shifts from amber to green as completeness increases
    - Celebration animation plays when all items complete, then card auto-hides
    - Card stays hidden permanently after completion (localStorage)
    - Greeting changed to static "Hello, [Name]!"
  </done>
</task>

<task type="auto">
  <name>Task 2: Avatar in header/nav + driver identity in tracking view</name>
  <files>
    src/components/ui/driver/DriverNav.tsx
    src/components/ui/driver/DriverHeader.tsx
    src/components/ui/driver/index.ts
    src/app/(driver)/driver/layout.tsx
    src/app/api/tracking/[orderId]/route.ts
  </files>
  <action>
    **1. Update `src/components/ui/driver/DriverHeader.tsx`:**
    - Add props: `avatarUrl?: string | null`, `driverName?: string | null`
    - When `avatarUrl` or `driverName` provided: show small avatar (24px) in the right side of header
      - If avatarUrl: circular `<img>` with object-cover
      - If no avatarUrl but driverName: use InitialsAvatar component (size='sm')
    - Avatar tap: show dropdown menu (use a simple popover/sheet)
      - Menu items: "Profile" (links to /driver/profile), "Settings" (placeholder, disabled), "Sign Out" (calls supabase.auth.signOut)
    - Keep existing `rightContent` prop working — avatar renders alongside it if both provided

    **2. Update `src/components/ui/driver/DriverNav.tsx`:**
    - Add optional prop: `avatarUrl?: string | null`, `driverName?: string | null`
    - Replace the "Home" tab icon with a small avatar when avatarUrl or driverName is available:
      - If avatarUrl: 24px circular image
      - If driverName only: InitialsAvatar (size='sm')
      - If neither: keep the Home icon as-is
    - The label remains "Home"

    **3. Update `src/app/(driver)/driver/layout.tsx`:**
    - The layout already queries driver data (vehicle_type, rating_avg, deliveries_count)
    - Add `profile_image_url` to the SELECT query on drivers table
    - Add `full_name` to a profiles table query (or join)
    - Pass `avatarUrl={driver.profile_image_url}` and `driverName={profile?.full_name}` to both `<DriverNav>` and contextually to pages that use DriverHeader
    - Since DriverHeader is used inside pages (not layout), the avatar data needs to flow through. Options:
      - Create a minimal React context `DriverProfileContext` providing `{ avatarUrl, driverName }` in the layout
      - Pages that render DriverHeader can consume the context
      - OR: just pass as props through layout → children isn't possible in Next.js layouts
      - **Best approach:** Create a `DriverAvatarContext` provider in layout, consumed by DriverHeader instances

    **4. Update `src/components/ui/driver/index.ts`:**
    - Add re-exports for InitialsAvatar, AvatarUpload (from Plan 01)
    - Add re-export for any new context if created

    **5. Update `src/app/api/tracking/[orderId]/route.ts`:**
    - The tracking API already returns driver info via `TrackingDriverInfo` type
    - Verify that the API returns: `fullName`, `profileImageUrl`, `phone`, `vehicleType`
    - Add `licensePlate` to the response if not already present:
      - Add `license_plate` to the driver SELECT query
      - Add `licensePlate: driver.license_plate` to the response
    - The `TrackingDriverInfo` type in `src/types/driver.ts` already has `vehicleType?` — ensure it's populated
    - Add `licensePlate?: string | null` to `TrackingDriverInfo` type if not present
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm build` succeeds
    - Avatar appears in bottom nav and header on driver pages
    - Header avatar dropdown shows Profile/Sign Out options
    - Tracking API returns driver identity fields
    - `pnpm lint` passes
  </verify>
  <done>
    - Driver avatar (photo or initials) visible in bottom nav Home tab
    - Driver avatar visible in header with dropdown menu (Profile, Sign Out)
    - Tracking API returns driver name, photo, phone, vehicleType, licensePlate
    - InitialsAvatar and AvatarUpload re-exported from driver component barrel
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm build` all pass
- Profile completeness card shows on dashboard with correct item states
- Clicking incomplete item deep links to profile page with highlight
- Celebration animation plays on 100% completion
- Card permanently hides after completion celebration
- Avatar shows in header and nav
- Dashboard greeting is "Hello, [Name]!" (not time-based)
- Tracking API includes all driver identity fields
</verification>

<success_criteria>
- Completeness card visible on dashboard when profile is incomplete
- 5 checklist items with correct labels, green checkmarks for done, deep links for incomplete
- Color progression from amber to green on card border
- Celebration animation at 100%, 3-second display, permanent hide
- Avatar in header with dropdown menu
- Avatar in bottom nav Home tab
- Tracking API returns full driver identity for customer display
</success_criteria>

<output>
After completion, create `.planning/phases/71-driver-profile-setup/71-02-SUMMARY.md`
</output>
