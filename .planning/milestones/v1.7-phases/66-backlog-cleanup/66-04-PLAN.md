---
phase: 66-backlog-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["66-02"]
files_modified:
  - src/components/ui/orders/tracking/DriverCard.tsx
  - src/components/ui/orders/tracking/OrderSummary.tsx
  - src/components/ui/orders/tracking/DeliveryNotesEditor.tsx
  - src/components/ui/orders/tracking/ETADisplay.tsx
  - src/components/ui/orders/tracking/ETACountdown.tsx
  - src/app/api/orders/[id]/notes/route.ts
  - src/components/ui/orders/tracking/index.ts
autonomous: true

must_haves:
  truths:
    - "Driver name and profile photo displayed once driver is assigned/en route"
    - "Call driver button initiates phone call on tap"
    - "Full item list visible in info section (not collapsed)"
    - "Delivery instructions visible and editable on tracking page before delivery"
    - "ETA shows countdown format (<30 min) or time window (>30 min)"
    - "Vertical timeline below stepper shows timestamps and detailed history"
  artifacts:
    - path: "src/components/ui/orders/tracking/DriverCard.tsx"
      provides: "Enhanced driver card with photo, name, call button"
      contains: "Call Driver"
    - path: "src/components/ui/orders/tracking/DeliveryNotesEditor.tsx"
      provides: "Editable delivery instructions with save"
      contains: "DeliveryNotesEditor"
    - path: "src/app/api/orders/[id]/notes/route.ts"
      provides: "PATCH endpoint for updating delivery instructions"
      exports: ["PATCH"]
  key_links:
    - from: "src/components/ui/orders/tracking/DriverCard.tsx"
      to: "tel: link"
      via: "anchor tag with tel: protocol"
      pattern: "href.*tel:"
    - from: "src/components/ui/orders/tracking/DeliveryNotesEditor.tsx"
      to: "src/app/api/orders/[id]/notes/route.ts"
      via: "PATCH fetch call to update notes"
      pattern: "fetch.*orders.*notes"
---

<objective>
Enhance tracking info section: driver card with photo/call, full item list, editable delivery notes, and ETA format upgrade.

Purpose: BKLG-03 info section -- driver info, order details, and delivery notes create a complete tracking experience
Output: Enhanced DriverCard, DeliveryNotesEditor, expanded OrderSummary, improved ETA display
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-backlog-cleanup/66-02-SUMMARY.md
@src/components/ui/orders/tracking/DriverCard.tsx
@src/components/ui/orders/tracking/OrderSummary.tsx
@src/components/ui/orders/tracking/ETACountdown.tsx
@src/components/ui/orders/tracking/ETADisplay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhanced DriverCard with photo, call button; expanded OrderSummary</name>
  <files>
    src/components/ui/orders/tracking/DriverCard.tsx
    src/components/ui/orders/tracking/OrderSummary.tsx
  </files>
  <action>
**1. Enhance DriverCard (`DriverCard.tsx`):**
- Add driver profile photo: use Next.js `Image` component with `fill` prop inside a circular container
  - Fallback: initials avatar (first letter of name) with jade background when no photo
  - Size: 48x48px circle
- Add driver name display: bold text below/beside photo
- Add "Call Driver" button:
  - `<a href={`tel:${driver.phone}`}>` wrapped in a styled button
  - Show phone icon (lucide `Phone`) + "Call Driver" text
  - Only render when `driver.phone` is not null
  - Haptic feedback on tap: use `navigator.vibrate?.(50)` if available
- Add vehicle type display: small badge showing "Car" / "Motorcycle" / "Bicycle" with matching icon
- Layout: horizontal card with photo left, name+vehicle center, call button right
- Keep existing stop progress display (X of Y stops)
- Add `m.div` entrance animation: `initial={{ opacity: 0, y: 10 }}` `animate={{ opacity: 1, y: 0 }}`

**2. Expand OrderSummary (`OrderSummary.tsx`):**

- Change from collapsed/expandable to fully visible by default (per locked decision: "full item list visible, not collapsed")
- Remove any accordion/collapsible behavior on the items list
- Each item row: quantity x name, modifiers listed below in smaller text
- Keep price summary (subtotal, delivery fee, tax, total) at bottom
- Keep delivery window display
- Add delivery address display: line1, city, state (from the existing `deliveryAddress` prop or add if missing)
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
  </verify>
  <done>
- DriverCard shows profile photo (or initials fallback), name, vehicle type
- Call Driver button with `tel:` link and haptic feedback
- OrderSummary shows full item list by default (not collapsed)
- Items display with quantity, name, modifiers
  </done>
  </task>

<task type="auto">
  <name>Task 2: DeliveryNotesEditor and ETA format upgrade</name>
  <files>
    src/components/ui/orders/tracking/DeliveryNotesEditor.tsx
    src/components/ui/orders/tracking/ETADisplay.tsx
    src/components/ui/orders/tracking/ETACountdown.tsx
    src/app/api/orders/[id]/notes/route.ts
    src/components/ui/orders/tracking/index.ts
  </files>
  <action>
**1. Create DeliveryNotesEditor.tsx:**
- Props: `orderId: string`, `initialNotes: string | null`, `isEditable: boolean` (false when delivered/cancelled)
- Displays current delivery instructions text
- "Edit" pencil icon button to toggle edit mode
- In edit mode: Textarea (max 500 chars) with "Save" and "Cancel" buttons
- On save: PATCH `/api/orders/${orderId}/notes` with `{ notes: string }`
  - Show loading spinner on Save button during request
  - Show toast "Delivery instructions updated" on success
  - Show error toast on failure
- When `isEditable` is false (post-delivery): show text-only, no edit button
- `'use client'` directive

**2. Create API route for notes update (`src/app/api/orders/[id]/notes/route.ts`):**

- PATCH handler:
  - Authenticate user
  - Validate `orderId` param
  - Parse body: `{ notes: string }` with max 500 character validation
  - Check order ownership (user_id matches) OR user is admin
  - Check order status is NOT `delivered` or `cancelled` (locked decision: editable before delivery)
  - Update `orders.special_instructions` in Supabase
  - Return 200 with updated notes

**3. Upgrade ETA display format:**

- Modify ETACountdown or ETADisplay to implement dual format (per Claude's discretion):
  - When `minMinutes <= 30`: show countdown format "~12 min" with a circular countdown animation
  - When `minMinutes > 30`: show time window format "4:15 - 4:30 PM" based on `estimatedArrival`
- Add "nearby" indicator: when `minMinutes <= 5`, show pulsing green badge "Almost here!"
- Add `useSafeInterval` for countdown timer (prevents mobile crashes -- per ERROR_HISTORY.md pattern)

**4. Update tracking index.ts barrel:**

- Export `DeliveryNotesEditor` from `./DeliveryNotesEditor`
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- `pnpm test` passes
- Verify notes API endpoint responds correctly (run dev server, then test):
  ```bash
  # Should return 401 without auth:
  curl -s -o /dev/null -w "%{http_code}" -X PATCH http://localhost:3000/api/orders/test-id/notes -H "Content-Type: application/json" -d '{"notes":"test"}'
  # Expected: 401
  ```
  If integration tests exist, run `pnpm test -- --grep "notes"` to catch related tests.
  </verify>
  <done>
- DeliveryNotesEditor renders current instructions with edit/save/cancel flow
- Notes API endpoint validates ownership and status before updating
- ETA shows countdown when <30 min, time window when >30 min
- "Almost here!" indicator when ETA <= 5 min
- Barrel exports updated
  </done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- Visual: driver photo/name/call button visible when driver assigned
- Visual: full item list visible (not behind accordion)
- Visual: delivery notes editable with save confirmation
- Visual: ETA format switches between countdown and time window
</verification>

<success_criteria>

- Driver name + photo shown with call button (tel: link)
- Full item list visible (not collapsed)
- Delivery instructions editable before delivery, read-only after
- ETA dual format: countdown (<30 min) / time window (>30 min)
- "Almost here!" indicator when driver is ~5 min away
  </success_criteria>

<output>
After completion, create `.planning/phases/66-backlog-cleanup/66-04-SUMMARY.md`
</output>
