---
phase: 61-admin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/orders/[id]/details/route.ts
  - src/app/api/admin/orders/[id]/status/route.ts
  - src/app/api/admin/orders/[id]/priority/route.ts
  - supabase/migrations/20260214_add_orders_is_priority.sql
autonomous: true

must_haves:
  truths:
    - "Order details API returns delivery_window_start, delivery_window_end, and stripe_payment_intent_id"
    - "Status PATCH accepts notifyCustomer boolean and optional reason, triggers email on transition"
    - "Priority PATCH toggles is_priority boolean on an order"
    - "is_priority column exists in orders table"
  artifacts:
    - path: "src/app/api/admin/orders/[id]/details/route.ts"
      provides: "Extended order details with delivery window + Stripe payment ID + discount fields"
      contains: "delivery_window_start"
    - path: "src/app/api/admin/orders/[id]/status/route.ts"
      provides: "Status update with email notification + reason + audit log"
      contains: "notifyCustomer"
    - path: "src/app/api/admin/orders/[id]/priority/route.ts"
      provides: "Priority toggle endpoint"
      exports: ["PATCH"]
    - path: "supabase/migrations/20260214_add_orders_is_priority.sql"
      provides: "is_priority boolean column on orders"
      contains: "is_priority"
  key_links:
    - from: "src/app/api/admin/orders/[id]/status/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendEmail call on status transition"
      pattern: "sendEmail|buildEmailElement"
---

<objective>
Extend order detail and status APIs for Phase 61 order detail page requirements. Add delivery window, Stripe payment intent, and discount fields to the details response. Extend status PATCH to support email notifications and reason logging. Add priority flag toggle endpoint and database migration.

Purpose: Backend foundation for order detail page UI — all required data must be available via API before building the frontend.
Output: Extended API routes + new priority endpoint + migration SQL
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-admin-pages/61-RESEARCH.md
@src/app/api/admin/orders/[id]/details/route.ts
@src/app/api/admin/orders/[id]/status/route.ts
@src/app/api/admin/orders/[id]/cancel/route.ts
@src/lib/email/send.ts
@src/lib/email/build.ts
@src/lib/email/types.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend order details API and add priority endpoint + migration</name>
  <files>
    src/app/api/admin/orders/[id]/details/route.ts
    src/app/api/admin/orders/[id]/priority/route.ts
    supabase/migrations/20260214_add_orders_is_priority.sql
  </files>
  <action>
**1. Extend details route** (`src/app/api/admin/orders/[id]/details/route.ts`):

Add these fields to the Supabase select query on the orders table:

- `delivery_window_start`
- `delivery_window_end`
- `stripe_payment_intent_id`
- `is_priority`
- `discount_cents` (if column exists, else default 0)

Update the `OrderRow` interface to include these fields. Update the response mapping to include:

```typescript
deliveryWindowStart: order.delivery_window_start ?? null,
deliveryWindowEnd: order.delivery_window_end ?? null,
stripePaymentIntentId: order.stripe_payment_intent_id ?? null,
isPriority: order.is_priority ?? false,
discountCents: order.discount_cents ?? 0,
```

Check if `discount_cents` column exists in the database types. If not, omit it and hardcode 0.

**2. Create priority toggle endpoint** (`src/app/api/admin/orders/[id]/priority/route.ts`):

Create PATCH handler:

- `requireAdmin()` auth check
- Parse body: `{ isPriority: boolean }` via Zod schema
- Update `orders.is_priority` via Supabase
- Return `{ success: true, orderId, isPriority }`
- Log to `order_audit_log` with action `"priority_change"`

**3. Create migration** (`supabase/migrations/20260214_add_orders_is_priority.sql`):

```sql
ALTER TABLE orders ADD COLUMN IF NOT EXISTS is_priority BOOLEAN DEFAULT FALSE;
```

Keep migration minimal — single column addition.
</action>
<verify>

- `pnpm typecheck` passes
- Migration SQL is valid (no syntax errors)
- Details route compiles with new fields
- Priority route compiles
  </verify>
  <done>
- GET /api/admin/orders/[id]/details returns deliveryWindowStart, deliveryWindowEnd, stripePaymentIntentId, isPriority in response
- PATCH /api/admin/orders/[id]/priority accepts { isPriority: boolean } and updates the order
- Migration file exists with is_priority column
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extend status route with email notification and reason support</name>
  <files>
    src/app/api/admin/orders/[id]/status/route.ts
  </files>
  <action>
Extend the existing status PATCH route to support email notifications on status transitions:

**1. Extend the request schema:**

```typescript
const updateStatusSchema = z.object({
  status: z.enum([
    "pending",
    "confirmed",
    "preparing",
    "out_for_delivery",
    "delivered",
    "cancelled",
  ]),
  notifyCustomer: z.boolean().default(true),
  reason: z.string().max(500).optional(),
});
```

**2. After successful status update, add audit log entry:**
Insert into `order_audit_log`:

- `order_id`: orderId
- `action`: "status_change"
- `actor_role`: "admin"
- `reason`: parsed reason or null
- `metadata`: `{ previousStatus, newStatus }`

Check if the cancel route (`src/app/api/admin/orders/[id]/cancel/route.ts`) already logs to audit. If so, follow same pattern.

**3. If `notifyCustomer` is true, trigger email:**

- Import `sendEmail` and `buildEmailElement` from `@/lib/email`
- Look up existing email templates: check `src/lib/email/build.ts` for template functions
- For status transitions, call the appropriate email builder (order_confirmation for confirmed, etc.)
- If no specific template exists for a transition, use a generic status update notification
- Fetch customer email from order's profile join (already in the details query pattern)
- Wrap email send in try/catch — email failure should NOT block the status update response
- Log email send result to `notification_logs` via the existing sendEmail pipeline

**4. Return extended response:**

```typescript
return NextResponse.json({
  success: true,
  orderId,
  previousStatus: currentStatus,
  newStatus,
  emailSent: notifyCustomer ? (emailResult?.success ?? false) : false,
});
```

Reference the cancel route for the email sending pattern — it already handles email on cancellation. Follow the same approach for other transitions.
</action>
<verify>

- `pnpm typecheck` passes
- Status route compiles with new schema fields
- Email import paths resolve correctly
  </verify>
  <done>
- PATCH /api/admin/orders/[id]/status accepts notifyCustomer boolean and optional reason
- Status changes are logged to order_audit_log with reason
- Customer receives email notification when notifyCustomer is true
- Email failure does not block status update success response
  </done>
  </task>

</tasks>

<verification>
- `pnpm typecheck` passes with no errors
- `pnpm lint` passes
- All three API routes compile: details (extended), status (extended), priority (new)
- Migration SQL file exists at expected path
</verification>

<success_criteria>

- Order details API returns delivery window, Stripe payment ID, and priority fields
- Status API accepts notifyCustomer + reason, sends email, logs audit
- Priority toggle endpoint works as PATCH
- Database migration adds is_priority column
  </success_criteria>

<output>
After completion, create `.planning/phases/61-admin-pages/61-01-SUMMARY.md`
</output>
