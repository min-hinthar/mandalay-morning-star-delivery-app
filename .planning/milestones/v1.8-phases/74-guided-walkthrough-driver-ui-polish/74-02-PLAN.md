---
phase: 74-guided-walkthrough-driver-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(driver)/driver/test-delivery/page.tsx
  - src/components/ui/driver/ActiveRouteView.tsx
  - src/components/ui/driver/StopDetailView.tsx
  - src/components/ui/driver/DeliveryActions.tsx
  - src/components/ui/driver/NavigationButton.tsx
  - src/components/ui/driver/LocationTracker.tsx
  - src/components/ui/driver/ExceptionModal.tsx
autonomous: true
requirements:
  - DPROF-05

must_haves:
  truths:
    - "Driver can access /driver/test-delivery page from walkthrough or direct URL"
    - "Test delivery exercises all 5 steps: route overview, start route, stop detail, photo capture, exception modal"
    - "Zero database writes occur during test delivery flow"
    - "Test delivery is re-runnable — all state resets on Run Again"
    - "Components in testMode show simulated delays instead of real API calls"
  artifacts:
    - path: "src/app/(driver)/driver/test-delivery/page.tsx"
      provides: "Client-side test delivery page with mock data and view swapping"
      min_lines: 150
  key_links:
    - from: "src/app/(driver)/driver/test-delivery/page.tsx"
      to: "ActiveRouteView, StopDetailView, DeliveryActions, PhotoCapture, ExceptionModal"
      via: "component imports with testMode=true prop"
      pattern: "testMode"
    - from: "src/components/ui/driver/DeliveryActions.tsx"
      to: "fetch API"
      via: "testMode guard intercepts fetch calls"
      pattern: "if.*testMode"
---

<objective>
Create the test delivery page at /driver/test-delivery that lets new drivers practice the full delivery flow with mock data and no database writes.

Purpose: New drivers need to experience the delivery workflow before their first real assignment without risk of corrupting production data.
Output: Test delivery page with mock route data, testMode prop on API-calling components, view swapping via AnimatePresence.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/driver/ActiveRouteView.tsx
@src/components/ui/driver/StopDetailView.tsx
@src/components/ui/driver/StopDetail.tsx
@src/components/ui/driver/DeliveryActions.tsx
@src/components/ui/driver/NavigationButton.tsx
@src/components/ui/driver/LocationTracker.tsx
@src/components/ui/driver/ExceptionModal.tsx
@src/components/ui/driver/PhotoCapture.tsx
@src/types/driver.ts
@src/lib/motion-tokens/core.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testMode prop to API-calling driver components</name>
  <files>
    src/components/ui/driver/DeliveryActions.tsx
    src/components/ui/driver/NavigationButton.tsx
    src/components/ui/driver/LocationTracker.tsx
    src/components/ui/driver/ExceptionModal.tsx
  </files>
  <action>
Add `testMode?: boolean` prop to each component that makes API calls. When `testMode` is true, intercept all `fetch()` calls with simulated delays and local state updates.

**DeliveryActions.tsx:**
- Add `testMode?: boolean` to `DeliveryActionsProps`
- In `updateStatus()`, add early return when `testMode`:
  ```typescript
  if (testMode) {
    setIsLoading(true);
    await new Promise(r => setTimeout(r, 600 + Math.random() * 400));
    setIsLoading(false);
    onStatusChange?.(newStatus);
    return;
  }
  ```
- This must be the FIRST check in `updateStatus`, before the `navigator.onLine` check

**NavigationButton.tsx:**
- Read the file to check its interface and behavior
- Add `testMode?: boolean` prop
- When `testMode`, instead of opening Google Maps URL, show a toast or alert: "Navigation not active in test mode"
- Import toast from sonner if available, otherwise use `window.alert` as fallback
- Check what toast library the project uses first

**LocationTracker.tsx:**
- Add `testMode?: boolean` prop
- When `testMode`, skip all geolocation API calls and location tracking
- Render nothing or a disabled state indicator
- Do NOT request location permissions in test mode

**ExceptionModal.tsx:**
- Add `testMode?: boolean` prop
- In `handleSubmit()`, add early return when `testMode`:
  ```typescript
  if (testMode) {
    setIsSubmitting(true);
    await new Promise(r => setTimeout(r, 500));
    setIsSubmitting(false);
    onSuccess?.();
    onClose();
    return;
  }
  ```

**PhotoCapture.tsx:** NO changes needed — it already works with blob-only capture flow. The test delivery page will pass a no-op `onUpload` that doesn't call any API.

All `testMode` checks must appear before any `fetch()` or `navigator.mediaDevices` calls to guarantee zero network requests.
  </action>
  <verify>
- `pnpm typecheck` passes — all new props are optional
- `pnpm lint` passes
- Existing behavior unchanged when testMode is undefined/false
- Each component's testMode path has a simulated delay (not instant)
  </verify>
  <done>
- DeliveryActions, NavigationButton, LocationTracker, ExceptionModal all accept testMode prop
- When testMode=true, zero fetch() calls are made
- Simulated delays give realistic UX feel (600-1000ms)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test delivery page with mock data and view swapping</name>
  <files>
    src/app/(driver)/driver/test-delivery/page.tsx
  </files>
  <action>
Create `src/app/(driver)/driver/test-delivery/page.tsx` as a fully client-side page (`"use client"` directive).

**Page structure:**
- Uses `useState<TestStep>()` to drive view swapping where `TestStep = "overview" | "route" | "stop" | "complete"`
- `AnimatePresence mode="wait"` wraps the view switch
- Inherits driver layout automatically (inside `(driver)/driver/` route group)
- Uses `DriverHeader` with `title="Test Delivery"` and `showBack={true}` `backHref="/driver"`

**Mock data:**
- Define `MOCK_STOPS: StopData[]` as static constants at module scope
- Use clearly fake data:
  ```typescript
  const MOCK_STOPS: StopData[] = [
    {
      id: "test-stop-1",
      stopIndex: 1,
      status: "pending", // managed by local state
      eta: null,
      order: {
        id: "test-order-1",
        deliveryWindowStart: null,
        deliveryWindowEnd: null,
        customer: { fullName: "Test Customer A" },
        address: { line1: "123 Practice Lane", line2: "Apt 4B", city: "Portland", state: "OR" },
      },
    },
    {
      id: "test-stop-2",
      stopIndex: 2,
      status: "pending",
      eta: null,
      order: {
        id: "test-order-2",
        deliveryWindowStart: null,
        deliveryWindowEnd: null,
        customer: { fullName: "Test Customer B" },
        address: { line1: "456 Demo Street", line2: null, city: "Portland", state: "OR" },
      },
    },
  ];
  ```
- `MOCK_ROUTE_ID = "test-route-000"` — clearly fake

**View: "overview" (initial):**
- Brief intro card: "Practice Delivery — Walk through a delivery without affecting real data"
- Show `ActiveRouteView` with `testMode` stops (all pending initially)
- "Start Route" button → transitions to `"route"` step
- Pass `testMode` to suppress real API calls

**View: "route":**
- Show `ActiveRouteView` with current stop statuses
- Clicking a stop → transitions to `"stop"` view with that stop's data
- Start route button works via local state change (route status: planned → in_progress)

**View: "stop":**
- Show `StopDetailView` with the selected stop
- Pass `testMode={true}` to `DeliveryActions`, `ExceptionModal`, `NavigationButton`
- Status changes update local state array
- When stop status changes, animate back to route view
- Include `PhotoCapture` with `onUpload` that just resolves (no real upload)

**View: "complete":**
- Shown when all stops are delivered or skipped
- "Practice Complete!" card with:
  - Stats summary: stops delivered, stops skipped
  - `spring.ultraBouncy` entrance animation
  - "Run Again" button → resets all state to initial values
  - "Back to Home" button → `router.push("/driver")`

**State management:**
- `stops` state: `useState(MOCK_STOPS)` — mutated locally on status changes
- `currentStopId` state: tracks which stop is being viewed
- `routeStatus` state: `"planned" | "in_progress" | "completed"`
- `testStep` state: drives view swapping
- `resetAll()` function: resets all state to initial for "Run Again"

**Animation:**
- Use `m.div` with `initial/animate/exit` for view transitions
- `AnimatePresence mode="wait"` on the view container
- Follow driver glass shell for cards: `rounded-2xl border-2 shadow-card bg-surface-primary/80 sm:backdrop-blur-sm`
- Use `shouldAnimate` guards from `useAnimationPreference`

**Critical constraints:**
- ZERO router.push during delivery flow — swap views via local state only
- ZERO fetch() calls — all state managed locally
- `testMode={true}` on every component that has the prop
- Page must be re-runnable: "Run Again" resets everything cleanly
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Navigate to /driver/test-delivery — page loads
- Complete full flow: overview → start → arrive → deliver → complete
- No network requests visible in browser DevTools during test flow
- "Run Again" resets all state, flow restarts from overview
  </verify>
  <done>
- /driver/test-delivery page exists and is accessible
- All 5 delivery steps exercisable: overview, start route, stop detail, photo capture, exception modal
- Zero database writes during entire flow
- Re-runnable via "Run Again" button with full state reset
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint` pass
- Test delivery page loads at /driver/test-delivery
- Full delivery flow completable without any API calls
- Existing delivery flow (non-test) works unchanged
- Re-running test delivery produces clean state
</verification>

<success_criteria>
- test-delivery/page.tsx exists with mock data and view swapping
- 4 components have testMode prop (DeliveryActions, NavigationButton, LocationTracker, ExceptionModal)
- Zero network requests during test delivery flow
- TypeScript compilation and linting clean
</success_criteria>

<output>
After completion, create `.planning/phases/74-guided-walkthrough-driver-ui-polish/74-02-SUMMARY.md`
</output>
