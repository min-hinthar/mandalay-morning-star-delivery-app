---
phase: 57-admin-driver-polish
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/app/(admin)/admin/orders/page.tsx
  - src/components/ui/admin/OrdersTable.tsx
  - src/components/ui/admin/orders/OrdersPageSkeleton.tsx
  - src/components/ui/admin/orders/OrderCardRow.tsx
  - src/components/ui/admin/orders/OrderDetailDrawer.tsx
autonomous: true

must_haves:
  truths:
    - "Orders page uses AdminPageHeader with animated count badge"
    - "Orders table uses card rows with 12px radius, scale+shadow hover, status tint, staggered entry"
    - "Orders loading state uses shimmer Skeleton with crossfade (no animate-pulse)"
    - "Orders empty state shows food emoji illustration with playful messaging and CTA"
    - "Clicking an order row opens a slide-in drawer overlay with order details"
    - "Status badges on order rows pulse for active statuses"
    - "Admin accent color is teal (not red/saffron) on orders page"
  artifacts:
    - path: "src/components/ui/admin/orders/OrderCardRow.tsx"
      provides: "Card-based order row with status tint, badge, actions"
    - path: "src/components/ui/admin/orders/OrdersPageSkeleton.tsx"
      provides: "Shimmer skeleton matching orders page layout"
    - path: "src/components/ui/admin/orders/OrderDetailDrawer.tsx"
      provides: "Slide-in drawer for order detail view"
  key_links:
    - from: "src/app/(admin)/admin/orders/page.tsx"
      to: "src/components/ui/admin/AdminPageHeader.tsx"
      via: "AdminPageHeader import"
      pattern: "AdminPageHeader"
    - from: "src/components/ui/admin/orders/OrderCardRow.tsx"
      to: "src/components/ui/admin/CardRow.tsx"
      via: "CardRow base component import"
      pattern: "CardRow"
---

<objective>
Transform admin orders page from traditional table to premium card-based layout with shimmer skeleton, animated status badges, slide-in drawer detail view, and teal accent color.

Purpose: Orders is the most-used admin page. Premium card rows, crossfade loading, and drawer detail view deliver the highest-impact visual upgrade. Covers POLH-01 (skeleton shimmer), POLH-02 (hover micro-interactions), POLH-03 (status badge animation), POLH-04 (empty states), POLH-12 (orders table premium styling).
Output: Card-based orders table with drawer, skeleton, empty state
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-RESEARCH.md
@.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md
@src/app/(admin)/admin/orders/page.tsx
@src/components/ui/admin/OrdersTable.tsx
@src/components/ui/admin/CardRow.tsx
@src/components/ui/admin/StatusBadge.tsx
@src/components/ui/admin/AdminPageHeader.tsx
@src/components/ui/admin/SkeletonCrossfade.tsx
@src/components/ui/admin/InlineErrorCard.tsx
@src/components/ui/EmptyState.tsx
@src/lib/motion-tokens/variants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrderCardRow + OrdersPageSkeleton + OrderDetailDrawer</name>
  <files>
    src/components/ui/admin/orders/OrderCardRow.tsx
    src/components/ui/admin/orders/OrdersPageSkeleton.tsx
    src/components/ui/admin/orders/OrderDetailDrawer.tsx
  </files>
  <action>
**OrderCardRow:**
- Create `src/components/ui/admin/orders/OrderCardRow.tsx` with `'use client'`
- Props: `order: Order` (match existing type from OrdersTable), `selected?: boolean`, `onClick?: () => void`
- Uses `CardRow` base component for hover/selection/animation
- Layout (desktop): flex row with: order ID (mono font), customer name, items count, total (formatted), status badge (StatusBadge component), date/time, action buttons (always visible per CONTEXT)
- Layout (mobile): stacked card showing status badge + customer name + items/total + time (responsive via Tailwind breakpoints)
- Status tint mapping: `STATUS_TINTS` record mapping order status to subtle bg class (e.g., `pending: 'bg-amber-50/50'`, `delivered: 'bg-green-50/50'`, `cancelled: 'bg-gray-50/50'`)
- Always-visible action buttons: "View" button (opens drawer via onClick)
- Date display: use relative format for today/yesterday, absolute for older

**OrdersPageSkeleton:**

- Create `src/components/ui/admin/orders/OrdersPageSkeleton.tsx`
- Match exact layout of real orders page: header area (Skeleton w=200 h=36), filter bar (6 Skeleton pills w=100 h=28 radius-full), 5 card rows (Skeleton w=100% h=72 radius-xl)
- Uses Skeleton component from `@/components/ui/skeleton` (not animate-pulse)
- Import and use shimmer variant

**OrderDetailDrawer:**

- Create `src/components/ui/admin/orders/OrderDetailDrawer.tsx` with `'use client'`
- Props: `order: Order | null`, `open: boolean`, `onClose: () => void`
- Uses `overlay.drawer` variant from motion-tokens for slide-in from right
- Dark backdrop: `m.div` with `bg-black/40` fixed inset-0, onClick=onClose
- Panel: `m.div` fixed right-0 top-0 bottom-0 w-full sm:w-[480px] bg-surface-primary shadow-2xl z-50 overflow-y-auto
- Content: order header (ID, status badge, date), customer info section, items list, totals section, delivery address, action buttons (Mark as Delivered, Cancel, etc. matching existing actions from OrdersTable)
- Close button: top-right X icon button
- Use AnimatePresence wrapping the whole drawer for enter/exit
- NO backdrop-blur (mobile anti-pattern)
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint` passes - All 3 new files exist in src/components/ui/admin/orders/
  </verify>
  <done>OrderCardRow renders card-based row with status tint and badge. OrdersPageSkeleton uses Skeleton shimmer. OrderDetailDrawer slides in from right with dark backdrop.</done>
  </task>

<task type="auto">
  <name>Task 2: Wire orders page with card rows, skeleton crossfade, empty state, drawer, teal accent</name>
  <files>
    src/app/(admin)/admin/orders/page.tsx
    src/components/ui/admin/OrdersTable.tsx
  </files>
  <action>
**Orders page.tsx refactor:**
- Replace existing loading block (animate-pulse divs at lines 128-136) with `SkeletonCrossfade` wrapping `OrdersPageSkeleton`
- Replace page title/header section with `AdminPageHeader` component: `title="Orders"`, `count={filteredOrders.length}`, actions slot for existing filter controls
- Add error state: if fetch fails, show `InlineErrorCard` with onRetry
- Migrate color accents: replace any `brand-red`, `saffron`, `curry`, `primary` accent usage with `accent-teal` for non-destructive UI elements. Keep `primary` (red) only for destructive actions (cancel order).
- Add breadcrumbs to AdminPageHeader: `[{ label: 'Dashboard', href: '/admin' }, { label: 'Orders' }]`

**OrdersTable.tsx refactor:**

- Replace `<Table>` markup with card row layout:
  - Container: `m.div` with `variants={cardContainer}` `initial="hidden"` `animate="visible"` for staggered entry
  - Each row: `OrderCardRow` component (replaces `<TableRow>`)
- Add sticky header row (column labels): `sticky top-0 z-10 bg-surface-primary/95 shadow-sm` div above cards with column names (hidden on mobile)
- Sorting: preserve existing sort logic but add `layout` prop on `m.div` rows for animated reorder (Framer Motion layout animations shuffle rows smoothly per CONTEXT "animated column sorting")
- Selected row state: track `selectedOrderId` in local state, pass `selected` prop to CardRow
- On row click: open OrderDetailDrawer, pass selected order
- Empty state: when filtered orders is empty, render `EmptyState variant="admin-orders-filtered"` with onAction clearing filters. When truly empty (no orders at all), render `EmptyState variant="admin-orders"`.
- "Load more" pagination: if orders > 20, show initial 20, add "Load More" button at bottom (m.button with scale hover)
- Date section headers: group orders by date, render sticky section headers ("Today", "Yesterday", "Feb 8, 2026") between groups
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint && pnpm lint:css` passes - Grep orders/page.tsx for `animate-pulse` returns 0 results - Grep OrdersTable.tsx for `<Table` returns 0 results (replaced with card rows) - Grep orders/page.tsx for `AdminPageHeader` confirms usage - Grep OrdersTable.tsx for `EmptyState` confirms empty state usage
  </verify>
  <done>Orders page uses shimmer skeleton crossfade, AdminPageHeader with teal count badge, card-based rows with stagger/hover/selection, drawer detail view, empty states, date grouping, load more pagination. No animate-pulse. Teal accent color throughout.</done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- Orders page renders card rows instead of table
- Loading shows shimmer skeleton crossfade
- Empty state shows food emoji with CTA
- Clicking row opens slide-in drawer
- Status badges pulse for active orders
- No animate-pulse in orders page or OrdersTable
- Teal accent color used (not red/saffron)
</verification>

<success_criteria>
Admin orders page has premium card-based layout with all CONTEXT.md decisions implemented: card rows with hover, status tint, stagger entry, animated badges, drawer detail, skeleton crossfade, empty states, teal accent, date grouping, load more.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-02-SUMMARY.md`
</output>
