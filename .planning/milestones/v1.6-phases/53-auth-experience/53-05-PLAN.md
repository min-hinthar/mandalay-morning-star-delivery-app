---
phase: 53-auth-experience
plan: 05
type: execute
wave: 3
depends_on: ["53-01", "53-03", "53-04"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/components/ui/auth/index.ts
  - src/components/ui/auth/UserMenu.tsx
  - src/app/auth/callback/route.ts
  - src/app/auth/expired/page.tsx
autonomous: true
user_setup:
  - service: google-oauth
    why: "Google social login via Supabase"
    env_vars: []
    dashboard_config:
      - task: "Enable Google Auth Provider in Supabase Dashboard > Auth > Providers > Google"
        location: "Supabase Dashboard > Auth > Providers"
      - task: "Create OAuth 2.0 credentials in Google Cloud Console"
        location: "Google Cloud Console > APIs & Services > Credentials"
      - task: "Add redirect URI: https://[project-ref].supabase.co/auth/v1/callback"
        location: "Google Cloud Console OAuth Client > Authorized Redirect URIs"
  - service: apple-oauth
    why: "Apple social login via Supabase"
    env_vars: []
    dashboard_config:
      - task: "Enable Apple Auth Provider in Supabase Dashboard > Auth > Providers > Apple"
        location: "Supabase Dashboard > Auth > Providers"
      - task: "Create Services ID with Sign In with Apple in Apple Developer Portal"
        location: "Apple Developer Portal > Certificates, Identifiers & Profiles"
      - task: "Add domain and return URL, generate key"
        location: "Apple Developer Portal > Services ID > Web Authentication"
  - service: supabase-email-template
    why: "Custom branded magic link email"
    env_vars: []
    dashboard_config:
      - task: "Replace default Magic Link email template with branded HTML"
        location: "Supabase Dashboard > Auth > Email Templates > Magic Link"

must_haves:
  truths:
    - "Login page renders AuthBackground + AuthCard with MagicLinkForm and SocialLoginButtons"
    - "Submitting email transitions card to MagicLinkConfirmation in-place"
    - "UserMenu shows only 'Sign In' button (no 'Sign Up') for unauthenticated users"
    - "Auth callback handles OAuth errors with redirect to /login?error=..."
    - "Expired magic link page shows friendly message with resend button"
    - "Auth barrel re-exports all new components"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Complete auth page composing all auth components"
    - path: "src/components/ui/auth/index.ts"
      provides: "Barrel re-exporting all auth components"
      exports:
        [
          "UserMenu",
          "AuthCard",
          "AuthBackground",
          "MagicLinkForm",
          "SocialLoginButtons",
          "MagicLinkConfirmation",
          "OAuthLoadingOverlay",
        ]
    - path: "src/app/auth/expired/page.tsx"
      provides: "Expired magic link page with resend"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/ui/auth/AuthCard.tsx"
      via: "Composes AuthCard with form/confirmation states"
      pattern: "AuthCard|AuthBackground"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/ui/auth/MagicLinkForm.tsx"
      via: "Renders inside AuthCard for form state"
      pattern: "MagicLinkForm"
    - from: "src/app/auth/callback/route.ts"
      to: "/login?error="
      via: "OAuth error redirect"
      pattern: "error=.*expired"
---

<objective>
Assemble the complete login page, update supporting files, and create the expired link page.

Purpose: Wire all auth components into the login page, update the auth barrel exports, clean up UserMenu (remove Sign Up), enhance the callback route for OAuth error handling, and create the expired magic link page.

Output: Fully functional login page at /login with all auth flows working. Clean barrel exports. Expired link handling.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-auth-experience/53-CONTEXT.md
@.planning/phases/53-auth-experience/53-RESEARCH.md
@.planning/phases/53-auth-experience/53-01-SUMMARY.md
@.planning/phases/53-auth-experience/53-02-SUMMARY.md
@.planning/phases/53-auth-experience/53-03-SUMMARY.md
@.planning/phases/53-auth-experience/53-04-SUMMARY.md
@src/app/(auth)/login/page.tsx
@src/components/ui/auth/UserMenu.tsx
@src/app/auth/callback/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite login page.tsx and update barrel + UserMenu</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/components/ui/auth/index.ts
    src/components/ui/auth/UserMenu.tsx
  </files>
  <action>
    1. **Rewrite `src/app/(auth)/login/page.tsx`:**
       - Keep as server component for auth check (redirect if already logged in)
       - Import and render `LoginPageClient` (new client component in same file or separate)
       - Actually: since the page needs `createClient` for auth check AND client interactivity, use a pattern:
         - Server component does auth check + redirect
         - Renders `<LoginPageClient />` which is a client component
       - **LoginPageClient** (can be inline in page.tsx if under 400 lines, otherwise separate file):
         - `'use client'` if separate file
         - Renders `<AuthBackground>` wrapping `<AuthCard>`
         - Inside AuthCard, use `useAuthCard` context to conditionally render:
           - `state === 'form'`: `<m.div key="form">` containing `<MagicLinkForm onSuccess={handleMagicLinkSent} />` and `<SocialLoginButtons />`
           - `state === 'confirmation'`: `<m.div key="confirm">` containing `<MagicLinkConfirmation email={email} onBack={handleBackToForm} />`
           - `state === 'error'`: Error display with retry
         - `handleMagicLinkSent(email)`: sets email in context, transitions to 'confirmation'
         - `handleBackToForm()`: transitions back to 'form'
         - Check URL params for `?error=` and show toast if present (from OAuth error redirect)
       - **OAuthLoadingOverlay:** Render at top level, controlled by SocialLoginButtons via state
       - Use `m.div` for each state section with slide-up-fade animations inside AnimatePresence

       **Important architecture note:** AuthCard provides the AnimatePresence wrapper and context. The login page's client component consumes that context.

    2. **Update `src/components/ui/auth/index.ts`:**
       - Export all new components:
         ```typescript
         export { UserMenu } from "./UserMenu";
         export { AuthCard, useAuthCard } from "./AuthCard";
         export { AuthBackground } from "./AuthBackground";
         export { MagicLinkForm } from "./MagicLinkForm";
         export { SocialLoginButtons } from "./SocialLoginButtons";
         export { MagicLinkConfirmation } from "./MagicLinkConfirmation";
         export { OAuthLoadingOverlay } from "./OAuthLoadingOverlay";
         ```

    3. **Update `src/components/ui/auth/UserMenu.tsx`:**
       - Remove the "Sign Up" button entirely (line 56-58 area)
       - Keep "Sign In" button for both desktop and mobile
       - The `!user` branch should only show Sign In links (desktop text + mobile icon)
       - Remove `Link` import for `/signup` since it no longer exists

  </action>
  <verify>
    - `pnpm typecheck` passes
    - Login page imports and renders all auth components
    - UserMenu no longer references /signup
    - Barrel exports all new components
  </verify>
  <done>
    - Login page renders full auth experience (background + card + form + social)
    - State transitions work: form -> confirmation (on magic link sent) and back
    - URL error params shown as toast
    - UserMenu shows only "Sign In" for unauthenticated users
    - Barrel exports updated with all new components
  </done>
</task>

<task type="auto">
  <name>Task 2: Create expired link page and update callback route</name>
  <files>
    src/app/auth/expired/page.tsx
    src/app/auth/callback/route.ts
  </files>
  <action>
    1. **Create `src/app/auth/expired/page.tsx`:**
       - Client component (`'use client'`)
       - **Per context decision:** Friendly error page with one-tap "Send a new link" button
       - Layout: centered card (simpler than full auth card -- just a message card)
         - `min-h-screen flex items-center justify-center p-4 bg-background`
         - Card: `max-w-md w-full p-8 rounded-2xl bg-surface-primary shadow-lg text-center`
       - Content:
         - Sad/expired icon (e.g., `Clock` or `AlertCircle` from lucide-react, large)
         - Heading: "Link expired"
         - Subtext: "This magic link has expired or has already been used."
         - Email input field (pre-filled from URL param if available): let user enter/confirm email
         - "Send a new link" button -- calls `signInWithMagicLink` and on success redirects to `/login` with toast
         - "Back to sign in" link -> `/login`
       - Use `useSearchParams` for `?email=` pre-fill (wrap in Suspense)

    2. **Update `src/app/auth/callback/route.ts`:**
       - Current behavior is correct for code exchange and driver invite flow
       - Add handling for expired/invalid link errors:
         - After `exchangeCodeForSession` fails, check if error message indicates expiry
         - If error contains "expired" or "invalid": redirect to `/auth/expired?email=...` instead of generic `/login?error=`
       - Add `?next=` param support for OAuth: after successful OAuth login, read `next` param and redirect there
       - No other changes needed -- existing driver invite flow stays as-is

  </action>
  <verify>
    - `pnpm typecheck` passes
    - Expired page renders at /auth/expired
    - Callback route redirects expired links to /auth/expired
    - Callback route still handles driver invite flow correctly
  </verify>
  <done>
    - Expired link page shows friendly message with email input and "Send a new link" button
    - Auth callback redirects expired/invalid links to /auth/expired
    - OAuth error handling preserves existing behavior
    - Driver invite flow unchanged
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- Login page renders (verify via build)
- No references to /signup in user-facing navigation
- Auth barrel exports all 7+ components
</verification>

<success_criteria>

- Login page renders AuthBackground + AuthCard + MagicLinkForm + SocialLoginButtons
- Form -> confirmation transition works on magic link send
- UserMenu only shows "Sign In" (no "Sign Up")
- Expired link page exists with resend capability
- Callback route handles expired links gracefully
  </success_criteria>

<output>
After completion, create `.planning/phases/53-auth-experience/53-05-SUMMARY.md`
</output>
