---
phase: 54-email-system
plan: 08
type: execute
wave: 4
depends_on: ["54-05", "54-06", "54-07"]
files_modified:
  - src/components/ui/admin/settings/EmailSettingsForm.tsx
  - src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx
  - src/app/(admin)/admin/emails/page.tsx
  - src/app/(admin)/admin/emails/loading.tsx
  - src/app/(admin)/admin/emails/error.tsx
  - src/app/(admin)/admin/orders/[id]/EmailHistory.tsx
autonomous: true

must_haves:
  truths:
    - "Admin settings page has Email section with kill switch toggle and Send Test Email buttons"
    - "Admin email log page shows filterable/searchable email list with status badges"
    - "Admin order detail page shows per-order email history"
    - "Email kill switch toggle persists to app_settings table"
    - "Test email buttons send actual emails via the test API route"
  artifacts:
    - path: "src/components/ui/admin/settings/EmailSettingsForm.tsx"
      provides: "Email settings section with kill switch + test buttons"
      exports: ["EmailSettingsForm"]
    - path: "src/app/(admin)/admin/emails/page.tsx"
      provides: "Admin email log page"
      exports: ["default"]
    - path: "src/app/(admin)/admin/orders/[id]/EmailHistory.tsx"
      provides: "Per-order email history component"
      exports: ["EmailHistory"]
  key_links:
    - from: "src/components/ui/admin/settings/EmailSettingsForm.tsx"
      to: "src/app/api/emails/test/route.ts"
      via: "fetch POST to test endpoint"
      pattern: "api/emails/test"
    - from: "src/app/(admin)/admin/emails/page.tsx"
      to: "src/app/api/admin/emails/route.ts"
      via: "fetch GET for email log"
      pattern: "api/admin/emails"
    - from: "src/app/(admin)/admin/orders/[id]/EmailHistory.tsx"
      to: "src/app/api/admin/emails/route.ts"
      via: "fetch GET with orderId filter"
      pattern: "api/admin/emails"
---

<objective>
Build admin email management UI: email settings form with kill switch + test buttons, global email log page, and per-order email history.

Purpose: Admin can manage email system settings, monitor delivery, and view per-order email history from the UI.
Output: Admin email settings section, email log page, per-order email history component
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-05-SUMMARY.md
@.planning/phases/54-email-system/54-07-SUMMARY.md
@src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx
@src/components/ui/admin/settings/settings-types.ts
@src/components/ui/admin/settings/ToggleSwitch.tsx
@src/components/ui/admin/settings/SaveButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Email settings form + integration</name>
  <files>
    src/components/ui/admin/settings/EmailSettingsForm.tsx
    src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx
  </files>
  <action>
    **1. Create `src/components/ui/admin/settings/EmailSettingsForm.tsx`:**
    - 'use client' directive
    - Props: { emailEnabled: boolean, onToggle: (enabled: boolean) => void }
    - Layout follows existing admin settings form pattern (DeliverySettingsForm, OperationsSettingsForm, NotificationSettingsForm)
    - Section heading: "Email System" with Mail icon from lucide-react
    - **Kill switch toggle:**
      - ToggleSwitch component (reuse existing)
      - Label: "Email Sending Enabled"
      - Description: "Master switch to enable/disable all outbound email sending"
      - When OFF: amber warning callout — "All email sending is disabled. Customers will not receive any transactional emails."
      - Persists via parent's save mechanism (already in SettingsClient)
    - **Test email section:**
      - Heading: "Send Test Emails"
      - Description: "Send a test email with sample data to verify templates"
      - Input field for recipient email (default to current admin's email)
      - 4 test email buttons (one per type): "Order Confirmation", "Cancellation", "Refund", "Delivery Reminder"
      - Each button: calls POST /api/emails/test with { emailType, recipientEmail }
      - Loading state per button (spinner while sending)
      - Success/error toast via sonner (toast.success/toast.error)
      - Buttons disabled when email system is OFF (kill switch)
    - **Email stats summary (optional, lightweight):**
      - Small stat cards: "Emails Sent (24h)", "Failed (24h)"
      - Fetch from /api/admin/emails?from={24h ago}&limit=0 to get count
      - Or skip if too complex — email log page covers this

    **2. Modify `src/components/ui/admin/settings/SettingsClient/SettingsClient.tsx`:**
    - Import EmailSettingsForm
    - Add 'email' to the settings tabs/sections
    - Add email_sending_enabled to the settings state (default: true)
    - Load email_sending_enabled from app_settings on mount (existing fetch pattern)
    - Save email_sending_enabled to app_settings on save (existing save pattern)
    - Render EmailSettingsForm in the appropriate tab/section position
    - Keep file under 400 lines — if adding EmailSettingsForm pushes it over, the form is already extracted

  </action>
  <verify>
    - `pnpm typecheck` passes
    - EmailSettingsForm file exists with 'use client' directive
    - SettingsClient imports and renders EmailSettingsForm
    - Kill switch toggle uses existing ToggleSwitch component
    - Test email buttons call /api/emails/test
  </verify>
  <done>
    Admin settings has Email section with kill switch toggle (persists to app_settings) and Send Test Email buttons for all 4 email types
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin email log page + per-order email history</name>
  <files>
    src/app/(admin)/admin/emails/page.tsx
    src/app/(admin)/admin/emails/loading.tsx
    src/app/(admin)/admin/emails/error.tsx
    src/app/(admin)/admin/orders/[id]/EmailHistory.tsx
  </files>
  <action>
    **1. Create `src/app/(admin)/admin/emails/page.tsx`:**
    - 'use client' directive
    - Page title: "Email Log"
    - Filter bar at top:
      - Search input for order ID
      - Dropdown for email type (all types from EmailType)
      - Dropdown for status (pending, sent, delivered, opened, clicked, failed, bounced)
      - Date range picker (from/to inputs, type="date")
    - Results table:
      - Columns: Recipient, Type (badge), Subject, Status (colored badge), Sent At, Actions
      - Status badges: sent=blue, delivered=green, opened=purple, clicked=teal, failed=red, bounced=orange, pending=gray
      - Actions: "View" link to order detail, "Resend" button on failed rows
      - Resend button calls POST /api/admin/emails/[id]/resend, shows toast on result
    - Pagination at bottom (page numbers, prev/next)
    - Fetch from /api/admin/emails with query params
    - Use useEffect + useState for client-side data fetching
    - Empty state: "No emails found" with Mail icon
    - Stats row at top: total sent, failed count, bounce rate percentage

    **2. Create `src/app/(admin)/admin/emails/loading.tsx`:**
    - Import RouteLoading with message "Loading email log..."

    **3. Create `src/app/(admin)/admin/emails/error.tsx`:**
    - Import RouteError with context "email-log"

    **4. Create `src/app/(admin)/admin/orders/[id]/EmailHistory.tsx`:**
    - 'use client' directive
    - Props: { orderId: string }
    - Fetches email log filtered by orderId: GET /api/admin/emails?orderId={orderId}
    - Renders compact list of emails for this order:
      - Each row: type icon, subject, status badge, timestamp
      - Expandable detail on click showing: recipient, resend_id, error_message if failed, status timeline (populated by Resend webhook events from Plan 06)
      - "Resend" button on failed emails
      - "Send Email" button at bottom (manual trigger) -> opens dropdown to select type, calls /api/admin/emails/send
    - Empty state: "No emails sent for this order"
    - This component is designed to be embedded in the admin order detail page

    **Integration note:** The admin order detail page (`src/app/(admin)/admin/orders/[id]/page.tsx`) does NOT exist yet. Create EmailHistory as a standalone component with a clear `{ orderId: string }` prop interface. Do NOT create the order detail page in this plan. Instead, add a code comment at the top of EmailHistory.tsx: `// Integration: Import and render <EmailHistory orderId={order.id} /> in the admin order detail page when it is created.` This serves as a checkpoint for future integration when the order detail page is built.

  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm build` succeeds
    - All 4 files exist
    - Email log page has filter bar, results table, pagination
    - EmailHistory component is self-contained with orderId prop
    - loading.tsx and error.tsx follow existing patterns
  </verify>
  <done>
    Admin email log page with search/filter/pagination, per-order email history component, status badges, resend capability, and manual trigger
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` succeeds
- `pnpm lint` passes
- Admin settings has email section
- Email log page renders at /admin/emails
- EmailHistory component is standalone and embeddable
</verification>

<success_criteria>
Admin has full email management UI: kill switch + test emails in settings, global email log with filtering/search/pagination, per-order email history, resend capability for failed emails, and manual trigger for any email type.
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-08-SUMMARY.md`
</output>
