---
phase: 54-email-system
plan: 03
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - src/emails/OrderConfirmation.tsx
  - src/emails/OrderCancellation.tsx
autonomous: true

must_haves:
  truths:
    - "OrderConfirmation renders branded email with items, totals, delivery window, address, order number, and CTAs"
    - "OrderConfirmation includes Mingalabar greeting, food emoji subject line, clickable order number"
    - "OrderConfirmation shows order status tracker at Confirmed step"
    - "OrderCancellation renders warm/apologetic tone with order summary, reason, refund status, and Place New Order CTA"
  artifacts:
    - path: "src/emails/OrderConfirmation.tsx"
      provides: "MAIL-01 order confirmation email template"
      exports: ["OrderConfirmation"]
    - path: "src/emails/OrderCancellation.tsx"
      provides: "MAIL-02 cancellation email template"
      exports: ["OrderCancellation"]
  key_links:
    - from: "src/emails/OrderConfirmation.tsx"
      to: "src/emails/components/EmailLayout.tsx"
      via: "EmailLayout wrapper"
      pattern: "EmailLayout"
    - from: "src/emails/OrderConfirmation.tsx"
      to: "src/emails/components/OrderItemsTable.tsx"
      via: "OrderItemsTable for item rendering"
      pattern: "OrderItemsTable"
    - from: "src/emails/OrderCancellation.tsx"
      to: "src/emails/components/EmailLayout.tsx"
      via: "EmailLayout wrapper"
      pattern: "EmailLayout"
---

<objective>
Build Order Confirmation (MAIL-01) and Order Cancellation (MAIL-02) email templates using React Email components.

Purpose: These are the two most common transactional emails — confirmation fires on every order, cancellation when customers or admins cancel.
Output: Two fully branded email template components
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-RESEARCH.md
@.planning/phases/54-email-system/54-CONTEXT.md
@.planning/phases/54-email-system/54-01-SUMMARY.md
@.planning/phases/54-email-system/54-02-SUMMARY.md
@src/emails/components/EmailLayout.tsx
@src/emails/components/OrderItemsTable.tsx
@src/emails/components/OrderStatusTracker.tsx
@src/emails/components/DeliveryBlock.tsx
@src/emails/fixtures.ts
@supabase/functions/send-order-confirmation/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order Confirmation email template (MAIL-01)</name>
  <files>src/emails/OrderConfirmation.tsx</files>
  <action>
    Create `src/emails/OrderConfirmation.tsx` as a React Email component:

    **Props interface (OrderConfirmationProps):**
    - customerName: string
    - orderId: string
    - items: Array<{ name, quantity, lineTotalCents, category?, modifiers?: Array<{name, priceDelta?}> }>
    - subtotalCents: number
    - deliveryFeeCents: number
    - taxCents: number
    - tipCents?: number (show only if provided and > 0)
    - totalCents: number
    - deliveryWindowStart?: string (ISO)
    - deliveryWindowEnd?: string (ISO)
    - address: { line1, line2?, city, state, postalCode }
    - specialInstructions?: string
    - deliveryInstructions?: string
    - driverName?: string
    - paymentMethod?: string (e.g., "Visa ending 4242")
    - dietaryRestrictions?: string[]
    - placedAt: string (ISO)

    **Template structure (top to bottom):**
    1. EmailLayout wrapper with emailType='confirmation', previewText: "Mingalabar! Your order #{orderId short} is confirmed"
    2. Greeting: "Mingalabar! {customerName}," in warm brown, Georgia/serif heading
    3. Confirmation message: "Thank you for your order! We're excited to prepare your delicious Burmese meal."
    4. OrderStatusTracker at step 'confirmed'
    5. Order details box (gray bg): Order # (clickable link to APP_URL/orders/{orderId}, display first 8 chars uppercase), Placed date
    6. DeliveryBlock component with address, window, instructions, driver
    7. Dietary restrictions callout (amber bg) if dietaryRestrictions provided and non-empty
    8. OrderItemsTable with all items (grouped by category)
    9. Special instructions callout (amber bg) if provided
    10. Totals section: subtotal, delivery fee (show "FREE" if 0 in green), tax, tip (if provided), total (bold, larger, dark-brown color)
    11. Payment method info if provided
    12. Primary CTA: "View Your Order" button linking to APP_URL/orders/{orderId} (brand-gold bg, white text, rounded-lg, 14px 32px padding)
    13. Secondary CTA: "Reorder" link (text link, not button)
    14. "You might also like" section: 3 placeholder popular items (Mohinga, Tea Leaf Salad, Samosa) with names only
    15. "Need help?" support section: reply to email or contact support@mandalaymorningstar.com

    **Styling per locked decisions:**
    - Food emoji in preview text
    - Warm/food-forward palette throughout
    - Mobile-first: single column, full-width on mobile
    - Web-safe fonts: Georgia for headings, system sans-serif for body
    - Dark mode meta tags in EmailLayout handle color scheme
    - Button uses bg-[#D4A017] matching existing Edge Function
    - All prices formatted as $X.XX from cents
    - Date formatted: "Saturday, February 15, 2026"
    - Time formatted: "10:00 AM - 12:00 PM"

    Export as named + default export.

  </action>
  <verify>
    - `pnpm typecheck` passes
    - File exports OrderConfirmation component
    - Component renders with fixture data (no runtime errors when imported)
  </verify>
  <done>
    Order confirmation email template renders branded receipt with items, totals, delivery info, status tracker, CTAs, and support section
  </done>
</task>

<task type="auto">
  <name>Task 2: Order Cancellation email template (MAIL-02)</name>
  <files>src/emails/OrderCancellation.tsx</files>
  <action>
    Create `src/emails/OrderCancellation.tsx` as a React Email component:

    **Props interface (OrderCancellationProps):**
    - customerName: string
    - orderId: string
    - items: Array<{ name, quantity, lineTotalCents }> (simplified, no modifiers needed)
    - totalCents: number
    - cancellationReason?: string
    - cancelledAt: string (ISO)
    - refundIssued: boolean
    - refundAmountCents?: number
    - refundMethod?: string
    - refundTimeline?: string (e.g., "3-5 business days")

    **Template structure (per locked decisions — warm & apologetic tone):**
    1. EmailLayout wrapper with emailType='cancellation', previewText: "Your order #{orderId short} has been cancelled"
    2. Greeting: "Dear {customerName}," in warm tone
    3. Apologetic message: "We're sorry to see this order go. Your order has been cancelled." (warm & apologetic per CONTEXT)
    4. OrderStatusTracker not shown (cancelled is not in the status flow)
    5. Cancellation details box (light red/pink bg): Order # (clickable), cancelled date, cancellation reason if provided
    6. Order summary: simplified item list (name + quantity only, no modifiers), total
    7. Refund status section:
       - If refundIssued: "A refund of ${refundAmountCents/100} will be returned to your {refundMethod} within {refundTimeline}." in green text with checkmark
       - If not refundIssued: "No refund has been issued for this order. If you believe this is an error, please contact us." in neutral tone
    8. Primary CTA: "Place a New Order" button linking to APP_URL/menu (brand-gold bg)
    9. "Need help?" support section

    **Tone per locked decisions:**
    - "We're sorry to see this order go"
    - Warm, empathetic, not corporate
    - Still branded and on-palette (warm bg, brand colors)
    - No food excitement (unlike confirmation)

    Export as named + default export.

  </action>
  <verify>
    - `pnpm typecheck` passes
    - File exports OrderCancellation component
    - Component accepts refundIssued=true and refundIssued=false without errors
  </verify>
  <done>
    Cancellation email template renders apologetic branded email with order summary, cancellation reason, refund status, and Place New Order CTA
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Both template files exist in src/emails/
- Each template uses EmailLayout wrapper and shared components
- OrderConfirmation includes all receipt fields per MAIL-01
- OrderCancellation includes refund status per MAIL-02
</verification>

<success_criteria>
Two complete email templates that compose from shared components, follow locked branding decisions (warm palette, Mingalabar greeting, food-forward style, apologetic cancellation tone), and are ready for integration with sendEmail().
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-03-SUMMARY.md`
</output>
