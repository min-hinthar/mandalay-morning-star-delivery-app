---
phase: 100-admin-route-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/DragReorderList/index.tsx
  - src/components/ui/DragReorderList/DragReorderList.tsx
  - src/components/ui/DragReorderList/SortableItem.tsx
  - src/components/ui/DragReorderList/DragHandle.tsx
  - src/components/ui/DragReorderList/MoveButtons.tsx
  - src/components/ui/admin/routes/RouteStopCard/index.tsx
  - src/components/ui/admin/routes/RouteStopCard/RouteStopCard.tsx
  - src/components/ui/admin/routes/RouteStopCard/StopCardContent.tsx
  - src/components/ui/admin/routes/RouteStopCard/StopCardActions.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteActionsMenu.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
  - src/components/ui/admin/routes/RouteStopCard.tsx
  - src/components/ui/admin/routes/StopsList.tsx
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02

must_haves:
  truths:
    - "DragReorderList component renders a sortable list with drag handles on desktop and move buttons on mobile"
    - "RouteStopCard is extracted into subfolder with barrel export and no behavior changes"
    - "RouteHeader has a three-dot Actions dropdown shell (empty items, ready for split/merge/delete)"
  artifacts:
    - path: "src/components/ui/DragReorderList/DragReorderList.tsx"
      provides: "Generic drag-reorder list component"
      exports: ["DragReorderList"]
    - path: "src/components/ui/DragReorderList/DragHandle.tsx"
      provides: "6-dot grip drag handle for desktop"
      exports: ["DragHandle"]
    - path: "src/components/ui/DragReorderList/MoveButtons.tsx"
      provides: "Up/down chevron buttons for mobile"
      exports: ["MoveButtons"]
    - path: "src/components/ui/DragReorderList/SortableItem.tsx"
      provides: "Sortable wrapper with useSortable hook"
      exports: ["SortableItem"]
    - path: "src/components/ui/admin/routes/RouteStopCard/index.tsx"
      provides: "Barrel re-export of RouteStopCard"
      exports: ["RouteStopCard"]
    - path: "src/components/ui/admin/routes/RouteDetailClient/RouteActionsMenu.tsx"
      provides: "Three-dot dropdown shell"
      exports: ["RouteActionsMenu"]
  key_links:
    - from: "src/components/ui/DragReorderList/DragReorderList.tsx"
      to: "src/components/ui/DragReorderList/SortableItem.tsx"
      via: "renders SortableItem for each item"
      pattern: "SortableItem"
    - from: "src/components/ui/admin/routes/RouteStopCard/index.tsx"
      to: "src/components/ui/admin/routes/RouteStopCard/RouteStopCard.tsx"
      via: "barrel re-export"
      pattern: "export.*RouteStopCard"
    - from: "src/components/ui/admin/routes/StopsList.tsx"
      to: "src/components/ui/admin/routes/RouteStopCard/index.tsx"
      via: "import RouteStopCard"
      pattern: "from.*RouteStopCard"
---

<objective>
Install @dnd-kit, extract RouteStopCard into subfolder, build DragReorderList generic component, and add Actions dropdown shell to RouteHeader.

Purpose: Infrastructure for drag-reorder (ROUTE-01/02) and split/merge/delete actions (ROUTE-03/04/05). No behavior changes to existing UI -- pure extraction and new component creation.

Output: @dnd-kit installed, DragReorderList ready for wiring, RouteStopCard extracted, Actions dropdown shell in RouteHeader.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-admin-route-editing/100-CONTEXT.md
@.planning/phases/100-admin-route-editing/100-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From src/components/ui/admin/routes/RouteDetailClient/types.ts:
```typescript
export interface RouteDetailResponse {
  id: string;
  deliveryDate: string;
  status: RouteStatus;
  optimizedPolyline: string | null;
  stats: RouteStats | null;
  startedAt: string | null;
  completedAt: string | null;
  driver: DriverListItem | null;
  stops: StopDetail[];
}
export interface DriverOption { id: string; userId: string; fullName: string | null; isActive: boolean; availability?: DriverAvailability | null; }
```

From src/components/ui/admin/routes/RouteStopCard.tsx (line 35-43):
```typescript
interface RouteStopCardProps {
  stop: StopDetail;
  index: number;
  routeStatus: RouteStatus;
  onStatusChange: (stopId: string, status: RouteStopStatus) => void;
  onRemoveStop: (stopId: string) => void;
  availableRoutes?: AvailableRoute[];
  onReassign?: (stopId: string, targetRouteId: string) => void;
}
```

From src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx (line 50-60):
```typescript
interface RouteHeaderProps {
  route: RouteDetailResponse;
  routeId: string;
  isUpdating: boolean;
  isManuallyReordered: boolean;
  onStatusChange: (status: RouteStatus) => void;
  onOptimize: () => void;
  onAddStops: () => void;
  onRefresh: () => void;
  onBack: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit, extract RouteStopCard subfolder, build DragReorderList</name>
  <files>
    package.json,
    src/components/ui/DragReorderList/index.tsx,
    src/components/ui/DragReorderList/DragReorderList.tsx,
    src/components/ui/DragReorderList/SortableItem.tsx,
    src/components/ui/DragReorderList/DragHandle.tsx,
    src/components/ui/DragReorderList/MoveButtons.tsx,
    src/components/ui/admin/routes/RouteStopCard/index.tsx,
    src/components/ui/admin/routes/RouteStopCard/RouteStopCard.tsx,
    src/components/ui/admin/routes/RouteStopCard/StopCardContent.tsx,
    src/components/ui/admin/routes/RouteStopCard/StopCardActions.tsx,
    src/components/ui/admin/routes/RouteStopCard.tsx,
    src/components/ui/admin/routes/StopsList.tsx
  </files>
  <action>
    **Step 1: Install @dnd-kit packages.**
    ```bash
    pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
    ```

    **Step 2: Build DragReorderList generic component** at `src/components/ui/DragReorderList/`.

    DragReorderList.tsx — Generic reusable component. Use the RESEARCH.md code examples as the implementation reference. Key details:
    - Props: `items: T[], onReorder: (items: T[]) => void, renderItem: (item: T, isDragging: boolean) => ReactNode, renderOverlay: (item: T) => ReactNode, getItemId: (item: T) => string, disabled?: boolean`
    - DndContext with closestCenter collision detection
    - PointerSensor with activationConstraint: { distance: 8 }
    - KeyboardSensor with sortableKeyboardCoordinates
    - SortableContext with verticalListSortingStrategy
    - DragOverlay with dropAnimation: { duration: 200, easing: 'ease' }
    - When disabled=true, render items without DndContext (plain list)
    - `'use client'` directive

    SortableItem.tsx — Wraps children with useSortable. Passes listeners/attributes to children via context or render prop. Style: transform via CSS.Transform.toString, transition from useSortable, opacity 0.4 when isDragging (ghost effect). Sets dashed border-2 border-dashed border-border-v5 when isDragging for placeholder effect. `'use client'`

    DragHandle.tsx — 6-dot grip icon (GripVertical from lucide-react). Hidden below md: breakpoint (`hidden md:flex`). CSS `touch-none` on the button. Receives listeners and attributes props from parent SortableItem. aria-label="Drag to reorder". `'use client'`

    MoveButtons.tsx — Up/down chevrons (ChevronUp, ChevronDown from lucide-react). Visible only below md: (`flex md:hidden`). 44px touch targets (h-11 w-11 className). Props: onMoveUp, onMoveDown, isFirst, isLast. Disabled state on first/last. Uses Button from @/components/ui/button with variant="ghost" size="icon-sm". `'use client'`

    index.tsx — Barrel export: `export { DragReorderList } from './DragReorderList'; export { SortableItem } from './SortableItem'; export { DragHandle } from './DragHandle'; export { MoveButtons } from './MoveButtons';`

    **Step 3: Extract RouteStopCard into subfolder** at `src/components/ui/admin/routes/RouteStopCard/`.

    Read the existing RouteStopCard.tsx (317 lines). Split into:
    - StopCardContent.tsx — Display-only portion: customer info (name, phone, address), order summary (total, item count), timestamps (arrivedAt, deliveredAt), delivery photo, delivery notes, special instructions. All the JSX that renders stop data. `'use client'` (uses Image from next/image).
    - StopCardActions.tsx — Interactive portion: status dropdown, reassign stop dropdown, remove button, ConfirmDialog for removal. All the event handlers and dropdowns. `'use client'`
    - RouteStopCard.tsx — Orchestrator: renders StopCardContent + StopCardActions inside a card container. Accepts same RouteStopCardProps interface. Keep m.div animation wrapper. `'use client'`
    - index.tsx — Barrel: `export { RouteStopCard } from './RouteStopCard'; export type { RouteStopCardProps } from './RouteStopCard';` (also export the type)

    **CRITICAL:** Delete the OLD RouteStopCard.tsx file at `src/components/ui/admin/routes/RouteStopCard.tsx` after creating the subfolder. The import from StopsList.tsx (`from "./RouteStopCard"`) will resolve to the subfolder's index.tsx automatically.

    **Step 4: Update StopsList.tsx import** if needed. The import `from "./RouteStopCard"` should auto-resolve to the new subfolder barrel. Verify no changes needed, but if the import path uses file extension, update it.

    ALL extracted files MUST preserve the exact same behavior and props. This is a pure refactor -- no functional changes.
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>
    - @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities installed in package.json
    - DragReorderList component exists with DragHandle, MoveButtons, SortableItem subcomponents
    - RouteStopCard extracted into subfolder with StopCardContent + StopCardActions
    - Old RouteStopCard.tsx deleted, StopsList imports resolve correctly
    - Build passes with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add RouteActionsMenu dropdown shell to RouteHeader</name>
  <files>
    src/components/ui/admin/routes/RouteDetailClient/RouteActionsMenu.tsx,
    src/components/ui/admin/routes/RouteDetailClient/RouteHeader.tsx
  </files>
  <action>
    **Create RouteActionsMenu.tsx** — A three-dot (MoreVertical icon) Radix DropdownMenu component.
    - `'use client'` directive
    - Props: `routeStatus: RouteStatus, pendingStopCount: number, hasSameDatePlannedRoutes: boolean, onSplit: () => void, onMerge: () => void, onDelete: () => void`
    - Trigger: MoreVertical icon inside a Button with variant="ghost" size="icon", 44px touch target via padding (p-2.5 or h-11 w-11), aria-label="Route actions"
    - Items (contextually filtered per CONTEXT.md -- hide unavailable, don't disable):
      - "Split Route" with Scissors icon — shown when `routeStatus !== 'completed' && pendingStopCount > 1`
      - "Merge Route" with GitMerge icon — shown when `routeStatus !== 'completed' && routeStatus !== 'in_progress' && hasSameDatePlannedRoutes` (merge FROM blocked on in_progress per CONTEXT.md, but merge INTO is allowed -- this menu is for actions ON this route, so "Merge Route" means "absorb another route into this one": show when `routeStatus !== 'completed' && hasSameDatePlannedRoutes`)
      - DropdownMenuSeparator
      - "Delete Route" with Trash2 icon in text-destructive color — shown when `routeStatus !== 'in_progress' && routeStatus !== 'completed'` (only on planned routes)
    - If no items would show (completed route), don't render the trigger at all
    - Use existing DropdownMenu components from @/components/ui/dropdown-menu

    **Update RouteHeader.tsx** — Add RouteActionsMenu next to the existing buttons.
    - Import RouteActionsMenu
    - Add new props to RouteHeaderProps: `pendingStopCount: number, hasSameDatePlannedRoutes: boolean, onSplit: () => void, onMerge: () => void, onDelete: () => void`
    - Render RouteActionsMenu after the Refresh button, passing the relevant props
    - No other changes to RouteHeader

    NOTE: The onSplit/onMerge/onDelete callbacks will be no-ops initially. RouteDetailClient.tsx will wire them in Plan 100-04. For now just pass the props through.
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>
    - RouteActionsMenu.tsx exists with three-dot DropdownMenu, contextually filtered items
    - RouteHeader renders RouteActionsMenu with new props
    - Completed routes show no actions dropdown
    - Build passes
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes (all imports resolve, types match)
- `pnpm lint` passes (all files under 400 lines, no lint errors)
- `pnpm build` succeeds (no SSR issues, no missing imports)
- DragReorderList component files exist in src/components/ui/DragReorderList/
- RouteStopCard subfolder exists with barrel export
- Old RouteStopCard.tsx removed
- RouteActionsMenu renders in RouteHeader
</verification>

<success_criteria>
- @dnd-kit packages installed and importable
- DragReorderList is a complete generic component ready to accept items for drag reorder
- RouteStopCard extracted cleanly -- existing route detail page renders identically
- Actions dropdown shell visible on non-completed routes with correct item filtering
</success_criteria>

<output>
After completion, create `.planning/phases/100-admin-route-editing/100-01-SUMMARY.md`
</output>
