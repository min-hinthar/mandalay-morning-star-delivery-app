---
phase: 100-admin-route-editing
plan: 04
type: execute
wave: 3
depends_on: ["100-02", "100-03"]
files_modified:
  - src/components/ui/admin/routes/route-selection-utils.ts
  - src/components/ui/admin/routes/__tests__/route-selection.test.ts
  - src/lib/hooks/useSplitRoute.ts
  - src/lib/hooks/useMergeRoutes.ts
  - src/lib/hooks/useRouteActions.ts
  - src/lib/hooks/__tests__/useSplitRoute.test.ts
  - src/lib/hooks/__tests__/useMergeRoutes.test.ts
  - src/components/ui/admin/routes/RouteDetailClient/SplitRouteModal.tsx
  - src/components/ui/admin/routes/RouteDetailClient/MergeRouteModal.tsx
  - src/components/ui/admin/routes/RouteStopCard/RouteStopCard.tsx
  - src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
  - src/components/ui/admin/routes/StopsList.tsx
autonomous: true
requirements:
  - ROUTE-03
  - ROUTE-04

must_haves:
  truths:
    - "Admin clicks 'Split Route' in Actions dropdown, enters selection mode with checkboxes on each stop card inline"
    - "Admin selects stops via checkboxes, clicks confirm to open SplitRouteModal with driver picker and stop summary"
    - "Admin can merge a same-date planned route into the current route"
    - "Split preserves at least 1 stop in source route"
    - "Only pending stops are selectable for split on in_progress routes"
    - "Merged route is deleted, stops appended to destination"
    - "After split: success toast with link to new route"
    - "After merge: success toast suggesting optimization"
  artifacts:
    - path: "src/components/ui/admin/routes/route-selection-utils.ts"
      provides: "Pure selection logic functions (toggle, selectAll, validate, getSelectable)"
      exports: ["toggleStopSelection", "selectAllStops", "deselectAllStops", "validateSplitSelection", "getSelectableStops"]
    - path: "src/components/ui/admin/routes/RouteDetailClient/SplitRouteModal.tsx"
      provides: "Split confirmation modal with driver picker and selected stop summary"
      exports: ["SplitRouteModal"]
    - path: "src/components/ui/admin/routes/RouteDetailClient/MergeRouteModal.tsx"
      provides: "Merge route modal with route picker"
      exports: ["MergeRouteModal"]
    - path: "src/lib/hooks/useSplitRoute.ts"
      provides: "Split route mutation hook"
      exports: ["useSplitRoute"]
    - path: "src/lib/hooks/useMergeRoutes.ts"
      provides: "Merge routes mutation hook"
      exports: ["useMergeRoutes"]
    - path: "src/lib/hooks/useRouteActions.ts"
      provides: "Split/merge/delete state management extracted from RouteDetailClient"
      exports: ["useRouteActions"]
  key_links:
    - from: "src/lib/hooks/useSplitRoute.ts"
      to: "/api/admin/routes/[id]/split"
      via: "POST fetch"
      pattern: "fetch.*split"
    - from: "src/lib/hooks/useMergeRoutes.ts"
      to: "/api/admin/routes/[id]/merge"
      via: "POST fetch"
      pattern: "fetch.*merge"
    - from: "src/components/ui/admin/routes/RouteDetailClient/SplitRouteModal.tsx"
      to: "src/components/ui/admin/routes/route-selection-utils.ts"
      via: "imports validation helper"
      pattern: "from.*route-selection-utils"
    - from: "src/components/ui/admin/routes/__tests__/route-selection.test.ts"
      to: "src/components/ui/admin/routes/route-selection-utils.ts"
      via: "imports selection functions for testing"
      pattern: "from.*route-selection-utils"
    - from: "src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx"
      to: "src/lib/hooks/useRouteActions.ts"
      via: "imports useRouteActions for split/merge/delete state"
      pattern: "useRouteActions"
    - from: "src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx"
      to: "src/components/ui/admin/routes/RouteDetailClient/SplitRouteModal.tsx"
      via: "renders SplitRouteModal when split confirmed"
      pattern: "SplitRouteModal"
    - from: "src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx"
      to: "src/components/ui/admin/routes/RouteDetailClient/MergeRouteModal.tsx"
      via: "renders MergeRouteModal when merge triggered"
      pattern: "MergeRouteModal"
    - from: "src/lib/hooks/useRouteActions.ts"
      to: "src/components/ui/admin/routes/route-selection-utils.ts"
      via: "imports selection logic for split flow"
      pattern: "from.*route-selection-utils"
---

<objective>
Implement split route flow (inline checkbox selection mode on stop cards, confirmation modal, API call) and merge route flow (route picker modal, API call). Extract useRouteActions hook. Wire both to RouteActionsMenu and RouteDetailClient.

Purpose: Delivers ROUTE-03 (split overloaded route) and ROUTE-04 (merge light routes). These are the route composition editing capabilities that let admins rebalance workload on Saturday ops.

Output: Working split and merge flows with inline selection mode, modals, mutation hooks, useRouteActions extraction, and proper toast feedback.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-admin-route-editing/100-CONTEXT.md
@.planning/phases/100-admin-route-editing/100-RESEARCH.md
@.planning/phases/100-admin-route-editing/100-01-SUMMARY.md
@.planning/phases/100-admin-route-editing/100-02-SUMMARY.md
@.planning/phases/100-admin-route-editing/100-03-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 100-02 -->
```typescript
// POST /api/admin/routes/[id]/split
// Body: { stopIds: string[], driverId?: string }
// Response: { newRouteId: string }

// POST /api/admin/routes/[id]/merge
// Body: { sourceRouteId: string }
// Response: { totalStops: number }
```

<!-- RouteActionsMenu from Plan 100-01 -->
```typescript
// Props: routeStatus, pendingStopCount, hasSameDatePlannedRoutes, onSplit, onMerge, onDelete
```

<!-- RouteStopCard from Plan 100-01/03 -->
```typescript
interface RouteStopCardProps {
  stop: StopDetail;
  index: number;
  routeStatus: RouteStatus;
  // ... existing props
  dragHandle?: React.ReactNode;
  moveButtons?: React.ReactNode;
  // NEW for selection mode:
  selectionMode?: boolean;
  selected?: boolean;
  onToggleSelect?: (stopId: string) => void;
  selectionDisabled?: boolean; // non-pending stops on in_progress routes
}
```

<!-- DriverOption type -->
```typescript
interface DriverOption { id: string; userId: string; fullName: string | null; isActive: boolean; }
```

<!-- Route list API for same-date routes -->
```typescript
// GET /api/admin/routes?date={YYYY-MM-DD} returns RouteListItem[]
// RouteListItem has: id, deliveryDate, status, driver, stopCount
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSplitRoute, useMergeRoutes hooks, route-selection-utils, and tests</name>
  <files>
    src/components/ui/admin/routes/route-selection-utils.ts,
    src/components/ui/admin/routes/__tests__/route-selection.test.ts,
    src/lib/hooks/useSplitRoute.ts,
    src/lib/hooks/useMergeRoutes.ts,
    src/lib/hooks/__tests__/useSplitRoute.test.ts,
    src/lib/hooks/__tests__/useMergeRoutes.test.ts
  </files>
  <behavior>
    useSplitRoute:
    - splitRoute(routeId, stopIds, driverId?) fires POST to /api/admin/routes/[routeId]/split
    - Returns { splitRoute, isSplitting }
    - On success: shows toast with "View new route" link, calls onSuccess callback
    - On error: shows error toast, modal stays open
    - Rejects if stopIds is empty

    useMergeRoutes:
    - mergeRoutes(destinationRouteId, sourceRouteId) fires POST to /api/admin/routes/[destinationRouteId]/merge
    - Returns { mergeRoutes, isMerging }
    - On success: shows toast with "Optimize" suggestion, calls onSuccess callback
    - On error: shows error toast, modal stays open

    Selection logic (pure functions in route-selection-utils.ts):
    - toggleStopSelection(selectedIds: Set, stopId: string) returns new Set with toggled ID
    - selectAllStops(stops: StopDetail[], routeStatus: RouteStatus) returns Set of selectable stop IDs (pending only for in_progress)
    - deselectAllStops() returns empty Set
    - validateSplitSelection(selectedIds: Set, totalStopCount: number) returns { valid: boolean, error?: string } -- invalid if selectedIds.size >= totalStopCount (must leave at least 1)
    - getSelectableStops(stops: StopDetail[], routeStatus: RouteStatus) returns stop IDs that are selectable (all for planned, pending only for in_progress)
  </behavior>
  <action>
    **Write tests first (TDD).**

    **Create route-selection-utils.ts** at `src/components/ui/admin/routes/route-selection-utils.ts`:
    - Pure functions, no React imports needed
    - toggleStopSelection: clone Set, has(id) ? delete : add, return new Set
    - selectAllStops: filter stops by getSelectableStops, return as Set
    - deselectAllStops: return new Set()
    - validateSplitSelection: if selectedIds.size === 0 return { valid: false, error: "Select at least one stop" }; if selectedIds.size >= totalStopCount return { valid: false, error: "At least one stop must remain" }; else { valid: true }
    - getSelectableStops: if routeStatus === 'in_progress', return only stops with status 'pending'; else return all stop IDs

    **route-selection.test.ts** at `src/components/ui/admin/routes/__tests__/`:
    - Pure function tests (no React hooks needed)
    - Test toggleStopSelection adds/removes correctly
    - Test selectAllStops returns only pending stops for in_progress routes
    - Test validateSplitSelection rejects when all stops selected
    - Test validateSplitSelection rejects when 0 selected
    - Test validateSplitSelection accepts when at least 1 remains
    - Test getSelectableStops filters by status

    **useSplitRoute.ts:**
    - Fires POST fetch to `/api/admin/routes/${routeId}/split` with { stopIds, driverId }
    - On 200: extract newRouteId from response, show toast.success with message including "View new route" text, call onSuccess(newRouteId)
    - On error: toast.error, do NOT close modal (caller controls modal state)

    **useMergeRoutes.ts:**
    - Fires POST fetch to `/api/admin/routes/${destinationRouteId}/merge` with { sourceRouteId }
    - On 200: extract totalStops, show toast with "Merged successfully. Route may need reordering." and an Optimize action if onOptimize callback provided, call onSuccess(totalStops)
    - On error: toast.error

    **Hook tests:** Mock fetch globally. Test the hook behavior by calling the returned functions and asserting on fetch calls, state changes, and callback invocations. Use vitest's vi.fn() for callbacks. Tests are written BEFORE implementation code within the same task (test-first TDD pattern).
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm vitest run src/lib/hooks/__tests__/useSplitRoute.test.ts src/lib/hooks/__tests__/useMergeRoutes.test.ts src/components/ui/admin/routes/__tests__/route-selection.test.ts -x</automated>
  </verify>
  <done>
    - route-selection-utils.ts exports all 5 pure functions
    - useSplitRoute and useMergeRoutes hooks fire correct API calls with proper payloads
    - Selection utility functions handle toggle, select all, validate, and getSelectable correctly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Selection mode on RouteStopCard, SplitRouteModal, MergeRouteModal, useRouteActions, wire to RouteDetailClient</name>
  <files>
    src/components/ui/admin/routes/RouteStopCard/RouteStopCard.tsx,
    src/components/ui/admin/routes/StopsList.tsx,
    src/components/ui/admin/routes/RouteDetailClient/SplitRouteModal.tsx,
    src/components/ui/admin/routes/RouteDetailClient/MergeRouteModal.tsx,
    src/lib/hooks/useRouteActions.ts,
    src/components/ui/admin/routes/RouteDetailClient/RouteDetailClient.tsx
  </files>
  <action>
    **Update RouteStopCard.tsx** -- Add inline selection mode rendering per user decision.
    - New optional props: `selectionMode?: boolean, selected?: boolean, onToggleSelect?: (stopId: string) => void, selectionDisabled?: boolean`
    - When selectionMode=true: render SIMPLIFIED card layout -- checkbox on left + stop number + customer name + address. Hide StopCardActions, hide drag handle, hide move buttons. This is the locked user decision: "checkboxes appear on each stop card" with "simplified card layout (checkbox + stop number + customer name + address, action buttons hidden)".
    - Checkbox: use Checkbox from @/components/ui/checkbox, checked={selected}, onChange calls onToggleSelect(stop.id)
    - When selectionDisabled=true (non-pending on in_progress): checkbox disabled, row grayed out (opacity-50)
    - When selectionMode=false: render normal card with drag/move as before

    **Update StopsList.tsx** -- Add selection mode support.
    - New props: `selectionMode?: boolean, selectedStopIds?: Set<string>, onToggleSelect?: (stopId: string) => void, routeStatus: RouteStatus`
    - When selectionMode=true: render "Select All / Deselect All" toggle button at top with "{N} selected" count
    - Pass selectionMode, selected, onToggleSelect, selectionDisabled to each RouteStopCard
    - selectionDisabled = true for non-pending stops when routeStatus is 'in_progress'
    - When selectionMode=true: disable DragReorderList (no dragging during selection)

    **Create SplitRouteModal.tsx** -- Confirmation modal AFTER stops are already selected inline. `'use client'`
    - Props: `open: boolean, onOpenChange: (open: boolean) => void, selectedStops: StopDetail[], routeId: string, drivers: DriverOption[], onSuccess: (newRouteId: string) => void`
    - Uses Dialog from @/components/ui/dialog
    - Content:
      1. Header: "Split Route" with subtitle "{N} stops selected"
      2. Summary list of selected stops (read-only): stop number + customer name + address
      3. Driver picker: optional Select dropdown with driver options. Label "Assign driver (optional)"
      4. Footer: Cancel button + "Split {N} Stops" primary button
    - On confirm: call useSplitRoute.splitRoute(routeId, selectedStopIds, selectedDriverId)
    - Loading state: button shows spinner + disabled during split
    - On success: close modal, call onSuccess
    - On error: keep modal open (hook shows toast)
    - Import validateSplitSelection from route-selection-utils.ts for any last-mile validation

    **Create MergeRouteModal.tsx** -- Merge flow modal. `'use client'`
    - Props: `open: boolean, onOpenChange: (open: boolean) => void, routeId: string, availableRoutes: { id: string, driverName: string | null, stopCount: number, status: string }[], onSuccess: (totalStops: number) => void, onOptimize: () => void`
    - Receives availableRoutes as prop (RouteDetailClient fetches and filters)
    - Content:
      1. Header: "Merge Route" with subtitle "Select a route to merge into this one"
      2. Radio button list of available routes (same-date planned, excluding current)
      3. Each radio: short route ID, driver name, stop count, status badge
      4. Footer: Cancel + "Merge Route" primary button
      5. Merge button disabled when no route selected
    - On confirm: call useMergeRoutes.mergeRoutes(routeId, selectedSourceRouteId)
    - Loading state on button during merge
    - On success: close modal, call onSuccess
    - On error: keep modal open

    **Create useRouteActions.ts** -- MANDATORY extraction to keep RouteDetailClient under 400 lines. `src/lib/hooks/useRouteActions.ts`
    - Encapsulates: selectionMode state, selectedStopIds Set state, showSplitModal, showMergeModal, showDeleteConfirm
    - Selection handlers: enterSelectionMode(), exitSelectionMode(), toggleStopSelect(stopId), selectAll(stops, routeStatus), deselectAll()
    - Split flow: confirmSplit() opens SplitRouteModal (sets showSplitModal=true), onSplitSuccess(newRouteId) exits selection mode + closes modal
    - Merge flow: openMerge() sets showMergeModal=true, onMergeSuccess(totalStops) closes modal
    - Delete flow: openDelete() sets showDeleteConfirm=true, confirmDelete(routeId) fires DELETE /api/admin/routes/[id], cancelDelete() closes confirm
    - Import selection utils from route-selection-utils.ts
    - Returns all state + handlers as a flat object

    **Update RouteDetailClient.tsx** -- Wire everything using useRouteActions.
    - Import useRouteActions hook
    - Compute availableRoutes for merge: on mount, fetch GET /api/admin/routes?date={route.deliveryDate}, filter for status==='planned' and id !== current routeId. Store in state or useMemo.
    - Compute hasSameDatePlannedRoutes: availableRoutes.length > 0. Pass to RouteActionsMenu.
    - Pass pendingStopCount to RouteActionsMenu: `stops.filter(s => s.status === 'pending').length`
    - Wire RouteActionsMenu callbacks: onSplit calls routeActions.enterSelectionMode(), onMerge calls routeActions.openMerge(), onDelete calls routeActions.openDelete()
    - When selectionMode=true: show floating toolbar at bottom with "Cancel" + "Split {N} Stops" button (disabled if 0 selected or would leave 0 remaining). "Split" button calls routeActions.confirmSplit().
    - Pass selectionMode state down to StopsList
    - Render SplitRouteModal with selectedStops (filter stops by selectedStopIds)
    - Render MergeRouteModal with availableRoutes
    - Render ConfirmDialog for delete: "Delete route with {N} stops? Stops will be unassigned." On confirm calls routeActions.confirmDelete(routeId).
    - On split success: invalidate route detail query, exit selection mode
    - On merge success: invalidate route detail query + route list query
    - On delete success: navigate to /admin/routes

    **File size:** With useRouteActions extraction, RouteDetailClient.tsx adds ~30 lines of hook usage + JSX for modals/toolbar. The bulk of state logic lives in useRouteActions.ts (~120-150 lines). RouteDetailClient stays under 400 lines.
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <done>
    - RouteStopCard renders inline checkboxes in selection mode with simplified layout (per locked decision)
    - StopsList shows Select All/Deselect All toggle and selected count in selection mode
    - SplitRouteModal shows driver picker and selected stop summary (NOT checkboxes -- selection happens inline)
    - MergeRouteModal shows radio route picker with availableRoutes prop
    - useRouteActions.ts extracted with all split/merge/delete state (~120-150 lines)
    - RouteDetailClient.tsx stays under 400 lines
    - Actions dropdown triggers correct flows: Split enters selection mode, Merge opens modal, Delete opens confirm
    - hasSameDatePlannedRoutes computed from fetched route list
    - All tests pass, full verification suite passes
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lib/hooks/__tests__/useSplitRoute.test.ts src/lib/hooks/__tests__/useMergeRoutes.test.ts src/components/ui/admin/routes/__tests__/route-selection.test.ts` -- all tests pass
- `pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build` -- full suite passes
- Split flow: Actions dropdown "Split Route" -> selection mode with inline checkboxes on stop cards -> floating toolbar -> SplitRouteModal with driver picker
- Merge flow: Actions dropdown "Merge Route" -> MergeRouteModal with radio picker of same-date planned routes
- Delete: confirmation dialog
- Actions dropdown: contextually filtered by route status
- RouteDetailClient.tsx under 400 lines (useRouteActions extracted)
</verification>

<success_criteria>
- Admin can split a route: enters selection mode via Actions dropdown, selects stops with inline checkboxes, confirms in modal with optional driver
- Admin can merge a same-date planned route into current route via modal
- Admin can delete a planned route with confirmation
- Only pending stops selectable for split on in_progress routes
- At least 1 stop must remain in source route
- Success/error toasts for all operations
- Split toast includes "View new route" link
- Merge toast suggests optimization
- useRouteActions hook keeps RouteDetailClient under 400 lines
- All route editing operations are complete (ROUTE-01 through ROUTE-05)
</success_criteria>

<output>
After completion, create `.planning/phases/100-admin-route-editing/100-04-SUMMARY.md`
</output>
