---
phase: 83-driver-simplification
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/ui/driver/SimpleStopView.tsx
  - src/components/ui/driver/DeliveryConfirmDialog.tsx
  - src/app/(driver)/driver/route/page.tsx
  - src/app/(driver)/driver/route/[stopId]/page.tsx
  - src/app/(driver)/driver/page.tsx
autonomous: true
requirements: [DRV-01, DRV-02, DRV-03]

must_haves:
  truths:
    - "Simple mode route shows only the current stop (single-stop focus)"
    - "Stop shows customer name, address (tap opens Maps), phone (tap calls), and Mark Delivered button"
    - "Mark Delivered button triggers confirmation dialog with address"
    - "After delivery confirmation: success animation then auto-advance to next stop"
    - "Progress counter shows 'X of Y done'"
    - "Final stop shows 'All done!' celebration screen"
    - "Call for Help button replaces exception modal in simple mode"
  artifacts:
    - path: "src/components/ui/driver/SimpleStopView.tsx"
      provides: "Single-stop focus view for simple mode"
      exports: ["SimpleStopView"]
    - path: "src/components/ui/driver/DeliveryConfirmDialog.tsx"
      provides: "Confirmation dialog for Mark Delivered action"
      exports: ["DeliveryConfirmDialog"]
  key_links:
    - from: "src/components/ui/driver/SimpleStopView.tsx"
      to: "src/components/ui/driver/DeliveryActions.tsx"
      via: "Reuses updateStatus logic from DeliveryActions"
      pattern: "fetch.*stops.*PATCH"
    - from: "src/app/(driver)/driver/route/page.tsx"
      to: "src/components/ui/driver/SimpleStopView.tsx"
      via: "Conditional render based on useSimpleMode()"
      pattern: "isSimpleMode"
    - from: "src/app/(driver)/driver/page.tsx"
      to: "src/components/ui/driver/SimpleHome.tsx"
      via: "Conditional render based on useSimpleMode()"
      pattern: "isSimpleMode"
---

<objective>
Create SimpleStopView with single-stop focus, confirmation dialog, and wire simple mode into route and home pages.

Purpose: The core simple mode delivery experience — driver sees one stop at a time with large touch targets and delivery confirmation.
Output: SimpleStopView, DeliveryConfirmDialog, updated route page, updated stop detail page, updated home page.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/83-driver-simplification/83-01-SUMMARY.md
@src/components/ui/driver/StopDetail.tsx
@src/components/ui/driver/StopDetailView.tsx
@src/components/ui/driver/DeliveryActions.tsx
@src/components/ui/driver/NavigationButton.tsx
@src/app/(driver)/driver/route/page.tsx
@src/app/(driver)/driver/route/[stopId]/page.tsx
@src/app/(driver)/driver/page.tsx
@src/lib/hooks/useOfflineSync.ts

<interfaces>
<!-- Existing delivery action logic -->

From src/components/ui/driver/DeliveryActions.tsx:
```typescript
interface DeliveryActionsProps {
  routeId: string;
  stopId: string;
  currentStatus: RouteStopStatus;
  onStatusChange?: (newStatus: RouteStopStatus) => void;
  onQueuedOffline?: () => void;
  onException?: () => void;
  disabled?: boolean;
  testMode?: boolean;
}
// Uses fetch PATCH to /api/driver/routes/{routeId}/stops/{stopId}
// Falls back to queueStatusUpdate when offline
```

From src/components/ui/driver/StopDetailView.tsx:
```typescript
interface StopDetailViewProps {
  routeId: string;
  stopId: string;
  stopIndex: number;
  totalStops: number;
  status: RouteStopStatus;
  customer: { fullName: string | null; phone: string | null };
  address: { line1: string; line2: string | null; city: string; state: string; zipCode: string; latitude: number | null; longitude: number | null };
  timeWindow: { start: string | null; end: string | null };
  deliveryNotes: string | null;
  orderItems: OrderItem[];
}
```

Route page stop data shape:
```typescript
interface StopData {
  id: string;
  stopIndex: number;
  status: RouteStopStatus;
  eta: string | null;
  order: {
    id: string;
    deliveryWindowStart: string | null;
    deliveryWindowEnd: string | null;
    customer: { fullName: string | null };
    address: { line1: string; line2: string | null; city: string; state: string };
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeliveryConfirmDialog + SimpleStopView</name>
  <files>src/components/ui/driver/DeliveryConfirmDialog.tsx, src/components/ui/driver/SimpleStopView.tsx, src/components/ui/driver/index.ts</files>
  <action>
1. Create `src/components/ui/driver/DeliveryConfirmDialog.tsx`:
   - `'use client'` directive
   - Props: `{ isOpen: boolean; address: string; onConfirm: () => void; onCancel: () => void; isLoading?: boolean }`
   - Full-screen overlay with centered card (same pattern as ExceptionModal)
   - Content: "Mark as delivered at {address}?" in font-body text-lg
   - Two buttons in a flex row:
     * Cancel: `min-h-[56px] flex-1 border-2 border-border rounded-card-sm font-body font-medium`
     * "Yes, Delivered": `min-h-[56px] flex-1 bg-green text-text-inverse rounded-card-sm font-body font-semibold`
   - AnimatePresence for enter/exit (same spring as ExceptionModal)
   - Loading state: spinner on confirm button when isLoading=true
   - `useBodyScrollLock(isOpen)` for scroll prevention

2. Create `src/components/ui/driver/SimpleStopView.tsx`:
   - `'use client'` directive
   - Props: `{ routeId: string; stops: StopData[]; driverPhone?: string | null }` where StopData matches the route page interface (id, stopIndex, status, order with customer/address)
   - Also accept `operatorPhone?: string` (default hardcoded to env or fallback)
   - Internal state: `currentStop` = first stop with status 'pending' or 'enroute'
   - **Single-stop focus view** showing ONLY the current stop:

   A) **Progress counter** at top: "{deliveredCount} of {totalCount} done" (text-lg font-semibold, centered)
      - Thin progress bar below (h-2, bg-accent-teal, same as ActiveRouteView pattern)

   B) **Customer name**: font-display text-2xl font-bold, centered

   C) **Address card** — tappable, opens Google Maps:
      - MapPin icon + address text
      - `min-h-[56px]`, `rounded-card-sm`, `bg-surface-primary border border-border shadow-sm`
      - `onClick`: open Google Maps URL (same pattern as NavigationButton: `window.open(mapsUrl, '_blank')`)
      - Build maps URL: `https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(address)}&travelmode=driving`

   D) **Phone card** — tappable, initiates call:
      - Phone icon + phone number
      - `min-h-[56px]`, same card styling
      - `onClick`: `window.location.href = 'tel:' + phone` (same pattern as StopDetail)
      - Only render if customer phone exists

   E) **Mark Delivered button** — LARGE:
      - `min-h-[72px]` (extra large for non-tech users), `bg-green text-text-inverse`, `rounded-card-sm shadow-md`
      - Check icon + "Mark Delivered" text
      - On click: set `showConfirm = true` (do NOT directly update status)

   F) **DeliveryConfirmDialog**:
      - On confirm: call PATCH `/api/driver/routes/{routeId}/stops/{stopId}` with status 'delivered'
      - If offline: use `queueStatusUpdate` from `useOfflineSync`
      - On success: brief "Delivered!" animation (AnimatePresence, green check + text, 1.5s), then auto-advance to next stop
      - Auto-advance: update internal state to next pending stop

   G) **Call for Help button** — below Mark Delivered:
      - `min-h-[56px]`, `border-2 border-border bg-surface-primary text-text-primary`, `rounded-card-sm`
      - Phone icon + "Call for Help"
      - `onClick`: `window.location.href = 'tel:+16261234567'` (hardcode operator number; can be updated later)

   H) **All Done screen** — when no more pending stops:
      - Centered layout: large Check icon (h-16 w-16, text-green), "All done!" heading, "Great job! All deliveries complete." subtext
      - "Go Home" button (min-h-[56px], bg-accent-teal) that navigates to /driver
      - Confetti-like animation optional (Claude's discretion)

   - Use `useOfflineSync` for offline delivery marking
   - Use `useAnimationPreference` for motion control
   - Use Framer Motion `m` and `AnimatePresence` for transitions
   - Keep under 350 lines

3. Update `src/components/ui/driver/index.ts`:
   - Add: `export { SimpleStopView } from './SimpleStopView'`
   - Add: `export { DeliveryConfirmDialog } from './DeliveryConfirmDialog'`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>SimpleStopView renders single-stop focus with name/address/phone/mark-delivered. DeliveryConfirmDialog prompts "Mark as delivered at [address]?".</done>
</task>

<task type="auto">
  <name>Task 2: Wire simple mode into route page, stop detail page, and home page</name>
  <files>src/app/(driver)/driver/route/page.tsx, src/app/(driver)/driver/route/[stopId]/page.tsx, src/app/(driver)/driver/page.tsx</files>
  <action>
1. Update `src/app/(driver)/driver/page.tsx`:
   - The server component `DriverHomePageContent` currently renders `<DriverDashboard {...data} />`
   - Create a small client wrapper component (can be inline or in a sibling file like `DriverHomeSwitch.tsx`):
     * Import `useSimpleMode` from `@/components/ui/driver/SimpleModeProvider`
     * Import `SimpleHome` from `@/components/ui/driver/SimpleHome`
     * Import `DriverDashboard` from `@/components/ui/driver/DriverDashboard`
     * If `isSimpleMode`: render `<SimpleHome driverName={data.driver.fullName} todayRoute={data.todayRoute} dayOfWeek={data.dayOfWeek} dateDisplay={data.dateDisplay} />`
     * If not: render `<DriverDashboard {...data} />`
   - Server component passes all data to the client wrapper
   - Must keep the Suspense boundary and loading skeleton as-is

2. Update `src/app/(driver)/driver/route/page.tsx`:
   - Create a client wrapper for the route content (sibling file or inline)
   - Import `useSimpleMode` from `@/components/ui/driver/SimpleModeProvider`
   - If `isSimpleMode` AND route exists: render `<SimpleStopView routeId={route.id} stops={stops} />`
   - If `isSimpleMode` AND no route: render simple "No route today" message (Package icon + text, same as normal but simpler — no DriverPageHeader needed)
   - If NOT simple mode: render existing `<ActiveRouteView ... />` with `<DriverPageHeader>` (unchanged)
   - Pass stops data from server component to client wrapper
   - Note: the server component `getActiveRoute()` already fetches stops with customer name + address — reuse this data

3. Update `src/app/(driver)/driver/route/[stopId]/page.tsx`:
   - For simple mode: redirect to `/driver/route` (simple mode uses single-stop focus in the route page, not individual stop pages)
   - Add client wrapper or check: if `isSimpleMode`, use `router.replace('/driver/route')` or render the SimpleStopView
   - For normal mode: keep existing StopDetailView behavior unchanged
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>Home page conditionally renders SimpleHome or DriverDashboard. Route page conditionally renders SimpleStopView or ActiveRouteView. Stop detail redirects to route page in simple mode.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` passes
- Simple mode home shows greeting + route button only
- Simple mode route shows single current stop with name/address/phone/mark-delivered
- Mark Delivered triggers confirmation dialog
- After confirming delivery, auto-advance to next stop
- All Done screen appears when all stops delivered
</verification>

<success_criteria>
- Single-stop focus: only current (next undelivered) stop visible
- Customer name displayed prominently (text-2xl)
- Address tap opens Google Maps
- Phone tap initiates phone call
- Mark Delivered shows confirmation: "Mark as delivered at [address]?"
- Confirmation triggers status update (online) or offline queue
- Success animation plays before advancing to next stop
- "X of Y done" progress counter always visible
- "All done!" screen when route complete
- "Call for Help" button dials operator
- Normal mode pages completely unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/83-driver-simplification/83-03-SUMMARY.md`
</output>
