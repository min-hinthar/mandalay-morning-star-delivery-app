---
phase: 83-driver-simplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/driver/SimpleHome.tsx
  - src/components/ui/driver/SimpleModeToggle.tsx
  - src/app/(driver)/driver/profile/ProfilePageClient.tsx
  - src/components/ui/driver/DriverDashboard/index.tsx
  - src/components/ui/driver/index.ts
autonomous: true
requirements: [DRV-01, DRV-05]

must_haves:
  truths:
    - "Simple mode home shows greeting, date, and Start Today's Route button only"
    - "Simple mode home hides stats, badges, streaks, earnings, onboarding"
    - "Profile page has a simple mode toggle with clear explanation"
    - "Toggle switches mode instantly without page reload"
  artifacts:
    - path: "src/components/ui/driver/SimpleHome.tsx"
      provides: "Minimal home screen for simple mode drivers"
      exports: ["SimpleHome"]
    - path: "src/components/ui/driver/SimpleModeToggle.tsx"
      provides: "Toggle switch for simple mode on profile page"
      exports: ["SimpleModeToggle"]
  key_links:
    - from: "src/components/ui/driver/SimpleHome.tsx"
      to: "src/components/ui/driver/SimpleModeProvider.tsx"
      via: "useSimpleMode() for mode detection"
      pattern: "useSimpleMode"
    - from: "src/components/ui/driver/SimpleModeToggle.tsx"
      to: "src/components/ui/driver/SimpleModeProvider.tsx"
      via: "useSimpleMode() for toggle"
      pattern: "toggleSimpleMode"
---

<objective>
Create SimpleHome screen and SimpleModeToggle for the profile page.

Purpose: Simple mode home strips driver dashboard to essentials; toggle on profile page lets drivers/admin switch modes.
Output: SimpleHome component, SimpleModeToggle component, updated profile page, updated barrel exports.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/types.ts
@src/app/(driver)/driver/page.tsx
@src/app/(driver)/driver/profile/ProfilePageClient.tsx

<interfaces>
<!-- Key types from DriverDashboard -->

From src/components/ui/driver/DriverDashboard/types.ts (check for DriverDashboardProps):
The component receives: driver, todayRoute, streakDays, todayEarningsCents, weeklyEarningsCents, badges, nextRouteDate, dayOfWeek, dateDisplay

From src/app/(driver)/driver/page.tsx:
```typescript
// getDriverData() returns all dashboard props
// DriverHomePageContent passes them to DriverDashboard
```

From src/app/(driver)/driver/profile/ProfilePageClient.tsx:
```typescript
interface ProfilePageClientProps {
  driverId: string;
  fullName: string | null;
  email: string | null;
  phone: string | null;
  vehicleType: VehicleType | null;
  licensePlate: string | null;
  profileImageUrl: string | null;
  isActive: boolean;
  createdAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SimpleHome component</name>
  <files>src/components/ui/driver/SimpleHome.tsx, src/components/ui/driver/DriverDashboard/index.tsx, src/components/ui/driver/index.ts</files>
  <action>
1. Create `src/components/ui/driver/SimpleHome.tsx`:
   - `'use client'` directive
   - Props: `{ driverName: string | null; todayRoute: { id: string; status: string; stopCount: number; deliveredCount: number } | null; dayOfWeek: string; dateDisplay: string }`
   - Layout:
     * Greeting: "Hello, {firstName}!" (extract first name from driverName, fallback "Driver")
     * Date line: "{dayOfWeek}, {dateDisplay}" with Calendar icon (same as DriverDashboard)
     * If todayRoute exists AND status is 'planned': big green "Start Today's Route" button (min-h-[72px], full width, bg-green text-text-inverse). On click: POST to `/api/driver/routes/{routeId}/start`, then `router.push('/driver/route')`.
     * If todayRoute exists AND status is 'in_progress': big teal "Continue Route" button (min-h-[72px]). On click: `router.push('/driver/route')`. Show progress: "{deliveredCount} of {stopCount} done".
     * If no todayRoute: centered "No route today" message with Package icon, muted text
   - Use Framer Motion `m` for entry animations (opacity + y), `useAnimationPreference` for motion control
   - Design tokens: `bg-surface-primary`, `text-text-primary`, `font-display` for heading, `font-body` for text
   - All buttons: `rounded-card-sm`, `font-body font-semibold`, `shadow-md`
   - Keep component under 200 lines

2. Update `src/components/ui/driver/DriverDashboard/index.tsx`:
   - Import `SimpleHome` from `../SimpleHome`
   - Re-export: `export { SimpleHome } from '../SimpleHome'`

3. Update `src/components/ui/driver/index.ts`:
   - Add: `export { SimpleHome } from './SimpleHome'`
   - Add: `export { SimpleModeToggle } from './SimpleModeToggle'` (for Task 2)
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>SimpleHome renders greeting + date + route CTA. No stats, badges, earnings, streaks, onboarding, or profile completeness.</done>
</task>

<task type="auto">
  <name>Task 2: Create SimpleModeToggle + add to profile page</name>
  <files>src/components/ui/driver/SimpleModeToggle.tsx, src/app/(driver)/driver/profile/ProfilePageClient.tsx</files>
  <action>
1. Create `src/components/ui/driver/SimpleModeToggle.tsx`:
   - `'use client'` directive
   - Import `useSimpleMode` from `./SimpleModeProvider`
   - Render a card with:
     * Heading: "Simple Mode" with a brief explanation: "Shows only essential delivery info — perfect for new or occasional drivers"
     * Toggle switch (use a styled `<button>` with role="switch", aria-checked, and visual on/off state)
     * Visual: pill-shaped toggle track (48x24px), circle thumb (20px), bg-accent-teal when on, bg-surface-tertiary when off
     * On click: call `toggleSimpleMode()` from context
     * Show current state text: "On" / "Off"
   - Use `m` from framer-motion for thumb slide animation
   - Card styling: rounded-2xl border border-border shadow-card p-5 bg-surface-primary

2. Update `src/app/(driver)/driver/profile/ProfilePageClient.tsx`:
   - Import `SimpleModeToggle` from `@/components/ui/driver/SimpleModeToggle`
   - Add `<SimpleModeToggle />` section AFTER the "Save Changes" button, before the form closing tag or as a separate card below the form
   - Wrap in `m.div` with staggered animation delay (0.3)
   - Add a horizontal divider or spacing (mt-6) between save button and toggle
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>Profile page shows SimpleModeToggle card below save button. Toggle calls toggleSimpleMode() from context. Mode switches instantly without reload.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` passes
- SimpleHome renders minimal home (greeting + date + route button only)
- SimpleModeToggle visible on profile page
</verification>

<success_criteria>
- SimpleHome shows greeting with first name, today's date, and one large CTA button
- SimpleHome has zero references to stats, badges, streaks, earnings, or onboarding
- SimpleModeToggle renders accessible toggle switch with explanation text
- Toggling simple mode updates context immediately (optimistic UI)
- Profile page includes toggle below the save button
</success_criteria>

<output>
After completion, create `.planning/phases/83-driver-simplification/83-02-SUMMARY.md`
</output>
