---
phase: 36.2-feature-finalization-polish
plan: 04
type: execute
wave: 3
depends_on: ["36.2-02"]
files_modified:
  - src/app/(admin)/admin/drivers/page.tsx
  - src/components/ui/admin/drivers/PendingInvitesTab.tsx
  - src/components/ui/admin/drivers/InviteDriverModal.tsx
  - src/app/api/admin/drivers/invites/route.ts
autonomous: true

must_haves:
  truths:
    - "Drivers page has Pending tab alongside Active/Inactive"
    - "Admin can click Invite Driver button to open invite modal"
    - "Pending tab shows all pending invites with resend/revoke actions"
    - "Invites list shows email, sent date, expiration, status"
  artifacts:
    - path: "src/components/ui/admin/drivers/PendingInvitesTab.tsx"
      provides: "Table of pending driver invites with actions"
      exports: ["PendingInvitesTab"]
    - path: "src/components/ui/admin/drivers/InviteDriverModal.tsx"
      provides: "Modal for sending new driver invite"
      exports: ["InviteDriverModal"]
    - path: "src/app/api/admin/drivers/invites/route.ts"
      provides: "GET endpoint to list pending invites"
      exports: ["GET"]
  key_links:
    - from: "src/app/(admin)/admin/drivers/page.tsx"
      to: "src/components/ui/admin/drivers/PendingInvitesTab.tsx"
      via: "conditional render based on tab"
      pattern: "PendingInvitesTab"
    - from: "src/components/ui/admin/drivers/InviteDriverModal.tsx"
      to: "/api/admin/drivers/invite"
      via: "fetch POST on submit"
      pattern: "fetch.*invite"
---

<objective>
Add Pending tab to drivers page and invite driver functionality.

Purpose: UI for admin to manage driver invites - view pending, send new, resend, revoke.
Output: Updated drivers page with Pending tab, InviteDriverModal, PendingInvitesTab.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-feature-finalization-polish/36.2-CONTEXT.md
@.planning/phases/36.2-feature-finalization-polish/36.2-02-SUMMARY.md
@src/app/(admin)/admin/drivers/page.tsx
@src/components/ui/admin/drivers/DriverListTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create list invites API endpoint</name>
  <files>src/app/api/admin/drivers/invites/route.ts</files>
  <action>
    Create GET endpoint at `/api/admin/drivers/invites`:

    **Query:**
    - Fetch from driver_invites where revoked_at IS NULL AND accepted_at IS NULL
    - Join with profiles to get invited_by name
    - Order by created_at DESC

    **Response:**
    ```typescript
    {
      id: string;
      email: string;
      invitedBy: { id: string; name: string };
      createdAt: string;
      expiresAt: string;
      isExpired: boolean; // calculated: expires_at < NOW()
    }[]
    ```

    **Auth:**
    - Require admin role (same pattern as other admin endpoints)

  </action>
  <verify>Endpoint returns list of pending invites</verify>
  <done>GET /api/admin/drivers/invites returns pending invites with status</done>
</task>

<task type="auto">
  <name>Task 2: Create PendingInvitesTab and InviteDriverModal components</name>
  <files>src/components/ui/admin/drivers/PendingInvitesTab.tsx, src/components/ui/admin/drivers/InviteDriverModal.tsx</files>
  <action>
    **PendingInvitesTab:**
    - Fetch invites from /api/admin/drivers/invites
    - Display table with columns: Email, Invited By, Sent, Expires, Status, Actions
    - Status shows "Pending" (green badge) or "Expired" (red badge)
    - Actions column: Resend button, Revoke button (dropdown or inline)
    - Resend calls POST /api/admin/drivers/[id]/resend-invite
    - Revoke calls POST /api/admin/drivers/[id]/revoke-invite with confirmation
    - Empty state: illustration + "No pending invites" message

    **InviteDriverModal:**
    - Dialog/Modal using existing modal patterns
    - Single field: email input with validation
    - Submit calls POST /api/admin/drivers/invite
    - Show success toast with "Invite sent to {email}"
    - Show error toast if email already invited or registered
    - Close modal on success

    **Styling:**
    - Match DriverListTable patterns
    - Use existing Button, Badge, Input, Dialog components

  </action>
  <verify>Both components render and compile without errors</verify>
  <done>
    - PendingInvitesTab displays invites with resend/revoke
    - InviteDriverModal sends invites on submit
  </done>
</task>

<task type="auto">
  <name>Task 3: Update drivers page with Pending tab</name>
  <files>src/app/(admin)/admin/drivers/page.tsx</files>
  <action>
    **Add tab to STATUS_FILTERS:**
    - Add "pending" option: `{ value: "pending", label: "Pending Invites" }`
    - Keep existing: all, active, inactive

    **Update "Add Driver" button:**
    - Change to "Invite Driver" text
    - Change icon from UserPlus to Mail (or keep UserPlus if appropriate)
    - Opens InviteDriverModal instead of AddDriverModal

    **Conditional content:**
    - When statusFilter === "pending": render PendingInvitesTab
    - Otherwise: render existing DriverListTable with filtered drivers

    **Keep AddDriverModal:**
    - May still be useful for adding existing users as drivers
    - Or remove if invites are the only way to add drivers (per CONTEXT.md)
    - Decision: Keep AddDriverModal for now, both options available

    **Stats update:**
    - Add pendingInvites count to stats (separate API call or inline)
    - Show in Pending badge: "Pending (3)"

  </action>
  <verify>Page compiles, tab switching works, invite modal opens</verify>
  <done>
    - Pending tab shows pending invites
    - Invite Driver button opens modal
    - Tab counts reflect current data
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. Drivers page shows Pending tab with invite count
4. Invite modal sends invites successfully
5. Pending tab shows invites with resend/revoke actions
</verification>

<success_criteria>

- Admin can switch to Pending tab to view invites
- Admin can send new invite via modal
- Admin can resend pending invite
- Admin can revoke pending invite
- Expired invites clearly marked
- Empty state shown when no pending invites
  </success_criteria>

<output>
After completion, create `.planning/phases/36.2-feature-finalization-polish/36.2-04-SUMMARY.md`
</output>
