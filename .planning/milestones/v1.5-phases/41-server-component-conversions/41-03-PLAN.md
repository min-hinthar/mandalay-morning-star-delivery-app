---
phase: 41-server-component-conversions
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/app/(admin)/admin/analytics/loading.tsx
  - src/app/(admin)/admin/analytics/error.tsx
autonomous: true

must_haves:
  truths:
    - "Analytics page shows branded loading state during navigation"
    - "Analytics page shows branded error state on failure"
    - "Analytics page hydrates without console errors"
  artifacts:
    - path: "src/app/(admin)/admin/analytics/loading.tsx"
      provides: "Loading state for analytics route"
      exports: ["default"]
    - path: "src/app/(admin)/admin/analytics/error.tsx"
      provides: "Error boundary for analytics route"
      exports: ["default"]
  key_links:
    - from: "src/app/(admin)/admin/analytics/loading.tsx"
      to: "src/components/ui/RouteLoading.tsx"
      via: "import RouteLoading"
      pattern: "RouteLoading"
---

<objective>
Add loading/error files to the analytics route segment to validate infrastructure before converting higher-complexity pages.

Purpose: Low-risk validation that RouteLoading and RouteError components work correctly in a real route.
Output: loading.tsx and error.tsx for analytics route
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-server-component-conversions/41-01-SUMMARY.md
@src/app/(admin)/admin/analytics/page.tsx
@src/components/ui/RouteLoading.tsx
@src/components/ui/RouteError.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics loading.tsx</name>
  <files>src/app/(admin)/admin/analytics/loading.tsx</files>
  <action>
Create loading.tsx for the analytics route using the RouteLoading component:

```typescript
import { RouteLoading } from '@/components/ui/RouteLoading'

export default function AnalyticsLoading() {
  return <RouteLoading message="Loading analytics..." />
}
```

This is intentionally minimal - the RouteLoading component handles all the branding, animation, and accessibility.
</action>
<verify>File exists. TypeScript compiles: `pnpm typecheck`</verify>
<done>Analytics loading.tsx created with branded loading state</done>
</task>

<task type="auto">
  <name>Task 2: Create analytics error.tsx</name>
  <files>src/app/(admin)/admin/analytics/error.tsx</files>
  <action>
Create error.tsx for the analytics route using the RouteError component:

```typescript
'use client'

import { RouteError } from '@/components/ui/RouteError'

export default function AnalyticsError({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return <RouteError error={error} reset={reset} context="analytics" />
}
```

The 'use client' directive is required for error.tsx files in Next.js App Router.
The context prop provides route-specific error messaging.
</action>
<verify>File exists. TypeScript compiles: `pnpm typecheck`</verify>
<done>Analytics error.tsx created with route-specific error handling</done>
</task>

<task type="auto">
  <name>Task 3: Verify hydration and smoke test</name>
  <files></files>
  <action>
Run verification checks:

1. **Build check** - Ensure no hydration issues at build time:

   ```bash
   pnpm build 2>&1 | grep -i "hydrat" || echo "No hydration errors in build"
   ```

2. **Hydration smoke test** (if dev server available):

   ```bash
   pnpm test:e2e e2e/hydration-smoke.spec.ts --grep "Analytics"
   ```

3. **Manual verification checklist** (document in summary):

**Analytics Page Smoke Test Checklist:**

- [ ] Page loads without white flash
- [ ] Loading state shows branded spinner with "Loading analytics..."
- [ ] Page content renders completely
- [ ] Quick stats cards display correctly
- [ ] Dashboard links are clickable
- [ ] No hydration warnings in browser console
- [ ] Page is responsive on mobile viewport
      </action>
      <verify>Build succeeds. No hydration errors in console. Page renders correctly.</verify>
      <done>Analytics page verified: loading state works, error boundary in place, no hydration issues</done>
      </task>

</tasks>

<verification>
```bash
pnpm typecheck
pnpm lint
pnpm build

# If test environment available:

pnpm test:e2e e2e/hydration-smoke.spec.ts --grep "Analytics"

```
</verification>

<success_criteria>
- loading.tsx exists and uses RouteLoading component
- error.tsx exists and uses RouteError component
- Build completes without hydration warnings
- Analytics page loads correctly with loading state
</success_criteria>

<output>
After completion, create `.planning/phases/41-server-component-conversions/41-03-SUMMARY.md`

Include smoke test checklist results in summary.
</output>
```
