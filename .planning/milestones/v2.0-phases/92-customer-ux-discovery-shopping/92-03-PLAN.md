---
phase: 92-customer-ux-discovery-shopping
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/cart/CartBar.tsx
  - src/components/ui/cart/CartDrawerParts.tsx
  - src/components/ui/customer/OfflineBanner.tsx
  - src/components/ui/customer/index.ts
  - src/app/(public)/menu/page.tsx
  - src/app/(customer)/layout.tsx
autonomous: true
requirements: [CUX-06, CUX-07, CUX-09, CUX-10]

must_haves:
  truths:
    - "CartBar shows minimum order warning text above checkout button when subtotal < $25, and checkout button is disabled"
    - "Cart drawer header shows 'Saved' with checkmark after cart changes, 'Saving...' during sync, auto-hides after 2 seconds"
    - "Amber offline banner appears fixed at top when offline: 'You're offline -- browsing cached menu. Some items may be unavailable.' Dismissible. Auto-hides on reconnect with 'Back online!' toast"
    - "CartBar sticky footer works correctly on mobile with total and checkout button always visible (existing behavior verified)"
  artifacts:
    - path: "src/components/ui/cart/CartBar.tsx"
      provides: "CartBar with inline min-order warning and disabled checkout when below minimum"
      contains: "minimumOrderCents"
    - path: "src/components/ui/cart/CartDrawerParts.tsx"
      provides: "CartHeader with sync status indicator"
      contains: "Saving"
    - path: "src/components/ui/customer/OfflineBanner.tsx"
      provides: "Amber offline banner for customer pages"
      exports: ["OfflineBanner"]
    - path: "src/components/ui/customer/index.ts"
      provides: "Barrel export for customer components"
  key_links:
    - from: "src/components/ui/cart/CartBar.tsx"
      to: "BUSINESS_RULES_DEFAULTS.minimumOrderCents"
      via: "prop from page or default constant"
      pattern: "minimumOrderCents"
    - from: "src/components/ui/cart/CartDrawerParts.tsx"
      to: "cart-store"
      via: "Zustand subscribe for sync state changes"
      pattern: "subscribe|pendingSync"
    - from: "src/components/ui/customer/OfflineBanner.tsx"
      to: "useCustomerOfflineSync"
      via: "isOnline/wasOffline state"
      pattern: "useCustomerOfflineSync"
---

<objective>
Add minimum order warning to CartBar, sync status indicator to cart drawer, and offline banner for customer pages.

Purpose: Customers get clear feedback about order minimums, cart save status, and connectivity issues while shopping.
Output: Enhanced CartBar with min-order warning, CartHeader with sync indicator, new OfflineBanner component for customer pages.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-customer-ux-discovery-shopping/92-CONTEXT.md
@.planning/phases/92-customer-ux-discovery-shopping/92-RESEARCH.md

<interfaces>
From src/components/ui/cart/CartBar.tsx:
```typescript
export interface CartBarProps {
  className?: string;
  showCheckoutButton?: boolean;
}
// Uses: useCart().isEmpty, itemCount, estimatedTotal, amountToFreeDelivery, freeDeliveryThresholdCents
// Fixed bottom bar with iOS safe area, delivery progress, checkout button
```

From src/lib/hooks/useCart.ts:
```typescript
// Returns: { items, isEmpty, itemCount, estimatedTotal, itemsSubtotal, estimatedDeliveryFee,
//            freeDeliveryThresholdCents, amountToFreeDelivery, formattedSubtotal, formattedTotal }
```

From src/components/ui/cart/CartDrawerParts.tsx:
```typescript
interface CartHeaderProps {
  itemCount: number;
  onClose: () => void;
  onClearClick: () => void;
  showClear: boolean;
}
```

From src/lib/hooks/useCustomerOfflineSync.ts:
```typescript
interface UseCustomerOfflineSyncReturn {
  isOnline: boolean;
  wasOffline: boolean; // True for 3s after reconnecting
  checkStatus: () => void;
}
```

From src/lib/settings/business-rules.ts:
```typescript
export const BUSINESS_RULES_DEFAULTS: BusinessRules = {
  minimumOrderCents: 2500, // $25
  // ...
};
```

From src/lib/stores/cart-store.ts:
```typescript
// CartItem has: pendingSync?: boolean (set true when offline)
// Store has online listener that clears pendingSync on reconnect
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add min-order warning to CartBar and sync status to CartHeader</name>
  <files>src/components/ui/cart/CartBar.tsx, src/components/ui/cart/CartDrawerParts.tsx, src/app/(public)/menu/page.tsx</files>
  <action>
1. Update `src/components/ui/cart/CartBar.tsx` -- add minimum order warning:
   - Add `minimumOrderCents` prop (optional, defaults to `BUSINESS_RULES_DEFAULTS.minimumOrderCents` = 2500).
   - Import `BUSINESS_RULES_DEFAULTS` from `@/lib/settings/business-rules`.
   - Compute `shortfall = Math.max(0, minimumOrderCents - itemsSubtotal)` using `useCart().itemsSubtotal`.
   - NOTE: `useCart()` may not expose `itemsSubtotal` directly. Check the hook -- it does (grep confirms line 44). Use it.
   - When `shortfall > 0`: render warning text between DeliveryProgress/FreeDeliveryBanner and the main content row.
   - Warning text: `"$X.XX more to reach $25 minimum"` in `text-xs text-status-warning text-center px-4 pb-1`.
   - Use `AnimatePresence` + `m.div` for smooth enter/exit of warning text to avoid layout shift (per research pitfall #4).
   - Disable checkout button when `shortfall > 0`: add `disabled` prop to the Checkout Button, change `handleCheckout` to no-op when below minimum.
   - Verify CUX-07 (sticky footer): CartBar is already `fixed bottom-0 left-0 right-0` with iOS safe area `pb-[env(safe-area-inset-bottom)]`. No changes needed -- just confirm it works.

2. Update `src/app/(public)/menu/page.tsx`:
   - Pass `minimumOrderCents={rules.minimumOrderCents}` to a context or keep using default. Since CartBar is rendered globally (not in menu page), the default from `BUSINESS_RULES_DEFAULTS` suffices. No page change needed unless CartBar is rendered in menu page specifically. Check if CartBar is in a layout. If CartBar is rendered in a layout or customer shell, the default constant is fine.

3. Update `src/components/ui/cart/CartDrawerParts.tsx` -- add sync status to CartHeader:
   - Add sync status indicator next to "Your Cart" title in `CartHeader`.
   - Approach: use Zustand `useCartStore.subscribe()` pattern or simpler approach:
     - Track `lastChangeTime` via `useEffect` watching `items` from `useCart()`.
     - When items change: show "Saving..." for 500ms, then "Saved" with checkmark for 2s, then fade out.
     - Use `AnimatePresence` for enter/exit animation.
   - Style: `text-xs text-text-muted` with `Check` icon (lucide) for "Saved" state.
   - Per locked decision: "Google Docs auto-save style, subtle text in cart drawer header, disappears after 2 seconds."
   - NOTE: Do NOT use `pendingSync` from cart-store -- that only tracks offline items. Instead, use a simpler "any cart mutation = show saved" approach per research recommendation.
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>CartBar shows min-order warning when subtotal < $25 with disabled checkout. CartHeader shows "Saving..."/"Saved" indicator after cart changes.</done>
</task>

<task type="auto">
  <name>Task 2: Create customer OfflineBanner component and wire into layout</name>
  <files>src/components/ui/customer/OfflineBanner.tsx, src/components/ui/customer/index.ts, src/app/(customer)/layout.tsx</files>
  <action>
1. Create `src/components/ui/customer/OfflineBanner.tsx`:
   - `'use client'` directive.
   - Import `useCustomerOfflineSync` from `@/lib/hooks/useCustomerOfflineSync`.
   - Import `toast` from `@/lib/hooks/useToastV8` for "Back online!" toast.
   - Import `WifiOff`, `X` from `lucide-react`.
   - Import `m`, `AnimatePresence` from `framer-motion`.
   - Import `useAnimationPreference` for reduced motion.
   - State: `dismissed` (boolean, default false). Reset to false when going offline again.
   - Render logic:
     - When `!isOnline && !dismissed`: show fixed amber top banner.
     - Banner: `fixed top-0 left-0 right-0` with `z-[70]` (above header z-20, below modals).
     - Background: `bg-amber-500` (solid, no blur on mobile per project rule).
     - Content: WifiOff icon + "You're offline -- browsing cached menu. Some items may be unavailable." + X dismiss button.
     - Text: `text-text-inverse text-sm font-medium`.
     - Dismiss button: `X` icon, sets `dismissed = true`.
     - Animate: `m.div` with slide-down entry (`initial={{ y: -60 }}`, `animate={{ y: 0 }}`), slide-up exit.
   - When `wasOffline` transitions to true (reconnected): show toast `"Back online!"` via `toast({ message: "Back online!", type: "success" })`.
   - When going offline again after dismissing: reset `dismissed = false` (track prev online state with ref).
   - Ensure banner does NOT overlap CartBar (banner at top, CartBar at bottom -- no conflict per research).

2. Create `src/components/ui/customer/index.ts` barrel:
   - Export `{ OfflineBanner }` from `./OfflineBanner`.

3. Wire into customer layout:
   - Check `src/app/(customer)/layout.tsx` -- add `<OfflineBanner />` at the top of the layout's body.
   - Also check if the menu page (`(public)` group) uses a different layout. If so, add OfflineBanner there too, or add it in a shared layout higher up.
   - If there's no clean shared layout, add OfflineBanner to both `(public)` and `(customer)` layouts. The component is lightweight and self-contained.
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>Amber offline banner appears at top when offline, dismissible, auto-hides on reconnect with toast. Wired into customer-facing layouts. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build` succeeds
- CartBar shows "$X.XX more to reach $25 minimum" when subtotal < $25
- Checkout button disabled when below minimum
- Cart drawer header shows "Saving..."/"Saved" indicator
- Offline banner appears when network drops, dismissible, shows "Back online!" toast on reconnect
- CartBar remains fixed at bottom with iOS safe area (CUX-07 verified)
</verification>

<success_criteria>
- Min-order warning visible in CartBar when subtotal < $25, hidden when >= $25
- Checkout button disabled when below minimum, enabled when above
- Sync status shows briefly after cart mutations then auto-hides
- Offline banner renders at top with amber styling, dismissible
- "Back online!" toast on reconnect
- No z-index conflicts between banner, header, CartBar, and modals
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/92-customer-ux-discovery-shopping/92-03-SUMMARY.md`
</output>
