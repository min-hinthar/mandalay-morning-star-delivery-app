---
phase: 98-delivery-photo-signed-urls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/delivery-photos.ts
  - src/lib/supabase/__tests__/delivery-photos.test.ts
  - src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts
  - src/app/api/admin/routes/[id]/route.ts
  - src/app/api/tracking/[orderId]/route.ts
  - src/app/api/driver/routes/active/route.ts
  - src/app/api/driver/routes/[routeId]/route.ts
autonomous: true
requirements:
  - DRV-03
gap_closure: true

must_haves:
  truths:
    - "Admin order detail page renders delivery proof photos without 403 errors"
    - "Customer tracking page renders delivery proof photos without 403 errors"
    - "Driver route views render delivery proof photos without 403 errors"
    - "Newly uploaded photos are stored as paths (not full URLs) in DB"
    - "Existing rows with full public URLs are handled via backward-compatible path extraction"
  artifacts:
    - path: "src/lib/supabase/delivery-photos.ts"
      provides: "Shared getDeliveryPhotoSignedUrl helper + extractDeliveryPhotoPath"
      exports: ["getDeliveryPhotoSignedUrl"]
    - path: "src/lib/supabase/__tests__/delivery-photos.test.ts"
      provides: "Unit tests for path extraction and null handling"
      contains: "extractDeliveryPhotoPath"
  key_links:
    - from: "src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts"
      to: "src/lib/supabase/delivery-photos.ts"
      via: "import getDeliveryPhotoSignedUrl"
      pattern: "getDeliveryPhotoSignedUrl"
    - from: "src/app/api/admin/routes/[id]/route.ts"
      to: "src/lib/supabase/delivery-photos.ts"
      via: "import getDeliveryPhotoSignedUrl"
      pattern: "getDeliveryPhotoSignedUrl"
    - from: "src/app/api/tracking/[orderId]/route.ts"
      to: "src/lib/supabase/delivery-photos.ts"
      via: "import getDeliveryPhotoSignedUrl"
      pattern: "getDeliveryPhotoSignedUrl"
    - from: "src/app/api/driver/routes/active/route.ts"
      to: "src/lib/supabase/delivery-photos.ts"
      via: "import getDeliveryPhotoSignedUrl"
      pattern: "getDeliveryPhotoSignedUrl"
    - from: "src/app/api/driver/routes/[routeId]/route.ts"
      to: "src/lib/supabase/delivery-photos.ts"
      via: "import getDeliveryPhotoSignedUrl"
      pattern: "getDeliveryPhotoSignedUrl"
---

<objective>
Fix admin, customer, and driver viewing of delivery proof photos by replacing getPublicUrl (returns 403 on private bucket) with time-limited signed URLs.

Purpose: The `delivery-photos` Supabase Storage bucket is private, but the upload endpoint stores a `getPublicUrl()` URL in `route_stops.delivery_photo_url`. This returns 403. All 5 endpoints that serve or produce `delivery_photo_url` must be fixed.
Output: Working delivery photo display across admin, customer tracking, and driver views.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-delivery-photo-signed-urls/98-CONTEXT.md
@.planning/phases/98-delivery-photo-signed-urls/98-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/supabase/server.ts (line 50):
```typescript
export function createServiceClient(): SupabaseClient<Database>;
```

From src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts (lines 114-120 -- THE BUG):
```typescript
// Current broken code:
const { data: urlData } = supabase.storage.from("delivery-photos").getPublicUrl(filename);
const photoUrl = urlData.publicUrl;
await supabase.from("route_stops").update({ delivery_photo_url: photoUrl }).eq("id", stopId);
return NextResponse.json({ success: true, photoUrl });
```

From src/app/api/admin/routes/[id]/route.ts (line 170 -- passes raw DB value):
```typescript
deliveryPhotoUrl: stop.delivery_photo_url,
```

From src/app/api/tracking/[orderId]/route.ts (line 235 -- passes raw DB value):
```typescript
deliveryPhotoUrl: routeStopData.delivery_photo_url,
```

From src/app/api/driver/routes/active/route.ts (line 189 -- passes raw DB value):
```typescript
deliveryPhotoUrl: stop.delivery_photo_url,
```

From src/app/api/driver/routes/[routeId]/route.ts (line 185 -- passes raw DB value):
```typescript
deliveryPhotoUrl: stop.delivery_photo_url,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create signed URL helper, tests, and fix upload endpoint</name>
  <files>src/lib/supabase/delivery-photos.ts, src/lib/supabase/__tests__/delivery-photos.test.ts, src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts</files>
  <behavior>
    - Test: getDeliveryPhotoSignedUrl(null) returns null
    - Test: getDeliveryPhotoSignedUrl("") returns null
    - Test: extractDeliveryPhotoPath("routeId/orderId.jpg") returns "routeId/orderId.jpg" (already a path)
    - Test: extractDeliveryPhotoPath("https://xyz.supabase.co/storage/v1/object/public/delivery-photos/abc/def.jpg") returns "abc/def.jpg"
    - Test: extractDeliveryPhotoPath with URL that has no delivery-photos segment returns null
    - Test: getDeliveryPhotoSignedUrl calls createSignedUrl with correct bucket and 3600s expiry (mock supabase)
    - Test: getDeliveryPhotoSignedUrl returns null on SDK error (does not throw)
  </behavior>
  <action>
    1. Create `src/lib/supabase/delivery-photos.ts` (server-only module):
       - Import `createServiceClient` from `./server`
       - Constants: `DELIVERY_PHOTOS_BUCKET = "delivery-photos"`, `SIGNED_URL_EXPIRY = 3600`
       - Export `extractDeliveryPhotoPath(pathOrUrl: string): string | null`:
         - If input does not start with "http", return it as-is (already a path)
         - If input starts with "http", extract path after `delivery-photos/` via regex
         - Return null if no match
       - Export `async getDeliveryPhotoSignedUrl(pathOrUrl: string | null): Promise<string | null>`:
         - Return null for null/empty input
         - Call `extractDeliveryPhotoPath` to normalize
         - Call `createServiceClient().storage.from(BUCKET).createSignedUrl(path, 3600)`
         - On error: log with `console.error`, return null (photo is non-critical)
         - On success: return `data.signedUrl`

    2. Create `src/lib/supabase/__tests__/delivery-photos.test.ts`:
       - Mock `@/lib/supabase/server` module (vi.mock)
       - Mock `createServiceClient` to return a fake supabase client with `.storage.from().createSignedUrl()`
       - Test all behaviors listed above
       - Export `extractDeliveryPhotoPath` for direct testing (add named export in delivery-photos.ts)

    3. Fix upload endpoint `src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts`:
       - Add import: `import { getDeliveryPhotoSignedUrl } from "@/lib/supabase/delivery-photos"`
       - Replace lines 114-120:
         - Remove: `const { data: urlData } = supabase.storage.from("delivery-photos").getPublicUrl(filename);`
         - Remove: `const photoUrl = urlData.publicUrl;`
         - Change DB update to store path: `await supabase.from("route_stops").update({ delivery_photo_url: filename }).eq("id", stopId);`
         - Generate signed URL for response: `const signedUrl = await getDeliveryPhotoSignedUrl(filename);`
         - Return: `NextResponse.json({ success: true, photoUrl: signedUrl })`
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm test -- src/lib/supabase/__tests__/delivery-photos.test.ts -x</automated>
  </verify>
  <done>
    - delivery-photos.ts exports getDeliveryPhotoSignedUrl and extractDeliveryPhotoPath
    - All 7 unit tests pass (null, empty, path passthrough, URL extraction, no-match, SDK call, error handling)
    - Upload endpoint stores filename (path) not public URL in DB
    - Upload endpoint returns signed URL in response
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix all read endpoints to generate signed URLs</name>
  <files>src/app/api/admin/routes/[id]/route.ts, src/app/api/tracking/[orderId]/route.ts, src/app/api/driver/routes/active/route.ts, src/app/api/driver/routes/[routeId]/route.ts</files>
  <action>
    Fix 4 API endpoints to transform stored paths into signed URLs before returning JSON to clients.

    1. **Admin route detail** (`src/app/api/admin/routes/[id]/route.ts`):
       - Add import: `import { getDeliveryPhotoSignedUrl } from "@/lib/supabase/delivery-photos";`
       - In the GET handler, the stops mapping (line 163-210) currently does `deliveryPhotoUrl: stop.delivery_photo_url` (line 170).
       - The entire `response.stops` mapping must become async. Change `.map((stop) => ({` to use `Promise.all` with an async mapper.
       - Replace line 170: `deliveryPhotoUrl: await getDeliveryPhotoSignedUrl(stop.delivery_photo_url),`

    2. **Customer tracking** (`src/app/api/tracking/[orderId]/route.ts`):
       - Add import: `import { getDeliveryPhotoSignedUrl } from "@/lib/supabase/delivery-photos";`
       - At line 235 where `routeStop` is built, replace:
         `deliveryPhotoUrl: routeStopData.delivery_photo_url,`
         with:
         `deliveryPhotoUrl: await getDeliveryPhotoSignedUrl(routeStopData.delivery_photo_url),`
       - The surrounding function is already async so this works directly.

    3. **Driver active route** (`src/app/api/driver/routes/active/route.ts`):
       - Add import: `import { getDeliveryPhotoSignedUrl } from "@/lib/supabase/delivery-photos";`
       - The stops mapping (line 180-212) does `deliveryPhotoUrl: stop.delivery_photo_url` (line 189).
       - Change the `.map()` to use `Promise.all` with an async mapper.
       - Replace line 189: `deliveryPhotoUrl: await getDeliveryPhotoSignedUrl(stop.delivery_photo_url),`

    4. **Driver route detail** (`src/app/api/driver/routes/[routeId]/route.ts`):
       - Add import: `import { getDeliveryPhotoSignedUrl } from "@/lib/supabase/delivery-photos";`
       - The stops mapping (line 176-208) does `deliveryPhotoUrl: stop.delivery_photo_url` (line 185).
       - Change the `.map()` to use `Promise.all` with an async mapper.
       - Replace line 185: `deliveryPhotoUrl: await getDeliveryPhotoSignedUrl(stop.delivery_photo_url),`

    **Pattern for converting sync map to async (3 endpoints):**
    ```typescript
    // Before:
    const stops = route.route_stops.filter(...).map((stop) => ({ ...fields }));
    // After:
    const stops = await Promise.all(
      route.route_stops.filter(...).map(async (stop) => ({ ...fields }))
    );
    ```
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/mandalay-morning-star-delivery-app && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <done>
    - All 4 read endpoints import and call getDeliveryPhotoSignedUrl
    - Admin route GET returns signed URLs for stop.deliveryPhotoUrl
    - Customer tracking GET returns signed URL for routeStop.deliveryPhotoUrl
    - Driver active route GET returns signed URLs for stop.deliveryPhotoUrl
    - Driver route detail GET returns signed URLs for stop.deliveryPhotoUrl
    - TypeScript compiles clean, tests pass, build succeeds
  </done>
</task>

</tasks>

<verification>
1. `pnpm test -- src/lib/supabase/__tests__/delivery-photos.test.ts` -- unit tests for helper
2. `pnpm typecheck` -- no type errors across all modified files
3. `pnpm lint && pnpm lint:css && pnpm format:check` -- code style
4. `pnpm test` -- full test suite green
5. `pnpm build` -- production build succeeds
</verification>

<success_criteria>
- getDeliveryPhotoSignedUrl helper exists with path extraction and backward compatibility
- Upload endpoint stores path (not URL) and returns signed URL
- All 4 read endpoints generate signed URLs before returning to clients
- Unit tests cover null, empty, path, URL, error cases
- Full verification suite passes: lint, typecheck, test, build
</success_criteria>

<output>
After completion, create `.planning/phases/98-delivery-photo-signed-urls/98-01-SUMMARY.md`
</output>
