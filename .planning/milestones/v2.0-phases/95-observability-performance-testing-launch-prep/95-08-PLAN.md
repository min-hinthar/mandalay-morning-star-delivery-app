---
phase: 95-observability-performance-testing-launch-prep
plan: 08
type: execute
wave: 2
depends_on:
  - 95-01
  - 95-04
files_modified:
  - docs/LAUNCH_CHECKLIST.md
  - scripts/launch-check.ts
  - package.json
autonomous: false
requirements:
  - OBS-03
  - OBS-04
  - OBS-06
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03
  - LAUNCH-04
  - LAUNCH-05
  - LAUNCH-06
  - LAUNCH-07
  - LAUNCH-08
  - LAUNCH-09
  - LAUNCH-10
  - LAUNCH-11
user_setup:
  - service: betterstack
    why: "External uptime monitoring for /api/health?deep=true"
    env_vars: []
    dashboard_config:
      - task: "Create free account at betterstack.com"
        location: "BetterStack Dashboard -> Monitors"
      - task: "Add monitor: GET https://{domain}/api/health?deep=true, 3-min interval, alert on 503"
        location: "BetterStack Dashboard -> Monitors -> Create Monitor"
      - task: "Configure alert channels (email + SMS)"
        location: "BetterStack Dashboard -> Settings -> Integrations"
  - service: supabase
    why: "Daily database backups with point-in-time recovery"
    dashboard_config:
      - task: "Upgrade production project to Supabase Pro plan"
        location: "Supabase Dashboard -> Settings -> Billing"
      - task: "Verify daily backups appear in Backups section"
        location: "Supabase Dashboard -> Database -> Backups"
  - service: stripe
    why: "Live payment processing"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (live mode)"
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Publishable key (live mode)"
    dashboard_config:
      - task: "Create webhook endpoint pointing to https://{domain}/api/webhooks/stripe"
        location: "Stripe Dashboard -> Developers -> Webhooks -> Add endpoint"
      - task: "Subscribe to events: checkout.session.completed, checkout.session.expired, payment_intent.payment_failed, charge.refunded"
        location: "Stripe webhook endpoint settings"
  - service: resend
    why: "Transactional email delivery"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
    dashboard_config:
      - task: "Verify domain for production email sending"
        location: "Resend Dashboard -> Domains"
  - service: upstash
    why: "Redis for distributed rate limiting"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Vercel Marketplace -> Upstash Redis -> Database URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Vercel Marketplace -> Upstash Redis -> REST Token"
    dashboard_config:
      - task: "Provision Redis database via Vercel Marketplace"
        location: "Vercel Dashboard -> Integrations -> Upstash Redis"
  - service: vercel
    why: "Production deployment with custom domain"
    env_vars:
      - name: NEXT_PUBLIC_APP_URL
        source: "Your production domain (e.g., morningstardelivery.com)"
    dashboard_config:
      - task: "Add custom domain with SSL"
        location: "Vercel Dashboard -> Settings -> Domains"
      - task: "Set all production env vars"
        location: "Vercel Dashboard -> Settings -> Environment Variables"
  - service: google_maps
    why: "Geocoding and coverage checks"
    dashboard_config:
      - task: "Enable billing on Google Maps API project"
        location: "Google Cloud Console -> Billing"
      - task: "Set budget cap ($50/month recommended)"
        location: "Google Cloud Console -> Billing -> Budgets & alerts"

must_haves:
  truths:
    - "LAUNCH_CHECKLIST.md covers all 11 LAUNCH requirements with actionable steps"
    - "pnpm launch:check validates programmatic items (env vars, DNS, Redis)"
    - "Bundle audit results documented (current size vs 200KB target)"
    - "BetterStack setup steps documented for external alerting (OBS-03)"
    - "Supabase Pro backup requirement documented (OBS-04)"
    - "Admin ops walkthrough and driver test delivery procedures documented (LAUNCH-09, LAUNCH-10)"
    - "Refund and emergency procedures documented (LAUNCH-11)"
  artifacts:
    - path: "docs/LAUNCH_CHECKLIST.md"
      provides: "Complete pre-launch checklist with all 11 items"
      contains: "LAUNCH-01"
      min_lines: 100
    - path: "scripts/launch-check.ts"
      provides: "Automated validation of programmatic launch items"
      contains: "launch:check"
  key_links:
    - from: "scripts/launch-check.ts"
      to: "process.env"
      via: "env var validation"
      pattern: "STRIPE_SECRET_KEY|UPSTASH_REDIS|SUPABASE"
    - from: "package.json"
      to: "scripts/launch-check.ts"
      via: "pnpm launch:check command"
      pattern: "launch:check"
---

<objective>
Create comprehensive launch checklist, validation script, and perform bundle audit for production readiness.

Purpose: Ensure all pre-launch infrastructure is documented, programmatic items are automatically verifiable, and bundle size is within targets before going live with real-money operations.
Output: LAUNCH_CHECKLIST.md, pnpm launch:check command, bundle audit results documented.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-CONTEXT.md
@.planning/phases/95-observability-performance-testing-launch-prep/95-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch checklist and validation script (LAUNCH-01 to LAUNCH-11, OBS-03, OBS-04)</name>
  <files>docs/LAUNCH_CHECKLIST.md, scripts/launch-check.ts, package.json</files>
  <action>
    **A. Create `docs/LAUNCH_CHECKLIST.md`:**

    Structure with these sections (each maps to a LAUNCH requirement):

    **Infrastructure (LAUNCH-01 to LAUNCH-05):**
    - LAUNCH-01: Supabase production instance -- create project, apply migrations, set env vars
    - LAUNCH-02: Production env vars -- list all required vars with expected prefixes, validate with `pnpm launch:check`
    - LAUNCH-03: DNS + custom domain + SSL -- Vercel domain, DNS records, SSL cert, curl verification
    - LAUNCH-04: Google Maps API -- enable billing, budget cap ($50/mo), verify APIs enabled
    - LAUNCH-05: Upstash Redis -- provision via Vercel Marketplace, set env vars

    **External Monitoring (OBS-03, OBS-04):**
    - OBS-03: BetterStack uptime -- create account, add monitor for /api/health?deep=true, 3-min interval, alert on 503, email + SMS channels
    - OBS-04: Database backups -- upgrade Supabase to Pro, verify daily backups, confirm PITR active

    **Payment and Email (LAUNCH-06, LAUNCH-07):**
    - LAUNCH-06: Stripe webhook -- create endpoint, subscribe events, test with Stripe CLI
    - LAUNCH-07: Email delivery -- test all 4 templates (confirmation, reminder, tracking, feedback)

    **Device Testing (LAUNCH-08):**
    - iOS Safari, Android Chrome, PWA install, offline mode, touch targets

    **Training and Procedures (LAUNCH-09, LAUNCH-10, LAUNCH-11):**
    - LAUNCH-09: Admin training -- 10-step walkthrough (login, orders, assign driver, ops dashboard, bulk ops, emails, analytics, menu, refunds)
    - LAUNCH-10: Driver test deliveries -- 10-step walkthrough (signup, profile, route, navigate, contact, photo, deliver, complete)
    - LAUNCH-11: Refund process (6 steps) + Emergency procedures (payment down, app down, driver issues)

    **B. Create `scripts/launch-check.ts`:**

    Node script that validates programmatic launch items:
    1. Load env vars from process.env (dotenv for .env.local if needed)
    2. Required checks (FAIL if missing):
       - STRIPE_SECRET_KEY starts with "sk_live_"
       - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY starts with "pk_live_"
       - NEXT_PUBLIC_SUPABASE_URL is set
       - SUPABASE_SERVICE_ROLE_KEY is set
       - RESEND_API_KEY is set
       - GOOGLE_MAPS_API_KEY is set
       - UPSTASH_REDIS_REST_URL is set
       - UPSTASH_REDIS_REST_TOKEN is set
       - NEXT_PUBLIC_SENTRY_DSN is set
       - NEXT_PUBLIC_APP_URL is set
    3. Optional checks (WARN if missing): DELIVERY_TIMEZONE
    4. Connectivity checks: fetch health endpoint, Stripe customer list
    5. Print results table: Check | Status | Details
    6. Exit 0 if all required pass, 1 otherwise

    Add `"launch:check": "tsx scripts/launch-check.ts"` to package.json scripts.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm launch:check || true</automated>
  </verify>
  <done>LAUNCH_CHECKLIST.md covers all 11 LAUNCH items plus OBS-03 and OBS-04 with actionable steps. launch-check.ts validates env vars, DNS, and Stripe connectivity. `pnpm launch:check` command added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle audit and report (OBS-06)</name>
  <files></files>
  <action>
    1. Run `ANALYZE=true pnpm build` to generate bundle report
    2. Check first-load JS size against 200KB target
    3. If over 200KB: identify largest chunks, check optimizePackageImports coverage
    4. If 200-250KB: document as acceptable per user decision ("if 200-250KB, document and move on")
    5. If under 200KB: document as passing
    6. Record findings in SUMMARY with exact numbers:
       - Total first-load JS size
       - Largest 5 chunks with sizes
       - Any tree-shaking recommendations (for consideration, not blocking)
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Bundle audit completed. First-load JS size documented against 200KB target. Findings recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User review of launch checklist and bundle audit</name>
  <files>docs/LAUNCH_CHECKLIST.md, scripts/launch-check.ts</files>
  <action>
    Human reviews the following automated deliverables:
    1. LAUNCH_CHECKLIST.md with all 11 LAUNCH items + OBS-03/OBS-04 monitoring items
    2. `pnpm launch:check` validation script
    3. Bundle audit results

    Verification steps:
    1. Open `docs/LAUNCH_CHECKLIST.md` and verify:
       - All 11 LAUNCH items present with actionable steps
       - Admin training walkthrough matches actual dashboard (LAUNCH-09)
       - Driver test delivery steps are realistic (LAUNCH-10)
       - Refund/emergency procedures are complete (LAUNCH-11)
    2. Run `pnpm launch:check` and verify script output is clear
    3. Review bundle audit findings -- accept if within 200-250KB range per decision

    Resume: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>pnpm launch:check || true</automated>
  </verify>
  <done>User has approved the launch checklist, validation script output, and bundle audit findings.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `docs/LAUNCH_CHECKLIST.md` exists with all 11 LAUNCH items
- `scripts/launch-check.ts` compiles and runs
- `pnpm launch:check` executes (expected to show FAILs in dev environment)
- Bundle audit results documented
</verification>

<success_criteria>
- Every LAUNCH requirement (01-11) has a checklist item with actionable steps
- OBS-03 (BetterStack) and OBS-04 (Supabase Pro backup) documented
- OBS-06 bundle audit completed and results documented
- pnpm launch:check validates all programmatic items
- User approves checklist completeness and accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/95-observability-performance-testing-launch-prep/95-08-SUMMARY.md`
</output>
