---
phase: 13-legacy-removal-stricter-rules
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tracking/ETACountdown.tsx
  - src/components/tracking/TrackingMap.tsx
  - src/components/ui-v8/cart/CartItemV8.tsx
  - src/components/ui-v8/cart/FlyToCart.tsx
  - src/components/ui-v8/cart/QuantitySelector.tsx
  - src/components/ui-v8/menu/EmojiPlaceholder.tsx
  - src/components/ui-v8/menu/MenuGridV8.tsx
  - src/components/ui-v8/menu/MenuItemCardV8.tsx
  - src/components/ui/AnimatedLink.tsx
  - src/components/ui/MorphingMenu.tsx
  - src/components/ui/PriceTicker.tsx
  - src/components/ui/skeleton.tsx
  - src/lib/services/route-optimization.ts
  - src/lib/utils/order.ts
  - src/lib/web-vitals.ts
autonomous: true

must_haves:
  truths:
    - "Tracking components pass TypeScript strict checks"
    - "UI and UI-V8 components have no unused variable violations"
    - "Lib files have no unused variable violations"
  artifacts:
    - path: "src/components/tracking/**/*.tsx"
      provides: "Tracking components without unused React imports"
    - path: "src/components/ui-v8/**/*.tsx"
      provides: "UI-V8 components without unused variables"
    - path: "src/lib/**/*.ts"
      provides: "Lib files without unused variables"
  key_links:
    - from: "Component and lib files"
      to: "TypeScript compiler"
      via: "noUnusedLocals check"
---

<objective>
Fix TypeScript unused variable violations in tracking, ui-v8, ui, and lib files.

Purpose: Complete the TypeScript strict flag preparation by fixing remaining component and library files.

Output: 15 files with zero unused variable violations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-legacy-removal-stricter-rules/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tracking and ui-v8 components</name>
  <files>
    - src/components/tracking/ETACountdown.tsx
    - src/components/tracking/TrackingMap.tsx
    - src/components/ui-v8/cart/CartItemV8.tsx
    - src/components/ui-v8/cart/FlyToCart.tsx
    - src/components/ui-v8/cart/QuantitySelector.tsx
    - src/components/ui-v8/menu/EmojiPlaceholder.tsx
    - src/components/ui-v8/menu/MenuGridV8.tsx
    - src/components/ui-v8/menu/MenuItemCardV8.tsx
  </files>
  <action>
**Pattern A: Remove unused React imports**
Remove `import React from 'react';` from files that don't use React namespace features.

**Pattern B: Prefix unused variables**
Check each file for unused variables and prefix with underscore or remove if truly dead.
  </action>
  <verify>
`npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(tracking|ui-v8).*tsx" | wc -l` returns 0
  </verify>
  <done>All 8 tracking and ui-v8 component files have no unused variable violations.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ui components and lib files</name>
  <files>
    - src/components/ui/AnimatedLink.tsx
    - src/components/ui/MorphingMenu.tsx
    - src/components/ui/PriceTicker.tsx
    - src/components/ui/skeleton.tsx
    - src/lib/services/route-optimization.ts
    - src/lib/utils/order.ts
    - src/lib/web-vitals.ts
  </files>
  <action>
**Pattern A: Remove unused React imports**
Remove `import React from 'react';` from UI component files that don't use React namespace features.

**Pattern B: Prefix unused variables**
Check each file for unused variables and prefix with underscore or remove if truly dead.

For lib files, check for:
- Unused function parameters
- Unused local variables
- Unused imports
  </action>
  <verify>
`npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(ui/|lib/).*ts" | wc -l` returns 0
  </verify>
  <done>All 7 ui component and lib files have no unused variable violations.</done>
</task>

</tasks>

<verification>
Run after all tasks:
```bash
npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(tracking|ui-v8|ui/|lib/)" | wc -l
```
Expected: 0 (no errors in these directories)
</verification>

<success_criteria>
- [ ] No unused React imports in tracking, ui-v8, ui components
- [ ] All unused variables prefixed with underscore
- [ ] Lib files have no unused variables
- [ ] 15 files pass `npx tsc --noUnusedLocals --noUnusedParameters --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/13-legacy-removal-stricter-rules/13-04-SUMMARY.md`
</output>
