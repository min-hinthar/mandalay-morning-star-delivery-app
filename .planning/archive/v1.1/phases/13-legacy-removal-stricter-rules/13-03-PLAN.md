---
phase: 13-legacy-removal-stricter-rules
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/homepage/FloatingFood.tsx
  - src/components/layouts/CheckoutLayout.tsx
  - src/components/layouts/PageTransition.tsx
  - src/components/mascot/BrandMascot.tsx
  - src/components/menu/CategoryCarousel.tsx
  - src/components/menu/ItemDetail.tsx
  - src/components/menu/MenuLayout.tsx
  - src/components/menu/ModifierToggle.tsx
  - src/components/menu/VisualPreview.tsx
  - src/components/theme/DynamicThemeProvider.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage and layouts components pass TypeScript strict checks"
    - "Menu components have no unused variable violations"
  artifacts:
    - path: "src/components/menu/**/*.tsx"
      provides: "Menu components without unused React imports"
    - path: "src/components/layouts/**/*.tsx"
      provides: "Layout components without unused variables"
  key_links:
    - from: "Component files"
      to: "TypeScript compiler"
      via: "noUnusedLocals check"
---

<objective>
Fix TypeScript unused variable violations in homepage, layouts, mascot, menu, and theme components.

Purpose: Prepare component layer for TypeScript strict flags by removing unused React imports and prefixing unused variables.

Output: 10 component files with zero unused variable violations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-legacy-removal-stricter-rules/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix homepage, layouts, mascot, theme components</name>
  <files>
    - src/components/homepage/FloatingFood.tsx
    - src/components/layouts/CheckoutLayout.tsx
    - src/components/layouts/PageTransition.tsx
    - src/components/mascot/BrandMascot.tsx
    - src/components/theme/DynamicThemeProvider.tsx
  </files>
  <action>
**Pattern A: Remove unused React imports**
Remove `import React from 'react';` from files that don't use React namespace features.

**Pattern B: Prefix unused variables**
- `_isFuture` in CheckoutLayout.tsx - verify already prefixed
- `_x` in CategoryCarousel.tsx - verify already prefixed

Check for any other unused variables in these files.
  </action>
  <verify>
`npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(homepage|layouts|mascot|theme).*tsx" | wc -l` returns 0
  </verify>
  <done>All 5 homepage/layouts/mascot/theme component files have no unused variable violations.</done>
</task>

<task type="auto">
  <name>Task 2: Fix menu components</name>
  <files>
    - src/components/menu/CategoryCarousel.tsx
    - src/components/menu/ItemDetail.tsx
    - src/components/menu/MenuLayout.tsx
    - src/components/menu/ModifierToggle.tsx
    - src/components/menu/VisualPreview.tsx
  </files>
  <action>
**Pattern A: Remove unused React imports**
Remove `import React from 'react';` from files that don't use React namespace features.

**Pattern B: Prefix unused variables**
Check each file for unused variables and prefix with underscore or remove.
  </action>
  <verify>
`npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "menu.*tsx" | wc -l` returns 0
  </verify>
  <done>All 5 menu component files have no unused variable violations.</done>
</task>

</tasks>

<verification>
Run after all tasks:
```bash
npx tsc --noUnusedLocals --noUnusedParameters --noEmit 2>&1 | grep -E "(homepage|layouts|mascot|menu|theme).*tsx" | wc -l
```
Expected: 0 (no errors in these component directories)
</verification>

<success_criteria>
- [ ] No unused React imports in any of the 10 component files
- [ ] All unused variables prefixed with underscore
- [ ] 10 files pass `npx tsc --noUnusedLocals --noUnusedParameters --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/13-legacy-removal-stricter-rules/13-03-SUMMARY.md`
</output>
