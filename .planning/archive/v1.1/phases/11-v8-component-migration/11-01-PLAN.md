---
phase: 11-v8-component-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin dashboard page imports AdminDashboard directly (no v7-index)"
    - "Admin dashboard renders correctly with V8 import"
    - "KPIData type imports from direct path"
  artifacts:
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Admin dashboard page with V8 imports"
      contains: "from \"@/components/admin/AdminDashboard\""
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/components/admin/AdminDashboard.tsx"
      via: "direct import"
      pattern: "import.*AdminDashboard.*from.*admin/AdminDashboard"
---

<objective>
Migrate admin dashboard page from v7-index barrel import to direct V8 component import.

Purpose: Eliminate v7-index dependency in admin flow, enabling future barrel file removal.
Output: Admin page importing AdminDashboard directly from its source file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v8-component-migration/11-RESEARCH.md

@src/app/(admin)/admin/page.tsx
@src/components/admin/AdminDashboard.tsx
@src/components/admin/v7-index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin page imports to direct V8 paths</name>
  <files>src/app/(admin)/admin/page.tsx</files>
  <action>
Update the import statement at line 15-16:

FROM:
```typescript
import { AdminDashboard } from "@/components/admin/v7-index";
import type { KPIData } from "@/components/admin/v7-index";
```

TO:
```typescript
import { AdminDashboard } from "@/components/admin/AdminDashboard";
import type { KPIData } from "@/components/admin/AdminDashboard";
```

No other changes needed - the component usage remains identical.
  </action>
  <verify>
- `grep -r "admin/v7-index" src/app/` returns empty
- `pnpm typecheck` passes
- `pnpm build` completes without errors
  </verify>
  <done>Admin page imports AdminDashboard directly, no v7-index reference remains in admin pages</done>
</task>

<task type="auto">
  <name>Task 2: Verify admin dashboard renders correctly</name>
  <files>src/app/(admin)/admin/page.tsx</files>
  <action>
Run the dev server and verify the admin dashboard loads:

1. Start dev server if not running
2. Navigate to /admin
3. Verify KPI cards render with data
4. Verify revenue chart displays
5. Verify recent orders section shows

This is a sanity check that the import change didn't break anything.
  </action>
  <verify>
- Dev server runs without errors on /admin route
- `pnpm build` completes (confirms SSR works)
  </verify>
  <done>Admin dashboard renders correctly with direct V8 import</done>
</task>

</tasks>

<verification>
- `grep -r "admin/v7-index" src/app/` returns no matches
- `grep "AdminDashboard.*from.*admin/AdminDashboard" src/app/(admin)/admin/page.tsx` returns match
- `pnpm typecheck && pnpm build` passes
</verification>

<success_criteria>
1. Admin page imports AdminDashboard from "@/components/admin/AdminDashboard"
2. Admin page imports KPIData type from same direct path
3. No imports from admin/v7-index in src/app/ directory
4. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-v8-component-migration/11-01-SUMMARY.md`
</output>
