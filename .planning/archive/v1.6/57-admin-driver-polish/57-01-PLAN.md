---
phase: 57-admin-driver-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/admin/AdminPageHeader.tsx
  - src/components/ui/admin/CardRow.tsx
  - src/components/ui/admin/StatusBadge.tsx
  - src/components/ui/admin/InlineErrorCard.tsx
  - src/components/ui/admin/SkeletonCrossfade.tsx
  - src/components/ui/FloatingLabelInput.tsx
  - src/components/ui/EmptyState.tsx
  - src/styles/tokens.css
  - src/styles/globals.css
autonomous: true

must_haves:
  truths:
    - "AdminPageHeader renders page title with animated count badge and action buttons slot"
    - "CardRow base component provides 12px radius, scale+shadow hover, status tint, stagger entry, selected state"
    - "StatusBadge animates with soft-pulse for active statuses and stays static for terminal statuses"
    - "FloatingLabelInput animates label from inside input to above on focus (CSS peer pattern)"
    - "SkeletonCrossfade wraps any loading/content pair with AnimatePresence fade transition"
    - "InlineErrorCard replaces skeleton area with error message and retry button"
    - "EmptyState has new admin-drivers, admin-routes, driver-history variants with food emojis and CTAs"
    - "Teal accent tokens registered in @theme inline and available as Tailwind utilities"
  artifacts:
    - path: "src/components/ui/admin/AdminPageHeader.tsx"
      provides: "Unified admin page header with AnimatedValue count badge"
    - path: "src/components/ui/admin/CardRow.tsx"
      provides: "Card row primitive with hover, selection, status tint, stagger"
    - path: "src/components/ui/admin/StatusBadge.tsx"
      provides: "Animated status badge with pulse for active states"
    - path: "src/components/ui/FloatingLabelInput.tsx"
      provides: "Floating label input using CSS peer selectors"
    - path: "src/components/ui/admin/SkeletonCrossfade.tsx"
      provides: "AnimatePresence wrapper for skeleton-to-content transitions"
    - path: "src/components/ui/admin/InlineErrorCard.tsx"
      provides: "Error card with retry button for failed data loads"
  key_links:
    - from: "src/components/ui/admin/CardRow.tsx"
      to: "src/lib/motion-tokens"
      via: "staggerContainer, staggerItem, hover presets"
      pattern: "staggerContainer.*0\\.04"
    - from: "src/components/ui/admin/AdminPageHeader.tsx"
      to: "src/components/ui/admin/AdminDashboard/AnimatedValue.tsx"
      via: "AnimatedValue import for count badge"
      pattern: "AnimatedValue"
---

<objective>
Create shared primitive components for the admin/driver polish phase: AdminPageHeader, CardRow, StatusBadge, FloatingLabelInput, SkeletonCrossfade, InlineErrorCard, and new EmptyState variants. Register teal accent tokens.

Purpose: All subsequent plans depend on these reusable primitives. Building them first prevents duplication and ensures consistency across admin orders, drivers, routes, and driver pages.
Output: 7 new/updated components + teal token registration
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-admin-driver-polish/57-CONTEXT.md
@.planning/phases/57-admin-driver-polish/57-RESEARCH.md
@src/lib/motion-tokens/index.ts
@src/lib/motion-tokens/stagger.ts
@src/lib/motion-tokens/variants.ts
@src/lib/motion-tokens/core.ts
@src/components/ui/skeleton/base.tsx
@src/components/ui/EmptyState.tsx
@src/components/ui/admin/AdminDashboard/AnimatedValue.tsx
@src/components/ui/admin/AdminDashboard/KPICard.tsx
@src/components/ui/auth/MagicLinkForm.tsx
@src/styles/tokens.css
@src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teal tokens + AdminPageHeader + SkeletonCrossfade + InlineErrorCard</name>
  <files>
    src/styles/tokens.css
    src/styles/globals.css
    src/components/ui/admin/AdminPageHeader.tsx
    src/components/ui/admin/SkeletonCrossfade.tsx
    src/components/ui/admin/InlineErrorCard.tsx
  </files>
  <action>
**Teal tokens (tokens.css + globals.css):**
- Add/verify `--color-accent-teal` exists in tokens.css (light: `#00979D`, dark: `#2DD4DB`)
- Add extended status colors if missing: `--color-status-in-transit: #3B82F6` (blue), verify green/amber/red/gray exist
- In globals.css `@theme inline {}` block, add self-referencing entries: `--color-accent-teal: var(--color-accent-teal);` and any new status colors
- Add `--color-accent-teal-subtle: var(--color-accent-teal-subtle);` for card tints (light: `rgba(0,151,157,0.05)`, dark: `rgba(45,212,219,0.08)`)

**AdminPageHeader component:**

- Create `src/components/ui/admin/AdminPageHeader.tsx` with `'use client'`
- Props: `title: string`, `count?: number`, `countFormat?: 'number' | 'currency'`, `actions?: ReactNode`, `breadcrumbs?: { label: string, href?: string }[]`
- Layout: flex row with title on left (h1 text-2xl font-heading font-bold) + AnimatedValue count badge next to title (bg-accent-teal/10 text-accent-teal rounded-full px-3 py-1 text-sm font-semibold) + actions slot on right
- Breadcrumbs: optional row above title with chevron separators, each parent is a Link, last item is span (no link). Use m.div with `initial={{ opacity: 0, x: -8 }}` animate to `{ opacity: 1, x: 0 }` per crumb with 50ms stagger
- Import `AnimatedValue` from `@/components/ui/admin/AdminDashboard/AnimatedValue`

**SkeletonCrossfade:**

- Create `src/components/ui/admin/SkeletonCrossfade.tsx` with `'use client'`
- Props: `isLoading: boolean`, `skeleton: ReactNode`, `children: ReactNode`, `minDisplayMs?: number` (default 300)
- Uses `AnimatePresence mode="wait"` wrapping two `m.div` keyed "skeleton" and "content"
- Skeleton: `initial={{ opacity: 1 }}`, `exit={{ opacity: 0 }}`, `transition={{ duration: 0.2 }}`
- Content: `initial={{ opacity: 0 }}`, `animate={{ opacity: 1 }}`, `transition={{ duration: 0.2 }}`
- `minDisplayMs` uses useRef + useEffect to track mount time; if loading resolves before min, delay content render

**InlineErrorCard:**

- Create `src/components/ui/admin/InlineErrorCard.tsx`
- Props: `message?: string` (default "Failed to load data"), `onRetry?: () => void`
- Renders a rounded-xl border border-status-error/20 bg-status-error/5 p-6 card with AlertTriangle icon, message text, and "Retry" button (Button variant="outline" onClick=onRetry)
- Entry animation: `m.div` with `initial={{ opacity: 0, scale: 0.95 }}` animate to `{ opacity: 1, scale: 1 }`
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint` passes - Grep tokens.css for `accent-teal` shows the token - Grep globals.css for `accent-teal` in @theme inline block
  </verify>
  <done>AdminPageHeader renders title + count badge + breadcrumbs + actions. SkeletonCrossfade handles loading crossfade. InlineErrorCard shows error with retry. Teal tokens are registered and available.</done>
  </task>

<task type="auto">
  <name>Task 2: CardRow + StatusBadge + FloatingLabelInput + EmptyState variants</name>
  <files>
    src/components/ui/admin/CardRow.tsx
    src/components/ui/admin/StatusBadge.tsx
    src/components/ui/FloatingLabelInput.tsx
    src/components/ui/EmptyState.tsx
  </files>
  <action>
**CardRow component:**
- Create `src/components/ui/admin/CardRow.tsx` with `'use client'`
- Props: `children: ReactNode`, `statusTint?: string` (Tailwind bg class), `selected?: boolean`, `onClick?: () => void`, `className?: string`, `layoutId?: string`
- Renders `m.div` with:
  - `whileHover={{ scale: 1.01, boxShadow: "0 4px 12px rgba(0,0,0,0.08)" }}` using `spring.gentle` transition
  - `whileTap={{ scale: 0.995 }}` for click feedback
  - Base classes: `rounded-xl p-4 bg-surface-primary border border-border cursor-pointer transition-shadow`
  - Status tint applied as background class when provided
  - Selected state: add `ring-2 ring-accent-teal shadow-lg bg-accent-teal/[0.04] scale-[1.01]` (triple distinction per CONTEXT)
  - 12px border radius (rounded-xl = 12px)
- Export `cardContainer` variant function: `staggerContainer(0.04, 0.06)` -- 40ms stagger per CONTEXT decision
- Export `cardItem` variant: use `staggerItem` from motion-tokens

**StatusBadge component:**

- Create `src/components/ui/admin/StatusBadge.tsx` with `'use client'`
- Props: `status: string`, `label?: string`, `size?: 'sm' | 'md'`
- STATUS_COLORS map: `{ pending: 'bg-amber-100 text-amber-800', confirmed: 'bg-teal-100 text-teal-800', preparing: 'bg-purple-100 text-purple-800', in_transit: 'bg-blue-100 text-blue-800', out_for_delivery: 'bg-blue-100 text-blue-800', delivered: 'bg-green-100 text-green-800', completed: 'bg-green-100 text-green-800', cancelled: 'bg-gray-100 text-gray-800', failed: 'bg-red-100 text-red-800', active: 'bg-green-100 text-green-800', inactive: 'bg-gray-100 text-gray-800', skipped: 'bg-amber-100 text-amber-800' }`
- Active statuses list: `['pending', 'confirmed', 'preparing', 'in_transit', 'out_for_delivery', 'active']`
- Wrap badge in `m.div`. If active status, apply `animate={{ scale: [1, 1.05, 1], opacity: [1, 0.8, 1] }}` with `transition={{ duration: 2, repeat: 5 }}` (finite, not infinite per anti-pattern rules)
- Badge itself: `rounded-full px-2.5 py-0.5 text-xs font-semibold` (sm) or `px-3 py-1 text-sm` (md)
- Label fallback: capitalize and replace underscores with spaces

**FloatingLabelInput:**

- Create `src/components/ui/FloatingLabelInput.tsx` with `'use client'`
- Props: extend `React.InputHTMLAttributes<HTMLInputElement>` + `label: string`, `error?: string`, `icon?: LucideIcon`
- Use CSS `peer` pattern from MagicLinkForm.tsx:
  - Input: `peer w-full rounded-2xl border bg-surface-secondary/50 pl-11 pr-4 pt-6 pb-2` (with icon) or `pl-4` (without icon)
  - Label: `absolute left-11 top-1/2 -translate-y-1/2 text-sm text-muted-foreground transition-all duration-200 pointer-events-none peer-focus:top-3 peer-focus:text-xs peer-focus:text-accent-teal peer-focus:font-medium peer-[:not(:placeholder-shown)]:top-3 peer-[:not(:placeholder-shown)]:text-xs`
  - Input MUST have `placeholder=" "` (space) for `:not(:placeholder-shown)` to work
- Error state: `border-status-error` on input + red text below + add CSS keyframe `animate-shake` (translateX(-4px, 4px, -4px, 0) over 0.3s)
- Forward ref with React.forwardRef

**EmptyState variants:**

- Read existing EmptyState.tsx and add new variants to the VARIANTS config:
  - `admin-drivers`: emoji `"👨‍🍳🚗👩‍🍳"`, title `"No drivers yet"`, description `"Your delivery team is waiting to be assembled"`, actionLabel `"Add Driver"`, actionHref `undefined` (parent provides onClick)
  - `admin-routes`: emoji `"🗺️🚚📍"`, title `"No routes planned"`, description `"Time to map out today's deliveries"`, actionLabel `"Create Route"`
  - `driver-history`: emoji `"📋🕐📦"`, title `"No delivery history yet"`, description `"Your past routes and deliveries will appear here"`, actionLabel `undefined` (no CTA)
  - `admin-orders-filtered`: emoji `"🔍🍜"`, title `"No matching orders"`, description `"Try adjusting your filters to see more results"`, actionLabel `"Clear Filters"`
  - `admin-drivers-filtered`: emoji `"🔍👨‍🍳"`, title `"No matching drivers"`, description `"Try adjusting your search to see more results"`, actionLabel `"Clear Search"`
  - `admin-routes-filtered`: emoji `"🔍🗺️"`, title `"No matching routes"`, description `"Try a different date or filter to see routes"`, actionLabel `"Clear Filters"`
- Add gentle floating animation to emoji composition: wrap emoji span in `m.span` with `animate={{ y: [0, -6, 0] }}` `transition={{ duration: 3, repeat: 5, ease: "easeInOut" }}`
- Ensure every variant with an actionLabel renders a Button CTA
  </action>
  <verify> - `pnpm typecheck` passes - `pnpm lint` passes - `pnpm lint:css` passes - Grep CardRow.tsx for `staggerContainer(0.04` confirms 40ms stagger - Grep StatusBadge.tsx for `repeat: 5` confirms finite animation - Grep FloatingLabelInput.tsx for `peer` confirms CSS peer pattern - Grep EmptyState.tsx for `admin-drivers` confirms new variants
  </verify>
  <done>CardRow provides card-based row with hover/selection/tint. StatusBadge pulses for active states. FloatingLabelInput uses peer CSS pattern with shake error. EmptyState has 6 new admin/driver variants with floating emoji animation.</done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm typecheck` all pass
- All 7 new/updated component files exist and export correctly
- Teal tokens resolve in Tailwind (not transparent)
- No animate-pulse patterns in new code
- No infinite repeat animations
- No backdrop-blur without sm: prefix
</verification>

<success_criteria>
Shared primitives are ready for consumption by plans 02-08. AdminPageHeader, CardRow, StatusBadge, FloatingLabelInput, SkeletonCrossfade, InlineErrorCard all render correctly. EmptyState has admin/driver variants. Teal accent tokens registered.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-driver-polish/57-01-SUMMARY.md`
</output>
