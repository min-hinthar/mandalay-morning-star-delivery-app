---
phase: 54-email-system
plan: 05
type: execute
wave: 3
depends_on: ["54-01", "54-03", "54-04"]
files_modified:
  - src/app/api/webhooks/stripe/route.ts
  - src/app/api/admin/orders/[id]/cancel/route.ts
  - src/app/api/admin/orders/[id]/refund/route.ts
  - src/app/api/account/orders/[id]/cancel/route.ts
autonomous: true

must_haves:
  truths:
    - "Stripe webhook checks webhook_events table before processing any event (idempotency)"
    - "checkout.session.completed triggers order confirmation email via sendEmail()"
    - "charge.refunded triggers refund notification email via sendEmail()"
    - "Admin cancel route triggers cancellation email when notifyCustomer is true"
    - "Admin refund route triggers refund email when notifyCustomer is true"
    - "Customer cancel route triggers cancellation email"
    - "Email sending is async and does not block API responses"
    - "Resend idempotency keys prevent duplicate emails"
  artifacts:
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Webhook with idempotency + email triggers"
      exports: ["POST"]
    - path: "src/app/api/admin/orders/[id]/cancel/route.ts"
      provides: "Admin cancel with email trigger"
      exports: ["POST"]
    - path: "src/app/api/admin/orders/[id]/refund/route.ts"
      provides: "Admin refund with email trigger"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "webhook_events table"
      via: "idempotency check before processing"
      pattern: "webhook_events"
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendEmail() call"
      pattern: "sendEmail"
    - from: "src/app/api/admin/orders/[id]/cancel/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendEmail() for cancellation"
      pattern: "sendEmail"
    - from: "src/app/api/admin/orders/[id]/refund/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendEmail() for refund"
      pattern: "sendEmail"
---

<objective>
Wire email triggers into Stripe webhook + admin/customer API routes. Add webhook idempotency table check.

Purpose: This is the critical integration plan — connects the email service layer and templates to the actual order lifecycle events. Implements MAIL-05 (webhook idempotency).
Output: All 4 API routes trigger appropriate emails on order lifecycle events
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-email-system/54-01-SUMMARY.md
@.planning/phases/54-email-system/54-03-SUMMARY.md
@.planning/phases/54-email-system/54-04-SUMMARY.md
@src/app/api/webhooks/stripe/route.ts
@src/app/api/admin/orders/[id]/cancel/route.ts
@src/app/api/admin/orders/[id]/refund/route.ts
@src/app/api/account/orders/[id]/cancel/route.ts
@src/lib/email/send.ts
@src/emails/OrderConfirmation.tsx
@src/emails/OrderCancellation.tsx
@src/emails/RefundNotification.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stripe webhook idempotency + email triggers</name>
  <files>src/app/api/webhooks/stripe/route.ts</files>
  <action>
    Modify `src/app/api/webhooks/stripe/route.ts`:

    **1. Add webhook idempotency check at top of POST handler (after event construction, before switch):**
    ```
    // Check idempotency — has this event already been processed?
    const { data: existing } = await supabase
      .from('webhook_events')
      .select('id')
      .eq('event_id', event.id)
      .single();

    if (existing) {
      return NextResponse.json({ received: true, duplicate: true });
    }

    // Claim the event (UNIQUE constraint is the atomic guard)
    const { error: claimError } = await supabase
      .from('webhook_events')
      .insert({ event_id: event.id, event_type: event.type });

    if (claimError) {
      // Unique constraint violation = another instance processing
      return NextResponse.json({ received: true, duplicate: true });
    }
    ```

    **2. Replace sendOrderConfirmationEmail() in handleCheckoutSessionCompleted:**
    - Remove the old `sendOrderConfirmationEmail()` function entirely (lines 130-158)
    - After order status update to 'confirmed', fetch full order data (reuse the Edge Function's query pattern: order with profiles, addresses, order_items with order_item_modifiers)
    - Import sendEmail from '@/lib/email' and OrderConfirmation from '@/emails/OrderConfirmation'
    - Create OrderConfirmation React element with all order data mapped to props
    - Call sendEmail() with: to (customer email), subject with food emoji ("Your order is confirmed! Order #{orderId.slice(0,8).toUpperCase()}"), react element, type 'order_confirmation', orderId, userId, mandatory: true, idempotencyKey: `order-confirmation-${orderId}`
    - Use fire-and-forget pattern: `void sendEmail(...)` (don't await — webhook must return quickly)
    - Keep the existing logger.info after email trigger

    **3. Add refund email trigger in handleChargeRefunded:**
    - After processing refund (both full and partial), fetch order with customer email from profiles
    - Import RefundNotification from '@/emails/RefundNotification'
    - Determine isPartialRefund from charge.amount_refunded vs charge.amount
    - Call sendEmail() with: RefundNotification element, type 'refund', mandatory: true, idempotencyKey: `refund-${charge.id}`
    - Fire-and-forget pattern

    **4. Remove old SUPABASE_URL constant** (no longer needed for Edge Function call)

    **5. Add import for React (needed for JSX in route file):**
    - Add `import React from 'react'` at top if not present (needed for React.createElement in email rendering)

  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm build` succeeds
    - webhook_events idempotency check is BEFORE event processing switch
    - Old sendOrderConfirmationEmail function is removed
    - sendEmail calls use fire-and-forget pattern (void keyword)
  </verify>
  <done>
    Stripe webhook has idempotency table guard (MAIL-05), order confirmation email trigger (MAIL-01), and refund email trigger (MAIL-03). Old Edge Function call removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin + customer cancel/refund email triggers</name>
  <files>
    src/app/api/admin/orders/[id]/cancel/route.ts
    src/app/api/admin/orders/[id]/refund/route.ts
    src/app/api/account/orders/[id]/cancel/route.ts
  </files>
  <action>
    **1. Modify `src/app/api/admin/orders/[id]/cancel/route.ts`:**
    - Replace the TODO block (lines 107-114) with actual email trigger
    - When notifyCustomer is true:
      - Fetch customer email: query profiles table for order.user_id to get email + full_name
      - Fetch order items for the order summary
      - Import sendEmail from '@/lib/email', OrderCancellation from '@/emails/OrderCancellation'
      - Create OrderCancellation element with: customerName, orderId, items (simplified), totalCents, cancellationReason (reason), cancelledAt (now), refundIssued: false (admin can issue refund separately)
      - Fire-and-forget: `void sendEmail({ to: email, subject: "Your order has been cancelled", react: element, type: 'cancellation', orderId, userId: order.user_id, idempotencyKey: \`cancellation-${orderId}\` })`
    - Import React at top if needed

    **2. Modify `src/app/api/admin/orders/[id]/refund/route.ts`:**
    - Replace the TODO block (lines 212-219) with actual email trigger
    - When notifyCustomer is true:
      - Fetch customer email: query profiles for order.user_id
      - Import sendEmail from '@/lib/email', RefundNotification from '@/emails/RefundNotification'
      - Determine isPartialRefund: compare totalRefundCents to order's total (need to fetch order.total_cents in the initial query — update the select on line 61 to include total_cents)
      - Create RefundNotification element with: customerName, orderId, isPartialRefund, refundedItems, originalTotalCents (order.total_cents), refundAmountCents (totalRefundCents), shippingRefundCents, refundMethod (default "Original payment method"), refundTimeline ("3-5 business days"), processedAt (now)
      - Fire-and-forget sendEmail with type 'refund', mandatory: true, idempotencyKey: `refund-${orderId}-${Date.now()}`

    **3. Modify `src/app/api/account/orders/[id]/cancel/route.ts`:**
    - After successful order cancellation (after the update succeeds), add email trigger
    - Fetch customer email from supabase.auth.getUser() (already have user object)
    - Also query profiles for full_name
    - Fetch order items for the cancelled order
    - Import sendEmail from '@/lib/email', OrderCancellation from '@/emails/OrderCancellation'
    - Create OrderCancellation element with: customerName, orderId, items, totalCents (need to fetch total_cents — update initial select to include total_cents), cancellationReason (reason), cancelledAt, refundIssued: false
    - Fire-and-forget sendEmail with type 'cancellation', idempotencyKey: `cancellation-${orderId}`

    **Key principle:** All email sends are fire-and-forget (void). Email failure must never fail the API response. The sendEmail function handles its own retry and error logging.

  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm build` succeeds
    - All three files import sendEmail and the appropriate email template
    - TODO comments are replaced with actual email trigger code
    - No email send is awaited in the response path (fire-and-forget)
  </verify>
  <done>
    Admin cancel, admin refund, and customer cancel routes all trigger appropriate emails. Email sending is async and does not block responses.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` succeeds
- All 4 modified route files have email triggers
- Webhook has idempotency check before processing
- No await on sendEmail in response path
</verification>

<success_criteria>
All order lifecycle events trigger the correct branded email: confirmation on checkout complete, cancellation on admin/customer cancel, refund on charge.refunded and admin refund. Webhook idempotency prevents duplicate processing (MAIL-05). Email sending never blocks API responses.
</success_criteria>

<output>
After completion, create `.planning/phases/54-email-system/54-05-SUMMARY.md`
</output>
