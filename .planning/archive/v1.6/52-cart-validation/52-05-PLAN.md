---
phase: 52-cart-validation
plan: 05
type: execute
wave: 3
depends_on: ["52-03", "52-04"]
files_modified:
  - src/components/ui/cart/CartPage/CartPageContent.tsx
  - src/components/ui/cart/CartDrawer.tsx
  - src/components/ui/cart/CartItem/CartItem.tsx
autonomous: true

must_haves:
  truths:
    - "Opening the cart after a menu change shows visual indicators on sold-out items (badge, gray-out)"
    - "Items with changed prices show stale price warning with the updated price"
    - "Unavailable items show inline error with remove or replace action"
    - "Cart page at /cart is fully implemented with complete cart UI (not a stub)"
    - "Cart validation does not fire before Zustand hydration completes (no false positives on fresh page load)"
  artifacts:
    - path: "src/app/(customer)/cart/page.tsx"
      provides: "Full cart page (not a stub)"
    - path: "src/lib/hooks/useCartValidation.ts"
      provides: "Hydration-safe validation hook"
  key_links:
    - from: "src/app/(customer)/cart/page.tsx"
      to: "src/components/ui/cart/CartPage/CartPageContent.tsx"
      via: "CartPageContent component import"
      pattern: "CartPageContent"
    - from: "src/components/ui/cart/CartDrawer.tsx"
      to: "src/lib/hooks/useCartValidation.ts"
      via: "Validation on drawer mount"
      pattern: "useCartValidation"
---

<objective>
Final integration pass: polish animations, fix edge cases, ensure all 5 success criteria pass, run full verification suite.

Purpose: Ensure the entire cart validation and cart page system works end-to-end with correct animations and no regressions.
Output: Fully verified Phase 52 with all success criteria passing.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cart-validation/52-RESEARCH.md
@.planning/phases/52-cart-validation/52-01-SUMMARY.md
@.planning/phases/52-cart-validation/52-02-SUMMARY.md
@.planning/phases/52-cart-validation/52-03-SUMMARY.md
@.planning/phases/52-cart-validation/52-04-SUMMARY.md

@src/app/(customer)/cart/page.tsx
@src/components/ui/cart/CartPage/CartPageContent.tsx
@src/components/ui/cart/CartDrawer.tsx
@src/components/ui/cart/CartItem/CartItem.tsx
@src/lib/hooks/useCartValidation.ts
@src/lib/stores/cart-store.ts
@src/types/cart.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animation polish and edge case handling</name>
  <files>src/components/ui/cart/CartPage/CartPageContent.tsx, src/components/ui/cart/CartDrawer.tsx, src/components/ui/cart/CartItem/CartItem.tsx</files>
  <action>
    **Animation polish:**
    1. Checkout button pulse: when transitioning from disabled to enabled (all issues resolved), play a one-time scale 1->1.08->1 spring animation with green glow transition (per decision: pulses from disabled gray to active green). Use `m.div` with `key` change to trigger entrance animation. No infinite pulse.
    2. Attention section collapse: verify `AnimatePresence` exit animation on AttentionSection is smooth -- height collapses, opacity fades, margin shrinks to 0. Test: remove last problem item -> section smoothly disappears.
    3. Item removal slide-out: verify exit animation `{ opacity: 0, x: -200 }` works in both cart page and drawer contexts. Ensure `AnimatePresence mode="sync"` (not popLayout per existing learnings).
    4. Order total count-up: verify `PriceTicker` handles price changes correctly when items are removed or prices update. The existing PriceTicker component handles animated number transitions.
    5. Swipe-to-delete on mobile: verify existing swipe behavior works alongside validation overlays. Sold-out/unavailable items should NOT be swipeable (drag disabled when overlay present).

    **Edge cases:**
    1. Empty cart after removing all stale items: transition smoothly from AttentionSection to CartEmptyState
    2. All items are stale: only AttentionSection visible, no category groups, checkout fully disabled
    3. Cart with single item that becomes sold-out: replacement should work, attention section shows 1 item
    4. Network error during validation: verify silent fail -- no error UI shown, checkout button stays enabled, items render normally
    5. Rapid navigation between cart page and menu: validation doesn't fire on stale Zustand state
    6. Hydration race: on fresh page load with items in localStorage, no flash of empty cart before hydration completes. Skeleton/loading shown until hydrated.
    7. Price change badge dismissal: after dismissing, the item's price in CartSummary/CartPageSummary reflects the new price immediately

    **Final integration checks:**
    1. Cart page swipe-to-delete on mobile matches existing cart drawer behavior (red area with trash icon)
    2. "Continue Shopping" and "+ Add more items" both link to /menu
    3. "Clear Cart" uses existing `ClearCartConfirmation` dialog (no new implementation needed)
    4. CartNavigationGuard still works on cart page (preserved from stub)

  </action>
  <verify>
    Run full verification suite:
    ```bash
    pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build
    ```
    All must pass with zero errors.
  </verify>
  <done>All animations are polished and finite. Edge cases handled. Full verification suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 52 success criteria verification</name>
  <files></files>
  <action>
    Verify each of the 5 phase success criteria:

    1. **"Opening the cart after a menu change shows visual indicators on sold-out items (badge, gray-out)"**
       - Confirm: CartItem with `validationStatus='sold-out'` renders ValidationOverlay with gray overlay + amber "Sold Out" badge
       - Confirm: Both cart page and cart drawer show this treatment

    2. **"Items with changed prices show stale price warning with the updated price"**
       - Confirm: CartItem with `validationStatus='price-changed'` renders PriceChangeBadge
       - Confirm: Badge shows amber (price up) or green (price down)
       - Confirm: Badge is dismissable, dismissing updates persisted price

    3. **"Unavailable items show inline error with remove or replace action"**
       - Confirm: CartItem with `validationStatus='unavailable'` renders ValidationOverlay with red "Unavailable" badge + remove button
       - Confirm: SuggestionRow appears below with up to 3 replacement suggestions
       - Confirm: Tapping suggestion auto-replaces with original quantity

    4. **"Cart page at /cart is fully implemented with complete cart UI (not a stub)"**
       - Confirm: /cart page renders CartPageContent (not placeholder text)
       - Confirm: Two-column layout on desktop, single column on mobile
       - Confirm: Items grouped by category, order summary with itemized breakdown, checkout gate

    5. **"Cart validation does not fire before Zustand hydration completes (no false positives on fresh page load)"**
       - Confirm: useCartHydrated gates validation
       - Confirm: Skeleton/loading shown during hydration
       - Confirm: No "empty cart" flash on page refresh when cart has items

    Run the full verification suite one final time:
    ```bash
    pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build
    ```

    If any E2E tests exist for cart flow, run those too:
    ```bash
    pnpm test:e2e -- --grep "cart" 2>/dev/null || echo "No cart E2E tests"
    ```

  </action>
  <verify>All 5 success criteria verified. Full build passes. No lint/type errors.</verify>
  <done>Phase 52 complete. All 5 success criteria verified. Cart validates items against menu, shows visual indicators for stale/sold-out/unavailable/price-changed items, cart page is fully functional, and validation is hydration-safe.</done>
</task>

</tasks>

<verification>
1. `pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build` all pass
2. All 5 phase success criteria verified (see task 2)
3. No infinite animations (mobile safety)
4. No AnimatePresence mode="popLayout" (crash prevention)
5. Cart drawer validation matches cart page validation treatment
6. Existing E2E cart tests still pass (if any)
</verification>

<success_criteria>

- All 5 phase success criteria pass
- Full verification suite passes with zero errors
- No animation regressions
- Cart validation is hydration-safe
- Cart page replaces stub completely
  </success_criteria>

<output>
After completion, create `.planning/phases/52-cart-validation/52-05-SUMMARY.md`
</output>
