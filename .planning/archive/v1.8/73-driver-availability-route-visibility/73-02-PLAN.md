---
phase: 73-driver-availability-route-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/driver/routes/upcoming/route.ts
  - src/app/(driver)/driver/schedule/page.tsx
  - src/app/(driver)/driver/schedule/SchedulePageClient.tsx
  - src/components/ui/driver/DriverDashboard/NextRouteChip.tsx
  - src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
  - src/components/ui/driver/DriverDashboard/types.ts
  - src/app/(driver)/driver/page.tsx
autonomous: true
requirements: [DDASH-04, DDASH-05]

must_haves:
  truths:
    - "Driver sees upcoming assigned routes beyond today on the schedule page"
    - "Schedule page shows routes grouped by day-of-week headers"
    - "Driver home shows a 'Next route: [day]' chip when future route exists"
    - "Tapping the next route chip navigates to /driver/schedule"
  artifacts:
    - path: "src/app/api/driver/routes/upcoming/route.ts"
      provides: "GET upcoming routes endpoint"
      exports: ["GET"]
    - path: "src/app/(driver)/driver/schedule/page.tsx"
      provides: "Server component with auth + data fetch"
    - path: "src/app/(driver)/driver/schedule/SchedulePageClient.tsx"
      provides: "Client component with grouped route list"
    - path: "src/components/ui/driver/DriverDashboard/NextRouteChip.tsx"
      provides: "Passive next-route indicator chip"
  key_links:
    - from: "src/app/(driver)/driver/schedule/page.tsx"
      to: "/api/driver/routes/upcoming"
      via: "Server-side Supabase query (direct, not API call)"
      pattern: "routes.*delivery_date.*gt"
    - from: "src/components/ui/driver/DriverDashboard/NextRouteChip.tsx"
      to: "/driver/schedule"
      via: "Link navigation"
      pattern: "href.*driver/schedule"
    - from: "src/app/(driver)/driver/page.tsx"
      to: "NextRouteChip"
      via: "Component rendering with nextRouteDate prop"
      pattern: "NextRouteChip"
---

<objective>
Upcoming routes API, full schedule page with day-grouped route cards, and "Next route" chip on driver dashboard.

Purpose: Drivers can see their upcoming assigned routes beyond today and navigate to a weekly schedule view.
Output: Upcoming routes API, schedule page with HistorySummaryCard-based route list, dashboard next-route indicator.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-driver-availability-route-visibility/73-RESEARCH.md

@src/app/(driver)/driver/page.tsx
@src/app/(driver)/driver/schedule/page.tsx
@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/types.ts
@src/components/ui/driver/DriverDashboard/HistorySummaryCard.tsx
@src/app/api/driver/routes/history/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upcoming routes API and schedule page</name>
  <files>
    src/app/api/driver/routes/upcoming/route.ts
    src/app/(driver)/driver/schedule/page.tsx
    src/app/(driver)/driver/schedule/SchedulePageClient.tsx
  </files>
  <action>
1. **Upcoming routes API** (`src/app/api/driver/routes/upcoming/route.ts`):
   - GET endpoint with `requireDriver()` auth and `checkRateLimit(driverActionLimiter)`
   - Query `routes` table: `driver_id = driverId`, `delivery_date >= todayStr`, `status in ['planned', 'in_progress']`, order by `delivery_date asc`, limit 14
   - For each route, also fetch stops via join or separate query: `route_stops` with `route_id = route.id`, including order address for display
   - Use LA timezone for today calculation (same `getDateInfo` pattern from driver page.tsx -- use `Intl.DateTimeFormat("en-CA", { timeZone: "America/Los_Angeles" })`)
   - Return shape: `{ routes: UpcomingRoute[] }` where `UpcomingRoute` has `id, deliveryDate, status, stopCount, stops: { id, status, address }[]`
   - Transform stops to match `HistoryStopData` interface so `HistorySummaryCard` can render them directly: `{ id, status, address, deliveredAt: null }`

2. **Schedule page server component** (`src/app/(driver)/driver/schedule/page.tsx`):
   - Replace the existing placeholder with a full server component
   - Use `createClient()` + auth check (same pattern as driver/page.tsx)
   - Query upcoming routes directly via Supabase (server-side, not API call):
     ```
     routes: id, delivery_date, status, stats_json, started_at
     ```
     Where `driver_id = driverId`, `delivery_date >= todayStr`, `status in ['planned', 'in_progress']`, order `delivery_date asc`, limit 14
   - For each route, query `route_stops` joined with `orders` joined with `addresses` to get stop addresses
   - Transform to `HistoryRouteData[]` format (same shape as history page uses)
   - Also fetch driver's `availability_json` from drivers table
   - Pass data to `SchedulePageClient`
   - Add `Suspense` wrapper with skeleton loading (same pattern as driver/page.tsx)

3. **Schedule page client component** (`src/app/(driver)/driver/schedule/SchedulePageClient.tsx`):
   - `'use client'` directive
   - Props: `routes: HistoryRouteData[]`, `availability: DriverAvailability | null`
   - Top section: Availability pills row using `DayOfWeekPills` from `@/components/ui/driver/AvailabilityPicker` -- display only (non-interactive here, read from props). Show selected days as amber-600, unselected as muted. Label: "Your Available Days"
     - NOTE: The availability pills on schedule page are **read-only display**. Editing availability happens via a separate section or the settings. For simplicity, make them clickable here too -- when changed, PATCH to `/api/driver/availability` to save. This gives the driver a natural place to manage availability.
   - Route list: Group routes by date header. Use `Intl.DateTimeFormat("en-US", { weekday: "long", month: "short", day: "numeric" })` for headers like "Saturday, Feb 22"
   - Each route renders as `HistorySummaryCard` (import from `@/components/ui/driver/DriverDashboard/HistorySummaryCard`)
   - Stagger animation using `staggerContainer` from `@/lib/motion-tokens`
   - Empty state when no upcoming routes: centered icon (CalendarDays from lucide), "No upcoming routes" heading, "Routes will appear here when assigned by admin" subtext. Style: `rounded-2xl bg-surface-primary p-6 shadow-card border border-border text-center`
   - Header: CalendarDays icon + "Schedule" h1 (matching existing earnings page header pattern)
   - Import `useAnimationPreference` for animation toggling
   - Wrap in `px-4 py-6 space-y-6` container
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Schedule page renders without errors (replaces placeholder)
    - API endpoint returns JSON with routes array
  </verify>
  <done>
    - GET /api/driver/routes/upcoming returns upcoming routes with stops
    - Schedule page shows routes grouped by day with HistorySummaryCard
    - Availability pills displayed at top of schedule page (interactive)
    - Empty state shown when no upcoming routes
  </done>
</task>

<task type="auto">
  <name>Task 2: Next route chip on driver dashboard</name>
  <files>
    src/components/ui/driver/DriverDashboard/NextRouteChip.tsx
    src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
    src/components/ui/driver/DriverDashboard/types.ts
    src/components/ui/driver/DriverDashboard/index.tsx
    src/app/(driver)/driver/page.tsx
  </files>
  <action>
1. **NextRouteChip component** (`src/components/ui/driver/DriverDashboard/NextRouteChip.tsx`):
   - `'use client'` directive
   - Props: `nextRouteDate: string` (YYYY-MM-DD format)
   - Format date to display day name: `new Intl.DateTimeFormat("en-US", { weekday: "long" }).format(new Date(nextRouteDate + "T12:00:00Z"))`
   - Render as a tappable Link to `/driver/schedule`
   - Layout: horizontal chip with CalendarDays icon + "Next route: {DayName}" + ChevronRight icon
   - Style: `inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-accent-teal/10 border border-accent-teal/20 text-accent-teal text-sm font-medium`
   - Framer Motion: fade in from below with spring animation (same pattern as other dashboard cards)
   - Use `useAnimationPreference` for animation toggle

2. **Update types** (`src/components/ui/driver/DriverDashboard/types.ts`):
   - Add `nextRouteDate?: string | null;` to `DriverDashboardProps`

3. **Update DriverDashboard** (`src/components/ui/driver/DriverDashboard/DriverDashboard.tsx`):
   - Accept `nextRouteDate` prop from `DriverDashboardProps`
   - Render `<NextRouteChip nextRouteDate={nextRouteDate} />` below the `<RouteCard>` section, only when `nextRouteDate` is truthy and `todayRoute` is null or absent (show "next route" hint when there's no route today)
   - Actually, per CONTEXT.md: show chip below RouteCard when a future route exists. So render when `nextRouteDate` is truthy regardless of todayRoute state.

4. **Update barrel** (`src/components/ui/driver/DriverDashboard/index.tsx`):
   - Add `export { NextRouteChip } from "./NextRouteChip";`

5. **Update driver home page** (`src/app/(driver)/driver/page.tsx`):
   - In `getDriverData()`, add a query for the next upcoming route after today:
     ```typescript
     const nextRouteResult = await supabase
       .from("routes")
       .select("delivery_date")
       .eq("driver_id", driver.id)
       .gt("delivery_date", todayStr)
       .in("status", ["planned", "in_progress"])
       .order("delivery_date", { ascending: true })
       .limit(1)
       .single();
     ```
   - Add to the Promise.all (parallel with existing queries)
   - Return `nextRouteDate: nextRouteResult.data?.delivery_date ?? null` in the data object
   - Pass `nextRouteDate` to `<DriverDashboard>` component
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - NextRouteChip renders with correct day name
    - Driver home page passes nextRouteDate to dashboard
  </verify>
  <done>
    - NextRouteChip shows "Next route: Saturday" (or relevant day) below route card
    - Chip taps navigate to /driver/schedule
    - Dashboard only shows chip when a future route exists
    - Driver page fetches next route date in parallel with other queries
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` -- no type errors
- `pnpm lint` -- all files pass
- `pnpm build` -- production build succeeds
- Schedule page at /driver/schedule renders route list (or empty state)
- Dashboard at /driver shows NextRouteChip when future route exists
</verification>

<success_criteria>
- Upcoming routes visible on schedule page grouped by day
- Driver home shows passive "Next route" indicator
- Schedule page has availability pills at top
- Empty state handles no-routes gracefully
- All components follow established animation and token patterns
</success_criteria>

<output>
After completion, create `.planning/phases/73-driver-availability-route-visibility/73-02-SUMMARY.md`
</output>
