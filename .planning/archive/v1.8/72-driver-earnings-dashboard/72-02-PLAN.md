---
phase: 72-driver-earnings-dashboard
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - src/app/(driver)/driver/page.tsx
  - src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
  - src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx
  - src/components/ui/driver/DriverDashboard/types.ts
  - src/components/ui/driver/DriverDashboard/index.tsx
  - src/app/api/driver/routes/[routeId]/complete/route.ts
  - src/lib/badges/thresholds.ts
  - src/lib/badges/index.ts
autonomous: true
requirements: [DDASH-01, DDASH-11, DDASH-12]

must_haves:
  truths:
    - "Dashboard shows weekly earnings summary card with real computed amount"
    - "Earnings card shows Today's Earnings and This Week in 2-column grid"
    - "Route completion triggers badge award check and inserts new badges"
    - "Badge thresholds match: first_delivery(1), delivery_10(10), delivery_50(50), delivery_100(100), streak_5(5), streak_10(10), five_star(rating>=5.0 AND deliveries>=10)"
  artifacts:
    - path: "src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx"
      provides: "2-column earnings grid with StatCard pattern"
      contains: "EarningsSummaryCard"
    - path: "src/lib/badges/thresholds.ts"
      provides: "Badge threshold definitions and award logic"
      exports: ["BADGE_THRESHOLDS", "checkAndAwardBadges"]
    - path: "src/app/api/driver/routes/[routeId]/complete/route.ts"
      provides: "Route completion with badge award"
      contains: "checkAndAwardBadges"
  key_links:
    - from: "src/components/ui/driver/DriverDashboard/DriverDashboard.tsx"
      to: "src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx"
      via: "import and render"
      pattern: "EarningsSummaryCard"
    - from: "src/app/api/driver/routes/[routeId]/complete/route.ts"
      to: "src/lib/badges/thresholds.ts"
      via: "import checkAndAwardBadges"
      pattern: "checkAndAwardBadges"
    - from: "src/app/(driver)/driver/page.tsx"
      to: "app_settings"
      via: "query for driver_pay_per_stop_cents"
      pattern: "driver_pay_per_stop_cents"
---

<objective>
Wire dashboard earnings card to real computed data and add badge award logic to route completion.

Purpose: Dashboard shows actual earnings instead of dead code. Route completion awards badges based on delivery count and streak thresholds.
Output: Live earnings summary on dashboard, badge award on route completion.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-driver-earnings-dashboard/72-RESEARCH.md
@.planning/phases/72-driver-earnings-dashboard/72-01-SUMMARY.md
@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/types.ts
@src/components/ui/driver/DriverDashboard/StatCard.tsx
@src/app/(driver)/driver/page.tsx
@src/app/api/driver/routes/[routeId]/complete/route.ts
@supabase/migrations/021_driver_gamification.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: EarningsSummaryCard + dashboard wiring</name>
  <files>
    src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx
    src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
    src/components/ui/driver/DriverDashboard/types.ts
    src/components/ui/driver/DriverDashboard/index.tsx
    src/app/(driver)/driver/page.tsx
  </files>
  <action>
    1. **EarningsSummaryCard.tsx** — new client component:
       - 2-column grid (`grid-cols-2 gap-4`) with two StatCards
       - Left card: "Today's Earnings" with `DollarSign` icon, `animatedFormat="currency"`, `color="bg-secondary"`
       - Right card: "This Week" with `Banknote` icon, `animatedFormat="currency"`, `color="bg-accent-teal"`
       - Props: `todayEarningsCents: number, weeklyEarningsCents: number`
       - Use existing `StatCard` component (already supports AnimatedValue with format="currency")
       - Format value: `(cents / 100)` for display — AnimatedValue handles the dollar sign
       - Wrap in `m.div` with staggerContainer pattern matching existing stats grid
       - `'use client'` directive required

    2. **types.ts** — update DriverDashboardProps:
       - Change `weeklyEarningsCents?: number` to `weeklyEarningsCents: number` (no longer optional — always computed)
       - Add `todayEarningsCents: number`
       - Remove the underscore prefix on weeklyEarningsCents usage in DriverDashboard.tsx

    3. **DriverDashboard.tsx** — integrate EarningsSummaryCard:
       - Import and render `EarningsSummaryCard` between ProfileCompletenessCard and StreakDisplay
       - Pass `todayEarningsCents` and `weeklyEarningsCents` props
       - Remove `_weeklyEarningsCents` destructure aliasing — use `weeklyEarningsCents` directly
       - Keep existing StatCard grid (Total Deliveries, Rating) below the earnings card

    4. **index.tsx** — re-export EarningsSummaryCard from barrel

    5. **page.tsx (driver home)** — wire real earnings data:
       - Query `app_settings` for `driver_pay_per_stop_cents` (readable by all roles, USING(true))
       - Query today's completed route_stops for today's earnings: count delivered stops for today * rate
       - Use existing `weeklyResult` from `calculate_driver_weekly_deliveries` RPC — multiply by rate for weeklyEarningsCents
       - Remove `void weeklyResult;` line — this is now used
       - Pass `todayEarningsCents` and `weeklyEarningsCents` to DriverDashboard
       - Add queries to existing `Promise.all` for parallel execution
  </action>
  <verify>
    - `pnpm typecheck` passes (no optional vs required type errors)
    - `pnpm lint` passes
    - Dashboard renders EarningsSummaryCard with two stat cards
    - Earnings values are computed (not hardcoded 0)
  </verify>
  <done>
    Dashboard shows 2-column earnings grid above route card. Today's Earnings shows computed amount from today's delivered stops. This Week shows computed weekly total. Both use animated count-up.
  </done>
</task>

<task type="auto">
  <name>Task 2: Badge award logic on route completion</name>
  <files>
    src/lib/badges/thresholds.ts
    src/lib/badges/index.ts
    src/app/api/driver/routes/[routeId]/complete/route.ts
  </files>
  <action>
    1. **Badge thresholds (src/lib/badges/thresholds.ts):**
       - Define `BADGE_THRESHOLDS` array matching CONTEXT decisions:
         ```
         { type: 'first_delivery', threshold: 1, name: 'First Delivery', icon: 'star-emoji' }
         { type: 'delivery_10', threshold: 10, name: '10 Deliveries', icon: 'star-emoji' }
         { type: 'delivery_50', threshold: 50, name: '50 Deliveries', icon: 'trophy-emoji' }
         { type: 'delivery_100', threshold: 100, name: '100 Deliveries', icon: 'gem-emoji' }
         { type: 'streak_5', threshold: 5, name: '5-Day Streak', icon: 'fire-emoji' }
         { type: 'streak_10', threshold: 10, name: '10-Day Streak', icon: 'star2-emoji' }
         { type: 'five_star', threshold: null, name: 'Perfect Rating', icon: 'sparkles-emoji' }
         ```
       - Export `checkAndAwardBadges(serviceSupabase, driverId, stats)` async function:
         - `stats` shape: `{ totalDeliveries: number, streakDays: number, ratingAvg: number }`
         - Query existing `driver_badges` for this driver to get earned badge_types
         - For delivery count badges: check `stats.totalDeliveries >= threshold`
         - For streak badges: check `stats.streakDays >= threshold`
         - For five_star: check `stats.ratingAvg >= 5.0 AND stats.totalDeliveries >= 10` (CRITICAL: both conditions required per CONTEXT)
         - Filter out already-earned badges
         - INSERT new badges using **service client** (admin-only INSERT policy on driver_badges)
         - Return array of newly awarded badge names (for potential toast notification)
       - Barrel re-export from `src/lib/badges/index.ts`

    2. **Route completion endpoint — add badge award:**
       - Import `checkAndAwardBadges` from `@/lib/badges`
       - Import `createServiceClient` from `@/lib/supabase/server`
       - After successful route completion (after status update to "completed"):
         - Get updated delivery count: `driver.deliveries_count + stats.delivered_stops`
         - Call `calculate_driver_streak` RPC with `p_driver_id`
         - Get rating from driver record
         - Call `checkAndAwardBadges(serviceClient, driverId, { totalDeliveries, streakDays, ratingAvg })`
         - Log newly awarded badges via logger.info
         - Include `newBadges` in response JSON (so client can optionally show toast)
       - Use try/catch around badge logic — badge failure should NOT fail route completion
       - Use service client for badge INSERT (driver_badges has admin-only INSERT policy)
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Badge thresholds match CONTEXT exactly (7 badge types)
    - five_star badge requires BOTH ratingAvg >= 5.0 AND totalDeliveries >= 10
    - Badge INSERT uses service client (not user client)
    - Route completion still works even if badge award fails
  </verify>
  <done>
    Route completion triggers badge check. New badges inserted to driver_badges table. five_star badge requires 10+ deliveries AND 5.0 rating. Badge failure does not block route completion. Response includes newBadges array.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm build` — all pass
- Dashboard shows Today's Earnings and This Week cards with real data
- Route completion response includes newBadges array
- Badge thresholds: 7 types defined, five_star requires dual condition
- No regression on existing dashboard features (streak, route card, badges display)
</verification>

<success_criteria>
- Dashboard earnings card replaces dead `weeklyEarningsCents` prop with real computed data
- EarningsSummaryCard renders 2-column grid with animated currency values
- Badge award logic runs on every route completion
- Service client used for badge INSERT (respects admin-only INSERT policy)
- Badge failure is non-blocking (wrapped in try/catch)
</success_criteria>

<output>
After completion, create `.planning/phases/72-driver-earnings-dashboard/72-02-SUMMARY.md`
</output>
