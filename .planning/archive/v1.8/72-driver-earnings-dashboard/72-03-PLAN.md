---
phase: 72-driver-earnings-dashboard
plan: 03
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - src/app/(driver)/driver/earnings/page.tsx
  - src/app/(driver)/driver/earnings/EarningsPageClient.tsx
  - src/app/(driver)/driver/earnings/loading.tsx
autonomous: true
requirements: [DDASH-02, DDASH-03, DDASH-11]

must_haves:
  truths:
    - "Earnings page shows AreaChart with weekly/monthly/daily period toggle"
    - "Per-route earnings breakdown with collapsible card rows"
    - "Period toggle switches between Daily (14d), Weekly (12wk default), Monthly (12mo)"
    - "All earned badges displayed on earnings page (not limited to 5)"
    - "Chart uses saffron color (var(--color-secondary)) matching CHART_COLORS.primary"
  artifacts:
    - path: "src/app/(driver)/driver/earnings/page.tsx"
      provides: "Server component that fetches earnings data and passes to client"
      exports: ["default"]
    - path: "src/app/(driver)/driver/earnings/EarningsPageClient.tsx"
      provides: "Client component with chart, period toggle, route breakdown, badges"
      exports: ["EarningsPageClient"]
  key_links:
    - from: "src/app/(driver)/driver/earnings/page.tsx"
      to: "/api/driver/earnings"
      via: "server-side fetch or direct Supabase query"
      pattern: "earnings"
    - from: "src/app/(driver)/driver/earnings/EarningsPageClient.tsx"
      to: "src/components/ui/admin/analytics/PerformanceChart.tsx"
      via: "import PerformanceChart"
      pattern: "PerformanceChart"
    - from: "src/app/(driver)/driver/earnings/EarningsPageClient.tsx"
      to: "src/components/ui/driver/DriverDashboard/BadgesDisplay.tsx"
      via: "import BadgesDisplay (or inline all-badges variant)"
      pattern: "BadgesDisplay|badges"
---

<objective>
Full earnings page with chart, period toggle, per-route breakdown, and all-badges display.

Purpose: Drivers see their earnings history with visual trends and route-level detail.
Output: Complete /driver/earnings page with interactive chart and period selection.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-driver-earnings-dashboard/72-RESEARCH.md
@.planning/phases/72-driver-earnings-dashboard/72-01-SUMMARY.md
@src/components/ui/admin/analytics/PerformanceChart.tsx
@src/components/ui/driver/DriverDashboard/BadgesDisplay.tsx
@src/components/ui/driver/DriverDashboard/HistorySummaryCard.tsx
@src/lib/earnings/compute.ts
@src/app/api/driver/earnings/route.ts
@src/types/analytics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Earnings page server component with data fetching</name>
  <files>
    src/app/(driver)/driver/earnings/page.tsx
    src/app/(driver)/driver/earnings/loading.tsx
  </files>
  <action>
    1. **page.tsx** — replace the placeholder from Plan 01:
       - Server component wrapped in `<Suspense fallback={<EarningsLoading />}>`
       - Inner async component `EarningsPageContent`:
         - Authenticate with `createClient()` + `getUser()`
         - Get driver record (id, deliveries_count, rating_avg)
         - Parallel queries:
           a. `app_settings` for `driver_pay_per_stop_cents` (fallback 500)
           b. `routes` with nested `route_stops` for last 12 months (driver_id match, order by delivery_date desc)
           c. `driver_badges` for this driver (all badges, ordered by earned_at desc)
           d. `calculate_driver_streak` RPC
         - Use `computeRouteEarnings` from `@/lib/earnings` to transform routes
         - Pass all data to `<EarningsPageClient>`:
           ```
           routeEarnings, rateCents, badges, streakDays, driverStats: { deliveriesCount, ratingAvg }
           ```
       - Redirect to login if unauthenticated, to / if no driver record

    2. **loading.tsx** — update the skeleton from Plan 01:
       - Keep or enhance the skeleton with:
         - Period toggle skeleton (3 pills)
         - Chart area skeleton (h-[220px] on mobile)
         - "Total Earnings" header skeleton
         - 3 route card skeletons
         - Badge section skeleton
       - Use `Skeleton` from `@/components/ui/skeleton/base`
       - Match driver page loading pattern
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Page renders with Suspense boundary
    - Data fetching uses parallel Promise.all
    - Redirects work for unauthenticated/non-driver users
  </verify>
  <done>
    /driver/earnings loads with real data. Server component fetches all earnings data in parallel and passes to client component. Loading state shows skeleton.
  </done>
</task>

<task type="auto">
  <name>Task 2: Earnings client component with chart, period toggle, route breakdown, badges</name>
  <files>
    src/app/(driver)/driver/earnings/EarningsPageClient.tsx
  </files>
  <action>
    1. **EarningsPageClient.tsx** — `'use client'` component:

    **Layout structure (top to bottom):**
    a. **Header:** "Earnings" heading with Banknote icon, total earnings for selected period displayed prominently
    b. **Period toggle:** Pill selector matching admin analytics pattern:
       - Container: `rounded-lg border bg-surface-primary p-1 flex gap-0.5`
       - Active pill: `bg-secondary text-text-inverse rounded-md px-3 py-1.5 text-sm font-medium`
       - Inactive: `text-text-secondary hover:text-text-primary`
       - Options: Daily (14 days), Weekly (12 weeks — default), Monthly (12 months)
       - State: `useState<'daily' | 'weekly' | 'monthly'>('weekly')`
    c. **Chart section:**
       - Use existing `PerformanceChart` component with `type="area"`
       - Color: `var(--color-secondary)` (saffron) matching CHART_COLORS.primary
       - Height: 220 on mobile (default), override to 300 on desktop via container
       - Data: `aggregateByPeriod(routeEarnings, selectedPeriod)` mapped to `DailyMetricPoint[]` format (`{ date: label, value: cents/100 }`)
       - Title: "Earnings Trend"
       - Re-aggregate on period change using `useMemo`
    d. **Per-route breakdown section:**
       - Heading: "Route Earnings" with route count
       - Filter `routeEarnings` by selected period's date range
       - Collapsible card rows using the `HistorySummaryCard` expand/collapse pattern but adapted for earnings:
         - Collapsed: date, stop count, earnings amount (`$X.XX`)
         - Expanded: individual stop details (reuse existing stop rendering from HistorySummaryCard)
       - Since HistorySummaryCard has a specific interface, create inline `EarningsRouteCard` within this file that follows the same AnimatePresence expand/collapse pattern:
         - m.div with initial/animate for stagger
         - onClick toggle expanded state
         - ChevronDown icon rotates on expand
         - Collapsed: date + delivered stops count + earnings formatted as `$XX.XX`
         - Expanded: "X stops delivered at $Y.XX per stop = $Z.ZZ"
       - Card shell: `bg-surface-primary/80 backdrop-blur-sm rounded-2xl border-2 shadow-card` per CONTEXT
       - NOTE: Do NOT use backdrop-blur on the BottomNav (mobile crash), but these are content cards not fixed nav — blur is safe here
    e. **Badges section (at bottom):**
       - Heading: "Achievements" with Award icon
       - Show ALL badges (not limited to 5 like dashboard)
       - Reuse the visual pattern from `BadgesDisplay` but without the 5-badge limit:
         - 48x48 circular container, gradient bg, emoji icon
         - Entry animation: `scale: 0 → 1` with spring.ultraBouncy + 0.1s stagger
       - Grid layout: `grid grid-cols-4 gap-3` (or flex-wrap for variable count)
       - Empty state: "Complete deliveries to earn badges!"
       - If streak > 0, show StreakDisplay component at top of badges section

    **Props interface:**
    ```typescript
    interface EarningsPageClientProps {
      routeEarnings: RouteEarning[];
      rateCents: number;
      badges: Array<{ id: string; name: string; icon: string; earnedAt: string }>;
      streakDays: number;
      driverStats: { deliveriesCount: number; ratingAvg: number };
    }
    ```

    **Animation:**
    - Page entrance: staggerContainer for sections
    - Period toggle: instant switch (no animation on toggle itself)
    - Chart: PerformanceChart handles its own entry animation
    - Route cards: stagger with 0.04s delay per card

    **Error handling:**
    - If routeEarnings is empty, show empty state: "No earnings yet. Complete your first delivery to see earnings here!"
    - If chart data is empty for selected period, show "No data for this period" in chart area
  </action>
  <verify>
    - `pnpm typecheck` passes (all prop types match)
    - `pnpm lint` passes (semantic tokens, no raw colors)
    - Period toggle switches between 3 states
    - Chart renders with area type and saffron color
    - Route cards expand/collapse with animation
    - All badges render (not limited to 5)
    - Empty states display correctly
  </verify>
  <done>
    Earnings page shows period toggle (Daily/Weekly/Monthly), area chart with saffron trend line, per-route earnings breakdown with expand/collapse cards, and full badges grid. Period selection re-aggregates chart and filters route list.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm build` — all pass
- /driver/earnings renders with chart, toggle, route breakdown, badges
- Period toggle switches between Daily/Weekly/Monthly views
- Chart uses AreaChart with saffron color
- Route cards expand to show per-stop earnings detail
- Badges section shows all earned badges (not limited to 5)
- Loading skeleton displays during Suspense
- Empty state shown when no earnings data
</verification>

<success_criteria>
- Earnings page is the definitive view of driver earnings history
- Chart shows trends across configurable time periods
- Per-route breakdown gives line-item detail
- All badges displayed with animations
- Page loads efficiently with server-side data fetching
- Consistent with driver app visual patterns (glass cards, semantic tokens, motion)
</success_criteria>

<output>
After completion, create `.planning/phases/72-driver-earnings-dashboard/72-03-SUMMARY.md`
</output>
