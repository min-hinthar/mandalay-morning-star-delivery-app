---
phase: 69-distributed-rate-limiting
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - src/lib/supabase/actions.ts
  - src/lib/utils/rate-limit.ts
  - src/app/api/checkout/session/route.ts
  - src/app/api/driver/location/route.ts
  - src/app/api/driver/onboard/route.ts
  - src/app/api/orders/[id]/cancel/route.ts
  - src/app/api/orders/[id]/retry-payment/route.ts
  - src/app/api/orders/[id]/rating/route.ts
  - src/app/api/orders/[id]/notes/route.ts
  - src/app/api/account/orders/[id]/reorder/route.ts
  - src/app/api/menu/route.ts
  - src/app/api/menu/search/route.ts
  - src/app/api/sections/route.ts
  - src/app/api/coverage/check/route.ts
  - src/app/api/analytics/vitals/route.ts
  - src/app/api/tracking/[orderId]/route.ts
  - src/app/api/addresses/route.ts
  - src/app/api/addresses/[id]/route.ts
  - src/app/api/account/profile/route.ts
  - src/app/api/account/settings/route.ts
  - src/app/api/driver/me/route.ts
  - src/app/api/driver/routes/active/route.ts
  - src/app/api/driver/routes/history/route.ts
  - src/app/api/driver/routes/[routeId]/route.ts
  - src/app/api/driver/routes/[routeId]/start/route.ts
  - src/app/api/driver/routes/[routeId]/complete/route.ts
  - src/app/api/driver/routes/[routeId]/stops/[stopId]/route.ts
  - src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts
  - src/app/api/driver/routes/[routeId]/stops/[stopId]/exception/route.ts
  - src/app/api/health/route.ts
autonomous: true

must_haves:
  truths:
    - "Auth Server Actions (signIn, resend invite) enforce rate limits via Upstash Redis instead of in-memory Map"
    - "Checkout endpoint (POST /api/checkout/session) is rate-limited at 3/min per userId to prevent double-orders"
    - "Driver location updates use Upstash limiter instead of DB-query-based rate check (removes one DB query per request)"
    - "Public endpoints (menu, sections, search, coverage) are rate-limited by IP to prevent scraping"
    - "Customer CRUD endpoints are rate-limited by userId"
    - "Webhook endpoints (Stripe, Resend) and cron endpoints are NOT rate-limited"
    - "Health endpoint reports Redis connectivity status"
    - "Old in-memory rate-limit.ts is deleted"
  artifacts:
    - path: "src/lib/supabase/actions.ts"
      provides: "Auth rate limiting via Upstash"
      contains: "checkServerActionRateLimit"
    - path: "src/app/api/checkout/session/route.ts"
      provides: "Checkout rate limiting"
      contains: "apiWriteLimiter"
    - path: "src/app/api/driver/location/route.ts"
      provides: "Driver location rate limiting via Upstash (replaces DB query)"
      contains: "driverLocationLimiter"
    - path: "src/app/api/health/route.ts"
      provides: "Redis health status in health check"
      contains: "redis.ping"
  key_links:
    - from: "src/lib/supabase/actions.ts"
      to: "src/lib/rate-limit"
      via: "checkServerActionRateLimit import"
      pattern: "from.*@/lib/rate-limit"
    - from: "src/app/api/checkout/session/route.ts"
      to: "src/lib/rate-limit"
      via: "checkRateLimit at handler top"
      pattern: "checkRateLimit"
    - from: "src/app/api/driver/location/route.ts"
      to: "src/lib/rate-limit"
      via: "driverLocationLimiter replaces DB rate check"
      pattern: "driverLocationLimiter"
---

<objective>
Apply distributed rate limiting to auth Server Actions, all public endpoints, customer endpoints, and driver endpoints. Replace the old in-memory rate limiter and DB-query-based rate check. Add Redis health reporting.

Purpose: Every non-exempt route is protected by Upstash-backed rate limiting. Webhooks and cron remain exempt.
Output: Updated route handlers with rate limit checks, deleted old rate-limit.ts, Redis-aware health endpoint.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-distributed-rate-limiting/69-RESEARCH.md
@.planning/phases/69-distributed-rate-limiting/69-01-SUMMARY.md
@src/lib/supabase/actions.ts
@src/lib/utils/rate-limit.ts
@src/app/api/driver/location/route.ts
@src/app/api/checkout/session/route.ts
@src/app/api/health/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace auth Server Action rate limiting and apply to critical write routes</name>
  <files>
    src/lib/supabase/actions.ts
    src/lib/utils/rate-limit.ts
    src/app/api/checkout/session/route.ts
    src/app/api/driver/location/route.ts
    src/app/api/driver/onboard/route.ts
    src/app/api/orders/[id]/cancel/route.ts
    src/app/api/orders/[id]/retry-payment/route.ts
    src/app/api/orders/[id]/rating/route.ts
    src/app/api/orders/[id]/notes/route.ts
    src/app/api/account/orders/[id]/reorder/route.ts
  </files>
  <action>
    **A. Update `src/lib/supabase/actions.ts`:**
    - Remove import of `checkRateLimit` from `@/lib/utils/rate-limit`
    - Import `{ checkServerActionRateLimit, getServerActionIp, authSignInLimiter, authSignUpLimiter }` from `@/lib/rate-limit`
    - In `signInWithMagicLink`:
      - Replace the old `checkRateLimit(email, "signIn")` call with:
        ```typescript
        const rateCheck = await checkServerActionRateLimit({
          limiter: authSignInLimiter,
          identifier: email.toLowerCase(),
          role: "anon",
          route: "auth/signIn",
        });
        if (rateCheck.limited) {
          return { error: `Too many login attempts. Please try again in ${rateCheck.retryAfterSeconds} seconds.` };
        }
        ```
    - In `resendDriverInvite`:
      - Replace the old `checkRateLimit(invite.email, "signIn")` call with the same pattern but route "auth/resendInvite"

    **B. Delete `src/lib/utils/rate-limit.ts`:**
    - Remove the entire file. It's the old broken in-memory implementation.
    - Verify no other imports reference it: `grep -r "rate-limit" src/ --include="*.ts" --include="*.tsx"` should only show new `@/lib/rate-limit` imports.

    **C. Update `src/app/api/checkout/session/route.ts`:**
    - Import `{ checkRateLimit, apiWriteLimiter, getIdentifier }` from `@/lib/rate-limit`
    - After the auth check (after `const user = ...`), add rate limit check:
      ```typescript
      const rl = await checkRateLimit({
        limiter: apiWriteLimiter,
        identifier: user.id,
        role: "customer",
        route: "checkout/session",
      });
      if (rl.limited) return rl.response;
      ```
    - Use the special order-specific error message for checkout: override the 429 response body to `{ error: { code: "RATE_LIMITED", message: "Your order is being processed. Please don't submit again." } }`. To do this, after the rate limit check, if limited, construct a custom NextResponse with the reassuring message instead of using the default rl.response. Example:
      ```typescript
      if (rl.limited) {
        return NextResponse.json(
          { error: { code: "RATE_LIMITED", message: "Your order is being processed. Please don't submit again." } },
          { status: 429, headers: { "Retry-After": rl.response.headers.get("Retry-After") ?? "60" } }
        );
      }
      ```
      Actually, better approach: add an optional `customMessage` param to checkRateLimit. Or simply reconstruct here. Keep it simple -- just construct the custom response inline after the check.

    **D. Update `src/app/api/driver/location/route.ts`:**
    - Import `{ checkRateLimit, driverLocationLimiter }` from `@/lib/rate-limit`
    - Remove the DB-query-based rate limit block (lines ~38-59: the `lastUpdate` query, the time comparison, the 429 return). This eliminates one DB query per location update request.
    - Add Upstash rate limit check after auth but before the route verification:
      ```typescript
      const rl = await checkRateLimit({
        limiter: driverLocationLimiter,
        identifier: driverId,
        role: "driver",
        route: "driver/location",
      });
      if (rl.limited) return rl.response;
      ```
    - Remove the `MIN_UPDATE_INTERVAL_MS` constant and `LastLocationResult` interface (no longer needed).

    **E. Update `src/app/api/driver/onboard/route.ts`:**
    - Import `{ checkRateLimit, apiWriteLimiter, getIdentifier }` from `@/lib/rate-limit`
    - Add rate limit check at handler top (after auth): use `apiWriteLimiter` with userId, role "driver", route "driver/onboard"

    **F. Update order mutation routes** (`cancel`, `retry-payment`, `rating`, `notes`, `reorder`):
    - For each POST/PATCH handler in these files:
      - Import `{ checkRateLimit, apiWriteLimiter }` from `@/lib/rate-limit`
      - After auth check, add rate limit using `apiWriteLimiter` with userId, role "customer", route matching the path
    - These routes already authenticate the user -- use `user.id` as identifier
    - Pattern for each (add after auth, before business logic):
      ```typescript
      const rl = await checkRateLimit({
        limiter: apiWriteLimiter,
        identifier: user.id,
        role: "customer",
        route: "orders/cancel", // or appropriate route name
      });
      if (rl.limited) return rl.response;
      ```
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - `grep -r "utils/rate-limit" src/` returns NO results (old import fully removed)
    - `grep -r "@/lib/rate-limit" src/` returns results for all modified files
    - `ls src/lib/utils/rate-limit.ts` fails (file deleted)
    - Checkout route contains "Your order is being processed" message
    - Driver location route no longer queries `location_updates` for rate check
  </verify>
  <done>
    Auth Server Actions and all write-path routes enforce Upstash-backed rate limits. Old in-memory rate limiter deleted. Driver location saves one DB query per request.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply rate limiting to public, customer read, and driver action routes + health check</name>
  <files>
    src/app/api/menu/route.ts
    src/app/api/menu/search/route.ts
    src/app/api/sections/route.ts
    src/app/api/coverage/check/route.ts
    src/app/api/analytics/vitals/route.ts
    src/app/api/tracking/[orderId]/route.ts
    src/app/api/addresses/route.ts
    src/app/api/addresses/[id]/route.ts
    src/app/api/account/profile/route.ts
    src/app/api/account/settings/route.ts
    src/app/api/driver/me/route.ts
    src/app/api/driver/routes/active/route.ts
    src/app/api/driver/routes/history/route.ts
    src/app/api/driver/routes/[routeId]/route.ts
    src/app/api/driver/routes/[routeId]/start/route.ts
    src/app/api/driver/routes/[routeId]/complete/route.ts
    src/app/api/driver/routes/[routeId]/stops/[stopId]/route.ts
    src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts
    src/app/api/driver/routes/[routeId]/stops/[stopId]/exception/route.ts
    src/app/api/health/route.ts
  </files>
  <action>
    **A. Public endpoints (rate limit by IP via `publicReadLimiter`):**

    For each of these files, add at the top of every exported handler (GET/POST):
    - Import `{ checkRateLimit, publicReadLimiter, getClientIp }` from `@/lib/rate-limit`
    - Extract IP: `const ip = getClientIp(request);`
    - Check: `const rl = await checkRateLimit({ limiter: publicReadLimiter, identifier: ip, role: "anon", route: "menu" });`
    - Guard: `if (rl.limited) return rl.response;`

    Routes to update:
    - `GET /api/menu/route.ts` -- route name "menu"
    - `GET /api/menu/search/route.ts` -- route name "menu/search"
    - `GET /api/sections/route.ts` -- route name "sections"
    - `POST /api/coverage/check/route.ts` -- route name "coverage/check". Use `publicReadLimiter` (it's a read-like operation with no auth).
    - `POST /api/analytics/vitals/route.ts` -- route name "analytics/vitals". This is a fire-and-forget beacon. Use `publicReadLimiter` with IP.

    **B. Customer read/CRUD endpoints (rate limit by userId via `customerLimiter`):**

    For each handler:
    - Import `{ checkRateLimit, customerLimiter }` from `@/lib/rate-limit`
    - After the auth check (where `user.id` is available), add:
      ```typescript
      const rl = await checkRateLimit({ limiter: customerLimiter, identifier: user.id, role: "customer", route: "..." });
      if (rl.limited) return rl.response;
      ```

    Routes to update:
    - `GET /api/tracking/[orderId]/route.ts` -- route name "tracking"
    - `GET/POST /api/addresses/route.ts` -- route name "addresses" (apply to each handler)
    - `GET/PATCH/DELETE /api/addresses/[id]/route.ts` -- route name "addresses/:id"
    - `GET/PATCH /api/account/profile/route.ts` -- route name "account/profile"
    - `GET/PATCH /api/account/settings/route.ts` -- route name "account/settings"

    **C. Driver action routes (already handled by Task 1 for location; this covers remaining driver routes):**

    Check which driver routes exist beyond `/location` and `/onboard`. If `src/app/api/driver/routes/` contains route handlers for start, complete, stops, etc., apply `driverActionLimiter`:
    - Import `{ checkRateLimit, driverActionLimiter }` from `@/lib/rate-limit`
    - After driver auth (where `driverId` is available), add rate limit check with role "driver"
    - Apply to all 9 driver action route files:
      - `src/app/api/driver/me/route.ts` -- route "driver/me"
      - `src/app/api/driver/routes/active/route.ts` -- route "driver/routes/active"
      - `src/app/api/driver/routes/history/route.ts` -- route "driver/routes/history"
      - `src/app/api/driver/routes/[routeId]/route.ts` -- route "driver/routes/:routeId"
      - `src/app/api/driver/routes/[routeId]/start/route.ts` -- route "driver/routes/:routeId/start"
      - `src/app/api/driver/routes/[routeId]/complete/route.ts` -- route "driver/routes/:routeId/complete"
      - `src/app/api/driver/routes/[routeId]/stops/[stopId]/route.ts` -- route "driver/routes/:routeId/stops/:stopId"
      - `src/app/api/driver/routes/[routeId]/stops/[stopId]/photo/route.ts` -- route "driver/routes/:routeId/stops/:stopId/photo"
      - `src/app/api/driver/routes/[routeId]/stops/[stopId]/exception/route.ts` -- route "driver/routes/:routeId/stops/:stopId/exception"

    **D. Update health endpoint (`src/app/api/health/route.ts`):**
    - Import `{ redis }` from `@/lib/rate-limit`
    - In the `services` object, add a `redis` entry:
      ```typescript
      redis: redis ? { status: "healthy", configured: true } : { status: "down", configured: false },
      ```
    - In deep mode, actually ping Redis:
      ```typescript
      if (deep && redis) {
        try {
          await redis.ping();
          services.redis = { status: "healthy", configured: true };
        } catch {
          services.redis = { status: "degraded", configured: true };
        }
      }
      ```
    - Update the `HealthResponse` type in `src/lib/health/index.ts` (or wherever it's defined) to include `redis?: ServiceStatus` in the services object. If the type doesn't allow extension, add `redis` to the type.
    - Add `services.redis.status` to the `allStatuses` array for worst-status computation. Use "degraded" not "down" since rate limiting fails open.

    **E. Verify exempt routes are NOT touched:**
    - `POST /api/webhooks/stripe` -- NO rate limiting (Stripe-controlled)
    - `POST /api/webhooks/resend` -- NO rate limiting (Resend-controlled)
    - `GET /api/cron/delivery-reminders` -- NO rate limiting (CRON_SECRET protected)
    - `GET /api/debug/sentry` -- NO rate limiting (dev-only)
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - `pnpm build` succeeds
    - `grep -r "checkRateLimit" src/app/api/ | wc -l` shows rate limit calls in most route files
    - `grep -r "checkRateLimit" src/app/api/webhooks/` returns NO results (webhooks exempt)
    - `grep -r "checkRateLimit" src/app/api/cron/` returns NO results (cron exempt)
    - `grep -rl "driverActionLimiter" src/app/api/driver/ | wc -l` returns >= 9 (all driver action route files)
    - Health route imports redis from rate-limit module
  </verify>
  <done>
    All non-exempt API routes enforce distributed rate limiting. Public routes use IP-based limits, authenticated routes use userId/driverId. Health endpoint reports Redis status. Webhooks and cron are confirmed exempt.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm build` all pass
- Old `src/lib/utils/rate-limit.ts` is deleted
- No imports from `@/lib/utils/rate-limit` remain anywhere in codebase
- Every non-exempt API route has a `checkRateLimit` call
- Checkout route uses special "processing" message
- Driver location route no longer does DB-based rate check
- Health endpoint includes Redis in service status
</verification>

<success_criteria>
- Auth endpoints enforce 5/min signIn, 3/hr signUp via Upstash Redis
- Checkout rate-limited at 10/min with reassuring 429 message
- Driver location uses Upstash instead of DB query (performance improvement)
- Public endpoints rate-limited by IP at 60/min
- Webhooks and cron confirmed exempt
- Old in-memory rate limiter fully removed
- Health check reports Redis connectivity
</success_criteria>

<output>
After completion, create `.planning/phases/69-distributed-rate-limiting/69-02-SUMMARY.md`
</output>
