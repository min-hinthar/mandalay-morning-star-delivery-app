---
phase: 74-guided-walkthrough-driver-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 74-01
  - 74-02
files_modified:
  - src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
  - src/components/ui/driver/DriverDashboard/RouteCard.tsx
  - src/components/ui/driver/DriverDashboard/StatCard.tsx
  - src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx
  - src/components/ui/driver/DriverDashboard/StreakDisplay.tsx
  - src/components/ui/driver/DriverDashboard/BadgesDisplay.tsx
  - src/components/ui/driver/DriverDashboard/NextRouteChip.tsx
  - src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx
  - src/components/ui/driver/StopCard.tsx
  - src/components/ui/driver/ActiveRouteView.tsx
  - src/components/ui/driver/DriverShell.tsx
  - src/app/(driver)/driver/schedule/SchedulePageClient.tsx
  - src/app/(driver)/driver/earnings/EarningsPageClient.tsx
  - src/app/(driver)/driver/history/DriverHistoryContent.tsx
  - src/app/(driver)/driver/test-delivery/page.tsx
autonomous: true
requirements:
  - DUI-03

must_haves:
  truths:
    - "Driver dashboard cards use glassmorphism shell (bg-surface-primary/80 sm:backdrop-blur-sm rounded-2xl border-2 shadow-card)"
    - "All driver list surfaces use staggerContainer + staggerItem animation"
    - "Tappable cards have hover lift effect (y: -4, scale: 1.03)"
    - "Numeric displays use AnimatedValue count-up animation"
    - "Single high-emphasis CTA has animate-shine-sweep effect"
    - "Driver pages feel visually consistent with customer-side quality"
  artifacts:
    - path: "src/components/ui/driver/DriverDashboard/DriverDashboard.tsx"
      provides: "Polished dashboard with consistent glass cards and animation"
      min_lines: 100
  key_links:
    - from: "src/components/ui/driver/DriverDashboard/RouteCard.tsx"
      to: "motion-tokens"
      via: "spring, stagger imports for card animations"
      pattern: "spring|stagger"
    - from: "src/components/ui/driver/StopCard.tsx"
      to: "motion-tokens"
      via: "hover lift effect"
      pattern: "whileHover.*y.*-4"
---

<objective>
Apply consistent animation polish, glassmorphism cards, and design tokens across all driver pages to achieve visual parity with customer-side quality.

Purpose: Driver interface currently has inconsistent visual treatment. This plan brings all cards to the established Phase 72 glass shell, adds hover lift effects, ensures stagger animation on all lists, and applies shine-sweep to the primary CTA.
Output: All driver pages polished with consistent glass cards, stagger animations, hover effects, and design token alignment.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-guided-walkthrough-driver-ui-polish/74-01-SUMMARY.md
@.planning/phases/74-guided-walkthrough-driver-ui-polish/74-02-SUMMARY.md
@src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
@src/components/ui/driver/DriverDashboard/RouteCard.tsx
@src/components/ui/driver/DriverDashboard/StatCard.tsx
@src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx
@src/components/ui/driver/DriverDashboard/StreakDisplay.tsx
@src/components/ui/driver/DriverDashboard/BadgesDisplay.tsx
@src/components/ui/driver/StopCard.tsx
@src/components/ui/driver/ActiveRouteView.tsx
@src/components/ui/driver/DriverShell.tsx
@src/lib/motion-tokens/core.ts
@src/lib/motion-tokens/stagger.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply glassmorphism shell and hover lift to dashboard cards</name>
  <files>
    src/components/ui/driver/DriverDashboard/RouteCard.tsx
    src/components/ui/driver/DriverDashboard/StatCard.tsx
    src/components/ui/driver/DriverDashboard/EarningsSummaryCard.tsx
    src/components/ui/driver/DriverDashboard/StreakDisplay.tsx
    src/components/ui/driver/DriverDashboard/BadgesDisplay.tsx
    src/components/ui/driver/DriverDashboard/NextRouteChip.tsx
    src/components/ui/driver/DriverDashboard/OnboardingWalkthroughCard.tsx
  </files>
  <action>
Read each file first, then apply consistent visual treatment:

**Glass shell standard (apply to all dashboard cards):**
```
bg-surface-primary/80 sm:backdrop-blur-sm rounded-2xl border-2 shadow-card
```

Replace any inconsistent card styling. Specifically:
- Cards using `bg-surface-primary` without opacity → add `/80`
- Cards using `rounded-xl` → upgrade to `rounded-2xl`
- Cards using `border border-border` → upgrade to `border-2`
- Cards using `shadow-sm` → upgrade to `shadow-card`
- Cards missing `sm:backdrop-blur-sm` → add it (MUST be `sm:` prefixed for mobile safety)

**Do NOT add backdrop-blur without the `sm:` prefix** — mobile Safari crashes. The existing comment in DriverHeader and DriverNav documents this.

**Hover lift effect (apply to tappable cards):**
- RouteCard, StatCard, BadgesDisplay items, NextRouteChip
- Add `whileHover={isFullMotion ? { y: -4, scale: 1.03 } : undefined}` and `whileTap={isFullMotion ? { scale: 0.98 } : undefined}`
- Requires `useAnimationPreference()` hook — add if not already imported
- Non-interactive cards (StreakDisplay, EarningsSummaryCard) do NOT get hover lift

**Card header gradient (apply where appropriate):**
- `bg-gradient-to-r from-accent-teal/10 to-secondary/10` for card header sections
- Apply to RouteCard header area, StreakDisplay header

**Ensure each file imports `useAnimationPreference` if using `isFullMotion` guard.**
**Ensure `m` is imported from framer-motion if adding whileHover/whileTap.**

**OnboardingWalkthroughCard:** Verify it already uses the glass shell from Plan 01. If not, apply the standard. Add hover lift to milestone items that are tappable (incomplete milestones).
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- All dashboard cards visually consistent with glass shell
- Hover effect only on tappable cards (not on static displays)
- No `backdrop-blur` without `sm:` prefix
  </verify>
  <done>
- All dashboard cards use glass shell: bg-surface-primary/80 sm:backdrop-blur-sm rounded-2xl border-2 shadow-card
- Tappable cards have hover: { y: -4, scale: 1.03 } lift effect
- Card headers use accent gradient where appropriate
- No mobile Safari blur crashes (all blur is sm: prefixed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply stagger animations, AnimatedValue, and shine-sweep across driver pages</name>
  <files>
    src/components/ui/driver/DriverDashboard/DriverDashboard.tsx
    src/components/ui/driver/StopCard.tsx
    src/components/ui/driver/ActiveRouteView.tsx
    src/components/ui/driver/DriverShell.tsx
    src/app/(driver)/driver/schedule/SchedulePageClient.tsx
    src/app/(driver)/driver/earnings/EarningsPageClient.tsx
    src/app/(driver)/driver/history/DriverHistoryContent.tsx
    src/app/(driver)/driver/test-delivery/page.tsx
  </files>
  <action>
Read each file first, then apply consistent animation patterns:

**Stagger animation on list surfaces:**
- Ensure ALL list containers use `staggerContainer` + `staggerItem` variants
- Files to check: StopCard parent (StopList), dashboard card layout, earnings route list, history route list, schedule day groups
- Pattern:
  ```tsx
  <m.div variants={staggerContainer(0.04, 0.08)} initial="hidden" animate="visible">
    <m.div variants={staggerItem}>...</m.div>
  </m.div>
  ```
- If stagger is already applied, leave it. Only add where missing.

**AnimatedValue for numeric displays:**
- Ensure all visible numeric values use `<AnimatedValue>` component
- Already done in: dashboard stats (StatCard), earnings (EarningsSummaryCard)
- Check and add to: SchedulePageClient route counts, DriverHistoryContent stat cards (verify they use it)
- Import from `@/components/ui/admin/AdminDashboard/AnimatedValue`

**animate-shine-sweep on single high-emphasis CTA:**
- Identify the single most prominent CTA on the driver dashboard: the "Start Route" button in RouteCard
- Add `animate-shine-sweep` class to that button ONLY
- This class is already defined in globals.css — do NOT create new CSS
- Apply ONLY to the Start Route button. Do NOT apply to any other element.
- Guard with `isFullMotion` — only show shine in full motion mode

**shadow-glow-primary for active states:**
- ActiveRouteView: add `shadow-glow-primary` to in-progress route container
- Only when route is `in_progress` status, not planned/completed
- Use conditional class: `routeStatus === "in_progress" && "shadow-glow-primary"`

**Token alignment verification (check and fix):**
- No `shadow-colorful` usage on driver pages (use `shadow-card` instead)
- No `glass-menu-card` class on driver pages (use inline glass shell)
- No GSAP imports on driver pages
- No `whileInView` usage on driver pages (use `initial/animate` mount-triggered)

**Animation preference guards:**
- Every new motion element must use `shouldAnimate` conditional
- Shine-sweep and glow effects gated by `isFullMotion`

**DriverShell.tsx:** Read and verify it provides consistent page wrapper styling. If it wraps content in a container, ensure the container has appropriate background gradient matching dashboard.
  </action>
  <verify>
- `pnpm typecheck` passes
- `pnpm lint` passes
- Stagger animation visible on all list surfaces
- AnimatedValue count-up on all numeric displays
- Shine-sweep ONLY on Start Route button
- Glow effect ONLY on in-progress route
- No prohibited patterns (shadow-colorful, glass-menu-card, GSAP, whileInView) on driver pages
  </verify>
  <done>
- All list surfaces animate with staggerContainer/staggerItem
- All numeric displays use AnimatedValue
- Single shine-sweep on Start Route CTA
- Active route glow effect on in-progress status
- Zero prohibited animation patterns on driver pages
- Visual parity with customer-side achieved
  </done>
</task>

</tasks>

<verification>
- `pnpm lint && pnpm lint:css && pnpm format:check && pnpm typecheck && pnpm test && pnpm build` all pass
- Driver dashboard cards are visually consistent with glass shell
- Stagger animations play on mount for all list surfaces
- Hover lift on all tappable cards
- Shine-sweep on Start Route button only
- Reduced motion mode disables all animations
- No mobile Safari backdrop-blur crashes
</verification>

<success_criteria>
- All driver cards use consistent glass shell styling
- Animation patterns (stagger, hover lift, AnimatedValue, shine-sweep) applied consistently
- No prohibited patterns on driver pages
- Full verification pipeline passes
</success_criteria>

<output>
After completion, create `.planning/phases/74-guided-walkthrough-driver-ui-polish/74-03-SUMMARY.md`
</output>
