---
phase: 70-role-based-auth-redirects
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/driver/deactivated/page.tsx
  - src/app/(public)/driver/onboard/page.tsx
  - src/components/ui/driver/OnboardingForm.tsx
  - src/app/api/driver/onboard/route.ts
  - src/components/ui/driver/UpgradeConfirmation.tsx
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "Deactivated drivers see a specific 'Account Deactivated' page with admin contact info from app_settings"
    - "Onboarding form has no password or confirm password fields"
    - "Onboarding form shows invite metadata (admin name, invite date, expiry, email)"
    - "Onboard API does not require or process a password field"
    - "Customer-to-driver upgrade shows confirmation dialog before role change"
    - "After successful onboarding, user is redirected to /driver"
    - "Expired invites show 'invitation expired' message"
  artifacts:
    - path: "src/app/(public)/driver/deactivated/page.tsx"
      provides: "Deactivated driver page with admin contact info"
      contains: "Account Deactivated"
    - path: "src/components/ui/driver/UpgradeConfirmation.tsx"
      provides: "Customer-to-driver upgrade confirmation dialog"
      contains: "UpgradeConfirmation"
    - path: "src/components/ui/driver/OnboardingForm.tsx"
      provides: "Password-free onboarding form with invite details"
    - path: "src/app/api/driver/onboard/route.ts"
      provides: "Password-free onboard API"
  key_links:
    - from: "src/app/(public)/driver/deactivated/page.tsx"
      to: "app_settings table"
      via: "query admin_contact_info"
      pattern: "admin_contact_info"
    - from: "src/app/(public)/driver/onboard/page.tsx"
      to: "src/components/ui/driver/UpgradeConfirmation.tsx"
      via: "renders UpgradeConfirmation when user has customer role"
      pattern: "UpgradeConfirmation"
    - from: "src/components/ui/driver/OnboardingForm.tsx"
      to: "src/app/api/driver/onboard/route.ts"
      via: "POST /api/driver/onboard"
      pattern: "fetch.*api/driver/onboard"
---

<objective>
Create the deactivated driver page, remove password from driver onboarding (both form and API), add invite metadata display, and build the customer-to-driver upgrade confirmation dialog.

Purpose: Complete the driver onboarding lifecycle — deactivated drivers see a helpful page, new drivers onboard without passwords (magic link auth only), and customer-to-driver upgrades require explicit confirmation.

Output: Deactivated page, updated onboard page/form/API, upgrade confirmation component.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-role-based-auth-redirects/70-RESEARCH.md
@src/app/(public)/driver/onboard/page.tsx
@src/components/ui/driver/OnboardingForm.tsx
@src/app/api/driver/onboard/route.ts
@src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deactivated driver page and update onboard page with invite details + upgrade confirmation</name>
  <files>
    src/app/(public)/driver/deactivated/page.tsx
    src/app/(public)/driver/onboard/page.tsx
    src/components/ui/driver/UpgradeConfirmation.tsx
  </files>
  <action>
**Create `src/app/(public)/driver/deactivated/page.tsx`:**
- Server component. Check auth with `createClient().auth.getUser()`. If not authenticated, redirect to `/login`.
- Use `createServiceClient()` to fetch `admin_contact_info` from `app_settings` (key = `admin_contact_info`). Parse value as `{ email?: string; phone?: string }`.
- Render centered card using existing `Card`, `CardContent` from `@/components/ui/card` and `AlertCircle` from `lucide-react`:
  - Heading: "Account Deactivated"
  - Message: "Your driver account has been deactivated. If you believe this is an error, please contact the admin."
  - If email exists: show clickable mailto link
  - If phone exists: show clickable tel link
  - Bottom link: "Go to home page" linking to `/`
- Style consistent with existing onboard page patterns (min-h-screen bg-background, flex center, max-w-md)
- Use `Card variant="alert" alertAccent="error"` for the error card styling (matches existing pattern)

**Create `src/components/ui/driver/UpgradeConfirmation.tsx`:**
- Client component (`'use client'`)
- Props: `onConfirm: () => void`, `onCancel: () => void`
- Render a centered dialog-style card explaining: "You're currently a customer. Accepting this driver invitation will change your account to a driver account. You can still access the menu from your driver dashboard."
- Two buttons: "Cancel" (outline variant, calls onCancel) and "Accept & Continue" (primary variant, calls onConfirm)
- Use existing `Button` component from `@/components/ui/button`

**Update `src/app/(public)/driver/onboard/page.tsx`:**
- Add invite metadata display ABOVE the form. After fetching the invite, also fetch the admin name who created it:
  ```typescript
  // Fetch invite with metadata
  const { data: inviteWithMeta } = await serviceSupabase
    .from("driver_invites")
    .select("id, email, accepted_at, created_at, expires_at, invited_by")
    .eq("id", inviteId)
    .single();
  ```
  Then fetch the admin's name: `profiles.full_name` where `id = inviteWithMeta.invited_by`
- Check if invite is expired: compare `expires_at` with current time. If expired, show an error card: "Your invitation has expired. Please contact your administrator for a new invitation." with the admin contact info. Do NOT show the form.
- Check if user's current role is `customer` (query profiles table). If customer role detected while an invite exists, show the `UpgradeConfirmation` component BEFORE the onboarding form. The onboard page becomes a client-server hybrid:
  - Server side: fetch all data (invite, profile role, admin name)
  - Pass `isUpgrade={profile?.role === 'customer'}` and invite metadata as props to a wrapper client component
  - The wrapper shows UpgradeConfirmation first if `isUpgrade`, and on confirm reveals the OnboardingForm
- Pass invite metadata to OnboardingForm as new props: `invitedBy` (admin name), `inviteDate` (created_at), `expiryDate` (expires_at), `inviteEmail` (email)
- Keep existing "No Invitation Found" state unchanged (locked decision)
  </action>
  <verify>
    - `pnpm typecheck` passes
    - File exists at `src/app/(public)/driver/deactivated/page.tsx`
    - File exists at `src/components/ui/driver/UpgradeConfirmation.tsx`
    - Deactivated page queries `admin_contact_info` from app_settings
    - Onboard page checks for expired invites
  </verify>
  <done>
    - Deactivated driver page shows "Account Deactivated" with admin contact info from app_settings
    - Expired invites blocked with "invitation expired" message
    - Customer-to-driver upgrade shows confirmation dialog before proceeding
    - Invite metadata (admin name, date, expiry, email) displayed on onboard page
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove password from onboarding form and API</name>
  <files>
    src/components/ui/driver/OnboardingForm.tsx
    src/app/api/driver/onboard/route.ts
  </files>
  <action>
**Update `src/components/ui/driver/OnboardingForm.tsx`:**
- Remove `password` and `confirmPassword` from the Zod schema (`onboardingSchema`). Remove the `.refine()` for password matching.
- Remove `password` and `confirmPassword` from formData state
- Remove the password and confirm password input fields from the JSX (lines ~238-270)
- Remove `password` from the fetch body in handleSubmit
- Add invite metadata display above the form fields. Accept new props:
  ```typescript
  interface OnboardingFormProps {
    email: string;
    inviteId: string;
    invitedBy?: string;    // Admin name
    inviteDate?: string;   // ISO date string
    expiryDate?: string;   // ISO date string
    inviteEmail?: string;  // Invite email
  }
  ```
- Render invite metadata as a read-only info section above the form fields:
  ```
  Invited by: {invitedBy}
  Invited: {formatted inviteDate}
  Expires: {formatted expiryDate}
  Email: {inviteEmail}
  ```
  Use subdued styling (text-sm text-text-secondary, bg-surface-secondary rounded-lg p-3)

**Update `src/app/api/driver/onboard/route.ts`:**
- Remove `password` from `onboardSchema` (the Zod validation schema)
- Remove `password` from the destructured `result.data`
- Remove the entire "Step 1: Update user password" block (lines ~147-158 including the `supabase.auth.updateUser({ password })` call and error handling)
- Renumber remaining steps: "Step 2" becomes "Step 1", "Step 3" becomes "Step 2", "Step 4" becomes "Step 3"
- Everything else stays the same — profile upsert, driver record creation, invite acceptance
  </action>
  <verify>
    - `pnpm typecheck` passes
    - `pnpm lint` passes
    - Grep for `password` in OnboardingForm.tsx — should NOT find `password` in schema or form fields (may find in unrelated places like `type="password"` which should be removed)
    - Grep for `password` in `api/driver/onboard/route.ts` — should NOT be found
    - Grep for `updateUser.*password` in `api/driver/onboard/route.ts` — should NOT be found
  </verify>
  <done>
    - Onboarding form collects only: fullName, phone, vehicleType, licensePlate (no password fields)
    - Onboard API validates only: inviteId, fullName, phone, vehicleType, licensePlate
    - No password-related operations in the onboard flow
    - Invite metadata (admin name, invite date, expiry, email) displayed in form
    - Form and API schemas are in sync
  </done>
</task>

</tasks>

<verification>
```bash
pnpm typecheck && pnpm lint && pnpm build
```

Manual checks:
- No `password` field in onboard Zod schemas (API or form)
- No `updateUser({ password })` call in onboard API
- Deactivated page renders admin contact info
- Upgrade confirmation component exists with confirm/cancel buttons
</verification>

<success_criteria>
- Deactivated driver page exists at /driver/deactivated with admin contact info
- Onboarding is fully passwordless (no password in form or API)
- Invite metadata visible on onboard page
- Expired invites blocked with appropriate message
- Customer-to-driver upgrade requires explicit confirmation
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/70-role-based-auth-redirects/70-02-SUMMARY.md`
</output>
