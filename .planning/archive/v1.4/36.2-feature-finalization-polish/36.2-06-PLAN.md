---
phase: 36.2-feature-finalization-polish
plan: 06
type: execute
wave: 4
depends_on: ["36.2-01"]
files_modified:
  - src/components/ui/admin/orders/OrderDetailExpanded.tsx
  - src/components/ui/admin/OrdersTable.tsx
  - src/app/api/admin/orders/[id]/items/route.ts
  - src/app/api/admin/orders/[id]/driver/route.ts
autonomous: true

must_haves:
  truths:
    - "Order row can expand to show full details inline"
    - "Expanded view shows customer info, items, driver, status history"
    - "Admin can change status from expanded view"
    - "Admin can assign driver from expanded view"
    - "Admin can edit items from expanded view"
  artifacts:
    - path: "src/components/ui/admin/orders/OrderDetailExpanded.tsx"
      provides: "Expanded order detail with full CRUD actions"
      exports: ["OrderDetailExpanded"]
    - path: "src/app/api/admin/orders/[id]/items/route.ts"
      provides: "PATCH endpoint to edit order items"
      exports: ["PATCH"]
    - path: "src/app/api/admin/orders/[id]/driver/route.ts"
      provides: "PATCH endpoint to assign driver to order"
      exports: ["PATCH"]
  key_links:
    - from: "src/components/ui/admin/orders/OrderDetailExpanded.tsx"
      to: "/api/admin/orders/[id]/status"
      via: "status change fetch"
      pattern: "fetch.*status"
    - from: "src/components/ui/admin/orders/OrderDetailExpanded.tsx"
      to: "/api/admin/orders/[id]/driver"
      via: "driver assign fetch"
      pattern: "fetch.*driver"
---

<objective>
Create expanded order detail view with full CRUD actions in orders table.

Purpose: Allow admins to view full order details and take actions without leaving the orders page.
Output: OrderDetailExpanded component with status, driver assignment, and item editing.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-feature-finalization-polish/36.2-CONTEXT.md
@src/components/ui/admin/OrdersTable.tsx
@src/app/api/admin/orders/[id]/cancel/route.ts
@src/app/api/admin/orders/[id]/refund/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API endpoints for item editing and driver assignment</name>
  <files>src/app/api/admin/orders/[id]/items/route.ts, src/app/api/admin/orders/[id]/driver/route.ts</files>
  <action>
    **Items endpoint** at `/api/admin/orders/[id]/items`:
    - PATCH method
    - Body: `{ items: Array<{ id: string; quantity: number }>, reason: string }`
    - Validate admin auth
    - Update order_items quantities (or remove if quantity = 0)
    - Recalculate order total
    - Log to order_audit_log with reason
    - Notify customer (if notification service available)
    - Return updated order with new total

    **Driver assignment endpoint** at `/api/admin/orders/[id]/driver`:
    - PATCH method
    - Body: `{ driverId: string | null }` (null to unassign)
    - Validate admin auth
    - Validate driver exists and is active
    - Update order assigned_driver_id
    - Log to order_audit_log
    - Return updated order

    **Error handling:**
    - 400: Invalid input
    - 401: Not authenticated
    - 403: Not admin
    - 404: Order or driver not found
    - 409: Order in invalid state for edit

  </action>
  <verify>Both endpoints compile, follow existing API patterns</verify>
  <done>
    - Items endpoint updates quantities and recalculates total
    - Driver endpoint assigns/unassigns driver
    - Both log to audit trail
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OrderDetailExpanded component</name>
  <files>src/components/ui/admin/orders/OrderDetailExpanded.tsx</files>
  <action>
    Create expandable detail component:

    **Sections:**
    1. **Customer Info**
       - Name, email, phone
       - Call/Text buttons (tel: and sms: links)
       - Delivery address

    2. **Order Items**
       - List with name, quantity, price
       - Edit button to modify quantities
       - Total auto-updates when items change

    3. **Status & Actions**
       - Current status badge
       - Status action buttons: "Mark Preparing", "Mark Ready", etc.
       - Based on current status, show next logical action

    4. **Driver Assignment**
       - Current driver (if assigned) with badge
       - Assign dropdown with available drivers
       - Smart suggestion: "Recommended: [Driver] - Closest to area"

    5. **Cancel/Refund**
       - Cancel button (for pending/confirmed orders)
       - Refund button with modal for amount/items
       - Both require reason

    6. **Audit Log**
       - Timeline of order changes
       - Show actor, action, timestamp
       - Fetch from order_audit_log

    **Props:**
    ```typescript
    interface OrderDetailExpandedProps {
      orderId: string;
      onUpdate: () => void; // Refresh parent
    }
    ```

    **State:**
    - Fetch full order details on mount
    - Loading state while fetching
    - Optimistic updates for status changes

  </action>
  <verify>Component renders all sections, actions work</verify>
  <done>
    - Shows complete order information
    - Status change, driver assignment, item edit all functional
    - Cancel/refund actions available
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate expanded view into OrdersTable</name>
  <files>src/components/ui/admin/OrdersTable.tsx</files>
  <action>
    **Add expansion support:**
    - Track expandedOrderId state
    - Add "View Details" button or click on row to expand
    - When expanded, render OrderDetailExpanded below the row
    - Smooth animation for expand/collapse (AnimatePresence)

    **Row changes:**
    - Add expand/collapse chevron icon
    - Highlight expanded row
    - Keep existing status dropdown for quick changes

    **Collapse behavior:**
    - Clicking different row expands that row, collapses previous
    - Clicking same row collapses it
    - ESC key collapses current

    **Refresh:**
    - When OrderDetailExpanded calls onUpdate, refresh orders list
    - Optimistic updates where possible

  </action>
  <verify>Table rows expand to show OrderDetailExpanded</verify>
  <done>
    - Click row to expand/collapse
    - Expanded view shows full order details with actions
    - Updates reflect in parent table
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. Orders table allows row expansion
4. Expanded view shows all order details
5. Status, driver, and item actions work correctly
</verification>

<success_criteria>

- Admin can expand order row to see full details
- Customer contact info with call/text buttons visible
- Status changes work from expanded view
- Driver assignment works with smart suggestions
- Item editing updates total automatically
- Cancel/refund actions available with reason requirement
- Audit log shows complete history
  </success_criteria>

<output>
After completion, create `.planning/phases/36.2-feature-finalization-polish/36.2-06-SUMMARY.md`
</output>
