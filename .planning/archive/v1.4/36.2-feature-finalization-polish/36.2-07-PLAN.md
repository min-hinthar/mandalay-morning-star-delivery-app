---
phase: 36.2-feature-finalization-polish
plan: 07
type: execute
wave: 5
depends_on: ["36.2-01"]
files_modified:
  - src/components/ui/admin/settings/DeliverySettingsForm.tsx
  - src/components/ui/admin/settings/OperationsSettingsForm.tsx
  - src/components/ui/admin/settings/NotificationSettingsForm.tsx
  - src/components/ui/admin/settings/SettingsClient.tsx
  - src/components/ui/DiscardChangesModal.tsx
autonomous: true

must_haves:
  truths:
    - "All settings forms show inline validation errors"
    - "Save button shows spinner while saving"
    - "Success shows checkmark on button and toast"
    - "Navigating away with unsaved changes shows discard modal"
    - "Skeleton loaders shown while data loads"
  artifacts:
    - path: "src/components/ui/DiscardChangesModal.tsx"
      provides: "Reusable discard changes confirmation modal"
      exports: ["DiscardChangesModal"]
  key_links:
    - from: "src/components/ui/admin/settings/SettingsClient.tsx"
      to: "src/components/ui/DiscardChangesModal.tsx"
      via: "modal render on navigation"
      pattern: "DiscardChangesModal"
---

<objective>
Polish admin settings forms with inline validation, loading states, and discard changes modal.

Purpose: Better UX for settings management with clear feedback and safety for unsaved changes.
Output: Polished settings forms and reusable DiscardChangesModal component.
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-feature-finalization-polish/36.2-CONTEXT.md
@src/components/ui/admin/settings/SettingsClient.tsx
@src/components/ui/admin/settings/DeliverySettingsForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DiscardChangesModal component</name>
  <files>src/components/ui/DiscardChangesModal.tsx</files>
  <action>
    Create reusable modal for unsaved changes warning:

    **Props:**
    ```typescript
    interface DiscardChangesModalProps {
      open: boolean;
      onDiscard: () => void;
      onSave: () => void;
      onCancel: () => void;
      isSaving?: boolean;
    }
    ```

    **Content:**
    - Title: "Discard changes?"
    - Description: "You have unsaved changes. Would you like to save them before leaving?"
    - Three buttons:
      1. Cancel (outline) - closes modal, stays on page
      2. Discard (destructive/outline) - calls onDiscard
      3. Save Changes (primary) - calls onSave, shows spinner if isSaving

    **Styling:**
    - Use AlertDialog from shadcn/ui pattern
    - Match existing modal styling
    - Proper focus trap and accessibility

  </action>
  <verify>Modal compiles and renders correctly</verify>
  <done>Reusable discard changes modal with save/discard/cancel options</done>
</task>

<task type="auto">
  <name>Task 2: Add inline validation to settings forms</name>
  <files>src/components/ui/admin/settings/DeliverySettingsForm.tsx, src/components/ui/admin/settings/OperationsSettingsForm.tsx, src/components/ui/admin/settings/NotificationSettingsForm.tsx</files>
  <action>
    For each settings form, add validation:

    **Delivery form validation:**
    - deliveryRadiusMiles: min 1, max 100
    - minimumOrderCents: min 0 (no minimum), max 10000
    - freeDeliveryThresholdCents: min 0
    - baseDeliveryFeeCents: min 0
    - deliveryCutoffTime: valid HH:MM format

    **Operations form validation:**
    - maxStopsPerRoute: min 1, max 50
    - defaultVehicleType: must be valid option

    **Notifications form:**
    - All boolean, no validation needed beyond type

    **Error display:**
    - Show inline error text below invalid fields (red text, small size)
    - Use existing text-status-error token
    - Only validate on form submit (per CONTEXT.md)
    - Clear errors when field is modified

    **Pattern:**
    - Use React Hook Form with Zod schema
    - Or use controlled inputs with manual validation on submit
    - Match existing form patterns in codebase

  </action>
  <verify>Forms show inline errors for invalid input</verify>
  <done>
    - All settings forms validate on submit
    - Inline errors shown below invalid fields
    - Errors clear when field modified
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance SettingsClient with loading states and discard modal</name>
  <files>src/components/ui/admin/settings/SettingsClient.tsx</files>
  <action>
    **Loading state improvements:**
    - Replace simple Loader2 with skeleton UI
    - Show skeletons for each form field
    - Match form layout so content doesn't jump

    **Save button enhancements:**
    - Show spinner while saving (already exists)
    - After success: briefly show checkmark icon (1-2 seconds)
    - Then return to normal Save icon
    - Toast already shows on success (keep that)

    **Discard changes modal:**
    - Replace browser confirm() with DiscardChangesModal
    - Show modal when:
      1. User clicks browser back button (beforeunload already handles)
      2. User clicks tab that would lose changes (add check)
      3. User navigates via sidebar/header links (use router intercept)
    - For in-app navigation: intercept with Next.js router events
    - Modal offers Save, Discard, or Cancel

    **Implementation:**
    - Add showDiscardModal state
    - Add pendingNavigation state (where to go if discarded)
    - Intercept navigation in useEffect
    - On Save from modal: save, then navigate
    - On Discard: navigate without saving

  </action>
  <verify>Skeleton shows during load, discard modal appears on unsaved navigation</verify>
  <done>
    - Skeleton UI during initial load
    - Checkmark flash on successful save
    - Discard modal for unsaved changes navigation
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. Settings page shows skeleton during load
4. Forms show inline errors on invalid submit
5. Discard modal appears when navigating with unsaved changes
</verification>

<success_criteria>

- All settings forms have inline validation errors
- Save button shows spinner, then checkmark on success
- Skeleton UI during initial data load
- Discard changes modal replaces browser confirm
- Modal offers Save, Discard, or Cancel options
  </success_criteria>

<output>
After completion, create `.planning/phases/36.2-feature-finalization-polish/36.2-07-SUMMARY.md`
</output>
