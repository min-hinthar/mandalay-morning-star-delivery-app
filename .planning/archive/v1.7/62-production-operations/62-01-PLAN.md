---
phase: 62-production-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "GET /sitemap.xml returns valid XML with public routes only"
    - "GET /robots.txt disallows auth-gated routes and references sitemap"
    - "Root layout metadata includes google-site-verification meta tag from env var"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Programmatic sitemap with public routes"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Programmatic robots.txt with disallow rules"
      exports: ["default"]
    - path: "src/app/layout.tsx"
      provides: "Google Search Console verification metadata"
      contains: "verification"
  key_links:
    - from: "src/app/layout.tsx"
      to: "GOOGLE_SITE_VERIFICATION env var"
      via: "process.env.GOOGLE_SITE_VERIFICATION"
      pattern: "verification.*google"
    - from: "src/app/robots.ts"
      to: "src/app/sitemap.ts"
      via: "sitemap URL reference"
      pattern: "sitemap.*mandalaymorningstar"
---

<objective>
Create SEO foundation: programmatic sitemap, robots.txt, and Google Search Console verification metadata.

Purpose: Enable search engine indexing of public pages while blocking auth-gated routes. Prepare for Search Console domain verification.
Output: sitemap.ts, robots.ts, layout.tsx verification metadata
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap.ts and robots.ts</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <action>
Create `src/app/sitemap.ts`:
- Export default function returning `MetadataRoute.Sitemap` array
- Import `MetadataRoute` from `next`
- Base URL: `https://mandalaymorningstar.com`
- Include ONLY public routes:
  - `/` (priority 1, changeFrequency weekly)
  - `/menu` (priority 0.9, changeFrequency weekly)
  - `/login` (priority 0.3, changeFrequency monthly)
  - `/privacy` (priority 0.2, changeFrequency yearly)
  - `/terms` (priority 0.2, changeFrequency yearly)
- All entries use `lastModified: new Date()`
- Do NOT include any auth-gated routes (/admin, /driver, /cart, /checkout, /account, /orders, /api, /auth)

Create `src/app/robots.ts`:

- Export default function returning `MetadataRoute.Robots`
- Import `MetadataRoute` from `next`
- Single rule for userAgent `*`:
  - `allow: '/'`
  - `disallow`: array of `/admin/`, `/driver/`, `/api/`, `/auth/`, `/cart`, `/checkout`, `/account`, `/orders`, `/debug/`
- `sitemap: 'https://mandalaymorningstar.com/sitemap.xml'`
  </action>
  <verify>
- `pnpm typecheck` passes (no TS errors)
- `pnpm build` succeeds
- Both files are valid TypeScript with correct MetadataRoute types
  </verify>
  <done>sitemap.ts and robots.ts exist with correct public routes and disallow rules</done>
  </task>

<task type="auto">
  <name>Task 2: Add Google Search Console verification metadata</name>
  <files>src/app/layout.tsx</files>
  <action>
In `src/app/layout.tsx`, add `verification` property to the existing `metadata` export:

```typescript
export const metadata: Metadata = {
  // ...existing metadata fields...
  verification: {
    google: process.env.GOOGLE_SITE_VERIFICATION,
  },
};
```

Place the `verification` field after the existing `icons` field in the metadata object.

This uses an env var so the verification code can differ between environments. The env var will be set in Vercel production env vars during Plan 04 (after obtaining the code from Google Search Console). When the env var is undefined, Next.js simply omits the meta tag -- no build error.

Do NOT hardcode the verification code.
</action>
<verify>

- `pnpm typecheck` passes
- `pnpm build` succeeds
- Confirm in build output that metadata compiles without error
  </verify>
  <done>layout.tsx metadata includes verification.google referencing GOOGLE_SITE_VERIFICATION env var</done>
  </task>

</tasks>

<verification>
- `pnpm lint && pnpm typecheck && pnpm build` passes
- `src/app/sitemap.ts` exists and exports default function
- `src/app/robots.ts` exists and exports default function
- `src/app/layout.tsx` metadata includes `verification.google`
</verification>

<success_criteria>

- sitemap.ts lists exactly 5 public routes with correct priorities
- robots.txt disallows all auth-gated route prefixes
- Search Console verification metadata reads from env var (not hardcoded)
  </success_criteria>

<output>
After completion, create `.planning/phases/62-production-operations/62-01-SUMMARY.md`
</output>
