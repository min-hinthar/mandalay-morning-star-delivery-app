"use client";

/**
 * Admin Settings Page
 * Centralized settings management for delivery, operations, and notifications
 */

import { SettingsClient } from "@/components/ui/admin/settings/SettingsClient";

export default function AdminSettingsPage() {
  return <SettingsClient />;
}
