"use client";

import { DriverDetailClient } from "@/components/ui/admin/drivers/DriverDetailClient";

/**
 * Admin Driver Detail Page
 *
 * Activity-focused layout showing driver profile, recent routes,
 * ratings, and management actions.
 */
export default function AdminDriverDetailPage() {
  return <DriverDetailClient />;
}
